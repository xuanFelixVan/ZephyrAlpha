---
task_id: "OPS-2026062621"
title: "阶段0备份+阶段1 DB 6域ID改名（4323行，最高风险）"
priority: "P0"
status: "pending"
classification: "db_modification"
safety_level: "H"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: ""
---

# OPS-2026062621：阶段0备份+阶段1 DB 6域ID改名

> **对应方案**：[domain_id_hyphen_rename_plan.md](../../domain_id_hyphen_rename_plan.md) 第四部分 4.1~4.2（动作 0.1 + 1.1~1.8）
> **风险等级**：🔴 最高（直接修改 depgraph.db，4323 行）
> **不可跳过**：DB 改名是后续所有阶段的前提，必须先完成且验收通过

## 目标

根因：6 个域 ID 使用连字符（D-GOV-DOCS / D-GOV-ENFORCEMENT / D-GOV-SCRIPTS / D-GOV_AUDIT_TESTS / D-INTEGRATION-GATEWAY / D-SECURITY-LLM）违反 NR-002 命名规则（^D-[A-Z][A-Z0-9_]*$）。治根：用 `apply_depgraph.py --rename-domain` 逐域改名，17 步 UPDATE + B1 兜底扫描覆盖 11 表所有 TEXT 列。

**改名映射**：

| 原域 ID | 新域 ID | 预期行数 |
|---|---|---|
| D-GOV-DOCS | D-GOV_DOCS | 756 |
| D-GOV-ENFORCEMENT | D-GOV_ENFORCEMENT | 436 |
| D-GOV-SCRIPTS | D-GOV_SCRIPTS | 2517 |
| D-GOV_AUDIT_TESTS | D-AUDITTEST | 156 |
| D-INTEGRATION-GATEWAY | D-INTEGRATION_GATEWAY | 252 |
| D-SECURITY-LLM | D-SECURITY_LLM | 206 |
| **合计** | | **4323** |

## 施工步骤

### 动作 0.1：Git 备份当前 depgraph.db

```bash
python scripts/git_commit.py --session rename-hyphen-backup \
  --files data/databases/depgraph.db \
  --message "backup: pre-rename-hyphen depgraph.db snapshot [GW:rename-hyphen]"
```

**验证**：`git log --oneline -1` 确认 commit 存在，记录此 commit hash 为 `<BACKUP>`（回滚时使用，见回滚章节）。

### 动作 1.1：D-GOV-DOCS → D-GOV_DOCS

```bash
# 步骤 A：dry-run 预览
python scripts/governance/apply_depgraph.py --rename-domain D-GOV-DOCS D-GOV_DOCS --dry-run
# 预期 stderr：[DRY RUN] cmd_rename_domain(D-GOV-DOCS -> D-GOV_DOCS): total 756 rows

# 步骤 B：执行改名
python scripts/governance/apply_depgraph.py --rename-domain D-GOV-DOCS D-GOV_DOCS

# 步骤 C：验证 0 残留
python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');print(c.execute(\"SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%D-GOV-DOCS%'\").fetchone()[0])"
# 预期输出：0
```

### 动作 1.2：D-GOV-ENFORCEMENT → D-GOV_ENFORCEMENT

```bash
python scripts/governance/apply_depgraph.py --rename-domain D-GOV-ENFORCEMENT D-GOV_ENFORCEMENT --dry-run
# 预期：total 436 rows
python scripts/governance/apply_depgraph.py --rename-domain D-GOV-ENFORCEMENT D-GOV_ENFORCEMENT
python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');print(c.execute(\"SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%D-GOV-ENFORCEMENT%'\").fetchone()[0])"
# 预期输出：0
```

### 动作 1.3：D-GOV-SCRIPTS → D-GOV_SCRIPTS

```bash
# 注意：此域含前缀匹配（D-GOV-SCRIPTS-META 在 belongs_to 中），B1 兜底会处理
python scripts/governance/apply_depgraph.py --rename-domain D-GOV-SCRIPTS D-GOV_SCRIPTS --dry-run
# 预期：total 2517 rows（含 D-GOV-SCRIPTS-META → D-GOV_SCRIPTS-META 的 REPLACE）
python scripts/governance/apply_depgraph.py --rename-domain D-GOV-SCRIPTS D-GOV_SCRIPTS
python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');print(c.execute(\"SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%D-GOV-SCRIPTS%'\").fetchone()[0])"
# 预期输出：0
```

### 动作 1.4：D-GOV_AUDIT_TESTS → D-AUDITTEST

```bash
python scripts/governance/apply_depgraph.py --rename-domain D-GOV_AUDIT_TESTS D-AUDITTEST --dry-run
# 预期：total 156 rows
python scripts/governance/apply_depgraph.py --rename-domain D-GOV_AUDIT_TESTS D-AUDITTEST
python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');print(c.execute(\"SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%D-GOV_AUDIT_TESTS%'\").fetchone()[0])"
# 预期输出：0
# 注意：D-GOV_AUDIT 域不受影响（D-GOV_AUDIT_TESTS 不是 D-GOV_AUDIT 的子串前缀）
```

### 动作 1.5：D-INTEGRATION-GATEWAY → D-INTEGRATION_GATEWAY

```bash
python scripts/governance/apply_depgraph.py --rename-domain D-INTEGRATION-GATEWAY D-INTEGRATION_GATEWAY --dry-run
# 预期：total 252 rows
python scripts/governance/apply_depgraph.py --rename-domain D-INTEGRATION-GATEWAY D-INTEGRATION_GATEWAY
python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');print(c.execute(\"SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%D-INTEGRATION-GATEWAY%'\").fetchone()[0])"
# 预期输出：0
```

### 动作 1.6：D-SECURITY-LLM → D-SECURITY_LLM

```bash
python scripts/governance/apply_depgraph.py --rename-domain D-SECURITY-LLM D-SECURITY_LLM --dry-run
# 预期：total 206 rows
python scripts/governance/apply_depgraph.py --rename-domain D-SECURITY-LLM D-SECURITY_LLM
python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');print(c.execute(\"SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%D-SECURITY-LLM%'\").fetchone()[0])"
# 预期输出：0
```

### 动作 1.7：全量残留扫描

```bash
python -c "
import sqlite3
c = sqlite3.connect('data/databases/depgraph.db')
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
tables = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name NOT IN ('domain_naming_rules','_schema_version','governance_audit_logs')\").fetchall()]
total = 0
for tbl in tables:
    cols = [col[1] for col in c.execute(f'PRAGMA table_info({tbl})').fetchall()]
    for col in cols:
        for oid in old_ids:
            cnt = c.execute(f'SELECT COUNT(*) FROM {tbl} WHERE CAST({col} AS TEXT) LIKE ?', (f'%{oid}%',)).fetchone()[0]
            if cnt > 0:
                print(f'  RESIDUAL: {tbl}.{col} contains {oid}: {cnt} rows')
                total += cnt
print(f'Total residual (excl. 3 excluded tables): {total}')
"
# 预期输出：Total residual: 0
```

**若残留 > 0**：说明某表 TEXT 列未被 17 步枚举也未被 B1 兜底覆盖。记录残留表/列/行，回退到方案第五部分回滚，并在方案文档补充遗漏步骤后重跑。

### 动作 1.8：Git 提交 DB 改名

```bash
python scripts/git_commit.py --session rename-hyphen-db \
  --files data/databases/depgraph.db \
  --message "rename: 6 domain IDs hyphen→underscore (4323 rows updated) [GW:rename-hyphen]"
```

**验证**：`git log --oneline -3` 确认出现 `rename: 6 domain IDs hyphen→underscore` commit。

## files_in_scope

- D:\ZephyrAlpha\data\databases\depgraph.db
- D:\ZephyrAlpha\scripts\governance\apply_depgraph.py（只读调用，不修改）
- D:\ZephyrAlpha\scripts\git_commit.py（只读调用，不修改）

## deliverables

- 改名后的 depgraph.db（6 域 ID 全部下划线风格，0 残留）
- 2 个 git commit：backup commit（动作 0.1）+ rename commit（动作 1.8）
- `<BACKUP>` commit hash 记录（回滚锚点）

## applicable_rules

- {module_id: NR-002, section: domain_naming_rules, reason: 域ID格式必须为全大写+下划线}
- {module_id: MOD-GOV-SCRIPTS, section: apply_depgraph.py, reason: 全景图修改唯一入口}
- {project_rule: "改 depgraph.db 前必须 git commit 备份", reason: trae_054 STEP0}

## allowed_touch

- D:\ZephyrAlpha\data\databases\depgraph.db（仅通过 apply_depgraph.py --rename-domain 修改，禁止直接 SQL 改库）

## 验收标准

1. 动作 0.1 backup commit 存在，hash 已记录为 `<BACKUP>`
2. 6 个 dry-run 输出的 total 行数分别等于 756/436/2517/156/252/206（合计 4323）
3. 6 个改名后 `SELECT COUNT(*) FROM domains WHERE domain_id LIKE '%旧ID%'` 均输出 0
4. 动作 1.7 全量残留扫描输出 `Total residual: 0`
5. 动作 1.8 rename commit 存在
6. 6 个新域 ID 存在于 domains 表：
   ```bash
   python -c "import sqlite3;c=sqlite3.connect('data/databases/depgraph.db');[print(r) for r in c.execute(\"SELECT domain_id FROM domains WHERE domain_id IN ('D-GOV_DOCS','D-GOV_ENFORCEMENT','D-GOV_SCRIPTS','D-AUDITTEST','D-INTEGRATION_GATEWAY','D-SECURITY_LLM')\").fetchall()]"
   ```
   预期输出 6 行

## 回滚

**场景 A（动作 1.8 commit 前）**：DB 改名仅在工作区
```bash
git checkout data/databases/depgraph.db
```

**场景 B（动作 1.8 commit 后）**：需整体回退到备份点
```bash
# 警告：丢弃 <BACKUP> 之后所有 commit
git reset --hard <BACKUP>
```

若仅回退 DB 不回退后续文件（部分回滚）：
```bash
git checkout <BACKUP> -- data/databases/depgraph.db
```

## 完成记录

- [ ] 动作 0.1 已执行，<BACKUP>=________
- [ ] 动作 1.1 D-GOV-DOCS→D-GOV_DOCS（756 rows）
- [ ] 动作 1.2 D-GOV-ENFORCEMENT→D-GOV_ENFORCEMENT（436 rows）
- [ ] 动作 1.3 D-GOV-SCRIPTS→D-GOV_SCRIPTS（2517 rows）
- [ ] 动作 1.4 D-GOV_AUDIT_TESTS→D-AUDITTEST（156 rows）
- [ ] 动作 1.5 D-INTEGRATION-GATEWAY→D-INTEGRATION_GATEWAY（252 rows）
- [ ] 动作 1.6 D-SECURITY-LLM→D-SECURITY_LLM（206 rows）
- [ ] 动作 1.7 残留扫描=0
- [ ] 动作 1.8 rename commit=________
