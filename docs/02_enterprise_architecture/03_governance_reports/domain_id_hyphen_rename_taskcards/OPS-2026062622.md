---
task_id: "OPS-2026062622"
title: "元审查：循环审查修复 OPS-2026062621（DB 6域改名验证）"
priority: "P0"
status: "pending"
classification: "meta_review"
safety_level: "H"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062621"
---

# OPS-2026062622：元审查 OPS-2026062621

> **被审查主卡**：[OPS-2026062621](OPS-2026062621.md)
> **审查目标**：逐条验证主卡 6 项验收标准，循环修复至连续 2 轮全通过
> **不可跳过**：本卡通过前，禁止执行 OPS-2026062623 及后续任何主卡

## 审查流程

```
1. 读取 OPS-2026062621 的验收标准（6 项）
2. 逐条执行下方验证命令
3. 记录每条结果（PASS/FAIL）
4. 判定：
   - 6 条全 PASS → 标记本轮"通过"，进入下一轮
   - 有 FAIL → 执行对应修复流程，重新执行本轮全部验证
5. 循环条件：连续 2 轮 6 条全 PASS → 本元审查卡 COMPLETED
```

## 验证项

### V1：backup commit 存在且 hash 已记录

**验证命令**：
```bash
git log --oneline --all | grep "backup: pre-rename-hyphen depgraph.db snapshot"
```

**通过判定**：输出 1 行，含 commit hash。

**FAIL 修复**：若输出为空，说明动作 0.1 未执行。回到 OPS-2026062621 动作 0.1 执行 backup commit。记录 hash 为 `<BACKUP>`。

### V2：6 个旧域 ID 在 domains 表零残留

**验证命令**：
```bash
python -c "
import sqlite3
c = sqlite3.connect('data/databases/depgraph.db')
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
for oid in old_ids:
    cnt = c.execute('SELECT COUNT(*) FROM domains WHERE domain_id LIKE ?', (f'%{oid}%',)).fetchone()[0]
    status = 'PASS' if cnt == 0 else 'FAIL'
    print(f'{status}: domains.{oid} residual={cnt}')
"
```

**通过判定**：6 行全部 `PASS: ... residual=0`。

**FAIL 修复**：若有 `FAIL`，说明对应域的改名未完成或未覆盖 domains 表。回 OPS-2026062621 对应动作重新执行 `apply_depgraph.py --rename-domain`。

### V3：6 个新域 ID 存在于 domains 表

**验证命令**：
```bash
python -c "
import sqlite3
c = sqlite3.connect('data/databases/depgraph.db')
new_ids = ['D-GOV_DOCS','D-GOV_ENFORCEMENT','D-GOV_SCRIPTS','D-AUDITTEST','D-INTEGRATION_GATEWAY','D-SECURITY_LLM']
for nid in new_ids:
    cnt = c.execute('SELECT COUNT(*) FROM domains WHERE domain_id=?', (nid,)).fetchone()[0]
    status = 'PASS' if cnt == 1 else 'FAIL'
    print(f'{status}: domains.{nid} exists={cnt}')
"
```

**通过判定**：6 行全部 `PASS: ... exists=1`。

**FAIL 修复**：若有 `FAIL`，说明改名未生效。检查 apply_depgraph.py 是否报错，回 OPS-2026062621 对应动作重跑。

### V4：全表全列残留扫描=0

**验证命令**（与主卡动作 1.7 相同，独立复跑确认）：
```bash
python -c "
import sqlite3
c = sqlite3.connect('data/databases/depgraph.db')
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
tables = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name NOT IN ('domain_naming_rules','_schema_version','governance_audit_logs')\").fetchall()]
total = 0
for tbl in tables:
    cols = [col[1] for col in c.execute(f'PRAGMA table_info({tbl})').fetchall()]
    for col in cols:
        for oid in old_ids:
            cnt = c.execute(f'SELECT COUNT(*) FROM {tbl} WHERE CAST({col} AS TEXT) LIKE ?', (f'%{oid}%',)).fetchone()[0]
            if cnt > 0:
                print(f'  RESIDUAL: {tbl}.{col} contains {oid}: {cnt} rows')
                total += cnt
print(f'PASS' if total == 0 else 'FAIL' + f': Total residual={total}')
"
```

**通过判定**：最后一行 `PASS: Total residual=0`，无 RESIDUAL 行。

**FAIL 修复**：若有残留，记录残留的表/列。检查是否为 B1 兜底排除的 3 表（domain_naming_rules/_schema_version/governance_audit_logs）或 3 列（blueprint_id/path/blueprint_path）——这些排除是有意为之，不算 FAIL。若是其他表列残留，说明 apply_depgraph.py 17步+B1 有遗漏，需在方案文档补充步骤后回滚重跑。

### V5：rename commit 存在

**验证命令**：
```bash
git log --oneline --all | grep "rename: 6 domain IDs hyphen→underscore (4323 rows updated)"
```

**通过判定**：输出 1 行，含 commit hash。

**FAIL 修复**：若为空，说明动作 1.8 未执行。回 OPS-2026062621 动作 1.8 执行 rename commit。

### V6：行数合计=4323（间接验证，通过 commit message 核对）

**验证命令**：
```bash
git log -1 --format="%s" | grep "4323 rows"
```

**通过判定**：输出含 `4323 rows`。

**FAIL 修复**：若行数不符，说明改名覆盖范围与预期不一致。回滚（场景 B）后核查 dry-run 输出，确认每域行数与方案 3.2 节表格一致。

## 循环记录表

| 轮次 | V1 | V2 | V3 | V4 | V5 | V6 | 判定 |
|---|---|---|---|---|---|---|---|
| 第 1 轮 | | | | | | | |
| 第 2 轮 | | | | | | | |

**连续 2 轮全 PASS → 本卡 COMPLETED，进入 OPS-2026062623**

## 修复历史记录

（每轮 FAIL 的修复动作记录于此，格式：轮次-验证项-问题-修复动作）

- 第___轮-V___：________→ 修复：________

## 完成记录

- [ ] 连续 2 轮 V1~V6 全 PASS
- [ ] OPS-2026062621 标记为 status=COMPLETED
- [ ] 进入 OPS-2026062623
