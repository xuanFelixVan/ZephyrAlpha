---
task_id: "OPS-2026062626"
title: "元审查：循环审查修复 OPS-2026062625（活文档+CSV验证）"
priority: "P1"
status: "pending"
classification: "meta_review"
safety_level: "M"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062625"
doc_type: operational_rule
---

# OPS-2026062626：元审查 OPS-2026062625

> **被审查主卡**：[OPS-2026062625](task-ops-2026062625.md)
> **审查目标**：逐条验证主卡 3 项验收标准，循环修复至连续 2 轮全通过

## 审查流程

```
1. 读取 OPS-2026062625 的验收标准（3 项）
2. 逐条执行下方验证命令
3. 判定：全 PASS → 本轮通过；有 FAIL → 修复后重跑本轮
4. 循环条件：连续 2 轮全 PASS → COMPLETED
```

## 验证项

### V1：5 个活文档旧域名零残留

**验证命令**：
```bash
python -c "
import re, os
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
pat = re.compile('|'.join(r'(?<![A-Z])' + re.escape(d) for d in old_ids))
files = [
    'docs/02_enterprise_architecture/target_architecture/architecture_model/index.yaml',
    'docs/02_enterprise_architecture/target_architecture/capability_heatmap.md',
    'docs/02_enterprise_architecture/target_architecture/index.md',
    'docs/02_enterprise_architecture/target_architecture/overview.md',
    'docs/02_enterprise_architecture/target_architecture/application_architecture.md',
]
all_pass = True
for f in files:
    content = open(f, encoding='utf-8', errors='ignore').read()
    matches = pat.findall(content)
    status = 'PASS' if not matches else 'FAIL'
    if matches: all_pass = False
    print(f'{status}: {os.path.basename(f)} residual={len(matches)}' + (f' {matches[:3]}' if matches else ''))
print('OVERALL:', 'PASS' if all_pass else 'FAIL')
"
```

**通过判定**：5 行全 PASS + OVERALL: PASS。

**FAIL 修复**：列出残留文件和行号，回 OPS-2026062625 对应动作重新 Edit。注意 `(?<![A-Z])` 正则排除 MOD-XXX 子串误匹配，残留应均为真实旧域名。

### V2：_t17_domain_suggestions.csv 旧域名零残留

**验证命令**：
```bash
python -c "
import re
text = open('scripts/_t17_domain_suggestions.csv', encoding='utf-8').read()
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
pat = re.compile('|'.join(r'(?<![A-Z])' + re.escape(d) for d in old_ids))
matches = pat.findall(text)
print(f'PASS: 0 residual' if not matches else f'FAIL: {len(matches)} residual: {matches[:5]}')
"
```

**通过判定**：`PASS: 0 residual`。

**FAIL 修复**：回 OPS-2026062625 动作 5.1，检查是否漏了某个域的 replace_all。

### V3：6 个新域名在活文档中出现（替换成功的正向验证）

**验证命令**：
```bash
python -c "
new_ids = ['D-GOV_DOCS','D-GOV_ENFORCEMENT','D-GOV_SCRIPTS','D-AUDITTEST','D-INTEGRATION_GATEWAY','D-SECURITY_LLM']
files = [
    'docs/02_enterprise_architecture/target_architecture/architecture_model/index.yaml',
    'docs/02_enterprise_architecture/target_architecture/capability_heatmap.md',
    'docs/02_enterprise_architecture/target_architecture/index.md',
    'docs/02_enterprise_architecture/target_architecture/overview.md',
    'docs/02_enterprise_architecture/target_architecture/application_architecture.md',
    'scripts/_t17_domain_suggestions.csv',
]
import os
all_found = {nid: False for nid in new_ids}
for f in files:
    content = open(f, encoding='utf-8', errors='ignore').read()
    for nid in new_ids:
        if nid in content:
            all_found[nid] = True
for nid, found in all_found.items():
    print(f'{\"PASS\" if found else \"FAIL\"}: {nid} found in files')
"
```

**通过判定**：6 行全 PASS（每个新域名至少在 1 个文件中出现）。

**FAIL 修复**：若有新域名未出现，说明该域替换未生效。回 OPS-2026062625 对应动作核查 old_string 是否匹配成功。

## 循环记录表

| 轮次 | V1 | V2 | V3 | 判定 |
|---|---|---|---|---|
| 第 1 轮 | | | | |
| 第 2 轮 | | | | |

**连续 2 轮全 PASS → 本卡 COMPLETED，进入 OPS-2026062627**

## 修复历史记录

- 第___轮-V___：________→ 修复：________

## 完成记录

- [ ] 连续 2 轮 V1~V3 全 PASS
- [ ] OPS-2026062625 标记为 status=COMPLETED
- [ ] 进入 OPS-2026062627
