---
task_id: "OPS-2026062633"
title: "阶段9 Git 提交所有文件变更（17文件+生成制品）"
priority: "P1"
status: "pending"
classification: "git_commit"
safety_level: "M"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062632"
---

# OPS-2026062633：阶段9 Git 提交所有文件变更

> **对应方案**：[domain_id_hyphen_rename_plan.md](../../domain_id_hyphen_rename_plan.md) 第四部分 4.10（动作 9.1~9.2）
> **前置条件**：OPS-2026062632（循环验收元审查）已 COMPLETED
> **风险等级**：🟡 中（commit 不可逆，需确保前置验收全通过）

## 目标

将 17 个手动修改文件 + 重新生成的制品提交到 git。DB 改名已在动作 1.8 提交（OPS-2026062621），本卡提交文件变更和生成制品。

## 施工步骤

### 动作 9.1：提交所有修改文件（17 个）

`--files` 参数为逗号分隔的单字符串（见 `git_commit.py` 的 `_parse_files`，L52-58），**不可用空格分隔**。路径基于 cwd（项目根目录）解析。

```bash
python scripts/git_commit.py --session rename-hyphen-files \
  --files "docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml,scripts/governance/sync_yaml_to_depgraph.py,scripts/governance/d5_architecture/dm200912_rewrite_views.py,scripts/governance/d5_architecture/generators/generate_capability_heatmap.py,docs/02_enterprise_architecture/target_architecture/architecture_model/index.yaml,docs/02_enterprise_architecture/target_architecture/capability_heatmap.md,docs/02_enterprise_architecture/target_architecture/index.md,docs/02_enterprise_architecture/target_architecture/overview.md,docs/02_enterprise_architecture/target_architecture/application_architecture.md,scripts/_t17_domain_suggestions.csv,docs/02_enterprise_architecture/03_governance_reports/preexisting_db_issues_investigation_report.md,docs/02_enterprise_architecture/dependency_architecture_panorama.md,docs/02_enterprise_architecture/architecture_diagram_construction_plan.md,docs/02_enterprise_architecture/_archive/phase4b_cleanup_construction_plan.md,docs/_working/domain_split_plan_4_oversized_domains.md,docs/decomposition/tasks/DM-100254.md,data/archive/taskcards/DM-100257.md" \
  --message "rename: 6 domain IDs hyphen→underscore - file sync (17 files) [GW:rename-hyphen]"
```

**验证**：`git log --oneline -1` 出现 `rename: 6 domain IDs hyphen→underscore - file sync (17 files)`。

**注意**：
- 此 commit 含 7 个 .md 文件，GATE-15 pre-commit 钩子会校验 ttl。动作 8.4 已确认通过。
- 永久区文件（docs/01_policies/, docs/02_enterprise/）修改非新增，无需 --allow-promote。

### 动作 9.2：提交重新生成的制品

`--files` 同样为逗号分隔。目录条目（以 `/` 结尾）作为 pathspec 传入，`git add` 会递归添加目录下所有变更文件。

注意：`git_commit.py` 的 `os.path.isfile()` 对目录返回 False，会触发 `git ls-files --error-unmatch` 二次校验——已跟踪目录会通过校验（日志可能误报"已跟踪但工作区已删除"，属正常行为，不阻断提交）。

```bash
python scripts/git_commit.py --session rename-hyphen-regen \
  --files "data/asset_index/target_path_tree.yaml,data/asset_index/project_entity_depgraph.yaml,docs/02_enterprise_architecture/02_domain_architecture_docs/,docs/02_enterprise_architecture/generated/,docs/02_enterprise_architecture/01_global_architecture_diagram/,docs/02_enterprise_architecture/03_governance_reports/design_vs_production.md,docs/02_enterprise_architecture/03_governance_reports/constraint_violations.md,docs/02_enterprise_architecture/03_governance_reports/capacity_report.md" \
  --message "rename: regenerate artifacts after 6 domain ID rename [GW:rename-hyphen]"
```

**验证**：`git log --oneline -2` 出现 2 个 rename commit（file sync + regenerate artifacts）。

## files_in_scope

- 17 个手动修改文件（动作 9.1）
- 生成制品文件/目录（动作 9.2）

## deliverables

- 2 个 git commit（file sync + regenerate artifacts）
- 加上动作 1.8 的 DB commit，共 3 个 rename 相关 commit + 1 个 backup commit = 4 个 commit

## applicable_rules

- {rule: "所有git commit操作必须通过GitCommitGateway工具执行", reason: 禁止裸git commit}
- {rule: "GitCommitGateway必须设置ZEPHYR_COMMIT_GATEWAY=1环境变量并在commit message追加[GW:session_id]标记", reason: 网关标记}
- {rule: "--files参数为逗号分隔", reason: _parse_files 按 split(",") 解析}

## allowed_touch

- （本卡为提交卡，不修改文件内容，仅 git add+commit）

## 验收标准

1. 动作 9.1 commit 成功，message 含 `file sync (17 files)` 和 `[GW:rename-hyphen]`
2. 动作 9.2 commit 成功，message 含 `regenerate artifacts` 和 `[GW:rename-hyphen]`
3. `git log --oneline -4` 显示 4 个相关 commit：backup + DB rename + file sync + regenerate
4. `git status` 工作区干净（无未提交变更）

## 回滚

**场景 A（commit 后，需回退）**：
```bash
# 回退到动作 1.8 之前（DB 改名 commit 之前）
# 警告：丢弃所有 rename 相关 commit
git reset --hard <DB_RENAME_COMMIT>~1
# 或回退到 backup commit
git reset --hard <BACKUP>
```

若仅回退文件 commit 不回退 DB commit：
```bash
git reset --hard HEAD~2  # 回退 file sync + regenerate 两个 commit，保留 DB rename commit
```

## 完成记录

- [ ] 动作 9.1 file sync commit=________
- [ ] 动作 9.2 regenerate artifacts commit=________
- [ ] git status 工作区干净
