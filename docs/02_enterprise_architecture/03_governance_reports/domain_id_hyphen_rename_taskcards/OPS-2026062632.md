---
task_id: "OPS-2026062632"
title: "元审查：循环审查修复 OPS-2026062631（验收关卡验证）"
priority: "P1"
status: "pending"
classification: "meta_review"
safety_level: "M"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062631"
---

# OPS-2026062632：元审查 OPS-2026062631

> **被审查主卡**：[OPS-2026062631](OPS-2026062631.md)
> **审查目标**：逐条验证主卡 4 项验收标准，循环修复至连续 2 轮全通过
> **特殊性**：本元卡审查的是"验收卡"本身——确认验收流程确实执行且结果真实

## 审查流程

```
1. 读取 OPS-2026062631 的验收标准（4 项）
2. 独立复跑验证命令（不轻信主卡声称的结果）
3. 判定：全 PASS → 本轮通过；有 FAIL → 修复后重跑本轮
4. 循环条件：连续 2 轮全 PASS → COMPLETED
```

## 验证项

### V1：diagnose_depgraph.py 输出 0 error（独立复跑）

**验证命令**：
```bash
python scripts/governance/diagnose_depgraph.py 2>&1 | findstr /C:"error" /C:"ERROR" /C:"Error"
```

**通过判定**：无输出（或仅输出 info/warning 行，无 error 行）。另执行完整命令确认退出码：
```bash
python scripts/governance/diagnose_depgraph.py
echo EXIT_CODE=%ERRORLEVEL%
```
退出码 0 = 通过。

**FAIL 修复**：若有 error，根据 error 类型回对应修改卡修复后重跑 diagnose。

### V2：文件残留扫描 = 0（独立复跑）

**验证命令**（与主卡动作 8.3 相同脚本，独立复跑）：
```bash
python -c "
import os, re
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
pat = re.compile('|'.join(r'(?<![A-Z])' + re.escape(d) for d in old_ids))
check_files = [
    'docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml',
    'docs/02_enterprise_architecture/target_architecture/architecture_model/index.yaml',
    'docs/02_enterprise_architecture/target_architecture/capability_heatmap.md',
    'docs/02_enterprise_architecture/target_architecture/index.md',
    'docs/02_enterprise_architecture/target_architecture/overview.md',
    'docs/02_enterprise_architecture/target_architecture/application_architecture.md',
    'scripts/governance/sync_yaml_to_depgraph.py',
    'scripts/governance/d5_architecture/dm200912_rewrite_views.py',
    'scripts/governance/d5_architecture/generators/generate_capability_heatmap.py',
    'scripts/_t17_domain_suggestions.csv',
]
total = 0
for f in check_files:
    if not os.path.exists(f): continue
    content = open(f, encoding='utf-8', errors='ignore').read()
    for m in pat.finditer(content):
        start = m.start()
        if start > 0 and content[start-1].isupper(): continue
        if '[BLUEPRINT]' in content[max(0,start-50):start+50]: continue
        print(f'  RESIDUAL: {f}:{content.count(chr(10),0,start)+1}: {m.group()}')
        total += 1
print(f'PASS' if total == 0 else 'FAIL' + f': residual={total}')
"
```

**通过判定**：`PASS: residual=0`，无 RESIDUAL 行。

**FAIL 修复**：回 OPS-2026062623 或 OPS-2026062625 对应文件修复残留。

### V3：GATE-15 退出码 = 0（独立复跑）

**验证命令**：
```bash
python scripts/governance/d3_metadata/check_frontmatter_metadata.py \
  docs/02_enterprise_architecture/03_governance_reports/preexisting_db_issues_investigation_report.md \
  docs/02_enterprise_architecture/dependency_architecture_panorama.md \
  docs/02_enterprise_architecture/architecture_diagram_construction_plan.md \
  docs/02_enterprise_architecture/_archive/phase4b_cleanup_construction_plan.md \
  docs/_working/domain_split_plan_4_oversized_domains.md \
  docs/decomposition/tasks/DM-100254.md \
  data/archive/taskcards/DM-100257.md
echo EXIT_CODE=%ERRORLEVEL%
```

**通过判定**：EXIT_CODE=0，无 `missing required field 'ttl'` 或 `invalid ttl` 行。

**FAIL 修复**：回 OPS-2026062627 动作 6.3a 补 ttl。

### V4：连续 2 轮 diagnose 零错误（确认主卡确实跑了 2 轮）

**验证方式**：检查 OPS-2026062631 完成记录，确认动作 8.1 和 8.2 均已勾选，且两轮之间有修复记录（或直接 2 轮通过）。再独立跑 1 轮 diagnose 确认当前状态仍为 0 error（V1 已覆盖）。

**通过判定**：主卡完成记录中 8.1/8.2 均勾选 + V1 当前复跑 0 error。

**FAIL 修复**：若主卡只跑了 1 轮就声称通过，回 OPS-2026062631 补跑第 2 轮。

## 循环记录表

| 轮次 | V1 | V2 | V3 | V4 | 判定 |
|---|---|---|---|---|---|
| 第 1 轮 | | | | | |
| 第 2 轮 | | | | | |

**连续 2 轮全 PASS → 本卡 COMPLETED，进入 OPS-2026062633**

## 修复历史记录

- 第___轮-V___：________→ 修复：________

## 完成记录

- [ ] 连续 2 轮 V1~V4 全 PASS
- [ ] OPS-2026062631 标记为 status=COMPLETED
- [ ] 进入 OPS-2026062633
