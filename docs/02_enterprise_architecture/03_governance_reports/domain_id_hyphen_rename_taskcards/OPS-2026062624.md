---
task_id: "OPS-2026062624"
title: "元审查：循环审查修复 OPS-2026062623（YAML+脚本验证）"
priority: "P1"
status: "pending"
classification: "meta_review"
safety_level: "M"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062623"
doc_type: operational_rule
---

# OPS-2026062624：元审查 OPS-2026062623

> **被审查主卡**：[OPS-2026062623](OPS-2026062623.md)
> **审查目标**：逐条验证主卡 5 项验收标准，循环修复至连续 2 轮全通过

## 审查流程

```
1. 读取 OPS-2026062623 的验收标准（5 项）
2. 逐条执行下方验证命令
3. 判定：全 PASS → 本轮通过；有 FAIL → 修复后重跑本轮
4. 循环条件：连续 2 轮全 PASS → COMPLETED
```

## 验证项

### V1：functional_domain_registry.yaml 旧域名零残留

**验证命令**：
```bash
python -c "
import re
text = open('docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml', encoding='utf-8').read()
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
pat = re.compile('|'.join(r'(?<![A-Z])' + re.escape(d) for d in old_ids))
matches = pat.findall(text)
print(f'PASS: 0 residual' if not matches else f'FAIL: {len(matches)} residual: {matches[:5]}')
"
```

**通过判定**：`PASS: 0 residual`。

**FAIL 修复**：列出残留行号，回 OPS-2026062623 动作 2.1 对应行重新 Edit 替换。注意 L54/L811 可能只改了一处。

### V2：sync_yaml_to_depgraph.py L706 已改为 D-GOV_DOCS

**验证命令**：
```bash
python -c "
text = open('scripts/governance/sync_yaml_to_depgraph.py', encoding='utf-8').read()
lines = text.split(chr(10))
line = lines[705] if len(lines) > 705 else ''
has_new = 'D-GOV_DOCS' in line
has_old = 'D-GOV-DOCS' in line
status = 'PASS' if has_new and not has_old else 'FAIL'
print(f'{status}: L706={line.strip()[:80]}')
"
```

**通过判定**：`PASS: L706=...D-GOV_DOCS...`（含 D-GOV_DOCS，不含 D-GOV-DOCS）。

**FAIL 修复**：回 OPS-2026062623 动作 3.1 重新 Edit。

### V3：dm200912_rewrite_views.py L829/L832 已改

**验证命令**：
```bash
python -c "
text = open('scripts/governance/d5_architecture/dm200912_rewrite_views.py', encoding='utf-8').read()
lines = text.split(chr(10))
l829 = lines[828] if len(lines) > 828 else ''
l832 = lines[831] if len(lines) > 831 else ''
ok1 = 'D-SECURITY_LLM' in l829 and 'D-SECURITY-LLM' not in l829
ok2 = 'D-INTEGRATION_GATEWAY' in l832 and 'D-INTEGRATION-GATEWAY' not in l832
print(f'{\"PASS\" if ok1 else \"FAIL\"}: L829={l829.strip()[:60]}')
print(f'{\"PASS\" if ok2 else \"FAIL\"}: L832={l832.strip()[:60]}')
"
```

**通过判定**：两行均 PASS。

**FAIL 修复**：回 OPS-2026062623 动作 3.2 重新 Edit 对应行。

### V4：generate_capability_heatmap.py L116/L127 已改

**验证命令**：
```bash
python -c "
text = open('scripts/governance/d5_architecture/generators/generate_capability_heatmap.py', encoding='utf-8').read()
lines = text.split(chr(10))
l116 = lines[115] if len(lines) > 115 else ''
l127 = lines[126] if len(lines) > 126 else ''
ok1 = 'D-SECURITY_LLM' in l116 and 'D-SECURITY-LLM' not in l116
ok2 = 'D-INTEGRATION_GATEWAY' in l127 and 'D-INTEGRATION-GATEWAY' not in l127
print(f'{\"PASS\" if ok1 else \"FAIL\"}: L116={l116.strip()[:60]}')
print(f'{\"PASS\" if ok2 else \"FAIL\"}: L127={l127.strip()[:60]}')
"
```

**通过判定**：两行均 PASS。

**FAIL 修复**：回 OPS-2026062623 动作 3.3 重新 Edit 对应行。

### V5：sync_yaml_to_depgraph.py 执行无 diff

**验证命令**：
```bash
python scripts/governance/sync_yaml_to_depgraph.py 2>&1 | findstr /C:"diff" /C:"DIFF" /C:"mismatch" /C:"不一致"
```

**通过判定**：无输出（或仅输出 sync 成功信息，无 diff/mismatch 行）。

**FAIL 修复**：若有 diff，说明 YAML 和 DB 不一致。检查 YAML 是否有遗漏行（回 V1），或 DB 改名是否完整（回 OPS-2026062622 的 V2/V3）。

## 循环记录表

| 轮次 | V1 | V2 | V3 | V4 | V5 | 判定 |
|---|---|---|---|---|---|---|
| 第 1 轮 | | | | | | |
| 第 2 轮 | | | | | | |

**连续 2 轮全 PASS → 本卡 COMPLETED，进入 OPS-2026062625**

## 修复历史记录

- 第___轮-V___：________→ 修复：________

## 完成记录

- [ ] 连续 2 轮 V1~V5 全 PASS
- [ ] OPS-2026062623 标记为 status=COMPLETED
- [ ] 进入 OPS-2026062625
