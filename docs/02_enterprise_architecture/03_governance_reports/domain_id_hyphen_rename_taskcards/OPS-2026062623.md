---
task_id: "OPS-2026062623"
title: "阶段2 YAML registry+阶段3 生成器脚本修改（4文件，~12处替换）"
priority: "P1"
status: "pending"
classification: "file_modification"
safety_level: "M"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062622"
---

# OPS-2026062623：阶段2 YAML + 阶段3 生成器脚本修改

> **对应方案**：[domain_id_hyphen_rename_plan.md](../../domain_id_hyphen_rename_plan.md) 第四部分 4.3~4.4（动作 2.1~2.2 + 3.1~3.3）
> **前置条件**：OPS-2026062622（DB 改名元审查）已 COMPLETED
> **风险等级**：🟡 中（代码/YAML 修改，影响生成器输出）

## 目标

DB 域 ID 改名后，YAML 真源（functional_domain_registry.yaml）和 3 个生成器脚本中的硬编码域名引用必须同步更新，否则 sync_yaml_to_depgraph.py 会将旧名重新同步回 DB（覆盖改名），且生成器会输出旧域名。

## 施工步骤

### 动作 2.1：修改 functional_domain_registry.yaml（7 行）

文件：`docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml`

用 Edit 工具逐行精确替换。**注意 L54 和 L811 内容相同**（`- domain: D-GOV-SCRIPTS`），需用 replace_all=true 或带上下文精确替换。

| 行号 | old_string | new_string |
|---|---|---|
| L27 | `- domain: D-GOV-ENFORCEMENT` | `- domain: D-GOV_ENFORCEMENT` |
| L54 | `- domain: D-GOV-SCRIPTS` | `- domain: D-GOV_SCRIPTS` |
| L212 | `- domain: D-SECURITY-LLM` | `- domain: D-SECURITY_LLM` |
| L757 | `- domain: D-INTEGRATION-GATEWAY` | `- domain: D-INTEGRATION_GATEWAY` |
| L811 | `- domain: D-GOV-SCRIPTS` | `- domain: D-GOV_SCRIPTS`（与 L54 相同，replace_all 或上下文区分） |
| L903 | `- domain: D-GOV_AUDIT_TESTS` | `- domain: D-AUDITTEST` |
| L921 | `- domain: D-GOV-DOCS` | `- domain: D-GOV_DOCS` |

**施工动作**：对每个行号，先 Read 确认该行内容（含前后上下文），再用 Edit 工具执行替换。L54/L811 因 old_string 相同，建议用 replace_all=true 一次替换两处。

### 动作 2.2：同步 YAML 到 DB

```bash
python scripts/governance/sync_yaml_to_depgraph.py
# 预期：YAML 与 DB 一致，无 diff 输出（因 DB 已在阶段1改名，YAML 现在也改了，两者应一致）
```

**验证**：若 sync 输出 diff（说明 YAML 和 DB 不一致），检查是否有遗漏行未改。

### 动作 3.1：修改 sync_yaml_to_depgraph.py（1 处）

文件：`scripts/governance/sync_yaml_to_depgraph.py`
行号：L706

```
原：VALUES (?, ?, 'file', 'D-GOV-DOCS', ?, 'design')
新：VALUES (?, ?, 'file', 'D-GOV_DOCS', ?, 'design')
```

**施工动作**：Read 该文件 L700-L710 确认上下文，用 Edit 工具替换 `D-GOV-DOCS` → `D-GOV_DOCS`（注意此行是 SQL VALUES 占位，不是 `- domain:` 行，精确匹配 `'D-GOV-DOCS'` 字符串）。

### 动作 3.2：修改 dm200912_rewrite_views.py（2 处）

文件：`scripts/governance/d5_architecture/dm200912_rewrite_views.py`

| 行号 | 替换内容 |
|---|---|
| L829 | `D-SECURITY-LLM` → `D-SECURITY_LLM`（在域列表字符串 `"安全（横切）", "D-SECURITY, D-SECURITY-LLM, ..."` 中） |
| L832 | `D-INTEGRATION-GATEWAY` → `D-INTEGRATION_GATEWAY`（在域列表字符串 `"D-INFRA_OPS, ..., D-INTEGRATION-GATEWAY, ..."` 中） |

**施工动作**：Read L825-L835 确认上下文，逐处 Edit 替换。

### 动作 3.3：修改 generate_capability_heatmap.py（2 处）

文件：`scripts/governance/d5_architecture/generators/generate_capability_heatmap.py`

| 行号 | 替换内容 |
|---|---|
| L116 | `"D-SECURITY-LLM"` → `"D-SECURITY_LLM"`（在 Python list `["D-SECURITY", "D-SECURITY-LLM", ...]` 中） |
| L127 | `"D-INTEGRATION-GATEWAY"` → `"D-INTEGRATION_GATEWAY"`（在 Python list 中） |

**施工动作**：Read L110-L130 确认上下文，逐处 Edit 替换。

## files_in_scope

- D:\ZephyrAlpha\docs\01_policies_and_standards\_registry\catalogs\functional_domain_registry.yaml
- D:\ZephyrAlpha\scripts\governance\sync_yaml_to_depgraph.py
- D:\ZephyrAlpha\scripts\governance\d5_architecture\dm200912_rewrite_views.py
- D:\ZephyrAlpha\scripts\governance\d5_architecture\generators\generate_capability_heatmap.py

## deliverables

- 4 个文件修改完成，6 个旧域名零残留
- sync_yaml_to_depgraph.py 执行无 diff

## applicable_rules

- {rule: "YAML文件是规则数据的唯一真源", reason: functional_domain_registry.yaml 为域定义真源}
- {rule: "DB规则表为只读缓存，仅通过sync_yaml_to_depgraph.py单向同步", reason: 脚本硬编码也需同步}

## allowed_touch

- docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml
- scripts/governance/sync_yaml_to_depgraph.py
- scripts/governance/d5_architecture/dm200912_rewrite_views.py
- scripts/governance/d5_architecture/generators/generate_capability_heatmap.py

## 验收标准

1. functional_domain_registry.yaml 中 6 个旧域名零残留（grep 验证）
2. sync_yaml_to_depgraph.py L706 已改为 D-GOV_DOCS
3. dm200912_rewrite_views.py L829/L832 已改为新域名
4. generate_capability_heatmap.py L116/L127 已改为新域名
5. 动作 2.2 sync_yaml_to_depgraph.py 执行无 diff 输出

## 回滚

**场景 A（commit 前）**：
```bash
git checkout -- docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml scripts/governance/sync_yaml_to_depgraph.py scripts/governance/d5_architecture/dm200912_rewrite_views.py scripts/governance/d5_architecture/generators/generate_capability_heatmap.py
```

## 完成记录

- [ ] 动作 2.1 YAML 7 行替换完成
- [ ] 动作 2.2 sync 无 diff
- [ ] 动作 3.1 sync_yaml_to_depgraph.py L706
- [ ] 动作 3.2 dm200912_rewrite_views.py L829/L832
- [ ] 动作 3.3 generate_capability_heatmap.py L116/L127
