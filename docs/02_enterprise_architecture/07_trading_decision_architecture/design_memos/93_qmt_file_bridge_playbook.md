---
ttl: permanent
doc_type: architecture_view
title: 大QMT文件桥双向通道操作手册（miniQMT 替代方案）
owner: ZephyrAlpha-Owner
language: zh
status: active
version: "1.0.0"
date: 2026-08-25
topic: qmt_file_bridge
scope: 07_trading_decision_architecture
---

# 大QMT文件桥双向通道操作手册（miniQMT 替代方案）

> 本备忘记录 miniQMT 清退后，项目与券商终端之间**唯一合规双向通道**的架构、实证证据与逐步打开方法。
> 上游衔接：[40_execution_broker](40_execution_broker.md) 决策⑲（MiniQMT 政策关注）——2026-08-25 政策风险正式兑现，本备忘是兑现后的落地答案。
> 性质：永久态操作手册，全部步骤经 2026-08-25 实盘终端逐项实证，不是推测。

## 1. 一句话结论

**miniQMT（迅投轻型直连接口，本地 Python 通过 xtquant 直连交易）死了，大QMT（迅投标准版客户端，策略运行在客户端沙箱内）活着。项目与沙箱之间的双向通道 = "项目写指令文件 → 沙箱内哑执行器读文件下单（进口）"+"官方自动导出与策略查询把资金/持仓/成交写成文件（出口）"。本系统这种"本地重型大脑"的个人开发者，这是唯一现实合规通道。**

## 2. 背景与裁定

### 2.1 政策时间线（已核实）
- 2026-07-06：多家券商停止 miniQMT 新增开通（国金在列）
- 2026-08-21：国金证券向存量用户下发通知，**2026-09-18 miniQMT 全面清退**
- 监管逻辑：miniQMT 策略跑在券商视野外（"外接系统"），违反穿透式监管导向；大QMT 策略跑在客户端沙箱内，券商看得见、可风控、留痕可回溯，故保留

### 2.2 唯一性论证（为什么只剩这条路）
| 候选通道 | 判定 | 原因 |
|---|---|---|
| 换券商再开 miniQMT（财达/东北等） | ❌ | 监管统一清退，缓刑几个月且白压资金 |
| 大QMT 文件桥（本方案） | ✅ **唯一解** | 执行进沙箱（合规）+ 文件交换（官方认可介质）+ 本地大脑零改动 |
| PTrade（恒生云端托管终端，策略部署在券商服务器运行） | 备选备胎 | 装不下本项目（无本地文件、无 ClickHouse/Ollama、共享算力），适合轻策略 |
| 预埋单导入（QMT 交易面板从文本文件导入委托单一键发送） | 兜底 | 半自动（每天需人工点一次发送），进口通道的政策级后备 |
| 机构柜台 | ❌ | 300 万门槛，不适用 |

### 2.3 终端辨识（防混淆）
- 大QMT 主程序：`XtItClient.exe`（全功能客户端，**策略沙箱在此**）
- miniQMT 终端：`XtMiniQmt.exe`（极简交易，**被清退对象**）
- 国金实盘安装目录：`E:\国金证券QMT交易端`（两终端同目录，靠 exe 名区分）
- 实盘账号：`8887871993`；模拟盘账号 `8886156677`（另目录，含"模拟"后缀）

## 3. 通道架构

```
ZephyrAlpha（大脑：因子/情绪/组合/风控，本机，零改动）
        │  每天产出指令文件 orders_YYYYMMDD.csv（GBK 或 ASCII）
        ▼
┌─ 大QMT 沙箱（XtItClient 内置 Python 3.6）─────────────────┐
│  哑执行器策略（纯 ASCII 源码，常驻模型交易）                │
│    读指令文件 → 校验（幂等/金额上限）→ passorder 下单       │
│    passorder＝大QMT 内置下单函数（事件驱动框架的官方委托口）  │
└──────────────────────────────────────────────┬───────────┘
        │ 出口双通道                             │ 报单进券商柜台
        ▼                                        ▼
① 官方自动导出（系统设置级，零代码）：          ② 策略内 get_trade_detail_data 查询
   Stock/PositionStatics.csv（持仓）              get_trade_detail_data＝大QMT 内置账户
   Stock/Account.csv（资金）                      查询函数（资金/持仓/委托/成交）
   Stock/Order.csv + Deal.csv（委托/成交）
   Message.csv（消息提示/废单原因）
        │
        ▼
E:\qmt_bridge\（桥接目录，GBK 编码文件）
        │
        ▼
ZephyrAlpha 读取 → 持仓监控/对账/风控闭环
```

- **进口（项目→QMT）**：指令文件 → 哑执行器 → passorder
- **出口（QMT→项目）**：官方自动导出（主）+ 策略内查询（辅）
- 全部交换文件为 **GBK 编码**（QMT 侧默认），项目侧读写按 GBK 处理

## 4. 实测证据（2026-08-25 晚，实盘终端逐项实证）

| # | 测试 | 结果 | 结论 |
|---|---|---|---|
| 1 | 外部 Python `xttrader.connect()` 连大QMT | 返回 -1 | 大QMT 无任何外部交易接口 |
| 2 | 外部 Python `xtdata.get_full_tick()` | 报错"无法连接xtquant服务" | 大QMT 不对外供行情（miniquote 服务未起） |
| 3 | 官方自动导出设置后 | `E:\qmt_bridge\Stock\` 产出 PositionStatics/Account/Order/Deal.csv，持仓市值 271,483.10 与客户端内部日志分毫不差 | **出口通道官方原生可用** |
| 4 | 编辑器【运行】按钮跑策略 | 日志仅"开始运行/结束运行"，init 未执行 | 编辑器运行=指标试跑，是假动作 |
| 5 | 模型交易启动 GBK 中文策略 | `SyntaxError: 'utf-8' codec can't decode byte 0xb3` | **引擎按 UTF-8 解码源码，编辑器按 GBK 保存** |
| 6 | 模型交易启动纯 ASCII 探针策略（实盘终端） | `INIT_OK_12345` / `SET_ACCOUNT_OK` / `ACCOUNT_ROWS=1` / `AVAILABLE=106263.6` / `FILE_WRITE_OK` | **init、账户绑定、资金查询、文件读写全通** |
| 7 | 官方网格策略（加密）在模型交易 | 正常运行并打印 | 引擎环境无问题；官方策略加密需密码，不可复制 |
| 8 | 模拟终端 passorder 下单探针（22:27，模拟号 8886156677） | `SENT T1001 buy 510300.SH x100` 无异常；`ORDER_COUNT=0` | passorder 枚举（opType 23=买/24=卖、orderType 1101=按股、prType 5=最新价/11=限价）与国金版本兼容 |
| 9 | "启动本地python"勾选状态对照 | 勾选 → 秒起秒停无任何日志；不勾 → 策略正常运行打印 | **该勾选是"秒起秒停"总根源**（国金此版本本地 python 路径假死）；用户策略必须**不勾** |
| 10 | init 下单 vs handlebar 下单、quickTrade 0/1、最新价/限价（夜间） | 全部 `SENT` 无异常但均不落地、无消息、无任务 | 夜间柜台对**程序化报单**静默废单（人工 GUI 单可入）；参数变量全部排除，非代码问题 |
| 11 | miniQMT xttrader 对照组（夜间，同柜台） | `order_id=1090519048` 受理，但终态 `status=57 废单`，`msg=[COUNTER][120147]`（非交易时间） | **夜间程序单被废与通道无关**（miniQMT 一样被废）；`SENT`/正数 order_id ≠ 落地，必须回执确认 |
| 12 | **盘中 passorder 下单（2026-08-26 10:06，模拟终端实盘模式）** | `SENT T1009` → 2 秒后 `ORDER_COUNT=1`；xttrader 交叉验证：`status=50 已报`、冻结 1805.52 元；官方导出 `Order.csv` 捕获全字段（任务来源=ZEPHYR_EXEC、下单方式=函数下单、投资备注=T1009） | **进口下单全通**；userOrderId 进入柜台"投资备注"字段，审计留痕完整 |
| 13 | **盘中撤单（同日 10:09）** | `CANCEL_SENT T1009 sysid=2337` → xttrader 验证 `status=54 已撤`、资金解冻回 1000 万整 | **撤单链路通**；内置撤单签名 `cancel(sysid, ACCOUNT, 'stock', ContextInfo)` 命中 |
| 14 | 引擎日志深挖 passorder 静默根因（同日 09:44-10:02） | `passorder cp, skip ... isLastBar:false`；编辑器实例（SH 前缀 requestID）反复抢先发射、纸面不进柜台；`ContextInfo._done` 跨 tick 不持久 | **三大静默根因**：① passorder 防回放保护只在 isLastBar 执行；② 编辑器运行=纸面上下文且会污染本地幂等；③ 状态必须落盘或查柜台。**幂等正解=柜台 remark 比对（权威）** |

## 5. 打开方法 SOP（标准作业程序，照做即可复现）

### A. 出口通道：官方自动导出（零代码，10 分钟）
路径：大QMT →【设置】→【系统设置】→【导出设置】

| 设置项 | 值 |
|---|---|
| 自动导出 | ✅ 勾 |
| 导出间隔 | 委托/成交 0.10 秒；资金/持仓/标的券/负债 10 秒 |
| 开始/结束时间 | 08:50:00 – 15:40:00 |
| 盘后导出 | ✅ 勾（收盘时间 15:20） |
| 导出文件以日期时间为后缀 | ❌ 不勾（保持固定文件名，程序好读） |
| 设置默认导出路径 | ✅ 勾，设为 `E:\qmt_bridge` |
| 导出历史数据天数 | 7 天 |
| 文本导出共享读模式 | ✅ 勾（允许外部边写边读，防文件占用冲突） |
| 股票交易→持仓/委托/成交/账号导出 | 列全选，路径在 `E:\qmt_bridge\Stock\` 下 |
| 全局信息→消息提示导出 | 列全选（日期/时间/账号/类别/级别/消息），路径 `E:\qmt_bridge\Message.csv` |

产出文件（GBK 编码）：`Stock/PositionStatics.csv`（持仓）、`Stock/Account.csv`（资金）、`Stock/Order.csv`（委托）、`Stock/Deal.csv`（成交）、`Message.csv`（废单原因等）。注意：导出只在时间窗（08:50-15:40）内刷新，盘后无新文件属正常。

**⚠ 两个终端级铁律（2026-08-25 模拟终端实证踩出）**：
1. **导出设置按终端独立**——实盘/模拟/未来每个 QMT 终端各自持有一份设置，新终端必须各自配一遍（模拟终端委托/成交导出间隔默认 1.00 秒，与实盘 0.10 秒不同，注意核对）。
2. **覆盖陷阱：多终端禁止共用同一导出目录**——实盘与模拟都导 `E:\qmt_bridge` 会互相覆盖 `Stock\PositionStatics.csv` 等同名文件（实盘真持仓会被模拟假数据盖掉）。**分区约定**：实盘 = `E:\qmt_bridge\`，模拟 = `E:\qmt_bridge_sim\`；多账户再按 `acct_<资金账号>\` 二级分区（指令文件与回执同样按账号分区，连接器设计的一等公民）。

### B. 进口通道：策略创建与启动（三条铁律见 §6）

**B-1 写策略代码（铁律一：纯 ASCII 源码）**
- 源码只允许 ASCII 字符：注释用英文，中文路径用 `\uXXXX` 转义（例：`E:\国金证券QMT交易端` = `'E:\\u56fd\u91d1\u8bc1\u5238QMT\u4ea4\u6613\u7aef'`）
- 头部声明 `# -*- coding: utf-8 -*-`
- 原因：编辑器按 GBK 保存、引擎按 UTF-8 解码，任何中文字符 → SyntaxError → 秒起秒停无解释

**B-2 注册策略**：策略编辑器 →【新建策略文件】→ 粘贴 → 保存（命名，如 ZEPHYR_EXEC）→【编译】
- AI 直写磁盘文件（`E:\国金证券QMT交易端\python\<名>.py`）**不会**自动出现在模型交易列表——模型交易列表读注册表，必须在编辑器里保存过一次才算注册

**B-3 启动策略（铁律二：正确入口）**：大QMT【模型交易】面板 →【新建策略交易】：
- 策略类型=刚保存的策略；账号类型=**股票**；资金账号=`8887871993`
- 主图代码=`000300`（仅驱动 K 线的壳，逻辑不依赖）；运行周期=`1分钟`
- 运行模式：先**模拟**验证，稳定后切**实盘**
- 可选：勾【终端启动后自动运行】（QMT 一开策略自启，免每日手动）
- ⚠ **不要点编辑器里的【运行】**——那是指标试跑，不执行 init，纯假动作
- ⚠ **不要在编辑器里重新保存 AI 直写的 UTF-8 文件**——编辑器会把它覆盖回 GBK

**B-4 验收**：面板下方【策略日志】出现策略名前缀的打印（如 `INIT_OK_12345`）即通；引擎底层日志在 `E:\国金证券QMT交易端\userdata\log\XtClient_Formula_YYYYMMDD.log`（GBK）

### C. 双向探针验证代码（实测通过版，可直接复用）

```python
# -*- coding: utf-8 -*-
def init(ContextInfo):
    print('INIT_OK_12345')
    try:
        ContextInfo.set_account('8887871993')   # set_account＝内置账户绑定函数，查询前必须先调
        print('SET_ACCOUNT_OK')
    except Exception as e:
        print('SET_ACCOUNT_FAIL %r' % e)
    try:
        r = get_trade_detail_data('8887871993', 'stock', 'account')
        print('ACCOUNT_ROWS=%d' % (len(r) if r else 0))
        if r:
            print('AVAILABLE=%s' % getattr(r[0], 'm_dAvailable', 'NO_FIELD'))
    except Exception as e:
        print('QUERY_FAIL %r' % e)
    try:
        f = open(r'E:\qmt_bridge\probe_init.txt', 'w')
        f.write('init ran ok\n')
        f.close()
        print('FILE_WRITE_OK')
    except Exception as e:
        print('FILE_WRITE_FAIL %r' % e)

def handlebar(ContextInfo):
    pass
```

## 6. 排障表（2026-08-25 实测踩出，后来者可按症状直查）

| 症状 | 病根 | 修法 |
|---|---|---|
| 点编辑器【运行】，日志只有"开始运行/结束运行"，init 不执行 | 编辑器运行=指标试跑，不调用 init/handlebar（init/handlebar＝大QMT 策略框架的初始化/行情驱动两个入口函数） | 去【模型交易】面板新建策略交易并启动 |
| 模型交易里启动秒起秒停、策略日志空白、引擎日志无 subscribe | **"启动本地python"被勾选**（国金此版本本地 python 路径假死） | 编辑器右侧面板**取消勾选"启动本地python"**再启动；官方策略能跑+取消勾选后能跑=实锤 |
| 策略日志报 `SyntaxError: 'utf-8' codec can't decode byte 0xXX` | 源码含中文，编辑器 GBK 保存 vs 引擎 UTF-8 解码打架 | 源码改纯 ASCII（中文路径 `\u` 转义），头部 `coding: utf-8` |
| 策略 `SENT` 但委托不落地、无报错、无消息（夜间） | 柜台夜间对**程序化报单**静默废单（人工 GUI 单可入）；`SENT`/正数 order_id 只是"终端受理"≠落地 | 属预期行为，**盘中（9:15-15:00）复验**；执行器必须做"下单后 1-2 秒查委托"的回执确认 |
| 模型交易某行反复秒起秒停（排除勾选问题后） | 该行配置损坏（账号/模式未配对），每次启动重复同一坏配置 | 删除该行，重新【新建策略交易】配好再启动 |
| AI 直写模拟终端策略文件后改动不生效 | **模拟终端编辑器保存后策略即加密落盘**（实盘终端明文，两终端行为不同） | 模拟终端的策略修改只能走编辑器粘贴+保存，禁止直写磁盘 |
| 策略 `SENT` 但柜台查无此单，引擎日志见 `passorder cp, skip ... isLastBar:false` | **passorder 防回放保护**：启动时回放数千根历史 K 线，非最新 bar 上一律 skip（init 里下单同理会打在 bar 0 被 skip） | 下单代码必须包 `if not ContextInfo.is_last_bar(): return` 守卫，只在最新 bar 执行 |
| 策略每 ~3 秒重复发射同一指令/本地幂等文件被污染 | **编辑器【运行】的影子实例**（引擎日志 SH 前缀 requestID）纸面发射+抢先写幂等文件；`ContextInfo._done` 自定义属性跨 tick 不持久 | ① 编辑器永不点【运行】，策略只在模型交易跑；② 幂等用**柜台 remark 比对**（passorder 的 userOrderId 会进柜台"投资备注"字段，撤单/防重按它定位），本地 done 文件仅作快路径 |

## 7. 合规定位

- 报单发自券商沙箱内终端，券商看得见、可风控、留痕可回溯——满足穿透式监管核心诉求
- 指令文件是离散"交易指令单"（买什么/多少股/什么价），非实时交易行为外移；执行器保持"哑"（只翻译指令 + 终端侧保命校验：幂等去重、单笔金额上限、价格笼子检查），策略逻辑永不进桥
- 上线前按 40_execution_broker 决策⑱ 完成程序化交易报备（策略类型/服务器地址/风控参数）
- 项目为中低频（日/周级调仓），距高频认定线（每秒 15 笔/单日 2 万笔）数个数量级，无高频合规暴露

## 8. 备用与兜底

| 场景 | 预案 |
|---|---|
| 大QMT 文件桥政策再收紧 | 预埋单导入（项目每日生成预埋单格式文件，QMT 交易面板导入→一键发送，人工一下点击）；导出设置中【按预埋单格式导出数据】选项即为该格式预留 |
| 大QMT 整体不可用 | PTrade 备胎（国金 10 万档可双开，策略需简化为托管形态） |
| 策略实盘前验证 | 模型交易"模拟"模式（真实账户数据+模拟撮合，不下真实单）先跑 3-5 个交易日 |

## 9. 后续施工清单（本备忘对应工程项）

1. 正式执行器策略（纯 ASCII：读指令 CSV → 校验幂等/金额上限 → passorder → 回执 CSV）替换探针，模拟模式验证后切实盘
2. 删除模型交易中 ZEPHYR_BRIDGE_TEST / ZEPHYR_EXEC 两个坏行，正式策略新建行配置
3. 项目侧 ExecutionGateway 适配层：指令文件生成器 + 官方导出 CSV 读取器（GBK 解析、共享读容错重试）
4. 数据源主源从 miniQMT 切至 akshare/tushare（miniQMT 行情 9·18 前并行回填历史 tick 囤货）
5. 国金账户加开 PTrade 权限（备胎通道）
6. 程序化交易报备落地
7. 模拟终端导出路径改 `E:\qmt_bridge_sim`（防覆盖，见 §5-A 终端级铁律）；正式连接器落地账号二级分区（`acct_<资金账号>\`）

## 10. 盘中验证清单与备用阶梯（2026-08-26 执行）

### 10.1 盘中验证清单（9:15 集合竞价后逐项打勾）

| # | 步骤 | 预期 | 判定 |
|---|---|---|---|
| 1 | 模拟终端 ZEPHYR_EXEC（模拟模式/实盘模式各一枪）重启，指令单放 1 笔限价单（低于市价 2%，安全挂着） | `SENT` + `ORDER_COUNT≥1` + 委托面板出现该单 | ✅ **2026-08-26 10:06 通过**（实盘模式 T1009，status=50 已报） |
| 2 | 模拟终端【委托】面板肉眼确认策略单存在，状态"已报" | 与手动单并列可见 | ✅ 同日通过（用户肉眼确认+xttrader 交叉验证） |
| 3 | `E:\qmt_bridge_sim\Stock\Order.csv` 出现该委托 | 官方导出同步捕获 | ✅ 同日通过（任务来源=ZEPHYR_EXEC、下单方式=函数下单、投资备注=T1009） |
| 4 | 用策略对该单做撤单测试（如有撤单函数）或手动撤 | 撤单成功 | ✅ 同日 10:09 通过（cancel 签名命中，status=54 已撤，资金解冻） |
| 5 | 成交回报：把限价改到市价附近促成成交，查 `Deal.csv` + 持仓变化 | 成交可见、持仓同步 | ⏳ 未测（验证单故意低于市价不成交，安全设计；正式执行器模拟期首日补测） |

**验证结论（2026-08-26）：进口下单/查单/撤单/幂等回执四动作全部实证通过，文件桥方案正式确立为 miniQMT 替代主通道。下一步进入连接器蓝图阶段（SOP Step 1.5 起）。**

### 10.2 备用阶梯（主力通道失效时的降级路线，逐级启用）

| 级 | 方案 | 自动化程度 | 启用条件 | 工作量 |
|---|---|---|---|---|
| L0 | **大QMT 文件桥（主力）** | 全自动 | 盘中验证通过 | 已完成 90% |
| L1 | **预埋单导入**：项目每日生成预埋单格式指令文件，QMT 交易面板【预埋单】导入 → 一键发送 | 半自动（每天人工点 1 次，约 5 秒） | L0 盘中仍废单 | 当天可用（需先验证国金预埋单文件格式） |
| L2 | **找国金客户经理确认"程序化交易/策略交易"权限开关**：若 L0/L1 均废单，大概率是账户级权限未开而非技术问题 | — | L0 盘中废单且预埋单也不可用 | 一个电话 |
| L3 | **PTrade 通道**：国金加开 PTrade（10 万档），本地大脑暴露只读信号接口，PTrade 托管策略定时拉取下单（需先验证 PTrade 是否允许外发 HTTP） | 全自动 | QMT 体系整体不可用 | 中（策略瘦身+信号接口） |
| L4 | 条件单/手动执行（降级保底） | 手动 | 以上全失效 | — |

### 10.3 过渡期纪律（9·18 前）

- miniQMT 仍为生产主通道（苟到清退日），文件桥并行验证，**验证通过后再切主备**
- 9·18 前用 miniQMT 行情并行回填历史 tick 进 ClickHouse 囤货
- 每日收盘比对：miniQMT 持仓 vs 官方导出 PositionStatics.csv，验证出口数据一致性

## 11. 修订记录

| 版本 | 日期 | 内容 |
|---|---|---|
| 1.0.0 | 2026-08-25 | 初版：miniQMT 清退实证、双向通道架构、SOP、三大坑排障表、合规定位 |
| 1.1.0 | 2026-08-25 | 增补：模拟终端 passorder 下单实证（进口链路通，枚举兼容）；导出设置终端级两铁律（按终端独立、多终端分区防覆盖）；施工清单补模拟终端路径整改项 |
| 1.2.0 | 2026-08-25 | 增补：实证#9-11（"启动本地python"勾选=秒起秒停总根源；夜间程序单静默废单与通道无关，miniQMT 对照组 status=57 铁证；SENT≠落地须回执确认）；排障表扩至六条；新增 §10 盘中验证清单与备用阶梯 L0-L4、过渡期纪律 |
| 1.3.0 | 2026-08-26 | **盘中验证收官**：实证#12-14（下单/撤单全通，cancel 签名命中，userOrderId 进柜台"投资备注"字段）；排障表补 isLastBar 防回放保护与影子实例两条（全案最大根因）；§10.1 验证清单 4/5 项打勾通过，文件桥正式确立为替代主通道；下一步进入连接器蓝图阶段 |
