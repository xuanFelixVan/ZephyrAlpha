# [A_config] module_id=CFG-scripts-model | layer=config | stability=stable | safety=L | ai_autonomy=human_gated
# --- 治理锚定 ---
# blueprint: DOM-GOV-001 | docs/03_modules/_domain-governance/blueprint.md | §
# module_id: DOM-GOV-001
# stability: evolving
# safety_level: L
# ai_autonomy: ai_modifiable
# --- 治理锚定结束 ---
module_id: DOM-GOV-001
schema_version: "2.0"
partition:
  id: scripts
  name: Governance & Audit Scripts
  layer_position: null
  runtime_plane: cold
  view_file: 03-application_architecture.md
  view_section: "§5"

modules:

  # ══════════════════════════════════════════════════════════════�?
  # governance/ �?仓库级文件治理自动化
  # ══════════════════════════════════════════════════════════════�?

  - id: scripts-gov-doc-guard
    name: Doc Guard / 文档 frontmatter 校验
    submodule_path: scripts/governance/doc_guard/
    status: active
    priority: P0
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      文档 frontmatter 校验与合规扫描；
      检查所�?Markdown 文件�?frontmatter schema 合规性�?
      触发时机：pre-commit + CI�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（构建期 / 定时批调度），对�?runtime_planes.yaml gov-factory

  - id: scripts-gov-encoding
    name: Encoding Guard / 文件编码检�?
    submodule_path: scripts/governance/encoding/
    status: active
    priority: P0
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      文件编码检测与强制执行；防�?UTF-8 文件被误判为 GBK/Latin-1 写回导致乱码�?
      触发时机：pre-commit�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（构建期），对应 runtime_planes.yaml gov-factory

  - id: scripts-gov-naming
    name: Naming Convention Check / 命名规范检�?
    submodule_path: scripts/governance/naming/
    status: planned
    priority: P1
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      文件与目录命名规范检查；强制全小�?kebab-case 命名规范（doc-naming-standard.md v2.0.0）�?
      触发时机：pre-commit + CI�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（构建期），对应 runtime_planes.yaml gov-factory

  - id: scripts-gov-memory-pipeline
    name: Memory Pipeline / 结构化记忆生产流水线
    submodule_path: scripts/governance/memory_pipeline/
    status: planned
    priority: P2
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      结构化记忆生产异步流水线（见 OQ-010）；
      将架构决策、知识条目等结构化为 AI 可消费的记忆格式�?
      触发时机：定�?手动触发�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（定时批调度），对应 runtime_planes.yaml gov-factory

  # ══════════════════════════════════════════════════════════════�?
  # audit/ �?仓库级审计与扫描
  # ══════════════════════════════════════════════════════════════�?

  - id: scripts-audit-schema-scan
    name: Schema Scan / frontmatter schema 合规扫描
    submodule_path: scripts/governance/schema_scan/
    status: planned
    priority: P0
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      全文�?frontmatter schema 合规扫描�?
      对全仓库所�?Markdown 文件执行 schema 一致性检查�?
      触发时机：CI (push/PR)�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（CI 批量扫描），对应 runtime_planes.yaml gov-factory

  - id: scripts-audit-link-check
    name: Link Check / 内部链接有效性检�?
    submodule_path: scripts/governance/link_check/
    status: active
    priority: P0
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      内部交叉引用与链接有效性检查；
      检测断链、孤儿文件、引用路径错误�?
      触发时机：CI (push/PR)�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（CI 批量扫描），对应 runtime_planes.yaml gov-factory

  - id: scripts-audit-reports
    name: Audit Reports / 审计扫描结果输出
    submodule_path: scripts/governance/reports/
    status: planned
    priority: P1
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      审计扫描结果输出目录�?
      存放 schema_scan / link_check 等扫描产物（LATEST 覆盖写入模式）�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（批量产物存储）

  # ══════════════════════════════════════════════════════════════�?
  # hooks/ �?pre-commit 与本地守卫钩�?
  # ══════════════════════════════════════════════════════════════�?

  - id: scripts-hooks-pre-commit
    name: Pre-commit Hooks / pre-commit 钩子脚本
    submodule_path: scripts/hooks/pre_commit/
    status: planned
    priority: P0
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      pre-commit 钩子脚本；对暂存文件运行 governance/ + audit/ 检查�?
      触发时机：git commit�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（git commit 触发），对应 runtime_planes.yaml gov-factory

  - id: scripts-hooks-guards
    name: Local Guards / 本地安全守卫
    submodule_path: scripts/hooks/guards/
    status: planned
    priority: P0
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      本地安全守卫（编码守卫、配置守卫等）；
      防止危险操作（如修改风控配置未加前缀、编码损坏文件提交等）�?
      触发时机：git commit�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（git commit 触发），对应 runtime_planes.yaml gov-factory

  # ══════════════════════════════════════════════════════════════�?
  # ci_audit/ �?CI 侧审计与校验逻辑
  # ══════════════════════════════════════════════════════════════�?

  - id: scripts-ci-full-scan
    name: CI Full Scan / CI 全量审计
    submodule_path: scripts/ci_audit/full_scan/
    status: planned
    priority: P1
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      推�?PR 时全仓库审计�?
      对整个仓库执行完整的合规性、链接、schema 检查�?
      触发时机：push / PR�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（CI push/PR 触发），对应 runtime_planes.yaml gov-factory

  - id: scripts-ci-diff-scan
    name: CI Diff Scan / CI 差量审计
    submodule_path: scripts/ci_audit/diff_scan/
    status: planned
    priority: P1
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      仅对变更文件的差量审计；
      提高 CI 效率，只检查本�?push/PR 变更的文件�?
      触发时机：push / PR�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（CI push/PR 触发），对应 runtime_planes.yaml gov-factory

  - id: scripts-ci-reports
    name: CI Audit Reports / CI 审计结果输出
    submodule_path: scripts/ci_audit/reports/
    status: planned
    priority: P1
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      CI 审计结果输出；流�?docs/18_audit_and_evidence/（当前项目路径）�?
      LATEST 覆盖写入模式，不按日期堆积文件�?
    interfaces: []
    kb_ref: []
    plane_note: Cold（CI 批量产物），对应 runtime_planes.yaml gov-factory

  # ══════════════════════════════════════════════════════════════�?
  # 特殊条目：Scout Agent（跨域，治理归属 scripts �?cron�?
  # ══════════════════════════════════════════════════════════════�?

  - id: scripts-gov-scout-d02
    name: Scout Agent Cron / D-02 Scout Agent 定时任务
    submodule_path: src/zephyr/ml_train/scout/
    status: planned
    priority: P2
    runtime_plane: cold
    blueprint_ref: MOD-INF-005
    description: >
      D-02 Scout Agent（每日抓取批任务）；
      cron 定时触发 L11 ml_platform/scout/ 执行外部资讯抓取（arXiv/SSRN/GitHub Trending/业界博客）�?
      所有抓取必须走 AISG（network jail + 防外泄）�?
      治理归属�?09-GOV 全三层（Policy 白名�?/ Factory 脱敏编译�?/ Runtime 拦截审计）�?
    interfaces: []
    kb_ref: [KBG-0009, KBG-0010]
    plane_note: Cold（每�?cron），对应 runtime_planes.yaml gov-scout-d02

summary:
  total: 13
  blueprint_ref: MOD-INF-005
  blueprint_version: "1.0.0"
  by_status:
    planned: 10
    active: 3
    deferred: 0
    deprecated: 0
  by_priority:
    P0: 6
    P1: 5
    P2: 2
    P3: 0
  by_runtime_plane:
    hot: 0
    warm: 0
    cold: 13
