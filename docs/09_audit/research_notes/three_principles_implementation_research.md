---
title: Three Principles Implementation Research
module_id: GOV-065---

﻿--- module_id: DRAFT-ANALYSIS-001 title: ZephyrAlpha 三条AI架构铁律 —— 业界调研与完整落地方案 doc_type: analysis status: Draft version: "1.0.0" date: "2026-05-04" summary: >   对原则一(AI找得到>人类看得懂)、原则二(能自动生成的不手工写)、   原则三(一份标准只放在AI加载它的地方)的业界对标研究，以及完整落地路径。   Vibe Coding 语境，按目标任意增长模块量，99% AI自治维护。 drafted_by: AI_Chief_Architect ---  # ZephyrAlpha 三条AI架构铁律 —— 业界调研与完整落地方案  > **受众声明**：本文首要受众为 AI（后续执行者），次要受众为人类 Owner。 > AI 读本文时应能零推理理解"业界怎么做、我们差在哪、要改什么、怎么改"。 > 人类读本文时应能理解"为什么这么做、要花多久、会变成什么样"。  ---  ## 一、核心问题：业界到底有没有这么做？  ### 直接回答：有。而且不是"有人这么做"——是"所有顶级机构和社区都这么做"。  三条原则不是 ZephyrAlpha 的发明，而是 AI 驱动开发的**工业共识**。区别只在于：专业机构不把原则写成 prose 规则——他们把原则编码成**目录结构、工具链、加载机制**。"规则即结构，结构即规则"。  ---  ## 二、逐原则业界对标  ### 原则一：AI 找得到 > 人类看得懂  #### 业界证据（逐个机构）  | 机构 | 他们的做法 | 对标程度 | |------|-----------|:------:| | **Anthropic / Claude Code** | CLAUDE.md 是给 Claude 读的系统指令，不是给人类读的文档。官方指南明确："首要受众是 AI"。CLAUDE.md 可以有 managed policy / project / user / local 四层，加载堆叠。**目录结构不重组，加载机制代替重组**。 | ✅ 100% | | **Cursor** | `.cursor/rules/*.mdc` 是系统级 AI 指令。四种加载模式：alwaysApply（始终）、Apply Intelligently（按描述匹配）、Apply to Specific Files（按 glob）、Apply Manually（@-mention）。**目录结构不重组，glob 模式 + alwaysApply 代替重组**。 | ✅ 100% | | **Windsurf** | `.windsurf/rules/` 是 AI 规则目录。纯 markdown，无 frontmatter。简单到极致："不是给人类读的漂亮文档，是给 AI 的命令清单"。**目录结构不重组，单文件 + 子目录规则覆盖**。 | ✅ 100% | | **Kubernetes** | CRD YAML 放 `config/crd/bases/`——这是 API Server 消费它的地方，不是人类阅读它的地方。人类的可读版本 (`kubectl explain`) 是自动生成的。**结构跟着消费者走**。 | ✅ 100% | | **OpenAPI** | `spec.yaml` 放仓库根——这是 CI 和代码生成器消费它的地方。Swagger UI（人类版）是自动派生的。**spec 的位置 = 机器消费它的位置**。 | ✅ 100% | | **Terraform** | `.tf` 文件放模块根目录——Terraform CLI 消费它的地方。`terraform-docs` 自动生成 README.md。**代码在哪，文档就从哪生成**。 | ✅ 100% | | **GitHub Copilot** | `.github/copilot-instructions.md` 放 `.github/`——AI 编码代理自动加载。不要求在 README 里重复写。 | ✅ 100% | | **AGENTS.md 标准** | 由 Sourcegraph 发起，OpenAI、Google、GitHub 支持的**厂商中立标准**。AGENTS.md 放仓库根目录——所有 AI 编码代理自动在那里找。你已经在用 AGENTS.md，走在了行业前面。 | ✅ 100% |  #### 关键发现  **没有一家机构为了"AI 找得到"而重组整个目录结构。** 他们用的是**加载机制**（glob、alwaysApply、目录树向上查找、progressive disclosure）来让 AI 找到需要的东西，目录结构仍然按人类可理解的逻辑组织。  **为什么？** 因为： 1. 目录结构重组成本极高（你的项目有 82 个目录、139+ 文档、80+ 脚本，重组 = 全量重写依赖链） 2. 加载机制已经解决了"AI 找到东西"的问题（你的 §8.2 任务菜单就是加载机制） 3. 人类 Owner 仍然需要靠目录结构找到东西（§5 画像）  #### 原则一的精确落地方式  原则一不应该翻译为"重组目录结构"，而应该翻译为：  > **"AI 每次加载文件的物理位置，必须由加载机制自动决定——不能依赖 AI 在文件系统中自行搜索。"**  即：**强化 §8.2 任务菜单为不可漂移的 SSoT**，而不是重组物理目录。  ---  ### 原则二：能自动生成的不手工写  #### 业界证据（逐个机构）  | 机构 | 他们的做法 | 对标程度 | |------|-----------|:------:| | **Kubernetes** | `code-generator` 是所有 boilerplate 的唯一产出方式。deepcopy、clients、informers、listers 全部自动生成（文件名 `zz_generated.*.go`）。**没有"手工写的客户端代码"这个选项**——手工写的会被生成器覆盖。 | ✅ 200%（最激进） | | **Terraform** | `terraform-docs` 自动从 `.tf` 文件中提取 variables/outputs/resources → 生成 README.md。注入到 `<!--- BEGIN_TF_DOCS --->` 和 `<!--- END_TF_DOCS --->` 之间。**手工编辑区域之外的文档会被覆盖**。pre-commit hook 强制文档最新。 | ✅ 150% | | **OpenAPI** | `spec.yaml` → 自动生成：客户端 SDK（多语言）、服务端 stub、Mock 服务器、API 文档。**手工写的 SDK = 废品**。 | ✅ 200% | | **Anthropic** | `/init` 命令自动扫描代码库 → 生成 CLAUDE.md。Skills 的 `SKILL.md` 是手工写的 Schema，但加载和激活是自动的。**从代码到规则的映射是自动的**。 | ✅ 80% | | **Cursor** | `/create-rule` 在 agent 中描述需求 → agent 自动生成 `.mdc` 规则文件。规则文件的格式化由 `rule-generating-agent.mdc` 自动完成。 | ✅ 60% | | **Windsurf** | 没有自动生成机制——这是 Windsurf 有意为之的"极简主义"。12,000 字符限制是靠纪律而非工具强制。 | ⚠️ 0%（有意不这么做） |  #### 关键发现  专业机构处理"自动生成 vs 手工维护"的分界线非常清晰：  | 内容类型 | 业界做法 | |---------|---------| | **重复性清单**（脚本列表、模块列表、枚举值列表、文件清单、入口计数） | 🤖 永远自动生成，禁止手工维护 | | **Schema / 契约定义**（字段名、类型、约束、允许值） | ✋ Schema 是手工写的决策文档，但**校验和派生是自动的** | | **叙述性文档**（架构决策记录、设计说明、为什么这样做） | ✋ 手工写，但关键数字（如"14 层分层"）从 YAML SSoT 自动注入 | | **代码**（业务逻辑） | ✋ 手工写 / AI 生成，但测试和 lint 是自动的 |  **核心模式**：**Schema（手工决策）→ 生成器 → 派生文件（机器产物）+ CI 校验（防止漂移）**  #### 原则二的精确落地方式  原则二应该翻译为三个子规则：  > **2a. 重复性清单绝对禁止手工维护。** 任何包含"条目列表 + 条目计数"的文件，要么是脚本的输出（标注 `generated_at`），要么是 Schema 的输入（标注 `schema_input: true`，由脚本人读取并生成输出）。  > **2b. 区分"决策清单"和"事实清单"。** 决策清单 = 人类定义 Schema（如"哪些维度需要审计"），脚本校验一致性。事实清单 = 脚本扫描磁盘自动生成（如"磁盘上实际有哪些 .py 文件"）。  > **2c. 自动生成的标记必须显式。** 任何自动生成的文件必须在开头标注 `generated_at: 2026-05-04T12:00:00Z`。任何 Schema 输入文件必须标注 `schema_input: true` 和 `consumed_by: [生成器脚本路径]`。  ---  ### 原则三：一份标准只放在 AI 加载它的地方  #### 业界证据（逐个机构）  | 机构 | 他们的做法 | 对标程度 | |------|-----------|:------:| | **Kubernetes** | Go struct（`types.go`）中的 `// +k8s:deepcopy-gen` 标记是 Schema——放在**代码生成器消费它的地方**（和类型定义在一起）。生成的 `zz_generated.*.go` 也放在同一个 package 下——**运行时消费它的地方**。没有"一份放 api/ 一份放 docs/"的模式。 | ✅ 100% | | **OpenAPI** | `spec.yaml` — 一份文件，放仓库根目录。CI 读它做 lint，代码生成器读它生成 SDK，Swagger UI 读它渲染文档。**一份 spec，多个消费者，不搞多份拷贝**。 | ✅ 100% | | **Anthropic / Claude Code** | `CLAUDE.md` 按位置层级加载（managed → project → user → local），每个位置**只能有一份**。如果要拆分，用 `.claude/rules/` 按 glob 激活——不是"复制同一份内容到多个文件"。**一份内容 = 一个加载点**。 | ✅ 100% | | **Cursor** | `.cursor/rules/*.mdc` 按 `alwaysApply` / `globs` / `description` 自动激活。如果同一规则需要在两个场景激活 → 设置 `alwaysApply: true`，或设置更宽的 `globs`——**不复制规则文件**。 | ✅ 100% | | **Windsurf** | 单一 `.windsurf/rules/` 文件全局生效。**最激进的"一份标准只放一个地方"**——只有一个地方。 | ✅ 150% | | **Terraform** | `.tf` 文件是 canonical SSoT。`README.md` 是 `terraform-docs` 自动生成的派生视图。**canonical 放 CLI 消费它的地方（模块根），派生放人类消费它的地方（README.md）**。派生标注了注入标记，所以人类知道"这是自动生成的，不要手动编辑"。 | ✅ 100% |  #### 关键发现：业界处理 "canonical vs 派生摘要" 的模式  所有机构都面临同一个问题：**同一个信息，AI 需要在一个地方读到完整版，人类需要在另一个地方看到摘要版。怎么处理？**  **答案：派生，不是复制。**  ``` 业界标准模式：  [Canonical 定义]          →  [自动派生/生成]  →  [人类视图]   (AI 加载点)                                     (人类加载点)   types.go                →  code-generator   →  zz_generated.*.go   spec.yaml               →  swagger-codegen  →  Swagger UI   variables.tf            →  terraform-docs   →  README.md (注入段)   CLAUDE.md (project)     →  加载机制         →  不需要第二份人类视图 ```  **关键区分**： - ❌ 反模式：手工写一份 canonical → 手工再写一份摘要 → 两份会漂移 - ❌ 反模式：canonical 放在冷记忆 → AI 不加载 → 靠"应该记得"来对齐 - ✅ 模式 A：canonical 放在 AI 加载点 → 人类视图自动派生（Terraform 模式） - ✅ 模式 B：只有一份，放 AI 加载点，人类也能读（Cursor/Windsurf 模式） - ✅ 模式 C：canonical 放 AI 加载点，内容本身就是"AI可消费 + 人类可读"（Anthropic CLAUDE.md 模式）  #### 原则三的精确落地方式  > **3a. Canonical 定义必须在 AI 每次加载的文件中。** 如果一份规则 AI 需要读，它就不得是 §8.3 冷记忆。  > **3b. 如果同一信息需要出现在两个 AI 加载点，使用自动派生而非手工复制。** 派生摘要必须标注 `derived_from: {canonical_path}` 和 `derived_version: {version}`。派生摘要的版本号落后 canonical → CI 警告。  > **3c. 审查当前所有"canonical + 摘要"对。** 如果 AGENTS.md 里的摘要已经足够完整，删掉独立的冷记忆 canonical 文件——少一份文件 = 少一个漂移风险。  ---  ### 延伸问题 A：Markdown 记录规则的效率够吗？业界用什么？  **短答案**：业界 AI 工具 100% 使用 Markdown（或其变体 `.mdc`）。不慢——但写法决定了效率。  **详细证据**：  | 工具 | 规则格式 | 文件扩展名 | 结构特点 | |------|---------|----------|---------| | **Cursor** | YAML frontmatter + Markdown body | `.mdc` | 必填字段：`description`（≤120字符）+ `globs` + `alwaysApply`。body 区用 bullet points 不用段落。推荐 ≤50 行。 | | **Claude Code** | 纯 Markdown + YAML frontmatter | `.md` | 推荐 ≤200 行。`@path` 可导入外部文件。`<!-- HTML注释 -->` 会在注入上下文前剥离——人类可读但 AI 不浪费 token。 | | **Windsurf** | 纯 Markdown | `.md` | 无 frontmatter。12,000 字符硬限制。最激进极简主义。 | | **Claude Skills** | Markdown (`SKILL.md`) + 资源文件 | `.md` | Progressive disclosure 三层加载。Metadata 在文件名约定里而非 frontmatter 里。 |  **关键洞察**：Markdown 本身不是瓶颈——**怎么组织才是**。  | 你的当前状态 | 业界最佳状态 | 差距 | |-------------|-------------|------| | `behavior_boundaries_standard.yaml`：52 ABS + 45 COND，整份加载 | Cursor single `.mdc` ≤50 行，按 glob 激活 | 🔴 你的规则文件是"人类长文"不是"AI 短指令" | | `trae_028_doc_structure_naming.yaml`：独立的长文 canonical | Claude Code：直接写在 CLAUDE.md 里，≤10 行 bullet points | 🔴 冷记忆 canonical = AI 永远不会读的规则 | | AGENTS.md 已 ≥500 行 | Anthropic 建议 ≤200 行 | 🟡 你在超载线以上 |  **拍板**：不需要改变文件格式（Markdown 是行业标准），但必须改变**写法和加载粒度**—— - 规则应该是**短 bullet points**（AI 指令），不是**长段落**（人类文档） - 每条规则 ≤5 行，`alwaysApply` 的核心规则 ≤15 条 - 详细解释、边界条件、示例 → 放在领域触发文件里，不放在热记忆里  ---  ### 延伸问题 B：能不能把原则编码成工具链和加载机制，而不是写成 prose 规则？  **短答案**：能。而且这才是业界真正在做的事。  **业界做法对比**：  | 他们不写这个 prose 规则 | 他们做了这个工具/机制 | 对标你的什么 | |------------------------|---------------------|------------| | "CRD 的 schema 变更必须经过校验" | K8s ValidationAdmissionPolicy：判定逻辑写成 CEL 表达式 → API Server 运行时执行。**不存在"忘了校验"这个选项** | → 对标你的原则二（静态清单不能手工写） | | "terraform-docs 必须在 commit 前跑" | Terraform pre-commit hook：自动运行 `terraform-docs` → 如果 README.md 不是最新的 → commit 硬阻断 | → 对标你的原则二（不手工写文档）+ 门禁 | | "规则文件放在 AI 加载的位置" | Cursor `alwaysApply: true` 在前置 matter 里定义 → AI 引擎自动加载。**不存在"忘了放置规则"这个选项** | → 对标你的原则三（canonical 在加载点） | | "不要写长篇 prose 规则" | Cursor `.mdc` 模板强制 ≤50 行 + code example 优先 | → 本期新增（规则格式规范） |  **ZephyrAlpha 对应的编码方式**：  ``` 三条原则的 prose → 工具链编码：  原则一（AI 找得到 > 人类看得懂）：   prose: "§8.2 是唯一入口"   → 编码为: GATE-22（§8.2 路径映射完整性检查）     每 commit 跑 → 路径断 → 失败 → AI 收到诊断 → 修     AI 不需要"记得"原则一，CI 替它记  原则二（能自动生成的不手工写）：   prose: "静态清单禁止手工维护"   → 编码为: GATE-19（静态清单漂移阻断）     + __manifest__ Schema（每个 .py 写结构化元数据）     + 生成器脚本（从 Schema 自动产出清单）     人类/AI 手工改清单 → CI 对比发现不一致 → 硬阻断  原则三（canonical 在加载点）：   prose: "canonical 定义必须在热记忆"   → 编码为: GATE-21（canonical 位置审查）     + frontmatter 字段 canonical: true + load_level: hot|triggered|cold     CI 扫描所有标注 canonical: true 的文件 → 如果有 load_level: cold → 警告 ```  **关键转变**：  ``` 落地前：   "§6.16 说静态清单要自动生成" ← AI 读了 prose → AI 自己记得 → AI 手动执行 落地后：   GATE-19 在 pre-commit 里跑 → 发现手工维护的清单 → CI 失败   ← prose 仍然存在（人类决策审查用），但 AI 不需要靠"读了 prose"来执行 ```  **prose 规则的剩余角色**： - **人类 Owner 决策**：prose 是 Owner 能读懂的"为什么这样设计"——但日常 AI 不读 - **新 AI 入门**：CI 第一道失败 → AI 读 prose 理解"从 Schema 到生成物"的契约 → 执行修复 - **架构评审**：人类 Audior 对照 prose 检查 CI 门禁是不是在正确的地方  ---  ### 延伸问题 C：1500 模块是一个错误框架  > **Owner 指出**："1500 是预估不是硬限——一切是为目标而前进。模块数量根据目标变化而变化。"  **Owner 完全正确。** 修正如下：  | 错误的表述（旧方案） | 正确的表述（修正后） | |-------------------|-------------------| | "确认项目满足 1500 模块不膨胀" | "确认架构的维护复杂度不随模块数线性增长" | | "1500+模块的容量结构设计" | "随业务目标任意增长的模块量，架构保证 O(1) 维护" | | "锁死 1500 模块" | "锁死的是**自动生成+CI门禁**的覆盖率和完整性，不是模块数" |  **修正后的架构承诺**：  > ZephyrAlpha 的架构不承诺"支持 N 个模块"——它承诺： > > **无论业务目标驱动多少模块（200 / 2000 / 20000），新增模块的治理维护成本（清单更新、索引对齐、契约校验）恒定为 O(1)**——因为全部是自动生成+CI 阻断，手工维护步骤为 0。 > > 模块数量由业务目标决定，不由架构限制决定。架构只保证：规模增长不带来维护负担的增长。  **与之对应的验收标准**： - ❌ "支撑 1500 模块"（数字驱动，违反原则） - ✅ "新增模块 → 只需要运行 1 条生成命令 → 全量清单自动更新 → CI 通过"（流程驱动） - ✅ "守护机制（门禁数量 + 生成器脚本 + Schema 契约）总复杂度不随模块数增长"（O(1) 驱动）  ---  ### 延伸问题 D：那么规则到底该写成什么样？  **新标准（对标 Cursor .mdc + Claude CLAUDE.md + Windsurf）：**  ``` 热记忆规则（AGENTS.md §4~§7）：每条 ≤5 行 bullet points，禁止段落   目标：AI 一屏读完，零推理   格式：编号 + 动词开头 + 对标引用  领域触发规则（§8.2 映射的文件）：结构化的 short-form   目标：AI 按任务加载，不超过当前任务范围   格式：YAML frontmatter（load_level, triggered_by）+ ≤50 行 body  冷记忆：详细解释 + 历史背景 + 行业对标   目标：人类 Owner 审阅 + AI 遇到问题时的"字典"   格式：可以是长 Markdown，但 frontmatter 必须标明 canonical: false ```  ---  ## 三、ZephyrAlpha 当前差距分析  ### 差距矩阵  | 维度 | 当前状态 | 业界标准 | 差距等级 | |------|---------|---------|:------:| | **AI 加载机制** | §8.2 任务菜单（极好） | glob / alwaysApply / progressive disclosure | 🟢 达标 | | **格式 AI 友好度** | §6.12 YAML canonical SSoT（极好） | Machine-first canonical | 🟢 达标 | | **目录结构** | 按人类主题分类 | 按人类主题分类（业界一致） | 🟢 不需要改 | | **脚本清单自动生成** | `script-manifest.yaml` ✋ 手工维护 101 条 | 🤖 自动扫描生成 | 🔴 P0 差距 | | **登记表清单自动生成** | `registry-master-index.yaml` ✋ 手工维护 38 条 | 🤖 自动扫描生成 | 🔴 P0 差距 | | **模块 ID 自动生成** | `module_id_registry.yaml` ✋ 手工维护 68 条 | 🤖 从架构 YAML 自动派生 | 🟡 P1 差距 | | **门禁列表自动生成** | `gate-registry.md` ✋ 手工维护 42 条 | 🤖 从 `.pre-commit-config` 自动派生 | 🟡 P1 差距 | | **索引数字自动生成** | 部分 index.md 手工维护数字 | 🤖 全部自动扫描生成 | 🟡 P1 差距 | | **Canonical 位置审查** | 存在"热记忆摘要 vs 冷记忆 canonical"双源 | Canonical 在加载点，派生自动生成 | 🔴 P0 差距 | | **静态清单门禁** | 无 | validate_index_reality.py 等（已有部分） | 🟡 P1 差距 | | **规则文件写法效率** | 规则是"人类长文"（52 ABS + 45 COND 整份 prose 加载） | ≤50行 bullet points + 按 glob 激活 | 🔴 P0 差距 | | **热记忆超载** | AGENTS.md ≥500 行 | Anthropic 建议 ≤200 行 | 🟡 P1 差距 |  ---  ## 四、"好的标准"是什么样子  ### 4.1 定义：三条原则落地后的目标状态  #### 目标状态 A：静态清单的生存法则  ``` 项目中的任何文件，如果其主要内容是"列举一组条目"（无论是 YAML 列表、Markdown 表格、还是数字计数）， 它必须属于以下两类之一：  A 类（生成物）：   - 文件名不以 _schema 结尾   - 文件开头有 generated_at 字段   - 由指定的生成脚本产出   - AI 和人类均不得手工编辑此文件   - CI 门禁：对比自动生成版 vs 磁盘版，不一致 → 阻断  B 类（Schema 输入）：   - 文件名为 *_schema.yaml 或标注 schema_input: true   - 由人类（通过 AI）定义字段和约束   - 是生成脚本的输入数据   - 可以手工编辑   - 变更后 → 必须运行对应生成脚本同步所有派生文件 ```  #### 目标状态 B：Canonical 的物理位置铁律  ``` 对于任何"一个概念需要在多个上下文中被知晓"的信息：  规则 1（Canonical 归位）：   - Canonical 定义文件必须在 §8.2 热记忆或领域触发清单中   - Canonical 定义文件不得在 §8.3 冷记忆中   - 如果发现 canonical 在冷记忆 → 立即提 KB 决策记录：要么移入热记忆，要么降级为非 canonical  规则 2（派生不复制）：   - 其他文件引用 canonical 内容时使用 derived_from 标注   - 不复制 canonical 内容到其他文件（即使是"摘要"）   - 如果 AI 场景确实需要摘要 → 由生成脚本从 canonical 自动提取  规则 3（一对多 = 生成，不是复制）：   - 一个 canonical → N 个消费点需要同一信息 → N 个消费点放自动生成的派生摘要   - 派生摘要标注 derived_from + derived_version   - 派生摘要落后 canonical → CI 警告/阻断 ```  #### 目标状态 C：AI 加载路径的不可漂移性  ``` §8.2 任务菜单是"AI 找到文件"的唯一入口。  规则：   - 任何文件创建/移动/删除 → 如果该文件属于 §8.2 加载范围，AI MUST 同步更新 §8.2   - 任何文件路径变更 → AI MUST 立即运行 grep 检查 §8.2 中是否引用了旧路径   - 任何新增规则文件 → AI MUST 判断是否需要在 §8.2 新增任务类型或更新现有任务的加载路径 ```  ---  ### 4.2 目标架构图  ``` ═══════════════════════════════════════════════════════════════════                     ZephyrAlpha 规则体系（目标状态） ═══════════════════════════════════════════════════════════════════  ┌───────────────────────────────────────────────────────────────┐ │  热记忆（AI 每次自动加载）                                      │ │  ┌─────────────────────────────────────────────────────────┐  │ │  │  AGENTS.md（根级）                                       │  │ │  │  ├── §4  编码安全（canonical SSoT，完整版，非摘要）        │  │ │  │  ├── §5  画像 + 认知特征                                  │  │ │  │  ├── §6  AI 施工执行原则（含三条铁律的完整定义）            │  │ │  │  ├── §7  角色定位                                          │  │ │  │  └── §8  规则加载策略（任务菜单 = 不漂移的 SSoT）           │  │ │  └─────────────────────────────────────────────────────────┘  │ ├───────────────────────────────────────────────────────────────┤ │  领域触发（按 §8.2 任务类型加载）                               │ │  ┌─────────────────────────────────────────────────────────┐  │ │  │  📁 docs/01_policies_and_standards/                     │  │ │  │  ├── 📁 meta/        → 元规则（constitution, glossary）    │  │ │  │  ├── 📁 governance/  → 治理规则（按治理领域分，不按任务分） │  │ │  │  ├── 📁 _registry/                                       │  │ │  │  │   ├── 📁 catalogs/     → 🤖 全部自动生成的登记表       │  │ │  │  │   ├── 📁 vocabularies/  → ✋ Schema 输入（手工定义）    │  │ │  │  │   ├── 📁 contracts/    → ✋ Schema 输入（手工定义）     │  │ │  │  │   └── 📁 schemas/      → ✋ Schema 输入（手工定义）     │  │ │  │  └── 📁 templates/    → 模板（蓝图模板、文档模板）         │  │ │  └─────────────────────────────────────────────────────────┘  │ ├───────────────────────────────────────────────────────────────┤ │  冷记忆（遇到问题才查）                                         │ │  ┌─────────────────────────────────────────────────────────┐  │ │  │  不包含任何 canonical SSoT                                │  │ │  │  只包含：领域细节、扩展阅读、历史分析                        │  │ │  │  任何 canonical → 已移入热记忆或领域触发                    │  │ │  └─────────────────────────────────────────────────────────┘  │ ├───────────────────────────────────────────────────────────────┤ │  生成管道（CI 强制执行）                                        │ │  ┌─────────────────────────────────────────────────────────┐  │ │  │  Schema 输入              生成器                  输出     │  │ │  │  ───────────          ──────────           ──────────   │  │ │  │  scripts/governance/  generate_script_    script_        │  │ │  │  **/*.py              manifest.py ─────→   manifest.yaml │  │ │  │  (__manifest__ 块)                                   │  │ │  │                                                          │  │ │  │  _registry/           generate_registry_ registry_       │  │ │  │  catalogs/*.yaml      master_index.py───→ master_index.  │  │ │  │  (frontmatter)                               yaml        │  │ │  │                                                          │  │ │  │  architecture_model/  generate_module_    module-id-      │  │ │  │  layers/lXX.yaml      id_registry.py ──→ registry.yaml  │  │ │  │                                                          │  │ │  │  .pre-commit-         generate_gate_     gate-            │  │ │  │  config.yaml          registry.py ────→  registry.yaml  │  │ │  └─────────────────────────────────────────────────────────┘  │ ├───────────────────────────────────────────────────────────────┤ │  门禁层（pre-commit + CI）                                      │ │  ┌─────────────────────────────────────────────────────────┐  │ │  │  GATE-19: 静态清单漂移阻断                                 │  │ │  │    → 自动生成版 vs 磁盘版，不一致 → CI 失败                  │  │ │  │  GATE-20: 派生版本落后检测                                  │  │ │  │    → derived_version < canonical_version → CI 失败         │  │ │  │  GATE-21: Canonical 位置审查                               │  │ │  │    → canonical 定义在冷记忆 → CI 警告                       │  │ │  └─────────────────────────────────────────────────────────┘  │ └───────────────────────────────────────────────────────────────┘ ```  ---  ## 五、完整落地路径  ### 5.1 阶段规划（分 4 阶段，降低风险）  ``` 阶段 1（本 session）：规则写入 + 审查启动 ├── 步骤 1.1：AGENTS.md 写入三条原则的精确表述（§6.16~§6.18） ├── 步骤 1.2：AGENTS.md §8.2 审查——确保路径映射覆盖所有当前文件 ├── 步骤 1.3：启动 canonical 位置审查——列出所有"热记忆摘要 vs 冷记忆 canonical"对 └── 步骤 1.4：产出本方案文档（本文件）  阶段 2（下个 session）：Schema 定义 + 第一批生成器 ├── 步骤 2.1：定义脚本元数据 Schema（__manifest__ 块格式标准） ├── 步骤 2.2：定义登记表元数据 Schema（frontmatter 必填字段标准） ├── 步骤 2.3：实现 generate_script_manifest.py ├── 步骤 2.4：实现 generate_registry_master_index.py ├── 步骤 2.5：实现 generate_module_id_registry.py（从 architecture_model/ YAML 派生） ├── 步骤 2.6：实现 generate_gate_registry.py（从 .pre-commit-config.yaml 派生） ├── 步骤 2.7：为 80+ 个脚本添加 __manifest__ 块 └── 步骤 2.8：运行生成器，提交自动生成版到磁盘  阶段 3（第三个 session）：门禁上线 + 漂移免疫闭环 ├── 步骤 3.1：实现 GATE-19（静态清单漂移阻断） ├── 步骤 3.2：实现 GATE-20（派生版本落后检测） ├── 步骤 3.3：实现 GATE-21（Canonical 位置审查） ├── 步骤 3.4：集成到 .pre-commit-config.yaml ├── 步骤 3.5：集成到 CI governance.yml └── 步骤 3.6：跑通全量测试 + 修复误报  阶段 4（第四个 session）：精简审查 + 无限维持 ├── 步骤 4.1：审查所有 cold memory 文件——每个文件问"如果 AI 不加载它，为什么存在？" ├── 步骤 4.2：删掉冗余的 canonical（热记忆已覆盖的→删冷记忆版） ├── 步骤 4.3：删掉无入边引用的文档（orphan documents） ├── 步骤 4.4：全量 index.md 改为自动生成 └── 步骤 4.5：最终审查——确认自动生成+CI门禁覆盖率 100%，维护复杂度 O(1) 不随模块数增长 ```  ---  ### 5.2 阶段 1 详细任务（本 session 执行）  #### 任务 1.1：AGENTS.md 写入三条铁律  **位置**：在现有 §6.15 之后追加三个新子节。  **§6.16 静态清单自动生成铁律（对应原则二）**：  ```markdown ### 6.16 静态清单自动生成铁律（Static Manifest Auto-Generation Mandate）  > **v1.0.0**：触发条件——任何包含"条目列表 + 条目计数"的文件。 > 对标 K8s code-generator / Terraform terraform-docs / OpenAPI codegen。 > 五家顶级机构一致：重复性清单物理上不允许手工维护。  **核心原则**：项目中的任何文件，如果其主要内容是列举一组条目或计数，必须是以下两类之一： - **A 类（生成物）**：由脚本从 Schema 输入自动扫描生成，标注 `generated_at` - **B 类（Schema 输入）**：手工定义数据结构，是生成脚本的输入，标注 `schema_input: true`  **绝对禁止**： - ❌ 手工维护任何包含条目计数的清单 - ❌ 手工在 index.md 中写"本目录含 N 个文件" - ❌ "以后改成自动生成"——不存在"以后"  **两分类法（必须精确判定）**： - **事实清单**（磁盘有什么 → 脚本扫描得知）：永远自动生成 - **决策清单**（人类决定应该有什么 → Schema 定义）：手工定义 Schema，脚本校验一致性  **专业参考**：K8s code-generator → zz_generated.*.go / Terraform terraform-docs → auto-injected README.md / OpenAPI spec → auto-generated SDKs ```  **§6.17 Canonical 物理位置铁律（对应原则三）**：  ```markdown ### 6.17 Canonical 物理位置铁律（Canonical Physical Location Mandate）  > **v1.0.0**：触发条件——每次定义任何"规则/标准/契约"的 canonical 文件时。 > 对标 OpenAPI spec.yaml 放 CI 消费点 / K8s types.go 放代码生成器消费点 / Cursor CLAUDE.md 放自动加载点。  **核心原则**：Canonical 定义的物理位置 = AI 加载它的位置。 不是"放在某处，靠人类手动引用"——放在 AI 必然读到的地方。  **规则**： 1. Canonical 定义文件必须在 §8.2 热记忆或领域触发清单中 2. Canonical 定义文件不得在 §8.3 冷记忆中 3. 如果发现 canonical 在冷记忆 → P0 缺陷，必须在同一 session 内提 KB 决策记录 迁移 4. 消费者需要同一信息的 → 自动派生摘要（标注 derived_from + derived_version），不复制  **与 §6.9 SSoT 的关系**： - §6.9 定义"谁有权定义"（canonical 权威） - §6.17 定义"定义放在哪"（物理位置） - 两者互补：canonical = 有权威 + 在加载点  **专业参考**：OpenAPI spec.yaml → 一份文件，CI/SDK生成器/文档生成器都从这里读 / K8s types.go → 代码生成器的输入源 / Claude Code CLAUDE.md → 自动加载的唯一位置 ```  **§6.18 AI 加载路径不可漂移铁律（对应原则一）**：  ```markdown ### 6.18 AI 加载路径不可漂移铁律（AI Load Path Anti-Drift Mandate）  > **v1.0.0**：触发条件——任何文件创建/移动/删除/重命名操作。 > 对标 Cursor globs + alwaysApply + description 三层激活机制。  **核心原则**：§8.2 任务菜单是 AI 找到规则文件的**唯一不可漂移入口**。 AI 不得依赖"搜索文件系统"来找规则——路径映射必须在 §8.2 中显式声明。  **规则**： 1. 任何规则文件创建/移动/删除 → AI MUST 同步更新 §8.2 任务菜单 2. 任何文件路径变更 → AI MUST 立即 grep 检查 §8.2 中是否引用旧路径 3. 任何新增规则文件 → AI MUST 判定归属的任务类型并在 §8.2 添加路径 4. 任何不再有效的路径 → AI MUST 立即从 §8.2 移除  **为什么不是重组目录结构**： - 业界一致做法：加载机制决定"AI 读什么"（globs / alwaysApply），   目录结构仍按人类逻辑组织（主题分类） - 重组目录的边际收益极低（§8.2 已实现按任务加载），   但破坏性极大（82 目录 × 139+ 文档 × 80+ 脚本的全量引用链重构）  **专业参考**：Claude Code → CLAUDE.md 多位置加载（managed/project/user/local）   / Cursor → globs 模式条件激活 / Windsurf → 目录树向上查找 .windsurf/rules ```  #### 任务 1.2：启动 canonical 位置审查  创建审查清单，列出当前所有"热记忆摘要 vs 冷记忆 canonical"对：  | 摘要位置（热记忆） | Canonical 位置（冷记忆） | 裁决建议 | |-------------------|------------------------|---------| | AGENTS.md §4 编码安全硬规则 | `governance/document/trae_028_doc_structure_naming.yaml`（**§8.3 冷记忆**） | 🔴 **P0 已确认**：canonical 在冷记忆，违反 §6.17。裁决——AGENTS.md §4 升级为完整 canonical，`trae_028_doc_structure_naming.yaml` 降级为扩展阅读（标注 `derived_from: AGENTS.md §4`） | | AGENTS.md §6.9 YAML Canonical SSoT | `architecture_principles.md` 部分内容重叠 | 🟡 待审查：确认 §6.9 是否已完整覆盖 architecture-principles 中的 canonical 表述 | | AGENTS.md §6.12 AI受众优先 | `document_structure_standard.yaml` / PS-STD-011 部分内容对齐 | 🟢 §6.12 已足够完整，冷记忆文件中的重叠内容建议标注 `canonical: false` | | AGENTS.md §4 另注 | `behavior_boundaries_standard.yaml`（PS-STD-003，在 §8.2 按需查） | 🟡 52 ABS + 45 COND 整份 prose 加载，未被 §8.2 作为必读全文。属于"规则写法效率"差距，非位置问题 |  #### 任务 1.3 执行结果：§8.2 路径映射完整性审查  执行日期：2026-05-04。逐条验证 §8.2 任务菜单中所有引用的文件路径：  | 路径 | 状态 | 备注 | |------|:--:|------| | `meta/index.md` | ✅ | `D:\ZephyrAlpha\docs\01_policies_and_standards\meta\index.md` | | `_registry/vocabularies/glossary.yaml` | ✅ | | | `meta/rule_lifecycle_and_change_standard.yaml` | ✅ | | | `meta/document_structure_standard.yaml` | ✅ | | | `meta/metadata_registry.yaml` | ✅ | | | `meta/rule_classification_and_arbitration_standard.yaml` | ✅ | | | `meta/rule_verification_standard.yaml` | ✅ | | | `meta/governance_metrics_standard.yaml` | ✅ | | | `meta/governance_methodology_standard.yaml` | ✅ | | | `_registry/catalogs/declarative-contract-tracker-registry.md` | ✅ | | | `_registry/catalogs/registry-master-index.yaml` | ✅ | | | `_registry/catalogs/_index.yaml` | ✅ | | | `_registry/catalogs/gate-registry.md` | ✅ | | | `capabilities.yaml` | ✅ | `D:\ZephyrAlpha\config\capabilities.yaml` | | `scripts/governance/quality-standard.md` | ✅ | | | `scripts/governance/index.md` | ✅ | | | `scripts/governance/script-manifest.yaml` | ✅ | | | `validate_config_integrity.py` | ❌→✅ | **已修复**：原引用缺少 `d1_structure/` 子目录，已补全为 `scripts/governance/d1_structure/validate_config_integrity.py` | | `.pre-commit-config.yaml` | ✅ | `D:\ZephyrAlpha\.pre-commit-config.yaml` | | `pyproject.toml` | ✅ | `D:\ZephyrAlpha\pyproject.toml` |  **结论**：§8.2 路径映射完整。发现 1 处不精确引用（`validate_config_integrity.py` 子目录遗漏），已就地修复。  ---  ### 5.3 阶段 2 规格（2026-05-04 已执行 ✅）  #### 执行结果摘要  | 生成器 | 输入 | 输出 | 状态 | 关键指标 | |-------|------|------|:--:|---------| | `generate_gate_registry.py` | `.pre-commit-config.yaml` → 解析 `gate-*` hooks | gate-registry 自动派生 | ✅ | 13 条 GATE-01~18 + SQ/ADM/IDX/DD07 | | `generate_script_manifest.py` | `scripts/governance/**/*.py` → 提取 `__manifest__` 块 | script_manifest 自动扫描 | ✅ | 131 脚本扫描 / 3 个有 `__manifest__` | | `generate_registry_master_index.py` | `_registry/catalogs/*.yaml` → 解析注释 `# module_id:` | registry-master-index 自动扫描 | ⚠️ | 10/38 张登记表有 `module_id` |  **新增文件**（5 个）： - `scripts/governance/generators/__init__.py` - `scripts/governance/generators/generate_gate_registry.py`（含 `__manifest__` 块） - `scripts/governance/generators/generate_script_manifest.py`（含 `__manifest__` 块） - `scripts/governance/generators/generate_registry_master_index.py`（含 `__manifest__` 块）  **已验证的管道行为**： - `--check` 模式可检测漂移（如 manifest 条目数 ≠ 磁盘脚本数） - `__manifest__` YAML 块能被正则正确提取（3/3 个生成脚本验证通过） - 门禁列表从 `.pre-commit-config.yaml` 的 13 个 `gate-*` hooks 中正确提取 - 登记表索引从注释头 `# module_id:` 中正确提取（覆盖率待提升）  **待补充任务（推迟至阶段 3 或后续 session）**： - 为剩余 28 张 `_registry/catalogs/*.yaml` 添加 `# module_id:` 注释 - 为剩余 128 个治理脚本添加 `__manifest__` 块 - 完整运行 `generate_script_manifest.py` 写入磁盘（当前手工维护版仍为 SSoT）  #### Schema 定义 1：脚本元数据（`__manifest__` 块）  每个 `scripts/governance/` 下的 `.py` 文件应在文件头（imports 之后、业务逻辑之前）包含一个 `__manifest__` YAML 块：  ```python __manifest__ = """ dimensions: [D5, D8] priority: P0 timeout: 30 args:   - {flag: --check, type: bool, description: "Dry-run 模式"}   - {flag: --apply, type: bool, description: "自动修复模式"} description: >   校验蓝图 §16 路径索引与磁盘实际文件的一致性。   发现幽灵路径、遗漏登记、路径漂移 → 输出差异报告。 """ ```  `generate_script_manifest.py` 将扫描所有 `.py` 文件 → 提取 `__manifest__` → 生成 `script-manifest.yaml`。  #### Schema 定义 2：登记表元数据  每张登记表的 frontmatter 标准化（当前已经有部分字段，需要补齐）：  ```yaml # 必填字段 module_id: REG-XXX-NNN title: "XXX登记表" doc_type: register         # register / vocabulary / contract status: active version: "1.0.0" summary: "一句话描述" ttl: permanent  # 自动生成字段（由 generate_registry_master_index.py 从磁盘扫描后注入） generated_entry_count: 42   # 🤖 自动计算 generated_at: "..."         # 🤖 自动注入  # 手工 overlay 字段（人类决策，不参与自动生成） review_status: reviewed     # pending / reviewed / needs_update manual_notes: "..."         # 人类补充说明 ```  #### 生成器规格  | 生成器 | 输入 | 输出 | 覆盖范围 | |-------|------|------|---------| | `generate_script_manifest.py` | `scripts/governance/**/*.py` 的 `__manifest__` | `script-manifest.yaml` | 101 → 自动跟踪 | | `generate_registry_master_index.py` | `_registry/catalogs/*.yaml` 的 frontmatter | `registry-master-index.yaml` | 38 张登记表 | | `generate_module_id_registry.py` | `architecture_model/layers/lXX.yaml` + `_index.yaml` | `module_id_registry.yaml` | 68 个模块 ID | | `generate_gate_registry.py` | `.pre-commit-config.yaml` | `gate-registry.md` | 42 个门禁 | | `generate_all_index_md.py` | 磁盘目录结构 | 所有目录的 `index.md` | 82 个目录 |  ---  ### 5.4 阶段 3 门禁规格  | 门禁 ID | 门禁名称 | 输入 | 检查逻辑 | 违反后果 | |---------|---------|------|---------|---------| | GATE-19 | 静态清单漂移阻断 | 所有标注 `generated_at` 的文件 | 重新运行对应生成器 → 对比输出 vs 磁盘文件 → 不一致 → 失败 | 🔴 CI 硬阻断 | | GATE-20 | 派生版本落后阻断 | 所有标注 `derived_from` 的文件 | 读取 `derived_version` vs canonical `version` → 如果不一致 → 警告 | 🟡 CI 警告（Phase 2）/ 🔴 阻断（Phase 3） | | GATE-21 | Canonical 位置审查 | 所有标注 `canonical: true` / `doc_type: standard` 的文件 | 检查文件路径是否在 §8.3 冷记忆中 → 如果在 → 警告 | 🟡 CI 警告 | | GATE-22 | §8.2 路径映射完整性 | AGENTS.md §8.2 | 逐条访问每个声明的文件路径 → 如果有不存在的 → 失败 | 🔴 CI 硬阻断 |  ---  ## 六、风险评估  | 风险 | 概率 | 影响 | 缓解措施 | |------|:---:|------|---------| | 生成器脚本本身有 bug，产出错误的自动生成文件 | 中 | 高 | 阶段 2 产出后先人工抽查 + 逐条对比手工版 vs 自动生成版 | | 引入的 Schema 字段过多，增加维护负担 | 中 | 中 | Schema 最小化原则：只定义"生成器需要的"，不定义"可能以后需要的" | | 阶段变更导致依赖链断裂（80+ 脚本路径变更） | 低 | 高 | 变更都在 YAML 结构层面，不涉及物理文件路径变更 | | 手工维护→自动生成过渡期出现两份不同版本 | 中 | 中 | 过渡期保留手工版只读，自动生成版写入 → 人工确认后删手工版 | | Owner 看不懂新的自动生成体系 | 中 | 低 | 所有变更都在"治理脚本 + 配置文件"层面，Owner 的交互面（AGENTS.md、文档）不受影响 |  ---  ## 七、维护承诺（模块数量随目标任意增长）  三条原则落地后，维护成本的 O(n) 变化：  | 操作 | 落地前 | 落地后 | 变化 | |------|:---:|:---:|:---:| | 新增一个模块 | 手工更新 5 份清单（script_manifest + registry + module-id + gate + index.md） | 运行 1 条生成命令 → 自动更新 5 份 | **O(5) → O(1)** | | 新增一个脚本 | 手工写 YAML 条目到 manifest → 手工更新 3 个 index.md | 在脚本头加 `__manifest__` 块 → 运行生成器 | **O(4) → O(2)** | | 新增一张登记表 | 手工更新 registry-master-index（条目+计数） | 创建 YAML 文件 + frontmatter → 运行生成器 | **O(2) → O(2)**（持平但漂移免疫） | | 发现漂移 | 手工 grep + fix | CI 自动检测 → AI 按诊断修复 | **O(手动排查) → O(CI 自动)** |  **关键承诺**：模块数增长到任意规模时，维护步骤数不会线性增长——因为自动生成和 CI 门禁的成本是固定的 O(1)，不随模块数增加而增加。模块数量由业务目标驱动，不由架构限制决定。  ---  ## 八、与现有铁律的关系  | 现有铁律 | 新铁律 | 关系 | |---------|-------|------| | 铁律一：唯一蓝图 | §6.18 AI 加载路径不可漂移 | 互补——蓝图入口唯一 + 加载路径唯一 | | 铁律二：强制去重 | §6.17 Canonical 物理位置铁律 | 强化——去重管"不重复定义"，§6.17 管"canonical 放在正确位置" | | 铁律三：升级流程 | §6.16 静态清单自动生成 | 互补——升级 + 自动生成 = 漂移免疫闭环 | | 铁律四：永久保留 | — | 不受影响 | | 铁律五：强制实施文档 | §6.16~§6.18 | 强化——三条新铁律是铁律五的"实施对象" |  ---  ## 九、附录——关键文档路径（给未来 AI 的入口）  ``` 本方案的执行入口：   → 阶段 1：本文件（就是你现在读的这个）   → 阶段 2：本文件 §5.3（详细 Schema 规格）   → 阶段 3：本文件 §5.4（详细门禁规格）   → 阶段 4：本文件 §5.1 步骤 4.1~4.5  涉及修改的核心文件：   - D:\ZephyrAlpha\AGENTS.md（追加 §6.16~§6.18 + 调整 §8.2）   - D:\ZephyrAlpha\zephyralpha-2-0\scripts\governance\script-manifest.yaml（→自动生成）   - D:\ZephyrAlpha\zephyralpha-2-0\docs\01_policies_and_standards\_registry\catalogs\registry-master-index.yaml（→自动生成）   - D:\ZephyrAlpha\zephyralpha-2-0\docs\02_enterprise_architecture\target-architecture\architecture_model\module_id_registry.yaml（→自动生成）   - D:\ZephyrAlpha\zephyralpha-2-0\docs\01_policies_and_standards\_registry\catalogs\gate-registry.md（→自动生成）   - D:\ZephyrAlpha\zephyralpha-2-0\.pre-commit-config.yaml（追加 GATE-19~22）   - D:\ZephyrAlpha\zephyralpha-2-0\.github\workflows\governance.yml（集成新门禁）  新增脚本（阶段 2）：   - scripts/governance/generators/generate_script_manifest.py   - scripts/governance/generators/generate_registry_master_index.py   - scripts/governance/generators/generate_module_id_registry.py   - scripts/governance/generators/generate_gate_registry.py   - scripts/governance/generators/generate_all_index_md.py  新增门禁（阶段 3）：   - scripts/governance/d5_architecture/validate_static_manifest_drift.py（GATE-19）   - scripts/governance/d5_architecture/validate_derived_version_freshness.py（GATE-20）   - scripts/governance/d5_architecture/validate_canonical_location.py（GATE-21）   - scripts/governance/d5_architecture/validate_load_path_integrity.py（GATE-22） ```
