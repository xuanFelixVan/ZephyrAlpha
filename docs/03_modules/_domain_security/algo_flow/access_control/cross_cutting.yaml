# ALGO_FLOW 外部真源——cross_cutting（_domain_security 域，真源 src/zephyr/security/access_control/cross_cutting.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.cross_cutting
source_of_truth: src/zephyr/security/access_control/cross_cutting.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: cross_cutting.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① PermissionTopology
    #   name_en: PermissionTopology
    #   intro: 权限拓扑图.
    #   desc: 权限拓扑图. 维护权限节点邻接表，检测循环依赖。；公共方法（定义序）: add_node, add_edge, detect_cycles；源码 L79-L139
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: PermissionTopology
    #   downstream: tests/agent_rbac/test_redteam_adversarial.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
