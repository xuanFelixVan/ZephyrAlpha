# ALGO_FLOW 外部真源——key_hierarchy（_domain_security 域，真源 src/zephyr/security/access_control/key_hierarchy.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.key_hierarchy
source_of_truth: src/zephyr/security/access_control/key_hierarchy.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: master_key 参数
    #   fields: 参数 master_key（无注解）
    #   code: key_hierarchy.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: registry_path 参数
    #   fields: 参数 registry_path（无注解）
    #   code: key_hierarchy.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: rotation_days 参数
    #   fields: 参数 rotation_days（无注解）
    #   code: key_hierarchy.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: key_hierarchy.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① KeyHierarchy
    #   name_en: KeyHierarchy
    #   intro: 三层密钥层级：主密钥→KEK→DEK 按域派发 + 90 天轮换 + 审计 + 启动自检。
    #   desc: 三层密钥层级：主密钥→KEK→DEK 按域派发 + 90 天轮换 + 审计 + 启动自检。；公共方法（定义序）: derive_kek, dispatch_dek, export_state, seal, open,…
    #   inputs: master_key registry_path rotation_days audit_sink clock
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: KeyHierarchy
    #   downstream: N/A (all consumers verified as phantom — stale references removed)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
