# ALGO_FLOW 外部真源——contract_verifier（_domain_security 域，真源 src/zephyr/security/access_control/verifiers/contract_verifier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.verifiers.contract_verifier
source_of_truth: src/zephyr/security/access_control/verifiers/contract_verifier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: contract_verifier.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContractVerifier
    #   name_en: ContractVerifier
    #   intro: 契约验证器 — 验证四项核心契约.
    #   desc: 契约验证器 — 验证四项核心契约.；公共方法（定义序）: verify_all, verify_gct001, verify_gct004, verify_gct007, verify_gct008；源码 L94-L1…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ContractVerifier
    #   downstream: tests/agent_rbac/test_integration_agent_rbac.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
