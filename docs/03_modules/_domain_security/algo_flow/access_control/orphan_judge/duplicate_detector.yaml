# ALGO_FLOW 外部真源——duplicate_detector（_domain_security 域，真源 src/zephyr/security/access_control/orphan_judge/duplicate_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.orphan_judge.duplicate_detector
source_of_truth: src/zephyr/security/access_control/orphan_judge/duplicate_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: similarity_threshold 参数
    #   fields: 参数 similarity_threshold（无注解）
    #   code: duplicate_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DuplicateDetector
    #   name_en: DuplicateDetector
    #   intro: L2 功能重复检测器——基于 AST 哈希的 Jaccard 相似度检测模块间功能重叠。
    #   desc: L2 功能重复检测器——基于 AST 哈希的 Jaccard 相似度检测模块间功能重叠。 检测策略： 1. 解析每个 .py 文件的 AST，提取函数/类名+参数签名 2. 生成…；公共方法（定义序）: detect,…
    #   inputs: similarity_threshold
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: DuplicateDetector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
