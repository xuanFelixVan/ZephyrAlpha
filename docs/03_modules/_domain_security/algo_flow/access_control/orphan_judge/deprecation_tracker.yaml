# ALGO_FLOW 外部真源——deprecation_tracker（_domain_security 域，真源 src/zephyr/security/access_control/orphan_judge/deprecation_tracker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.orphan_judge.deprecation_tracker
source_of_truth: src/zephyr/security/access_control/orphan_judge/deprecation_tracker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: deprecation_tracker.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DeprecationTracker
    #   name_en: DeprecationTracker
    #   intro: 废弃文件追踪器——标记和追踪废弃文件的生命周期。
    #   desc: 废弃文件追踪器——标记和追踪废弃文件的生命周期。 存储到 .aideprecations/ 目录下的 JSON 文件， 每个废弃文件一条记录，过期后可批量移除。；公共方法（定义序）: deprecate, check_…
    #   inputs: project_root
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: DeprecationTracker
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
