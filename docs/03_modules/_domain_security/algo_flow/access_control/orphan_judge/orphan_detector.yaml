# ALGO_FLOW 外部真源——orphan_detector（_domain_security 域，真源 src/zephyr/security/access_control/orphan_judge/orphan_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.orphan_judge.orphan_detector
source_of_truth: src/zephyr/security/access_control/orphan_judge/orphan_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: scanner 参数
    #   fields: 参数 scanner（无注解）
    #   code: orphan_detector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: registry 参数
    #   fields: 参数 registry（无注解）
    #   code: orphan_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① OrphanDetector
    #   name_en: OrphanDetector
    #   intro: 孤儿检测器——持续监控孤儿率，驱动大脑向终极目标靠近。
    #   desc: 孤儿检测器——持续监控孤儿率，驱动大脑向终极目标靠近。；公共方法（定义序）: registry, scanner, compute_orphan_rate, find_orphans, prioritize_orpha…
    #   inputs: scanner registry
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: OrphanDetector
    #   downstream: zephyr.security.access_control.orphan_judge.__init__
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
