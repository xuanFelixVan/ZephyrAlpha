# ALGO_FLOW 外部真源——standalone_evaluator（_domain_security 域，真源 src/zephyr/security/access_control/orphan_judge/standalone_evaluator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.orphan_judge.standalone_evaluator
source_of_truth: src/zephyr/security/access_control/orphan_judge/standalone_evaluator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: standalone_evaluator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① StandaloneEvaluator
    #   name_en: StandaloneEvaluator
    #   intro: class StandaloneEvaluator 源码 L80-L192
    #   desc: 公共方法（定义序）: check；源码 L80-L192
    #   inputs: project_root
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: StandaloneEvaluator
    #   downstream: orphan-judge.judge._run_layer L4
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
