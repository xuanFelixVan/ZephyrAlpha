# ALGO_FLOW 外部真源——audit_log_guard（_domain_security 域，真源 src/zephyr/security/access_control/guards/audit_log_guard.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.guards.audit_log_guard
source_of_truth: src/zephyr/security/access_control/guards/audit_log_guard.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: audit_log_guard.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① AuditLogGuard
    #   name_en: AuditLogGuard
    #   intro: 审计日志注入防护守卫——治本（ G4）。
    #   desc: 审计日志注入防护守卫——治本（ G4）。 提供 sanitize/validate_entry/validate_dict 三个方法，确保写入审计日志的 字符串不包含控制字符或转…；公共方法（定义序）: sanitiz…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AuditLogGuard
    #   downstream: tests.agent_rbac.test_forensic_c
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
