# ALGO_FLOW 外部真源——capability_check（_domain_security 域，真源 src/zephyr/security/access_control/capability_check.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.capability_check
source_of_truth: src/zephyr/security/access_control/capability_check.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: cap 参数
    #   fields: 参数 cap，类型注解 'AgentCapability'
    #   code: capability_check.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① verify_capability_scope
    #   name_en: verify_capability_scope
    #   intro: 验证 agent 声明的 capability 集合是否在合法范围内。
    #   desc: 验证 agent 声明的 capability 集合是否在合法范围内。 Args: cap: AgentCapability 实例，含 agent_id (str) 和 capa…；源码 L84-L128
    #   inputs: cap
    #   outputs: dict[str, Any]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: tests.governance.test_adversarial_contract_attacks ; tests.governance.test_gct_…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
