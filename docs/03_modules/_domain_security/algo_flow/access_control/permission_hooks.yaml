# ALGO_FLOW 外部真源——permission_hooks（_domain_security 域，真源 src/zephyr/security/access_control/permission_hooks.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.permission_hooks
source_of_truth: src/zephyr/security/access_control/permission_hooks.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: permission_hooks.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① PermissionHooks
    #   name_en: PermissionHooks
    #   intro: 权限钩子注册表.
    #   desc: 权限钩子注册表. 管理权限检查生命周期中的钩子函数。；公共方法（定义序）: hooks, register, run, register_defaults；源码 L57-L172
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: PermissionHooks
    #   downstream: tests/agent_rbac/test_redteam_adversarial.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
