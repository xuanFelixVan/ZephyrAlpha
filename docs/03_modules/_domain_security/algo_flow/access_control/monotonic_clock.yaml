# ALGO_FLOW 外部真源——monotonic_clock（_domain_security 域，真源 src/zephyr/security/access_control/monotonic_clock.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.monotonic_clock
source_of_truth: src/zephyr/security/access_control/monotonic_clock.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: monotonic_clock.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① MonotonicClock
    #   name_en: MonotonicClock
    #   intro: 单调时钟.
    #   desc: 单调时钟. 保证 now() 返回值非递减——即使系统时钟回拨也不影响。；公共方法（定义序）: now, verify；源码 L58-L104
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: MonotonicClock
    #   downstream: tests/agent_rbac/test_redteam_adversarial.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
