# ALGO_FLOW 外部真源——multi_agent_collusion_detector（_domain_security 域，真源 src/zephyr/security/access_control/detectors/multi_agent_collusion_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.access_control.detectors.multi_agent_collusion_detector
source_of_truth: src/zephyr/security/access_control/detectors/multi_agent_collusion_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: multi_agent_collusion_detector.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① MultiAgentCollusionDetector
    #   name_en: MultiAgentCollusionDetector
    #   intro: 多 agent 合谋检测器 — 基于交互模式识别合谋.
    #   desc: 多 agent 合谋检测器 — 基于交互模式识别合谋.；公共方法（定义序）: record_interaction, check；源码 L80-L119
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: MultiAgentCollusionDetector
    #   downstream: tests/agent_rbac/test_redteam_adversarial.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
