# ALGO_FLOW 外部真源——siem_correlation_engine（_domain_security 域，真源 src/zephyr/security/siem_correlation_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.siem_correlation_engine
source_of_truth: src/zephyr/security/siem_correlation_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: rules 参数
    #   fields: 参数 rules（无注解）
    #   code: siem_correlation_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: siem_correlation_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: immediate_router 参数
    #   fields: 参数 immediate_router（无注解）
    #   code: siem_correlation_engine.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: summary_sink 参数
    #   fields: 参数 summary_sink（无注解）
    #   code: siem_correlation_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SiemCorrelationEngine
    #   name_en: SiemCorrelationEngine
    #   intro: SIEM 关联规则引擎（滑窗序列聚合 + 提级 + 分级路由）。
    #   desc: SIEM 关联规则引擎（滑窗序列聚合 + 提级 + 分级路由）。；公共方法（定义序）: ingest, flush_summary；源码 L156-L293
    #   inputs: rules clock immediate_router summary_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: SiemCorrelationEngine
    #   downstream: 运行时装配批（安全事件总线订阅端装配关联引擎与分级路由回调）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
