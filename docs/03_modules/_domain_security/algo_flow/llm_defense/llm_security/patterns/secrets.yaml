# ALGO_FLOW 外部真源——secrets（_domain_security 域，真源 src/zephyr/security/llm_defense/llm_security/patterns/secrets.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.llm_defense.llm_security.patterns.secrets
source_of_truth: src/zephyr/security/llm_defense/llm_security/patterns/secrets.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: content 参数
    #   fields: 参数 content，类型注解 str
    #   code: secrets.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① scan_secrets
    #   name_en: scan_secrets
    #   intro: scan_secrets(content) 源码 L266-L280
    #   desc: 源码 L266-L280
    #   inputs: content
    #   outputs: list[dict[str, Any]]
    # 层: 输出
    # - id: O1
    #   name_zh: list[dict[str, Any]]
    #   name_en: list[dict[str, Any]]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
