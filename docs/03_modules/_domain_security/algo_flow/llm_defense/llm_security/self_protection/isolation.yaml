# ALGO_FLOW 外部真源——isolation（_domain_security 域，真源 src/zephyr/security/llm_defense/llm_security/self_protection/isolation.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.llm_defense.llm_security.self_protection.isolation
source_of_truth: src/zephyr/security/llm_defense/llm_security/self_protection/isolation.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: level 参数
    #   fields: 参数 level（无注解）
    #   code: isolation.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LSGIsolation
    #   name_en: LSGIsolation
    #   intro: LSG 自身隔离策略.
    #   desc: LSG 自身隔离策略. 核心功能: - 文件系统访问最小化: 只读 src/zephyr/llm-security/, 只写 /tmp + 日志目录 - 网络隔离: 禁止 LSG…；公共方法（定义序）: check_f…
    #   inputs: level
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: LSGIsolation
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
