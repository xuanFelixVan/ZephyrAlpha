# ALGO_FLOW 外部真源——red_team_scanner（_domain_security 域，真源 src/zephyr/security/llm_defense/llm_security/self_protection/red_team_scanner.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.llm_defense.llm_security.self_protection.red_team_scanner
source_of_truth: src/zephyr/security/llm_defense/llm_security/self_protection/red_team_scanner.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: mode 参数
    #   fields: 参数 mode（无注解）
    #   code: red_team_scanner.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: target 参数
    #   fields: 参数 target（无注解）
    #   code: red_team_scanner.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RedTeamScanner
    #   name_en: RedTeamScanner
    #   intro: L7 Red Team 对抗扫描器.
    #   desc: L7 Red Team 对抗扫描器. 功能: - 加载 red-team-payloads.yaml 中的 200+ 攻击载荷 - 三种扫描模式: quick（采样）/ full…；公共方法（定义序）: run, re…
    #   inputs: mode target
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: RedTeamScanner
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
