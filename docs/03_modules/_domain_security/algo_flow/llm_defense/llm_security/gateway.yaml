# ALGO_FLOW 外部真源——gateway（_domain_security 域，真源 src/zephyr/security/llm_defense/llm_security/gateway.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.llm_defense.llm_security.gateway
source_of_truth: src/zephyr/security/llm_defense/llm_security/gateway.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: model_digest_registry 参数
    #   fields: 参数 model_digest_registry（无注解）
    #   code: gateway.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: rules_file_baselines 参数
    #   fields: 参数 rules_file_baselines（无注解）
    #   code: gateway.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: gateway.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: max_tokens 参数
    #   fields: 参数 max_tokens（无注解）
    #   code: gateway.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LSGSecurityGateway
    #   name_en: LSGSecurityGateway
    #   intro: LLM Security Gateway — L0-L8 九层纵深防御统一编排入口.
    #   desc: LLM Security Gateway — L0-L8 九层纵深防御统一编排入口. 原则：fail-closed —— 任一层 DENY -> 整体 DENY，LSG 不可用…；公共方法（定义序）: layers,…
    #   inputs: model_digest_registry rules_file_baselines project_root max_tokens ma…
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: LSGSecurityGateway
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
