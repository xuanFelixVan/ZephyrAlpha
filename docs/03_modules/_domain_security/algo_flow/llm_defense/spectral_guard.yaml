# ALGO_FLOW 外部真源——spectral_guard（_domain_security 域，真源 src/zephyr/security/llm_defense/spectral_guard.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.llm_defense.spectral_guard
source_of_truth: src/zephyr/security/llm_defense/spectral_guard.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: thresholds 参数
    #   fields: 参数 thresholds（无注解）
    #   code: spectral_guard.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: recall_first 参数
    #   fields: 参数 recall_first（无注解）
    #   code: spectral_guard.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SpectralGuard
    #   name_en: SpectralGuard
    #   intro: Spectral 注意力谱幻觉检测器（谱特征 + 双阈值 + recall 优先）。
    #   desc: Spectral 注意力谱幻觉检测器（谱特征 + 双阈值 + recall 优先）。；公共方法（定义序）: features, evaluate；源码 L128-L222
    #   inputs: thresholds recall_first
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: SpectralGuard
    #   downstream: 运行时装配批（LLM 输出护栏装配点注入模型阈值表后调用判定）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
