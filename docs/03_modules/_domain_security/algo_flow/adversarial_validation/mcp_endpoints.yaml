# ALGO_FLOW 外部真源——mcp_endpoints（_domain_security 域，真源 src/zephyr/security/adversarial_validation/mcp_endpoints.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.mcp_endpoints
source_of_truth: src/zephyr/security/adversarial_validation/mcp_endpoints.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: mcp_endpoints.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① MCPEndpoints
    #   name_en: MCPEndpoints
    #   intro: class MCPEndpoints 源码 L80-L173
    #   desc: 公共方法（定义序）: get_tools, handle；源码 L80-L173
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: MCPEndpoints
    #   downstream: MCP Server; external AI Agents
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
