# ALGO_FLOW 外部真源——defense_runner（_domain_security 域，真源 src/zephyr/security/adversarial_validation/defense_runner.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.defense_runner
source_of_truth: src/zephyr/security/adversarial_validation/defense_runner.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: gate_engine 参数
    #   fields: 参数 gate_engine（无注解）
    #   code: defense_runner.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: jsonl_output 参数
    #   fields: 参数 jsonl_output（无注解）
    #   code: defense_runner.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DefenseRunner
    #   name_en: DefenseRunner
    #   intro: class DefenseRunner 源码 L128-L315
    #   desc: 公共方法（定义序）: gate_engine, run_defense, evaluate_gate, try_real_gate, simulate_gate, results, close；源码 L128-L315
    #   inputs: gate_engine jsonl_output
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: DefenseRunner
    #   downstream: validator.py; game_day_runner.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
