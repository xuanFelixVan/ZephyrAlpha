# ALGO_FLOW 外部真源——cold_start（_domain_security 域，真源 src/zephyr/security/adversarial_validation/cold_start.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.cold_start
source_of_truth: src/zephyr/security/adversarial_validation/cold_start.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry_path 参数
    #   fields: 参数 registry_path（无注解）
    #   code: cold_start.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ColdStart
    #   name_en: ColdStart
    #   intro: class ColdStart 源码 L117-L270
    #   desc: 公共方法（定义序）: phase, onboard_module, onboard_batch, registry_path, classify, is_registered, verify_registration,…
    #   inputs: registry_path
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ColdStart
    #   downstream: game_day_runner.py; validator.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
