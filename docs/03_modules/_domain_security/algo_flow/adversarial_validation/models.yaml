# ALGO_FLOW 外部真源——models（_domain_security 域，真源 src/zephyr/security/adversarial_validation/models.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.models
source_of_truth: src/zephyr/security/adversarial_validation/models.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: models.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① 数据契约声明
    #   name_en: data class declarations
    #   intro: 纯声明类（无公共方法，AST 事实）: Severity, AttackTier, BlastRadiusLevel, ResultClass, Scenar…
    #   desc: 数据契约/异常/枚举声明共 18 类；无算法流程（AST 事实）
    #   inputs: I1
    #   outputs: 数据契约类集合
    # 层: 输出
    # - id: O1
    #   name_zh: 数据契约声明（18 类）
    #   name_en: data classes
    #   intro: Severity, AttackTier, BlastRadiusLevel, ResultClass, ScenarioSource, InjectionT…
    #   downstream: scenario_loader.py; validator.py; defense_runner.py; bypass_recorder.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
