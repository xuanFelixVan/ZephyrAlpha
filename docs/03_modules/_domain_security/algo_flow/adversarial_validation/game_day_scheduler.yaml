# ALGO_FLOW 外部真源——game_day_scheduler（_domain_security 域，真源 src/zephyr/security/adversarial_validation/game_day_scheduler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.game_day_scheduler
source_of_truth: src/zephyr/security/adversarial_validation/game_day_scheduler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: state_path 参数
    #   fields: 参数 state_path（无注解）
    #   code: game_day_scheduler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① GameDayScheduler
    #   name_en: GameDayScheduler
    #   intro: 游戏日调度状态机（事件主触发 + CI 定时兜底）。
    #   desc: 游戏日调度状态机（事件主触发 + CI 定时兜底）。 触发模型（原则②创造必全自动）： - 主触发=事件：git push webhook → handle_webhook("p…；公共方法（定义序）: state,…
    #   inputs: state_path
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: GameDayScheduler
    #   downstream: cli.py; CI/CD workflow
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
