# ALGO_FLOW 外部真源——bypass_recorder（_domain_security 域，真源 src/zephyr/security/adversarial_validation/bypass_recorder.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.bypass_recorder
source_of_truth: src/zephyr/security/adversarial_validation/bypass_recorder.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: log_dir 参数
    #   fields: 参数 log_dir（无注解）
    #   code: bypass_recorder.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① BypassRecorder
    #   name_en: BypassRecorder
    #   intro: class BypassRecorder 源码 L66-L151
    #   desc: 公共方法（定义序）: record_bypass, query_bypasses, escalated_entries, total_bypasses；源码 L66-L151
    #   inputs: log_dir
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: BypassRecorder
    #   downstream: validator.py ; convergence_checker.py ; escalation-engine
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
