# ALGO_FLOW 外部真源——async_monitor（_domain_security 域，真源 src/zephyr/security/adversarial_validation/async_monitor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.security.adversarial_validation.async_monitor
source_of_truth: src/zephyr/security/adversarial_validation/async_monitor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: poll_interval_s 参数
    #   fields: 参数 poll_interval_s（无注解）
    #   code: async_monitor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AsyncMonitor
    #   name_en: AsyncMonitor
    #   intro: class AsyncMonitor 源码 L92-L261
    #   desc: 公共方法（定义序）: state, start, stop, alerts, alert_count, clear_alerts, stop_event, poll_interval_s, thread, circui…
    #   inputs: poll_interval_s
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: AsyncMonitor
    #   downstream: cli.py; mcp_endpoints.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
