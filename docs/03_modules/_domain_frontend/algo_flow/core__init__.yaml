# ALGO_FLOW 外部真源——__init__（_domain_frontend 域，真源 src/zephyr/frontend/core/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.frontend.core.__init__
source_of_truth: src/zephyr/frontend/core/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: frontend/core 子包命名空间
    #   fields: 无运行时数据字段（纯包占位，无函数无类）
    #   code: src/zephyr/frontend/core/__init__.py L1
    # 层: 算法
    # - id: A1
    #   name_zh: ① 空导出初始化
    #   name_en: __all__
    #   intro: 声明空 __all__ 列表作为子包占位，不对外导出任何符号
    #   desc: 模块级赋值 __all__: list[str] = []，全文仅 3 行
    #   inputs: I1
    #   outputs: 空导出列表
    # 层: 输出
    # - id: O1
    #   name_zh: 空导出符号表
    #   name_en: __all__
    #   intro: frontend/core 当前不导出任何符号，仅占住包路径
    #   downstream: 无下游/内部使用
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
