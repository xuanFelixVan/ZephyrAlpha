# --- 治理锚定 ---
# doc_type: register | 功能验收单（冻结"什么样子算对"）
# ttl: permanent
# 修订权限：Owner 专有（AI 可提议不可自改已冻结条项）
# --- 治理锚定结束 ---

feature_id: F-CHAINMAP-GALAXY
feature_name: 产业星系图（L1 族聚合全景）
frozen_at: 2026-09-08
frozen_by: ZephyrAlpha-Owner
revision: 3
revision_policy: "修订=Owner 批准+改原文+revision 链留痕；AI 不可自改已冻结条项"
# rev2 (2026-09-09, Owner 裁定): item2 数字带 30~48 废止——脏数据时代产物（旧输入混 33.6% 已关闭
# 落位与垃圾权重伪造假簇）；任务书 8~15 拍脑袋估计同步撤回。簇数=修好数据后的自然结果，不是目标。
# 新 item2=四闸：15~48 自然簇数 + 无巨簇（最大簇≤活跃链总数 25%）+ 族间 links>0
# + 主题自检（抽 5 簇看 top 链名同族可读，机断+目检双检）。rev1 原文存 item2_rev1_text 留档。
# rev3 (2026-09-10, Owner 对话批准"加载体验批"施工): 新增 item7/8 加载态——首算 3-6s（冷缓存
# 最长 20s）不再白屏：星云闪烁骨架+已等待秒数诚实计数；断线画布内失败态（红字+暂停闪烁+重试
# 计数）+15s 自动重试机制不变；成功即隐藏。零假数据（演示诚实纪律）。

# check 取值：目检（截图人工/AI 视觉对） | 机断（DOM/接口断言）
items:
  - { id: 1, text: "页面注入后 #cm-canvas-galaxy 与 #cm-nav 存在且默认可见", check: 机断 }
  - { id: 2, text: "/api/chainmap-galaxy 返回 ok=true 且 clusters 15~48 个（塌簇修复后自然簇数）+ 无巨簇（最大簇 ≤ 活跃链总数 25%）+ 族间 links>0 + 主题自检（抽 5 簇看 top 链名，同族可读；机断+目检双检）", check: 机断, item2_rev1_text: "/api/chainmap-galaxy 返回 ok=true 且 clusters 30~48 个、links≥0" }
  - { id: 3, text: "族节点圆卡渲染数量=clusters 数，节点直径随公司数变化", check: 机断 }
  - { id: 4, text: "族间连线粗细/透明度随权重区分，hover 节点高亮直连边+邻点、其余淡化", check: 目检 }
  - { id: 5, text: "滚轮缩放（光标锚点）/拖动平移/双击空白重置三交互手感与 tdm 页一致", check: 目检 }
  - { id: 6, text: "点族节点切到 L2 链层并隐藏 L1 画布；坐标刷新（generated_at 变化）后布局重算不残留旧缓存", check: 目检 }
  - { id: 7, text: "取数期间 #cm-loading-galaxy 可见且含'星系聚类首算中'文案与已等待秒数（1s 递增）；取数成功后浮层隐藏", check: 机断 }
  - { id: 8, text: "断线时浮层转失败态（cm-load-fail 红字+闪烁暂停+已自动重试 N 次计数），15s 重试机制不回归（item 沿用 meta 行双通道）", check: 机断 }

execution:
  trigger: "任何涉及本功能的 UI 改动 commit 前"
  flow: "起面板 API（8890）→ 打开 #chainmap → 截图 → 逐条对 → 全绿才走 GitCommitGateway"
  baseline_screenshot: "acceptance/screenshots/F-CHAINMAP-GALAXY-baseline.png"
  # rev3 证据（本地留档惯例：验收截图不入库——docs/03_modules DCR-005 只许 .md/.yaml，
  # 存量 PNG 均 untracked；截图法=8899 http.server 静态壳 + 8890 API 真源 + Playwright page.route）
  evidence_rev3:
    - "acceptance/screenshots/F-CHAINMAP-GALAXY-item7-loading.png（加载态：已等待 2s 诚实计数）"
    - "acceptance/screenshots/F-CHAINMAP-GALAXY-item8-fail.png（断线失败态：红字+已自动重试 1 次）"

关联:
  frontend_map: F-CHAINMAP-GALAXY
  code_ref: "src/zephyr/frontend/dashboard/web/features/chainmap/chainmap-galaxy.js"
