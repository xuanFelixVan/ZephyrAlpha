# --- 治理锚定 ---
# doc_type: register | 功能验收单（冻结"什么样子算对"）
# ttl: permanent
# 修订权限：Owner 专有（AI 可提议不可自改已冻结条项）
# --- 治理锚定结束 ---

feature_id: F-CHAINMAP-CLUSTER
feature_name: 产业链层（L2 上中下游列式+环节公司面板）
frozen_at: 2026-09-08
frozen_by: ZephyrAlpha-Owner
revision: 7
revision_policy: "修订=Owner 批准+改原文+revision 链留痕；AI 不可自改已冻结条项"
# rev7 (2026-09-13/14, Owner 两指令：iFinD 形态定稿+指定"完全复刻 TDM 逻辑"；AI 按 Owner 指令修订留痕):
# ①视觉范式整体替代（commit e0c7a2d1fb，Owner 2026-09-12 定稿）：item1/2/7 描述的列式卡/列头视觉
#   退役→iFinD 等距流程图 SVG（#cm-wires-cluster 唯一画布：职能分区底板+蛇形折行+等距立方体+
#   公司 chip+锚点大块；列头/L1..Ln/tier 字样全退场）。原条项原文保留作历史，语义 superseded。
# ②item10 废除（commit 7571d3f099，Owner 2026-09-13 指定 TDM 交互）：首开自动聚焦删除——进场
#   全亮；聚焦只由链选轨/链名/导航直达触发；双击空白=取消聚焦回全亮+镜头复位。
# ③连线交互化（commit dc7c029c90）：结构边默认隐藏；悬停环节临时亮其上下游，点开抽屉钉选常亮，
#   关抽屉熄灭（Owner："没点选就不显示线，这样干净"）。
# ④底板半透明（commit dc7c029c90）：分区/锚点底板 rgba(20,31,56,.55)、总图族底板 .48。
# ⑤兜底占位节点诚实化（commit 2acd8cf50c+本批）：name=行业聚合 显示"未细分环节"（真名留 title），
#   抽屉副标注明"兜底聚合节点：公司未细分到环节，待数据治理迁移"。
# ⑥结构边稀疏诚实提示（本批）：≥8 环节且链内结构边<环节×0.15 的链，标题追加"结构边 N 条
#   （稀疏·数据待补）"琥珀 tspan——探针实证 ig_fact 公司级投影对传感器链零覆盖（宇宙不相交），
#   补边只能走数据采集通道，前端不造数据。
# rev6 (2026-09-11, Owner 截图反馈 字遮挡+无自动排序): ①显示名剥离「（已并入 ND-xxx…）」墓碑
# 后缀（title 保留全名）——长 hash 名撑爆 48px 卡=字遮挡根因，卡高 48→50；②列内重心法拓扑
# 排序（barycentric 两轮，链粒度 chip 不离散）——组序按邻边对端平均 y，上下游连线顺读。
# rev5 (2026-09-10, Owner 截图再反馈仍空→数据现实=该族所有链皆稀薄,选链救不了画面):
# 照抄 TDM 全景口径——整族全量绘制（所有链环节卡+跨列边同画布，不再 >RAIL_MIN 切单链视图，
# 单链模式退役）；聚焦=压暗他链不删卡（TDM 血统聚焦同构）；选单轨/链 chip=聚焦开关
# （点聚焦/再点取消/Esc 取消）；首开自动聚焦=活跃环节（有公司落位）最多→公司数→总环节，
# 墓碑链（已并入/零落位）自然沉底（rev4 的纯环节数规则作废，item8 同步 superseded）。
# rev4 (2026-09-10, Owner 实测截图反馈默认聚焦画面空): 聚焦模式默认链改=环节数最多
# （并列取公司数最大）——原按公司数最大选链会落单节点聚合链（"消费电子行业"88 公司
# 1 环节）甬道只剩一卡；显式选链（rail/chip/cm:goto-chain）仍最优先。新增 item8。
# rev3 (2026-09-10, Owner 出示 TDM 截图指定"直接照抄交易决策全景视觉效果"): 卡面配色照抄 TDM——
# 蓝卡=TDM production 板（bg #0f2740/边 #1e5a8f）＝有公司落位；棕虚线=TDM 红节点板
# （bg #2a1c0e/边 #8a4a12 dashed）＝暂无公司落位（语义同构：TDM 红节点=模块未建）；描述行=TDM
# 灰字（职能/公司数去徽章框）；列头=TDM 流根标签板（#1a2436/#3d8bff 圆角 tag）+环节计数；
# 新增顶部图例条（同 TDM 工具栏图例）。item1-6 行为语义不变。

# check 取值：目检（截图人工/AI 视觉对） | 机断（DOM/接口断言）
items:
  - { id: 1, text: "/api/chainmap-cluster?cid= 返回 ok 且环节按 上游→中游→下游→其他→通用 列位渲染（列头带环节计数）", check: 机断 }
  - { id: 2, text: "链=chip 分组（公司数降序），环节卡=TDM 双行卡（名称加粗+职能/股权徽章+公司数），ig_edge 跨列结构边以贝塞尔连线（同列边不画）", check: 目检 }
  - { id: 3, text: "点环节卡 → 右侧公司面板：角色徽标（龙头金/参与蓝/提及灰）+名称+代码+置信度，按角色置信度排序，上限 200 如实标注", check: 机断 }
  - { id: 4, text: "面包屑 ‹ 全景 可返回 L1；链 chip 点击聚焦该链（其余淡化），导航树高亮联动", check: 目检 }
  - { id: 5, text: "搜索落位（cm:goto-chain 带 focus_node）→ 目标环节居中+金色描边高亮", check: 目检 }
  - { id: 6, text: "滚轮缩放/拖动平移/双击重置可用；簇加载失败显示空态文案（零演示数据）", check: 目检 }
  - { id: 7, text: "双行卡机断：.cm-n 高≈48px 含 .nm（600 加粗）+.sub（职能灰字/股权徽章/公司数）两行；蓝卡底色 #0f2740；0 公司环节挂 .empty0 棕虚线；列头 .cm-col-h=TDM tag 板含 .cc 计数；图例条 #cm-legend-cluster 存在", check: 机断 }
  - { id: 8, text: "聚焦模式首开默认链=环节数最多（并列取公司数最大，机断：act 选单项与 API 按此规则算出的链一致）；显式选链（rail/chip/goto-chain）优先于默认", check: 机断, superseded: rev5（改活跃环节优先） }
  - { id: 9, text: "整族全量绘制机断：任一族打开后画布环节卡数=该族全链环节总数（非单链子集）；选单轨仅 >RAIL_MIN 链的族出现", check: 机断 }
  - { id: 10, text: "聚焦=压暗不删卡：点选单轨/链 chip 聚焦他链 .dimmed、再点同项/Esc 取消全图恢复全亮；首开自动聚焦=活跃环节数最多链（并列公司数→总环节）", check: 机断 }
  - { id: 11, text: "墓碑显示名剥离：名称含「（已并入」的环节卡显示名不含 ND-hash（title 保留全名），卡高 50px 无字遮挡", check: 机断 }
  - { id: 12, text: "列内重心法拓扑排序：有跨列边的组按对端平均 y 排序（上下游连线顺读），无边组保持原位；chip 分组不离散", check: 机断 }
  - { id: 13, text: "进场全亮机断（rev7②）：打开族画布后 g.cm-ch.dimmed 计数=0；点链选轨聚焦后他链 .dimmed；双击空白后 .dimmed 回 0", check: 机断 }
  - { id: 14, text: "连线交互化机断（rev7③）：.cm-edge 默认不可见（opacity 0）；悬停环节或点开抽屉后其相关 .cm-edge.lit>0；关抽屉熄灭", check: 机断 }
  - { id: 15, text: "结构边稀疏诚实提示机断（rev7⑥）：环节≥8 且链内边<环节×0.15 的链标题含「稀疏·数据待补」tspan；S21 断链链不重复加", check: 机断 }
  - { id: 16, text: "兜底节点诚实化机断（rev7⑤）：name=行业聚合 的环节显示「未细分环节」，title 保留真名；其抽屉副标含「兜底聚合节点」", check: 机断 }

execution:
  trigger: "任何涉及本功能的 UI 改动 commit 前"
  flow: "起面板 API（8890）→ 打开 #chainmap → 截图 → 逐条对 → 全绿才走 GitCommitGateway"
  baseline_screenshot: "acceptance/screenshots/F-CHAINMAP-CLUSTER-baseline.png"

关联:
  frontend_map: F-CHAINMAP-CLUSTER
  code_ref: "src/zephyr/frontend/dashboard/web/features/chainmap/chainmap-cluster.js"
