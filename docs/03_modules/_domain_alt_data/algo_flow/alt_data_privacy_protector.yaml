# ALGO_FLOW 外部真源——alt_data_privacy_protector（_domain_alt_data 域，真源 src/zephyr/alt_data/alt_data_privacy_protector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.alt_data.alt_data_privacy_protector
source_of_truth: src/zephyr/alt_data/alt_data_privacy_protector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: extra_patterns 参数
    #   fields: 参数 extra_patterns（无注解）
    #   code: alt_data_privacy_protector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: alt_data_privacy_protector.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: alt_data_privacy_protector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AltDataPrivacyProtector
    #   name_en: AltDataPrivacyProtector
    #   intro: 另类数据隐私保护器（PII 识别 + 脱敏管道 + 最小化留存 + 访问审计）。
    #   desc: 另类数据隐私保护器（PII 识别 + 脱敏管道 + 最小化留存 + 访问审计）。；公共方法（定义序）: detect_pii, sanitize_text, sanitize_record, register_ttl…
    #   inputs: extra_patterns clock audit_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: AltDataPrivacyProtector
    #   downstream: 运行时装配批（另类数据入库前脱敏管道挂 connector 落库路径 / 访问审计接 alert·gov_audit 路由）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
