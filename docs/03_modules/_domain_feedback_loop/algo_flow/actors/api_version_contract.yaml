# ALGO_FLOW 外部真源——api_version_contract（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/actors/api_version_contract.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.actors.api_version_contract
source_of_truth: src/zephyr/feedback_loop/actors/api_version_contract.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: API 版本契约 + 调用时刻
    #   fields: APIVersionContract（api_name/version/sunset_date）+ now_utc()
    #   code: 契约 dataclass 与状态判定入口
    # 层: 算法
    # - id: A1
    #   name_zh: 版本生命周期判定
    #   name_en: version_lifecycle_verdict
    #   intro: 按 sunset_date 与当前时间判定 ACTIVE/DEPRECATED/SUNSET，阻断过期版本调用
    #   code: 状态判定逻辑（VersionStatus）
    # 层: 输出
    # - id: O1
    #   name_zh: 版本可用性裁决
    #   name_en: version_verdict
    #   intro: Agent 可读的版本状态（SUNSET 即拒用）
    #   downstream: 调用方 Agent / feedback_loop actors
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
