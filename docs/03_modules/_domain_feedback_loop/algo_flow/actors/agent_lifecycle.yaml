# ALGO_FLOW 外部真源——agent_lifecycle（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/actors/agent_lifecycle.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.actors.agent_lifecycle
source_of_truth: src/zephyr/feedback_loop/actors/agent_lifecycle.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 子代理退休请求
    #   fields: agent_id
    #   code: AgentLifecycle.retire
    # 层: 算法
    # - id: A1
    #   name_zh: 子代理退休登记
    #   name_en: agent_retirement_registry
    #   intro: 在 agents 注册表中将 agent_id 状态置为 RETIRED
    #   code: AgentLifecycle.retire
    # 层: 输出
    # - id: O1
    #   name_zh: 代理生命周期状态表
    #   name_en: agent_state_registry
    #   intro: agents dict 中更新后的生命周期状态
    #   downstream: FLE 子代理编排方（multi_agent_orchestrator）
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
