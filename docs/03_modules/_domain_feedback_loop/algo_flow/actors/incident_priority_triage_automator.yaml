# ALGO_FLOW 外部真源——incident_priority_triage_automator（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/actors/incident_priority_triage_automator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.actors.incident_priority_triage_automator
source_of_truth: src/zephyr/feedback_loop/actors/incident_priority_triage_automator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 安全/运维事件
    #   fields: incident(data_sensitive / user_facing / system_critical / blast_radius)
    #   code: IncidentPriorityTriageAutomator.triage
    # 层: 算法
    # - id: A1
    #   name_zh: SEV 评分分级
    #   name_en: severity_score_classification
    #   intro: 数据敏感+3、用户可见+2、系统关键+2、爆炸半径≤4 累加，总分映射 P0-P4
    #   code: IncidentPriorityTriageAutomator.classify
    # - id: A2
    #   name_zh: 分诊动作裁决
    #   name_en: triage_action_decision
    #   intro: 严重度优于等于 auto_page_threshold(P1) → PAGE，否则 BATCH 批量处理
    #   code: IncidentPriorityTriageAutomator.triage
    # 层: 输出
    # - id: O1
    #   name_zh: 分诊结果
    #   name_en: triage_result
    #   intro: 带 severity 与 action(PAGE/BATCH) 的事件 dict，并累计 triage_count
    #   downstream: 告警通道与值班响应（secondary_alert_channel / owner）
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> A2 ; A2 --> O1
