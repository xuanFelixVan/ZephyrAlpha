# ALGO_FLOW 外部真源——global_action_scheduler（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/actors/global_action_scheduler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.actors.global_action_scheduler
source_of_truth: src/zephyr/feedback_loop/actors/global_action_scheduler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 待调度动作
    #   fields: ScheduledAction(action_id, priority, target_resources)
    #   code: GlobalActionScheduler.enqueue
    # 层: 算法
    # - id: A1
    #   name_zh: 优先级队列调度
    #   name_en: priority_queue_dispatch
    #   intro: enqueue 按 priority 降序排序，_dispatch 在 max_concurrent=3 上限内出队执行
    #   code: GlobalActionScheduler.enqueue / _dispatch
    # - id: A2
    #   name_zh: 资源冲突死锁检测
    #   name_en: resource_deadlock_detection
    #   intro: 扫描运行中动作的 target_resources，共享同一资源的动作对判为死锁
    #   code: GlobalActionScheduler.detect_deadlock
    # 层: 输出
    # - id: O1
    #   name_zh: 运行态与死锁清单
    #   name_en: running_state_and_deadlocks
    #   intro: running 中 RUNNING 动作及 detect_deadlock 返回的冲突 action_id 列表
    #   downstream: FLE 动作执行层（actors 各执行器）
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> A2 ; A2 --> O1
