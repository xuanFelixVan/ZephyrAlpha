# ALGO_FLOW 外部真源——multi_agent_orchestrator（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/actors/multi_agent_orchestrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.actors.multi_agent_orchestrator
source_of_truth: src/zephyr/feedback_loop/actors/multi_agent_orchestrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 任务委派请求
    #   fields: task 描述 + agent_id；agents 注册表
    #   code: MultiAgentOrchestrator.delegate
    # 层: 算法
    # - id: A1
    #   name_zh: 委派目标存在性检查
    #   name_en: delegation_target_check
    #   intro: 仅当 agent_id 已注册于 agents 时受理委派（返回 True），否则拒绝
    #   code: MultiAgentOrchestrator.delegate
    # 层: 输出
    # - id: O1
    #   name_zh: 委派受理结论
    #   name_en: delegation_acceptance
    #   intro: bool——任务是否成功委派给目标代理
    #   downstream: FLE 子代理执行层
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
