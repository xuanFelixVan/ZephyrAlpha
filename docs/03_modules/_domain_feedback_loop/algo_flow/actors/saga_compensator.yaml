# ALGO_FLOW 外部真源——saga_compensator（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/actors/saga_compensator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.actors.saga_compensator
source_of_truth: src/zephyr/feedback_loop/actors/saga_compensator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 已完成步骤清单
    #   fields: completed_steps: list[str]（多步修复中已成功的步骤）
    #   code: SagaCompensator.compensate
    # 层: 算法
    # - id: A1
    #   name_zh: 逆序补偿动作生成
    #   name_en: reverse_compensation_generation
    #   intro: 将已完成步骤逆序映射为 undo_<step> 补偿动作序列
    #   code: SagaCompensator.compensate
    # 层: 输出
    # - id: O1
    #   name_zh: 补偿动作序列
    #   name_en: compensation_steps
    #   intro: list[str]——按逆序排列的 undo 动作，用于回滚半成品修复
    #   downstream: FLE 修复执行器（saga 回滚执行）
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
