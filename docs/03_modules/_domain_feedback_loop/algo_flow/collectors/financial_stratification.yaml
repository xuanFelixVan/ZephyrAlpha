# ALGO_FLOW 外部真源——financial_stratification（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/collectors/financial_stratification.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.collectors.financial_stratification
source_of_truth: src/zephyr/feedback_loop/collectors/financial_stratification.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 资产类别上下文
    #   fields: asset_class 字符串（默认 EQUITY）
    #   code: FinancialStratification 数据类字段
    # 层: 算法
    # - id: A1
    #   name_zh: 资产类别分层标志持有
    #   name_en: asset_class_stratification_carrier
    #   intro: 纯数据载体——持有 asset_class 供上层按资产类别分流诊断（本模块无行为逻辑）
    #   code: FinancialStratification
    # 层: 输出
    # - id: O1
    #   name_zh: 分层上下文
    #   name_en: stratification_context
    #   intro: 含 asset_class 的分层实例
    #   downstream: FLE 诊断方（按资产类别选择诊断策略）
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
