# ALGO_FLOW 外部真源——__init__（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/detectors/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.detectors.__init__
source_of_truth: src/zephyr/feedback_loop/detectors/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 旧路径导入请求
    #   fields: zephyr.feedback_loop.detectors.<leaf>（60 个叶子旧路径）
    #   code: _LeafAliasFinder.find_spec / __getattr__
    # 层: 算法
    # - id: A1
    #   name_zh: 叶子→子包重定向
    #   name_en: leaf_subpkg_redirect
    #   intro: _LEAF_TO_SUBPKG 映射旧叶子名到 5 个子包，importlib 加载真实模块并回填 sys.modules
    #   code: _LeafAliasLoader.exec_module / __getattr__
    # 层: 输出
    # - id: O1
    #   name_zh: 兼容导入的叶子模块
    #   name_en: aliased_leaf_module
    #   intro: 旧路径可导入的子包叶子模块（__all__ 不变）
    #   downstream: zephyr.integration.runtime_core.feedback_loop 及 detectors 子包消费者
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
