# ALGO_FLOW 外部真源——feedback_collector（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/feedback_collector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.feedback_collector
source_of_truth: src/zephyr/feedback_loop/feedback_collector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: store_path 参数
    #   fields: 参数 store_path（无注解）
    #   code: feedback_collector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FeedbackCollector
    #   name_en: FeedbackCollector
    #   intro: Collect and manage task execution feedback.
    #   desc: Collect and manage task execution feedback. Parameters ---------- store_path : Path | Non…；公共方法（定义序）: add, ge…
    #   inputs: store_path
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: FeedbackCollector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
