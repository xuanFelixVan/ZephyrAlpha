# ALGO_FLOW 外部真源——decision_engine（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/decision_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.decision_engine
source_of_truth: src/zephyr/feedback_loop/decision_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: payload 参数
    #   fields: 参数 payload，类型注解 dict[str, Any]
    #   code: decision_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DecisionEngine
    #   name_en: DecisionEngine
    #   intro: class DecisionEngine 源码 L134-L218
    #   desc: 公共方法（定义序）: adapter, pending, evaluate_anomaly, flush_pending, pending_count；源码 L134-L218
    #   inputs: adapter
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② reflect_on_blueprint
    #   name_en: reflect_on_blueprint
    #   intro: reflect_on_blueprint(payload) 源码 L221-L246
    #   desc: 源码 L221-L246
    #   inputs: payload
    #   outputs: dict[str, Any]
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
