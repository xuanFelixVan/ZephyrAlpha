# ALGO_FLOW 外部真源——core（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/core.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.core
source_of_truth: src/zephyr/feedback_loop/core.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: proposal_dir 参数
    #   fields: 参数 proposal_dir（无注解）
    #   code: core.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FeedbackLoop
    #   name_en: FeedbackLoop
    #   intro: 反馈闭环——登记表裁定驱动规则进化。
    #   desc: 反馈闭环——登记表裁定驱动规则进化。 借鉴: - K8s Controller: 调和失败->调整->重试 - LangGraph: Human-in-the-Loop 反馈注入…；公共方法（定义序）: analyze…
    #   inputs: proposal_dir
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: FeedbackLoop
    #   downstream: zephyr.trading.auto_runtime_core; zephyr.trading.lifecycle_manager; zephyr.gov_…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
