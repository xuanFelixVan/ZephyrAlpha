# ALGO_FLOW 外部真源——safety_gate_l40_l41（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/gates/safety_gate_l40_l41.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.gates.safety_gate_l40_l41
source_of_truth: src/zephyr/feedback_loop/gates/safety_gate_l40_l41.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 动作上下文与门禁状态
    #   fields: ctx；门禁态 immutable_core_violation / operational_window_prohibited / container_mutable / image_drift_detected
    #   code: SafetyGateL40L41.evaluate
    # 层: 算法
    # - id: A1
    #   name_zh: L40 自身完整性校验
    #   name_en: l40_self_integrity
    #   intro: 不可变核心被篡改或处于禁用操作窗口 → REJECT
    #   code: _l40_self_integrity
    # - id: A2
    #   name_zh: L41 容器不可变性校验
    #   name_en: l41_container_immutability
    #   intro: 镜像漂移 → REJECT 阻断部署；容器可变 → OBSERVE_ONLY 告警（A1 未拒才执行）
    #   code: _l41_container_immutability
    # 层: 输出
    # - id: O1
    #   name_zh: 门禁裁决列表
    #   name_en: gate_results
    #   intro: list[GateResult]（PASS / REJECT / OBSERVE_ONLY，HARD 门）
    #   downstream: MOD-GATE_ENGINE 门禁编排聚合 → 动作授权决策
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> A2 ; A2 --> O1
