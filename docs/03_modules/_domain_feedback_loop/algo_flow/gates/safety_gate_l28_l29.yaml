# ALGO_FLOW 外部真源——safety_gate_l28_l29（_domain_feedback_loop 域，真源 src/zephyr/feedback_loop/gates/safety_gate_l28_l29.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.feedback_loop.gates.safety_gate_l28_l29
source_of_truth: src/zephyr/feedback_loop/gates/safety_gate_l28_l29.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 动作上下文与门禁状态
    #   fields: ctx.action_type；门禁态 last_drill / cve_active / skill_trust
    #   code: SafetyGateL28L29.evaluate
    # 层: 算法
    # - id: A1
    #   name_zh: L28 DR 就绪度校验
    #   name_en: l28_dr_readiness
    #   intro: DR 演练距今 >90d 且动作属 REPAIR/DEPLOY 则 REJECT，否则 PASS
    #   code: _l28_dr_readiness
    # - id: A2
    #   name_zh: L29 供应链校验
    #   name_en: l29_supply_chain
    #   intro: 存在活跃 CVE → OBSERVE_ONLY；skill_trust<0.5 → REJECT（A1 未拒才执行）
    #   code: _l29_supply_chain
    # 层: 输出
    # - id: O1
    #   name_zh: 门禁裁决列表
    #   name_en: gate_results
    #   intro: list[GateResult]（PASS / REJECT / OBSERVE_ONLY，HARD 门）
    #   downstream: MOD-GATE_ENGINE 门禁编排聚合 → 动作授权决策
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> A2 ; A2 --> O1
