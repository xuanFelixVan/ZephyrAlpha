# ALGO_FLOW 外部真源——kill_switch_orchestrator（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/kill_switch_orchestrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.kill_switch_orchestrator
source_of_truth: src/zephyr/autonomy_core/kill_switch_orchestrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: runtime_dir 参数
    #   fields: 参数 runtime_dir（无注解）
    #   code: kill_switch_orchestrator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: repo_root 参数
    #   fields: 参数 repo_root（无注解）
    #   code: kill_switch_orchestrator.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: register_defaults 参数
    #   fields: 参数 register_defaults（无注解）
    #   code: kill_switch_orchestrator.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: system_switch 参数
    #   fields: 参数 system_switch（无注解）
    #   code: kill_switch_orchestrator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SwitchAdapter
    #   name_en: SwitchAdapter
    #   intro: 域级/系统级开关适配器协议（鸭子类型；
    #   desc: 域级/系统级开关适配器协议（鸭子类型；编排器只依赖本协议）.；公共方法（定义序）: trip, reset, is_tripped；源码 L127-L137
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② KillSwitchOrchestrator
    #   name_en: KillSwitchOrchestrator
    #   intro: Kill Switch 两级编排器（只编排不持态）.
    #   desc: Kill Switch 两级编排器（只编排不持态）. 用法:: orch = KillSwitchOrchestrator() # 默认注册系统级 + 4 套域级开关 orch.…；公共方法（定义序）: registe…
    #   inputs: runtime_dir repo_root register_defaults system_switch project_root
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: SwitchAdapter, KillSwitchOrchestrator
    #   downstream: tests/autonomy/test_kill_switch_orchestrator.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
