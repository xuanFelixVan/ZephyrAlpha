# ALGO_FLOW 外部真源——skill_efficacy_calibrator（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/skills/skill_efficacy_calibrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.skills.skill_efficacy_calibrator
source_of_truth: src/zephyr/autonomy_core/skills/skill_efficacy_calibrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: history_path 参数
    #   fields: 参数 history_path（无注解）
    #   code: skill_efficacy_calibrator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SkillsBenchRunner
    #   name_en: SkillsBenchRunner
    #   intro: 基准测试执行器
    #   desc: 基准测试执行器；公共方法（定义序）: history, history_path, record_run, get_history, detect_regression；源码 L77-L184
    #   inputs: history_path
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② SkillEfficacyCalibrator
    #   name_en: SkillEfficacyCalibrator
    #   intro: Skill 效能实证校准器
    #   desc: Skill 效能实证校准器；公共方法（定义序）: bench_results, runner, run_benchmark, calibrate；源码 L233-L359
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: SkillsBenchRunner, SkillEfficacyCalibrator
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
