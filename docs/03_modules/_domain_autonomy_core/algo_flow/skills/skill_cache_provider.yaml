# ALGO_FLOW 外部真源——skill_cache_provider（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/skills/skill_cache_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.skills.skill_cache_provider
source_of_truth: src/zephyr/autonomy_core/skills/skill_cache_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: backend 参数
    #   fields: 参数 backend（无注解）
    #   code: skill_cache_provider.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SkillCacheProvider
    #   name_en: SkillCacheProvider
    #   intro: class SkillCacheProvider 源码 L144-L192
    #   desc: 公共方法（定义序）: backend_name, configure, get, set, invalidate, clear；源码 L144-L192
    #   inputs: backend
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: SkillCacheProvider
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
