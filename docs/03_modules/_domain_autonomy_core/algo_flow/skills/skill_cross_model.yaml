# ALGO_FLOW 外部真源——skill_cross_model（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/skills/skill_cross_model.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.skills.skill_cross_model
source_of_truth: src/zephyr/autonomy_core/skills/skill_cross_model.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: default_provider 参数
    #   fields: 参数 default_provider（无注解）
    #   code: skill_cross_model.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SkillCrossModel
    #   name_en: SkillCrossModel
    #   intro: class SkillCrossModel 源码 L138-L316
    #   desc: 公共方法（定义序）: adapter_registry, default_provider, fallback_chain, get_capability, supports_feature, set_fallback…
    #   inputs: default_provider
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: SkillCrossModel
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
