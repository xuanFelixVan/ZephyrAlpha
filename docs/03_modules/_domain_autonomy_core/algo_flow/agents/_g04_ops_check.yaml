# ALGO_FLOW 外部真源——_g04_ops_check（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/agents/_g04_ops_check.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.agents._g04_ops_check
source_of_truth: src/zephyr/autonomy_core/agents/_g04_ops_check.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: ticket 参数
    #   fields: 参数 ticket，类型注解 dict[str, Any]
    #   code: _g04_ops_check.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: role 参数
    #   fields: 参数 role（无注解）
    #   code: _g04_ops_check.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: runtime_dir 参数
    #   fields: 参数 runtime_dir（无注解）
    #   code: _g04_ops_check.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: repo_root 参数
    #   fields: 参数 repo_root（无注解）
    #   code: _g04_ops_check.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① run_g04_strategy_ops_check
    #   name_en: run_g04_strategy_ops_check
    #   intro: 执行一张 G04 三策略运营核对工单（端到端落盘，仅建议语义）.
    #   desc: 执行一张 G04 三策略运营核对工单（端到端落盘，仅建议语义）. Args: ticket: {"ticket_id", "kind"="g04_strategy_ops_che…；源码 L219-L265
    #   inputs: ticket role runtime_dir repo_root catalogs
    #   outputs: dict[str, Any]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.autonomy_core.agents.business_agent_entry（懒加载薄委派）; tests/autonomy/test_b…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
