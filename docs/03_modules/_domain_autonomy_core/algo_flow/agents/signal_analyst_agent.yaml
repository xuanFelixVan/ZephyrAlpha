# ALGO_FLOW 外部真源——signal_analyst_agent（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/agents/signal_analyst_agent.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.agents.signal_analyst_agent
source_of_truth: src/zephyr/autonomy_core/agents/signal_analyst_agent.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: thresholds 参数
    #   fields: 参数 thresholds（无注解）
    #   code: signal_analyst_agent.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: degrade_sink 参数
    #   fields: 参数 degrade_sink（无注解）
    #   code: signal_analyst_agent.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: signal_analyst_agent.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SignalAnalystAgent
    #   name_en: SignalAnalystAgent
    #   intro: 信号 Agent：IC 衰减×拥挤度 → 漏斗处置建议（判定核心纯函数）。
    #   desc: 信号 Agent：IC 衰减×拥挤度 → 漏斗处置建议（判定核心纯函数）。 Args: thresholds: 判定阈值配置。 degrade_sink: 降级建议回调；异常不阻…；公共方法（定义序）: assess,…
    #   inputs: thresholds degrade_sink audit_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 8 个公共定义未列入（含 8 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（9 定义）
    #   name_en: public defs
    #   intro: SignalAnalystAgent
    #   downstream: 运行时装配批（C-028 输出批量装配 / 漏斗权重真实调整 / SIGQC 指标对接）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
