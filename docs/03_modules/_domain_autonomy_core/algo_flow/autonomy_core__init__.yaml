# ALGO_FLOW 外部真源——__init__（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.__init__
source_of_truth: src/zephyr/autonomy_core/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 包导入请求（无数据输入）
    #   fields: docstring 内 ARCH-033 包结构指引（skill_*→skills/、context_*/ce_*→context/、其余平铺）
    #   code: src/zephyr/autonomy_core/__init__.py L1-10
    # 层: 算法
    # - id: A1
    #   name_zh: ① 子模块白名单导出与归位指引
    #   name_en: __init__（模块级 __all__）
    #   intro: 声明根包 13 个子模块名，并用 docstring 告诉新人模块该往哪个子包放
    #   desc: docstring 给出命名前缀归位规则与 Glob 查找方式（L3-9）；__all__ 列出 13 个根模块名（L12-26），无导入无初始化逻辑
    #   inputs: I1
    #   outputs: 13 个子模块名导出表
    # 层: 输出
    # - id: O1
    #   name_zh: 根包子模块导出表
    #   name_en: __all__
    #   intro: agent_observability/spec_engine/trigger_router 等 13 个根模块名对外可见
    #   downstream: skills/context 子包及平铺子模块（如 MOD-AUTONOMY_CORE_context）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
