# ALGO_FLOW 外部真源——self_evolution_fidelity_gate（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/self_evolution_fidelity_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.self_evolution_fidelity_gate
source_of_truth: src/zephyr/autonomy_core/self_evolution_fidelity_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: self_evolution_fidelity_gate.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① SemanticSignature
    #   name_en: SemanticSignature
    #   intro: class SemanticSignature 源码 L78-L100
    #   desc: 公共方法（定义序）: diff；源码 L78-L100
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② SelfEvolutionFidelityGate
    #   name_en: SelfEvolutionFidelityGate
    #   intro: 自进化保真度门控——EchoTrap 验证引擎
    #   desc: 自进化保真度门控——EchoTrap 验证引擎；公共方法（定义序）: extract_signature, score_toxicity, score_coherence, compute_similarity, ve…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: SemanticSignature, SelfEvolutionFidelityGate
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
