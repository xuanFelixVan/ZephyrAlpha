# ALGO_FLOW 外部真源——context_playground（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/context/context_playground.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.context.context_playground
source_of_truth: src/zephyr/autonomy_core/context/context_playground.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: task 参数
    #   fields: 参数 task，类型注解 str
    #   code: context_playground.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContextPlayground
    #   name_en: ContextPlayground
    #   intro: dry-run CLI /sc:dry-run <task> — 展示 build 全链路 (DD79).
    #   desc: dry-run CLI /sc:dry-run <task> — 展示 build 全链路 (DD79).；公共方法（定义序）: dry_run；源码 L68-L72
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② playground_cli
    #   name_en: playground_cli
    #   intro: playground_cli(task) 源码 L75-L76
    #   desc: 源码 L75-L76
    #   inputs: task
    #   outputs: DryRunResult
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: DryRunResult
    #   name_en: DryRunResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
