# ALGO_FLOW 外部真源——context_evictor（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/context/context_evictor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.context.context_evictor
source_of_truth: src/zephyr/autonomy_core/context/context_evictor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: w_p 参数
    #   fields: 参数 w_p（无注解）
    #   code: context_evictor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: w_f 参数
    #   fields: 参数 w_f（无注解）
    #   code: context_evictor.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: w_r 参数
    #   fields: 参数 w_r（无注解）
    #   code: context_evictor.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: weights 参数
    #   fields: 参数 weights（无注解）
    #   code: context_evictor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContextBlock
    #   name_en: ContextBlock
    #   intro: class ContextBlock 源码 L104-L124
    #   desc: 公共方法（定义序）: is_pinned, is_mandatory, compute_eviction_score；源码 L104-L124
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② EvictionResult
    #   name_en: EvictionResult
    #   intro: class EvictionResult 源码 L128-L140
    #   desc: 公共方法（定义序）: compression_ratio；源码 L128-L140
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A3
    #   name_zh: ③ ContextEvictor
    #   name_en: ContextEvictor
    #   intro: 三维逐出器 (DD9)。
    #   desc: 三维逐出器 (DD9)。 公式: score = w_p × (1 - priority_norm) + w_f × (1 - freshness) + w_r × (1 - r…；公共方法（定义序）: reset_i…
    #   inputs: w_p w_f w_r weights
    #   outputs: 返回值
    #   （注：A3 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: ContextBlock, EvictionResult, ContextEvictor
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1
