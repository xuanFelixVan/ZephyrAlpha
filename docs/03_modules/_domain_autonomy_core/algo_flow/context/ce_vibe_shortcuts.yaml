# ALGO_FLOW 外部真源——ce_vibe_shortcuts（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/context/ce_vibe_shortcuts.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.context.ce_vibe_shortcuts
source_of_truth: src/zephyr/autonomy_core/context/ce_vibe_shortcuts.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: ce_vibe_shortcuts.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ce_vibe
    #   name_en: ce_vibe
    #   intro: Switch to vibe mode: expand top_k + lower threshold.
    #   desc: Switch to vibe mode: expand top_k + lower threshold.；源码 L79-L81
    #   inputs: 无参数
    #   outputs: ModeConfig
    # - id: A2
    #   name_zh: ② ce_strict
    #   name_en: ce_strict
    #   intro: Switch to strict mode: restore conservative params.
    #   desc: Switch to strict mode: restore conservative params.；源码 L84-L86
    #   inputs: 无参数
    #   outputs: ModeConfig
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: ModeConfig
    #   name_en: ModeConfig
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
