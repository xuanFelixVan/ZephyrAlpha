# ALGO_FLOW 外部真源——file_autoregister（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/file_autoregister.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.file_autoregister
source_of_truth: src/zephyr/autonomy_core/file_autoregister.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: manifest_path 参数
    #   fields: 参数 manifest_path（无注解）
    #   code: file_autoregister.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FileAutoRegister
    #   name_en: FileAutoRegister
    #   intro: class FileAutoRegister 源码 L57-L89
    #   desc: 公共方法（定义序）: register；源码 L57-L89
    #   inputs: manifest_path
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: FileAutoRegister
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
