# ALGO_FLOW 外部真源——drift_semantic_reviewer（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/drift_semantic_reviewer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.drift_semantic_reviewer
source_of_truth: src/zephyr/autonomy_core/drift_semantic_reviewer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: a 参数
    #   fields: 参数 a，类型注解 Sequence[float]
    #   code: drift_semantic_reviewer.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: b 参数
    #   fields: 参数 b，类型注解 Sequence[float]
    #   code: drift_semantic_reviewer.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: sessions 参数
    #   fields: 参数 sessions，类型注解 Sequence[SessionChainReview]
    #   code: drift_semantic_reviewer.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: embedder 参数
    #   fields: 参数 embedder，类型注解 EmbeddingProvider
    #   code: drift_semantic_reviewer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① EmbeddingProvider
    #   name_en: EmbeddingProvider
    #   intro: 嵌入模型注入协议（实现方：真实模型适配器 / 测试 mock；本模块不自带实现）。
    #   desc: 嵌入模型注入协议（实现方：真实模型适配器 / 测试 mock；本模块不自带实现）。；公共方法（定义序）: embed；源码 L153-L158
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② SemanticReviewConfig
    #   name_en: SemanticReviewConfig
    #   intro: 复核参数（similarity_threshold 人定，15号文 Q2 关联；frequency=daily/wee…
    #   desc: 复核参数（similarity_threshold 人定，15号文 Q2 关联；frequency=daily/weekly）。；公共方法（定义序）: validate；源码 L171-L184
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A3
    #   name_zh: ③ SemanticReviewReport
    #   name_en: SemanticReviewReport
    #   intro: 日/周频复核报告（人审载体，不自动处置）。
    #   desc: 日/周频复核报告（人审载体，不自动处置）。；公共方法（定义序）: to_dict；源码 L197-L226
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A4
    #   name_zh: ④ cosine_similarity
    #   name_en: cosine_similarity
    #   intro: 余弦相似度；零向量 → 0.0（fail-safe，不放大为相似）。
    #   desc: 余弦相似度；零向量 → 0.0（fail-safe，不放大为相似）。；源码 L229-L236
    #   inputs: a b
    #   outputs: float
    # - id: A5
    #   name_zh: ⑤ review_sessions
    #   name_en: review_sessions
    #   intro: S1.3 复核核：批量嵌入 + 成对余弦相似度比对（当前动作链 vs 原始任务意图）。
    #   desc: S1.3 复核核：批量嵌入 + 成对余弦相似度比对（当前动作链 vs 原始任务意图）。 相似度 < similarity_threshold → 疑似意图偏差，收入 flagge…；源码 L243-L299
    #   inputs: sessions embedder config
    #   outputs: SemanticReviewReport
    # - id: A6
    #   name_zh: ⑥ run_batch
    #   name_en: run_batch
    #   intro: 日/周频跑批骨架：复核 + 报告 JSON 落盘，返回 (报告, 文件名)。
    #   desc: 日/周频跑批骨架：复核 + 报告 JSON 落盘，返回 (报告, 文件名)。 调度（日/周频、避开交易时段与 GPU 高峰）由外部编排负责；本函数不含调度逻辑。 落盘 IO 失败…；源码 L302-L322
    #   inputs: sessions embedder report_dir config
    #   outputs: tuple[SemanticReviewReport, str]
    # - id: A7
    #   name_zh: ⑦ SemanticReviewer
    #   name_en: SemanticReviewer
    #   intro: S1.3 类封装：注入 embedder + report_dir 的复核器（等价函数入口的持有型形态）。
    #   desc: S1.3 类封装：注入 embedder + report_dir 的复核器（等价函数入口的持有型形态）。；公共方法（定义序）: review, run_batch；源码 L325-L344
    #   inputs: embedder report_dir config
    #   outputs: 返回值
    #   （注：A7 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: float
    #   name_en: float
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: tests/autonomy_core/test_drift_semantic_reviewer.py；外部跑批编排（日/周频调度）
    # - id: O2
    #   name_zh: SemanticReviewReport
    #   name_en: SemanticReviewReport
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: tests/autonomy_core/test_drift_semantic_reviewer.py；外部跑批编排（日/周频调度）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> A4
    # A4 --> A5
    # A5 --> A6
    # A6 --> A7
    # A7 --> O1
