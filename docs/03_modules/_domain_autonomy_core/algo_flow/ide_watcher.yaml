# ALGO_FLOW 外部真源——ide_watcher（_domain_autonomy_core 域，真源 src/zephyr/autonomy_core/ide_watcher.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.autonomy_core.ide_watcher
source_of_truth: src/zephyr/autonomy_core/ide_watcher.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: skills_dir 参数
    #   fields: 参数 skills_dir（无注解）
    #   code: ide_watcher.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① IDEWatcher
    #   name_en: IDEWatcher
    #   intro: IDE 热重载监视器——Skill 文件变更自动刷新 AGENTS.
    #   desc: IDE 热重载监视器——Skill 文件变更自动刷新 AGENTS.md；公共方法（定义序）: callbacks, last_mtimes, scan, on_change；源码 L57-L105
    #   inputs: skills_dir
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: IDEWatcher
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
