# ALGO_FLOW 外部真源——overfitting_adjudicator（_domain_backtest 域，真源 src/zephyr/backtest/core/overfitting_adjudicator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.backtest.core.overfitting_adjudicator
source_of_truth: src/zephyr/backtest/core/overfitting_adjudicator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: num_trials 参数
    #   fields: 参数 num_trials，类型注解 int
    #   code: overfitting_adjudicator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: sharpe 参数
    #   fields: 参数 sharpe，类型注解 float
    #   code: overfitting_adjudicator.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: num_obs 参数
    #   fields: 参数 num_obs，类型注解 int
    #   code: overfitting_adjudicator.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: skewness 参数
    #   fields: 参数 skewness，类型注解 float
    #   code: overfitting_adjudicator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① expected_max_sharpe_z
    #   name_en: expected_max_sharpe_z
    #   intro: 多重试验期望最大值 E[max(Z_N)]——校验入参后委托 MOD-SIM-024 唯一真源。
    #   desc: 多重试验期望最大值 E[max(Z_N)]——校验入参后委托 MOD-SIM-024 唯一真源。 本件原自建 Acklam 有理逼近 Φ⁻¹ + 论文闭式块已随 SDC-3/SDC-4 口径统一删除…；源码 L95-L112
    #   inputs: num_trials
    #   outputs: float
    # - id: A2
    #   name_zh: ② adjudicate_dsr
    #   name_en: adjudicate_dsr
    #   intro: DSR 裁定: 多次试验后真实 Sharpe 折减(Bailey & López de Prado 2014)，数学全委托 MOD-SIM-024。
    #   desc: DSR 裁定: 多次试验后真实 Sharpe 折减(Bailey & López de Prado 2014)，数学全委托 MOD-SIM-024。 V[SR]/E[max]/退化判定不自写公式；峰度入参为超额口径(正态=0)，+3 转 Pearson 由真源完成。 退化态 Fail-Closed: dsr=DSR_UNDECIDABLE(0.0)+degenerate=True，永不判显著（旧"SR>0→1.0"fail-open 分支已删）；源码 L147-L223
    #   inputs: sharpe num_trials num_obs skewness kurtosis threshold
    #   outputs: DSRVerdict
    # - id: A3
    #   name_zh: ③ summarize_walk_forward
    #   name_en: summarize_walk_forward
    #   intro: 检验器①: 各折 OOS/IS 衰减比分布 + 最差折汇总。
    #   desc: 检验器①: 各折 OOS/IS 衰减比分布 + 最差折汇总。 Args: folds: 各折 (is_sharpe, oos_sharpe) 对, 至少 1 折; IS<=0 折…；源码 L260-L326
    #   inputs: folds threshold
    #   outputs: WalkForwardDecaySummary
    # - id: A4
    #   name_zh: ④ perturbation_stability
    #   name_en: perturbation_stability
    #   intro: 检验器③: 策略参数 ±pct one-at-a-time 扰动, 统计绩效衰减率与稳健区间占比。
    #   desc: 检验器③: 策略参数 ±pct one-at-a-time 扰动, 统计绩效衰减率与稳健区间占比。 回测引擎走契约接口: backtest_fn(params) -> 绩效标量(…；源码 L382-L474
    #   inputs: base_params backtest_fn pct tolerance min_robust_share
    #   outputs: PerturbationStabilityReport
    # - id: A5
    #   name_zh: ⑤ OverfitGateHook
    #   name_en: OverfitGateHook
    #   intro: 上线门禁挂钩点 Protocol——预留接口, 默认不接真门禁。
    #   desc: 上线门禁挂钩点 Protocol——预留接口, 默认不接真门禁。 真门禁实现方(上线流水线)按本 Protocol 注入: 裁定完成后收到 OverfitAdjudication…；公共方法（定义序）: on_adju…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A6
    #   name_zh: ⑥ OverfittingAdjudicator
    #   name_en: OverfittingAdjudicator
    #   intro: P-5 过拟合裁定器: 三检验器编排 + 门禁挂钩点回调。
    #   desc: P-5 过拟合裁定器: 三检验器编排 + 门禁挂钩点回调。 未提供的检验器视为未检测(不参与否决), 与 overfitting_detector.detect 口径一致。 ga…；公共方法（定义序）: adjudic…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A6 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: float
    #   name_en: float
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 上线评审流程(挂钩点预留, 未接真门禁)
    # - id: O2
    #   name_zh: DSRVerdict
    #   name_en: DSRVerdict
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 上线评审流程(挂钩点预留, 未接真门禁)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> A4
    # A4 --> A5
    # A5 --> A6
    # A6 --> O1
