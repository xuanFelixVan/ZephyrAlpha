# ALGO_FLOW 外部真源——strategy_validation_pipeline（_domain_backtest 域，真源 src/zephyr/backtest/core/strategy_validation_pipeline.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.backtest.core.strategy_validation_pipeline
source_of_truth: src/zephyr/backtest/core/strategy_validation_pipeline.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: request 参数
    #   fields: 参数 request，类型注解 StrategyValidationRequest
    #   code: strategy_validation_pipeline.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: gate 参数
    #   fields: 参数 gate（无注解）
    #   code: strategy_validation_pipeline.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: detector 参数
    #   fields: 参数 detector（无注解）
    #   code: strategy_validation_pipeline.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① run_strategy_validation
    #   name_en: run_strategy_validation
    #   intro: 策略验证流水线编排入口(过拟合检测 → 三阶段门控 → 综合裁决)
    #   desc: 策略验证流水线编排入口(过拟合检测 → 三阶段门控 → 综合裁决) Args: request: 验证请求(各阶段回测产物输入注入) gate: 决策门控实例(可注入自定义配置;…；源码 L158-L222
    #   inputs: request gate detector
    #   outputs: StrategyValidationVerdict
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: StrategyValidationVerdict
    #   name_en: StrategyValidationVerdict
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 首批策略上线验证(52号§7①编排入口)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
