# ALGO_FLOW 外部真源——tick_replay（_domain_backtest 域，真源 src/zephyr/backtest/core/tick_replay.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.backtest.core.tick_replay
source_of_truth: src/zephyr/backtest/core/tick_replay.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: provider 参数
    #   fields: 参数 provider（无注解）
    #   code: tick_replay.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: symbols 参数
    #   fields: 参数 symbols（无注解）
    #   code: tick_replay.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: start 参数
    #   fields: 参数 start（无注解）
    #   code: tick_replay.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: end 参数
    #   fields: 参数 end（无注解）
    #   code: tick_replay.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TickReplayEngine
    #   name_en: TickReplayEngine
    #   intro: Tick 回放引擎（秒级做T专用）
    #   desc: Tick 回放引擎（秒级做T专用） 按 timestamp 严格排序逐 Tick 推送，支持多标的按时间戳对齐回放。 回放速度控制: - real_time: 每Tick间隔=原…；公共方法（定义序）: run, ge…
    #   inputs: provider symbols start end config
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: TickReplayEngine
    #   downstream: zephyr.backtest.implementations.event_driven_engine; zephyr.frontend.dashboard.…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
