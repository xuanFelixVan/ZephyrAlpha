# ALGO_FLOW 外部真源——purged_kfold（_domain_backtest 域，真源 src/zephyr/backtest/core/purged_kfold.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.backtest.core.purged_kfold
source_of_truth: src/zephyr/backtest/core/purged_kfold.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: n_samples 参数
    #   fields: 参数 n_samples，类型注解 int
    #   code: purged_kfold.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: n_splits 参数
    #   fields: 参数 n_splits，类型注解 int
    #   code: purged_kfold.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: t1 参数
    #   fields: 参数 t1（无注解）
    #   code: purged_kfold.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: embargo 参数
    #   fields: 参数 embargo（无注解）
    #   code: purged_kfold.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① purged_kfold_split
    #   name_en: purged_kfold_split
    #   intro: Purged K-Fold 切分(净化+隔离)
    #   desc: Purged K-Fold 切分(净化+隔离) 算法: 1. 将 [0, n_samples) 均分为 n_splits 个连续折(前 remainder 折多 1 样本) 2.…；源码 L104-L175
    #   inputs: n_samples n_splits t1 embargo
    #   outputs: list[tuple[tuple[int, ...], tuple[int,…
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: list[tuple[tuple[int, ...], tuple[int,…
    #   name_en: list[tuple[tuple[int, ...], tuple[int,…
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 预留(标签重叠窗口策略上线时接入, 52号§6 BM-BT-04-C)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: n_samples/n_splits + t1(各样本标签末端索引,可选) + embargo(隔离样本数)
    # A1: purged_kfold_split(K折连续块→test; 其余train; 按t1重叠purge+test末端embargo剔除)
    # O1: list[(train_indices, test_indices)] 长度=n_splits
    # [/ALGO_FLOW]
