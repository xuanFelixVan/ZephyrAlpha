# ALGO_FLOW 外部真源——event_driven_engine（MOD-BT-001 域）
# 2026-09-15 外审遗留①（P2-1 契约头减负）试点：docstring 内 ~60 行机器块出仓至此，
# 源码 docstring 只留锚行 "# [ALGO_FLOW] external: <本文件相对路径>"。
# 本块内容 = 迁移时源码 docstring 内联块逐字节副本（含边段），由
# code_algorithm_extractor._load_external_algo_flow 加载后走同一 parse_algo_flow 管线。
# 消费方：generate_module_algorithm_overview / generate_domain_doc / translation_reconciler（经 extractor）。
# 维护约定：算法公共面变化时同步更新本块（ALGO-NOTE-SYNC 门禁语义不变——其管 decision_map 的 algo_note_zh）。
doc_type: architecture_view
module_id: MOD-BT-001
module: zephyr.backtest.implementations.event_driven_engine
source_of_truth: src/zephyr/backtest/implementations/event_driven_engine.py
ttl: permanent
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: event_driven_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: matching_config 参数
    #   fields: 参数 matching_config（无注解）
    #   code: event_driven_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① EventDrivenEngine
    #   name_en: EventDrivenEngine
    #   intro: 事件驱动回测引擎（Tick 级，做T专用）
    #   desc: 事件驱动回测引擎（Tick 级，做T专用） v1.1.0 新增：基于 TickReplayEngine 的 Tick 级事件驱动回测。 与 DefaultBacktestEngi…；公共方法（定义序）: run, ru…
    #   inputs: config matching_config
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: EventDrivenEngine
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
