# ALGO_FLOW 外部真源——walk_forward（_domain_backtest 域，真源 src/zephyr/backtest/core/walk_forward.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.backtest.core.walk_forward
source_of_truth: src/zephyr/backtest/core/walk_forward.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: walk_forward.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① WalkForwardAnalyzer
    #   name_en: WalkForwardAnalyzer
    #   intro: Walk-Forward分析器
    #   desc: Walk-Forward分析器 提供三模式时间序列切分与White's Reality Check多重比较偏差校正。 按策略类型选择切分模式: - rolling: 短周期/非平…；公共方法（定义序）: split_r…
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: WalkForwardAnalyzer
    #   downstream: zephyr.backtest.implementations.vectorized_engine; zephyr.backtest.implementati…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
