# ALGO_FLOW 外部真源——rag_pipeline（_domain_knowledge 域，真源 src/zephyr/knowledge/rag_pipeline.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.knowledge.rag_pipeline
source_of_truth: src/zephyr/knowledge/rag_pipeline.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: rag_pipeline.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: vector_searcher 参数
    #   fields: 参数 vector_searcher（无注解）
    #   code: rag_pipeline.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: keyword_searcher 参数
    #   fields: 参数 keyword_searcher（无注解）
    #   code: rag_pipeline.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: reranker 参数
    #   fields: 参数 reranker（无注解）
    #   code: rag_pipeline.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RagPipeline
    #   name_en: RagPipeline
    #   intro: RAG 问答管道（分块→hybrid RRF 检索→重排→生成→引用溯源）。
    #   desc: RAG 问答管道（分块→hybrid RRF 检索→重排→生成→引用溯源）。；公共方法（定义序）: ingest, get_chunk, chunks_of, query；源码 L144-L305
    #   inputs: clock vector_searcher keyword_searcher reranker generator chunk_size…
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: RagPipeline
    #   downstream: 运行时装配批（文档统一 ingest / 向量+关键词双路检索器绑定 / 本地 LLM 生成器绑定 / 问答路由）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
