# ALGO_FLOW 外部真源——factor_knowledge_base（_domain_knowledge 域，真源 src/zephyr/knowledge/factor_knowledge_base.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.knowledge.factor_knowledge_base
source_of_truth: src/zephyr/knowledge/factor_knowledge_base.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: factor_knowledge_base.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: kb_writer 参数
    #   fields: 参数 kb_writer（无注解）
    #   code: factor_knowledge_base.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FactorKnowledgeBase
    #   name_en: FactorKnowledgeBase
    #   intro: 因子知识库（定义/关系/历史三表 + kb 写入回调 + 确定性查询）。
    #   desc: 因子知识库（定义/关系/历史三表 + kb 写入回调 + 确定性查询）。；公共方法（定义序）: register_factor, get_definition, get_status, add_relation, re…
    #   inputs: clock kb_writer
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: FactorKnowledgeBase
    #   downstream: 运行时装配批（因子注册入库 / vector_memory knowledge 集合写入绑定 / 因子查询路由）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
