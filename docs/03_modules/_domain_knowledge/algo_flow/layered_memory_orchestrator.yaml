# ALGO_FLOW 外部真源——layered_memory_orchestrator（_domain_knowledge 域，真源 src/zephyr/knowledge/layered_memory_orchestrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.knowledge.layered_memory_orchestrator
source_of_truth: src/zephyr/knowledge/layered_memory_orchestrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: adapters 参数
    #   fields: 参数 adapters（无注解）
    #   code: layered_memory_orchestrator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: layered_memory_orchestrator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LayeredMemoryOrchestrator
    #   name_en: LayeredMemoryOrchestrator
    #   intro: 五层记忆收口编排件（层注册 + 统一检索 + 降级 + 健康检查）。
    #   desc: 五层记忆收口编排件（层注册 + 统一检索 + 降级 + 健康检查）。；公共方法（定义序）: register_layer, unregister_layer, registered_layers, search, he…
    #   inputs: adapters clock
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: LayeredMemoryOrchestrator
    #   downstream: 运行时装配批（五层记忆统一注入点装配 / RAG 检索收口）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
