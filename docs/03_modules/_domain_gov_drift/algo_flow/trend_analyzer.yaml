# ALGO_FLOW 外部真源——trend_analyzer（_domain_gov_drift 域，真源 src/zephyr/gov_drift/trend_analyzer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_drift.trend_analyzer
source_of_truth: src/zephyr/gov_drift/trend_analyzer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: trend_analyzer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TrendAnalyzer
    #   name_en: TrendAnalyzer
    #   intro: class TrendAnalyzer 源码 L93-L327
    #   desc: 公共方法（定义序）: archive_dir, db_dir, db_path, project_root, compute_metrics, check_trend_alerts, archive_old_data；…
    #   inputs: project_root
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: TrendAnalyzer
    #   downstream: src/zephyr/gov_drift/_analysis.py ; tests/audit/test_trend_analyzer.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
