# ALGO_FLOW 外部真源——drift_bridge（_domain_gov_drift 域，真源 src/zephyr/gov_drift/detector_core/bridges/drift_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_drift.detector_core.bridges.drift_bridge
source_of_truth: src/zephyr/gov_drift/detector_core/bridges/drift_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: drift_bridge.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① subscribe_eventbus
    #   name_en: subscribe_eventbus
    #   intro: 订阅 EventBusBackpressure 的 gate_blocked / task_completed 事件。
    #   desc: 订阅 EventBusBackpressure 的 gate_blocked / task_completed 事件。 幂等：重复调用安全。Backpressure 总线不可用时…；源码 L73-L91
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: subscribe_eventbus
    #   downstream: zephyr.trading.boot_hooks
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
