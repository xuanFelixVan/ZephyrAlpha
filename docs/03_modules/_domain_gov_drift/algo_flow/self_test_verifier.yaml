# ALGO_FLOW 外部真源——self_test_verifier（_domain_gov_drift 域，真源 src/zephyr/gov_drift/self_test_verifier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_drift.self_test_verifier
source_of_truth: src/zephyr/gov_drift/self_test_verifier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: base_dir 参数
    #   fields: 参数 base_dir（无注解）
    #   code: self_test_verifier.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SelfTestVerifier
    #   name_en: SelfTestVerifier
    #   intro: class SelfTestVerifier 源码 L94-L464
    #   desc: 公共方法（定义序）: base_dir, check_circular_import, check_cascade_recursion, check_logic_fragmentation, check_data_in…
    #   inputs: base_dir
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: SelfTestVerifier
    #   downstream: src/zephyr/gov_drift/__main__.py; tests/infrastructure/drift_red_blue_adversari…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
