# ALGO_FLOW 外部真源——manual_calendar_events（_domain_data 域，真源 src/zephyr/data/manual_calendar_events.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.manual_calendar_events
source_of_truth: src/zephyr/data/manual_calendar_events.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: path 参数
    #   fields: 参数 path，类型注解 str | Path
    #   code: manual_calendar_events.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① validate_manual_events_csv
    #   name_en: validate_manual_events_csv
    #   intro: 校验 manual 日历事件 CSV，返回 (合法行, 错误清单)。
    #   desc: 校验 manual 日历事件 CSV，返回 (合法行, 错误清单)。 Args: path: CSV 文件路径（utf-8-sig 容忍 BOM；# 开头注释行与空行跳过）。 R…；源码 L83-L150
    #   inputs: path
    #   outputs: tuple[list[dict[str, str]], list[str]]
    # 层: 输出
    # - id: O1
    #   name_zh: tuple[list[dict[str, str]], list[str]]
    #   name_en: tuple[list[dict[str, str]], list[str]]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 一次性 IMPORT 脚本（17号 §6.3，本批次不施工 IMPORT 执行）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
