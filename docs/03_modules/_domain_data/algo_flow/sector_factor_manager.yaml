# ALGO_FLOW 外部真源——sector_factor_manager（_domain_data 域，真源 src/zephyr/data/sector_factor_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.sector_factor_manager
source_of_truth: src/zephyr/data/sector_factor_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: sector_factor_manager.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① SectorFactorManager
    #   name_en: SectorFactorManager
    #   intro: 板块因子数据管理器——完整性校验/成分挂接/轮动因子/质量评分。
    #   desc: 板块因子数据管理器——完整性校验/成分挂接/轮动因子/质量评分。；公共方法（定义序）: check_coverage, attach_constituent_map, compute_rotation_factors…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: SectorFactorManager
    #   downstream: （P1 接线：因子库写入方 + sector_report_builder）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
