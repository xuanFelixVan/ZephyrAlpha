# ALGO_FLOW 外部真源——alerter（_domain_data 域，真源 src/zephyr/data/alerter.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.alerter
source_of_truth: src/zephyr/data/alerter.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: failures_dir 参数
    #   fields: 参数 failures_dir（无注解）
    #   code: alerter.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Alerter
    #   name_en: Alerter
    #   intro: 告警管理器。
    #   desc: 告警管理器。 用法： alerter = Alerter() alerter.notify("kline_daily_incremental", "连接超时", level=LE…；公共方法（定义序）: notify…
    #   inputs: failures_dir
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: Alerter
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
