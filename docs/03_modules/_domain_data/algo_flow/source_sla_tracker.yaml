# ALGO_FLOW 外部真源——source_sla_tracker（_domain_data 域，真源 src/zephyr/data/source_sla_tracker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.source_sla_tracker
source_of_truth: src/zephyr/data/source_sla_tracker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: source_sla_tracker.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① SourceSlaTracker
    #   name_en: SourceSlaTracker
    #   intro: 数据源 SLA 追踪器（SLI 聚合 + 日周报 + 达标判定 + 看板）。
    #   desc: 数据源 SLA 追踪器（SLI 聚合 + 日周报 + 达标判定 + 看板）。；公共方法（定义序）: register_target, target_of, ingest, aggregate, report_daily…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: SourceSlaTracker
    #   downstream: 运行时装配批（探测记录接入 / SLA 目标注册 / 日周报计划任务 / 看板读侧）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
