# ALGO_FLOW 外部真源——baostock_provider（_domain_data 域，真源 src/zephyr/data/implementations/baostock_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.baostock_provider
source_of_truth: src/zephyr/data/implementations/baostock_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: baostock_provider.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① BaostockProvider
    #   name_en: BaostockProvider
    #   intro: Baostock 免费数据源 Provider。
    #   desc: Baostock 免费数据源 Provider。 匿名访问、thread_local 登录模型。每线程独立 bs.login() 会话。 已知问题：数据滞后约 1 周。；公共方法（定义序）: tls, connect,…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: BaostockProvider
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: FetchPayload(table/symbols/start/end) + capability 路由键 + SourcePolicy(降级策略)
    # I2: baostock SDK(bs.login/thread_local 登录态/query_history_k_data_plus/query_hs300_stocks/query_trade_date)
    # A1: fetch 路由分派(capability→_fetch_index_constituent/_fetch_trade_calendar/_fetch_kline_daily/_fetch_kline_daily_delisted)
    # A2: _fetch_kline_daily 主表口径=不复权(adjustflag=3，#196：对齐 miniQMT 主口径，防 ReplacingMergeTree 同键 raw/qfq 跑序漂移)
    # A3: 退市股回填链(_fetch_delisted_universe→_kline_span_map 缺口探测→_fetch_one_delisted_kline 逐标的补缺)
    # O1: Iterator[FetchResult](CH 表行；fetch 异常->yield FetchResult(error=str))
    # [/ALGO_FLOW]
