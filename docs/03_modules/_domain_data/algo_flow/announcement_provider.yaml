# ALGO_FLOW 外部真源——announcement_provider（_domain_data 域，真源 src/zephyr/data/implementations/announcement_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.announcement_provider
source_of_truth: src/zephyr/data/implementations/announcement_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: http_post 参数
    #   fields: 参数 http_post（无注解）
    #   code: announcement_provider.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: http_get 参数
    #   fields: 参数 http_get（无注解）
    #   code: announcement_provider.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: feed_parse 参数
    #   fields: 参数 feed_parse（无注解）
    #   code: announcement_provider.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: feeds 参数
    #   fields: 参数 feeds（无注解）
    #   code: announcement_provider.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AnnouncementProvider
    #   name_en: AnnouncementProvider
    #   intro: 巨潮/交易所公告 Provider——announcement_news 能力。
    #   desc: 巨潮/交易所公告 Provider——announcement_news 能力。 匿名访问、shared 线程安全模型。生产 HTTP 走 requests（延迟 import）…；公共方法（定义序）: connect…
    #   inputs: http_post http_get feed_parse feeds
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AnnouncementProvider
    #   downstream: zephyr.data.scheduler（P1 接线：tasks.yaml announcement_news 任务）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
