# ALGO_FLOW 外部真源——multi_timeframe_fusion（_domain_data 域，真源 src/zephyr/data/multi_timeframe_fusion.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.multi_timeframe_fusion
source_of_truth: src/zephyr/data/multi_timeframe_fusion.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: multi_timeframe_fusion.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MultiTimeframeFusion
    #   name_en: MultiTimeframeFusion
    #   intro: 多周期数据融合器（纯 pandas 内存计算，零 IO）。
    #   desc: 多周期数据融合器（纯 pandas 内存计算，零 IO）。；公共方法（定义序）: resample；源码 L124-L274
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: MultiTimeframeFusion
    #   downstream: 运行时装配批（miniqmt_service/mkt_data 多周期消费接线 / 交易日历真源装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
