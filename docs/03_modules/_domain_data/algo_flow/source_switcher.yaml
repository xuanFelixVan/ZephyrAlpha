# ALGO_FLOW 外部真源——source_switcher（_domain_data 域，真源 src/zephyr/data/redundant_source/source_switcher.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.redundant_source.source_switcher
source_of_truth: src/zephyr/data/redundant_source/source_switcher.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: primary 参数
    #   fields: 参数 primary（无注解）
    #   code: source_switcher.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: backup 参数
    #   fields: 参数 backup（无注解）
    #   code: source_switcher.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: heartbeat 参数
    #   fields: 参数 heartbeat（无注解）
    #   code: source_switcher.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: check_interval 参数
    #   fields: 参数 check_interval（无注解）
    #   code: source_switcher.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SourceProvider
    #   name_en: SourceProvider
    #   intro: 数据源提供者抽象接口。
    #   desc: 数据源提供者抽象接口。 主源（QMT）和备源（通达信/其他）都实现此接口。；公共方法（定义序）: name, start, stop, is_running；源码 L89-L109
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② SourceSwitcher
    #   name_en: SourceSwitcher
    #   intro: 数据源切换控制器。
    #   desc: 数据源切换控制器。 状态机： - PRIMARY: 主源活跃 - BACKUP: 备源活跃（主源中断） - RECOVERY_WAIT: 主源恢复，等待稳定期；公共方法（定义序）: start, stop, get_a…
    #   inputs: primary backup heartbeat check_interval recovery_stable_period
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: SourceProvider, SourceSwitcher
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
