# ALGO_FLOW 外部真源——financial_parser（_domain_data 域，真源 src/zephyr/data/financial_parser.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.financial_parser
source_of_truth: src/zephyr/data/financial_parser.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: pdf_extractor 参数
    #   fields: 参数 pdf_extractor（无注解）
    #   code: financial_parser.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: xbrl_parser 参数
    #   fields: 参数 xbrl_parser（无注解）
    #   code: financial_parser.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: llm_fallback 参数
    #   fields: 参数 llm_fallback（无注解）
    #   code: financial_parser.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FinancialParser
    #   name_en: FinancialParser
    #   intro: 财报解析管道（判定核心纯内存，解析器全注入式）。
    #   desc: 财报解析管道（判定核心纯内存，解析器全注入式）。；公共方法（定义序）: parse_report；源码 L139-L264
    #   inputs: pdf_extractor xbrl_parser llm_fallback
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: FinancialParser
    #   downstream: 运行时装配批（巨潮 PDF 下载执行 / pdfplumber·Arelle 真实绑定 / qwen3:8b 本地调用 / c3 财务表写入接线）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
