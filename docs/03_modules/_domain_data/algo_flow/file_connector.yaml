# ALGO_FLOW 外部真源——file_connector（_domain_data 域，真源 src/zephyr/data/connectors/file_connector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.connectors.file_connector
source_of_truth: src/zephyr/data/connectors/file_connector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: base_dir 参数
    #   fields: 参数 base_dir（无注解）
    #   code: file_connector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: date_column 参数
    #   fields: 参数 date_column（无注解）
    #   code: file_connector.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: symbol_column 参数
    #   fields: 参数 symbol_column（无注解）
    #   code: file_connector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FileConnector
    #   name_en: FileConnector
    #   intro: 本地 CSV 目录连接器（只读）。
    #   desc: 本地 CSV 目录连接器（只读）。；公共方法（定义序）: name, connect, health_check, fetch_batch, close；源码 L77-L132
    #   inputs: base_dir date_column symbol_column
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: FileConnector
    #   downstream: zephyr.data.connectors.__init__
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
