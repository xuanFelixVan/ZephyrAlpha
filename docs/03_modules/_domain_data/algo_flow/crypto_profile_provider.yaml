# ALGO_FLOW 外部真源——crypto_profile_provider（_domain_data 域，真源 src/zephyr/data/implementations/crypto_profile_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.crypto_profile_provider
source_of_truth: src/zephyr/data/implementations/crypto_profile_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: crypto_profile_provider.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① CoinProfile
    #   name_en: CoinProfile
    #   intro: 币种档案（解析中间态；统一行格式经 to_row() 输出）。
    #   desc: 币种档案（解析中间态；统一行格式经 to_row() 输出）。 whitepaper/contract_address 属"支持采集"字段（白皮书、合约地址）， 不进入 7 列统…；公共方法（定义序）: to_row；…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② CryptoProfileProvider
    #   name_en: CryptoProfileProvider
    #   intro: 币圈档案 Provider（CoinGecko 免费 API 默认源 / CoinMarketCap 免费 tier…
    #   desc: 币圈档案 Provider（CoinGecko 免费 API 默认源 / CoinMarketCap 免费 tier 备选源）。 shared 线程安全模型；CoinGecko…；公共方法（定义序）: connect,…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: CoinProfile, CryptoProfileProvider
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
