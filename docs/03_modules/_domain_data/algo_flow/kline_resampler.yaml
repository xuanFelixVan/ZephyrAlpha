# ALGO_FLOW 外部真源——kline_resampler（_domain_data 域，真源 src/zephyr/data/kline_resampler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.kline_resampler
source_of_truth: src/zephyr/data/kline_resampler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: kline_resampler.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① main
    #   name_en: main
    #   intro: K线合成器主入口。
    #   desc: K线合成器主入口。；源码 L209-L246
    #   inputs: 无参数
    #   outputs: int
    # 层: 输出
    # - id: O1
    #   name_zh: int
    #   name_en: int
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
