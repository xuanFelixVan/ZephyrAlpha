# ALGO_FLOW 外部真源——task_queue（_domain_data 域，真源 src/zephyr/data/task_queue.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.task_queue
source_of_truth: src/zephyr/data/task_queue.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: task_queue.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① TaskQueue
    #   name_en: TaskQueue
    #   intro: 任务依赖图 + 优先级队列。
    #   desc: 任务依赖图 + 优先级队列。 用法： q = TaskQueue() q.load_yaml("config/tasks.yaml") for task_id in q.get_…；公共方法（定义序）: load_ya…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: TaskQueue
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
