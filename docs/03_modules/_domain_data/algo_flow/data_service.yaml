# ALGO_FLOW 外部真源——data_service（_domain_data 域，真源 src/zephyr/data/data_service.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.data_service
source_of_truth: src/zephyr/data/data_service.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: redis_client 参数
    #   fields: 参数 redis_client（无注解）
    #   code: data_service.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: pit 参数
    #   fields: 参数 pit（无注解）
    #   code: data_service.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: lineage_tracker 参数
    #   fields: 参数 lineage_tracker（无注解）
    #   code: data_service.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: event_reader 参数
    #   fields: 参数 event_reader（无注解）
    #   code: data_service.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DataService
    #   name_en: DataService
    #   intro: 统一数据服务门面（四能力一口径 + SLA 计量）。
    #   desc: 统一数据服务门面（四能力一口径 + SLA 计量）。 Usage: svc = DataService(redis_client=r, pit=FinancialPITQuery…；公共方法（定义序）: sla_rep…
    #   inputs: redis_client pit lineage_tracker event_reader clock
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② main
    #   name_en: main
    #   intro: 入口——待实现。
    #   desc: 入口——待实现。；源码 L305-L306
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: DataService, main
    #   downstream: zephyr.backtest.core.data_handler; L3 策略层 sleeve
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
