# ALGO_FLOW 外部真源——crypto_event_calendar（_domain_data 域，真源 src/zephyr/data/implementations/crypto_event_calendar.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.crypto_event_calendar
source_of_truth: src/zephyr/data/implementations/crypto_event_calendar.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: crypto_event_calendar.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① CryptoEventCalendarProvider
    #   name_en: CryptoEventCalendarProvider
    #   intro: 币版事件日历 Provider（减半/大额解锁/宏观事件）。
    #   desc: 币版事件日历 Provider（减半/大额解锁/宏观事件）。 全部数据为公开静态快照，无需密钥、无网络依赖；输出确定性。 shared 线程安全模型（无状态）。；公共方法（定义序）: connect, health_c…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: CryptoEventCalendarProvider
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
