# ALGO_FLOW 外部真源——limit_up_pool_collector（_domain_data 域，真源 src/zephyr/data/implementations/limit_up_pool_collector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.limit_up_pool_collector
source_of_truth: src/zephyr/data/implementations/limit_up_pool_collector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: akshare stock_zt_pool_em 东财涨停股池（单日全量）
    #   fields: 代码/名称/涨跌幅/最新价/成交额/流通市值/总市值/换手率/封板资金/首次封板时间/最后封板时间/炸板次数/涨停统计/连板数/所属行业
    # 层: 特征
    # - id: F1
    #   name_zh: 封单比派生
    #   formula: seal_ratio = 封板资金/流通市值（>0 才出，否则 None）
    # - id: F2
    #   name_zh: 封住时间派生
    #   formula: sealed_seconds = 15:00:00 − 最后封板时间（秒；脏时间 None）
    # 层: 算法
    # - id: A1
    #   name_zh: 解析层（纯函数）
    #   desc: parse_zt_pool_rows 行映射+时间规整+派生+非法代码跳过
    # - id: A2
    #   name_zh: 采集与落库
    #   desc: fetch（延迟 import akshare，异常→空列表）→ collect 参数化 INSERT（table 惰性经 table_registry 解析）
    # 层: 输出
    # - id: O1
    #   name_zh: LimitUpPoolEntry 行集 / INSERT 行数
    #   intro: 19 列 INSERT_COLUMNS 对齐 DDL 草稿列序；frozen dataclass asdict JSON 可序列化
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> F1
    # A1 --> F2
    # A1,F1,F2 --> A2
    # A2 --> O1
