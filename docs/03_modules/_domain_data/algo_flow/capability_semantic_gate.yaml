# ALGO_FLOW 外部真源——capability_semantic_gate（_domain_data 域，真源 src/zephyr/data/capability_semantic_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.capability_semantic_gate
source_of_truth: src/zephyr/data/capability_semantic_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: content 参数
    #   fields: 参数 content，类型注解 str
    #   code: capability_semantic_gate.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: registry 参数
    #   fields: 参数 registry，类型注解 tuple[CapabilitySemanticEntry, ...]
    #   code: capability_semantic_gate.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: file_path 参数
    #   fields: 参数 file_path，类型注解 Path
    #   code: capability_semantic_gate.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① check_capability_api_whitelist_content
    #   name_en: check_capability_api_whitelist_content
    #   intro: 校验 provider 文件内容的 capability-API 语义一致性（17 号 §5.8 项 3）。
    #   desc: 校验 provider 文件内容的 capability-API 语义一致性（17 号 §5.8 项 3）。 对已登记 capability：AST 提取 ``_fetch_<c…；源码 L226-L251
    #   inputs: content registry
    #   outputs: list[str]
    # - id: A2
    #   name_zh: ② check_capability_api_whitelist
    #   name_en: check_capability_api_whitelist
    #   intro: 校验 provider 文件的 capability-API 语义一致性（文件读取后委托 content 版）。
    #   desc: 校验 provider 文件的 capability-API 语义一致性（文件读取后委托 content 版）。；源码 L254-L263
    #   inputs: file_path registry
    #   outputs: list[str]
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: list[str]
    #   name_en: list[str]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: commit gate（capability_validator AST gate 装配批，17 号 §5.4 施工项 3）; 调用方（capability-…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1
