# ALGO_FLOW 外部真源——__init__（_domain_data 域，真源 src/zephyr/data/calendar/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.calendar.__init__
source_of_truth: src/zephyr/data/calendar/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: market 参数
    #   fields: 参数 market，类型注解 str
    #   code: __init__.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① get_market_calendar
    #   name_en: get_market_calendar
    #   intro: 装配层注入入口：按市场名取日历单例（无状态共享）。
    #   desc: 装配层注入入口：按市场名取日历单例（无状态共享）。 Args: market: 市场名（"ashare"/"crypto"）。 Returns: 对应市场的 MarketCale…；源码 L74-L89
    #   inputs: market
    #   outputs: MarketCalendar
    # 层: 输出
    # - id: O1
    #   name_zh: MarketCalendar
    #   name_en: MarketCalendar
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 装配层（scheduler/fusion/pit_query/回测装配 按市场注入）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
