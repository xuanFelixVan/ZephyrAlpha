# ALGO_FLOW 外部真源——buffered_writer（_domain_data 域，真源 src/zephyr/data/buffered_writer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.buffered_writer
source_of_truth: src/zephyr/data/buffered_writer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: table 参数
    #   fields: 参数 table（无注解）
    #   code: buffered_writer.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: max_rows 参数
    #   fields: 参数 max_rows（无注解）
    #   code: buffered_writer.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: max_seconds 参数
    #   fields: 参数 max_seconds（无注解）
    #   code: buffered_writer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① BufferedWriter
    #   name_en: BufferedWriter
    #   intro: 批量聚合写入器——攒批后一次性写入 ClickHouse。
    #   desc: 批量聚合写入器——攒批后一次性写入 ClickHouse。 per-task 实例（每个下载任务一个 BufferedWriter），无需线程安全。 scheduler 的 ma…；公共方法（定义序）: add, fl…
    #   inputs: table max_rows max_seconds
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: BufferedWriter
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
