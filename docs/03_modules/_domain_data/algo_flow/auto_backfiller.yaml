# ALGO_FLOW 外部真源——auto_backfiller（_domain_data 域，真源 src/zephyr/data/auto_backfiller.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.auto_backfiller
source_of_truth: src/zephyr/data/auto_backfiller.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: auto_backfiller.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: executor 参数
    #   fields: 参数 executor（无注解）
    #   code: auto_backfiller.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: trading_days_provider 参数
    #   fields: 参数 trading_days_provider（无注解）
    #   code: auto_backfiller.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: sample_validator 参数
    #   fields: 参数 sample_validator（无注解）
    #   code: auto_backfiller.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AutoBackfiller
    #   name_en: AutoBackfiller
    #   intro: 事件触发式自动回填器（判定核心纯内存，执行体/外发全注入）。
    #   desc: 事件触发式自动回填器（判定核心纯内存，执行体/外发全注入）。；公共方法（定义序）: plan, run；源码 L158-L295
    #   inputs: config executor trading_days_provider sample_validator lineage_sink r…
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: AutoBackfiller
    #   downstream: 运行时装配批（触发事件装配 / executor 接因子重算 / lineage_sink 接血缘真源 / retrain_sink 接 auto-retra…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
