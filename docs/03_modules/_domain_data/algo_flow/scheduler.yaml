# ALGO_FLOW 外部真源——scheduler（_domain_data 域，真源 src/zephyr/data/scheduler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.scheduler
source_of_truth: src/zephyr/data/scheduler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: lock_path 参数
    #   fields: 参数 lock_path，类型注解 str | Path | None
    #   code: scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: scheduler 参数
    #   fields: 参数 scheduler，类型注解 IntegratorScheduler
    #   code: scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: port 参数
    #   fields: 参数 port，类型注解 int
    #   code: scheduler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① acquire_single_instance_lock
    #   name_en: acquire_single_instance_lock
    #   intro: 获取调度器进程级单实例锁（OS 级文件锁）。
    #   desc: 获取调度器进程级单实例锁（OS 级文件锁）。 根因（D1 遗留，2026-08-24 实证）：看门狗 start_scheduler.ps1 的 PID 文件锁存在 check-…；源码 L173-L216
    #   inputs: lock_path
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② IntegratorScheduler
    #   name_en: IntegratorScheduler
    #   intro: 数据源集成器调度器。
    #   desc: 数据源集成器调度器。 用法： sched = IntegratorScheduler() sched.start() # 启动常驻进程，按 cron 自动触发 # 手动执行单个任…；公共方法（定义序）: provide…
    #   inputs: config_dir progress_db jobs_db startup_probes calendar
    #   outputs: 返回值
    # - id: A3
    #   name_zh: ③ start_monitor
    #   name_en: start_monitor
    #   intro: 启动监控 HTTP server（后台守护线程）。
    #   desc: 启动监控 HTTP server（后台守护线程）。 Args: scheduler: 调度器实例 port: 监听端口，默认 9100（Prometheus 标准端口段） 容错：…；源码 L2178-L2200
    #   inputs: scheduler port
    #   outputs: 返回值
    # - id: A4
    #   name_zh: ④ main
    #   name_en: main
    #   intro: 调度器入口：启动常驻进程。
    #   desc: 调度器入口：启动常驻进程。 用法： python -m zephyr.data.scheduler；源码 L2220-L2277
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: acquire_single_instance_lock, IntegratorScheduler, start_monitor, main
    #   downstream: CLI(zephyr.data.cli 阶段3+); main()入口
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> A4
    # A4 --> O1
