# ALGO_FLOW 外部真源——internal_compute_provider（_domain_data 域，真源 src/zephyr/data/implementations/internal_compute_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.internal_compute_provider
source_of_truth: src/zephyr/data/implementations/internal_compute_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: internal_compute_provider.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① InternalComputeProvider
    #   name_en: InternalComputeProvider
    #   intro: 内部计算 Provider——读 CH K线→本地计算指标→返回 FetchResult。
    #   desc: 内部计算 Provider——读 CH K线→本地计算指标→返回 FetchResult。 用法（由 scheduler 自动调用）： provider = InternalCo…；公共方法（定义序）: connect…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: InternalComputeProvider
    #   downstream: zephyr.data.scheduler (source=internal 分支)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
