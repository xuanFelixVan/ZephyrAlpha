# ALGO_FLOW 外部真源——news_dedup（_domain_data 域，真源 src/zephyr/data/news_dedup.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.news_dedup
source_of_truth: src/zephyr/data/news_dedup.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: where 参数
    #   fields: 参数 where，类型注解 str
    #   code: news_dedup.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: pub_date 参数
    #   fields: 参数 pub_date，类型注解 str
    #   code: news_dedup.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: title 参数
    #   fields: 参数 title，类型注解 str
    #   code: news_dedup.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: link 参数
    #   fields: 参数 link，类型注解 str
    #   code: news_dedup.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① existing_news_ids
    #   name_en: existing_news_ids
    #   intro: 写前预检：库内满足 where 条件的已存在 news_id 集合（CAND-DAT-025）。
    #   desc: 写前预检：库内满足 where 条件的已存在 news_id 集合（CAND-DAT-025）。 供批量回填脚本在写入侧过滤多版本冗余行（ReplacingMergeTree 按…；源码 L169-L189
    #   inputs: where
    #   outputs: set[str]
    # - id: A2
    #   name_zh: ② build_news_row
    #   name_en: build_news_row
    #   intro: 构造 news_data 表标准行，对齐 ClickHouse schema。
    #   desc: 构造 news_data 表标准行，对齐 ClickHouse schema。 自动计算 news_id（MD5 of source+title+publish_time）， 解…；源码 L192-L245
    #   inputs: pub_date title link summary source data_source region language
    #   outputs: tuple
    # - id: A3
    #   name_zh: ③ dedup_news_result
    #   name_en: dedup_news_result
    #   intro: 对 FetchResult 中的新闻数据去重。
    #   desc: 对 FetchResult 中的新闻数据去重。 基于 title 的 MD5 哈希，过滤掉已存在于 ClickHouse 中的重复新闻。 同时过滤同一批次内的重复新闻。 Args…；源码 L275-L325
    #   inputs: result
    #   outputs: FetchResult
    # 层: 输出
    # - id: O1
    #   name_zh: set[str]
    #   name_en: set[str]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.data.scheduler
    # - id: O2
    #   name_zh: tuple
    #   name_en: tuple
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1
