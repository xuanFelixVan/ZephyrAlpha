# ALGO_FLOW 外部真源——crypto_universe_selector（_domain_data 域，真源 src/zephyr/data/implementations/crypto_universe_selector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.crypto_universe_selector
source_of_truth: src/zephyr/data/implementations/crypto_universe_selector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: top_n 参数
    #   fields: 参数 top_n（无注解）
    #   code: crypto_universe_selector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: api_key 参数
    #   fields: 参数 api_key（无注解）
    #   code: crypto_universe_selector.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: static_universe 参数
    #   fields: 参数 static_universe（无注解）
    #   code: crypto_universe_selector.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: exclude_symbols 参数
    #   fields: 参数 exclude_symbols（无注解）
    #   code: crypto_universe_selector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CryptoUniverseSelector
    #   name_en: CryptoUniverseSelector
    #   intro: 条件选币器（市值前 N 宇宙）。
    #   desc: 条件选币器（市值前 N 宇宙）。 主路径 CoinMarketCap 免费 API；无 key 或 CMC 异常时静态配置兜底。 http_get 可注入（测试 mock，不依赖…；公共方法（定义序）: select；…
    #   inputs: top_n api_key static_universe exclude_symbols http_get
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: CryptoUniverseSelector
    #   downstream: universe_registry UNI-CRYPTO-001 Phase 2 扩池（候选：币版回测/信号装配层）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
