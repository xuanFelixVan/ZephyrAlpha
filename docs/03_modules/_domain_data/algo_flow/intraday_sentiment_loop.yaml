# ALGO_FLOW 外部真源——intraday_sentiment_loop（_domain_data 域，真源 src/zephyr/data/intraday_sentiment_loop.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.intraday_sentiment_loop
source_of_truth: src/zephyr/data/intraday_sentiment_loop.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: rows 参数
    #   fields: 参数 rows，类型注解 list[dict[str, Any]]
    #   code: intraday_sentiment_loop.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: ch_client 参数
    #   fields: 参数 ch_client，类型注解 Any | None
    #   code: intraday_sentiment_loop.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: db_path 参数
    #   fields: 参数 db_path（无注解）
    #   code: intraday_sentiment_loop.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: previous_board 参数
    #   fields: 参数 previous_board（无注解）
    #   code: intraday_sentiment_loop.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① rows_to_time_series
    #   name_en: rows_to_time_series
    #   intro: 快照行序列 → MarketSentimentInput.time_series 契约装配（纯函数，无 I/O）。
    #   desc: 快照行序列 → MarketSentimentInput.time_series 契约装配（纯函数，无 I/O）。 Args: rows: list[dict]，键=_BREAD…；源码 L244-L284
    #   inputs: rows
    #   outputs: tuple[BreadthTimeSeries, str] | None
    # - id: A2
    #   name_zh: ② run_once
    #   name_en: run_once
    #   intro: M1-④ 盘中情绪回路单拍执行（有界形态——常驻节拍交 APScheduler/P0-5 盘中族，本函数不含循环）。
    #   desc: M1-④ 盘中情绪回路单拍执行（有界形态——常驻节拍交 APScheduler/P0-5 盘中族，本函数不含循环）。 Args: ch_client: clickhouse-dr…；源码 L384-L528
    #   inputs: ch_client db_path previous_board analyzer sector_window_minutes
    #   outputs: IntradayLoopResult
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: tuple[BreadthTimeSeries, str] | None
    #   name_en: tuple[BreadthTimeSeries, str] | None
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: （常驻节拍交 APScheduler/P0-5 日循环 SOP 调度族挂接，本模块不注册任务——波5 交付单拍函数）
    # - id: O2
    #   name_zh: IntradayLoopResult
    #   name_en: IntradayLoopResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: （常驻节拍交 APScheduler/P0-5 日循环 SOP 调度族挂接，本模块不注册任务——波5 交付单拍函数）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
