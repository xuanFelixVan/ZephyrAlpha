# ALGO_FLOW 外部真源——miniqmt_provider（_domain_data 域，真源 src/zephyr/data/implementations/miniqmt_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.implementations.miniqmt_provider
source_of_truth: src/zephyr/data/implementations/miniqmt_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: miniqmt_provider.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① MiniQmtIngestProvider
    #   name_en: MiniQmtIngestProvider
    #   intro: miniQMT（迅投 xtquant）数据源 Provider。
    #   desc: miniQMT（迅投 xtquant）数据源 Provider。 通过本地 XtMiniQmt.exe 进程获取行情/财务/指数成分数据。 单线程使用（xtquant 非线程安全…；公共方法（定义序）: connect…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: MiniQmtIngestProvider
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 抓取载荷
    #   fields: FetchPayload（table/capability/symbols/start/end/extra）
    #   code: fetch → _route_kline_capability 等路由入口
    # - id: I2
    #   name: 本地 QMT 会话
    #   fields: xtdata SDK 连接（XtMiniQmt.exe 进程在跑为前提）
    #   code: connect()/_ensure_connected（_connected=False 触发自动重连）
    # 层: 算法
    # - id: A1
    #   name_zh: ① capability 路由
    #   name_en: fetch/_route_kline_capability
    #   intro: 按 payload.extra.capability 分派到 _fetch_kline/期权/转债/财报等抓取器
    #   inputs: I1
    #   outputs: 对应抓取器调用
    # - id: A2
    #   name_zh: ② 逐标的 K 线抓取（单票跳过 + 失败率熔断 + 断连标记）
    #   name_en: fetch_kline/_fetch_kline
    #   intro: 逐标的 download_history_data + get_market_data_ex；单票非连接异常记 warning 跳过继续；连接类异常（isNetError/OSError/10054）置 _connected=False 并中止本批；收尾失败率>5%（_KLINE_BATCH_FAIL_RATE_LIMIT）才 yield error 熔断，否则 SUCCESS 留告警
    #   inputs: I1 I2
    #   outputs: FetchResult 流（rows/last_key）+ 失败计数汇总
    # - id: A3
    #   name_zh: ③ 行规整与日期窗口
    #   name_en: 行解析/_ts_to_date
    #   intro: xtdata 返回转 CH 表列序，时间戳按 UTC 解释避免跨日，last_key 推进供断点续传
    #   inputs: I1
    #   outputs: 规整行列表
    # 层: 输出
    # - id: O1
    #   name_zh: FetchResult 流
    #   name_en: Iterator[FetchResult]
    #   intro: 每标的/批次一条 FetchResult；异常路径 yield error 不抛出（见 ERROR_CONTRACT）
