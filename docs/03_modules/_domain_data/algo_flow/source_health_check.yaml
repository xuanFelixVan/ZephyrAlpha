# ALGO_FLOW 外部真源——source_health_check（_domain_data 域，真源 src/zephyr/data/source_health_check.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data.source_health_check
source_of_truth: src/zephyr/data/source_health_check.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: source 参数
    #   fields: 参数 source，类型注解 str
    #   code: source_health_check.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① run_source_health_check
    #   name_en: run_source_health_check
    #   intro: 执行全量数据源健康检查（供 scheduler.start() 调用）。
    #   desc: 执行全量数据源健康检查（供 scheduler.start() 调用）。 Returns: {source_name: result_dict} 最新健康状态。；源码 L562-L624
    #   inputs: 无参数
    #   outputs: dict[str, dict]
    # - id: A2
    #   name_zh: ② get_source_health
    #   name_en: get_source_health
    #   intro: 查询单个数据源的最新健康状态（供 scheduler 调度决策用）。
    #   desc: 查询单个数据源的最新健康状态（供 scheduler 调度决策用）。 Returns: 最新健康状态 dict，或 None（未检查过）。；源码 L679-L686
    #   inputs: source
    #   outputs: dict | None
    # - id: A3
    #   name_zh: ③ get_all_source_health
    #   name_en: get_all_source_health
    #   intro: 查询所有数据源的最新健康状态。
    #   desc: 查询所有数据源的最新健康状态。；源码 L689-L692
    #   inputs: 无参数
    #   outputs: dict[str, dict]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, dict]
    #   name_en: dict[str, dict]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.data.scheduler
    # - id: O2
    #   name_zh: dict | None
    #   name_en: dict | None
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.data.scheduler
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1
