# ALGO_FLOW 外部真源——overfitting_protection_gate（_domain_simulation 域，真源 src/zephyr/simulation/overfitting_protection_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.simulation.overfitting_protection_gate
source_of_truth: src/zephyr/simulation/overfitting_protection_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: overfitting_protection_gate.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① OverfittingProtectionGate
    #   name_en: OverfittingProtectionGate
    #   intro: 四层过拟合防护统一门禁（注册表 + 编排 + 裁决 + 报告）。
    #   desc: 四层过拟合防护统一门禁（注册表 + 编排 + 裁决 + 报告）。；公共方法（定义序）: register_check, checks_of, evaluate；源码 L161-L270
    #   inputs: clock
    #   outputs: 返回值
    #   （注：A1 之后另有 8 个公共定义未列入（含 8 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（9 定义）
    #   name_en: public defs
    #   intro: OverfittingProtectionGate
    #   downstream: 运行时装配批（因子/策略/信号/ML 四层检查器绑定 / 上线前统一裁决装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
