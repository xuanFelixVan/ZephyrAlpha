# ALGO_FLOW 外部真源——premarket_workflow_engine（_domain_plan_engine 域，真源 src/zephyr/plan_engine/premarket_workflow_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.plan_engine.premarket_workflow_engine
source_of_truth: src/zephyr/plan_engine/premarket_workflow_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: handlers 参数
    #   fields: 参数 handlers（无注解）
    #   code: premarket_workflow_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: premarket_workflow_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: manual_steps 参数
    #   fields: 参数 manual_steps（无注解）
    #   code: premarket_workflow_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PremarketWorkflowEngine
    #   name_en: PremarketWorkflowEngine
    #   intro: 盘前六工序编排引擎（handler/时钟注入，纯内存确定性）。
    #   desc: 盘前六工序编排引擎（handler/时钟注入，纯内存确定性）。；公共方法（定义序）: run, confirm_manual；源码 L167-L326
    #   inputs: handlers clock manual_steps
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: PremarketWorkflowEngine
    #   downstream: 运行时装配批（盘前SOP六工序handler绑定 / 人工接管确认回路接线）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
