---
id: TASK-INF-025-0037
parent_blueprint: D:\ZephyrAlpha\docs\03_modules\l01_infrastructure\a2a-protocol\blueprint.md
module_id: MOD-INF-025
title: "触发条件监控基础设施 — §1.4 trigger_monitoring YAML 落地"
description: >
  实现蓝图 §1.4 中定义的 trigger_monitoring 监控系统——8 个核心指标（active_agent_count/
  inter_agent_handoff_per_day/conflict_count_per_day/concurrent_ide_sessions/
  ineffective_task_completions/cross_agent_behavioral_anomalies/
  multi_model_concurrency/vibe_coding_conflict_rate）的数据采集与阈值检查，
  以及 activation_rule + early_warning_rule + composite_trigger 决策逻辑。
status: Backlog
owner: AI
phase: scaffold
priority: P0
effort_estimate: 4h
tags: [MOD-INF-025, trigger-monitoring, §1.4, YAML, metrics]
created: 2026-05-06
updated: 2026-05-06
due_date: null
depends_on:
  - TASK-INF-025-0001
blocks:
  - TASK-INF-025-0033
upstream_files:
  - D:\ZephyrAlpha\docs\03_modules\l01_infrastructure\a2a-protocol\blueprint.md
downstream_outputs:
  - D:\ZephyrAlpha\src\zephyr\l01_infrastructure\a2a_protocol\trigger_monitor.py
  - D:\ZephyrAlpha\src\zephyr\l01_infrastructure\a2a_protocol\trigger_config.yaml
  - D:\ZephyrAlpha\tests\unit\l01_infrastructure\a2a_protocol\test_trigger_monitor.py
related_docs:
  - D:\ZephyrAlpha\docs\03_modules\l01_infrastructure\a2a-protocol\blueprint.md#§1.4
acceptance_criteria:
  - "AC-1: trigger_monitor.py 采集 8 个指标的数据并持久化"
  - "AC-2: 每个指标含 current/threshold/source/note 完整字段"
  - "AC-3: activation_rule: metric_1 >= 3 AND (metric_2 >= 5 OR metric_3 >= 2) 正确实现"
  - "AC-4: early_warning_rule: metric_4 >= 15 AND metric_1 >= 2 → 预热警告"
  - "AC-5: ineffective_task_completions (metric_5) >= 1 → early warning 触发"
  - "AC-6: cross_agent_behavioral_anomalies (metric_6) >= 1 → early warning 触发"
  - "AC-7: trigger_config.yaml 为配置 SSoT（指标阈值可调但结构不变）"
  - "AC-8: v0.7.0 新增 metric_7 + metric_8（多模型并发 + Vibe Coding 冲突率）已实现"
success_metrics:
  - "SM-1: activation_rule 评估延迟 <1s"
  - "SM-2: 指标采集不依赖 A2A 核心模块（先于 A2A 本身运作）"
rollback_instructions: >
  删除 trigger_monitor.py + trigger_config.yaml。触发器历史数据如有写入，清理对应目录。
context_assembly_manifest:
  - file: D:\ZephyrAlpha\docs\03_modules\l01_infrastructure\a2a-protocol\blueprint.md
    section: §1.4
    lines: 80-126
    role: 触发条件监控完整 YAML 定义
test_strategy: >
  模拟指标变化：metric_1 逐步增长 1→3 且 metric_2=5 → 验证 activation 触发。
  early_warning：metric_4=15 + metric_1=2 → 验证 warning 触发。metric_5/6 各 ≥1 独立触发测试。
security_review_required: false
gates:
  - G0_READINESS_CHECK
  - G1_PLAN_REVIEW
review_checklist:
  - "8 个指标 = blueprint.md §1.4 YAML 定义"
  - "activation_rule 逻辑 = 逐字匹配"
  - "early_warning_rule 逻辑 = 逐字匹配"
  - "trigger_config.yaml 不可被 A2A 核心模块修改"
---

# TASK-INF-025-0037: 触发条件监控基础设施

## 1. 背景

蓝图 §1.4 定义了 A2A 协议的**触发条件监控系统**——这是决定整个 MOD-INF-025 模块何时从 Hold 进入 scaffold 的关键决策基础设施。它不是 A2A 协议本身的一部分，而是决定"何时开始建 A2A"的前置条件检查器。

当前的 8 个监控指标及激活规则定义在蓝图 L80-L126 的 YAML 代码块中。

## 2. 施工内容

### 2.1 trigger_monitor.py
- 8 个指标采集函数（每个独立可测）
- `activation_rule` 评估器：`metric_1 >= 3 AND (metric_2 >= 5 OR metric_3 >= 2)`
- `early_warning_rule` 评估器：`(metric_4 >= 15 AND metric_1 >= 2) OR metric_5 >= 1 OR metric_6 >= 1`
- v0.7.0 新增 composite_trigger（weighted: net_risk_score > 2.0）

### 2.2 trigger_config.yaml
- 所有指标阈值配置
- 对 A2A 核心模块只读

## 3. 指标清单

| # | Metric | Current | Threshold | Source |
|---|--------|:-------:|:---------:|--------|
| 1 | active_agent_count | 1 | 3 | AgentIdentity 注册表 |
| 2 | inter_agent_handoff_per_day | 0 | 5 | Audit Trail |
| 3 | conflict_count_per_day | 0 | 2 | git merge conflict |
| 4 | concurrent_ide_sessions | 10 | 15 | IDE Session Tracker |
| 5 | ineffective_task_completions | 0 | 1 | Living Spec diff verification |
| 6 | cross_agent_behavioral_anomalies | 0 | 1 | MAScope CASF |
| 7 | multi_model_concurrency | 1 | 4 | LLM API Router |
| 8 | vibe_coding_conflict_rate | 0 | 3 | Living Spec + git |

## 4. 关联盲点

本卡无直接映射盲点——它是决定盲点关闭工作何时开始的前置基础设施，对应 §1.4 全节。

## 5. 回滚

删除生成文件。
