# ALGO_FLOW 外部真源——adaptive_risk_monitor（_domain_risk 域，真源 src/zephyr/risk/core/adaptive_risk_monitor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.adaptive_risk_monitor
source_of_truth: src/zephyr/risk/core/adaptive_risk_monitor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: liquidity 参数
    #   fields: 参数 liquidity，类型注解 Sequence[LiquidityWatchInput]
    #   code: adaptive_risk_monitor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: correlation_returns 参数
    #   fields: 参数 correlation_returns（无注解）
    #   code: adaptive_risk_monitor.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: adaptive_risk_monitor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① assess_risk_watch
    #   name_en: assess_risk_watch
    #   intro: 监控层主入口：流动性分级 + 相关性体制 → 仪表盘快照 + 告警数据。
    #   desc: 监控层主入口：流动性分级 + 相关性体制 → 仪表盘快照 + 告警数据。 Args: liquidity: 标的级流动性指标序列（非空） correlation_returns:…；源码 L195-L283
    #   inputs: liquidity correlation_returns config
    #   outputs: RiskWatchSnapshot
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: RiskWatchSnapshot
    #   name_en: RiskWatchSnapshot
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-RK-30(Adaptive Risk Coordinator, C-004 三层联动盘中监控); CTR-P1-008 风险仪表盘(设计契约); M…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: LiquidityWatchInput 序列(标的级 Amihud/萎缩/非流动标记, 上游 MOD-RK-048 产出)
    # I2: 相关性收益矩阵 {symbol: returns}(可空)
    # I3: RiskWatchConfig(非流动占比黄/红阈 + 相关性 regime 阈值)
    # A1: 流动性分级(illiquid_ratio≥红阈→red告警; ≥黄阈→yellow告警)
    # A2: 相关性体制评估(复用MOD-POS-012 assess_correlation_regime; HIGH→orange分散失效告警)
    # A3: 综合严重度取最严 + 告警聚合
    # O1: RiskWatchSnapshot(仪表盘快照+MonitoringAlert元组) → C-004/仪表盘/告警接线
    # [/ALGO_FLOW]
