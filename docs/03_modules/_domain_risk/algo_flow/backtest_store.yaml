# ALGO_FLOW 外部真源——backtest_store（_domain_risk 域，真源 src/zephyr/risk/core/backtest_store.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.backtest_store
source_of_truth: src/zephyr/risk/core/backtest_store.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: store 参数
    #   fields: 参数 store（无注解）
    #   code: backtest_store.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① VarBacktestStore
    #   name_en: VarBacktestStore
    #   intro: VaR 域持久化门面 (36号 §3.4/§3.13/§3.18)。
    #   desc: VaR 域持久化门面 (36号 §3.4/§3.13/§3.18)。 Args: store: JsonStateStore 或同接口对象 (save(namespace, di…；公共方法（定义序）: save_ba…
    #   inputs: store
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: VarBacktestStore
    #   downstream: daily_auditor(日终持久化); RiskLayerOrchestrator(盘前加载); 35号 §3.15/§3.16(entry_var 配对…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: JsonStateStore 同接口 store(save/load 单记录原语, 双后端经 make_state_store 工厂)
    # I2: trade_date + 各类载荷(回测报告/双轨P&L/盘中重算日志/盘前基线/entry_var)
    # A1: save_*(写入守卫: 有限值/cvar≥var/entry_var≥0 → 命名空间单记录原子写)
    # A2: load_*(读取: None=冷启动 / dict=记录 / StateCorruptError=损坏上抛; 历史按显式日期序列逐日加载)
    # O1: 持久化记录 → §3.19 盘前初始化 / §3.9 回测历史 clean P&L / 35号 §3.16 回撤归因
    # [/ALGO_FLOW]
