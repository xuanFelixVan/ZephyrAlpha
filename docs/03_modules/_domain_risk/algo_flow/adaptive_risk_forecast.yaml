# ALGO_FLOW 外部真源——adaptive_risk_forecast（_domain_risk 域，真源 src/zephyr/risk/core/adaptive_risk_forecast.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.adaptive_risk_forecast
source_of_truth: src/zephyr/risk/core/adaptive_risk_forecast.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: returns 参数
    #   fields: 参数 returns，类型注解 Iterable[float]
    #   code: adaptive_risk_forecast.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: conditions 参数
    #   fields: 参数 conditions（无注解）
    #   code: adaptive_risk_forecast.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: condition 参数
    #   fields: 参数 condition（无注解）
    #   code: adaptive_risk_forecast.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: calibration_predictions 参数
    #   fields: 参数 calibration_predictions（无注解）
    #   code: adaptive_risk_forecast.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① forecast_forward_var
    #   name_en: forecast_forward_var
    #   intro: 盘前前瞻 VaR/CVaR + 共形 VaR 预判主入口。
    #   desc: 盘前前瞻 VaR/CVaR + 共形 VaR 预判主入口。 Args: returns: 历史收益序列（trailing 使用，无未来函数） conditions: 与 retu…；源码 L171-L246
    #   inputs: returns conditions condition calibration_predictions calibration_actu…
    #   outputs: ForwardVarForecast
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: ForwardVarForecast
    #   name_en: ForwardVarForecast
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-RK-30(Adaptive Risk Coordinator, C-004 三层联动盘前预判); MOD-RK-05D(var_intraday_r…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: 历史收益序列 + 平行条件标签(可空) + 目标条件桶(可空)
    # I2: 共形校准 (预测, 实际) 对(可空)
    # I3: ForwardVarConfig(var_level/conformal_alpha/var_limit_pct/sit_out_var_pct/密度窗口)
    # A1: 条件PDF密度预测(复用MOD-SIG-043)→var_pct/cvar_pct(负值口径取正)
    # A2: 共形校准(复用MOD-SIG-044 SplitConformalPredictor)→margin q̂; 无校准→margin=0+degraded
    # A3: 限额对照(limit_scale=min(1,limit/conformal_var), breached, sit_out)
    # O1: ForwardVarForecast(frozen) → C-004 盘前裁决 / var_intraday_recalc 盘前基线
    # [/ALGO_FLOW]
