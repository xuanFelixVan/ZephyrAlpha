# ALGO_FLOW 外部真源——survival_line_monitor（_domain_risk 域，真源 src/zephyr/risk/core/survival_line_monitor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.survival_line_monitor
source_of_truth: src/zephyr/risk/core/survival_line_monitor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: metrics 参数
    #   fields: 参数 metrics，类型注解 SurvivalInput
    #   code: survival_line_monitor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: config 参数
    #   fields: 参数 config，类型注解 SurvivalLineConfig | None
    #   code: survival_line_monitor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① evaluate_survival_line
    #   name_en: evaluate_survival_line
    #   intro: 评估生存线/失败指标。
    #   desc: 评估生存线/失败指标。 判定顺序：失败指标优先（更严重），其次生存线。；源码 L127-L163
    #   inputs: metrics config
    #   outputs: SurvivalLineResult
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: SurvivalLineResult
    #   name_en: SurvivalLineResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 55 号 KPI 监控/复盘编排（接线待排期，本批仅交付模块本体）；告警通道复用 alert_rules.yaml（ALERT-KPI-001/002）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
