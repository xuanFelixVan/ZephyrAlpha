# ALGO_FLOW 外部真源——drawdown_forced_rest（_domain_risk 域，真源 src/zephyr/risk/core/drawdown_forced_rest.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.drawdown_forced_rest
source_of_truth: src/zephyr/risk/core/drawdown_forced_rest.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: drawdown_forced_rest.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ForcedRestTimer
    #   name_en: ForcedRestTimer
    #   intro: Level 4 强制休息计时器。
    #   desc: Level 4 强制休息计时器。 用法: timer = ForcedRestTimer() timer.trigger(trade_date=d0) # Level 4 触发…；公共方法（定义序）: trigger_…
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ForcedRestTimer
    #   downstream: RiskOrchestrator(§6.5 接线位); Kill Switch 复位链(§3.14 人工复位前置校验)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: trigger(trade_date)记录Level4触发日 + clear()人工清除
    # I2: ForcedRestConfig(rest_trading_days=5, §2.5.2 Level4) + 有序交易日历list[date](调用方注入)
    # F1: is_resting(current_date,trading_days)→触发日后已完成休息交易日数<5即休息中
    # F2: remaining_rest_days(current_date,trading_days)→max(0, 5-已完成休息交易日数)
    # O1: bool休息中 / int剩余休息天数 (消费方: 复位链前置校验+盘前启动闸门)
    # [/ALGO_FLOW]
