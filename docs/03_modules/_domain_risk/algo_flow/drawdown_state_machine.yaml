# ALGO_FLOW 外部真源——drawdown_state_machine（_domain_risk 域，真源 src/zephyr/risk/core/drawdown_state_machine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.drawdown_state_machine
source_of_truth: src/zephyr/risk/core/drawdown_state_machine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: state_store 参数
    #   fields: 参数 state_store（无注解）
    #   code: drawdown_state_machine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: drawdown_state_machine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: state_namespace 参数
    #   fields: 参数 state_namespace（无注解）
    #   code: drawdown_state_machine.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: reset_namespace 参数
    #   fields: 参数 reset_namespace（无注解）
    #   code: drawdown_state_machine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DrawdownStateMachine
    #   name_en: DrawdownStateMachine
    #   intro: 回撤持久化状态机——§3.11 转换守卫 + §3.20 hysteresis + §3.14 复位守卫。
    #   desc: 回撤持久化状态机——§3.11 转换守卫 + §3.20 hysteresis + §3.14 复位守卫。 用法: sm = DrawdownStateMachine(store…；公共方法（定义序）: current…
    #   inputs: state_store config state_namespace reset_namespace
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: DrawdownStateMachine
    #   downstream: zephyr.risk.core.drawdown_session_persistence; RiskOrchestrator(§6.5 接线位)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: evaluate日输入(trade_date+drawdown_pct+var_95/cvar_95+recovered_pct+black_swan_systemic+strategy_pnls)
    # I2: JsonStateStore(状态/复位历史外部化, namespace=drawdown_state_machine/drawdown_reset_history)
    # I3: DrawdownStateMachineConfig(升级阈值5/10/15/25%+VaR2/4/6%+CVaR10%; 半阈值比0.5; min_hold5/10/20; 持续窗3/3/5; 毕业准则3盈利日/0.3R/80%/1.2R; 复位守卫3次/20日+冷却3日+永久锁5次)
    # F1: 升级判定(多源OR取最严: dd/ var/cvar/BS-007→目标态, severity高于当前即直接升级)
    # F2: hysteresis降级判定(§3.20: 半阈值持续N日+min_hold门控+VaR回落目标态触发阈值下, 逐级降一档)
    # F3: RECOVERY阶梯机(0→1→2→NORMAL: recovered_pct≥0.5/0.75/1.0+毕业准则+阶梯min_hold5日; dd>15%回退阶梯, 阶梯0再退→KILL; dd>10%且step>0回退; dd>5%冻结5日)
    # A1: request_manual_reset(KILL→RECOVERY唯一出口: 三项确认+复位窗口/冷却/永久锁三重守卫, §3.14)
    # A2: 持久化(每次转换/日推进后原子写快照; load_or_none启动恢复, None=冷启动默认NORMAL)
    # O1: StateTransition(from/to/reason/trade_date)或None + position_cap/recovery_factor/defensive_only查询面
    # [/ALGO_FLOW]
