# ALGO_FLOW 外部真源——black_swan_pattern_library（_domain_risk 域，真源 src/zephyr/risk/core/black_swan_pattern_library.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.risk.core.black_swan_pattern_library
source_of_truth: src/zephyr/risk/core/black_swan_pattern_library.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: features 参数
    #   fields: 参数 features，类型注解 MarketFeatures
    #   code: black_swan_pattern_library.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: black_swan_pattern_library.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① get_pattern_templates
    #   name_en: get_pattern_templates
    #   intro: 返回 7 模式模板库（只读映射）。
    #   desc: 返回 7 模式模板库（只读映射）。；源码 L249-L251
    #   inputs: 无参数
    #   outputs: Mapping[BlackSwanMode, BlackSwanPattern]
    # - id: A2
    #   name_zh: ② screen_black_swan
    #   name_en: screen_black_swan
    #   intro: 黑天鹅模式事前筛查主入口。
    #   desc: 黑天鹅模式事前筛查主入口。 Args: features: 当前市场 7 维体征 config: 配置（None → 各模板自身阈值） Returns: BlackSwanScr…；源码 L300-L336
    #   inputs: features config
    #   outputs: BlackSwanScreenResult
    #   （注：A2 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: Mapping[BlackSwanMode, BlackSwanPattern]
    #   name_en: Mapping[BlackSwanMode, BlackSwanPattern]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-RK-30(Adaptive Risk Coordinator, black_swan_escalated 升级触发); 审计链(匹配记录持久化由调用…
    # - id: O2
    #   name_zh: BlackSwanScreenResult
    #   name_en: BlackSwanScreenResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-RK-30(Adaptive Risk Coordinator, black_swan_escalated 升级触发); 审计链(匹配记录持久化由调用…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: MarketFeatures(7维市场体征: 波动率倍数/回撤/相关性/流动性萎缩/跳空/跌停潮/外围跌幅)
    # I2: 7模式模板库(权重+参考水平+阈值+降仓建议) + BlackSwanConfig(统一阈值覆盖)
    # A1: 逐模式加权相似度评分(score=Σw·clamp(f/r,0,1)/Σw)
    # A2: 命中判定与升级规则(≥2模式或BS-007→escalate; 最严降仓建议)
    # O1: BlackSwanScreenResult(matches/escalate/scale/matching_log) → C-004 升级+提前降仓+审计
    # [/ALGO_FLOW]
