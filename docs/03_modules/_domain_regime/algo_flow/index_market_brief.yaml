# ALGO_FLOW 外部真源——index_market_brief（_domain_regime 域，真源 src/zephyr/regime/index_market_brief.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.regime.index_market_brief
source_of_truth: src/zephyr/regime/index_market_brief.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1 IndexRegimePanel（regime 腿，可 None）
    # - id: I2 各指数日 K dict[code → bars]（预判腿，可 None）
    # - id: I3 市场情绪 label/score（共享注入，可 None）
    # 层: 算法
    # - id: A1 裸码匹配 regime 卡
    # - id: A2 每指数 8 态预判（MOD-SIG-037 引擎）
    # 层: 输出
    # - id: O1 IndexMarketBrief（4 张 IndexBriefCard + 强弱序 + 情绪）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A2
    # A1,A2,I3 --> O1
