# ALGO_FLOW 外部真源——d1_confidence_grid（_domain_regime 域，真源 src/zephyr/regime/validation/d1_confidence_grid.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.regime.validation.d1_confidence_grid
source_of_truth: src/zephyr/regime/validation/d1_confidence_grid.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_p 参数
    #   fields: 参数 max_p，类型注解 float
    #   code: d1_confidence_grid.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: bands 参数
    #   fields: 参数 bands，类型注解 tuple[tuple[float, float], ...]
    #   code: d1_confidence_grid.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: max_p_values 参数
    #   fields: 参数 max_p_values，类型注解 Sequence[float]
    #   code: d1_confidence_grid.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: pct 参数
    #   fields: 参数 pct，类型注解 float
    #   code: d1_confidence_grid.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① apply_confidence_bands
    #   name_en: apply_confidence_bands
    #   intro: 单点 max(P) → base_confidence（从高到低取首个 max(P)≥下界 的档）。
    #   desc: 单点 max(P) → base_confidence（从高到低取首个 max(P)≥下界 的档）。；源码 L149-L154
    #   inputs: max_p bands
    #   outputs: float
    # - id: A2
    #   name_zh: ② run_d1_threshold_grid
    #   name_en: run_d1_threshold_grid
    #   intro: D1 主入口：四档下界 ±20% 全网格敏感性。
    #   desc: D1 主入口：四档下界 ±20% 全网格敏感性。 三档非零下界各取 {×(1−pct), ×1, ×(1+pct)} 共 27 组合；扰动后档界非严格 降序或越出 (0,1] 的…；源码 L167-L252
    #   inputs: max_p_values bands pct tolerance
    #   outputs: D1GridReport
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: float
    #   name_en: float
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 人工审查; 11_regime_backtest_validation_plan Phase 3 D1
    # - id: O2
    #   name_zh: D1GridReport
    #   name_en: D1GridReport
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 人工审查; 11_regime_backtest_validation_plan Phase 3 D1
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: max_p_values(walk-forward 全区间逐日 max(P) 序列, 既有 detector 产物)
    # I2: bands(四档映射 (下界,base) 降序, 默认镜像 detector 生产值) + pct=0.20 扰动幅度
    # F1: apply_confidence_bands(单点 max(P)→base_confidence, 从高到低取首个命中档)
    # A1: run_d1_threshold_grid(三档下界各 ×{0.8,1.0,1.2} 全网格 27 组合→合法组合重放序列→均值/档位占比)
    # A2: 相对变化 |均值−基线均值|/|基线均值| <30% 判定(§4.4 D 类稳健门槛)
    # O1: D1GridReport(基线均值 + 逐网格点统计 + max_rel_change + passed)
    # [/ALGO_FLOW]
