# ALGO_FLOW 外部真源——phase2_runner（_domain_regime 域，真源 src/zephyr/regime/validation/phase2/phase2_runner.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.regime.validation.phase2.phase2_runner
source_of_truth: src/zephyr/regime/validation/phase2/phase2_runner.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: hmm_params 参数
    #   fields: 参数 hmm_params（无注解）
    #   code: phase2_runner.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Phase2Report
    #   name_en: Phase2Report
    #   intro: Phase 2 综合报告。
    #   desc: Phase 2 综合报告。；公共方法（定义序）: to_dict；源码 L125-L145
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② Phase2Runner
    #   name_en: Phase2Runner
    #   intro: Phase 2 编排器：串 A1 → B4 → A2 → B1 → 综合报告.
    #   desc: Phase 2 编排器：串 A1 → B4 → A2 → B1 → 综合报告. Usage（real 模式，复用 C1 真实模式 builder 配置）: builder = R…；公共方法（定义序）: run；源码…
    #   inputs: hmm_params
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: Phase2Report, Phase2Runner
    #   downstream: scripts.tests.run_phase2_validation; BM-BT-05
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
