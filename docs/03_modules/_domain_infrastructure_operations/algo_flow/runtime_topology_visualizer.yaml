# ALGO_FLOW 外部真源——runtime_topology_visualizer（_domain_infrastructure_operations 域，真源 src/zephyr/infra_ops/runtime_topology_visualizer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infra_ops.runtime_topology_visualizer
source_of_truth: src/zephyr/infra_ops/runtime_topology_visualizer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: runtime_topology_visualizer.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: yellow_after_s 参数
    #   fields: 参数 yellow_after_s（无注解）
    #   code: runtime_topology_visualizer.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: red_after_s 参数
    #   fields: 参数 red_after_s（无注解）
    #   code: runtime_topology_visualizer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RuntimeTopologyVisualizer
    #   name_en: RuntimeTopologyVisualizer
    #   intro: 运行时依赖拓扑件（节点/边注册 + 心跳着色 + JSON 快照）。
    #   desc: 运行时依赖拓扑件（节点/边注册 + 心跳着色 + JSON 快照）。；公共方法（定义序）: register_node, register_edge, heartbeat, refresh_status, snapsh…
    #   inputs: clock yellow_after_s red_after_s
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: RuntimeTopologyVisualizer
    #   downstream: 运行时装配批（进程/Redis/GPU/消息/数据馈节点注册与心跳上报 / 仪表盘 JSON 消费）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
