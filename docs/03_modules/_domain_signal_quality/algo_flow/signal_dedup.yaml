# ALGO_FLOW 外部真源——signal_dedup（_domain_signal_quality 域，真源 src/zephyr/signal_quality/signal_dedup.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_quality.signal_dedup
source_of_truth: src/zephyr/signal_quality/signal_dedup.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: signal_dedup.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: signal_dedup.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: similarity_threshold 参数
    #   fields: 参数 similarity_threshold（无注解）
    #   code: signal_dedup.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: window 参数
    #   fields: 参数 window（无注解）
    #   code: signal_dedup.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SignalDedup
    #   name_en: SignalDedup
    #   intro: 信号指纹去重器（纯内存/DI）。
    #   desc: 信号指纹去重器（纯内存/DI）。 - 相似度：指纹四元组等权匹配占比（4 分量全同=1.0，3 同=0.75 …）。 - 合并：相似度**严格大于**阈值（默认 0.9）且 |e…；公共方法（定义序）: fingerp…
    #   inputs: clock audit_sink similarity_threshold window
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: SignalDedup
    #   downstream: 运行时装配批（信号汇流入库前去重 / 串谋检测复查审计回放）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
