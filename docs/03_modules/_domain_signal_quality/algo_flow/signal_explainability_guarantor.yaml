# ALGO_FLOW 外部真源——signal_explainability_guarantor（_domain_signal_quality 域，真源 src/zephyr/signal_quality/signal_explainability_guarantor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_quality.signal_explainability_guarantor
source_of_truth: src/zephyr/signal_quality/signal_explainability_guarantor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: signal_explainability_guarantor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: signal_explainability_guarantor.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: signal_explainability_guarantor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SignalExplainabilityGuarantor
    #   name_en: SignalExplainabilityGuarantor
    #   intro: 可解释性强制保障器（纯内存/DI；缺失即阻断）。
    #   desc: 可解释性强制保障器（纯内存/DI；缺失即阻断）。 强制契约：信号输出必须携带理由链三要素（触发因子/规则命中/置信度依据）。 缺失 → 告警（alert_sink）+ 阻断（抛…；公共方法（定义序）: enforce,…
    #   inputs: audit_sink alert_sink clock
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: SignalExplainabilityGuarantor
    #   downstream: 运行时装配批（信号出站前强制校验 / 审计链 sink 装配 / 事后回放反查）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
