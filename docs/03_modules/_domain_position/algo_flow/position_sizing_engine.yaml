# ALGO_FLOW 外部真源——position_sizing_engine（_domain_position 域，真源 src/zephyr/position/core/position_sizing_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.core.position_sizing_engine
source_of_truth: src/zephyr/position/core/position_sizing_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: position_sizing_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: position_sizing_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PositionSizingEngine
    #   name_en: PositionSizingEngine
    #   intro: 仓位决策引擎——预筛+Kelly+13约束+降级模式。
    #   desc: 仓位决策引擎——预筛+Kelly+13约束+降级模式。 用法: engine = PositionSizingEngine() plan = engine.size(Positi…；公共方法（定义序）: config,…
    #   inputs: config clock
    #   outputs: 返回值
    #   （注：A1 之后另有 10 个公共定义未列入（含 10 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（11 定义）
    #   name_en: public defs
    #   intro: PositionSizingEngine
    #   downstream: D-EX-CORE(执行PositionSizingPlan CTR-POS-001); D-PF-CORE(消费E-POS-01 PositionSized)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
