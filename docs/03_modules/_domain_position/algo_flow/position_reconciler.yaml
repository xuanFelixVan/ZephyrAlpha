# ALGO_FLOW 外部真源——position_reconciler（_domain_position 域，真源 src/zephyr/position/position_reconciler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.position_reconciler
source_of_truth: src/zephyr/position/position_reconciler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: position_reconciler.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① PositionReconciler
    #   name_en: PositionReconciler
    #   intro: 持仓对账器——事件驱动（ExecutionReport到达时触发）
    #   desc: 持仓对账器——事件驱动（ExecutionReport到达时触发）；公共方法（定义序）: reconcile, handle_execution_report, should_escalate；源码 L65-L123
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: PositionReconciler
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
