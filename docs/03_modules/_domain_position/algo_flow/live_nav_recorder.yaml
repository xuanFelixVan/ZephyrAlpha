# ALGO_FLOW 外部真源——live_nav_recorder（_domain_position 域，真源 src/zephyr/position/live_nav_recorder.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.live_nav_recorder
source_of_truth: src/zephyr/position/live_nav_recorder.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1 AssetSnapshot（现金/市值；资产源适配产出）
    # - id: I2 base_nav / benchmark_close / benchmark_base（基准注入位）
    # 层: 算法
    # - id: A1 日频 NavPoint（净值比/基准比）
    # - id: A2 曲线组装（累计收益/最大回撤/超额）
    # 层: 输出
    # - id: O1 NavPoint / NavCurve（frozen dataclass，JSON 可序列化）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1,I2 --> A1
    # A1 --> A2
    # A2 --> O1
