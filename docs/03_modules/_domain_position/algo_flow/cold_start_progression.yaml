# ALGO_FLOW 外部真源——cold_start_progression（_domain_position 域，真源 src/zephyr/position/core/cold_start_progression.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.core.cold_start_progression
source_of_truth: src/zephyr/position/core/cold_start_progression.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: inp 参数
    #   fields: 参数 inp，类型注解 ColdStartEvalInput
    #   code: cold_start_progression.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: cfg 参数
    #   fields: 参数 cfg，类型注解 ColdStartProgressionConfig | None
    #   code: cold_start_progression.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① evaluate_cold_start
    #   name_en: evaluate_cold_start
    #   intro: 评估冷启动阶段流转（纯函数，61 号 §3.1 双门控）。
    #   desc: 评估冷启动阶段流转（纯函数，61 号 §3.1 双门控）。 门控口径：晋升须正面证据（数据缺失 → HOLD 至阶段窗满才回退）； 回退须正面失败证据（divergence 超标…；源码 L199-L230
    #   inputs: inp cfg
    #   outputs: ColdStartEvalResult
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: ColdStartEvalResult
    #   name_en: ColdStartEvalResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 调用方（新策略上线冷启动编排；53 号迁移路径 PARALLEL→SHADOW→GRAY_RAMP 承载时装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: ColdStartEvalInput(stage/days_in_stage/divergence/risk_downgraded/rolling_sharpe/oos_sharpe/loss/decay_alert/consecutive_rollbacks)
    # I2: ColdStartProgressionConfig(T0 5-10 日/T1 10-20 日/divergence 阈值 0.30/Sharpe 系数 0.7/0.85/回退上限 2)
    # F1: T0 门控（divergence<阈值 且未风控降级 → PROMOTE T1；失败证据 → ROLLBACK SIMULATION）
    # F2: T1 门控（Rolling Sharpe≥OOS×0.7 且无连续 3 日亏损超限 → PROMOTE T2；否则 ROLLBACK T0）
    # F3: T2 门控（Rolling Sharpe≥OOS×0.85 且 Decay 无告警 → HOLD；否则 ROLLBACK T1）
    # F4: 回退计数（连续 2 次 → ESCALATE_RETIREMENT）；retrain_paused=stage≠T2
    # O1: ColdStartEvalResult(stage/action/position_ratio/retrain_paused/consecutive_rollbacks/detail)
    # [/ALGO_FLOW]
