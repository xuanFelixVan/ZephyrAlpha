# ALGO_FLOW 外部真源——position_behavior_classifier（_domain_position 域，真源 src/zephyr/position/core/position_behavior_classifier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.core.position_behavior_classifier
source_of_truth: src/zephyr/position/core/position_behavior_classifier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: features 参数
    #   fields: 参数 features，类型注解 Mapping[str, PositionFeatures]
    #   code: position_behavior_classifier.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① classify_positions
    #   name_en: classify_positions
    #   intro: 持仓行为分类（纯函数）。
    #   desc: 持仓行为分类（纯函数）。 Args: features: {symbol: PositionFeatures} Returns: ClassificationReport Rai…；源码 L165-L194
    #   inputs: features
    #   outputs: ClassificationReport
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: ClassificationReport
    #   name_en: ClassificationReport
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: D-SELL-DECISION(行为纠偏参考) ; D_RISK(行为风险预警)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
