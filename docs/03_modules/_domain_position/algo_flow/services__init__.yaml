# ALGO_FLOW 外部真源——__init__（_domain_position 域，真源 src/zephyr/position/services/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.services.__init__
source_of_truth: src/zephyr/position/services/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 审计记录器子模块 包内导入
    #   fields: PositionAuditLogger/PositionAuditRecord/PositionAuditReport/PositionAuditEventType/AuditSource/AuditChainError 共6个符号
    #   code: zephyr.position.services.position_audit_logger L22
    # 层: 算法
    # - id: A1
    #   name_zh: ① 包导出聚合
    #   name_en: __init__ re-export
    #   intro: 把审计子模块的6个符号聚成包级命名空间对外提供
    #   desc: from position_audit_logger import 6符号并写入__all__，无业务逻辑
    #   inputs: I1
    #   outputs: 包级导出列表
    # 层: 输出
    # - id: O1
    #   name_zh: 包命名空间导出
    #   name_en: zephyr.position.services.__all__
    #   intro: 仓位审计服务的统一入口，外部from包名即可拿到6个符号
    #   downstream: 无下游/内部使用(CONSUMERS为空)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
