# ALGO_FLOW 外部真源——core_satellite_allocator（_domain_position 域，真源 src/zephyr/position/core/core_satellite_allocator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.core.core_satellite_allocator
source_of_truth: src/zephyr/position/core/core_satellite_allocator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: core_satellite_allocator.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① CoreSatelliteAllocator
    #   name_en: CoreSatelliteAllocator
    #   intro: 核心-卫星仓位管理——结构分配+做T信号+换仓触发。
    #   desc: 核心-卫星仓位管理——结构分配+做T信号+换仓触发。 用法: allocator = CoreSatelliteAllocator() candidates = [Candida…；公共方法（定义序）: allocat…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 8 个公共定义未列入（含 8 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（9 定义）
    #   name_en: public defs
    #   intro: CoreSatelliteAllocator
    #   downstream: MOD-POS-001(精裁) ; MOD-POS-024(裁决) ; MOD-SELL-018(做T执行)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 候选标的 CandidateAsset
    #   fields: symbol/kelly_fraction/rs_pct/price/vwap/atr
    # - id: I2
    #   name: 核心-卫星配置 CoreSatelliteConfig
    #   fields: satellite_cap/single_name_cap/core_atr_k/satellite_atr_k/t_band_atr/rs_keep_pct
    # 层: 算法
    # - id: A1
    #   name_zh: ① 核心-卫星分组
    #   name_en: allocate
    #   intro: 按kelly降序,核心仓先配至core_budget,卫星≤satellite_cap硬帽,超出截断
    # - id: A2
    #   name_zh: ② 权重与止损参数分轨
    #   name_en: weight_and_stop
    #   intro: 单标的=half-kelly(kelly_fraction×0.5)≤single_name_cap;核心k=core_atr_k/卫星k=satellite_atr_k
    # - id: A3
    #   name_zh: ③ 卫星做T信号
    #   name_en: satellite_t_signals
    #   intro: price>vwap+t_band_atr×atr→SELL_PART; price<vwap−t_band_atr×atr→BUY_BACK; 仅卫星仓
    # - id: A4
    #   name_zh: ④ RS排名换仓触发
    #   name_en: rs_swap_check
    #   intro: 卫星rs_pct跌出rs_keep_pct且池外challenger rs_pct更高→SwapTrigger(out=掉队,in=challenger)
    # 层: 输出
    # - id: O1
    #   name_zh: 核心-卫星方案 CoreSatellitePlan
    #   name_en: CoreSatellitePlan
    #   intro: legs/satellite_weight/t_signals/swap_triggers/notes完整结构方案
    #   downstream: MOD-POS-001 精裁; MOD-POS-024 裁决; MOD-SELL-018 做T执行 ([CONSUMERS] 头)
    # [/ALGO_FLOW]
