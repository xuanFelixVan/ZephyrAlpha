# ALGO_FLOW 外部真源——intraday_position_constraint（_domain_position 域，真源 src/zephyr/position/core/intraday_position_constraint.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.core.intraday_position_constraint
source_of_truth: src/zephyr/position/core/intraday_position_constraint.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: intent 参数
    #   fields: 参数 intent，类型注解 IntradayConstraintInput
    #   code: intraday_position_constraint.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① check_intraday_constraints
    #   name_en: check_intraday_constraints
    #   intro: 盘中仓位约束校验（纯函数，Fail-Closed）。
    #   desc: 盘中仓位约束校验（纯函数，Fail-Closed）。 Args: intent: IntradayConstraintInput（昨仓/今买/今卖/拟买卖/上限） Returns…；源码 L164-L253
    #   inputs: intent
    #   outputs: IntradayConstraintResult
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: IntradayConstraintResult
    #   name_en: IntradayConstraintResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: D-EX-CORE(执行前盘中校验) ; MOD-SELL-018(做T协调器) ; D_RISK
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
