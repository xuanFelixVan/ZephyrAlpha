# ALGO_FLOW 外部真源——__init__（_domain_position 域，真源 src/zephyr/position/models/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.models.__init__
source_of_truth: src/zephyr/position/models/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 包初始化导入 空包
    #   fields: 无任何子模块导入，仅包标记注释
    #   code: models/__init__.py L1
    # 层: 算法
    # - id: A1
    #   name_zh: ① 包命名空间占位
    #   name_en: __init__ placeholder
    #   intro: 仅声明空的__all__列表，作为models包占位
    #   desc: __all__: list[str]=[]，无任何导入与逻辑
    #   inputs: I1
    #   outputs: 空导出列表
    # 层: 输出
    # - id: O1
    #   name_zh: 空包导出
    #   name_en: zephyr.position.models.__all__
    #   intro: 空列表，当前不对外导出任何符号
    #   downstream: 无下游/内部使用
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
