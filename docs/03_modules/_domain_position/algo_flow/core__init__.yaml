# ALGO_FLOW 外部真源——__init__（_domain_position 域，真源 src/zephyr/position/core/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.position.core.__init__
source_of_truth: src/zephyr/position/core/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 子模块公共类定义
    #   fields: DrawdownController（drawdown_controller）+ PositionSizingEngine（position_sizing_engine）
    #   code: position/core/__init__.py L7-8
    # 层: 算法
    # - id: A1
    #   name_zh: ① 包门面再导出
    #   name_en: position.core __init__
    #   intro: 把核心子模块的两个公共类聚合成 position.core 包级命名空间
    #   desc: from 子模块 import 两类 + __all__ 白名单声明（L7-10），无计算逻辑
    #   inputs: I1
    #   outputs: 包级公共 API
    # 层: 输出
    # - id: O1
    #   name_zh: position.core 公共 API
    #   name_en: DrawdownController / PositionSizingEngine
    #   intro: 对上游暴露回撤控制器与仓位裁决引擎两个入口类
    #   downstream: zephyr.position 内部使用（仓位域各模块 import）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
