# ALGO_FLOW 外部真源——admission_response（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/behavioral_admission/admission_response.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.behavioral_admission.admission_response
source_of_truth: src/zephyr/gov_enforcement/behavioral_admission/admission_response.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: admission_controller 参数
    #   fields: 参数 admission_controller（无注解）
    #   code: admission_response.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AdmissionResponseBuilder
    #   name_en: AdmissionResponseBuilder
    #   intro: class AdmissionResponseBuilder 源码 L136-L258
    #   desc: 公共方法（定义序）: build_response, build_batch_responses；源码 L136-L258
    #   inputs: admission_controller
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: AdmissionResponseBuilder
    #   downstream: GovernanceServer;run_all.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
