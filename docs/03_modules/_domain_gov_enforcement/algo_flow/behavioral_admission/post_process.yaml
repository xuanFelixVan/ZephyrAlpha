# ALGO_FLOW 外部真源——post_process（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/behavioral_admission/post_process.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.behavioral_admission.post_process
source_of_truth: src/zephyr/gov_enforcement/behavioral_admission/post_process.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: files 参数
    #   fields: 参数 files，类型注解 list[str] | None
    #   code: post_process.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PipelineResult
    #   name_en: PipelineResult
    #   intro: 后处理管道执行结果。
    #   desc: 后处理管道执行结果。；公共方法（定义序）: all_passed；源码 L143-L154
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② PostProcessPipeline
    #   name_en: PostProcessPipeline
    #   intro: AI 生成代码后处理管道——注册 hook -> 按序执行 -> 策略决策。
    #   desc: AI 生成代码后处理管道——注册 hook -> 按序执行 -> 策略决策。 Usage:: pipeline = PostProcessPipeline() pipeline.…；公共方法（定义序）: hooks,…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A3
    #   name_zh: ③ lint_hook
    #   name_en: lint_hook
    #   intro: 内置 lint hook——调用 ruff check。
    #   desc: 内置 lint hook——调用 ruff check。；源码 L301-L327
    #   inputs: files
    #   outputs: HookResult
    # - id: A4
    #   name_zh: ④ format_hook
    #   name_en: format_hook
    #   intro: 内置 format hook——调用 ruff format --check。
    #   desc: 内置 format hook——调用 ruff format --check。；源码 L330-L356
    #   inputs: files
    #   outputs: HookResult
    # - id: A5
    #   name_zh: ⑤ typecheck_hook
    #   name_en: typecheck_hook
    #   intro: 内置 typecheck hook——调用 pyright / mypy。
    #   desc: 内置 typecheck hook——调用 pyright / mypy。；源码 L359-L385
    #   inputs: files
    #   outputs: HookResult
    #   （注：A5 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: HookResult
    #   name_en: HookResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.shared (re-export)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> A4
    # A4 --> A5
    # A5 --> O1
