# ALGO_FLOW 外部真源——protection_index（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/behavioral_admission/protection_index.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.behavioral_admission.protection_index
source_of_truth: src/zephyr/gov_enforcement/behavioral_admission/protection_index.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: protection_index.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: bloom_expected_items 参数
    #   fields: 参数 bloom_expected_items（无注解）
    #   code: protection_index.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: bloom_fp_rate 参数
    #   fields: 参数 bloom_fp_rate（无注解）
    #   code: protection_index.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ProtectionIndex
    #   name_en: ProtectionIndex
    #   intro: class ProtectionIndex 源码 L266-L413
    #   desc: 公共方法（定义序）: query, query_batch, is_anchor, get_entry, register, unregister, rebuild, get_stats, verify_integri…
    #   inputs: project_root bloom_expected_items bloom_fp_rate
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ProtectionIndex
    #   downstream: zephyr.gov_enforcement.behavioral_admission.verdict_engine;MOD-INF-027(audit-or…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
