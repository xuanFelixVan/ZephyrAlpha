# ALGO_FLOW 外部真源——orchestrator（_domain_gov_enforcement 域，真源 src/zephyr/clone_guard/orchestrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.clone_guard.orchestrator
source_of_truth: src/zephyr/clone_guard/orchestrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: repo_root 参数
    #   fields: 参数 repo_root（无注解）
    #   code: orchestrator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: orchestrator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CloneGuardOrchestrator
    #   name_en: CloneGuardOrchestrator
    #   intro: CloneGuard 统一编排器。
    #   desc: CloneGuard 统一编排器。 Phase A: 仅调度 Echo-Guard Phase B: + ast-grep + reDUP 并发 + FindingAggrega…；公共方法（定义序）: check…
    #   inputs: repo_root config
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: CloneGuardOrchestrator
    #   downstream: zephyr.gov_enforcement.commit_gates.capability_overlap_gate; zephyr.clone_guard…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
