# ALGO_FLOW 外部真源——depgraph_freshness_gate（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/commit_gates/depgraph_freshness_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.commit_gates.depgraph_freshness_gate
source_of_truth: src/zephyr/gov_enforcement/commit_gates/depgraph_freshness_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: depgraph_freshness_gate.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① make_depgraph_freshness_gate
    #   name_en: make_depgraph_freshness_gate
    #   intro: 构造 depgraph 新鲜度门禁 GateSpec（dual-threshold，阻断型）；缓存权威源=主工作树副本。
    #   desc: 构造 depgraph 新鲜度门禁 GateSpec（dual-threshold，阻断型）。 Returns: GateSpec(gate_id="DEPGRAPH-FRESH…；新鲜度是项目整体事实，故 linked worktree（队列落地 worktree/会话 worktree）内以主工作树 .runtime 副本为权威，主树副本缺失才回落本地副本（fail-open 面不放松），权威优先同时封死"在 worktree 内植入新鲜副本"的逃逸；源码 L157-L251
    #   inputs: 无参数
    #   outputs: GateSpec
    # 层: 输出
    # - id: O1
    #   name_zh: GateSpec
    #   name_en: GateSpec
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.gov_enforcement.rule_bridge.git_commit_gateway.GitCommitGateway.__init__
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
