# ALGO_FLOW 外部真源——blueprint_node_id_hardcode_gate（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/commit_gates/blueprint_node_id_hardcode_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.commit_gates.blueprint_node_id_hardcode_gate
source_of_truth: src/zephyr/gov_enforcement/commit_gates/blueprint_node_id_hardcode_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: blueprint_node_id_hardcode_gate.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① make_blueprint_node_id_hardcode_gate
    #   name_en: make_blueprint_node_id_hardcode_gate
    #   intro: 构造 blueprint.md node_id/edge_id 硬编码阻断门禁 GateSpec（硬阻断型）。
    #   desc: 构造 blueprint.md node_id/edge_id 硬编码阻断门禁 GateSpec（硬阻断型）。 Returns: GateSpec(gate_id="BLUEPR…；源码 L241-L271
    #   inputs: 无参数
    #   outputs: GateSpec
    # 层: 输出
    # - id: O1
    #   name_zh: GateSpec
    #   name_en: GateSpec
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.gov_enforcement.rule_bridge.git_commit_gateway.GitCommitGateway.__init__…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
