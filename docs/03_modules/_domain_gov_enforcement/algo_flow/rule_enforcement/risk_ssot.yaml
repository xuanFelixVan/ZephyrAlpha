# ALGO_FLOW 外部真源——risk_ssot（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/rule_enforcement/risk_ssot.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.rule_enforcement.risk_ssot
source_of_truth: src/zephyr/gov_enforcement/rule_enforcement/risk_ssot.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root，类型注解 Path
    #   code: risk_ssot.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① load_risk_params_ssot
    #   name_en: load_risk_params_ssot
    #   intro: load_risk_params_ssot(project_root) 源码 L57-L64
    #   desc: 源码 L57-L64
    #   inputs: project_root
    #   outputs: dict[str, Any]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
