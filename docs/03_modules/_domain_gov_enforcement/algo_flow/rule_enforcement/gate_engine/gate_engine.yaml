# ALGO_FLOW 外部真源——gate_engine（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/rule_enforcement/gate_engine/gate_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.rule_enforcement.gate_engine.gate_engine
source_of_truth: src/zephyr/gov_enforcement/rule_enforcement/gate_engine/gate_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: gate_dir 参数
    #   fields: 参数 gate_dir（无注解）
    #   code: gate_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: db_path 参数
    #   fields: 参数 db_path（无注解）
    #   code: gate_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: gate_engine.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: auto_init 参数
    #   fields: 参数 auto_init（无注解）
    #   code: gate_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① GateEngine
    #   name_en: GateEngine
    #   intro: 门禁裁决引擎。
    #   desc: 门禁裁决引擎。 参数 ---- gate_dir 存放各门禁 YAML（含 ``task/*.yaml``）的目录；默认与本模块同级。 db_path SQLite 数据库路径；…；公共方法（定义序）: is_mand…
    #   inputs: gate_dir db_path project_root auto_init
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: GateEngine
    #   downstream: zephyr.governance.persistence.task_repo; zephyr.governance.lifecycle_governance…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
