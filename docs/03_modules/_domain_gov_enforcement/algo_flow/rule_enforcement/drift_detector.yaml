# ALGO_FLOW 外部真源——drift_detector（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/rule_enforcement/drift_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.rule_enforcement.drift_detector
source_of_truth: src/zephyr/gov_enforcement/rule_enforcement/drift_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: payload 参数
    #   fields: 参数 payload，类型注解 dict[str, Any]
    #   code: drift_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① trigger_recovery
    #   name_en: trigger_recovery
    #   intro: ``drift_detected`` 触发器恢复入口。
    #   desc: ``drift_detected`` 触发器恢复入口。 Parameters ---------- payload : dict trigger_router.dispatch(…；源码 L86-L182
    #   inputs: payload
    #   outputs: dict[str, Any]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
