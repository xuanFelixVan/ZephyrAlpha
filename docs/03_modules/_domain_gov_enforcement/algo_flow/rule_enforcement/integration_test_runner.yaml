# ALGO_FLOW 外部真源——integration_test_runner（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/rule_enforcement/integration_test_runner.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.rule_enforcement.integration_test_runner
source_of_truth: src/zephyr/gov_enforcement/rule_enforcement/integration_test_runner.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: test_id 参数
    #   fields: 参数 test_id（无注解）
    #   code: integration_test_runner.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: passed 参数
    #   fields: 参数 passed（无注解）
    #   code: integration_test_runner.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: tests_run 参数
    #   fields: 参数 tests_run（无注解）
    #   code: integration_test_runner.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: failures 参数
    #   fields: 参数 failures（无注解）
    #   code: integration_test_runner.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SelfTestResult
    #   name_en: SelfTestResult
    #   intro: 自检结果.
    #   desc: 自检结果. 每个 check 是 dict: {"check": str, "status": str, "detail": str} status 取值: PASS/FAIL/…；公共方法（定义序）: to_dict…
    #   inputs: test_id passed tests_run failures errors checks run_at
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② IntegrationTestRunner
    #   name_en: IntegrationTestRunner
    #   intro: 集成测试运行器——执行 pip_check/import_check/type_check 并聚合结果.
    #   desc: 集成测试运行器——执行 pip_check/import_check/type_check 并聚合结果.；公共方法（定义序）: finalize, project_root, result_dir, add_resul…
    #   inputs: project_root
    #   outputs: 返回值
    #   （注：A2 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: SelfTestResult, IntegrationTestRunner
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
