# ALGO_FLOW 外部真源——__init__（_domain_gov_enforcement 域，真源 src/zephyr/gov_enforcement/rule_enforcement/rule_engine/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_enforcement.rule_enforcement.rule_engine.__init__
source_of_truth: src/zephyr/gov_enforcement/rule_enforcement/rule_engine/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: rule_* 引擎子模块
    #   fields: rule_engine / rule_canary_manager / rule_debt_auditor / rule_shadow_runner 四个实现文件
    #   code: __all__ L22
    # 层: 算法
    # - id: A1
    #   name_zh: ① 包聚合声明
    #   name_en: __all__ 导出列表
    #   intro: 把五个 rule_* 引擎实现归并到一个包命名空间下，仅是 ARCH-042 拆分的聚合壳
    #   desc: 空包 __init__，仅声明 __all__ 列表登记子模块名，不做任何运行时逻辑
    #   inputs: I1
    #   outputs: 子模块命名空间
    # 层: 输出
    # - id: O1
    #   name_zh: rule_engine 包命名空间
    #   name_en: rule_engine package
    #   intro: 对外暴露五个子模块入口，供 rule_enforcement 内部按需 import
    #   downstream: rule_enforcement 内部模块（# [CONSUMERS] 头）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
