# ALGO_FLOW 外部真源——config（_domain_gov_enforcement 域，真源 src/zephyr/clone_guard/config.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.clone_guard.config
source_of_truth: src/zephyr/clone_guard/config.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: repo_root 参数
    #   fields: 参数 repo_root，类型注解 Path
    #   code: config.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CloneGuardConfig
    #   name_en: CloneGuardConfig
    #   intro: CloneGuard 统一配置（从 clone_guard.yml 加载）。
    #   desc: CloneGuard 统一配置（从 clone_guard.yml 加载）。；公共方法（定义序）: block_severities；源码 L73-L144
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② load_config
    #   name_en: load_config
    #   intro: 从 repo_root/clone_guard.yml 加载配置，失败时返回安全默认值。
    #   desc: 从 repo_root/clone_guard.yml 加载配置，失败时返回安全默认值。 Args: repo_root: 仓库根目录路径。 Returns: CloneGuar…；源码 L147-L216
    #   inputs: repo_root
    #   outputs: CloneGuardConfig
    # 层: 输出
    # - id: O1
    #   name_zh: CloneGuardConfig
    #   name_en: CloneGuardConfig
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.clone_guard.orchestrator; zephyr.clone_guard.engines.echo_guard_adapter
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
