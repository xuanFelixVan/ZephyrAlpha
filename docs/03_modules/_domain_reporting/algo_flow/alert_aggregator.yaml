# ALGO_FLOW 外部真源——alert_aggregator（_domain_reporting 域，真源 src/zephyr/reporting/alert_aggregator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.reporting.alert_aggregator
source_of_truth: src/zephyr/reporting/alert_aggregator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1 风控告警（Alert 鸭型：level/source/message/timestamp）
    # - id: I2 数据质量失败记录（task_id/error/level/source/timestamp/extra）
    # - id: I3 回测完成事件（run_id/status/summary/finished_at/metrics）
    # 层: 算法
    # - id: A1 三源适配器（→UnifiedAlert 四级严重度）
    # - id: A2 去重+排序（同批 (source,title) 保留 occurred_at 最新）
    # - id: A3 阈值派发（≥min_dispatch_severity → Notification → manager.send）
    # 层: 输出
    # - id: O1 AggregationResult（alerts 页面流 + dispatched/suppressed 计数 + notes）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1,I2,I3 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1
