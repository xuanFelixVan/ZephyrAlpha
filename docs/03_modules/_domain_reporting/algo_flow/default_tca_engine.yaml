# ALGO_FLOW 外部真源——default_tca_engine（_domain_reporting 域，真源 src/zephyr/reporting/default_tca_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.reporting.default_tca_engine
source_of_truth: src/zephyr/reporting/default_tca_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: benchmark_price_source 参数
    #   fields: 参数 benchmark_price_source（无注解）
    #   code: default_tca_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DefaultTCAEngine
    #   name_en: DefaultTCAEngine
    #   intro: 默认 TCA 引擎——滑点/佣金/冲击成本分析
    #   desc: 默认 TCA 引擎——滑点/佣金/冲击成本分析；公共方法（定义序）: analyze, analyze_batch；源码 L88-L203
    #   inputs: benchmark_price_source
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: DefaultTCAEngine
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
