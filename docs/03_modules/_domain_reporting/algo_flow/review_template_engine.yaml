# ALGO_FLOW 外部真源——review_template_engine（_domain_reporting 域，真源 src/zephyr/reporting/review_template_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.reporting.review_template_engine
source_of_truth: src/zephyr/reporting/review_template_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: period 参数
    #   fields: 参数 period，类型注解 str
    #   code: review_template_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: pnl_attribution 参数
    #   fields: 参数 pnl_attribution，类型注解 str
    #   code: review_template_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: deviation_events 参数
    #   fields: 参数 deviation_events，类型注解 str
    #   code: review_template_engine.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: threshold_changes 参数
    #   fields: 参数 threshold_changes，类型注解 str
    #   code: review_template_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① render_weekly_review
    #   name_en: render_weekly_review
    #   intro: 渲染四段式周复盘 markdown（结构固定，空段降级）。
    #   desc: 渲染四段式周复盘 markdown（结构固定，空段降级）。 Args: period: 复盘周期标识（如 "2026-W34"）。 pnl_attribution: 第①段 本周…；源码 L105-L154
    #   inputs: period pnl_attribution deviation_events threshold_changes action_item…
    #   outputs: str
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-RPT-009(ReviewOrchestrator.run_weekly 装配可选升级); 调用方(周复盘事件驱动)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: period(复盘周期) + 四段内容(pnl_attribution/deviation_events/threshold_changes 字符串, action_items 字符串序列) + risk_overview(可选风险概览行)
    # F1: render_weekly_review()——按 weekly_review_template.md 结构渲染四段式周报 markdown
    # F2: 空段降级——空字符串/空序列段渲染"（本期无）"(结构完整不留空洞)
    # A1: 段标题渲染顺序=WEEKLY_REVIEW_SECTIONS 常量序(四段固定)
    # O1: markdown 字符串(调用方经 ReportPublisher TRADING_REVIEW 源归档)
    # [/ALGO_FLOW]
