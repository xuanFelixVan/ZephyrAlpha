# ALGO_FLOW 外部真源——reconciliation_schema（_domain_reporting 域，真源 src/zephyr/reporting/reconciliation_schema.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.reporting.reconciliation_schema
source_of_truth: src/zephyr/reporting/reconciliation_schema.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: table_name 参数
    #   fields: 参数 table_name，类型注解 str
    #   code: reconciliation_schema.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① table_names
    #   name_en: table_names
    #   intro: 返回已定义的表名（有序）。
    #   desc: 返回已定义的表名（有序）。；源码 L171-L173
    #   inputs: 无参数
    #   outputs: tuple[str, ...]
    # - id: A2
    #   name_zh: ② get_ddl
    #   name_en: get_ddl
    #   intro: 按表名取 DDL；未知表名抛 KeyError。
    #   desc: 按表名取 DDL；未知表名抛 KeyError。；源码 L176-L178
    #   inputs: table_name
    #   outputs: str
    # 层: 输出
    # - id: O1
    #   name_zh: tuple[str, ...]
    #   name_en: tuple[str, ...]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 54_reconciliation_attribution §3.3/§3.7 落库施工批次(DDL 执行方)
    # - id: O2
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 54_reconciliation_attribution §3.3/§3.7 落库施工批次(DDL 执行方)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: 无运行时输入（纯 DDL 常量模块）
    # F1: reconciliation_differences——对账差异表(成交对账 5 类 DriftType 落库, 54 号 §3.3)
    # F2: attribution_results——归因结果表(策略层/firm 层两层归因产物, §3.5 两层架构)
    # F3: audit_trail——三阶段审计轨迹(原始事件/匹配决策/归因结果, §3.3, append-only + 哈希链)
    # F4: report_archive——报告归档表(§3.7 ReportPublisher DB 归档位, 对齐哈希链模式)
    # O1: ALL_TABLES 有序 DDL 元组 + table_names() 查询
    # [/ALGO_FLOW]
