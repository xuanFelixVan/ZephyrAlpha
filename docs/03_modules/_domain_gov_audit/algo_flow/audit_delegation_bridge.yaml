# ALGO_FLOW 外部真源——audit_delegation_bridge（_domain_gov_audit 域，真源 src/zephyr/gov_audit/bridges/audit_delegation_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.bridges.audit_delegation_bridge
source_of_truth: src/zephyr/gov_audit/bridges/audit_delegation_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: audit_delegation_bridge.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① AuditDelegationBridge
    #   name_en: AuditDelegationBridge
    #   intro: 审计↔委托链桥接器.
    #   desc: 审计↔委托链桥接器. 将委托操作写入审计日志， 并在委托深度超限或权限扩大时触发异常。；公共方法（定义序）: record_delegation, check_depth_anomaly, audit_delegati…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AuditDelegationBridge
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
