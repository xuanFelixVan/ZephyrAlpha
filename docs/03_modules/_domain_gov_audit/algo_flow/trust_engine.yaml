# ALGO_FLOW 外部真源——trust_engine（_domain_gov_audit 域，真源 src/zephyr/gov_audit/trust_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.trust_engine
source_of_truth: src/zephyr/gov_audit/trust_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: initial_score 参数
    #   fields: 参数 initial_score（无注解）
    #   code: trust_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: decay_rate 参数
    #   fields: 参数 decay_rate（无注解）
    #   code: trust_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: floor 参数
    #   fields: 参数 floor（无注解）
    #   code: trust_engine.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: ceiling 参数
    #   fields: 参数 ceiling（无注解）
    #   code: trust_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TrustEngine
    #   name_en: TrustEngine
    #   intro: class TrustEngine 源码 L90-L150
    #   desc: 公共方法（定义序）: calculate, update_history, trend；源码 L90-L150
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② TrustScoreEngine
    #   name_en: TrustScoreEngine
    #   intro: 信任评分引擎（补全测试期望接口）。
    #   desc: 信任评分引擎（补全测试期望接口）。 维护 agent -> TrustRecord 映射，支持评分调整、衰减和查询。；公共方法（定义序）: records, get_or_create, initial_score,…
    #   inputs: initial_score decay_rate floor ceiling
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: TrustEngine, TrustScoreEngine
    #   downstream: audit-orchestrator.integrity; pipeline_runner
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
