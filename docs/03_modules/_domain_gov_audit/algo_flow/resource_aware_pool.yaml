# ALGO_FLOW 外部真源——resource_aware_pool（_domain_gov_audit 域，真源 src/zephyr/gov_audit/resource_aware_pool.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.resource_aware_pool
source_of_truth: src/zephyr/gov_audit/resource_aware_pool.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: cpu_workers 参数
    #   fields: 参数 cpu_workers（无注解）
    #   code: resource_aware_pool.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: gpu_workers 参数
    #   fields: 参数 gpu_workers（无注解）
    #   code: resource_aware_pool.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: max_pending 参数
    #   fields: 参数 max_pending（无注解）
    #   code: resource_aware_pool.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ResourceAwarePool
    #   name_en: ResourceAwarePool
    #   intro: class ResourceAwarePool 源码 L83-L162
    #   desc: 公共方法（定义序）: gpu_futures, cpu_futures, submit, shutdown, stats；源码 L83-L162
    #   inputs: cpu_workers gpu_workers max_pending
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: ResourceAwarePool
    #   downstream: audit_admission_controller; orchestrator
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
