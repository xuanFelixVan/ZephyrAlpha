# ALGO_FLOW 外部真源——replay_engine（_domain_gov_audit 域，真源 src/zephyr/gov_audit/replay_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.replay_engine
source_of_truth: src/zephyr/gov_audit/replay_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: event_log_path 参数
    #   fields: 参数 event_log_path（无注解）
    #   code: replay_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: snapshot_interval 参数
    #   fields: 参数 snapshot_interval（无注解）
    #   code: replay_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ReplayEngine
    #   name_en: ReplayEngine
    #   intro: 重放引擎（补全测试期望接口）。
    #   desc: 重放引擎（补全测试期望接口）。 支持基于 Lamport clock 排序的事件重放，构建最终状态和快照。；公共方法（定义序）: snapshot_interval, replay, replay_range, ver…
    #   inputs: event_log_path snapshot_interval
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ReplayEngine
    #   downstream: audit-orchestrator.pipeline_runner; integrity
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
