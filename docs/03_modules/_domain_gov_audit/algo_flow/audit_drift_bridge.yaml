# ALGO_FLOW 外部真源——audit_drift_bridge（_domain_gov_audit 域，真源 src/zephyr/gov_audit/bridges/audit_drift_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.bridges.audit_drift_bridge
source_of_truth: src/zephyr/gov_audit/bridges/audit_drift_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: audit_events_path 参数
    #   fields: 参数 audit_events_path（无注解）
    #   code: audit_drift_bridge.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DriftBridge
    #   name_en: DriftBridge
    #   intro: 审计追踪链 ↔ 漂移检测器双向桥接。
    #   desc: 审计追踪链 ↔ 漂移检测器双向桥接。 使用方式: bridge = DriftBridge() result = bridge.sync() if result.critical…；公共方法（定义序）: sync；源码…
    #   inputs: audit_events_path
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: DriftBridge
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
