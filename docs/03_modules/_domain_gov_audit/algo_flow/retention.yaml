# ALGO_FLOW 外部真源——retention（_domain_gov_audit 域，真源 src/zephyr/gov_audit/retention.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.retention
source_of_truth: src/zephyr/gov_audit/retention.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: data_dir 参数
    #   fields: 参数 data_dir（无注解）
    #   code: retention.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: policy 参数
    #   fields: 参数 policy（无注解）
    #   code: retention.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RetentionEnforcer
    #   name_en: RetentionEnforcer
    #   intro: 保留策略执行器（补全测试期望接口）。
    #   desc: 保留策略执行器（补全测试期望接口）。 扫描 data_dir 下的 hot/warm/cold 子目录，识别过期文件并根据策略删除。 require_owner_approval…；公共方法（定义序）: approve…
    #   inputs: data_dir policy
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: RetentionEnforcer
    #   downstream: audit-orchestrator.tiered_storage; log_rotation
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
