# ALGO_FLOW 外部真源——audit_write_failure_protector（_domain_gov_audit 域，真源 src/zephyr/gov_audit/audit_write_failure_protector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.audit_write_failure_protector
source_of_truth: src/zephyr/gov_audit/audit_write_failure_protector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: writer 参数
    #   fields: 参数 writer（无注解）
    #   code: audit_write_failure_protector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AuditWriteProtector
    #   name_en: AuditWriteProtector
    #   intro: 审计写入失败保护器——委托 AuditWriter 内置保护。
    #   desc: 审计写入失败保护器——委托 AuditWriter 内置保护。；公共方法（定义序）: record_failure, can_write, reset；源码 L60-L96
    #   inputs: writer
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AuditWriteProtector
    #   downstream: zephyr.infrastructure.escalation
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
