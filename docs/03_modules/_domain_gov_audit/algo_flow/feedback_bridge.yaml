# ALGO_FLOW 外部真源——feedback_bridge（_domain_gov_audit 域，真源 src/zephyr/gov_audit/feedback_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.feedback_bridge
source_of_truth: src/zephyr/gov_audit/feedback_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: storage_path 参数
    #   fields: 参数 storage_path（无注解）
    #   code: feedback_bridge.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FeedbackBridge
    #   name_en: FeedbackBridge
    #   intro: Bridge between audit-trail anomaly findings and the Feedbac…
    #   desc: Bridge between audit-trail anomaly findings and the Feedback Loop Engine. Two parallel AP…；公共方法（定义序）: anomaly…
    #   inputs: storage_path
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: FeedbackBridge
    #   downstream: audit-orchestrator.feedback_policy(策略引擎消费反馈)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
