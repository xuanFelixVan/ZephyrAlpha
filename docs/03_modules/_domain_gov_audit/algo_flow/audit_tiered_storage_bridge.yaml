# ALGO_FLOW 外部真源——audit_tiered_storage_bridge（_domain_gov_audit 域，真源 src/zephyr/gov_audit/bridges/audit_tiered_storage_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.bridges.audit_tiered_storage_bridge
source_of_truth: src/zephyr/gov_audit/bridges/audit_tiered_storage_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: data_dir 参数
    #   fields: 参数 data_dir（无注解）
    #   code: audit_tiered_storage_bridge.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AuditTieredStorageBridge
    #   name_en: AuditTieredStorageBridge
    #   intro: 审计↔三层存储桥接器.
    #   desc: 审计↔三层存储桥接器. 将审计日志按时间分层: 热 (HOT): ≤7天 — JSONL 原文，SSD 级延迟 温 (WARM): 8~90天 — gzip 压缩 JSONL 冷…；公共方法（定义序）: cold_di…
    #   inputs: data_dir
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AuditTieredStorageBridge
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
