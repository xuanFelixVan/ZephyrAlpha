# ALGO_FLOW 外部真源——privacy（_domain_gov_audit 域，真源 src/zephyr/gov_audit/privacy.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_audit.privacy
source_of_truth: src/zephyr/gov_audit/privacy.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: path 参数
    #   fields: 参数 path，类型注解 str
    #   code: privacy.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PrivacyGuard
    #   name_en: PrivacyGuard
    #   intro: class PrivacyGuard 源码 L119-L208
    #   desc: 公共方法（定义序）: default_policy, patterns, detect_pii, redact, hash_path；源码 L119-L208
    #   inputs: custom_patterns default_policy
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② hash_path
    #   name_en: hash_path
    #   intro: hash_path(path) 源码 L211-L213
    #   desc: 源码 L211-L213
    #   inputs: path
    #   outputs: str
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
