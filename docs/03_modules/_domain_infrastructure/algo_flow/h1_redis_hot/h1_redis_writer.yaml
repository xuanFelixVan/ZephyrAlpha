# ALGO_FLOW 外部真源——h1_redis_writer（_domain_infrastructure 域，真源 src/zephyr/infrastructure/h1_redis_hot/h1_redis_writer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.h1_redis_hot.h1_redis_writer
source_of_truth: src/zephyr/infrastructure/h1_redis_hot/h1_redis_writer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: redis_conn 参数
    #   fields: 参数 redis_conn（无注解）
    #   code: h1_redis_writer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① H1RedisWriter
    #   name_en: H1RedisWriter
    #   intro: D-FACTOR Engine 每 3 秒截面写入 Redis（PIPELINE 模式）。
    #   desc: D-FACTOR Engine 每 3 秒截面写入 Redis（PIPELINE 模式）。 蓝图 §4.1 接口实现。；公共方法（定义序）: write_factor_cross_section, write_tick…
    #   inputs: redis_conn
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: H1RedisWriter
    #   downstream: zephyr.factor.engine; zephyr.data.ch_writer
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
