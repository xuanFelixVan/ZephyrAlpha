# ALGO_FLOW 外部真源——event_bus_upgrade（_domain_infrastructure 域，真源 src/zephyr/infrastructure/event_bus_upgrade.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.event_bus_upgrade
source_of_truth: src/zephyr/infrastructure/event_bus_upgrade.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: version_from 参数
    #   fields: 参数 version_from，类型注解 str
    #   code: event_bus_upgrade.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: version_to 参数
    #   fields: 参数 version_to，类型注解 str
    #   code: event_bus_upgrade.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① validate_upgrade_from_shared
    #   name_en: validate_upgrade_from_shared
    #   intro: validate_upgrade_from_shared(version_from, version_to)…
    #   desc: 源码 L72-L74
    #   inputs: version_from version_to
    #   outputs: _SharedUpgradePlan
    # 层: 输出
    # - id: O1
    #   name_zh: _SharedUpgradePlan
    #   name_en: _SharedUpgradePlan
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
