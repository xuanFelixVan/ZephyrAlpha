# ALGO_FLOW 外部真源——contract_tester（_domain_infrastructure 域，真源 src/zephyr/infrastructure/contract_tester.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.contract_tester
source_of_truth: src/zephyr/infrastructure/contract_tester.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: strict 参数
    #   fields: 参数 strict（无注解）
    #   code: contract_tester.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContractTestResult
    #   name_en: ContractTestResult
    #   intro: class ContractTestResult 源码 L88-L101
    #   desc: 公共方法（定义序）: passed, failure_count；源码 L88-L101
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② ContractTester
    #   name_en: ContractTester
    #   intro: 契约测试框架——验证代码与契约的一致性
    #   desc: 契约测试框架——验证代码与契约的一致性；公共方法（定义序）: strict, test_contract, test_directory, tests_run；源码 L104-L242
    #   inputs: strict
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ContractTestResult, ContractTester
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
