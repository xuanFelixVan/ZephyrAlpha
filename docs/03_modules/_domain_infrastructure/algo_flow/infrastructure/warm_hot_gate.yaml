# ALGO_FLOW 外部真源——warm_hot_gate（_domain_infrastructure 域，真源 src/zephyr/infrastructure/warm_hot_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.warm_hot_gate
source_of_truth: src/zephyr/infrastructure/warm_hot_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: require_all_passed 参数
    #   fields: 参数 require_all_passed（无注解）
    #   code: warm_hot_gate.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① GateCheckResult
    #   name_en: GateCheckResult
    #   intro: class GateCheckResult 源码 L86-L101
    #   desc: 公共方法（定义序）: blocked, requires_approval；源码 L86-L101
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② WarmHotGate
    #   name_en: WarmHotGate
    #   intro: Warm->Hot 阻断门
    #   desc: Warm->Hot 阻断门 在系统状态从 Warm（准备就绪）切换到 Hot（真正执行）之前的最后一道闸门。 检查项： 1. 契约一致性验证 2. 配置完整性检查 3. 安全扫描…；公共方法（定义序）: check,…
    #   inputs: require_all_passed
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: GateCheckResult, WarmHotGate
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
