# ALGO_FLOW 外部真源——event_store（_domain_infrastructure 域，真源 src/zephyr/infrastructure/event_store.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.event_store
source_of_truth: src/zephyr/infrastructure/event_store.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: db_path 参数
    #   fields: 参数 db_path（无注解）
    #   code: event_store.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: auto_init 参数
    #   fields: 参数 auto_init（无注解）
    #   code: event_store.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① StoredEvent
    #   name_en: StoredEvent
    #   intro: class StoredEvent 源码 L128-L164
    #   desc: 公共方法（定义序）: to_row, from_row；源码 L128-L164
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② EventStore
    #   name_en: EventStore
    #   intro: 事件存储——基于 SQLite 的不可篡改审计日志
    #   desc: 事件存储——基于 SQLite 的不可篡改审计日志 特性： - SQLite WAL 模式高并发写入 - SHA256 checksum 防篡改 - 按时间/组件/级别多维度查询…；公共方法（定义序）: record,…
    #   inputs: db_path auto_init
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: StoredEvent, EventStore
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
