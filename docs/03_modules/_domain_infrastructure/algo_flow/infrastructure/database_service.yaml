# ALGO_FLOW 外部真源——database_service（_domain_infrastructure 域，真源 src/zephyr/infrastructure/database_service.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.database_service
source_of_truth: src/zephyr/infrastructure/database_service.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: database_service.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① DatabaseService
    #   name_en: DatabaseService
    #   intro: 统一数据库服务层（治理 + 依赖图 + 业务数据库预留）
    #   desc: 统一数据库服务层（治理 + 依赖图 + 业务数据库预留） P-PLAN 专项工程：CRUD 方法（get_task/create_task/get_node 等 9 个）已抽取到…；公共方法（定义序）: get_gov…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: DatabaseService
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
