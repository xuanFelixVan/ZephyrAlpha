# ALGO_FLOW 外部真源——multi_agent（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/multi_agent.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.multi_agent
source_of_truth: src/zephyr/infrastructure/a2a_protocol/multi_agent.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: multi_agent.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① AgentCard
    #   name_en: AgentCard
    #   intro: Multi-agent orchestration AgentCard (dataclass; differs fro…
    #   desc: Multi-agent orchestration AgentCard (dataclass; differs from shared Pydantic AgentCard).；公共方法（定义序）: to_dict,…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② TaskDispatch
    #   name_en: TaskDispatch
    #   intro: 任务分派器——将 task 分派给合适的 Agent。
    #   desc: 任务分派器——将 task 分派给合适的 Agent。；公共方法（定义序）: register_agent, unregister_agent, assign, assign_to_capable, get_agent…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: AgentCard, TaskDispatch
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
