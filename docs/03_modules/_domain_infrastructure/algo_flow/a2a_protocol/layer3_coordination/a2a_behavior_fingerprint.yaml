# ALGO_FLOW 外部真源——a2a_behavior_fingerprint（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_behavior_fingerprint.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.a2a_behavior_fingerprint
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_behavior_fingerprint.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: a2a_behavior_fingerprint.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① BehaviorFingerprint
    #   name_en: BehaviorFingerprint
    #   intro: class BehaviorFingerprint 源码 L70-L95
    #   desc: 公共方法（定义序）: similarity, top_actions, avg_session_seconds；源码 L70-L95
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② A2ABehaviorFingerprint
    #   name_en: A2ABehaviorFingerprint
    #   intro: class A2ABehaviorFingerprint 源码 L98-L134
    #   desc: 公共方法（定义序）: record_action, record_file_touch, record_session, record_interaction, get_fingerprint, compare；源码…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: BehaviorFingerprint, A2ABehaviorFingerprint
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
