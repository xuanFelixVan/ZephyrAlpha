# ALGO_FLOW 外部真源——a2a_red_team（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_red_team.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.a2a_red_team
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_red_team.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: a2a_red_team.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① RedTeamReport
    #   name_en: RedTeamReport
    #   intro: class RedTeamReport 源码 L160-L181
    #   desc: 公共方法（定义序）: penetration_rate, defense_rate；源码 L160-L181
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② A2ARedTeam
    #   name_en: A2ARedTeam
    #   intro: A2A 红队引擎 — 攻击向量定义 + 实际执行框架.
    #   desc: A2A 红队引擎 — 攻击向量定义 + 实际执行框架. Phase 2: 所有 6 个攻击向量均已实现具体攻击逻辑。 每次 attack() 实际执行攻击并验证防御是否有效。 约…；公共方法（定义序）: attack_…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: RedTeamReport, A2ARedTeam
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
