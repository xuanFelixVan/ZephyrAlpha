# ALGO_FLOW 外部真源——a2a_voting（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_voting.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.a2a_voting
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_voting.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: default_quorum 参数
    #   fields: 参数 default_quorum（无注解）
    #   code: a2a_voting.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: vote_timeout_seconds 参数
    #   fields: 参数 vote_timeout_seconds（无注解）
    #   code: a2a_voting.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① A2AVoting
    #   name_en: A2AVoting
    #   intro: A2A 加权投票引擎.
    #   desc: A2A 加权投票引擎. 支持 approve/reject/abstain + quorum 检验. 权重来源: AgentMeta.role + reputation vect…；公共方法（定义序）: default…
    #   inputs: default_quorum vote_timeout_seconds
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: A2AVoting
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
