# ALGO_FLOW 外部真源——arbitrator（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/arbitrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.arbitrator
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/arbitrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: ownership_rules 参数
    #   fields: 参数 ownership_rules（无注解）
    #   code: arbitrator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: escalation_engine 参数
    #   fields: 参数 escalation_engine（无注解）
    #   code: arbitrator.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: deadlock_detector 参数
    #   fields: 参数 deadlock_detector（无注解）
    #   code: arbitrator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Arbitrator
    #   name_en: Arbitrator
    #   intro: A2A 三级仲裁引擎.
    #   desc: A2A 三级仲裁引擎. Tier 1: 角色优先级 — SUPERADMIN > SAFETY_OPERATOR > ... > BUILDER Tier 2: 文件归属 — 检…；公共方法（定义序）: escalat…
    #   inputs: ownership_rules escalation_engine deadlock_detector
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: Arbitrator
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
