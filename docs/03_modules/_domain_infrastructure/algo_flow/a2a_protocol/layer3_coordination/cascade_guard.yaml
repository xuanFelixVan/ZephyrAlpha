# ALGO_FLOW 外部真源——cascade_guard（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/cascade_guard.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.cascade_guard
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/cascade_guard.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: threshold 参数
    #   fields: 参数 threshold（无注解）
    #   code: cascade_guard.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CascadeGuard
    #   name_en: CascadeGuard
    #   intro: class CascadeGuard 源码 L65-L80
    #   desc: 公共方法（定义序）: check, record_failure；源码 L65-L80
    #   inputs: threshold
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: CascadeGuard
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
