# ALGO_FLOW 外部真源——conflict_detector（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/conflict_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.conflict_detector
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/conflict_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: line_overlap_tolerance 参数
    #   fields: 参数 line_overlap_tolerance（无注解）
    #   code: conflict_detector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: resource_exclusive 参数
    #   fields: 参数 resource_exclusive（无注解）
    #   code: conflict_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ChangeSet
    #   name_en: ChangeSet
    #   intro: class ChangeSet 源码 L100-L111
    #   desc: 公共方法（定义序）: add_file；源码 L100-L111
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② ConflictDetector
    #   name_en: ConflictDetector
    #   intro: A2A 冲突检测引擎.
    #   desc: A2A 冲突检测引擎. 在 Supervisor 调度前运行——检测两个 Agent 的变更集是否存在冲突。 若存在冲突 -> 交由 Arbitrator 仲裁。；公共方法（定义序）: detect, has_conf…
    #   inputs: line_overlap_tolerance resource_exclusive
    #   outputs: 返回值
    #   （注：A2 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: ChangeSet, ConflictDetector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
