# ALGO_FLOW 外部真源——a2a_anomaly_detector（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_anomaly_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.a2a_anomaly_detector
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_anomaly_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: baseline_window 参数
    #   fields: 参数 baseline_window（无注解）
    #   code: a2a_anomaly_detector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: min_samples_before_detect 参数
    #   fields: 参数 min_samples_before_detect（无注解）
    #   code: a2a_anomaly_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MetricBaseline
    #   name_en: MetricBaseline
    #   intro: class MetricBaseline 源码 L110-L141
    #   desc: 公共方法（定义序）: update, mean, std, z_score；源码 L110-L141
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② A2AAnomalyDetector
    #   name_en: A2AAnomalyDetector
    #   intro: Agent 行为统计异常检测器.
    #   desc: Agent 行为统计异常检测器. 学习每个 (Agent, Metric) 对的基线，偏差超过阈值时报告异常.；公共方法（定义序）: record, record_batch, get_baseline, get_ba…
    #   inputs: baseline_window min_samples_before_detect
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: MetricBaseline, A2AAnomalyDetector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
