# ALGO_FLOW 外部真源——a2a_idle_guard（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_idle_guard.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.a2a_idle_guard
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_idle_guard.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: idle_timeout 参数
    #   fields: 参数 idle_timeout（无注解）
    #   code: a2a_idle_guard.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① A2AIdleGuard
    #   name_en: A2AIdleGuard
    #   intro: class A2AIdleGuard 源码 L65-L72
    #   desc: 公共方法（定义序）: check_idle；源码 L65-L72
    #   inputs: idle_timeout
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: A2AIdleGuard
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
