# ALGO_FLOW 外部真源——a2a_debate（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_debate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer3_coordination.a2a_debate
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer3_coordination/a2a_debate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_rounds 参数
    #   fields: 参数 max_rounds（无注解）
    #   code: a2a_debate.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① A2ADebate
    #   name_en: A2ADebate
    #   intro: A2A 结构化辩论协议.
    #   desc: A2A 结构化辩论协议. 三轮递进式辩论: claim -> rebuttal -> synthesis max_rounds=3 为默认深度，Phase 3+ 可扩展到 N+1…；公共方法（定义序）: debate；…
    #   inputs: max_rounds
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: A2ADebate
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
