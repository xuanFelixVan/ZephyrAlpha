# ALGO_FLOW 外部真源——phase_hold（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/governance/phase_hold.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.governance.phase_hold
source_of_truth: src/zephyr/infrastructure/a2a_protocol/governance/phase_hold.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: phase_hold.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① Phase4Hold
    #   name_en: Phase4Hold
    #   intro: A2A Phase 4 施工锁定（governance 子包派生扩展）。
    #   desc: A2A Phase 4 施工锁定（governance 子包派生扩展）。 继承真源全部行为（check/can_proceed），新增 is_hold_active 便捷判定。；公共方法（定义序）: is_hold_a…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: Phase4Hold
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
