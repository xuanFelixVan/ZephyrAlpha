# ALGO_FLOW 外部真源——governance_adapter（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/governance/governance_adapter.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.governance.governance_adapter
source_of_truth: src/zephyr/infrastructure/a2a_protocol/governance/governance_adapter.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: governance_adapter.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① GovernanceAdapter
    #   name_en: GovernanceAdapter
    #   intro: A2A GovernanceAdapter — G-CT-008 消费端.
    #   desc: A2A GovernanceAdapter — G-CT-008 消费端. Phase 4 激活后，A2A 通信对在执行前 MUST 通过此适配器： 1. verify_a2a_…；公共方法（定义序）: verify_…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② MCPAdapter
    #   name_en: MCPAdapter
    #   intro: class MCPAdapter 源码 L172-L180
    #   desc: 公共方法（定义序）: adapt, validate；源码 L172-L180
    #   inputs: config
    #   outputs: 返回值
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: GovernanceAdapter, MCPAdapter
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
