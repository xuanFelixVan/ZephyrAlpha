# ALGO_FLOW 外部真源——a2a_registry（_domain_infrastructure 域，真源 src/zephyr/infrastructure/a2a_protocol/layer1_discovery/a2a_registry.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.a2a_protocol.layer1_discovery.a2a_registry
source_of_truth: src/zephyr/infrastructure/a2a_protocol/layer1_discovery/a2a_registry.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry_path 参数
    #   fields: 参数 registry_path（无注解）
    #   code: a2a_registry.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① A2ARegistry
    #   name_en: A2ARegistry
    #   intro: Agent Card 注册中心
    #   desc: Agent Card 注册中心；公共方法（定义序）: register, discover, get, unregister；源码 L53-L76
    #   inputs: registry_path
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: A2ARegistry
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
