# ALGO_FLOW 外部真源——trace_decorator（_domain_infrastructure 域，真源 src/zephyr/infrastructure/observability/trace_decorator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.observability.trace_decorator
source_of_truth: src/zephyr/infrastructure/observability/trace_decorator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: operation 参数
    #   fields: 参数 operation，类型注解 str
    #   code: trace_decorator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TraceCollector
    #   name_en: TraceCollector
    #   intro: class TraceCollector 源码 L91-L162
    #   desc: 公共方法（定义序）: get_instance, reset_instance, set_instance, spans, output_dir, add_span, flush；源码 L91-L162
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② trace
    #   name_en: trace
    #   intro: trace(operation) 源码 L165-L203
    #   desc: 源码 L165-L203
    #   inputs: operation
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: TraceCollector, trace
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
