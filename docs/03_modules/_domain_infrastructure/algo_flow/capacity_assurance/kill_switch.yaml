# ALGO_FLOW 外部真源——kill_switch（_domain_infrastructure 域，真源 src/zephyr/infrastructure/capacity_assurance/kill_switch.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.capacity_assurance.kill_switch
source_of_truth: src/zephyr/infrastructure/capacity_assurance/kill_switch.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: threshold 参数
    #   fields: 参数 threshold（无注解）
    #   code: kill_switch.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: auto_kill_threshold 参数
    #   fields: 参数 auto_kill_threshold（无注解）
    #   code: kill_switch.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① KillSwitch
    #   name_en: KillSwitch
    #   intro: per-session err>threshold -> fuse off.
    #   desc: per-session err>threshold -> fuse off. needs manual reset (DD110). Extended with batch er…；公共方法（定义序）: fuse_on…
    #   inputs: threshold auto_kill_threshold
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: KillSwitch
    #   downstream: zephyr.autonomy_core.context.context_pipeline_auto
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
