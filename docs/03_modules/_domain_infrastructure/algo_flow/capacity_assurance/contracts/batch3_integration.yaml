# ALGO_FLOW 外部真源——batch3_integration（_domain_infrastructure 域，真源 src/zephyr/infrastructure/capacity_assurance/contracts/batch3_integration.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.capacity_assurance.contracts.batch3_integration
source_of_truth: src/zephyr/infrastructure/capacity_assurance/contracts/batch3_integration.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: batch3_integration.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① 数据契约声明
    #   name_en: data class declarations
    #   intro: 纯声明类（无公共方法，AST 事实）: CtOt001, CtOt002, CtHs001, CtCt1, CtCt2, CtCt3, CtCt4, CtGd…
    #   desc: 数据契约/异常/枚举声明共 14 类；无算法流程（AST 事实）
    #   inputs: I1
    #   outputs: 数据契约类集合
    # 层: 输出
    # - id: O1
    #   name_zh: 数据契约声明（14 类）
    #   name_en: data classes
    #   intro: CtOt001, CtOt002, CtHs001, CtCt1, CtCt2, CtCt3, CtCt4, CtGd004
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
