# ALGO_FLOW 外部真源——contract_bus（_domain_infrastructure 域，真源 src/zephyr/infrastructure/capacity_assurance/contracts/contract_bus.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.capacity_assurance.contracts.contract_bus
source_of_truth: src/zephyr/infrastructure/capacity_assurance/contracts/contract_bus.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: contract_bus.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContractBusLoader
    #   name_en: ContractBusLoader
    #   intro: ContractBus 契约加载器——加载并校验全部 44 条 Pydantic v2 契约 Schema.
    #   desc: ContractBus 契约加载器——加载并校验全部 44 条 Pydantic v2 契约 Schema.；公共方法（定义序）: contract_count, batch_summary, get_contract…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② get_contract_bus_loader
    #   name_en: get_contract_bus_loader
    #   intro: get_contract_bus_loader() 源码 L112-L116
    #   desc: 源码 L112-L116
    #   inputs: 无参数
    #   outputs: ContractBusLoader
    # 层: 输出
    # - id: O1
    #   name_zh: ContractBusLoader
    #   name_en: ContractBusLoader
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
