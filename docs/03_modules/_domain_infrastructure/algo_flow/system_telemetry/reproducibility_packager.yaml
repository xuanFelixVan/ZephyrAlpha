# ALGO_FLOW 外部真源——reproducibility_packager（_domain_infrastructure 域，真源 src/zephyr/infrastructure/system_telemetry/reproducibility_packager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.system_telemetry.reproducibility_packager
source_of_truth: src/zephyr/infrastructure/system_telemetry/reproducibility_packager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: manifest 参数
    #   fields: 参数 manifest，类型注解 PackageManifest
    #   code: reproducibility_packager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① manifest_digest
    #   name_en: manifest_digest
    #   intro: 清单内容 sha256（不含 digest 本身；created_at 取 ISO 格式）。
    #   desc: 清单内容 sha256（不含 digest 本身；created_at 取 ISO 格式）。；源码 L112-L122
    #   inputs: manifest
    #   outputs: str
    # - id: A2
    #   name_zh: ② ReproducibilityPackager
    #   name_en: ReproducibilityPackager
    #   intro: 实验可复现打包器（打包/校验/回放指针解析三接口）。
    #   desc: 实验可复现打包器（打包/校验/回放指针解析三接口）。；公共方法（定义序）: root, build_package, verify_package, resolve_replay_pointer；源码 L125-L256
    #   inputs: root clock
    #   outputs: 返回值
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 运行时装配批（实验跟踪回放装配 / 审计快照引用）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
