# ALGO_FLOW 外部真源——incident_responder（_domain_infrastructure 域，真源 src/zephyr/infrastructure/system_telemetry/incident_responder.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.system_telemetry.incident_responder
source_of_truth: src/zephyr/infrastructure/system_telemetry/incident_responder.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: incident_responder.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: policies 参数
    #   fields: 参数 policies（无注解）
    #   code: incident_responder.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: rules 参数
    #   fields: 参数 rules（无注解）
    #   code: incident_responder.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: escalation_sink 参数
    #   fields: 参数 escalation_sink（无注解）
    #   code: incident_responder.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① IncidentResponder
    #   name_en: IncidentResponder
    #   intro: 事件响应器（策略表 + 升级规则 + 效果学习）。
    #   desc: 事件响应器（策略表 + 升级规则 + 效果学习）。；公共方法（定义序）: classify, register_policy, respond, policy_effectiveness, effectiveness_…
    #   inputs: clock policies rules escalation_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: IncidentResponder
    #   downstream: 运行时装配批（事件类型策略表绑定 / 升级路由接告警 / 效果统计入面板）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
