# ALGO_FLOW 外部真源——fix_scheduler（_domain_infrastructure 域，真源 src/zephyr/infrastructure/auto_fix_engine/fix_scheduler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.auto_fix_engine.fix_scheduler
source_of_truth: src/zephyr/infrastructure/auto_fix_engine/fix_scheduler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: mode 参数
    #   fields: 参数 mode（无注解）
    #   code: fix_scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: batch_interval_sec 参数
    #   fields: 参数 batch_interval_sec（无注解）
    #   code: fix_scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: fix_fn 参数
    #   fields: 参数 fix_fn（无注解）
    #   code: fix_scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: scan_fn 参数
    #   fields: 参数 scan_fn（无注解）
    #   code: fix_scheduler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FixScheduler
    #   name_en: FixScheduler
    #   intro: class FixScheduler 源码 L86-L169
    #   desc: 公共方法（定义序）: mode, is_running, batch_count, start, stop, submit_event, get_status；源码 L86-L169
    #   inputs: mode batch_interval_sec fix_fn scan_fn
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: FixScheduler
    #   downstream: engine.py;__main__.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
