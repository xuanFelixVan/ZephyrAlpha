# ALGO_FLOW 外部真源——state_machine（_domain_infrastructure 域，真源 src/zephyr/infrastructure/auto_fix_engine/state_machine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.auto_fix_engine.state_machine
source_of_truth: src/zephyr/infrastructure/auto_fix_engine/state_machine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: initial 参数
    #   fields: 参数 initial（无注解）
    #   code: state_machine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FixStateMachine
    #   name_en: FixStateMachine
    #   intro: class FixStateMachine 源码 L122-L204
    #   desc: 公共方法（定义序）: current_state, is_terminal, available_transitions, history, can_transition, transition, force_stat…
    #   inputs: initial
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② DriftStateMachine
    #   name_en: DriftStateMachine
    #   intro: 漂移状态机——对齐 test_state_machine.py 契约（ F1 治本）。
    #   desc: 漂移状态机——对齐 test_state_machine.py 契约（ F1 治本）。 旧桩实现仅 state/history 两个字段 + transition/can_tra…；公共方法（定义序）: events,…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: FixStateMachine, DriftStateMachine
    #   downstream: engine.py;fix_reliability.py;fix_health_check.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
