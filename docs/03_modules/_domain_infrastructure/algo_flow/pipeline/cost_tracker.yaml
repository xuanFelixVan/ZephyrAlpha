# ALGO_FLOW 外部真源——cost_tracker（_domain_infrastructure 域，真源 src/zephyr/infrastructure/pipeline/cost_tracker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.pipeline.cost_tracker
source_of_truth: src/zephyr/infrastructure/pipeline/cost_tracker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: cost_tracker.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① CostTracker
    #   name_en: CostTracker
    #   intro: LLM 调用成本追踪器。
    #   desc: LLM 调用成本追踪器。 记录每次模型调用的 token 使用量和美元成本，支持： - 实际 API 返回的成本记录 - 基于定价常量的模拟成本估算 - 按模型分组的成本汇总 -…；公共方法（定义序）: records…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: CostTracker
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
