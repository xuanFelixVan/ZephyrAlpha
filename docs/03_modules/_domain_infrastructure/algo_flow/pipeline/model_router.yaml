# ALGO_FLOW 外部真源——model_router（_domain_infrastructure 域，真源 src/zephyr/infrastructure/pipeline/model_router.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.pipeline.model_router
source_of_truth: src/zephyr/infrastructure/pipeline/model_router.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: model_router.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ModelRouter
    #   name_en: ModelRouter
    #   intro: 模型路由器 — 模型选择、降级链、成本估算。
    #   desc: 模型路由器 — 模型选择、降级链、成本估算。 所有属性和方法均为类级别（静态），无需实例化。；公共方法（定义序）: resolve_model, fallback_chain_for, estimate_cost, e…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: ModelRouter
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
