# ALGO_FLOW 外部真源——trust_anchor（_domain_infrastructure 域，真源 src/zephyr/infrastructure/asset_inventory/trust_anchor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.asset_inventory.trust_anchor
source_of_truth: src/zephyr/infrastructure/asset_inventory/trust_anchor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: trust_anchor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TripleTrustAnchorGate
    #   name_en: TripleTrustAnchorGate
    #   intro: class TripleTrustAnchorGate 源码 L90-L235
    #   desc: 公共方法（定义序）: root, cache, verify, check_audit_continuity, calculate_trust, recommend；源码 L90-L235
    #   inputs: project_root
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② BypassManager
    #   name_en: BypassManager
    #   intro: 紧急旁路协议——inventory_override.yaml -> 强制 GREEN + 自动过期 24h。
    #   desc: 紧急旁路协议——inventory_override.yaml -> 强制 GREEN + 自动过期 24h。；公共方法（定义序）: override_path, get_bypass_state, is_bypass…
    #   inputs: project_root
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: TripleTrustAnchorGate, BypassManager
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
