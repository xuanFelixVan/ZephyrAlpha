# ALGO_FLOW 外部真源——classifier（_domain_infrastructure 域，真源 src/zephyr/infrastructure/asset_inventory/classifier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.asset_inventory.classifier
source_of_truth: src/zephyr/infrastructure/asset_inventory/classifier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: type_mapping 参数
    #   fields: 参数 type_mapping（无注解）
    #   code: classifier.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: unknown_threshold_pct 参数
    #   fields: 参数 unknown_threshold_pct（无注解）
    #   code: classifier.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Classifier
    #   name_en: Classifier
    #   intro: 基于类型映射的资产自动分类器——Phase 1 实现（蓝图 §3.2）。
    #   desc: 基于类型映射的资产自动分类器——Phase 1 实现（蓝图 §3.2）。；公共方法（定义序）: classify, main；源码 L175-L282
    #   inputs: type_mapping unknown_threshold_pct
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② main
    #   name_en: main
    #   intro: main() 源码 L291-L292
    #   desc: 源码 L291-L292
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: Classifier, main
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
