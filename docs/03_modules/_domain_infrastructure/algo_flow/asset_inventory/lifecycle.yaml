# ALGO_FLOW 外部真源——lifecycle（_domain_infrastructure 域，真源 src/zephyr/infrastructure/asset_inventory/lifecycle.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.asset_inventory.lifecycle
source_of_truth: src/zephyr/infrastructure/asset_inventory/lifecycle.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: decay_days 参数
    #   fields: 参数 decay_days（无注解）
    #   code: lifecycle.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: root 参数
    #   fields: 参数 root（无注解）
    #   code: lifecycle.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Lifecycle
    #   name_en: Lifecycle
    #   intro: ITIL 生命周期自动化管理器——Phase 1 实现（蓝图 §3.5）。
    #   desc: ITIL 生命周期自动化管理器——Phase 1 实现（蓝图 §3.5）。；公共方法（定义序）: evaluate, main；源码 L93-L256
    #   inputs: decay_days root
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② main
    #   name_en: main
    #   intro: main() 源码 L265-L266
    #   desc: 源码 L265-L266
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: Lifecycle, main
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
