# ALGO_FLOW 外部真源——reconciler（_domain_infrastructure 域，真源 src/zephyr/infrastructure/asset_inventory/reconciler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.asset_inventory.reconciler
source_of_truth: src/zephyr/infrastructure/asset_inventory/reconciler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: orphan_tolerance_hours 参数
    #   fields: 参数 orphan_tolerance_hours（无注解）
    #   code: reconciler.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: ghost_max_age_days 参数
    #   fields: 参数 ghost_max_age_days（无注解）
    #   code: reconciler.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: root 参数
    #   fields: 参数 root（无注解）
    #   code: reconciler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Reconciler
    #   name_en: Reconciler
    #   intro: 对账引擎——对比磁盘实际 vs 注册表记录（蓝图 §3.4）。
    #   desc: 对账引擎——对比磁盘实际 vs 注册表记录（蓝图 §3.4）。；公共方法（定义序）: reconcile, save；源码 L92-L266
    #   inputs: orphan_tolerance_hours ghost_max_age_days root
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② main
    #   name_en: main
    #   intro: main() 源码 L353-L354
    #   desc: 源码 L353-L354
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: Reconciler, main
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1
