# ALGO_FLOW 外部真源——rollback_boot_integration（_domain_infrastructure 域，真源 src/zephyr/infrastructure/rollback/rollback_boot_integration.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.rollback.rollback_boot_integration
source_of_truth: src/zephyr/infrastructure/rollback/rollback_boot_integration.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: rollback_boot_integration.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RollbackBootIntegration
    #   name_en: RollbackBootIntegration
    #   intro: 回滚系统启动/关闭集成。
    #   desc: 回滚系统启动/关闭集成。 在系统启动时: 1. 初始化 RollbackWAL（检查完整性 + 恢复未完成的回滚） 2. 初始化 RollbackVerifier（G0 门禁就绪…；公共方法（定义序）: registe…
    #   inputs: project_root
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② subscribe_eventbus
    #   name_en: subscribe_eventbus
    #   intro: 订阅 EventBusBackpressure 的3个失败事件。
    #   desc: 订阅 EventBusBackpressure 的3个失败事件。 幂等：重复调用安全。Backpressure 总线不可用时静默跳过。 供 boot_hooks 统一调用。 事件…；源码 L207-L231
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: RollbackBootIntegration, subscribe_eventbus
    #   downstream: zephyr.trading.boot_hooks
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
