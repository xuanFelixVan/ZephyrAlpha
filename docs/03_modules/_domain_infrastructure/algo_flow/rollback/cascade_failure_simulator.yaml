# ALGO_FLOW 外部真源——cascade_failure_simulator（_domain_infrastructure 域，真源 src/zephyr/infrastructure/rollback/cascade_failure_simulator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.rollback.cascade_failure_simulator
source_of_truth: src/zephyr/infrastructure/rollback/cascade_failure_simulator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: injector 参数
    #   fields: 参数 injector（无注解）
    #   code: cascade_failure_simulator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: is_trading_hours 参数
    #   fields: 参数 is_trading_hours（无注解）
    #   code: cascade_failure_simulator.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: backup_confirmed 参数
    #   fields: 参数 backup_confirmed（无注解）
    #   code: cascade_failure_simulator.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: cascade_failure_simulator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CascadeFailureSimulator
    #   name_en: CascadeFailureSimulator
    #   intro: 级联失效仿真器（场景编排 + 护栏 + 传播记录 + 恢复测量）。
    #   desc: 级联失效仿真器（场景编排 + 护栏 + 传播记录 + 恢复测量）。；公共方法（定义序）: run, history；源码 L160-L272
    #   inputs: injector is_trading_hours backup_confirmed clock timeout_minutes
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: CascadeFailureSimulator
    #   downstream: 运行时装配批（灾备演练编排 / 恢复时间度量 / 失效传播复盘）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
