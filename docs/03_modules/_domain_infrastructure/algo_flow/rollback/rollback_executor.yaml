# ALGO_FLOW 外部真源——rollback_executor（_domain_infrastructure 域，真源 src/zephyr/infrastructure/rollback/rollback_executor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.rollback.rollback_executor
source_of_truth: src/zephyr/infrastructure/rollback/rollback_executor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: rollback_executor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: sqlite_dumper 参数
    #   fields: 参数 sqlite_dumper（无注解）
    #   code: rollback_executor.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: rollback_lock 参数
    #   fields: 参数 rollback_lock（无注解）
    #   code: rollback_executor.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: owner_session_id 参数
    #   fields: 参数 owner_session_id（无注解）
    #   code: rollback_executor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RollbackExecutor
    #   name_en: RollbackExecutor
    #   intro: class RollbackExecutor 源码 L185-L1307
    #   desc: 公共方法（定义序）: in_flight_path, g0_verify, audit_writer, project_root, owner_session_id, dumper, lock, in_flight_d…
    #   inputs: project_root sqlite_dumper rollback_lock owner_session_id
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: RollbackExecutor
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
