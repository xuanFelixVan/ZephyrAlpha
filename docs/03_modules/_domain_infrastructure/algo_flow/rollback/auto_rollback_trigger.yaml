# ALGO_FLOW 外部真源——auto_rollback_trigger（_domain_infrastructure 域，真源 src/zephyr/infrastructure/rollback/auto_rollback_trigger.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.rollback.auto_rollback_trigger
source_of_truth: src/zephyr/infrastructure/rollback/auto_rollback_trigger.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_retries 参数
    #   fields: 参数 max_retries（无注解）
    #   code: auto_rollback_trigger.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TriggerDecision
    #   name_en: TriggerDecision
    #   intro: class TriggerDecision 源码 L117-L132
    #   desc: 公共方法（定义序）: should_rollback, retry_allowed, forward_fix_allowed；源码 L117-L132
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② AutoRollbackTrigger
    #   name_en: AutoRollbackTrigger
    #   intro: class AutoRollbackTrigger 源码 L186-L276
    #   desc: 公共方法（定义序）: max_retries, classify, process_guard_result, retry_counts；源码 L186-L276
    #   inputs: max_retries
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: TriggerDecision, AutoRollbackTrigger
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
