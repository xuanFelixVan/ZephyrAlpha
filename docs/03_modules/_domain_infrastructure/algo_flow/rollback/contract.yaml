# ALGO_FLOW 外部真源——contract（_domain_infrastructure 域，真源 src/zephyr/infrastructure/rollback/contract.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.rollback.contract
source_of_truth: src/zephyr/infrastructure/rollback/contract.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: exit_code 参数
    #   fields: 参数 exit_code，类型注解 int
    #   code: contract.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: gate_action 参数
    #   fields: 参数 gate_action，类型注解 str
    #   code: contract.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① get_gate_action
    #   name_en: get_gate_action
    #   intro: get_gate_action(exit_code) 源码 L212-L213
    #   desc: 源码 L212-L213
    #   inputs: exit_code
    #   outputs: tuple[str, str]
    # - id: A2
    #   name_zh: ② get_pipeline_action
    #   name_en: get_pipeline_action
    #   intro: get_pipeline_action(gate_action) 源码 L216-L217
    #   desc: 源码 L216-L217
    #   inputs: gate_action
    #   outputs: str
    # - id: A3
    #   name_zh: ③ resolve_exit_code
    #   name_en: resolve_exit_code
    #   intro: resolve_exit_code(exit_code) 源码 L220-L228
    #   desc: 源码 L220-L228
    #   inputs: exit_code
    #   outputs: dict[str, str]
    #   （注：A3 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: tuple[str, str]
    #   name_en: tuple[str, str]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # - id: O2
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1
