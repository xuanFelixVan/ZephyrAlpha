# ALGO_FLOW 外部真源——warm_standby（_domain_infrastructure 域，真源 src/zephyr/infrastructure/rollback/warm_standby.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infrastructure.rollback.warm_standby
source_of_truth: src/zephyr/infrastructure/rollback/warm_standby.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: warm_standby.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① WarmStandby
    #   name_en: WarmStandby
    #   intro: class WarmStandby 源码 L104-L297
    #   desc: 公共方法（定义序）: read_state, save_state, state_path, standby_dir, project_root, initialize, cutover, rotate, verify…
    #   inputs: project_root
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: WarmStandby
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
