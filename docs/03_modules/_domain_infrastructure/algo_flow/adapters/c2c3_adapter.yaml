# ALGO_FLOW 外部真源——c2c3_adapter（_domain_infrastructure 域，真源 src/zephyr/experiment_tracking/adapters/c2c3_adapter.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.experiment_tracking.adapters.c2c3_adapter
source_of_truth: src/zephyr/experiment_tracking/adapters/c2c3_adapter.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: report 参数
    #   fields: 参数 report，类型注解 C2ProtectionReport
    #   code: c2c3_adapter.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: lineage 参数
    #   fields: 参数 lineage（无注解）
    #   code: c2c3_adapter.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: extra_tags 参数
    #   fields: 参数 extra_tags（无注解）
    #   code: c2c3_adapter.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① track_c2_result
    #   name_en: track_c2_result
    #   intro: 把 C2 极端事件保护报告记录为一个实验跟踪 run（鸭子类型，只读 report 属性）。
    #   desc: 把 C2 极端事件保护报告记录为一个实验跟踪 run（鸭子类型，只读 report 属性）。；源码 L109-L136
    #   inputs: report lineage extra_tags
    #   outputs: str
    # - id: A2
    #   name_zh: ② track_c3_result
    #   name_en: track_c3_result
    #   intro: 把 C3 节流归因报告记录为一个实验跟踪 run（鸭子类型，只读 report 属性）。
    #   desc: 把 C3 节流归因报告记录为一个实验跟踪 run（鸭子类型，只读 report 属性）。；源码 L139-L168
    #   inputs: report lineage extra_tags
    #   outputs: str
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: C2/C3 验证入口（track 时 lazy import 调用）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: C2ProtectionReport(events/mean_improvement/min_improvement/skipped/passed) 或 C3AttributionReport(states/total_avoided/defensive_share/bull_mean_shrinkage/passed)（鸭子类型）
    # I2: lineage(上游 run_id 映射,如 c1_run_id) + extra_tags
    # F1: track_c2_result(per-event 改善指标 + passed → run) / track_c3_result(per-state 归因 + passed → run)
    # O1: run_id（NullBackend="null-run"）
    # [/ALGO_FLOW]
