# ALGO_FLOW 外部真源——regime_adapter（_domain_infrastructure 域，真源 src/zephyr/experiment_tracking/adapters/regime_adapter.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.experiment_tracking.adapters.regime_adapter
source_of_truth: src/zephyr/experiment_tracking/adapters/regime_adapter.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: probabilities 参数
    #   fields: 参数 probabilities，类型注解 RegimeProbabilities
    #   code: regime_adapter.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: shrinkage 参数
    #   fields: 参数 shrinkage，类型注解 ShrinkageResult | None
    #   code: regime_adapter.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: feature_stats 参数
    #   fields: 参数 feature_stats（无注解）
    #   code: regime_adapter.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: model_params 参数
    #   fields: 参数 model_params（无注解）
    #   code: regime_adapter.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① track_regime_detection
    #   name_en: track_regime_detection
    #   intro: 把一次 regime 检测产出记录为一个实验跟踪 run。
    #   desc: 把一次 regime 检测产出记录为一个实验跟踪 run。 Args: probabilities: 7 态概率分布（鸭子类型，读 probabilities/dominant_…；源码 L92-L147
    #   inputs: probabilities shrinkage feature_stats model_params lineage extra_tags
    #   outputs: str
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: regime 验证/运行入口（track 时 lazy import 调用）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: probabilities(RegimeProbabilities 鸭子类型: probabilities/dominant_regime/dominant_frequency/confidence) + shrinkage(ShrinkageResult: value/confidence_signal/risk_signal/shrinkage_enabled)
    # I2: feature_stats(输入特征统计,可选) + model_params(HMM 超参,可选) + lineage(上游 run_id 映射,可选)
    # F1: _extract_metrics(输出状态分布 per-state 概率 + confidence + shrinkage 三值)
    # F2: track_regime_detection(start_run(component=regime-detector) → log_params/log_metrics → run_id)
    # O1: run_id（NullBackend="null-run"）
    # [/ALGO_FLOW]
