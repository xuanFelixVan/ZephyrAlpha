# ALGO_FLOW 外部真源——multifactor_degradation_chain（_domain_factor 域，真源 src/zephyr/factor/analysis/multifactor_degradation_chain.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.analysis.multifactor_degradation_chain
source_of_truth: src/zephyr/factor/analysis/multifactor_degradation_chain.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: factor_panel 参数
    #   fields: 参数 factor_panel，类型注解 dict[str, pd.Series]
    #   code: multifactor_degradation_chain.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: ic_history 参数
    #   fields: 参数 ic_history，类型注解 dict[str, Sequence[float]]
    #   code: multifactor_degradation_chain.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: forward_returns 参数
    #   fields: 参数 forward_returns，类型注解 pd.Series | None
    #   code: multifactor_degradation_chain.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: params 参数
    #   fields: 参数 params，类型注解 DegradationChainParams | None
    #   code: multifactor_degradation_chain.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① decide
    #   name_en: decide
    #   intro: 合成降级链决策——regression → ic_weighted → equal_weight 三级降级。
    #   desc: 合成降级链决策——regression → ic_weighted → equal_weight 三级降级。 Args: factor_panel: factor_id → 因子…；源码 L176-L239
    #   inputs: factor_panel ic_history forward_returns params
    #   outputs: SynthesisDecision
    # - id: A2
    #   name_zh: ② synthesize_with_degradation
    #   name_en: synthesize_with_degradation
    #   intro: 合成统一入口——decide() 后分派到 3 个 production 合成方法。
    #   desc: 合成统一入口——decide() 后分派到 3 个 production 合成方法。 Returns: (合成信号 pd.Series, SynthesisDecision)；源码 L242-L261
    #   inputs: factor_values ic_history forward_returns params
    #   outputs: tuple[pd.Series, SynthesisDecision]
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: SynthesisDecision
    #   name_en: SynthesisDecision
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: multifactor_pit_backtest
    # - id: O2
    #   name_zh: tuple[pd.Series, SynthesisDecision]
    #   name_en: tuple[pd.Series, SynthesisDecision]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: multifactor_pit_backtest
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: factor_panel(dict[str, pd.Series] 因子值面板) + ic_history(dict[str, list[float]] 历史IC) + forward_returns(可选前向收益)
    # I2: DegradationChainParams(ic_min_samples=20/concentration=0.70/ic_abs_floor=0.02/regression_min_obs=120/condition_number_max=50)
    # F1: decide(回归可行性→IC加权→等权兜底 三级降级决策, 输出SynthesisDecision(method+reason+ic_weights))
    # A1: 回归分支(forward_returns≥120 且 条件数<50→regression; 条件数≥50→降IC加权)
    # A2: IC加权分支(样本≥20: 全池|IC|均值<0.02→等权; 权重集中度>0.70→等权; 否则ic_weighted)
    # A3: 等权兜底(样本<20→equal_weight)
    # F2: synthesize_with_degradation(decide后分派到 multifactor_synthesis 三方法, 纯增量不替换既有方法)
    # O1: SynthesisDecision + 合成信号 pd.Series
    # [/ALGO_FLOW]
