# ALGO_FLOW 外部真源——wq_alpha_87（_domain_factor 域，真源 src/zephyr/factor/wq_alpha_87.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.wq_alpha_87
source_of_truth: src/zephyr/factor/wq_alpha_87.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: wq_alpha_87.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ops
    #   name_en: ops
    #   intro: WorldQuant 101 时序/截面算子集（全部 rolling/shift 实现，天然 PIT）。
    #   desc: WorldQuant 101 时序/截面算子集（全部 rolling/shift 实现，天然 PIT）。；公共方法（定义序）: rank, delay, delta, ts_sum, ts_mean, ts_min…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② WqAlpha87
    #   name_en: WqAlpha87
    #   intro: WorldQuant 101 精选 87 Alpha 公式库门面。
    #   desc: WorldQuant 101 精选 87 Alpha 公式库门面。 用法:: lib = WqAlpha87() values = lib.compute(101, {"open…；公共方法（定义序）: list_al…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ops, WqAlpha87
    #   downstream: 因子注册表/feature_store（register_all/validate_ic 委托注入点）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
