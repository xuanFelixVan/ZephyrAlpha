# ALGO_FLOW 外部真源——factor_availability_monitor（_domain_factor 域，真源 src/zephyr/factor/factor_availability_monitor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.factor_availability_monitor
source_of_truth: src/zephyr/factor/factor_availability_monitor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry 参数
    #   fields: 参数 registry（无注解）
    #   code: factor_availability_monitor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: factor_availability_monitor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① default_alert_sink
    #   name_en: default_alert_sink
    #   intro: 惰性装配 shared.alerts.alert_manager 告警路由（数据告警路由复用）。
    #   desc: 惰性装配 shared.alerts.alert_manager 告警路由（数据告警路由复用）。 Returns: sink(severity, title, message)：…；源码 L121-L144
    #   inputs: 无参数
    #   outputs: Callable[[str, str, str], None]
    # - id: A2
    #   name_zh: ② FactorAvailabilityMonitor
    #   name_en: FactorAvailabilityMonitor
    #   intro: 因子覆盖率 + 缺失比例监控，三级阈值门控。
    #   desc: 因子覆盖率 + 缺失比例监控，三级阈值门控。；公共方法（定义序）: compute_daily, signal_weight, annotate_signal；源码 L147-L284
    #   inputs: registry alert_sink
    #   outputs: 返回值
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: Callable[[str, str, str], None]
    #   name_en: Callable[[str, str, str], None]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
