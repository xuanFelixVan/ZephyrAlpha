# ALGO_FLOW 外部真源——__init__（_domain_factor 域，真源 src/zephyr/factor/services/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.services.__init__
source_of_truth: src/zephyr/factor/services/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 空包标记文件（无数据输入）
    #   fields: 无字段——包内仅此一个 __init__.py，无子模块无导入
    #   code: src/zephyr/factor/services/__init__.py L1-3
    # 层: 算法
    # - id: A1
    #   name_zh: ① 空包命名空间占位
    #   name_en: __init__（模块级 __all__）
    #   intro: 声明 factor.services 包存在，不导出任何符号
    #   desc: 全文仅包名注释 + __all__: list[str] = []（L1-3），无函数无导入，为后续因子域服务子模块预留挂载点
    #   inputs: I1
    #   outputs: 空导出列表
    # 层: 输出
    # - id: O1
    #   name_zh: 空公共 API 面
    #   name_en: __all__=[]
    #   intro: 对外不暴露任何符号，占位待扩展
    #   downstream: 无下游/内部使用（全库无模块 import zephyr.factor.services）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
