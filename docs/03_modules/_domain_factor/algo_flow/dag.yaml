# ALGO_FLOW 外部真源——dag（_domain_factor 域，真源 src/zephyr/factor/core/factor_dag/dag.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.core.factor_dag.dag
source_of_truth: src/zephyr/factor/core/factor_dag/dag.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: factor_ids 参数
    #   fields: 参数 factor_ids，类型注解 list[str]
    #   code: dag.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: dag_id 参数
    #   fields: 参数 dag_id，类型注解 str
    #   code: dag.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① build_dag_from_registry
    #   name_en: build_dag_from_registry
    #   intro: 从 FactorRegistry 查询 factor_ids 的 dependencies，自动构建 DAG。
    #   desc: 从 FactorRegistry 查询 factor_ids 的 dependencies，自动构建 DAG。 自动过滤 dependencies 中不在 factor_ids…；源码 L277-L313
    #   inputs: factor_ids dag_id
    #   outputs: FactorDAG
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: FactorDAG
    #   name_en: FactorDAG
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.factor.core.dag_manager; zephyr.factor.core.dist_feature_eng
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
