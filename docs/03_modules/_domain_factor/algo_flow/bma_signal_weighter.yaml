# ALGO_FLOW 外部真源——bma_signal_weighter（_domain_factor 域，真源 src/zephyr/factor/analysis/bma_signal_weighter.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.analysis.bma_signal_weighter
source_of_truth: src/zephyr/factor/analysis/bma_signal_weighter.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: evaluations 参数
    #   fields: 参数 evaluations，类型注解 Iterable[SignalEvaluation]
    #   code: bma_signal_weighter.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: bma_signal_weighter.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: prev_weights 参数
    #   fields: 参数 prev_weights（无注解）
    #   code: bma_signal_weighter.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① compute_bma_weights
    #   name_en: compute_bma_weights
    #   intro: BMA 动态权重纯函数：门禁 → 伪似然后验 → 平滑 → 一致性置信度/不操作裁定。
    #   desc: BMA 动态权重纯函数：门禁 → 伪似然后验 → 平滑 → 一致性置信度/不操作裁定。 Args: evaluations: 各信号预测力评估（IC/ICIR/IC 衰减/体制条…；源码 L266-L310
    #   inputs: evaluations config prev_weights
    #   outputs: BmaWeightReport
    # - id: A2
    #   name_zh: ② BmaSignalWeighter
    #   name_en: BmaSignalWeighter
    #   intro: BMA 动态信号权重器（TSV 核心类）：跨轮次时间平滑状态载体。
    #   desc: BMA 动态信号权重器（TSV 核心类）：跨轮次时间平滑状态载体。 Args: config: 权重器参数（缺省 BmaWeighterConfig()）。；公共方法（定义序）: prev_weights, updat…
    #   inputs: config
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: BmaWeightReport
    #   name_en: BmaWeightReport
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: （候选：决策编排上游信号合成层/多因子叠加择时 B10-01482 装配批）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: 各信号评估 SignalEvaluation(ic/icir/ic_decay_ratio/direction/regime_ic?)
    # A1: 预测力门禁——ic≤0.03 出局；decay>0.5 出局；icir<0.5 得分×0.5 降权；regime_ic≤0 出局
    # A2: BMA 后验权重——softmax(κ·ic·icir) 伪似然（Hoeting 1999 BIC 近似轻量替代），Σ=1
    # A3: 平滑——w_t=α·w_prev+(1-α)·w_raw（α=0.9），出局信号权重归 0 后重归一
    # A4: 一致性置信度——方向加权多数派占比 agreement；双低（agreement<0.6 且 top_weight<0.5）→ NO_TRADE
    # O1: BmaWeightReport(weights/gated_out/direction/agreement/confidence/decision)
    # [/ALGO_FLOW]
