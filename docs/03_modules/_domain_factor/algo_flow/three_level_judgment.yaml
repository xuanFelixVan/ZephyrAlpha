# ALGO_FLOW 外部真源——three_level_judgment（_domain_factor 域，真源 src/zephyr/factor/analysis/three_level_judgment.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.factor.analysis.three_level_judgment
source_of_truth: src/zephyr/factor/analysis/three_level_judgment.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 因子评估指标 float
    #   fields: ic 均值（ir/oos_rate 保留接口兼容未使用）
    #   code: judge_factor 函数参数
    # - id: I2
    #   name: 判定阈值配置
    #   fields: three_level_judgment.excellent_ic=0.1 / pass_ic=0.05
    #   code: _config.yaml L8-10
    # - id: I3
    #   name: 批量评估结果 dict[str, EvaluationResult]
    #   fields: factor_id → 评估结果（judge_batch 用）
    #   code: results 函数参数
    # 层: 算法
    # - id: A1
    #   name_zh: ① 单因子三级判定
    #   name_en: judge_factor
    #   intro: 按IC均值绝对值把因子分成优秀/合格/淘汰三档
    #   desc: |IC|≥excellent_ic→优秀；|IC|≥pass_ic→合格；否则→淘汰（L53-59）
    #   inputs: I1 I2
    #   outputs: 等级字符串（优秀/合格/淘汰）
    #   invariant: 纯函数无IO；判定仅基于IC均值绝对值
    # - id: A2
    #   name_zh: ② 批量三级判定
    #   name_en: judge_batch
    #   intro: 字典推导式逐因子调单因子判定，一次出一批等级
    #   desc: 对 results 每项取 ic_mean/ir/oos_positive_rate 调 judge_factor（L62-71）
    #   inputs: I3 A1
    #   outputs: dict[factor_id → 等级]
    # 层: 输出
    # - id: O1
    #   name_zh: 单因子等级 str
    #   name_en: factor grade
    #   intro: 优秀/合格/淘汰三档之一
    #   downstream: 无下游/内部使用
    # - id: O2
    #   name_zh: 批量等级映射 dict[str, str]
    #   name_en: batch grade dict
    #   intro: factor_id→等级的批量判定结果
    #   downstream: 无下游/内部使用
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A2
    # A1 --> A2
    # A1 --> O1
    # A2 --> O2
