# ALGO_FLOW 外部真源——async_intercept_queue（_domain_compliance 域，真源 src/zephyr/compliance/async_intercept_queue.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.compliance.async_intercept_queue
source_of_truth: src/zephyr/compliance/async_intercept_queue.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: gateway 参数
    #   fields: 参数 gateway（无注解）
    #   code: async_intercept_queue.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: max_queue_size 参数
    #   fields: 参数 max_queue_size（无注解）
    #   code: async_intercept_queue.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: cache_size 参数
    #   fields: 参数 cache_size（无注解）
    #   code: async_intercept_queue.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AsyncInterceptQueue
    #   name_en: AsyncInterceptQueue
    #   intro: 合规异步拦截队列（有界 + 缓存 + 线程安全）。
    #   desc: 合规异步拦截队列（有界 + 缓存 + 线程安全）。 Args: gateway: 既有安全网关实现（SecurityGateway OCP 扩展点）。 max_queue_siz…；公共方法（定义序）: submit…
    #   inputs: gateway max_queue_size cache_size
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: AsyncInterceptQueue
    #   downstream: MOD-L10-001(DefaultSecurityGateway 调用方异步化) ; 治理链路 AI 指令拦截
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
