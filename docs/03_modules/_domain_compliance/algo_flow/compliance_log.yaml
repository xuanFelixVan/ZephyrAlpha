# ALGO_FLOW 外部真源——compliance_log（_domain_compliance 域，真源 src/zephyr/compliance/compliance_log.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.compliance.compliance_log
source_of_truth: src/zephyr/compliance/compliance_log.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: path 参数
    #   fields: 参数 path（无注解）
    #   code: compliance_log.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ComplianceLogger
    #   name_en: ComplianceLogger
    #   intro: compliance_log JSONL 追写器。
    #   desc: compliance_log JSONL 追写器。 Args: path: 日志文件路径；None 时用默认生产路径（MAIN_REPO_ROOT 锚定）。；公共方法（定义序）: path, log, read_all…
    #   inputs: path
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ComplianceLogger
    #   downstream: zephyr.compliance.discipline_must_do_checker ; zephyr.compliance.discipline_pro…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
