# ALGO_FLOW 外部真源——license_usage_auditor（_domain_compliance 域，真源 src/zephyr/compliance/license_usage_auditor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.compliance.license_usage_auditor
source_of_truth: src/zephyr/compliance/license_usage_auditor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry_path 参数
    #   fields: 参数 registry_path（无注解）
    #   code: license_usage_auditor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: logger 参数
    #   fields: 参数 logger（无注解）
    #   code: license_usage_auditor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LicenseUsageAuditor
    #   name_en: LicenseUsageAuditor
    #   intro: 授权使用审计器。
    #   desc: 授权使用审计器。 Args: registry_path: data_asset_registry.yaml 路径（默认主仓真源）。 logger: 合规日志。；公共方法（定义序）: load_source, audi…
    #   inputs: registry_path logger
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: LicenseUsageAuditor
    #   downstream: 62 号 REG-DATAFLOW-001 治理流程（定期复核+新增消费模块触发，43 号 §5.3）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
