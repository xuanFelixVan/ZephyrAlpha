# ALGO_FLOW 外部真源——__init__（_domain_compliance 域，真源 src/zephyr/compliance/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.compliance.__init__
source_of_truth: src/zephyr/compliance/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 包导入请求 import zephyr.compliance
    #   fields: 仅触发包初始化，无任何运行时参数或数据输入
    #   code: src/zephyr/compliance/__init__.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① 空包骨架声明
    #   name_en: zephyr.compliance 包标记
    #   intro: 合规实现已全部迁至zephyr.governance/gov_audit等canonical包，本包只留子包骨架不导出任何符号
    #   desc: DM-291迁移后仅保留audit_trail/bridges等真实子包；5.60.8治本移除PEP 562 lazy re-export壳（全仓0消费者）；__all__=[]（L13-22）
    #   inputs: I1
    #   outputs: 空命名空间（无符号）
    #   invariant: 不再re-export任何符号
    # 层: 输出
    # - id: O1
    #   name_zh: 空导出列表 __all__=[]
    #   name_en: __all__
    #   intro: 不导出任何符号，调用方应直连canonical包（zephyr.governance等）
    #   downstream: 无下游/内部使用
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
