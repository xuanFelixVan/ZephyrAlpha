# ALGO_FLOW 外部真源——regulatory_change_tracker（_domain_compliance 域，真源 src/zephyr/compliance/regulatory_change_tracker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.compliance.regulatory_change_tracker
source_of_truth: src/zephyr/compliance/regulatory_change_tracker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: regulatory_change_tracker.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: source 参数
    #   fields: 参数 source（无注解）
    #   code: regulatory_change_tracker.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: extractor 参数
    #   fields: 参数 extractor（无注解）
    #   code: regulatory_change_tracker.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: impact_table 参数
    #   fields: 参数 impact_table（无注解）
    #   code: regulatory_change_tracker.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RegulatoryChangeTracker
    #   name_en: RegulatoryChangeTracker
    #   intro: 监管变更追踪器（采集 → 抽取校验 → 影响域映射 → 评审任务）。
    #   desc: 监管变更追踪器（采集 → 抽取校验 → 影响域映射 → 评审任务）。 Args: clock: 时钟注入。 source: 公告采集源注入（不真发请求）；None → Fail-…；公共方法（定义序）: collect…
    #   inputs: clock source extractor impact_table
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: RegulatoryChangeTracker
    #   downstream: 运行时装配批（公告源与LLM抽取统一注入 / 评审任务入人工确认队列）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
