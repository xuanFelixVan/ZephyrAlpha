# ALGO_FLOW 外部真源——strategy_cross_vote_funnel（_domain_signal 域，真源 src/zephyr/signal_ashare/strategy_signal/strategy_cross_vote_funnel.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.strategy_signal.strategy_cross_vote_funnel
source_of_truth: src/zephyr/signal_ashare/strategy_signal/strategy_cross_vote_funnel.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: strategy_cross_vote_funnel.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① StrategyCrossVoteFunnel
    #   name_en: StrategyCrossVoteFunnel
    #   intro: 多策略交叉投票漏斗（第五层在线信号层）。
    #   desc: 多策略交叉投票漏斗（第五层在线信号层）。；公共方法（定义序）: evaluate_symbol, run；源码 L145-L231
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: StrategyCrossVoteFunnel
    #   downstream: （候选：第六层组合优化 B10-01505 W-P1-21）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
