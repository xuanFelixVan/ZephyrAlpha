# ALGO_FLOW 外部真源——sector_siphon（_domain_signal 域，真源 src/zephyr/signal_ashare/sector/sector_siphon.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.sector.sector_siphon
source_of_truth: src/zephyr/signal_ashare/sector/sector_siphon.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: value 参数
    #   fields: 参数 value，类型注解 float
    #   code: sector_siphon.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: history 参数
    #   fields: 参数 history，类型注解 list[float]
    #   code: sector_siphon.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: sectors 参数
    #   fields: 参数 sectors，类型注解 list[SectorFlowSnapshot]
    #   code: sector_siphon.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: hhi_history 参数
    #   fields: 参数 hhi_history，类型注解 list[float]
    #   code: sector_siphon.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① rolling_zscore
    #   name_en: rolling_zscore
    #   intro: 当前值相对滚动窗口历史序列的 z-score。
    #   desc: 当前值相对滚动窗口历史序列的 z-score。 历史样本 <2 或标准差为 0 时返回 0.0（降级不触发，不误报）。；源码 L132-L144
    #   inputs: value history
    #   outputs: float
    # - id: A2
    #   name_zh: ② detect_siphon_state
    #   name_en: detect_siphon_state
    #   intro: 虹吸态识别——三信号滚动 z-score 加权。
    #   desc: 虹吸态识别——三信号滚动 z-score 加权。 Args: sectors: 当日全市场板块列表（含 turnover, net_inflow）。 hhi_history: 信…；源码 L147-L205
    #   inputs: sectors hhi_history conc_history outflow_history n_top threshold
    #   outputs: SiphonResult
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: float
    #   name_en: float
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: (待 G05 选股引擎市场状态适配输入 / sector_rotation_state 串联精判)
    # - id: O2
    #   name_zh: SiphonResult
    #   name_en: SiphonResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: (待 G05 选股引擎市场状态适配输入 / sector_rotation_state 串联精判)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: sectors(当日全市场板块 turnover+net_inflow, money_flow×sector_constituent 聚合)
    # I2: hhi/conc/outflow 三信号各自滚动窗口历史序列（z-score 标准化用）
    # A1: 信号① hhi_top_n = Σ(头部N板块成交额份额)², N=5
    # A2: 信号② inflow_concentration = 头部N净流入和 / 全市场|净流入|和
    # A3: 信号③ outflow_ratio = 其余板块净流出家数 / 其余板块家数
    # A4: rolling_zscore 三信号标准化 → siphon_score = 0.4×z_hhi + 0.35×z_conc + 0.25×z_outflow
    # O1: SiphonResult(is_siphon = score>1.5σ, siphon_score, siphon_sectors=头部N名单)
    # [/ALGO_FLOW]
