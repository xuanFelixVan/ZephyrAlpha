# ALGO_FLOW 外部真源——adaptive_conformal_tcp_rm_ddci（_domain_signal 域，真源 src/zephyr/signal_ashare/ml_forecast/adaptive_conformal_tcp_rm_ddci.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.ml_forecast.adaptive_conformal_tcp_rm_ddci
source_of_truth: src/zephyr/signal_ashare/ml_forecast/adaptive_conformal_tcp_rm_ddci.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: alpha 参数
    #   fields: 参数 alpha（无注解）
    #   code: adaptive_conformal_tcp_rm_ddci.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AdaptiveConformalTcpRmDdci
    #   name_en: AdaptiveConformalTcpRmDdci
    #   intro: 自适应保形（TCP-RM/DDCI 加权变体）。
    #   desc: 自适应保形（TCP-RM/DDCI 加权变体）。；公共方法（定义序）: margin, calibrate, predict_interval；源码 L96-L144
    #   inputs: alpha
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: AdaptiveConformalTcpRmDdci
    #   downstream: （远期：MOD-SIG-044 rolling 基线的加权升级消费方）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
