# ALGO_FLOW 外部真源——sell_news_overdraft_detector（_domain_signal 域，真源 src/zephyr/signal_ashare/sentiment/sell_news_overdraft_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.sentiment.sell_news_overdraft_detector
source_of_truth: src/zephyr/signal_ashare/sentiment/sell_news_overdraft_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: sell_news_overdraft_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SellNewsOverdraftDetector
    #   name_en: SellNewsOverdraftDetector
    #   intro: 利好落地变利空预期透支检测器。
    #   desc: 利好落地变利空预期透支检测器。；公共方法（定义序）: assess；源码 L161-L234
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: SellNewsOverdraftDetector
    #   downstream: （候选：L2-C 日历约束升级 / L2-A 落地日卖出信号装配层）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
