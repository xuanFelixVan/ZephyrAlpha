# ALGO_FLOW 外部真源——market_cap_tier（_domain_signal 域，真源 src/zephyr/signal_ashare/market_cap_tier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.market_cap_tier
source_of_truth: src/zephyr/signal_ashare/market_cap_tier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: float_mcap_yi 参数
    #   fields: 参数 float_mcap_yi，类型注解 float
    #   code: market_cap_tier.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① float_mcap_tier
    #   name_en: float_mcap_tier
    #   intro: 流通市值（亿元）→ 6 级分层。
    #   desc: 流通市值（亿元）→ 6 级分层。 Args: float_mcap_yi: 流通市值，单位亿元（≥0） Returns: MarketCapTier 分层标签 Raises: V…；源码 L87-L104
    #   inputs: float_mcap_yi
    #   outputs: MarketCapTier
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: MarketCapTier
    #   name_en: MarketCapTier
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: universe_registry 流通市值分层计算字段（登记待回填）；信号解释"市值定调子"第一道筛子
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
