# ALGO_FLOW 外部真源——kronos_tsfm_predictor（_domain_signal 域，真源 src/zephyr/signal_ashare/ml_forecast/kronos_tsfm_predictor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.ml_forecast.kronos_tsfm_predictor
source_of_truth: src/zephyr/signal_ashare/ml_forecast/kronos_tsfm_predictor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: kronos_tsfm_predictor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① KronosTsfmPredictor
    #   name_en: KronosTsfmPredictor
    #   intro: Kronos TSFM 预测器骨架（接口契约 + 朴素基线占位）。
    #   desc: Kronos TSFM 预测器骨架（接口契约 + 朴素基线占位）。；公共方法（定义序）: is_ready, load_checkpoint, fit_baseline, predict；源码 L97-L147
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: KronosTsfmPredictor
    #   downstream: （远期：信号层时序预测消费方）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
