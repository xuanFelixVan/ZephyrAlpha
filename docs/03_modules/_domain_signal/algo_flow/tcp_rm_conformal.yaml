# ALGO_FLOW 外部真源——tcp_rm_conformal（_domain_signal 域，真源 src/zephyr/signal_ashare/ml_forecast/tcp_rm_conformal.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.ml_forecast.tcp_rm_conformal
source_of_truth: src/zephyr/signal_ashare/ml_forecast/tcp_rm_conformal.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: tcp_rm_conformal.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: tcp_rm_conformal.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TcpRmConformal
    #   name_en: TcpRmConformal
    #   intro: TCP-RM 时序保形预测增强器（Robbins-Monro + DDCI 双反馈）。
    #   desc: TCP-RM 时序保形预测增强器（Robbins-Monro + DDCI 双反馈）。 在线语义：update(point, actual) 先用当前阈值出区间（无未来函数），再…；公共方法（定义序）: reset,…
    #   inputs: config clock
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: TcpRmConformal
    #   downstream: 运行时装配批（CP-VaR 回测 / 在线区间预测接密度预测下游）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
