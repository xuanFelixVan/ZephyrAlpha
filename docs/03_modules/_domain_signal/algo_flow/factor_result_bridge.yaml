# ALGO_FLOW 外部真源——factor_result_bridge（_domain_signal 域，真源 src/zephyr/signal_ashare/factor_result_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.factor_result_bridge
source_of_truth: src/zephyr/signal_ashare/factor_result_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: provider 参数
    #   fields: 参数 provider（无注解）
    #   code: factor_result_bridge.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: supported_versions 参数
    #   fields: 参数 supported_versions（无注解）
    #   code: factor_result_bridge.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: factor_result_bridge.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: factor_result_bridge.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FactorResultBridge
    #   name_en: FactorResultBridge
    #   intro: 因子计算结果消费桥接器。
    #   desc: 因子计算结果消费桥接器。 Args: provider: 取数回调 ``provider(as_of=None) -> FactorResultBatch`` （CTR-002…；公共方法（定义序）: audit_lo…
    #   inputs: provider supported_versions audit_sink clock
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: FactorResultBridge
    #   downstream: D_ASHARE_SIGNAL 信号合成侧（运行时装配批接线）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
