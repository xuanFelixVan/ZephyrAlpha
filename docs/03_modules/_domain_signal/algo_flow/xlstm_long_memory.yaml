# ALGO_FLOW 外部真源——xlstm_long_memory（_domain_signal 域，真源 src/zephyr/signal_ashare/ml_forecast/xlstm_long_memory.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.ml_forecast.xlstm_long_memory
source_of_truth: src/zephyr/signal_ashare/ml_forecast/xlstm_long_memory.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: decay 参数
    #   fields: 参数 decay（无注解）
    #   code: xlstm_long_memory.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① XLstmLongMemory
    #   name_en: XLstmLongMemory
    #   intro: xLSTM 长记忆骨架（EMA level/trend 占位）。
    #   desc: xLSTM 长记忆骨架（EMA level/trend 占位）。；公共方法（定义序）: is_fitted, fit_baseline, predict, memory_summary；源码 L66-L121
    #   inputs: decay
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: XLstmLongMemory
    #   downstream: （远期：长记忆时序特征消费方）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
