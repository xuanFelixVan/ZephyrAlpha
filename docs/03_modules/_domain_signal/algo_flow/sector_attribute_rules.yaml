# ALGO_FLOW 外部真源——sector_attribute_rules（_domain_signal 域，真源 src/zephyr/signal_ashare/sector/sector_attribute_rules.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.sector.sector_attribute_rules
source_of_truth: src/zephyr/signal_ashare/sector/sector_attribute_rules.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1 配置载荷 dict（yaml 解析结果或测试直注）
    # - id: I2 板块查询 (sector_name, sector_code) / 批量行
    # 层: 算法
    # - id: A1 载荷校验→规则表（fail-closed）
    # - id: A2 code 精确匹配 → keyword 子串匹配（首命中）
    # 层: 输出
    # - id: O1 SectorAttributeVerdict（attribute/label/rule_key/match_via）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1,I2 --> A2
    # A2 --> O1
