# ALGO_FLOW 外部真源——intraday_size_style（_domain_signal 域，真源 src/zephyr/signal_ashare/intraday_t0/intraday_size_style.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.intraday_t0.intraday_size_style
source_of_truth: src/zephyr/signal_ashare/intraday_t0/intraday_size_style.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: intraday_size_style.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① IntradaySizeStyle
    #   name_en: IntradaySizeStyle
    #   intro: 分时微结构与大小盘风格统计件（纯内存，时钟注入）。
    #   desc: 分时微结构与大小盘风格统计件（纯内存，时钟注入）。 Args: clock: 时钟注入（测试可控）；缺省系统时钟。；公共方法（定义序）: size_factor_series, style_persistence, i…
    #   inputs: clock
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: IntradaySizeStyle
    #   downstream: 运行时装配批（统一注入点装配：大小盘收益序列 / 分时价量接入）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
