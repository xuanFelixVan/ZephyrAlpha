# ALGO_FLOW 外部真源——calendar_effects_model（_domain_signal 域，真源 src/zephyr/signal_ashare/calendar_effects_model.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.calendar_effects_model
source_of_truth: src/zephyr/signal_ashare/calendar_effects_model.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: ttest_runner 参数
    #   fields: 参数 ttest_runner（无注解）
    #   code: calendar_effects_model.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: calendar_effects_model.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CalendarEffectsModel
    #   name_en: CalendarEffectsModel
    #   intro: 日历效应滚动统计件（纯内存；ttest_runner / clock 注入）。
    #   desc: 日历效应滚动统计件（纯内存；ttest_runner / clock 注入）。 Args: ttest_runner: 分组均值 t 检验回调 ``runner(group_a,…；公共方法（定义序）: monthly…
    #   inputs: ttest_runner clock
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: CalendarEffectsModel
    #   downstream: 运行时装配批（统一注入点装配：收益序列 / 日历映射 / 统计器注入）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
