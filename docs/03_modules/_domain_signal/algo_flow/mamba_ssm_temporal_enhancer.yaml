# ALGO_FLOW 外部真源——mamba_ssm_temporal_enhancer（_domain_signal 域，真源 src/zephyr/signal_ashare/ml_forecast/mamba_ssm_temporal_enhancer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.ml_forecast.mamba_ssm_temporal_enhancer
source_of_truth: src/zephyr/signal_ashare/ml_forecast/mamba_ssm_temporal_enhancer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: smoothing 参数
    #   fields: 参数 smoothing（无注解）
    #   code: mamba_ssm_temporal_enhancer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MambaSsmTemporalEnhancer
    #   name_en: MambaSsmTemporalEnhancer
    #   intro: Mamba-SSM 时序增强器骨架（z-score + EMA 占位）。
    #   desc: Mamba-SSM 时序增强器骨架（z-score + EMA 占位）。；公共方法（定义序）: is_fitted, fit, enhance；源码 L66-L113
    #   inputs: smoothing
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: MambaSsmTemporalEnhancer
    #   downstream: （远期：信号层时序特征增强消费方）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
