# ALGO_FLOW 外部真源——event_conditional_density（_domain_signal 域，真源 src/zephyr/signal_ashare/ml_forecast/event_conditional_density.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.ml_forecast.event_conditional_density
source_of_truth: src/zephyr/signal_ashare/ml_forecast/event_conditional_density.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: event_conditional_density.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: event_classifier 参数
    #   fields: 参数 event_classifier（无注解）
    #   code: event_conditional_density.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: event_conditional_density.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① EventConditionalDensity
    #   name_en: EventConditionalDensity
    #   intro: 事件驱动条件分布预测器（事件分桶 + 直方图/分位数 + 盘后批护栏）。
    #   desc: 事件驱动条件分布预测器（事件分桶 + 直方图/分位数 + 盘后批护栏）。；公共方法（定义序）: validate_conservation, classify_event, add_sample, add_sample…
    #   inputs: clock event_classifier config
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: EventConditionalDensity
    #   downstream: 运行时装配批（盘后事件条件分布批处理 / 信号-风控下游密度输入）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
