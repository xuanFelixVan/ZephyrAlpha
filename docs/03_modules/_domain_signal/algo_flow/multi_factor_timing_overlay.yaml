# ALGO_FLOW 外部真源——multi_factor_timing_overlay（_domain_signal 域，真源 src/zephyr/signal_ashare/multi_factor_timing_overlay.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.multi_factor_timing_overlay
source_of_truth: src/zephyr/signal_ashare/multi_factor_timing_overlay.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: multi_factor_timing_overlay.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MultiFactorTimingOverlay
    #   name_en: MultiFactorTimingOverlay
    #   intro: 多源择时信号叠加共振检测器。
    #   desc: 多源择时信号叠加共振检测器。；公共方法（定义序）: overlay；源码 L130-L206
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: MultiFactorTimingOverlay
    #   downstream: （候选：决策编排器+MOD-AU-010 裁决层）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
