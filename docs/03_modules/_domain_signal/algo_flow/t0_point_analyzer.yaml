# ALGO_FLOW 外部真源——t0_point_analyzer（_domain_signal 域，真源 src/zephyr/signal_ashare/intraday_t0/t0_point_analyzer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.intraday_t0.t0_point_analyzer
source_of_truth: src/zephyr/signal_ashare/intraday_t0/t0_point_analyzer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1 分钟bar序列 list[MinuteBar]（ts/open/high/low/close/volume，1分钟）
    # - id: I2 上下文 T0Context（symbol/prev_close/resistance/资金与情绪注入位）
    # 层: 特征
    # - id: F1 VWAP（典型价累计）
    # - id: F2 量比/偏离度/日内新高新低
    # 层: 算法
    # - id: A1 MOD-SIG-024 适配腿（点位检测映射）
    # - id: A2 做T专项三族检测
    # - id: A3 同方向冷却窗去重
    # - id: A4 +N 分钟命中回验+命中率聚合
    # 层: 输出
    # - id: O1 list[T0Signal] + T0VerifyReport（hits 逐信号回验 + stats pattern×window 命中率）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> F1
    # I1 --> F2
    # F1,F2,I2 --> A1
    # F1,F2 --> A2
    # A1,A2 --> A3
    # A3,I1 --> A4
    # A4 --> O1
