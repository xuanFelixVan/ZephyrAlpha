# ALGO_FLOW 外部真源——supply_chain_gnn（_domain_signal 域，真源 src/zephyr/signal_ashare/supply_chain_gnn.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.signal_ashare.supply_chain_gnn
source_of_truth: src/zephyr/signal_ashare/supply_chain_gnn.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: decay 参数
    #   fields: 参数 decay（无注解）
    #   code: supply_chain_gnn.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SupplyChainGnn
    #   name_en: SupplyChainGnn
    #   intro: 供应链 GNN 骨架（BFS 衰减传播占位）。
    #   desc: 供应链 GNN 骨架（BFS 衰减传播占位）。；公共方法（定义序）: is_fitted, fit_baseline, propagate_risk；源码 L64-L125
    #   inputs: decay
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: SupplyChainGnn
    #   downstream: （远期：供应链传导风险信号消费方）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
