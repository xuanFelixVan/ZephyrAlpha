---
task_id: TASK-INF-009-0019
title: "Capacity Assurance 对齐——Kill Switch + Token Budget（B95/B113 第七轮审计）"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P1
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: MEDIUM
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
  - "D:\\ZephyrAlpha\\docs\\03_modules\\l01_infrastructure\\capacity-assurance\\blueprint.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_orchestrator.py"
acceptance_criteria:
  - "Kill Switch（B95）：dispatch() 前检查 Capacity Assurance 的 Kill Switch 状态——如果 ON →拒绝新的 dispatch"
  - "Token Budget（B113）：跨 dispatch 的 Token 预算协调——200K 限额 + 80% 告警 + 100% block"
  - "_check_token_budget(dict[str,int])→bool：累积 Token 消耗超过限额→拒绝"
  - "Graceful Degradation 对齐：Kill Switch ON或Budget耗尽→Pipeline进入DEGRADED状态"
rollback_instructions: "回退 Kill Switch 和 Token Budget 检查→Pipeline无容量管控→可能Token超支"
context_assembly_manifest:
  blueprint_sections:
    - "§19 Capacity Assurance 对齐：Kill Switch + Token Budget（B95/B113 第七轮审计）"
key_contracts:
    - "MOD-INF-001 Capacity Assurance——Kill Switch 前置检查 + Token Budget 扣减 + Graceful Degradation"
depends_on:
  - TASK-INF-009-0002
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - capacity-assurance
  - kill-switch
  - token-budget
  - B95
  - B113
  - §19
---

# TASK-INF-009-0019: Capacity Assurance 对齐

## 背景

从蓝图 §19 提取（B95/B113 第七轮审计）：Pipeline 必须嵌入容量保障机制——在资源耗尽时主动拒绝新任务，而非"来者不拒直到系统崩溃"。对标 K8s ResourceQuota + LimitRange。

## 实现内容

### 1. Kill Switch 前置检查（B95）

```python
class KillSwitchStatus(BaseModel):
    active: bool
    reason: str
    activated_by: str
    activated_at: str

def _check_kill_switch(self) -> bool:
    """dispatch() 前检查——如果 Kill Switch 开启则拒绝"""
    ks = self.capacity_assurance.get_kill_switch()
    if ks.active:
        logger.warning(f"Kill Switch ACTIVE: {ks.reason}")
        return False
    return True
```

### 2. Token Budget 协调（B113）

```python
class TokenBudget(BaseModel):
    limit: int = 200000              # 200K tokens 每日限额
    consumed: int = 0
    warn_threshold: float = 0.80    # 80% 告警
    block_threshold: float = 1.00   # 100% 阻止

def _check_token_budget(self, estimated_tokens: int) -> tuple[bool, str]:
    """跨dispatch Token预算协调"""
    budget = self._token_budget
    
    if budget.consumed + estimated_tokens > budget.limit * budget.block_threshold:
        return False, f"Token预算耗尽：{budget.consumed}/{budget.limit}"
    
    if budget.consumed + estimated_tokens > budget.limit * budget.warn_threshold:
        # 发出WARNING→仍允许执行但通知Owner
        self._notify_budget_warning(budget.consumed + estimated_tokens, budget.limit)
    
    budget.consumed += estimated_tokens
    return True, ""
```

### 3. Graceful Degradation 对齐

当Kill Switch ON或Budget耗尽：
- Pipeline 状态 → DEGRADED
- 拒绝新dispatch
- 现有活跃dispatch → 标记需要尽快完成
- 通知Owner

## 验收标准

1. Kill Switch ON→dispatch拒绝并返回KillSwitchActive状态
2. Token消耗>80%→WARN通知
3. Token消耗>100%→BLOCK新dispatch
4. Budget状态跨dispatch共享

## 回滚说明

回退 KillSwitch+Buudget→Pipeline失去容量管控→Token消费可能失控。
