---
task_id: TASK-INF-009-0025
title: "与 Orchestrator 集成——CT-PIPE-ORC-001 全链路落地：Orc.create_task→Pipeline.dispatch→Orc.assign_session"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P0
task_type: INTEGRATION
layer: L01
estimated_complexity: HIGH
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_master-blueprint\\blueprint.md"
  - "D:\\ZephyrAlpha\\src\\zephyr\\orchestrator\\orchestrator.py"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\ct_pipe_routing.py"
  - "D:\\ZephyrAlpha\\config\\blueprint_routing.yaml"
acceptance_criteria:
  - "CT-PIPE-ORC-001：Orc.create_task()→Pipeline.dispatch()→Orc.assign_session()——全链路集成"
  - "Orchestrator 通过 PipelineRouter 从 TaskCard.hints 提取 target_layer→匹配19条路由配置"
  - "MOD-MASTER-001 §2.7 集成契约——Pipeline→Orc路由决策的正确性可在端到端测试中验证"
  - "Pipeline完成dispatch后→emit PIPELINE_COMPLETE事件→Orchestrator触发9个下游联动"
rollback_instructions: "断连 Pipeline→Orchestrator 集成。Orchestrator 恢复手工分配模型模式"
context_assembly_manifest:
  blueprint_sections:
    - "§25 与 Orchestrator 的集成（CT-PIPE-ORC-001）"
key_contracts:
    - "CT-PIPE-ORC-001——Pipeline→Orc路由决策集成契约，MOD-MASTER-001 §2.7定义"
depends_on:
  - TASK-INF-009-0003
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - orchestrator
  - CT-PIPE-ORC-001
  - integration
  - §25
---

# TASK-INF-009-0025: Orchestrator 集成

## 背景

从蓝图 §25 提取：CT-PIPE-ORC-001 集成契约——Pipeline 和 Orchestrator 的交互界面。
```
Orc.create_task(task_card) 
  → Pipeline.dispatch(task_card) → PipelineResult
  → Orc.assign_session(result)
  → 如果有 CLAUDE_RESCUE → Orc.schedule_rescue(task_card)
```

## 实现内容

### 完整调用链

```python
# Orchestrator 侧
class Orchestrator:
    def create_task(self, task_card: TaskCard):
        # Step 1: 交给Pipeline做路由决策
        result = self.pipeline.dispatch(task_card)
        
        # Step 2: 根据PipelineResult决定后续动作
        if result.overall_status == PipelineStatus.CLAUDE_RESCUE:
            self.schedule_rescue(task_card, result.rescue_reason)
        elif result.overall_status == PipelineStatus.LOCKED:
            self.deferred_queue.enqueue(task_card)  # B72 DeferredQueue
        else:
            self.assign_session(task_card, result)
            self.notify_owner(result)  # B515 告警触达
```

### PIPELINE_COMPLETE 事件→9个下游联动

Pipeline 完成 dispatch 后 emit PIPELINE_COMPLETE 事件，触发：
1. Orchestrator.assign_session()
2. FeedbackLoopEngine(B10 MOD-INF-010)接收反馈
3. CapacityAssurance(MOD-INF-001)更新Token Budget
4. SessionContinuity保存session状态
5. DeadLetterQueue检查是否需要replay
6. CostTracker累计成本
7. Descheduler扫描是否需要重调度
8. Notification System(B515)发送通知
9. AuditTrail(B101)写入决策日志

## 验收标准

1. Orc.create_task→Pipeline.dispatch→全链路可追踪
2. PIPELINE_COMPLETE事件→9个下游响应
3. Claude Rescue→Orchestrator正确调度
4. LOCKED→DeferredQueue正确入队

## 回滚说明

断连Pipeline→Orchestrator失去AI路由决策→恢复手工模型选择。
