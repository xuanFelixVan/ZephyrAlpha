---
task_id: TASK-INF-009-0008
title: "Pipeline 并发锁实现——MemoryLockBackend + FileLockBackend（跨进程）"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P0
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: MEDIUM
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_lock.py"
acceptance_criteria:
  - "PipelineLock.acquire(resource_id)→bool：原子获取锁"
  - "PipelineLock.release(resource_id)→bool：释放锁"
  - "MemoryLockBackend：进程内 dict 存储——用于单进程测试"
  - "FileLockBackend：os.mkdir 原子锁 + stale PID 检测——跨进程（v0.8.0 B133）"
  - "Lock TTL 过期：lock_ttl_s=300s stale 自动清理（v0.9.0 B167）"
  - "conflicts() 方法：检测当前活跃冲突"
rollback_instructions: "回退 pipeline_lock.py 中的 FileLockBackend，删掉 .pipeline_locks/ 目录。留 MemoryLockBackend 保底"
context_assembly_manifest:
  blueprint_sections:
    - "§8 Pipeline 并发锁（对标 K8s Resource Lock + Jenkins Build Blocker）"
key_features:
    - "B133 v0.8.0：跨进程文件锁——覆盖Trae+Cursor+RooCode多IDE场景"
    - "B167 v0.9.0：锁TTL过期——FileLockBackend lock_ttl_s=300s stale自动清理"
depends_on:
  - TASK-INF-009-0001
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - lock
  - concurrency
  - §8
---

# TASK-INF-009-0008: Pipeline 并发锁实现

## 背景

从蓝图 §8 提取：多个 IDE session（Trae + Cursor + RooCode）可能同时触发 dispatch，导致同一资源被并发修改。对标 K8s Resource Lock + Jenkins Build Blocker，Pipeline 需要跨进程锁机制。

## 实现内容

### 1. PipelineLock 接口

```python
class PipelineLock(BaseModel):
    backend: LockBackend
    
    def acquire(self, resource_id: str, owner: str, ttl_s: int=300) -> bool
    def release(self, resource_id: str, owner: str) -> bool
    def is_locked(self, resource_id: str) -> bool
    def get_owner(self, resource_id: str) -> str|None
    def conflicts(self) -> list[dict]
```

### 2. MemoryLockBackend（进程内）

```python
class MemoryLockBackend(LockBackend):
    _locks: dict[str, LockEntry] = {}  # resource_id → LockEntry
    
    def try_lock(self, resource_id, owner, ttl_s) -> bool:
        """dict.setdefault 原子操作"""
```

### 3. FileLockBackend（跨进程，B133）

```python
class FileLockBackend(LockBackend):
    lock_dir: str = ".pipeline_locks/"
    
    def try_lock(self, resource_id, owner, ttl_s) -> bool:
        """os.makedirs(lock_path, exist_ok=False)——文件系统原子性"""
        
    def _cleanup_stale(self):
        """检测 stale PID——TTL过期或进程已死→自动清理"""
```

### 4. Lock TTL 过期机制（B167）

- 默认 `lock_ttl_s=300s`（5分钟）
- 获取锁时写入 owner + PID + timestamp
- 释放锁时自动清理
- Stale 检测：锁超过 TTL 或 PID 不存在→自动释放

## 验收标准

1. MemoryLockBackend 在单进程中正确互斥
2. FileLockBackend 跨进程互斥——两个 Python 进程不能同时获取同一资源锁
3. TTL 过期后 stale 锁被正确清理
4. conflicts() 方法返回当前活跃锁冲突列表

## 回滚说明

回退 pipeline_lock.py 移除 FileLockBackend。删掉 `.pipeline_locks/` 目录。MemoryLockBackend 保底保留。
