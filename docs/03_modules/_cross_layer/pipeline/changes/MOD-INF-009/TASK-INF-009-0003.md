---
task_id: TASK-INF-009-0003
title: "路由决策树实现——CT-PIPE-ORC-001 落地 + Claude Rescue 触发器"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P0
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: HIGH
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
  - "D:\\ZephyrAlpha\\docs\\01_policies_and_standards\\governance\\ai\\GOV-AI-002.md"
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_master-blueprint\\blueprint.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\ct_pipe_routing.py"
  - "D:\\ZephyrAlpha\\config\\blueprint_routing.yaml"
acceptance_criteria:
  - "路由决策树7条规则可执行：task_type + priority + target_layer + complexity → PipelineRouteDecision"
  - "Claude Rescue 5条触发条件代码化：DeepSeek失败≥3 / GLM驳回≥2 / Owner critical / security标签 / experimental标签"
  - "Affinity约束在路由时强制执行：M3==M7 model → ABORT / M8!=M9 → WARN"
  - "从 TaskCard hints 提取 target_layer 可工作"
  - "ct_pipe_routing.py 实现 resolve 节点方法"
rollback_instructions: "回退 ct_pipe_routing.py 到上一版本，移除路由决策树相关代码"
context_assembly_manifest:
  blueprint_sections:
    - "§3 路由决策树（CT-PIPE-ORC-001 落地）"
    - "§3 routing_decision_tree YAML——7条路由规则 + 5条ClaudeRescue触发器 + affinity_enforcement"
    - "§25 与 Orchestrator 的集成（CT-PIPE-ORC-001）"
  key_contracts:
    - "CT-PIPE-ORC-001——Pipeline→Orc路由决策集成契约，Orc.create_task()→Pipeline.dispatch()→Orc.assign_session()"
depends_on:
  - TASK-INF-009-0002
blocks:
  - TASK-INF-009-0004
gate_profile: full_g0_g7
tags:
  - pipeline
  - routing
  - CT-PIPE-ORC-001
  - decision-tree
  - claude-rescue
  - §3
---

# TASK-INF-009-0003: 路由决策树实现（CT-PIPE-ORC-001 落地）

## 背景

从蓝图 §3 提取：路由决策树是 Pipeline 的大脑——输入 `TaskCard{task_type, priority, target_layer, estimated_complexity}` → 输出 `PipelineRouteDecision{node_id, execution_model, sandbox_profile, gate_profile}`。

集成契约 CT-PIPE-ORC-001：Orc.create_task() → Pipeline.dispatch() → Orc.assign_session()。Pipeline 路由决策必须可被 Orchestrator 消费。

## 实现内容

### 1. 路由决策树（7条规则）

```yaml
routing_decision_tree:
  input: "TaskCard { task_type, priority, target_layer, estimated_complexity }"
  output: "PipelineNode { node_id, execution_model, sandbox_profile, gate_profile }"

  rules:
    - condition: "task_type == MODEL_BUILD AND estimated_complexity == HIGH"
      route: "M1 (DeepSeek V4 Pro + full sandbox + full_g0_g7)"

    - condition: "task_type == MODEL_BUILD"
      route: "M2 (DeepSeek V4 Pro + standard sandbox + pre_commit_only)"

    - condition: "task_type == AUDIT AND priority == P0"
      route: "M3 (DeepSeek V4 Pro 复审 + audit sandbox + full_g0_g7)"

    - condition: "task_type == AUDIT"
      route: "M4 (DeepSeek V4 Pro + audit sandbox + post_exec_only)"

    - condition: "task_type ∈ {DOC_WRITE, REFACTOR} AND target_layer ∈ {L00,L01,L10}"
      route: "M5 (GLM-5.1 + standard sandbox + post_exec_only)"

    - condition: "task_type ∈ {DOC_WRITE, REFACTOR}"
      route: "M6 (DeepSeek V4 Pro + standard sandbox + pre_commit_only)"

    - condition: "task_type == AUTO_FIX"
      route: "M11 (DeepSeek V4 Pro + restricted + none)"
```

### 2. Claude Rescue 触发器（5条）

```python
claude_rescue_triggers:
    - "DeepSeek 失败次数 ≥ 3"
    - "GLM 驳回次数 ≥ 2"
    - "Owner 标记 critical/unsafe"
    - "security 标签"
    - "experimental 标签"
```

### 3. Affinity Enforcement（§2.5约束落地）

```yaml
affinity_enforcement:
  - check: "M3.model == M7.model"
    on_violation: "ABORT + escalate: 双盲审查模型冲突"
  - check: "M8.model != M9.model"
    on_violation: "WARN: 建议 M8/M9 使用不同模型交叉覆盖"
```

### 4. ct_pipe_routing.py 核心函数

- `resolve_route(task_card: TaskCard) -> PipelineRouteDecision`：根据路由决策树选择 M 节点
- `check_claude_rescue(context: RescueContext) -> bool`：判断是否需要 Claude 救援
- `extract_target_layer(task_card: TaskCard) -> str`：从 TaskCard hints 提取 target_layer
- `enforce_affinity(decision: PipelineRouteDecision, active_nodes: dict) -> list[str]`：校验 affinity 约束

## 验收标准

1. 7条路由规则全部可执行
2. 5条 Claude Rescue 触发条件代码化
3. Affinity enforce 在 ABORT 条件下正确中断
4. target_layer 从 CT-PIPE hints 正确提取
5. PipelineRouteDecision 包含 rationale（可审计）

## 回滚说明

回退 ct_pipe_routing.py，移除路由决策树逻辑。Orchestrator 调用方需移除对 resolve_route 的依赖。
