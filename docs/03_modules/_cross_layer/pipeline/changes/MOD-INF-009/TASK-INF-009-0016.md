---
task_id: TASK-INF-009-0016
title: "K8s 范式对齐——Descheduler 任务重平衡 + Scheduling Profiles（audit_strict/doc_fast/batch_low）（B92/B98 第七轮审计）"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P1
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: HIGH
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_orchestrator.py"
acceptance_criteria:
  - "Descheduler：后台定时扫描 STALE(>30min无推进)/MISROUTED(路由异常)/CLAUDE_STUCK(Claude超时5min) 任务→触发重调度"
  - "Scheduling Profiles 三配置：audit_strict(M3+M7双盲+full gate)/doc_fast(M1→M3跳过M7)/batch_low(攒P3 batch→30min或10个→batch dispatch)"
  - "Profile 选择规则：task_type=AUDIT+priority=P0→audit_strict / task_type∈{DOC_WRITE,REFACTOR}→doc_fast / priority=P3→batch_low"
rollback_instructions: "回退 Descheduler 定时任务和 Scheduling Profiles。恢复默认单配置调度"
context_assembly_manifest:
  blueprint_sections:
    - "§16 K8s 范式对齐：Descheduler + Scheduling Profiles（B92/B98 第七轮审计）"
depends_on:
  - TASK-INF-009-0002
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - descheduler
  - scheduling-profiles
  - k8s
  - B92
  - B98
  - §16
---

# TASK-INF-009-0016: K8s 范式对齐

## 背景

从蓝图 §16 提取（B92/B98 第七轮审计）：Pipeline 对标 Kubernetes Scheduler 的两个关键能力：
1. **Descheduler**：课后纠错——扫描已经路由但状态异常的 dispatch→重新调度
2. **Scheduling Profiles**：课前配置——不同任务类型用不同的调度策略

## 实现内容

### 1. Descheduler（后台定时扫描）

```python
class PipelineDescheduler:
    scan_interval_s: int = 300  # 每5分钟扫描
    
    def scan(self) -> list[dict]:
        stale_tasks = self._find_stale()        # >30min无状态推进
        misrouted_tasks = self._find_misrouted() # 路由与结果明显不匹配
        stuck_tasks = self._find_claude_stuck() # Claude Rescue超时5min
        
        for task in stale_tasks + misrouted_tasks + stuck_tasks:
            self._reschedule(task)  # 重新路由或升级到Claude
```

### 2. Scheduling Profiles（三配置）

| Profile | 适用条件 | 管线配置 |
|---------|---------|---------|
| **audit_strict** | task_type=AUDIT + priority=P0/P1 | M3+M7双盲 + full_g0_g7 |
| **doc_fast** | task_type∈{DOC_WRITE,REFACTOR} | M1→M3(跳过M7) + pre_commit_only |
| **batch_low** | priority=P3 | 攒P3 batch→30min或10个→batch dispatch + post_exec_only |

```python
class SchedulingProfile(BaseModel):
    name: str                     # "audit_strict"|"doc_fast"|"batch_low"
    timeout_s: float
    gate_profile: str
    skip_modules: list[str]       # doc_fast跳过 M7/M8/M9
    batch_window_s: int|None      # batch_low 30min batch window
    
PROFILES = {
    "audit_strict": SchedulingProfile("audit_strict", 600, "full_g0_g7", [], None),
    "doc_fast":     SchedulingProfile("doc_fast", 120, "pre_commit_only", ["M7","M8","M9"], None),
    "batch_low":    SchedulingProfile("batch_low", 1800, "post_exec_only", [], 1800),
}

def select_profile(task_card: TaskCard) -> SchedulingProfile:
    if task_card.task_type == "AUDIT" and task_card.priority in ("P0","P1"):
        return PROFILES["audit_strict"]
    elif task_card.task_type in ("DOC_WRITE","REFACTOR"):
        return PROFILES["doc_fast"]
    elif task_card.priority == "P3":
        return PROFILES["batch_low"]
    return PROFILES["audit_strict"]  # 默认
```

## 验收标准

1. Descheduler 扫描 STALE/MISROUTED/STUCK → 触发重调度
2. audit_strict 配置M3+M7双盲全覆盖
3. doc_fast 跳过审计管线节省30-50% Token

## 回滚说明

回退 Descheduler + Profiles → 所有任务用默认审计严格配置→Token消耗升高+审计过度。
