---
task_id: TASK-INF-009-0002
title: "M1-M11 双管线架构实现——A区生产管线 + B区审计管线 + 三层模型策略 + Fallback 链 + Affinity 约束矩阵"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P0
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: HIGH
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
  - "D:\\ZephyrAlpha\\docs\\01_policies_and_standards\\governance\\ai\\GOV-AI-002.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_orchestrator.py"
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\models.py"
acceptance_criteria:
  - "A区(M1-M5)生产管线节点定义完整：M1解析/M2上下文/M3生成/M4校验/M5打包"
  - "B区(M6-M11)审计管线节点定义完整：M6差异/M7审查/M8合规/M9风险/M10报告/M11裁决"
  - "三层模型策略实现：DeepSeek主力 + GLM审查 + Claude救援"
  - "Fallback链 DeepSeek→GLM→Claude 可运行"
  - "Affinity/Anti-Affinity约束矩阵(5条)代码化：M3↔M7 hard antiAffinity / M8↔M9 soft antiAffinity / A区sandbox约束 / A区pipeline跨区约束 / M8-M11 preferredAffinity"
  - "M3↔M7 antiAffinity硬约束影响链路文档化：deepseek不可用→M3降级glm→M7被迫用claude"
rollback_instructions: "回退 pipeline_orchestrator.py 和 models.py 到上一版本。移除 AffinityConstraint 相关代码"
context_assembly_manifest:
  blueprint_sections:
    - "§2 M1-M11 双管线架构（GOV-AI-002 v2.0.0）"
    - "§2.1 A区：生产管线（M1-M5）——节点/模型/Sandbox/Gate矩阵"
    - "§2.2 B区：审计管线（M6-M11）——节点/模型/Sandbox/Gate矩阵"
    - "§2.3 三层模型策略（GOV-AI-002 §一）——DeepSeek/GLM/Claude 成本+角色"
    - "§2.4 模型降级 Fallback 链（GOV-AI-002 §三）——DeepSeek→GLM→Claude"
    - "§2.5 Affinity / Anti-Affinity 约束矩阵——对标 K8s podAffinity/podAntiAffinity"
  key_contracts:
    - "GOV-AI-002 v2.0.0 模型路由策略——Pipeline必须遵循的三层模型+fallback+affinity完整规则集"
depends_on:
  - TASK-INF-009-0001
blocks:
  - TASK-INF-009-0003
gate_profile: full_g0_g7
tags:
  - pipeline
  - M1-M11
  - dual-pipeline
  - affinity
  - fallback
  - §2
---

# TASK-INF-009-0002: M1-M11 双管线架构实现

## 背景

从蓝图 §2 提取：Pipeline 的核心是 M1-M11 双管线架构：
- **A区(M1-M5)** = 生产管线——DeepSeek V4 Pro主力 + GLM-5.1打包
- **B区(M6-M11)** = 审计管线——DeepSeek/GLM分工审查 + DeepSeek裁决

三层模型策略（GOV-AI-002 §一）：DeepSeek V4 Pro(1.74/3.48$/M)→主力生产、GLM-5.1(免费)→深度审查、Claude Opus 4.7(5/25$/M)→特种救援。

关键创新：**Affinity/Anti-Affinity 约束矩阵**（对标 K8s podAffinity/podAntiAffinity），确保双盲审查的独立性（M3生成和M7审查必须用不同模型）。

## 实现内容

### 1. A区生产管线（M1-M5）节点定义

| 节点 | 职责 | 模型 | Sandbox | Gate |
|:---:|------|------|:---:|:---:|
| M1 | 任务卡解析→结构化执行计划 | DeepSeek V4 Pro | full | full_g0_g7 |
| M2 | 上下文装配→调用 context_engine | DeepSeek V4 Pro | standard | pre_commit_only |
| M3 | 代码/文档生成——核心生产 | DeepSeek V4 Pro | full | full_g0_g7 |
| M4 | 格式校验 | DeepSeek V4 Pro | standard | pre_commit_only |
| M5 | 产物打包 | GLM-5.1 | standard | post_exec_only |

### 2. B区审计管线（M6-M11）节点定义

| 节点 | 职责 | 模型 | Sandbox | Gate |
|:---:|------|------|:---:|:---:|
| M6 | 差异检测——产出 vs 期望（AP2边界标记） | DeepSeek V4 Pro | standard | pre_commit_only |
| M7 | 深度审查——逐个文件逻辑/合规 | GLM-5.1 | audit | full_g0_g7 |
| M8 | 标准合规——PS/GOV/ADR | DeepSeek V4 Pro | standard | post_exec_only |
| M9 | 风险评估——OWASP LLM Top 10 | DeepSeek V4 Pro | standard | post_exec_only |
| M10 | 审计报告→Finding 格式 | DeepSeek V4 Pro | standard | post_exec_only |
| M11 | 门禁裁决——G5/G6 | DeepSeek V4 Pro | restricted | none |

### 3. Affinity/Anti-Affinity 约束矩阵（5条）

```python
class AffinityWeight(str, Enum):
    HARD = "hard"    # mandatory —— 违反则 ABORT
    SOFT = "soft"    # preferred —— 违反则 WARN

class PipelineAffinityConstraint(BaseModel):
    constraint_type: str
    node_a: str
    node_b: str | None = None
    weight: AffinityWeight
    description: str

AFFINITY_CONSTRAINTS = [
    # 1. M3/M7 hard antiAffinity——双盲审查模型隔离
    PipelineAffinityConstraint("model", "M3", "M7", HARD, "双盲审查必须用不同模型"),
    # 2. M8/M9 soft antiAffinity——建议交叉
    PipelineAffinityConstraint("model", "M8", "M9", SOFT, "建议合规+风险用不同模型"),
    # 3. A区 sandbox mandatory affinity
    PipelineAffinityConstraint("sandbox", "M1", None, HARD, "M1-M4必须在full/standard"),
    # 4. A区 pipeline mandatory affinity——产出必须经M5→M6
    PipelineAffinityConstraint("pipeline", "A", None, HARD, "A区产出→M5打包→M6边界标记"),
    # 5. B区后半段 soft affinity
    PipelineAffinityConstraint("model", "M8", None, SOFT, "M8-M11优先deepseek降成本"),
]
```

### 4. M3↔M7 antiAffinity 硬约束影响链路

```
deepseek不可用 → M3降级到glm → M7被迫改用claude (不能同模)
→ claude成本上升但保证双盲独立性——这是双盲审计体系的安全底线
```

## 验收标准

1. A区5节点、B区6节点全部定义完整
2. 三层模型策略代码可执行
3. Fallback链：DeepSeek失败→GLM→Claude，GLM失败→DeepSeek→Claude，Claude失败→无降级
4. 5条affinity约束可校验
5. M3↔M7 hard antiAffinity ABORT机制生效

## 回滚说明

回退 pipeline_orchestrator.py 和 models.py 中 M1-M11 节点定义、AffinityConstraint 相关代码。
