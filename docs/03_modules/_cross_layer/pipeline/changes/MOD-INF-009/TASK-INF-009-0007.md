---
task_id: TASK-INF-009-0007
title: "优先级抢占实现——对标 K8s Priority Preemption + resume"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P1
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: MEDIUM
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_orchestrator.py"
acceptance_criteria:
  - "PreemptionRecord 数据模型：preempted_task_id/preempting_task_id/reason/timestamp/module_progress/savepoint"
  - "preempt_check(task_card) → bool：高优先级任务到达→低优先级任务挂起+保存检查点"
  - "resume(task_id) → PipelineResult：从检查点恢复被挂起任务"
  - "P0可抢占P2/P3，P1可抢占P3——抢占层级定义"
  - "挂起任务状态保存到 savepoint"
rollback_instructions: "回退 preempt_check/resume 逻辑，移除 PreemptionRecord"
context_assembly_manifest:
  blueprint_sections:
    - "§7 优先级抢占（对标 K8s Priority Preemption）"
depends_on:
  - TASK-INF-009-0002
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - preemption
  - priority
  - k8s
  - §7
---

# TASK-INF-009-0007: 优先级抢占实现

## 背景

从蓝图 §7 提取：当高优先级任务到达时，Pipeline 应像 K8s Scheduler 一样支持 Priority Preemption——挂起正在运行的低优先级任务，释放资源给高优先级任务，低优先级任务在资源恢复后从检查点 resume。

## 实现内容

### 1. PreemptionRecord

```python
class PreemptionRecord(BaseModel):
    preempted_task_id: str
    preempting_task_id: str
    reason: str
    timestamp: str
    module_progress: str      # 当前执行到哪个模块
    savepoint: dict           # 已保存的状态快照
```

### 2. 抢占层级

| 抢占者 | 可抢占 |
|:---:|:---:|
| P0 | P2, P3 |
| P1 | P3 |
| P2 | — |
| P3 | — |

### 3. preempt_check / resume

```python
def preempt_check(task_card: TaskCard, active_dispatches: dict[str, ModuleResult]) -> PreemptionRecord|None:
    """如果新任务优先级更高→选择最低优先级活跃任务→挂起"""

def resume(task_id: str) -> PipelineResult:
    """从 savepoint 恢复到挂起时的 module_progress→继续执行剩余模块"""
```

## 验收标准

1. P0到达时正确挂起P3任务
2. savepoint 保存挂起模块位置和已产出 Artifact
3. resume 从检查点正确恢复
4. 同优先级不触发抢占

## 回滚说明

回退 pipeline_orchestrator.py 中的 preempt_check/resume 方法，移除 PreemptionRecord。被挂起任务丢失检查点。
