---
task_id: TASK-INF-009-0012
title: "Lifecycle 与 EventBus 集成——LifecycleAware(on_init/on_startup/on_shutdown/health_check) + TASK_EVENT emit（B68/B71 第六轮审计）"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P1
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: MEDIUM
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
  - "D:\\ZephyrAlpha\\src\\zephyr\\shared\\lifecycle.py"
  - "D:\\ZephyrAlpha\\src\\zephyr\\shared\\event_bus.py"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_orchestrator.py"
acceptance_criteria:
  - "PipelineOrchestrator 实现 LifecycleAware 接口：on_init/on_startup/on_shutdown/health_check"
  - "on_startup：加载 state/config、预热模型连接"
  - "on_shutdown：等待活跃dispatch≤30s 优雅关机（v0.9.0 B152）"
  - "health_check：返回当前状态{status,active_dispatches,circuit_breaker,cost_today}"
  - "EventBus 集成：dispatch开始→TASK_EVENT(dispatched) / 完成→TASK_EVENT(completed) / 失败→TASK_EVENT(failed)"
rollback_instructions: "回退 pipeline_orchestrator.py 中的 LifecycleAware 实现和 EventBus 发布代码"
context_assembly_manifest:
  blueprint_sections:
    - "§12 Lifecycle 与 EventBus 集成（B68/B71 第六轮审计）"
    - "B68——Pipeline 生命周期管理"
    - "B71——Pipeline 与 EventBus 的 seam 位"
    - "B152 v0.9.0——优雅关机"
depends_on:
  - TASK-INF-009-0002
  - TASK-INF-009-0011
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - lifecycle
  - eventbus
  - B68
  - B71
  - §12
---

# TASK-INF-009-0012: Lifecycle 与 EventBus 集成

## 背景

从蓝图 §12 提取（B68/B71 第六轮审计 + B152 v0.9.0）：Pipeline 作为常驻服务组件，需要完整的生命周期管理——启动时加载状态、运行时发布事件、关闭时优雅退出。

## 实现内容

### 1. LifecycleAware 实现

```python
class PipelineOrchestrator(LifecycleAware):
    async def on_init(self):
        """加载config、注册metrics、预热API连接"""
        
    async def on_startup(self):
        """恢复上次未完成的dispatch、启动Descheduler定时器"""
        
    async def on_shutdown(self):
        """等待活跃dispatch ≤30s超时、保存state、释放锁"""
        # B152: graceful_shutdown——活跃dispatch超时后SIGKILL
        
    async def health_check(self) -> dict:
        """返回当前健康状态"""
        return {
            "status": "healthy"|"degraded"|"critical",
            "active_dispatches": len(self._active),
            "circuit_breaker": self.cb_state,
            "cost_today": self._cost_total,
            "dead_letter_count": len(self._dead_letters),
        }
```

### 2. EventBus 集成（B71）

```python
# dispatch 开始时
self.event_bus.emit(TaskEvent(
    event_type="TASK_DISPATCH_STARTED",
    task_id=task_id,
    pipeline=route_decision.node_id,
    timestamp=now()
))

# dispatch 完成时
self.event_bus.emit(TaskEvent(
    event_type="TASK_DISPATCH_COMPLETED",
    task_id=task_id,
    result=PipelineResult(...),
    timestamp=now()
))

# dispatch 失败时
self.event_bus.emit(TaskEvent(
    event_type="TASK_DISPATCH_FAILED",
    task_id=task_id,
    error=error_msg,
    timestamp=now()
))
```

### 3. Observer 模式

PipelineOrchestrator 通过 Observer 监听 EventBus 上的 PIPELINE_SIGNAL 事件——支持 cancel / modify_priority / switch_model。

## 验收标准

1. on_init→on_startup→health_check 生命周期可执行
2. on_shutdown 在30s内完成活跃dispatch等待
3. health_check 返回完整状态快照
4. TASK_DISPATCH_STARTED/COMPLETED/FAILED 事件正确发布

## 回滚说明

回退 LifecycleAware 实现和 EventBus 发布代码。保留 Pipeline 基础 dispatch 能力但失去生命周期管理和事件通知。
