---
task_id: TASK-INF-009-0009
title: "Pipeline→Agent 桥接实现——M→Role 映射 + directive chain（对标 K8s Scheduler→kubelet 映射）"
module_id: MOD-INF-009
related_blueprint: "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
status: planned
priority: P1
task_type: INFRASTRUCTURE
layer: L01
estimated_complexity: MEDIUM
assigned_to: ai-session
created: 2026-05-06
updated: 2026-05-06
upstream_files:
  - "D:\\ZephyrAlpha\\docs\\03_modules\\_cross_layer\\pipeline\\blueprint.md"
  - "D:\\ZephyrAlpha\\docs\\03_modules\\l01_infrastructure\\agent\\blueprint.md"
downstream_outputs:
  - "D:\\ZephyrAlpha\\src\\zephyr\\pipeline\\pipeline_agent_bridge.py"
acceptance_criteria:
  - "PipelineAgentBridge：M→Role 映射表——M1→planner_agent / M3→generator_agent / M7→reviewer_agent / M11→gating_agent"
  - "directive_chain：执行指令链——从路由决策→Agent角色→执行参数"
  - "桥接接口：bridge(route_decision)→AgentDirective"
  - "AgentDirective 包含：role/system_prompt/sandbox_type/model/task_card_ref/timeout"
rollback_instructions: "回退 pipeline_agent_bridge.py 中的桥接代码，移除 AgentDirective"
context_assembly_manifest:
  blueprint_sections:
    - "§9 Pipeline → Agent 桥接（对标 K8s Scheduler → kubelet Pod→Container 映射）"
depends_on:
  - TASK-INF-009-0003
blocks: []
gate_profile: full_g0_g7
tags:
  - pipeline
  - agent-bridge
  - M-to-Role
  - §9
---

# TASK-INF-009-0009: Pipeline→Agent 桥接实现

## 背景

从蓝图 §9 提取：Pipeline 选择 M 节点后，需要将路由决策翻译为 AgentOrchestrator 能理解的 Agent 指令。对标 K8s Scheduler→kubelet 的 Pod→Container 映射关系。

## 实现内容

### 1. M→Role 映射表

| M节点 | Agent Role | 说明 |
|:---:|------|------|
| M1 | planner_agent | 解析任务卡→执行计划 |
| M2 | context_agent | 上下文装配 |
| M3 | generator_agent | 代码/文档核心生成 |
| M4 | formatter_agent | 格式校验 |
| M5 | packager_agent | 产物打包 |
| M6 | diff_agent | 差异检测 |
| M7 | reviewer_agent | 深度审查 |
| M8 | compliance_agent | 标准合规 |
| M9 | risk_agent | 风险评估 |
| M10 | reporter_agent | 审计报告 |
| M11 | gating_agent | 门禁裁决 |

### 2. PipelineAgentBridge

```python
class AgentDirective(BaseModel):
    role: str               # 对应 M 节点的 agent role
    system_prompt: str      # 该 role 的 system prompt
    sandbox_type: str       # full/standard/audit/restricted
    model: str              # deepseek/glm/claude
    task_card_ref: str      # task_id
    timeout: float          # 超时秒数

class PipelineAgentBridge(BaseModel):
    role_mapping: dict[str, str]  # "M3" → "generator_agent"
    directive_chain: list[AgentDirective]
    
    def bridge(self, route_decision: PipelineRouteDecision) -> AgentDirective:
        """路由决策→AgentDirective翻译"""
```

## 验收标准

1. M1-M11 全部有 Agent Role 映射
2. bridge() 产出完整 AgentDirective
3. directive_chain 顺序与 DAG 拓扑一致

## 回滚说明

回退 pipeline_agent_bridge.py。AgentOrchestrator 需移除对 pipeline_agent_bridge 的 import。
