# ALGO_FLOW 外部真源——lineage_change_detector（_domain_data_governance 域，真源 src/zephyr/data_governance/lineage_change_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data_governance.lineage_change_detector
source_of_truth: src/zephyr/data_governance/lineage_change_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: detector_id 参数
    #   fields: 参数 detector_id（无注解）
    #   code: lineage_change_detector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: schedule 参数
    #   fields: 参数 schedule（无注解）
    #   code: lineage_change_detector.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: lineage_change_detector.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: notifier 参数
    #   fields: 参数 notifier（无注解）
    #   code: lineage_change_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LineageChangeDetector
    #   name_en: LineageChangeDetector
    #   intro: 血缘变更检测器（基线快照 + diff + DFS 影响集合 + 通知回调）。
    #   desc: 血缘变更检测器（基线快照 + diff + DFS 影响集合 + 通知回调）。；公共方法（定义序）: detector_id, schedule, baseline, fingerprint_of, downstrea…
    #   inputs: detector_id schedule clock notifier
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: LineageChangeDetector
    #   downstream: 运行时装配批（drift 检测器注册表挂载 / 下游依赖方通知路由）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
