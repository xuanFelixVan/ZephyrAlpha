# ALGO_FLOW 外部真源——auto_feature_discoverer（_domain_research 域，真源 src/zephyr/research/auto_feature_discoverer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.research.auto_feature_discoverer
source_of_truth: src/zephyr/research/auto_feature_discoverer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: ic_calculator 参数
    #   fields: 参数 ic_calculator（无注解）
    #   code: auto_feature_discoverer.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: min_ic 参数
    #   fields: 参数 min_ic（无注解）
    #   code: auto_feature_discoverer.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: min_ir 参数
    #   fields: 参数 min_ir（无注解）
    #   code: auto_feature_discoverer.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: top_n 参数
    #   fields: 参数 top_n（无注解）
    #   code: auto_feature_discoverer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AutoFeatureDiscoverer
    #   name_en: AutoFeatureDiscoverer
    #   intro: 价量算子模板组合特征发现器（生成 + IC/IR 初筛 + TopN 人工确认队列）。
    #   desc: 价量算子模板组合特征发现器（生成 + IC/IR 初筛 + TopN 人工确认队列）。 Args: ic_calculator: 注入 IC/IR 计算器，签名 ``expres…；公共方法（定义序）: generat…
    #   inputs: ic_calculator min_ic min_ir top_n base_series windows clock
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: AutoFeatureDiscoverer
    #   downstream: 运行时装配批（研究轨特征发现入口 / 因子库草稿治理串行合并）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
