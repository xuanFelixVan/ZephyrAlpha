# ALGO_FLOW 外部真源——event_driven_sleeve_strategy（_domain_portfolio_core 域，真源 src/zephyr/pf_core/strategies/event_driven_sleeve_strategy.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.pf_core.strategies.event_driven_sleeve_strategy
source_of_truth: src/zephyr/pf_core/strategies/event_driven_sleeve_strategy.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: event_driven_sleeve_strategy.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① EventDrivenSleeveStrategy
    #   name_en: EventDrivenSleeveStrategy
    #   intro: 事件驱动 sleeve 组装策略——事件冲击评分 × 异动确认取 Top-N。
    #   desc: 事件驱动 sleeve 组装策略——事件冲击评分 × 异动确认取 Top-N。 signals 负载约定（dict[str, dict]，键=标的代码）： { "600519":…；公共方法（定义序）: generat…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: EventDrivenSleeveStrategy
    #   downstream: zephyr.pf_core.strategies（lazy re-export）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
