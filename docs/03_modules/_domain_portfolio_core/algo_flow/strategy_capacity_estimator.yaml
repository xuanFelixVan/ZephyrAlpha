# ALGO_FLOW 外部真源——strategy_capacity_estimator（_domain_portfolio_core 域，真源 src/zephyr/pf_core/core/strategy_capacity_estimator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.pf_core.core.strategy_capacity_estimator
source_of_truth: src/zephyr/pf_core/core/strategy_capacity_estimator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 标的 ADV 表
    #   fields: adv_values {symbol: 日成交额>0}
    # - id: I2
    #   name: 策略参数
    #   fields: daily_turnover>0; participation_max; impact_tolerance_bps; current_aum≥0
    # 层: 算法
    # - id: A1
    #   name_zh: ① 有效参与率
    #   name_en: _effective_participation
    #   intro: p_impact=(tolerance/coef)²; effective=min(p_max,p_impact); 记录绑定约束
    # - id: A2
    #   name_zh: ② 容量与利用率
    #   name_en: estimate
    #   intro: capacity=Σadv×effective/turnover; utilization=current/capacity
    # - id: A3
    #   name_zh: ③ 预警与扩容建议
    #   name_en: _alert_advice
    #   intro: ≥1.0 BREACH(+DELEVERAGE), ≥0.8 WARNING; 建议按绑定约束枚举产出
    # 层: 输出
    # - id: O1
    #   name: StrategyCapacityReport
    #   fields: capacity_aum/binding/effective_participation/utilization/alert/advice（frozen）
    # 边:
    # I1 --> A2
    # I2 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1
    # [/ALGO_FLOW]
