# ALGO_FLOW 外部真源——__init__（_domain_portfolio_core 域，真源 src/zephyr/pf_core/strategies/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.pf_core.strategies.__init__
source_of_truth: src/zephyr/pf_core/strategies/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 属性访问请求 name参数
    #   fields: 属性名字符串（DefaultEquityStrategy / RebalanceMode）
    #   code: __getattr__(name) L15
    # - id: I2
    #   name: 懒加载映射表 _LAZY
    #   fields: 符号名 → (真实模块路径, 属性名) 共2条
    #   code: _LAZY L9
    # 层: 算法
    # - id: A1
    #   name_zh: ① 懒加载属性解析
    #   name_en: __getattr__
    #   intro: 首次访问符号时才import真实模块并缓存进globals，避免StrategyRegistry重复注册
    #   desc: name∈_LAZY → importlib.import_module(mod_path) → getattr取属性 → globals()[name]缓存 → 返回；不在映射表则 raise AttributeError
    #   inputs: I1 I2
    #   outputs: 目标类/枚举对象
    #   invariant: 真实源唯一=zephyr.pf_core.default_equity_strategy
    # 层: 输出
    # - id: O1
    #   name_zh: 再导出符号 默认权益策略+调仓模式枚举
    #   name_en: DefaultEquityStrategy / RebalanceMode
    #   intro: 兼容旧导入路径的再导出shim，真身在pf_core.default_equity_strategy
    #   invariant: __all__=[DefaultEquityStrategy, RebalanceMode]
    #   downstream: 无下游/内部使用
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
