# ALGO_FLOW 外部真源——exposure_manager（_domain_portfolio_core 域，真源 src/zephyr/pf_core/core/exposure_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.pf_core.core.exposure_manager
source_of_truth: src/zephyr/pf_core/core/exposure_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 组合持仓 + 行业映射（申万31）
    #   fields: positions {symbol: weight}（非负归一化）; industry_map {symbol: industry}
    # - id: I2
    #   name: 基准（二选一）
    #   fields: benchmark_weights（经映射合成行业权重）或 benchmark_industry_weights 直注
    # - id: I3
    #   name: 风格载荷 + 基准风格 + 行业动量
    #   fields: style_loadings/benchmark_style_exposures/industry_momentum（可选注入）
    # 层: 算法
    # - id: A1
    #   name_zh: ① 主动敞口计算
    #   name_en: _active
    #   intro: industry_active=p−b; style_active=Σw_p×loading−benchmark_style
    # - id: A2
    #   name_zh: ② 偏离分级
    #   name_en: _deviations
    #   intro: |active|>breach→BREACH, ≥warn→WARNING, 按|active/limit|降序
    # - id: A3
    #   name_zh: ③ 行业轮动信号
    #   name_en: _rotation
    #   intro: 动量横截面排名: top_n且active<band→OVERWEIGHT; bottom_n且active>−band→UNDERWEIGHT
    # 层: 输出
    # - id: O1
    #   name: ActiveExposureReport
    #   fields: industry_active/style_active/deviations/rotation/uncovered_symbols（frozen）
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A1 --> A3
    # A2 --> O1
    # A3 --> O1
    # [/ALGO_FLOW]
