# ALGO_FLOW 外部真源——multifactor_sleeve_strategy（_domain_portfolio_core 域，真源 src/zephyr/pf_core/strategies/multifactor_sleeve_strategy.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.pf_core.strategies.multifactor_sleeve_strategy
source_of_truth: src/zephyr/pf_core/strategies/multifactor_sleeve_strategy.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: multifactor_sleeve_strategy.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① MultifactorSleeveStrategy
    #   name_en: MultifactorSleeveStrategy
    #   intro: 多因子 sleeve 组装策略——横截面多因子合成打分取 Top-N 等权。
    #   desc: 多因子 sleeve 组装策略——横截面多因子合成打分取 Top-N 等权。 signals 负载约定（dict[str, dict[str, float]]，键=标的代码）：…；公共方法（定义序）: generate…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: MultifactorSleeveStrategy
    #   downstream: zephyr.pf_core.strategies（lazy re-export）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
