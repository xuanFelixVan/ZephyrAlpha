# ALGO_FLOW 外部真源——seat_pattern_classifier（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/implementations/seat_pattern_classifier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.implementations.seat_pattern_classifier
source_of_truth: src/zephyr/ml_train/implementations/seat_pattern_classifier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: seat_pattern_classifier.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① SeatPatternClassifierSkeleton
    #   name_en: SeatPatternClassifierSkeleton
    #   intro: 席位形态分类器骨架（禁真训练）。
    #   desc: 席位形态分类器骨架（禁真训练）。 数据接口：``features["X"]`` 为 dict[str, array-like]，键必须覆盖 ``SEAT_FEATURES``；`…；公共方法（定义序）: check_f…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: SeatPatternClassifierSkeleton
    #   downstream: MOD-ML-001 training_pipeline（编排位预留）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
