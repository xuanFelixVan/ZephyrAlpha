# ALGO_FLOW 外部真源——decision_annotation_dataset（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/decision_annotation_dataset.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.decision_annotation_dataset
source_of_truth: src/zephyr/ml_train/decision_annotation_dataset.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: connection 参数
    #   fields: 参数 connection（无注解）
    #   code: decision_annotation_dataset.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: decision_annotation_dataset.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DecisionAnnotationDataset
    #   name_en: DecisionAnnotationDataset
    #   intro: 交易决策标注数据集（SQLite 注入连接 + 版本管理）。
    #   desc: 交易决策标注数据集（SQLite 注入连接 + 版本管理）。；公共方法（定义序）: add_annotation, fill_outcome, export_sft_samples, export_review_dat…
    #   inputs: connection clock
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: DecisionAnnotationDataset
    #   downstream: 运行时装配批（SFT 样本导出/复盘数据集导出统一注入点装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
