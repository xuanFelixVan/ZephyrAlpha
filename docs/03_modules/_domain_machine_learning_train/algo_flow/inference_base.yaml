# ALGO_FLOW 外部真源——inference_base（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/inference_base.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.inference_base
source_of_truth: src/zephyr/ml_train/inference_base.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: inference_base.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① InferenceEngineBase
    #   name_en: InferenceEngineBase
    #   intro: 推理引擎抽象基类（OCP 扩展点 D_ML_TRAIN-INF）
    #   desc: 推理引擎抽象基类（OCP 扩展点 D_ML_TRAIN-INF） 契约对齐：CTR-P1-004（ModelServingRequest 入站）-> CTR-P1-005（Mod…；公共方法（定义序）: predict…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: InferenceEngineBase
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
