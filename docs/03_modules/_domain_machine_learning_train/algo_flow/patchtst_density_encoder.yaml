# ALGO_FLOW 外部真源——patchtst_density_encoder（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/implementations/patchtst_density_encoder.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.implementations.patchtst_density_encoder
source_of_truth: src/zephyr/ml_train/implementations/patchtst_density_encoder.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: patchtst_density_encoder.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PatchtstDensityEncoder
    #   name_en: PatchtstDensityEncoder
    #   intro: PatchTST 密度前置特征编码器。
    #   desc: PatchTST 密度前置特征编码器。 输入 (n, lookback, n_channels) 时序 → 输出密度预测前置特征。；公共方法（定义序）: fit, transform；源码 L154-L256
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: PatchtstDensityEncoder
    #   downstream: MOD-ML-DENSITY(密度头消费) ; MOD-ML-010(QNN Stage1前置特征)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 时序特征 x
    #   fields: (n, lookback, n_channels) 三维张量, 默认60天×60因子
    # - id: I2
    #   name: PatchtstEncoderConfig
    #   fields: patch_len=16, stride=8, d_proj=16, min_samples=8
    # 层: 算法
    # - id: A1
    #   name_zh: ① patchify
    #   name_en: patchify
    #   intro: 每通道切patch(通道独立)
    # - id: A2
    #   name_zh: ② SVD patch embedding
    #   name_en: svd_embedding
    #   intro: 全样本patch合并SVD取top-d_proj主成分
    # - id: A3
    #   name_zh: ③ 注意力池化
    #   name_en: attention_pool
    #   intro: query=均值embedding, softmax加权patch维→每通道d维向量
    # 层: 输出
    # - id: O1
    #   name_zh: PatchtstFeatures
    #   name_en: PatchtstFeatures
    #   intro: channel_embeddings(n,C,d)+pooled(n,d)作密度预测前置特征
    #   downstream: MOD-ML-DENSITY密度头; MOD-ML-010 QNN Stage1 ([CONSUMERS] 头)
    # [/ALGO_FLOW]
