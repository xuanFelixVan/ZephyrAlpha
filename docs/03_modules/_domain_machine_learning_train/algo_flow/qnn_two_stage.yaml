# ALGO_FLOW 外部真源——qnn_two_stage（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/implementations/qnn_two_stage.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.implementations.qnn_two_stage
source_of_truth: src/zephyr/ml_train/implementations/qnn_two_stage.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: qnn_two_stage.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TwoStageQnn
    #   name_en: TwoStageQnn
    #   intro: 两阶段分位数神经网络训练器 (UBS Quant Hub 2025)。
    #   desc: 两阶段分位数神经网络训练器 (UBS Quant Hub 2025)。 Stage1: 共性基座分位数网络 (跨标的复用)。 Stage2: per-symbol 仿射缩放头 (…；公共方法（定义序）: train,…
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: TwoStageQnn
    #   downstream: MOD-ML-DENSITY(密度头消费) ; D_REGIME(体制标签Stage2扩展)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 训练特征 features
    #   fields: X(n,d)特征矩阵 + symbol_ids(n,)标的标签 + feature_names(可选)
    # - id: I2
    #   name: 目标 target
    #   fields: (n,) 次日收益
    # 层: 算法
    # - id: A1
    #   name_zh: ① Stage1 共性基座
    #   name_en: stage1_commonality
    #   intro: 全样本合并池化,逐分位数拟合HGB quantile → q_base(x)
    # - id: A2
    #   name_zh: ② Stage2 市场缩放头
    #   name_en: stage2_scaling_head
    #   intro: per-symbol 仿射缩放 OLS估计;子样本不足degraded回退
    # - id: A3
    #   name_zh: ③ 体制快速重训
    #   name_en: retrain_stage2
    #   intro: 只重估仿射头,Stage1冻结,分钟级
    # - id: A4
    #   name_zh: ④ 分位数序列输出
    #   name_en: predict_quantiles
    #   intro: per-symbol 预测+单调不交叉修正
    # 层: 输出
    # - id: O1
    #   name_zh: 分位数序列预测
    #   name_en: dict[quantile, np.ndarray]
    #   intro: {q:(n,)}单调不减,下游GAP-F-01消费
    #   downstream: MOD-ML-DENSITY密度头; D_REGIME体制标签扩展 ([CONSUMERS] 头)
    # [/ALGO_FLOW]
