# ALGO_FLOW 外部真源——manager（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/training_dataset_manager/manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.training_dataset_manager.manager
source_of_truth: src/zephyr/ml_train/training_dataset_manager/manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: store 参数
    #   fields: 参数 store（无注解）
    #   code: manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TrainingDatasetManager
    #   name_en: TrainingDatasetManager
    #   intro: 训练数据集管理器（MOD-ML-003）。
    #   desc: 训练数据集管理器（MOD-ML-003）。 Parameters ---------- store : 持久化存储注入位（None=内存态）。需实现 ``save(dataset…；公共方法（定义序）: registe…
    #   inputs: store
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: TrainingDatasetManager
    #   downstream: MOD-ML-001 training_pipeline（load 段数据集版本锚定）；MOD-ML-009 learning_effect_feedback…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
