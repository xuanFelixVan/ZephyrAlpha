# ALGO_FLOW 外部真源——reproducibility_manager（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/reproducibility_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.reproducibility_manager
source_of_truth: src/zephyr/ml_train/reproducibility_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: env_collector 参数
    #   fields: 参数 env_collector（无注解）
    #   code: reproducibility_manager.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: reproducibility_manager.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: tracking_sink 参数
    #   fields: 参数 tracking_sink（无注解）
    #   code: reproducibility_manager.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: hasher 参数
    #   fields: 参数 hasher（无注解）
    #   code: reproducibility_manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ReproducibilityManager
    #   name_en: ReproducibilityManager
    #   intro: 可复现性管理器（环境快照 + 种子登记 + hash 校验 + 复现报告）。
    #   desc: 可复现性管理器（环境快照 + 种子登记 + hash 校验 + 复现报告）。；公共方法（定义序）: capture_environment, register_run, record_result, verify_re…
    #   inputs: env_collector clock tracking_sink hasher
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: ReproducibilityManager
    #   downstream: 运行时装配批（环境采集器绑定 / experiment_tracking 回调绑定 / 重跑比对编排装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
