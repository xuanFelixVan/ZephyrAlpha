# ALGO_FLOW 外部真源——limit_up_classifier（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/implementations/limit_up_classifier.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.implementations.limit_up_classifier
source_of_truth: src/zephyr/ml_train/implementations/limit_up_classifier.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: limit_up_classifier.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① LimitUpClassifierSkeleton
    #   name_en: LimitUpClassifierSkeleton
    #   intro: 打板涨停概率分类器骨架（禁真训练）。
    #   desc: 打板涨停概率分类器骨架（禁真训练）。 数据接口：``features["X"]`` 为 dict[str, array-like]，键必须覆盖 ``LIMIT_UP_FEATUR…；公共方法（定义序）: check_f…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: LimitUpClassifierSkeleton
    #   downstream: MOD-ML-001 training_pipeline（编排位预留）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
