# ALGO_FLOW 外部真源——pipeline_orchestrator（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/training_pipeline/pipeline_orchestrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.training_pipeline.pipeline_orchestrator
source_of_truth: src/zephyr/ml_train/training_pipeline/pipeline_orchestrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: pipeline_orchestrator.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① TrainingPipelineOrchestrator
    #   name_en: TrainingPipelineOrchestrator
    #   intro: 训练管线编排器（MOD-ML-001）。
    #   desc: 训练管线编排器（MOD-ML-001）。 用法:: result = TrainingPipelineOrchestrator().run(request) assert res…；公共方法（定义序）: run；源码…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: TrainingPipelineOrchestrator
    #   downstream: GAP-F-34/35 训练任务编排；MOD-ML-002 ai_operator（巡检消费运行记录）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
