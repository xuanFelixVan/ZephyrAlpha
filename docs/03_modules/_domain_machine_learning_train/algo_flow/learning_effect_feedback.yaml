# ALGO_FLOW 外部真源——learning_effect_feedback（_domain_machine_learning_train 域，真源 src/zephyr/ml_train/learning_effect_feedback.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ml_train.learning_effect_feedback
source_of_truth: src/zephyr/ml_train/learning_effect_feedback.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: decay_threshold 参数
    #   fields: 参数 decay_threshold（无注解）
    #   code: learning_effect_feedback.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: min_pairs 参数
    #   fields: 参数 min_pairs（无注解）
    #   code: learning_effect_feedback.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LearningEffectFeedback
    #   name_en: LearningEffectFeedback
    #   intro: 学习效果反馈回喂器（MOD-ML-009）。
    #   desc: 学习效果反馈回喂器（MOD-ML-009）。 Parameters ---------- decay_threshold : IC 衰减阈值（超过则推荐重训）。 min_pair…；公共方法（定义序）: compute…
    #   inputs: decay_threshold min_pairs
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: LearningEffectFeedback
    #   downstream: MOD-ML-007 meta_learning_evolution（经验回写位）；MOD-ML-002 ai_operator（巡检效果输入）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
