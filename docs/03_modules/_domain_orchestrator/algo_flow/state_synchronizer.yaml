# ALGO_FLOW 外部真源——state_synchronizer（_domain_orchestrator 域，真源 src/zephyr/orchestrator/lifecycle/state_synchronizer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.lifecycle.state_synchronizer
source_of_truth: src/zephyr/orchestrator/lifecycle/state_synchronizer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: db_path 参数
    #   fields: 参数 db_path（无注解）
    #   code: state_synchronizer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① StateSynchronizer
    #   name_en: StateSynchronizer
    #   intro: 同步 SQLite 状态与文件系统实际状态。
    #   desc: 同步 SQLite 状态与文件系统实际状态。；公共方法（定义序）: sync_all, sync_task, detect_orphans, detect_ghosts；源码 L199-L405
    #   inputs: db_path
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: StateSynchronizer
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
