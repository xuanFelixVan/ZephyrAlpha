# ALGO_FLOW 外部真源——capacity_budget（_domain_orchestrator 域，真源 src/zephyr/orchestrator/governance/capacity_budget.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.governance.capacity_budget
source_of_truth: src/zephyr/orchestrator/governance/capacity_budget.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_concurrent_tasks 参数
    #   fields: 参数 max_concurrent_tasks（无注解）
    #   code: capacity_budget.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CapacityBudgetController
    #   name_en: CapacityBudgetController
    #   intro: class CapacityBudgetController 源码 L113-L199
    #   desc: 公共方法（定义序）: budget, state, max_concurrent, can_accept, try_accept, release, get_queue_position, get_pool_quota…
    #   inputs: max_concurrent_tasks
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: CapacityBudgetController
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
