# ALGO_FLOW 外部真源——bulkhead_manager（_domain_orchestrator 域，真源 src/zephyr/orchestrator/fault_tolerance/bulkhead_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.fault_tolerance.bulkhead_manager
source_of_truth: src/zephyr/orchestrator/fault_tolerance/bulkhead_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: bulkhead_manager.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① BulkheadManager
    #   name_en: BulkheadManager
    #   intro: class BulkheadManager 源码 L73-L84
    #   desc: 公共方法（定义序）: get_quota, list_systems, detect_slow_call, get_shared_pool_limit；源码 L73-L84
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: BulkheadManager
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
