# ALGO_FLOW 外部真源——memory_writer（_domain_orchestrator 域，真源 src/zephyr/orchestrator/execution/memory_writer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.execution.memory_writer
source_of_truth: src/zephyr/orchestrator/execution/memory_writer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: task 参数
    #   fields: 参数 task，类型注解 object
    #   code: memory_writer.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: result 参数
    #   fields: 参数 result，类型注解 dict[str, Any] | None
    #   code: memory_writer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MemoryWriter
    #   name_en: MemoryWriter
    #   intro: class MemoryWriter 源码 L78-L98
    #   desc: 公共方法（定义序）: archive_to_vms；源码 L78-L98
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② archive_to_vms
    #   name_en: archive_to_vms
    #   intro: archive_to_vms(task, result) 源码 L101-L102
    #   desc: 源码 L101-L102
    #   inputs: task result
    #   outputs: ArchiveResult
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: ArchiveResult
    #   name_en: ArchiveResult
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.orchestrator.work_orchestrator
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
