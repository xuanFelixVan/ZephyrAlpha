# ALGO_FLOW 外部真源——agent_coordination_skill（_domain_orchestrator 域，真源 src/zephyr/orchestrator/agent_coordination_skill.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.agent_coordination_skill
source_of_truth: src/zephyr/orchestrator/agent_coordination_skill.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: cards 参数
    #   fields: 参数 cards（无注解）
    #   code: agent_coordination_skill.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: agent_coordination_skill.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: consensus 参数
    #   fields: 参数 consensus（无注解）
    #   code: agent_coordination_skill.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: agent_coordination_skill.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AgentCoordinationSkill
    #   name_en: AgentCoordinationSkill
    #   intro: Agent 协调技能（卡片库 + 分工 + 仲裁 + 共识 + A2A 网关 + 审计）。
    #   desc: Agent 协调技能（卡片库 + 分工 + 仲裁 + 共识 + A2A 网关 + 审计）。；公共方法（定义序）: register_card, card_of, agents, assign, arbitrate, t…
    #   inputs: cards clock consensus audit_sink a2a_gateway
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: AgentCoordinationSkill
    #   downstream: 运行时装配批（Agent Card 卡片库绑定 / 共识引擎回调 / 审计落库 / 真实 A2A 网关绑定）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
