# ALGO_FLOW 外部真源——deferred_queue（_domain_orchestrator 域，真源 src/zephyr/orchestrator/deferred_queue.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.deferred_queue
source_of_truth: src/zephyr/orchestrator/deferred_queue.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: observer 参数
    #   fields: 参数 observer（无注解）
    #   code: deferred_queue.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: db_path 参数
    #   fields: 参数 db_path（无注解）
    #   code: deferred_queue.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DeferredQueue
    #   name_en: DeferredQueue
    #   intro: Thread-safe deferred task queue with event-driven wake-up.
    #   desc: Thread-safe deferred task queue with event-driven wake-up. Tasks enter as WAITING and tra…；公共方法（定义序）: enqueue…
    #   inputs: observer db_path
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: DeferredQueue
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
