# ALGO_FLOW 外部真源——contract_registry（_domain_orchestrator 域，真源 src/zephyr/orchestrator/contracts/contract_registry.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.contracts.contract_registry
source_of_truth: src/zephyr/orchestrator/contracts/contract_registry.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: contract_registry.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContractRegistry
    #   name_en: ContractRegistry
    #   intro: class ContractRegistry 源码 L1013-L1103
    #   desc: 公共方法（定义序）: get, list_all, list_ids, check_ai_read_only, get_by_producer, get_by_consumer, get_route_target, s…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: ContractRegistry
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
