# ALGO_FLOW 外部真源——failure_matcher（_domain_orchestrator 域，真源 src/zephyr/orchestrator/resilience/failure_matcher.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.resilience.failure_matcher
source_of_truth: src/zephyr/orchestrator/resilience/failure_matcher.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: failure_matcher.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FailurePatternMatcher
    #   name_en: FailurePatternMatcher
    #   intro: 失败模式匹配器——分析 FAILED 事件并生成诊断报告。
    #   desc: 失败模式匹配器——分析 FAILED 事件并生成诊断报告。；公共方法（定义序）: active, activate, deactivate, analyze, diagnoses, clear_diagnoses；源码…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② FailureMatcher
    #   name_en: FailureMatcher
    #   intro: 错误文本分类器——按预定义模式将错误归类到 ``FailureCategory``。
    #   desc: 错误文本分类器——按预定义模式将错误归类到 ``FailureCategory``。 Public API: - ``match(error)``: 分类一段错误文本，返回 ``…；公共方法（定义序）: match,…
    #   inputs: config
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: FailurePatternMatcher, FailureMatcher
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
