# ALGO_FLOW 外部真源——chaos_hooks（_domain_orchestrator 域，真源 src/zephyr/orchestrator/fault_tolerance/chaos_hooks.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.fault_tolerance.chaos_hooks
source_of_truth: src/zephyr/orchestrator/fault_tolerance/chaos_hooks.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: engine 参数
    #   fields: 参数 engine（无注解）
    #   code: chaos_hooks.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: registry 参数
    #   fields: 参数 registry（无注解）
    #   code: chaos_hooks.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ChaosHookPolicy
    #   name_en: ChaosHookPolicy
    #   intro: class ChaosHookPolicy 源码 L91-L106
    #   desc: 公共方法（定义序）: add_step_fault；源码 L91-L106
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② ChaosHook
    #   name_en: ChaosHook
    #   intro: class ChaosHook 源码 L116-L198
    #   desc: 公共方法（定义序）: configure, pre_step_hook, post_step_hook, get_engine；源码 L116-L198
    #   inputs: engine registry
    #   outputs: 返回值
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: ChaosHookPolicy, ChaosHook
    #   downstream: zephyr.orchestrator.core.agent_orchestrator
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
