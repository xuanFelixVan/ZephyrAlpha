# ALGO_FLOW 外部真源——voting_first_multi_agent（_domain_orchestrator 域，真源 src/zephyr/orchestrator/voting_first_multi_agent.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.orchestrator.voting_first_multi_agent
source_of_truth: src/zephyr/orchestrator/voting_first_multi_agent.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: votes 参数
    #   fields: 参数 votes，类型注解 dict[str, str]
    #   code: voting_first_multi_agent.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: weights 参数
    #   fields: 参数 weights，类型注解 dict[str, float] | None
    #   code: voting_first_multi_agent.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① tally_votes
    #   name_en: tally_votes
    #   intro: 纯函数聚票：加权 plurality；平票时字典序小者确定性胜。
    #   desc: 纯函数聚票：加权 plurality；平票时字典序小者确定性胜。；源码 L108-L125
    #   inputs: votes weights
    #   outputs: VoteOutcome
    # - id: A2
    #   name_zh: ② VotingFirstMultiAgent
    #   name_en: VotingFirstMultiAgent
    #   intro: 投票优先多智能体编排器。
    #   desc: 投票优先多智能体编排器。；公共方法（定义序）: register_agent, run；源码 L128-L168
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: VoteOutcome
    #   name_en: VoteOutcome
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
