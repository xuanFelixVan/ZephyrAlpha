# ALGO_FLOW 外部真源——daban_exit_decision（_domain_execution_core 域，真源 src/zephyr/ex_core/daban_exit_decision.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.daban_exit_decision
source_of_truth: src/zephyr/ex_core/daban_exit_decision.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: position 参数
    #   fields: 参数 position，类型注解 dict
    #   code: daban_exit_decision.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: auction_data 参数
    #   fields: 参数 auction_data，类型注解 dict
    #   code: daban_exit_decision.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: holding_days 参数
    #   fields: 参数 holding_days，类型注解 int
    #   code: daban_exit_decision.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① NextDayExitDecision
    #   name_en: NextDayExitDecision
    #   intro: 次日出场完整决策（v1.9.2 补，整合 forecast_next_day_premium + 20号三档退出）。
    #   desc: 次日出场完整决策（v1.9.2 补，整合 forecast_next_day_premium + 20号三档退出）。；公共方法（定义序）: decide, classify_position_status…
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② reflush_next_day_exit_decision
    #   name_en: reflush_next_day_exit_decision
    #   intro: 反核二次出场决策（v1.9.2 补，Phase 5 候选）——§3.12 反核后次日不同走势的分别出场决策。
    #   desc: 反核二次出场决策（v1.9.2 补，Phase 5 候选）——§3.12 反核后次日不同走势的分别出场决策。；源码 L162-L178
    #   inputs: position auction_data holding_days
    #   outputs: dict
    # 层: 输出
    # - id: O1
    #   name_zh: dict
    #   name_en: dict
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.ex_core.daban_pit_safety（PIT 回测框架调用次日出场）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1
