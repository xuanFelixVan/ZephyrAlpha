# ALGO_FLOW 外部真源——crypto（_domain_execution_core 域，真源 src/zephyr/ex_core/rules/crypto.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.rules.crypto
source_of_truth: src/zephyr/ex_core/rules/crypto.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: crypto.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① CryptoRulePack
    #   name_en: CryptoRulePack
    #   intro: 数字货币交易规则包（T+0、无涨跌停、step_size/tick_size 按交易对各异）。
    #   desc: 数字货币交易规则包（T+0、无涨跌停、step_size/tick_size 按交易对各异）。 MVP 骨架：提供默认兜底值（BTC/ETH 现货常见口径），具体交易对元数据 等…；公共方法（定义序）: lot_rul…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: CryptoRulePack
    #   downstream: zephyr.ex_core.rules
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
