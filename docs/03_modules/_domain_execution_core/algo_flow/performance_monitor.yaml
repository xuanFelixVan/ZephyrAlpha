# ALGO_FLOW 外部真源——performance_monitor（_domain_execution_core 域，真源 src/zephyr/ex_core/performance_monitor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.performance_monitor
source_of_truth: src/zephyr/ex_core/performance_monitor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: window_size 参数
    #   fields: 参数 window_size（无注解）
    #   code: performance_monitor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: alerter 参数
    #   fields: 参数 alerter（无注解）
    #   code: performance_monitor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PerformanceMonitor
    #   name_en: PerformanceMonitor
    #   intro: 执行核心性能监控器（单机轻量）。
    #   desc: 执行核心性能监控器（单机轻量）。；公共方法（定义序）: record, metrics, stats, set_threshold, check_alerts, snapshot；源码 L101-L193
    #   inputs: window_size alerter
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: PerformanceMonitor
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
