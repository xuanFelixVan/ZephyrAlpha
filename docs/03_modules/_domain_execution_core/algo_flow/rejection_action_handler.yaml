# ALGO_FLOW 外部真源——rejection_action_handler（_domain_execution_core 域，真源 src/zephyr/ex_core/rejection_action_handler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.rejection_action_handler
source_of_truth: src/zephyr/ex_core/rejection_action_handler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: retry_fn 参数
    #   fields: 参数 retry_fn（无注解）
    #   code: rejection_action_handler.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: rejection_action_handler.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: reconcile_trigger 参数
    #   fields: 参数 reconcile_trigger（无注解）
    #   code: rejection_action_handler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RejectionActionExecutor
    #   name_en: RejectionActionExecutor
    #   intro: 拒单分类动作执行器（策略冻结表 + 执行器注入）。
    #   desc: 拒单分类动作执行器（策略冻结表 + 执行器注入）。 Args: retry_fn: 重试执行器 callable(order, error) -> broker_order_id…；公共方法（定义序）: is_stra…
    #   inputs: retry_fn alert_sink reconcile_trigger
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: RejectionActionExecutor
    #   downstream: TradingSession._handle_rejection(装配批次接线); OrderExecutionSaga(接管后归口)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: action(RejectionAction) + order(Order) + error(原始异常, 可选) + error_code(可选, classify_and_execute 入口)
    # I2: retry_fn(重试执行器注入) + alert_sink(告警出口注入) + reconcile_trigger(持仓对账触发注入)
    # F1: execute(action, order, error)——按 40 号 §2.7 表执行实际动作
    # A1: RETRY_ONCE——order_id 去重, 首拒调 retry_fn, 再拒降级放弃(撤单率≤15%防线)
    # A2: ALERT_FREEZE——告警 + strategy_id 入冻结表(is_strategy_frozen 供下单链查询, 人工 unfreeze)
    # A3: ALERT_RECONCILE——告警 + reconcile_trigger(strategy_id, symbol)(T+1锁定/持仓不一致对账)
    # A4: IDEMPOTENT_RETURN——返回已有 broker_order_id(幂等)
    # O1: RejectionActionResult(outcome/broker_order_id/frozen_strategy_id)——调用方留痕
    # [/ALGO_FLOW]
