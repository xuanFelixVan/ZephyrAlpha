# ALGO_FLOW 外部真源——execution_strategy_selector（_domain_execution_core 域，真源 src/zephyr/ex_core/execution_strategy_selector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.execution_strategy_selector
source_of_truth: src/zephyr/ex_core/execution_strategy_selector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: features 参数
    #   fields: 参数 features，类型注解 OrderFeatures
    #   code: execution_strategy_selector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① select_execution_strategy
    #   name_en: select_execution_strategy
    #   intro: 按订单特征选执行策略（纯函数：同输入必同输出，可独立单测）。
    #   desc: 按订单特征选执行策略（纯函数：同输入必同输出，可独立单测）。 Raises: StrategySelectionError: ADV/数量非正（Fail-Closed），或订单…；源码 L138-L194
    #   inputs: features
    #   outputs: StrategySelection
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: StrategySelection
    #   name_en: StrategySelection
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-EX-014(Order Splitter 消费选定算法) ; MOD-L06-001(TradingSession 执行编排)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
