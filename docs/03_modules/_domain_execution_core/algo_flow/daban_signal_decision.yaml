# ALGO_FLOW 外部真源——daban_signal_decision（_domain_execution_core 域，真源 src/zephyr/ex_core/daban_signal_decision.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.daban_signal_decision
source_of_truth: src/zephyr/ex_core/daban_signal_decision.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: echelon_health 参数
    #   fields: 参数 echelon_health，类型注解 str
    #   code: daban_signal_decision.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: echelon_height 参数
    #   fields: 参数 echelon_height，类型注解 int
    #   code: daban_signal_decision.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: sector_resonance 参数
    #   fields: 参数 sector_resonance，类型注解 float
    #   code: daban_signal_decision.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: follow_count 参数
    #   fields: 参数 follow_count，类型注解 int
    #   code: daban_signal_decision.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① pre_validate_daban_signal
    #   name_en: pre_validate_daban_signal
    #   intro: 打板信号前置质量评估（v1.9.3 补，梯队质量→yes/no门控），在 §3.2 情绪周期定位器之前调用。
    #   desc: 打板信号前置质量评估（v1.9.3 补，梯队质量→yes/no门控），在 §3.2 情绪周期定位器之前调用。 理论背书：arXiv:2607.27063 羊群 agent-bas…；源码 L109-L145
    #   inputs: echelon_health echelon_height sector_resonance follow_count
    #   outputs: dict
    # - id: A2
    #   name_zh: ② classify_decision_v192
    #   name_en: classify_decision_v192
    #   intro: 双引擎融合 7 类决策（v1.9.2 补第7类 REFLUSH_DIVE + 情绪周期门控切换）。
    #   desc: 双引擎融合 7 类决策（v1.9.2 补第7类 REFLUSH_DIVE + 情绪周期门控切换）。 §3.5 INVERSE_BOARD 是"地天反包"非"反核"，§3.12 反…；源码 L148-L172
    #   inputs: emotion_score tech_score phase is_limit_down_rebound
    #   outputs: str
    # 层: 输出
    # - id: O1
    #   name_zh: dict
    #   name_en: dict
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.ex_core.daban_pit_safety（PIT 回测框架主循环）
    # - id: O2
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.ex_core.daban_pit_safety（PIT 回测框架主循环）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: echelon_health/height/sector_resonance/follow_count（前置质量评估四输入）
    # I2: emotion_score/tech_score/phase/is_limit_down_rebound（7类决策四输入）
    # F1: pre_validate_daban_signal——健康40+高度20+共振20+跟风20 百分制门控
    # F2: classify_decision_v192——冰点/反核门控反核路径；否则 PHASE_THRESHOLDS 打板路径六类
    # O1: {pass, score, reason} / 决策类标签（BOARD/CONTINUE/INVERSE_BOARD/REFLUSH_DIVE/WATCH/REJECT/WAIT）
    # [/ALGO_FLOW]
