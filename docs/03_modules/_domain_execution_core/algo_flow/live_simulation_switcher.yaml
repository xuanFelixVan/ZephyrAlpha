# ALGO_FLOW 外部真源——live_simulation_switcher（_domain_execution_core 域，真源 src/zephyr/ex_core/live_simulation_switcher.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.live_simulation_switcher
source_of_truth: src/zephyr/ex_core/live_simulation_switcher.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: token_verifier 参数
    #   fields: 参数 token_verifier（无注解）
    #   code: live_simulation_switcher.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: live_simulation_switcher.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: audit_sink 参数
    #   fields: 参数 audit_sink（无注解）
    #   code: live_simulation_switcher.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LiveSimulationSwitcher
    #   name_en: LiveSimulationSwitcher
    #   intro: 实盘/模拟切换开关（默认模拟盘；切实盘需显式确认令牌+留痕）。
    #   desc: 实盘/模拟切换开关（默认模拟盘；切实盘需显式确认令牌+留痕）。 Args: token_verifier: 确认令牌验证器（注入；生产接线 Owner 签发的一次性 令牌校验，验…；公共方法（定义序）: current…
    #   inputs: token_verifier clock audit_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: LiveSimulationSwitcher
    #   downstream: MOD-L06-001(TradingSession 模式门) ; MOD-EX-058(miniQMT Channel Manager 实盘通道)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
