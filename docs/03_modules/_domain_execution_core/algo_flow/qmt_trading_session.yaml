# ALGO_FLOW 外部真源——qmt_trading_session（_domain_execution_core 域，真源 src/zephyr/ex_core/qmt_trading_session.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.qmt_trading_session
source_of_truth: src/zephyr/ex_core/qmt_trading_session.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: env 参数
    #   fields: 参数 env（无注解）
    #   code: qmt_trading_session.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: universe 参数
    #   fields: 参数 universe（无注解）
    #   code: qmt_trading_session.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: strategy 参数
    #   fields: 参数 strategy（无注解）
    #   code: qmt_trading_session.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: signal_provider 参数
    #   fields: 参数 signal_provider（无注解）
    #   code: qmt_trading_session.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① QmtTradingSession
    #   name_en: QmtTradingSession
    #   intro: QMT 文件桥交易会话
    #   desc: QMT 文件桥交易会话 Usage: session = QmtTradingSession( env="sim", universe=["510300.SH"], strate…；公共方法（定义序）: order_m…
    #   inputs: env universe strategy signal_provider price_provider options
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: QmtTradingSession
    #   downstream: scripts.construction.test_qmt_file_bridge_full; scripts.start_paper_session
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
