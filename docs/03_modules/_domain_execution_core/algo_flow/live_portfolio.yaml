# ALGO_FLOW 外部真源——live_portfolio（_domain_execution_core 域，真源 src/zephyr/ex_core/services/live_portfolio.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_core.services.live_portfolio
source_of_truth: src/zephyr/ex_core/services/live_portfolio.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: snapshot_supplier 参数
    #   fields: 参数 snapshot_supplier（无注解）
    #   code: live_portfolio.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LivePortfolioService
    #   name_en: LivePortfolioService
    #   intro: 实盘组合服务（持仓/可用/净值视图，数据走 RiskSnapshot 真源注入）。
    #   desc: 实盘组合服务（持仓/可用/净值视图，数据走 RiskSnapshot 真源注入）。 Args: snapshot_supplier: 风控快照供给器（生产接线 RiskDataP…；公共方法（定义序）: current…
    #   inputs: snapshot_supplier
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: LivePortfolioService
    #   downstream: MOD-L06-001(TradingSession 组合视图) ; 前端看板(持仓/可用/净值)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
