# ALGO_FLOW 外部真源——risk_limits（_domain_shared 域，真源 src/zephyr/shared/contracts/risk/risk_limits.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.contracts.risk.risk_limits
source_of_truth: src/zephyr/shared/contracts/risk/risk_limits.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 真源模块公共符号
    #   fields: RiskLimits
    #   code: zephyr.shared.contracts.risk_limits
    # 层: 算法
    # - id: A1
    #   name_zh: ① 符号转发
    #   name_en: re-export
    #   intro: 惰性转发全部公共符号，保证本导入路径兼容
    #   desc: __getattr__ + importlib，无自有实现
    #   inputs: I1
    #   outputs: 真源符号
    #   invariant: re-export shim，不包含任何自有实现
    # 层: 输出
    # - id: O1
    #   name_zh: RiskLimits 契约类
    #   name_en: risk_limits symbols
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
