# ALGO_FLOW 外部真源——alert_senders（_domain_shared 域，真源 src/zephyr/shared/alerts/alert_senders.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.alerts.alert_senders
source_of_truth: src/zephyr/shared/alerts/alert_senders.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: webhook_url 参数
    #   fields: 参数 webhook_url（无注解）
    #   code: alert_senders.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: http_post 参数
    #   fields: 参数 http_post（无注解）
    #   code: alert_senders.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: timeout_seconds 参数
    #   fields: 参数 timeout_seconds（无注解）
    #   code: alert_senders.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① WeChatWebhookSender
    #   name_en: WeChatWebhookSender
    #   intro: 企业微信机器人 webhook 实发 sender（markdown 消息）。
    #   desc: 企业微信机器人 webhook 实发 sender（markdown 消息）。 Args: webhook_url: 企业微信机器人 webhook 完整 URL（含 key；视…；公共方法（定义序）: send_ma…
    #   inputs: webhook_url http_post timeout_seconds
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② EmailSmtpSender
    #   name_en: EmailSmtpSender
    #   intro: SMTP 邮件实发 sender（SSL / STARTTLS）。
    #   desc: SMTP 邮件实发 sender（SSL / STARTTLS）。 Args: host/port: SMTP 服务地址。 username/password: 认证凭据（pas…；公共方法（定义序）: send_ma…
    #   inputs: host port username password from_addr to_addrs use_ssl smtp_factory
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: WeChatWebhookSender, EmailSmtpSender
    #   downstream: zephyr.risk.core.alert_generator(EmailChannel/WeChatChannel sender 注入); zephyr.…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: webhook_url(企业微信机器人) / SMTP(host/port/username/password/from/to) 配置 + http_post/smtp_factory 传输注入(测试逃生门)
    # I2: 待发内容(alert 对象/report 对象/纯文本)——duck-typing getattr 取字段, 不 import 上层域(防 shared→risk/reporting 反向依赖)
    # F1: WeChatWebhookSender.send_markdown(content)——企业微信 markdown 消息 POST, status_code==200 且 errcode==0 为送达
    # F2: EmailSmtpSender.send_mail(subject, body)——SMTP_SSL/STARTTLS 发送, 异常即 False
    # F3: as_alert_sender()/as_report_sender()——适配 alert_generator.Alert / report_publisher.ArchivedReport 的可调用门面
    # O1: bool 送达结果(False=软失败, 调用方记 FAILED/告警不阻断)
    # [/ALGO_FLOW]
