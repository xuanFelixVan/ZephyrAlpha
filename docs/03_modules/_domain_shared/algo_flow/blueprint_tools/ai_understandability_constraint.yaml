# ALGO_FLOW 外部真源——ai_understandability_constraint（_domain_shared 域，真源 src/zephyr/shared/blueprint_tools/ai_understandability_constraint.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.blueprint_tools.ai_understandability_constraint
source_of_truth: src/zephyr/shared/blueprint_tools/ai_understandability_constraint.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_line_length 参数
    #   fields: 参数 max_line_length（无注解）
    #   code: ai_understandability_constraint.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: max_nesting 参数
    #   fields: 参数 max_nesting（无注解）
    #   code: ai_understandability_constraint.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: min_comment_ratio 参数
    #   fields: 参数 min_comment_ratio（无注解）
    #   code: ai_understandability_constraint.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AiUnderstandabilityConstraint
    #   name_en: AiUnderstandabilityConstraint
    #   intro: class AiUnderstandabilityConstraint 源码 L71-L91
    #   desc: 公共方法（定义序）: check；源码 L71-L91
    #   inputs: max_line_length max_nesting min_comment_ratio
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: AiUnderstandabilityConstraint
    #   downstream: N/A (all consumers verified as phantom — stale references removed)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
