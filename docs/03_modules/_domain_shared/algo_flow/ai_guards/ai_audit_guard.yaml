# ALGO_FLOW 外部真源——ai_audit_guard（_domain_shared 域，真源 src/zephyr/shared/ai_guards/ai_audit_guard.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.ai_guards.ai_audit_guard
source_of_truth: src/zephyr/shared/ai_guards/ai_audit_guard.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: require_approval_for 参数
    #   fields: 参数 require_approval_for（无注解）
    #   code: ai_audit_guard.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AiAuditGuard
    #   name_en: AiAuditGuard
    #   intro: class AiAuditGuard 源码 L64-L95
    #   desc: 公共方法（定义序）: check, approve, get_pending；源码 L64-L95
    #   inputs: require_approval_for
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: AiAuditGuard
    #   downstream: N/A (all consumers verified as phantom — stale references removed)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
