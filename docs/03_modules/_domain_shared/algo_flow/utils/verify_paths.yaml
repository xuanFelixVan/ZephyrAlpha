# ALGO_FLOW 外部真源——verify_paths（_domain_shared 域，真源 src/zephyr/shared/utils/verify_paths.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.utils.verify_paths
source_of_truth: src/zephyr/shared/utils/verify_paths.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: verify_paths.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① verify_all
    #   name_en: verify_all
    #   intro: verify_all() 源码 L96-L136
    #   desc: 源码 L96-L136
    #   inputs: 无参数
    #   outputs: dict[str, Any]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
