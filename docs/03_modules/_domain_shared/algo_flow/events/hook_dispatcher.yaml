# ALGO_FLOW 外部真源——hook_dispatcher（_domain_shared 域，真源 src/zephyr/shared/events/hook_dispatcher.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.events.hook_dispatcher
source_of_truth: src/zephyr/shared/events/hook_dispatcher.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: event_bus 参数
    #   fields: 参数 event_bus（无注解）
    #   code: hook_dispatcher.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: data_dir 参数
    #   fields: 参数 data_dir（无注解）
    #   code: hook_dispatcher.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① HookDispatcher
    #   name_en: HookDispatcher
    #   intro: class HookDispatcher 源码 L104-L277
    #   desc: 公共方法（定义序）: bus, data_dir, hooks, register_hook, get_executions；源码 L104-L277
    #   inputs: event_bus data_dir
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: HookDispatcher
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
