# ALGO_FLOW 外部真源——circuit_breaker（_domain_shared 域，真源 src/zephyr/shared/resilience/circuit_breaker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.resilience.circuit_breaker
source_of_truth: src/zephyr/shared/resilience/circuit_breaker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: name 参数
    #   fields: 参数 name（无注解）
    #   code: circuit_breaker.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: failure_threshold 参数
    #   fields: 参数 failure_threshold（无注解）
    #   code: circuit_breaker.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: recovery_timeout_ms 参数
    #   fields: 参数 recovery_timeout_ms（无注解）
    #   code: circuit_breaker.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: half_open_max_calls 参数
    #   fields: 参数 half_open_max_calls（无注解）
    #   code: circuit_breaker.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CircuitBreaker
    #   name_en: CircuitBreaker
    #   intro: 纯内存三态熔断器。
    #   desc: 纯内存三态熔断器。 Usage:: cb = CircuitBreaker("llm_api", failure_threshold=5, recovery_timeout_ms…；公共方法（定义序）: transit…
    #   inputs: name failure_threshold recovery_timeout_ms half_open_max_calls
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: CircuitBreaker
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
