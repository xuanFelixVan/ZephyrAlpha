# ALGO_FLOW 外部真源——error_budget_tracker（_domain_shared 域，真源 src/zephyr/shared/resilience/error_budget_tracker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.resilience.error_budget_tracker
source_of_truth: src/zephyr/shared/resilience/error_budget_tracker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: slo_target 参数
    #   fields: 参数 slo_target（无注解）
    #   code: error_budget_tracker.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: window_hours 参数
    #   fields: 参数 window_hours（无注解）
    #   code: error_budget_tracker.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ErrorBudgetTracker
    #   name_en: ErrorBudgetTracker
    #   intro: class ErrorBudgetTracker 源码 L67-L94
    #   desc: 公共方法（定义序）: record_success, record_error, status；源码 L67-L94
    #   inputs: slo_target window_hours
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: ErrorBudgetTracker
    #   downstream: zephyr.infrastructure.capacity_assurance.modules.__init__ ; zephyr.feedback_loo…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
