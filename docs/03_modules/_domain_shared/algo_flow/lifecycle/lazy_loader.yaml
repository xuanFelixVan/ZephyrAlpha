# ALGO_FLOW 外部真源——lazy_loader（_domain_shared 域，真源 src/zephyr/shared/lifecycle/lazy_loader.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.lifecycle.lazy_loader
source_of_truth: src/zephyr/shared/lifecycle/lazy_loader.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: core_modules 参数
    #   fields: 参数 core_modules（无注解）
    #   code: lazy_loader.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LazyModuleRegistry
    #   name_en: LazyModuleRegistry
    #   intro: class LazyModuleRegistry 源码 L84-L188
    #   desc: 公共方法（定义序）: register, load, is_loaded, is_registered, get, load_core_modules, unload, list_entries, stats…
    #   inputs: core_modules
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: LazyModuleRegistry
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
