# ALGO_FLOW 外部真源——cache_consistency_manager（_domain_shared 域，真源 src/zephyr/shared/io/cache_consistency_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.io.cache_consistency_manager
source_of_truth: src/zephyr/shared/io/cache_consistency_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: cache_consistency_manager.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: cache_consistency_manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CacheConsistencyManager
    #   name_en: CacheConsistencyManager
    #   intro: 缓存一致性管理器（分层注册 + 失效策略 + 写策略裁定 + 巡检）。
    #   desc: 缓存一致性管理器（分层注册 + 失效策略 + 写策略裁定 + 巡检）。；公共方法（定义序）: register_entry, set_write_policy, write_policy_for, write, flu…
    #   inputs: clock alert_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: CacheConsistencyManager
    #   downstream: 运行时装配批（分层缓存注册 / 写策略裁定读侧 / 一致性巡检计划任务）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
