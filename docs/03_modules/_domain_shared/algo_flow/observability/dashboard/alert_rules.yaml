# ALGO_FLOW 外部真源——alert_rules（_domain_shared 域，真源 src/zephyr/shared/observability/dashboard/alert_rules.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.observability.dashboard.alert_rules
source_of_truth: src/zephyr/shared/observability/dashboard/alert_rules.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: alert_rules.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① generate_alert_rules_yaml
    #   name_en: generate_alert_rules_yaml
    #   intro: 生成 Prometheus 告警规则 YAML（用于 prometheus rules 文件）。
    #   desc: 生成 Prometheus 告警规则 YAML（用于 prometheus rules 文件）。 Returns: Prometheus alerting rules YAML…；源码 L105-L132
    #   inputs: 无参数
    #   outputs: str
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
