# ALGO_FLOW 外部真源——tracing（_domain_shared 域，真源 src/zephyr/shared/observability/tracing.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.observability.tracing
source_of_truth: src/zephyr/shared/observability/tracing.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: name 参数
    #   fields: 参数 name，类型注解 str
    #   code: tracing.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: attributes 参数
    #   fields: 参数 attributes，类型注解 dict[str, Any] | None
    #   code: tracing.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: kind 参数
    #   fields: 参数 kind，类型注解 str
    #   code: tracing.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① start_span
    #   name_en: start_span
    #   intro: start_span(name, attributes, kind) 源码 L138-L172
    #   desc: 源码 L138-L172
    #   inputs: name attributes kind
    #   outputs: Generator[Any, None, None]
    # - id: A2
    #   name_zh: ② traced
    #   name_en: traced
    #   intro: traced(name, kind) 源码 L175-L186
    #   desc: 源码 L175-L186
    #   inputs: name kind
    #   outputs: Callable[[Callable[..., Any]], Callable…
    # 层: 输出
    # - id: O1
    #   name_zh: Generator[Any, None, None]
    #   name_en: Generator[Any, None, None]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # - id: O2
    #   name_zh: Callable[[Callable[..., Any]], Callable…
    #   name_en: Callable[[Callable[..., Any]], Callable…
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> A2
    # A2 --> O1
