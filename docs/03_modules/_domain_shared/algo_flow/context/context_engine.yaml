# ALGO_FLOW 外部真源——context_engine（_domain_shared 域，真源 src/zephyr/shared/context/context_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.context.context_engine
source_of_truth: src/zephyr/shared/context/context_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: context_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: max_tokens 参数
    #   fields: 参数 max_tokens（无注解）
    #   code: context_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: now 参数
    #   fields: 参数 now（无注解）
    #   code: context_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ContextEngine
    #   name_en: ContextEngine
    #   intro: class ContextEngine 源码 L116-L549
    #   desc: 公共方法（定义序）: budget, max_tokens, project_root, assemble_context, check_token_budget, validate_pipeline_modules,…
    #   inputs: project_root max_tokens now
    #   outputs: 返回值
    #   （注：A1 之后另有 9 个公共定义未列入（含 9 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（10 定义）
    #   name_en: public defs
    #   intro: ContextEngine
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
