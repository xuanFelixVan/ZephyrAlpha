# ALGO_FLOW 外部真源——a2a_protocol（_domain_shared 域，真源 src/zephyr/shared/protocols/a2a/a2a_protocol.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.shared.protocols.a2a.a2a_protocol
source_of_truth: src/zephyr/shared/protocols/a2a/a2a_protocol.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: a2a_protocol.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① A2ACommunicationProtocol
    #   name_en: A2ACommunicationProtocol
    #   intro: Protocol interface for A2A communication handlers.
    #   desc: Protocol interface for A2A communication handlers. Any class that implements send_message…；公共方法（定义序）: send_me…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: A2ACommunicationProtocol
    #   downstream: zephyr.shared.protocols.a2a; zephyr.infrastructure.a2a_protocol
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
