# ALGO_FLOW 外部真源——__init__（_domain_integration 域，真源 src/zephyr/integration/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.integration.__init__
source_of_truth: src/zephyr/integration/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 包导入请求
    #   fields: import zephyr.integration（消费者拿门面符号与子模块命名空间）
    #   code: L15-16 from-import + __all__
    # 层: 算法
    # - id: A1
    #   name_zh: 门面再导出
    #   name_en: facade_reexport
    #   intro: 无业务逻辑——LLMBridge/get_asset_summary 顶层再导出，__all__ 声明子模块全集
    # 层: 输出
    # - id: O1
    #   name_zh: 包门面符号
    #   name_en: facade_symbols
    #   intro: LLMBridge / get_asset_summary + 12 个子模块命名空间
    #   downstream: D_INTEGRATION 全部消费者
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
