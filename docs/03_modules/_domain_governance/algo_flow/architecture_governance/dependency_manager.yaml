# ALGO_FLOW 外部真源——dependency_manager（_domain_governance 域，真源 src/zephyr/governance/architecture_governance/dependency_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.architecture_governance.dependency_manager
source_of_truth: src/zephyr/governance/architecture_governance/dependency_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: tier 参数
    #   fields: 参数 tier，类型注解 DependencyTier
    #   code: dependency_manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① get_by_tier
    #   name_en: get_by_tier
    #   intro: get_by_tier(tier) 源码 L104-L105
    #   desc: 源码 L104-L105
    #   inputs: tier
    #   outputs: list[ManagedDependency]
    # - id: A2
    #   name_zh: ② get_core_deps
    #   name_en: get_core_deps
    #   intro: get_core_deps() 源码 L108-L109
    #   desc: 源码 L108-L109
    #   inputs: 无参数
    #   outputs: list[ManagedDependency]
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: list[ManagedDependency]
    #   name_en: list[ManagedDependency]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
