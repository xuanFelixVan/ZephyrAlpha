# ALGO_FLOW 外部真源——retirement_workflow（_domain_governance 域，真源 src/zephyr/governance/lifecycle_governance/retirement_workflow.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.lifecycle_governance.retirement_workflow
source_of_truth: src/zephyr/governance/lifecycle_governance/retirement_workflow.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: diagnosis 参数
    #   fields: 参数 diagnosis，类型注解 RetirementDiagnosis
    #   code: retirement_workflow.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: strategy_id 参数
    #   fields: 参数 strategy_id，类型注解 str
    #   code: retirement_workflow.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: ports 参数
    #   fields: 参数 ports，类型注解 RetirementPorts
    #   code: retirement_workflow.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: sustained_min_days 参数
    #   fields: 参数 sustained_min_days（无注解）
    #   code: retirement_workflow.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① decide_retirement
    #   name_en: decide_retirement
    #   intro: 三选一决策矩阵（61 号 §3.9 Step 3，唯一真源）。
    #   desc: 三选一决策矩阵（61 号 §3.9 Step 3，唯一真源）。；源码 L209-L215
    #   inputs: diagnosis
    #   outputs: RetirementDecision
    # - id: A2
    #   name_zh: ② classify_decay_knight
    #   name_en: classify_decay_knight
    #   intro: 五骑士归因 MVP 规则（61 号 §3.9 Step 5；完整归因须人工/因子证据，MVP 默认映射）。
    #   desc: 五骑士归因 MVP 规则（61 号 §3.9 Step 5；完整归因须人工/因子证据，MVP 默认映射）。；源码 L218-L222
    #   inputs: diagnosis
    #   outputs: DecayKnight
    # - id: A3
    #   name_zh: ③ run_retirement_workflow
    #   name_en: run_retirement_workflow
    #   intro: 退役 5 步工作流编排（61 号 §3.9）。
    #   desc: 退役 5 步工作流编排（61 号 §3.9）。 Args: strategy_id: 策略 ID。 ports: 执行端口（四必备诊断 + 六可选执行）。 sustained_m…；源码 L225-L330
    #   inputs: strategy_id ports sustained_min_days lookback_days human_approved kni…
    #   outputs: RetirementWorkflowResult
    #   （注：A3 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: RetirementDecision
    #   name_en: RetirementDecision
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 调用方（周/月退役评审编排；首个退役策略触发时由运营入口装配）
    # - id: O2
    #   name_zh: DecayKnight
    #   name_en: DecayKnight
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 调用方（周/月退役评审编排；首个退役策略触发时由运营入口装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: strategy_id + RetirementPorts（get_decay_alerts/run_backtest/check_peers/get_regime_mismatch 四必备 + 执行六可选）
    # I2: sustained_min_days=10（持续告警阈值）+ lookback_days=126（诊断回测窗口）+ human_approved（评审制人工闸门）+ knight（五骑士归因，可选）
    # F1: Step1 触发——持续告警过滤（consecutive_days≥10）→ OBSERVING，未达标→MONITORING 返回
    # F2: Step2 诊断——oos_sharpe/is_regime_wide/regime_mismatch 三元组（端口供给，本模块不重算）
    # F3: Step3 决策——三选一矩阵（oos>0&mismatch→REOPTIMIZE；oos>-0.2&非全策略坏→PAUSE_CUT_SIZE；否则 RETIRE）
    # F4: Step4 退役执行——仅 RETIRE+human_approved：scale(0.5)→disable_new_entries→flatten→archive→ARCHIVED
    # F5: Step5 复盘——五骑士归因 classify_decay_knight + record_methodology 沉淀
    # O1: RetirementWorkflowResult（state/sustained_alerts/diagnosis/decision/executed_actions/knight/escalation_required）
    # [/ALGO_FLOW]
