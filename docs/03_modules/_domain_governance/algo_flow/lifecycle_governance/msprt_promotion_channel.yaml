# ALGO_FLOW 外部真源——msprt_promotion_channel（_domain_governance 域，真源 src/zephyr/governance/lifecycle_governance/msprt_promotion_channel.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.lifecycle_governance.msprt_promotion_channel
source_of_truth: src/zephyr/governance/lifecycle_governance/msprt_promotion_channel.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: alpha 参数
    #   fields: 参数 alpha（无注解）
    #   code: msprt_promotion_channel.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: tau 参数
    #   fields: 参数 tau（无注解）
    #   code: msprt_promotion_channel.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: historical_effects 参数
    #   fields: 参数 historical_effects（无注解）
    #   code: msprt_promotion_channel.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: window_size 参数
    #   fields: 参数 window_size（无注解）
    #   code: msprt_promotion_channel.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① PromotionChannelManager
    #   name_en: PromotionChannelManager
    #   intro: mSPRT 晋升通道管理器（多通道登记 + 可调度投喂入口）。
    #   desc: mSPRT 晋升通道管理器（多通道登记 + 可调度投喂入口）。 用法：调度器（64 号 §6.4 基座）对每个 champion/challenger 配对 register 一…；公共方法（定义序）: registe…
    #   inputs: alpha tau historical_effects window_size
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: PromotionChannelManager
    #   downstream: zephyr.governance.lifecycle_governance.factor_promotion_wiring（因子晋升场景）; 调用方（BM-…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
