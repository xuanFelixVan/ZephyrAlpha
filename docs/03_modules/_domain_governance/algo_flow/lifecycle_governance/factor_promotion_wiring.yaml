# ALGO_FLOW 外部真源——factor_promotion_wiring（_domain_governance 域，真源 src/zephyr/governance/lifecycle_governance/factor_promotion_wiring.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.lifecycle_governance.factor_promotion_wiring
source_of_truth: src/zephyr/governance/lifecycle_governance/factor_promotion_wiring.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: verdict 参数
    #   fields: 参数 verdict，类型注解 PromotionVerdict
    #   code: factor_promotion_wiring.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: fsm 参数
    #   fields: 参数 fsm，类型注解 StateMachine[str]
    #   code: factor_promotion_wiring.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① apply_promotion_verdict_to_factor_fsm
    #   name_en: apply_promotion_verdict_to_factor_fsm
    #   intro: 把 mSPRT 通道裁决应用到因子生命周期 FSM。
    #   desc: 把 mSPRT 通道裁决应用到因子生命周期 FSM。 Args: verdict: 通道裁决快照（仅终局态产生副作用）。 fsm: challenger 因子的生命周期状态机实例…；源码 L86-L124
    #   inputs: verdict fsm
    #   outputs: str | None
    # 层: 输出
    # - id: O1
    #   name_zh: str | None
    #   name_en: str | None
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 调用方（因子灰度晋升评审：champion-challenger 并行期结束后应用裁决）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
