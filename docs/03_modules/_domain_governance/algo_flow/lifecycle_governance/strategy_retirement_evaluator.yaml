# ALGO_FLOW 外部真源——strategy_retirement_evaluator（_domain_governance 域，真源 src/zephyr/governance/lifecycle_governance/strategy_retirement_evaluator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.lifecycle_governance.strategy_retirement_evaluator
source_of_truth: src/zephyr/governance/lifecycle_governance/strategy_retirement_evaluator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: publisher 参数
    #   fields: 参数 publisher（无注解）
    #   code: strategy_retirement_evaluator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: registry_path 参数
    #   fields: 参数 registry_path（无注解）
    #   code: strategy_retirement_evaluator.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: underperformance_window 参数
    #   fields: 参数 underperformance_window（无注解）
    #   code: strategy_retirement_evaluator.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: sharpe_window 参数
    #   fields: 参数 sharpe_window（无注解）
    #   code: strategy_retirement_evaluator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① StrategyRetirementEvaluator
    #   name_en: StrategyRetirementEvaluator
    #   intro: 策略退役评估器（双判据 + 评审制，55 号 §3.5）。
    #   desc: 策略退役评估器（双判据 + 评审制，55 号 §3.5）。 用法：周/月复盘时由调用方（或 ReviewOrchestrator 月复盘链路）对每个上线策略 调 evaluate…；公共方法（定义序）: thresho…
    #   inputs: publisher registry_path underperformance_window sharpe_window
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: StrategyRetirementEvaluator
    #   downstream: MOD-RPT-009(ReviewOrchestrator,月复盘退役判据扫描); 调用方(周/月退役评审)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
