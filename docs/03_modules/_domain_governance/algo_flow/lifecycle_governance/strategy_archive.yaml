# ALGO_FLOW 外部真源——strategy_archive（_domain_governance 域，真源 src/zephyr/governance/lifecycle_governance/strategy_archive.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.lifecycle_governance.strategy_archive
source_of_truth: src/zephyr/governance/lifecycle_governance/strategy_archive.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: strategy_id 参数
    #   fields: 参数 strategy_id，类型注解 str
    #   code: strategy_archive.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: artifacts 参数
    #   fields: 参数 artifacts，类型注解 StrategyArchiveArtifacts
    #   code: strategy_archive.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: archive_root 参数
    #   fields: 参数 archive_root（无注解）
    #   code: strategy_archive.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: archived_at 参数
    #   fields: 参数 archived_at（无注解）
    #   code: strategy_archive.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① archive_strategy
    #   name_en: archive_strategy
    #   intro: 把退役策略产物归档到 ``<archive_root>/<strategy_id>/``。
    #   desc: 把退役策略产物归档到 ``<archive_root>/<strategy_id>/``。 幂等纪律：归档只增不改——目录已存在（含 manifest.json）→ Strate…；源码 L156-L211
    #   inputs: strategy_id artifacts archive_root archived_at
    #   outputs: Path
    # - id: A2
    #   name_zh: ② retrieve_strategy_archive
    #   name_en: retrieve_strategy_archive
    #   intro: 取回退役策略归档（只读）：``{"manifest": dict, "files": {name: Path}}``。
    #   desc: 取回退役策略归档（只读）：``{"manifest": dict, "files": {name: Path}}``。；源码 L214-L231
    #   inputs: strategy_id archive_root
    #   outputs: dict
    # - id: A3
    #   name_zh: ③ list_archived_strategies
    #   name_en: list_archived_strategies
    #   intro: 列出全部已归档策略 ID（含 manifest.json 的子目录；根目录不存在 → 空列表）。
    #   desc: 列出全部已归档策略 ID（含 manifest.json 的子目录；根目录不存在 → 空列表）。；源码 L234-L239
    #   inputs: archive_root
    #   outputs: list[str]
    #   （注：A3 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: Path
    #   name_en: Path
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.governance.lifecycle_governance.retirement_workflow (archive 端口); 调用方（退役…
    # - id: O2
    #   name_zh: dict
    #   name_en: dict
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: zephyr.governance.lifecycle_governance.retirement_workflow (archive 端口); 调用方（退役…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> A3
    # A3 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: strategy_id + StrategyArchiveArtifacts(training_run_id/decay_knight/reason/params_snapshot/pnl_curve) + archive_root(默认 MAIN_REPO_ROOT/strategy_archive)
    # F1: archive_strategy(建 strategy_archive/<sid>/ → manifest.json + params_snapshot.json + pnl_curve.csv；重复归档拒)
    # F2: retrieve_strategy_archive(读 manifest.json + 产物文件清单；不存在→StrategyArchiveError)
    # F3: list_archived_strategies(扫描 archive_root 下含 manifest.json 的子目录)
    # O1: archive_strategy→归档目录 Path；retrieve→{manifest, files}
    # [/ALGO_FLOW]
