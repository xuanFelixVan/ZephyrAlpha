# ALGO_FLOW 外部真源——fix_result_prioritizer（_domain_governance 域，真源 src/zephyr/governance/semantic_audit/fix_result_prioritizer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.semantic_audit.fix_result_prioritizer
source_of_truth: src/zephyr/governance/semantic_audit/fix_result_prioritizer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: weights 参数
    #   fields: 参数 weights（无注解）
    #   code: fix_result_prioritizer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FixPrioritizer
    #   name_en: FixPrioritizer
    #   intro: 修复优先级排序器 — 蓝图 §3.
    #   desc: 修复优先级排序器 — 蓝图 §3.1 Stage 8. 四维排序规则（降序）: 1. severity: RED > YELLOW > INFO 2. impact: affec…；公共方法（定义序）: priorit…
    #   inputs: weights
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: FixPrioritizer
    #   downstream: blast_radius.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
