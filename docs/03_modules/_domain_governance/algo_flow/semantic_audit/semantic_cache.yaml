# ALGO_FLOW 外部真源——semantic_cache（_domain_governance 域，真源 src/zephyr/governance/semantic_audit/semantic_cache.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.semantic_audit.semantic_cache
source_of_truth: src/zephyr/governance/semantic_audit/semantic_cache.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_entries 参数
    #   fields: 参数 max_entries（无注解）
    #   code: semantic_cache.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: ttl 参数
    #   fields: 参数 ttl（无注解）
    #   code: semantic_cache.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SemanticCache
    #   name_en: SemanticCache
    #   intro: class SemanticCache 源码 L70-L152
    #   desc: 公共方法（定义序）: get, put, get_or_compute, hit_rate, total_saved, size, clear；源码 L70-L152
    #   inputs: max_entries ttl
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: SemanticCache
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
