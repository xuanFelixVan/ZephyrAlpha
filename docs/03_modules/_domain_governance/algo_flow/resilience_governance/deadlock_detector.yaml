# ALGO_FLOW 外部真源——deadlock_detector（_domain_governance 域，真源 src/zephyr/governance/resilience_governance/deadlock_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.resilience_governance.deadlock_detector
source_of_truth: src/zephyr/governance/resilience_governance/deadlock_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: deadlock_detector.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① DeadlockDetector
    #   name_en: DeadlockDetector
    #   intro: class DeadlockDetector 源码 L55-L186
    #   desc: 公共方法（定义序）: wait_graph, locks, lock_timestamps, preemption_order, add_edge, detect_cycle, break_deadlock, try_…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: DeadlockDetector
    #   downstream: zephyr.governance.intelligence_governance.delegation_engine;zephyr.infrastructu…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
