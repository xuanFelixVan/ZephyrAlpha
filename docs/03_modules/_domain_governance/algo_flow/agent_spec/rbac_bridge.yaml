# ALGO_FLOW 外部真源——rbac_bridge（_domain_governance 域，真源 src/zephyr/governance/agent_spec/rbac_bridge.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.agent_spec.rbac_bridge
source_of_truth: src/zephyr/governance/agent_spec/rbac_bridge.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: rbac_bridge.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① BudgetRBACBridge
    #   name_en: BudgetRBACBridge
    #   intro: 预算消耗->RBAC权限降级.
    #   desc: 预算消耗->RBAC权限降级.；公共方法（定义序）: evaluate_budget, check_budget；源码 L87-L105
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② EscalationRBACBridge
    #   name_en: EscalationRBACBridge
    #   intro: 升级事件->RBAC权限提升 + Pipeline 前置 RBAC 检查.
    #   desc: 升级事件->RBAC权限提升 + Pipeline 前置 RBAC 检查.；公共方法（定义序）: guard, request_escalation, pre_execute_check；源码 L118-L191
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: BudgetRBACBridge, EscalationRBACBridge
    #   downstream: zephyr.infrastructure.budget_enforcement
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
