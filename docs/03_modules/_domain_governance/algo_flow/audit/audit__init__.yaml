# ALGO_FLOW 外部真源——__init__（_domain_governance 域，真源 src/zephyr/governance/audit/__init__.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.audit.__init__
source_of_truth: src/zephyr/governance/audit/__init__.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 包内子模块 Python 模块
    #   fields: reconciliation_registry 与 snapshot_manager 两个子模块
    #   code: __all__ L2
    # 层: 算法
    # - id: A1
    #   name_zh: ① 包导出声明
    #   name_en: __all__
    #   intro: 声明 governance.audit 包对外导出的子模块清单
    #   desc: 自动生成 package init，仅列 __all__ = ['reconciliation_registry', 'snapshot_manager', 'default_attribution_engine']，无运行逻辑
    #   inputs: I1
    #   outputs: 包命名空间导出表
    # 层: 输出
    # - id: O1
    #   name_zh: governance.audit 包命名空间
    #   name_en: zephyr.governance.audit
    #   intro: 供外部按包路径导入 audit 子模块
    #   downstream: 无下游/内部使用（包内子模块与导入方）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
