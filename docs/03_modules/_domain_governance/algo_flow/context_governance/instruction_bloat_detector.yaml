# ALGO_FLOW 外部真源——instruction_bloat_detector（_domain_governance 域，真源 src/zephyr/governance/context_governance/instruction_bloat_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.context_governance.instruction_bloat_detector
source_of_truth: src/zephyr/governance/context_governance/instruction_bloat_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: targets 参数
    #   fields: 参数 targets（无注解）
    #   code: instruction_bloat_detector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: session_budget 参数
    #   fields: 参数 session_budget（无注解）
    #   code: instruction_bloat_detector.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: history_path 参数
    #   fields: 参数 history_path（无注解）
    #   code: instruction_bloat_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① InstructionBloatDetector
    #   name_en: InstructionBloatDetector
    #   intro: class InstructionBloatDetector 源码 L130-L290
    #   desc: 公共方法（定义序）: scan, suggest_compact, summary；源码 L130-L290
    #   inputs: targets session_budget history_path
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: InstructionBloatDetector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
