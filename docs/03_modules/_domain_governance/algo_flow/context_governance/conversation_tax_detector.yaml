# ALGO_FLOW 外部真源——conversation_tax_detector（_domain_governance 域，真源 src/zephyr/governance/context_governance/conversation_tax_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.context_governance.conversation_tax_detector
source_of_truth: src/zephyr/governance/context_governance/conversation_tax_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: drift_window 参数
    #   fields: 参数 drift_window（无注解）
    #   code: conversation_tax_detector.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: decay_threshold 参数
    #   fields: 参数 decay_threshold（无注解）
    #   code: conversation_tax_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ConversationTaxDetector
    #   name_en: ConversationTaxDetector
    #   intro: class ConversationTaxDetector 源码 L69-L170
    #   desc: 公共方法（定义序）: record_reply, assess, last_assessment, reset；源码 L69-L170
    #   inputs: drift_window decay_threshold
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: ConversationTaxDetector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
