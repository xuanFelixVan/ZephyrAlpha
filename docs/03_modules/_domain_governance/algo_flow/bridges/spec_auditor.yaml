# ALGO_FLOW 外部真源——spec_auditor（_domain_governance 域，真源 src/zephyr/governance/bridges/spec_auditor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.bridges.spec_auditor
source_of_truth: src/zephyr/governance/bridges/spec_auditor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: capability 参数
    #   fields: 参数 capability，类型注解 AgentCapability
    #   code: spec_auditor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① record_agent_spec
    #   name_en: record_agent_spec
    #   intro: record_agent_spec(capability) 源码 L54-L63
    #   desc: 源码 L54-L63
    #   inputs: capability
    #   outputs: dict[str, Any]
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, Any]
    #   name_en: dict[str, Any]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
