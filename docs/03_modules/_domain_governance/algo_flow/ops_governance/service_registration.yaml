# ALGO_FLOW 外部真源——service_registration（_domain_governance 域，真源 src/zephyr/governance/ops_governance/service_registration.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.ops_governance.service_registration
source_of_truth: src/zephyr/governance/ops_governance/service_registration.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: service_registration.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① register_services
    #   name_en: register_services
    #   intro: 将 D-DATA 实现注册到 ServiceRegistry。
    #   desc: 将 D-DATA 实现注册到 ServiceRegistry。幂等，可重复调用。；源码 L63-L120
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: register_services
    #   downstream: zephyr.trading.boot_hooks ; zephyr.__init__ (eager registration)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
