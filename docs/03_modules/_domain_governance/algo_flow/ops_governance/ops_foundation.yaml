# ALGO_FLOW 外部真源——ops_foundation（_domain_governance 域，真源 src/zephyr/governance/ops_governance/ops_foundation.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.ops_governance.ops_foundation
source_of_truth: src/zephyr/governance/ops_governance/ops_foundation.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: last_good 参数
    #   fields: 参数 last_good，类型注解 str
    #   code: ops_foundation.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: current 参数
    #   fields: 参数 current，类型注解 str
    #   code: ops_foundation.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① verify_config
    #   name_en: verify_config
    #   intro: verify_config(last_good, current) 源码 L82-L83
    #   desc: 源码 L82-L83
    #   inputs: last_good current
    #   outputs: tuple[bool, str]
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: tuple[bool, str]
    #   name_en: tuple[bool, str]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: MOD-INF-020;MOD-INF-018;MOD-INF-027
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
