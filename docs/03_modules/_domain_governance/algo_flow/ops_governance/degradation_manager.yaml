# ALGO_FLOW 外部真源——degradation_manager（_domain_governance 域，真源 src/zephyr/governance/ops_governance/degradation_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.ops_governance.degradation_manager
source_of_truth: src/zephyr/governance/ops_governance/degradation_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: recovery_cooldown 参数
    #   fields: 参数 recovery_cooldown（无注解）
    #   code: degradation_manager.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: anti_spiral_limit 参数
    #   fields: 参数 anti_spiral_limit（无注解）
    #   code: degradation_manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DegradationState
    #   name_en: DegradationState
    #   intro: class DegradationState 源码 L91-L108
    #   desc: 公共方法（定义序）: is_degraded, can_advance, can_retreat；源码 L91-L108
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② DegradationManager
    #   name_en: DegradationManager
    #   intro: class DegradationManager 源码 L111-L302
    #   desc: 公共方法（定义序）: compute_target_tier, state, evaluate, manual_retreat, record_dependency_failure, circuit_breaker_o…
    #   inputs: recovery_cooldown anti_spiral_limit
    #   outputs: 返回值
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: DegradationState, DegradationManager
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> A2
    # A2 --> O1
