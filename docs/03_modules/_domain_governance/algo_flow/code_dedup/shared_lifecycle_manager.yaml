# ALGO_FLOW 外部真源——shared_lifecycle_manager（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/shared_lifecycle_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.shared_lifecycle_manager
source_of_truth: src/zephyr/gov_code_quality/code_dedup/shared_lifecycle_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: lifecycle_path 参数
    #   fields: 参数 lifecycle_path（无注解）
    #   code: shared_lifecycle_manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SharedLifecycleManager
    #   name_en: SharedLifecycleManager
    #   intro: 共享函数生命周期管理器.
    #   desc: 共享函数生命周期管理器.；公共方法（定义序）: register_active, transition, generate_migration, remove_from_shadow_manifest, get_gra…
    #   inputs: lifecycle_path
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: SharedLifecycleManager
    #   downstream: tests/governance/shared/test_shared_lifecycle_manager.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
