# ALGO_FLOW 外部真源——ssot_registrar（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/ssot_registrar.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.ssot_registrar
source_of_truth: src/zephyr/gov_code_quality/code_dedup/ssot_registrar.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: manifest_path 参数
    #   fields: 参数 manifest_path（无注解）
    #   code: ssot_registrar.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SSOTRegistrar
    #   name_en: SSOTRegistrar
    #   intro: 共享函数 SSoT 注册器.
    #   desc: 共享函数 SSoT 注册器.；公共方法（定义序）: register；源码 L54-L93
    #   inputs: manifest_path
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: SSOTRegistrar
    #   downstream: N/A (all consumers verified as phantom — stale references removed)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
