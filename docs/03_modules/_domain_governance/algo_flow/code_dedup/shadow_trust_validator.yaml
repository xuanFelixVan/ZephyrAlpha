# ALGO_FLOW 外部真源——shadow_trust_validator（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/shadow_trust_validator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.shadow_trust_validator
source_of_truth: src/zephyr/gov_code_quality/code_dedup/shadow_trust_validator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: shadow_trust_validator.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ShadowTrustValidator
    #   name_en: ShadowTrustValidator
    #   intro: 影子清单信任链验证.
    #   desc: 影子清单信任链验证.；公共方法（定义序）: validate_imports；源码 L51-L78
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: ShadowTrustValidator
    #   downstream: tests/governance/delegation/test_shadow_trust_validator.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
