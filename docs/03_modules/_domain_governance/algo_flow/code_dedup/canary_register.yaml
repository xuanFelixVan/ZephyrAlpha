# ALGO_FLOW 外部真源——canary_register（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/canary_register.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.canary_register
source_of_truth: src/zephyr/gov_code_quality/code_dedup/canary_register.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry_path 参数
    #   fields: 参数 registry_path（无注解）
    #   code: canary_register.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CanaryRegister
    #   name_en: CanaryRegister
    #   intro: 金丝雀函数注册表.
    #   desc: 金丝雀函数注册表.；公共方法（定义序）: canaries, register, check_staleness；源码 L54-L116
    #   inputs: registry_path
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: CanaryRegister
    #   downstream: tests/canary/test_canary_register.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
