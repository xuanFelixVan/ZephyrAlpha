# ALGO_FLOW 外部真源——health_monitor（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/health_monitor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.health_monitor
source_of_truth: src/zephyr/gov_code_quality/code_dedup/health_monitor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: health_monitor.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① HealthMonitor
    #   name_en: HealthMonitor
    #   intro: 多维代码健康仪表盘.
    #   desc: 多维代码健康仪表盘.；公共方法（定义序）: classify_dimension, compute_grade, compute；源码 L81-L224
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: HealthMonitor
    #   downstream: tests/governance/ops/test_health_monitor.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
