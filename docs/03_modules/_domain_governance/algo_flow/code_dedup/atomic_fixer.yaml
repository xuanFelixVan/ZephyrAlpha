# ALGO_FLOW 外部真源——atomic_fixer（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/atomic_fixer.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.atomic_fixer
source_of_truth: src/zephyr/gov_code_quality/code_dedup/atomic_fixer.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: project_root 参数
    #   fields: 参数 project_root（无注解）
    #   code: atomic_fixer.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AtomicFixer
    #   name_en: AtomicFixer
    #   intro: WAL 式原子性修复引擎.
    #   desc: WAL 式原子性修复引擎.；公共方法（定义序）: preflight, checkpoint, apply, recover, scan_and_recover_all；源码 L95-L261
    #   inputs: project_root
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: AtomicFixer
    #   downstream: N/A (all consumers verified as phantom — stale references removed)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
