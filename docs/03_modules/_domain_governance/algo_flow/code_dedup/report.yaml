# ALGO_FLOW 外部真源——report（_domain_governance 域，真源 src/zephyr/gov_code_quality/code_dedup/report.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.gov_code_quality.code_dedup.report
source_of_truth: src/zephyr/gov_code_quality/code_dedup/report.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: report.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ReportGenerator
    #   name_en: ReportGenerator
    #   intro: YAML/JSON 报告生成器.
    #   desc: YAML/JSON 报告生成器.；公共方法（定义序）: degradation_level, exit_code, generate, to_yaml_dict, to_json, set_degradation, s…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: ReportGenerator
    #   downstream: zephyr.gov_code_quality.code_dedup.cli; tests/governance/observability/test_rep…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
