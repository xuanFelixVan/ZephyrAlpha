# ALGO_FLOW 外部真源——observability_dashboard（_domain_governance 域，真源 src/zephyr/governance/observability_governance/observability_dashboard.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.observability_governance.observability_dashboard
source_of_truth: src/zephyr/governance/observability_governance/observability_dashboard.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 仪表板配置请求
    #   fields: 无入参（default 工厂方法）
    #   code: DashboardConfig.default (L49)
    # 层: 算法
    # - id: A1
    #   name_zh: 默认面板装配
    #   name_en: default_panel_assembly
    #   intro: 组装 system_health/cost/order_flow/model_drift 四面板的 SLI 与指标清单
    #   code: DashboardConfig.default (L49)
    # 层: 输出
    # - id: O1
    #   name_zh: 仪表板配置
    #   name_en: dashboard_config
    #   intro: DashboardConfig（panels + refresh_interval_seconds=10）
    #   downstream: 可观测性展示层
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> O1
