# ALGO_FLOW 外部真源——agent_debate（_domain_governance 域，真源 src/zephyr/governance/intelligence_governance/agent_debate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.intelligence_governance.agent_debate
source_of_truth: src/zephyr/governance/intelligence_governance/agent_debate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 双模型响应
    #   fields: model_a/model_b 名称与内容文本（+override_decision）
    #   code: AgentDebate.debate (L67) / adjudicate (L104)
    # 层: 算法
    # - id: A1
    #   name_zh: 响应哈希化
    #   name_en: response_hashing
    #   intro: sha256 截断生成 response_hash/content_hash，供共识比对
    #   code: ModelResponse.from_content (L43)
    # - id: A2
    #   name_zh: 辩论裁决
    #   name_en: debate_adjudication
    #   intro: content_hash 一致→AGREE 共识；不一致→OVERRIDE 要求人工裁决，轮次入历史
    #   code: debate (L67) / adjudicate (L104)
    # 层: 输出
    # - id: O1
    #   name_zh: 裁决结果
    #   name_en: debate_verdict
    #   intro: DebateVerdict + DebateRound 历史（agreement_rate 统计一致率）
    #   downstream: 智能治理调用方
    # [/ALGO_FLOW]
    # 边: I1 --> A1 ; A1 --> A2 ; A2 --> O1
