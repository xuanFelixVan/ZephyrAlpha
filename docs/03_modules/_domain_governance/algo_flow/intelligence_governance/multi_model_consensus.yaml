# ALGO_FLOW 外部真源——multi_model_consensus（_domain_governance 域，真源 src/zephyr/governance/intelligence_governance/multi_model_consensus.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.intelligence_governance.multi_model_consensus
source_of_truth: src/zephyr/governance/intelligence_governance/multi_model_consensus.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: reason 参数
    #   fields: 参数 reason，类型注解 str
    #   code: multi_model_consensus.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① escalate_to_owner
    #   name_en: escalate_to_owner
    #   intro: escalate_to_owner(reason) 源码 L66-L67
    #   desc: 源码 L66-L67
    #   inputs: reason
    #   outputs: str
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: str
    #   name_en: str
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
