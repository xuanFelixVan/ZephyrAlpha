# ALGO_FLOW 外部真源——data_lifecycle（_domain_governance 域，真源 src/zephyr/governance/data_governance/data_lifecycle.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.data_governance.data_lifecycle
source_of_truth: src/zephyr/governance/data_governance/data_lifecycle.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: data_lifecycle.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① forget_pii
    #   name_en: forget_pii
    #   intro: forget_pii() 源码 L65-L66
    #   desc: 源码 L65-L66
    #   inputs: 无参数
    #   outputs: dict[str, str]
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: dict[str, str]
    #   name_en: dict[str, str]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
