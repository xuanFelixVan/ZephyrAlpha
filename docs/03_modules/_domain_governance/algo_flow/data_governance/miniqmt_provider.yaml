# ALGO_FLOW 外部真源——miniqmt_provider（_domain_governance 域，真源 src/zephyr/governance/data_governance/miniqmt_provider.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.governance.data_governance.miniqmt_provider
source_of_truth: src/zephyr/governance/data_governance/miniqmt_provider.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: path 参数
    #   fields: 参数 path（无注解）
    #   code: miniqmt_provider.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: session_id 参数
    #   fields: 参数 session_id（无注解）
    #   code: miniqmt_provider.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MiniQmtQuoteProvider
    #   name_en: MiniQmtQuoteProvider
    #   intro: MiniQMT 实盘行情 Provider——对接 xtdata，提供 Tick + 5档盘口
    #   desc: MiniQMT 实盘行情 Provider——对接 xtdata，提供 Tick + 5档盘口 对接国金证券 MiniQMT 终端（Level-1 五档盘口），支持: - 历史…；公共方法（定义序）: fetch_hi…
    #   inputs: path session_id
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: MiniQmtQuoteProvider
    #   downstream: zephyr.backtest.core.data_handler; zephyr.backtest.core.tick_replay; zephyr.ex_…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
