# ALGO_FLOW 外部真源——data_masking_engine（_domain_data_security 域，真源 src/zephyr/data_security/data_masking_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data_security.data_masking_engine
source_of_truth: src/zephyr/data_security/data_masking_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: key 参数
    #   fields: 参数 key（无注解）
    #   code: data_masking_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: cipher 参数
    #   fields: 参数 cipher（无注解）
    #   code: data_masking_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: rng 参数
    #   fields: 参数 rng（无注解）
    #   code: data_masking_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① DataMaskingEngine
    #   name_en: DataMaskingEngine
    #   intro: 脱敏引擎（FPE + 角色动态脱敏 + 拉普拉斯差分隐私噪声）。
    #   desc: 脱敏引擎（FPE + 角色动态脱敏 + 拉普拉斯差分隐私噪声）。；公共方法（定义序）: fpe_encrypt, fpe_decrypt, register_policy, policy_of, mask_field…
    #   inputs: key cipher rng
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: DataMaskingEngine
    #   downstream: 运行时装配批（身份证/账号出库加密装配点 / 按角色查询出参动态脱敏 / 统计输出差分隐私）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
