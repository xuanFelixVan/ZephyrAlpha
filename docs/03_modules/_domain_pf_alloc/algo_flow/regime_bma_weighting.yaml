# ALGO_FLOW 外部真源——regime_bma_weighting（_domain_pf_alloc 域，真源 src/zephyr/pf_alloc/core/regime_bma_weighting.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.pf_alloc.core.regime_bma_weighting
source_of_truth: src/zephyr/pf_alloc/core/regime_bma_weighting.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: window 参数
    #   fields: 参数 window（无注解）
    #   code: regime_bma_weighting.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: half_life 参数
    #   fields: 参数 half_life（无注解）
    #   code: regime_bma_weighting.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: min_samples 参数
    #   fields: 参数 min_samples（无注解）
    #   code: regime_bma_weighting.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: metric 参数
    #   fields: 参数 metric（无注解）
    #   code: regime_bma_weighting.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① RegimeBmaWeighting
    #   name_en: RegimeBmaWeighting
    #   intro: 体制条件 BMA 信号权重估计器（纯内存确定性，审计/时钟注入）。
    #   desc: 体制条件 BMA 信号权重估计器（纯内存确定性，审计/时钟注入）。；公共方法（定义序）: update, current_regime, current_weights；源码 L160-L282
    #   inputs: window half_life min_samples metric audit_sink clock
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: RegimeBmaWeighting
    #   downstream: 运行时装配批（signal_synthesis_combiner 权重源 / 体制切换审计落库）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
