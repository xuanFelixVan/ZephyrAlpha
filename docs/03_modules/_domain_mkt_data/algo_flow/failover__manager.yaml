# ALGO_FLOW 外部真源——manager（_domain_mkt_data 域，真源 src/zephyr/market_data/failover/manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.market_data.failover.manager
source_of_truth: src/zephyr/market_data/failover/manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry 参数
    #   fields: 参数 registry（无注解）
    #   code: manager.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FailoverManager
    #   name_en: FailoverManager
    #   intro: 故障切换管理器——主备切换 + 自动恢复。
    #   desc: 故障切换管理器——主备切换 + 自动恢复。 基于 VendorRegistry 管理多 vendor 的主备切换: - 主源 health_check 失败 → 切到下一个可用备…；公共方法（定义序）: get_act…
    #   inputs: registry config
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: FailoverManager
    #   downstream: D_EX_SOR
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
