# ALGO_FLOW 外部真源——base（_domain_mkt_data 域，真源 src/zephyr/market_data/connectors/base.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.market_data.connectors.base
source_of_truth: src/zephyr/market_data/connectors/base.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: base.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① MarketDataConnector
    #   name_en: MarketDataConnector
    #   intro: 行情数据连接器抽象基类——扩展 MarketDataVendor。
    #   desc: 行情数据连接器抽象基类——扩展 MarketDataVendor。 在 vendor_base 的数据获取接口之上增加: - 连接生命周期: connect/disconnect…；公共方法（定义序）: connect…
    #   inputs: config
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: MarketDataConnector
    #   downstream: zephyr.market_data.connectors.manager; tests/market_data/connectors/test_connector_base.py（D_EX_SOR 装配待排期）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
