# ALGO_FLOW 外部真源——reranker（_domain_intelligence 域，真源 src/zephyr/intelligence/model_evaluation/reranker.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.model_evaluation.reranker
source_of_truth: src/zephyr/intelligence/model_evaluation/reranker.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: query 参数
    #   fields: 参数 query，类型注解 str
    #   code: reranker.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: documents 参数
    #   fields: 参数 documents，类型注解 Sequence[str]
    #   code: reranker.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: model_name 参数
    #   fields: 参数 model_name（无注解）
    #   code: reranker.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: top_k 参数
    #   fields: 参数 top_k（无注解）
    #   code: reranker.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Reranker
    #   name_en: Reranker
    #   intro: class Reranker 源码 L118-L188
    #   desc: 公共方法（定义序）: available, rerank；源码 L118-L188
    #   inputs: model_name top_k score_threshold
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② rerank_batch
    #   name_en: rerank_batch
    #   intro: rerank_batch(query, documents, model_name, top_k, score_thr…
    #   desc: 源码 L206-L216
    #   inputs: query documents model_name top_k score_threshold metadatas
    #   outputs: list[RerankedHit]
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: list[RerankedHit]
    #   name_en: list[RerankedHit]
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
