# ALGO_FLOW 外部真源——llm_research_agent（_domain_intelligence 域，真源 src/zephyr/intelligence/llm_research_agent.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.llm_research_agent
source_of_truth: src/zephyr/intelligence/llm_research_agent.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: tools 参数
    #   fields: 参数 tools（无注解）
    #   code: llm_research_agent.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: planner 参数
    #   fields: 参数 planner（无注解）
    #   code: llm_research_agent.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: thinker 参数
    #   fields: 参数 thinker（无注解）
    #   code: llm_research_agent.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: reflector 参数
    #   fields: 参数 reflector（无注解）
    #   code: llm_research_agent.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LlmResearchAgent
    #   name_en: LlmResearchAgent
    #   intro: LLM 研究助手（规划 + 白名单工具 + ReAct 循环 + 事实回查）。
    #   desc: LLM 研究助手（规划 + 白名单工具 + ReAct 循环 + 事实回查）。；公共方法（定义序）: plan, invoke_tool, run, plans, reports；源码 L164-L343
    #   inputs: tools planner thinker reflector fact_checker kb_writer clock max_roun…
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: LlmResearchAgent
    #   downstream: 运行时装配批（本地模型优先绑定规划/思考/反思回调 / 检索计算数据库工具白名单注册 / KB 写库与事实回查装配）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
