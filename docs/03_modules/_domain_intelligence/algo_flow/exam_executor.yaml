# ALGO_FLOW 外部真源——exam_executor（_domain_intelligence 域，真源 src/zephyr/intelligence/model_profiling/exam_executor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.model_profiling.exam_executor
source_of_truth: src/zephyr/intelligence/model_profiling/exam_executor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: exam_executor.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① ExamExecutor
    #   name_en: ExamExecutor
    #   intro: 执行式评测——运行模型生成代码 + 测试断言，沙箱隔离。
    #   desc: 执行式评测——运行模型生成代码 + 测试断言，沙箱隔离。 参考 HumanEval pass@1: pass_rate = 通过测试数 / 总测试数。 语法错误/超时/异常 ->…；公共方法（定义序）: execute…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: ExamExecutor
    #   downstream: zephyr.intelligence.model_profiling.exam_orchestrator
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
