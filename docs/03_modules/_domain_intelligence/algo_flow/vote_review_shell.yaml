# ALGO_FLOW 外部真源——vote_review_shell（_domain_intelligence 域，真源 src/zephyr/intelligence/reflexion/vote_review_shell.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.reflexion.vote_review_shell
source_of_truth: src/zephyr/intelligence/reflexion/vote_review_shell.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: candidates_dir 参数
    #   fields: 参数 candidates_dir，类型注解 Path
    #   code: vote_review_shell.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: output_path 参数
    #   fields: 参数 output_path，类型注解 Path
    #   code: vote_review_shell.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: quorum 参数
    #   fields: 参数 quorum（无注解）
    #   code: vote_review_shell.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: engine 参数
    #   fields: 参数 engine（无注解）
    #   code: vote_review_shell.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① run_review
    #   name_en: run_review
    #   intro: 候选收集→A2AVoting 逐候选计票→最优落盘 selected/→裁决报告 JSON（人手动调用）.
    #   desc: 候选收集→A2AVoting 逐候选计票→最优落盘 selected/→裁决报告 JSON（人手动调用）.；源码 L174-L228
    #   inputs: candidates_dir output_path quorum engine
    #   outputs: dict
    # - id: A2
    #   name_zh: ② main
    #   name_en: main
    #   intro: 人手动 CLI 入口（唯一触发路径；
    #   desc: 人手动 CLI 入口（唯一触发路径；无调度器/定时器/导入副作用）.；源码 L231-L247
    #   inputs: argv
    #   outputs: int
    #   （注：A2 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: dict
    #   name_en: dict
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 人手动触发 CLI(python -m zephyr.intelligence.reflexion.vote_review_shell)
    # - id: O2
    #   name_zh: int
    #   name_en: int
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 人手动触发 CLI(python -m zephyr.intelligence.reflexion.vote_review_shell)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
