# ALGO_FLOW 外部真源——profiler（_domain_intelligence 域，真源 src/zephyr/intelligence/model_profiling/profiler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.model_profiling.profiler
source_of_truth: src/zephyr/intelligence/model_profiling/profiler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: ollama_url 参数
    #   fields: 参数 ollama_url（无注解）
    #   code: profiler.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: timeout_per_case_s 参数
    #   fields: 参数 timeout_per_case_s（无注解）
    #   code: profiler.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: max_ollama_models 参数
    #   fields: 参数 max_ollama_models（无注解）
    #   code: profiler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ModelProfiler
    #   name_en: ModelProfiler
    #   intro: 核心性能分析引擎——对每个模型运行全维度 benchmark。
    #   desc: 核心性能分析引擎——对每个模型运行全维度 benchmark。；公共方法（定义序）: url, timeout, max_models, should_skip_model, score_output, percent…
    #   inputs: ollama_url timeout_per_case_s max_ollama_models
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: ModelProfiler
    #   downstream: MOD-INF-009;MOD-INF-036
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
