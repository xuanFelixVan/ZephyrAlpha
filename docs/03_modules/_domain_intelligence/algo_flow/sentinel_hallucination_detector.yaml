# ALGO_FLOW 外部真源——sentinel_hallucination_detector（_domain_intelligence 域，真源 src/zephyr/intelligence/sentinel_hallucination_detector.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.sentinel_hallucination_detector
source_of_truth: src/zephyr/intelligence/sentinel_hallucination_detector.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: detector 参数
    #   fields: 参数 detector（无注解）
    #   code: sentinel_hallucination_detector.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① SentinelHallucinationDetector
    #   name_en: SentinelHallucinationDetector
    #   intro: CoVe 幻觉检测引擎的 intelligence 域哨兵封装（委托模式）。
    #   desc: CoVe 幻觉检测引擎的 intelligence 域哨兵封装（委托模式）。；公共方法（定义序）: detector, detect, audit_trail, verify_audit_chain, stats；源码…
    #   inputs: detector
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: SentinelHallucinationDetector
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
