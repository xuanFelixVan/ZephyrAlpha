# ALGO_FLOW 外部真源——preflect_store（_domain_intelligence 域，真源 src/zephyr/intelligence/reflexion/preflect_store.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.reflexion.preflect_store
source_of_truth: src/zephyr/intelligence/reflexion/preflect_store.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: root 参数
    #   fields: 参数 root（无注解）
    #   code: preflect_store.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① FailurePatternEntry
    #   name_en: FailurePatternEntry
    #   intro: 失败模式条目（§3.
    #   desc: 失败模式条目（§3.3 四要素冻结：模式/触发条件/规避建议/来源反思 ID）.；公共方法（定义序）: to_dict, from_dict；源码 L103-L194
    #   inputs: 无参数
    #   outputs: 返回值
    # - id: A2
    #   name_zh: ② PreFlectStore
    #   name_en: PreFlectStore
    #   intro: 失败模式库：落盘 root/preflect_patterns.
    #   desc: 失败模式库：落盘 root/preflect_patterns.json，可读回.；公共方法（定义序）: path, add, ingest_reflection, get, edit, retrieve, build…
    #   inputs: root
    #   outputs: 返回值
    #   （注：A2 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: FailurePatternEntry, PreFlectStore
    #   downstream: tests/intelligence/test_preflect_store.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> A2
    # A2 --> O1
