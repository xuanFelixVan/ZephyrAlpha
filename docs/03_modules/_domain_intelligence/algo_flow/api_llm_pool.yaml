# ALGO_FLOW 外部真源——api_llm_pool（_domain_intelligence 域，真源 src/zephyr/intelligence/api_llm_pool.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.api_llm_pool
source_of_truth: src/zephyr/intelligence/api_llm_pool.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: api_llm_pool.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: usage_sink 参数
    #   fields: 参数 usage_sink（无注解）
    #   code: api_llm_pool.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ApiLlmPool
    #   name_en: ApiLlmPool
    #   intro: API LLM 池：注册 + 计费台账 + 健康度调度（判定核心纯内存无 IO）。
    #   desc: API LLM 池：注册 + 计费台账 + 健康度调度（判定核心纯内存无 IO）。 Args: config: 池配置（不健康阈值/成本上限）。 usage_sink: 台账外发…；公共方法（定义序）: registe…
    #   inputs: config usage_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 9 个公共定义未列入（含 9 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（10 定义）
    #   name_en: public defs
    #   intro: ApiLlmPool
    #   downstream: 运行时装配批（provider 注册表装配 / usage_sink 接 cost_tracker / degrade_to_local 接本地池切换）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
