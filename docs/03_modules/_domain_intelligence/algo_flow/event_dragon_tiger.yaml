# ALGO_FLOW 外部真源——event_dragon_tiger（_domain_intelligence 域，真源 src/zephyr/intelligence/event_dragon_tiger.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.event_dragon_tiger
source_of_truth: src/zephyr/intelligence/event_dragon_tiger.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: data 参数
    #   fields: 参数 data，类型注解 DragonTigerData | None
    #   code: event_dragon_tiger.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① dragon_tiger_corroboration_modifier
    #   name_en: dragon_tiger_corroboration_modifier
    #   intro: 龙虎榜佐证修正因子（2026 失效校准）。
    #   desc: 龙虎榜佐证修正因子（2026 失效校准）。返回乘法因子 ∈ [0.7, 1.2]。 - 无数据 → 1.0（非龙虎榜标的不修正） - 净买率 ≥12% → 1.2（极端值硬阈值门…；源码 L108-L130
    #   inputs: data
    #   outputs: float
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: float
    #   name_en: float
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 事件驱动 sleeve（event_score 龙虎榜佐证乘法修正：event_score_final = event_score × modifier）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: DragonTigerData（net_buy_amount/total_turnover/total_buy/buyer_seats[type,buy_amount]）
    # F1: 净买率=net_buy/turnover 硬阈值门控（≥12%→1.2 否则→1.0）
    # F2: 量化席位过滤（≥3席：占比>30%→×0.7 hard；否则→×0.85 soft）
    # O1: modifier∈[0.7,1.2]（event_score 乘法修正因子）
    # [/ALGO_FLOW]
