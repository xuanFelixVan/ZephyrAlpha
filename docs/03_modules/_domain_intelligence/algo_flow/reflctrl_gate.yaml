# ALGO_FLOW 外部真源——reflctrl_gate（_domain_intelligence 域，真源 src/zephyr/intelligence/reflexion/reflctrl_gate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.intelligence.reflexion.reflctrl_gate
source_of_truth: src/zephyr/intelligence/reflexion/reflctrl_gate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: config 参数
    #   fields: 参数 config（无注解）
    #   code: reflctrl_gate.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: stats_root 参数
    #   fields: 参数 stats_root（无注解）
    #   code: reflctrl_gate.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ReflCtrlGate
    #   name_en: ReflCtrlGate
    #   intro: ReflCtrl 频率闸门：显式规则集裁决 + 全量留痕 + token 统计.
    #   desc: ReflCtrl 频率闸门：显式规则集裁决 + 全量留痕 + token 统计.；公共方法（定义序）: config, decide, total_estimated_tokens；源码 L171-L329
    #   inputs: config stats_root
    #   outputs: 返回值
    #   （注：A1 之后另有 3 个公共定义未列入（含 3 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（4 定义）
    #   name_en: public defs
    #   intro: ReflCtrlGate
    #   downstream: tests/intelligence/test_reflctrl_gate.py
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
