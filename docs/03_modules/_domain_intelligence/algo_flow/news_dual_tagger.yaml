# ALGO_FLOW 外部真源——news_dual_tagger（_domain_intelligence 域，真源 src/zephyr/nlp/news_dual_tagger.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.nlp.news_dual_tagger
source_of_truth: src/zephyr/nlp/news_dual_tagger.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1 新闻 list[NewsTagInput]（news_id/title/content/symbols/actual_value）
    # - id: I2 日历规则组 list[EventKeywordRule]（事件类型+关键词+提前披露标记）
    # - id: I3 资金痕迹 dict[symbol → 净流入]（注入位）
    # - id: I4 一致预期锚 dict[symbol → ConsensusAnchor]（analyst_forecast 或注入位）
    # 层: 特征
    # - id: F1 日历关键词命中
    # - id: F2 资金痕迹阈值判定
    # - id: F3 预期差 gap_pct
    # 层: 算法
    # - id: A1 双标签封闭集合判定（可预测性四态 × 预期差四态）
    # 层: 输出
    # - id: O1 NewsDualTagResult（items 逐条双标签 + counts 计数 + 降级留痕）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1,I2 --> F1
    # I1,I3 --> F2
    # I1,I4 --> F3
    # F1,F2,F3 --> A1
    # A1 --> O1
