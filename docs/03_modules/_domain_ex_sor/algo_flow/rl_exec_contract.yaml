# ALGO_FLOW 外部真源——rl_exec_contract（_domain_ex_sor 域，真源 src/zephyr/ex_sor/core/rl_exec_contract.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.ex_sor.core.rl_exec_contract
source_of_truth: src/zephyr/ex_sor/core/rl_exec_contract.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 母单与硬约束参数
    #   fields: symbol/side/total_quantity/slice_count/pov_limit/forbid_market/offset_levels/prev_close/arrival_price
    #   code: RlExecContract L61
    # 层: 算法
    # - id: A1
    #   name_zh: ① 冻结契约载体
    #   name_en: frozen_dataclass
    #   intro: 纯值对象无行为，字段即契约；默认值复用 MatchingConfig 撮合口径
    #   desc: frozen=True 实例化后不可变；price_limit_pct=0.10/lot_size=100 取自 MatchingConfig 默认
    #   inputs: I1
    #   outputs: 不可变契约实例
    #   invariant: frozen 契约实例化后不可变
    # 层: 输出
    # - id: O1
    #   name_zh: 执行约束契约
    #   name_en: RlExecContract
    #   intro: 被硬边界层（裁剪/拒绝判定）与环境（回合推进）共同消费
    #   downstream: zephyr.ex_sor.core.rl_exec_boundary ; zephyr.ex_sor.core.rl_exec_env
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
