# ALGO_FLOW 外部真源——cold_data_archive_manager（_domain_data_eng 域，真源 src/zephyr/data_eng/cold_data_archive_manager.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data_eng.cold_data_archive_manager
source_of_truth: src/zephyr/data_eng/cold_data_archive_manager.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: index_conn 参数
    #   fields: 参数 index_conn（无注解）
    #   code: cold_data_archive_manager.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: cold_data_archive_manager.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: archiver 参数
    #   fields: 参数 archiver（无注解）
    #   code: cold_data_archive_manager.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: purge_executor 参数
    #   fields: 参数 purge_executor（无注解）
    #   code: cold_data_archive_manager.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ColdDataArchiveManager
    #   name_en: ColdDataArchiveManager
    #   intro: 冷数据归档编排件（计划 + 索引 + 清理裁决 + 只读检索 + 周期计划）。
    #   desc: 冷数据归档编排件（计划 + 索引 + 清理裁决 + 只读检索 + 周期计划）。；公共方法（定义序）: plan_archive, run_archive, register_retention, plan_purge…
    #   inputs: index_conn clock archiver purge_executor alert_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: ColdDataArchiveManager
    #   downstream: 运行时装配批（归档调度挂 auto_archive 周期计划 / 归档索引接 SQLite / 清理接存储执行器）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
