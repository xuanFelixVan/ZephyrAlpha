# ALGO_FLOW 外部真源——expectation_governance（_domain_data_eng 域，真源 src/zephyr/data_eng/expectation_governance.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.data_eng.expectation_governance
source_of_truth: src/zephyr/data_eng/expectation_governance.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: archive_path 参数
    #   fields: 参数 archive_path（无注解）
    #   code: expectation_governance.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: now_fn 参数
    #   fields: 参数 now_fn（无注解）
    #   code: expectation_governance.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ExpectationGovernance
    #   name_en: ExpectationGovernance
    #   intro: 期望套件验证器 + 三档质量门控。
    #   desc: 期望套件验证器 + 三档质量门控。；公共方法（定义序）: load_suite, suite_from_ctr001, validate；源码 L142-L325
    #   inputs: archive_path now_fn
    #   outputs: 返回值
    #   （注：A1 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（5 定义）
    #   name_en: public defs
    #   intro: ExpectationGovernance
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
