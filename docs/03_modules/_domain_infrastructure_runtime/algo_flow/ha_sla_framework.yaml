# ALGO_FLOW 外部真源——ha_sla_framework（_domain_infrastructure_runtime 域，真源 src/zephyr/infra_runtime/ha_sla_framework.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infra_runtime.ha_sla_framework
source_of_truth: src/zephyr/infra_runtime/ha_sla_framework.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: ha_sla_framework.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: escalation_sink 参数
    #   fields: 参数 escalation_sink（无注解）
    #   code: ha_sla_framework.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① HaSlaFramework
    #   name_en: HaSlaFramework
    #   intro: 单机 HA/SLA 框架（SLA 注册表 + 探针编排 + 自动重启编排）。
    #   desc: 单机 HA/SLA 框架（SLA 注册表 + 探针编排 + 自动重启编排）。；公共方法（定义序）: register_sla, register_probe, bind_restart, run_probe, due_…
    #   inputs: clock escalation_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: HaSlaFramework
    #   downstream: 运行时装配批（单机进程 SLA 登记/健康探针编排/自动重启编排）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
