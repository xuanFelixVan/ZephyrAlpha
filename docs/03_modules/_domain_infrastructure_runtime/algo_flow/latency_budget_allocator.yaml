# ALGO_FLOW 外部真源——latency_budget_allocator（_domain_infrastructure_runtime 域，真源 src/zephyr/infra_runtime/latency_budget_allocator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infra_runtime.latency_budget_allocator
source_of_truth: src/zephyr/infra_runtime/latency_budget_allocator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: latency_budget_allocator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: latency_budget_allocator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① LatencyBudgetAllocator
    #   name_en: LatencyBudgetAllocator
    #   intro: 延迟预算分配件（分解登记 + 耗时上报 + 超支判定 + 消耗率报表）。
    #   desc: 延迟预算分配件（分解登记 + 耗时上报 + 超支判定 + 消耗率报表）。；公共方法（定义序）: allocate, table, record, report；源码 L135-L265
    #   inputs: clock alert_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 5 个公共定义未列入（含 5 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（6 定义）
    #   name_en: public defs
    #   intro: LatencyBudgetAllocator
    #   downstream: 运行时装配批（Hot/Warm 平面阶段预算登记 / 实际耗时上报 / 预算消耗率报表）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
