# ALGO_FLOW 外部真源——resource_scheduler（_domain_infrastructure_runtime 域，真源 src/zephyr/infra_runtime/resource_scheduler.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infra_runtime.resource_scheduler
source_of_truth: src/zephyr/infra_runtime/resource_scheduler.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: quotas 参数
    #   fields: 参数 quotas（无注解）
    #   code: resource_scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: resource_scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: executor 参数
    #   fields: 参数 executor（无注解）
    #   code: resource_scheduler.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: resource_scheduler.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ResourceScheduler
    #   name_en: ResourceScheduler
    #   intro: 三平面资源调度器（配额注册表 + 统一准入裁决 + executor 注入）。
    #   desc: 三平面资源调度器（配额注册表 + 统一准入裁决 + executor 注入）。；公共方法（定义序）: admit, plane_usage, applied_records；源码 L182-L339
    #   inputs: quotas clock executor alert_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: ResourceScheduler
    #   downstream: 运行时装配批（Hot/Warm/Cold 三平面进程资源准入统一入口）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
