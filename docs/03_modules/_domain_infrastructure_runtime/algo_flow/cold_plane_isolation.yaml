# ALGO_FLOW 外部真源——cold_plane_isolation（_domain_infrastructure_runtime 域，真源 src/zephyr/infra_runtime/cold_plane_isolation.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.infra_runtime.cold_plane_isolation
source_of_truth: src/zephyr/infra_runtime/cold_plane_isolation.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: clock 参数
    #   fields: 参数 clock（无注解）
    #   code: cold_plane_isolation.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: is_trading_hours 参数
    #   fields: 参数 is_trading_hours（无注解）
    #   code: cold_plane_isolation.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: alert_sink 参数
    #   fields: 参数 alert_sink（无注解）
    #   code: cold_plane_isolation.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① ColdPlaneIsolator
    #   name_en: ColdPlaneIsolator
    #   intro: Cold 平面隔离件（配额校验 + 通道门禁 + iFind 令牌桶 + 待激活队列）。
    #   desc: Cold 平面隔离件（配额校验 + 通道门禁 + iFind 令牌桶 + 待激活队列）。；公共方法（定义序）: declare_quota, open_channel, is_open, acquire_ifind,…
    #   inputs: clock is_trading_hours alert_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 6 个公共定义未列入（含 6 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（7 定义）
    #   name_en: public defs
    #   intro: ColdPlaneIsolator
    #   downstream: 运行时装配批（Cold 平面配额声明校验 / 通道白名单门禁 / 盘中产出盘后激活）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
