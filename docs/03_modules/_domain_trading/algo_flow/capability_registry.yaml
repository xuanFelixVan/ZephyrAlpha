# ALGO_FLOW 外部真源——capability_registry（_domain_trading 域，真源 src/zephyr/trading/capability_registry.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.capability_registry
source_of_truth: src/zephyr/trading/capability_registry.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: card_dir 参数
    #   fields: 参数 card_dir（无注解）
    #   code: capability_registry.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: cache_ttl_seconds 参数
    #   fields: 参数 cache_ttl_seconds（无注解）
    #   code: capability_registry.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① CapabilityRegistry
    #   name_en: CapabilityRegistry
    #   intro: 能力注册中心——解决'AI 不知道有这个功能'的问题。
    #   desc: 能力注册中心——解决'AI 不知道有这个功能'的问题。 对标: - Google A2A Agent Card: JSON 格式的能力自描述 - Anthropic MCP: t…；公共方法（定义序）: card_di…
    #   inputs: card_dir cache_ttl_seconds
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: CapabilityRegistry
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
