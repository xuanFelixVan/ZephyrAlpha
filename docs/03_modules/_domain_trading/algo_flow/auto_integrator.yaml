# ALGO_FLOW 外部真源——auto_integrator（_domain_trading 域，真源 src/zephyr/trading/auto_integrator.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.auto_integrator
source_of_truth: src/zephyr/trading/auto_integrator.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: registry 参数
    #   fields: 参数 registry（无注解）
    #   code: auto_integrator.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: max_daily_l3_activations 参数
    #   fields: 参数 max_daily_l3_activations（无注解）
    #   code: auto_integrator.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AutoIntegrator
    #   name_en: AutoIntegrator
    #   intro: 自动接入器——临时启动高级模型分析是否接入。
    #   desc: 自动接入器——临时启动高级模型分析是否接入。 借鉴: - Claude Code Self-Improving: 临时启动强推理分析 - K8s Admission Contro…；公共方法（定义序）: max_dai…
    #   inputs: registry max_daily_l3_activations
    #   outputs: 返回值
    #   （注：A1 之后另有 1 个公共定义未列入（含 1 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（2 定义）
    #   name_en: public defs
    #   intro: AutoIntegrator
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
