# ALGO_FLOW 外部真源——integration_registry（_domain_trading 域，真源 src/zephyr/trading/integration_registry.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.integration_registry
source_of_truth: src/zephyr/trading/integration_registry.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: integration_registry.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① IntegrationRegistry
    #   name_en: IntegrationRegistry
    #   intro: 集成注册表——AutoRuntime Core 与所有现有系统的连接点清单。
    #   desc: 集成注册表——AutoRuntime Core 与所有现有系统的连接点清单。；公共方法（定义序）: points, register, validate_all, status_all, list_points；源码…
    #   inputs: 无参数
    #   outputs: 返回值
    #   （注：A1 之后另有 2 个公共定义未列入（含 2 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（3 定义）
    #   name_en: public defs
    #   intro: IntegrationRegistry
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
