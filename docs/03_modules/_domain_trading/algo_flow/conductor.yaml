# ALGO_FLOW 外部真源——conductor（_domain_trading 域，真源 src/zephyr/trading/conductor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.conductor
source_of_truth: src/zephyr/trading/conductor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: session_id 参数
    #   fields: 参数 session_id（无注解）
    #   code: conductor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: db_path 参数
    #   fields: 参数 db_path（无注解）
    #   code: conductor.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: max_parallel 参数
    #   fields: 参数 max_parallel（无注解）
    #   code: conductor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① Conductor
    #   name_en: Conductor
    #   intro: 全自动指挥官 —— 认领 + 冲突检测 + 并行分组 + 状态管理。
    #   desc: 全自动指挥官 —— 认领 + 冲突检测 + 并行分组 + 状态管理。；公共方法（定义序）: max_parallel, db_path, autopilot, repo, plan_cycle, mark_comple…
    #   inputs: session_id db_path max_parallel
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: Conductor
    #   downstream: AI session conductor loop (replaces manual AutoPilot.run_cycle + serial executi…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # A1 --> O1
