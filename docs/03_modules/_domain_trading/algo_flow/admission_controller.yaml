# ALGO_FLOW 外部真源——admission_controller（_domain_trading 域，真源 src/zephyr/trading/admission_controller.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.admission_controller
source_of_truth: src/zephyr/trading/admission_controller.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: global_config 参数
    #   fields: 参数 global_config（无注解）
    #   code: admission_controller.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: per_type_config 参数
    #   fields: 参数 per_type_config（无注解）
    #   code: admission_controller.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: enable_circuit_breaker 参数
    #   fields: 参数 enable_circuit_breaker（无注解）
    #   code: admission_controller.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: cb_failure_threshold 参数
    #   fields: 参数 cb_failure_threshold（无注解）
    #   code: admission_controller.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AdmissionController
    #   name_en: AdmissionController
    #   intro: class AdmissionController 源码 L250-L402
    #   desc: 公共方法（定义序）: circuit_breaker, admit, admit_batch, get_metrics, get_retry_after, reset_circuit_breaker, update_r…
    #   inputs: global_config per_type_config enable_circuit_breaker cb_failure_thres…
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: AdmissionController
    #   downstream: zephyr.trading.verdict_engine;MOD-INF-027(audit-orchestrator)
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
