# ALGO_FLOW 外部真源——async_runtime（_domain_trading 域，真源 src/zephyr/trading/runtime/async_runtime.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.runtime.async_runtime
source_of_truth: src/zephyr/trading/runtime/async_runtime.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: max_workers 参数
    #   fields: 参数 max_workers（无注解）
    #   code: async_runtime.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: loop_timeout 参数
    #   fields: 参数 loop_timeout（无注解）
    #   code: async_runtime.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AsyncRuntime
    #   name_en: AsyncRuntime
    #   intro: 事件循环引导 + run_in_executor 桥接。
    #   desc: 事件循环引导 + run_in_executor 桥接。 职责： 1. 启动/停止 asyncio 事件循环（优雅关闭） 2. run_in_executor 桥接——让同步代码…；公共方法（定义序）: loop, i…
    #   inputs: max_workers loop_timeout
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AsyncRuntime
    #   downstream: R1-2 AsyncEventBus；R1-3 PipelineOrchestrator；R1-4 Conductor；__main__.py（未来迁移）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # A1 --> O1
