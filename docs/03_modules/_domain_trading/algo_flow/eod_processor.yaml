# ALGO_FLOW 外部真源——eod_processor（_domain_trading 域，真源 src/zephyr/trading/eod_processor.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.eod_processor
source_of_truth: src/zephyr/trading/eod_processor.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: trade_date 参数
    #   fields: 参数 trade_date，类型注解 str
    #   code: eod_processor.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: positions 参数
    #   fields: 参数 positions（无注解）
    #   code: eod_processor.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: cash 参数
    #   fields: 参数 cash（无注解）
    #   code: eod_processor.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: price_probe 参数
    #   fields: 参数 price_probe（无注解）
    #   code: eod_processor.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① build_eod_job_spec
    #   name_en: build_eod_job_spec
    #   intro: 日终 15:30 任务规格（与 post_settlement_pipeline 同窗口串联，调度层注册）。
    #   desc: 日终 15:30 任务规格（与 post_settlement_pipeline 同窗口串联，调度层注册）。；源码 L197-L205
    #   inputs: 无参数
    #   outputs: EodJobSpec
    # - id: A2
    #   name_zh: ② run_eod_processor
    #   name_en: run_eod_processor
    #   intro: 日终处理执行入口（价格快照 → NAV/P&L 确认 → 风险重估）。
    #   desc: 日终处理执行入口（价格快照 → NAV/P&L 确认 → 风险重估）。 Args: trade_date: 结算日（YYYY-MM-DD）。 positions: 日终持仓（Eo…；源码 L208-L335
    #   inputs: trade_date positions cash price_probe risk_reassess_fn expected_nav n…
    #   outputs: EodReport
    #   （注：A2 之后另有 4 个公共定义未列入（含 4 个数据契约/异常/枚举声明类），见源码）
    # - id: A3
    #   name_zh: ③ 日终风险重估（run_eod_processor 内步骤 3，委托既有 risk 件）
    #   name_en: risk_reassess_step
    #   intro: 拿确认后的 NAV 找既有 risk 件重估风险，出错只落状态字段，绝不让异常逃回调度器。
    #   desc: risk_reassess_fn(trade_date, nav)；未注入→risk_status=SKIPPED；抛异常→ERROR+errors+alert；成功→OK；源码 L239-L249（模块头 docstring 日终三步之③）
    #   inputs: trade_date nav risk_reassess_fn alert_sink audit_sink
    #   outputs: risk_status(OK/ERROR/SKIPPED) 落进 EodReport
    # 层: 输出
    # - id: O1
    #   name_zh: EodJobSpec
    #   name_en: EodJobSpec
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 调度层(data scheduler APScheduler / trading work_dag，build_eod_job_spec 注册入口，与 pos…
    # - id: O2
    #   name_zh: EodReport
    #   name_en: EodReport
    #   intro: 顶层公共函数返回值（真实返回注解，AST 提取）
    #   downstream: 调度层(data scheduler APScheduler / trading work_dag，build_eod_job_spec 注册入口，与 pos…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> A2
    # A2 --> O1
    # A2 --> A3
    # A3 --> O2

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: trade_date + positions(EodPosition) + cash
    # I2: price_probe/risk_reassess_fn/expected_nav/nav_tolerance(注入)
    # A1: 价格快照(逐symbol取日终价; 异常/非正→unpriced按0计如实披露)
    # A2: NAV/P&L(mv=Σqty×price; nav=cash+mv; pnl=Σ(price−cost)×qty; |nav−expected|>tol→DRIFT)
    # A3: 风险重估(委托回调; 异常→ERROR+alert; 未注入→SKIPPED)
    # O1: EodReport(frozen)
    # 边:
    # I1 --> A1
    # I1 --> A2
    # I2 --> A1
    # I2 --> A2
    # I2 --> A3
    # A1 --> A2
    # A1 --> O1
    # A2 --> O1
    # A3 --> O1
    # [/ALGO_FLOW]
