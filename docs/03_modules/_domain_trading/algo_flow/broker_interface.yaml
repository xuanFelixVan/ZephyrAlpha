# ALGO_FLOW 外部真源——broker_interface（_domain_trading 域，真源 src/zephyr/trading/trading_contracts/broker_interface.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.trading_contracts.broker_interface
source_of_truth: src/zephyr/trading/trading_contracts/broker_interface.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: 模块内部数据
    #   fields: 无公共形参/无再导出（AST 事实）
    #   code: broker_interface.py
    # 层: 算法
    # - id: A1
    #   name_zh: ① BrokerInterface
    #   name_en: BrokerInterface
    #   intro: 券商接口抽象基类（OCP-003 OCP 扩展点）
    #   desc: 券商接口抽象基类（OCP-003 OCP 扩展点） 所有券商适配器 MUST 实现此接口。 支持同时接入多家券商，通过 SOR 路由选择最优执行通道。；公共方法（定义序）: broker_id, connect, di…
    #   inputs: 无参数
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: BrokerInterface
    #   downstream: 见模块头 [CONSUMERS]
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
