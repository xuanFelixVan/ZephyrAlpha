# ALGO_FLOW 外部真源——verdict_engine（_domain_trading 域，真源 src/zephyr/trading/verdict_engine.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.verdict_engine
source_of_truth: src/zephyr/trading/verdict_engine.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: protection_index 参数
    #   fields: 参数 protection_index（无注解）
    #   code: verdict_engine.py 顶层公共函数形参（AST 提取）
    # - id: I2
    #   name: gpu_scheduler 参数
    #   fields: 参数 gpu_scheduler（无注解）
    #   code: verdict_engine.py 顶层公共函数形参（AST 提取）
    # - id: I3
    #   name: verdict_timeout_s 参数
    #   fields: 参数 verdict_timeout_s（无注解）
    #   code: verdict_engine.py 顶层公共函数形参（AST 提取）
    # - id: I4
    #   name: max_batch_size 参数
    #   fields: 参数 max_batch_size（无注解）
    #   code: verdict_engine.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① VerdictEngine
    #   name_en: VerdictEngine
    #   intro: class VerdictEngine 源码 L218-L595
    #   desc: 公共方法（定义序）: protection_index, gpu_scheduler, verdict_timeout_s, eval_count, red_count, yellow_count, pass_coun…
    #   inputs: protection_index gpu_scheduler verdict_timeout_s max_batch_size batch…
    #   outputs: 返回值
    #   （注：A1 之后另有 11 个公共定义未列入（含 11 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（12 定义）
    #   name_en: public defs
    #   intro: VerdictEngine
    #   downstream: MOD-INF-027(audit-orchestrator);MOD-INF-031(auto-fix-engine);zephyr.trading.adm…
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # I2 --> A1
    # I3 --> A1
    # I4 --> A1
    # A1 --> O1
