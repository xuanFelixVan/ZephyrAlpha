# ALGO_FLOW 外部真源——auto_dispatcher（_domain_trading 域，真源 src/zephyr/trading/auto_dispatcher.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.auto_dispatcher
source_of_truth: src/zephyr/trading/auto_dispatcher.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: task_repo 参数
    #   fields: 参数 task_repo（无注解）
    #   code: auto_dispatcher.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① AutoDispatcher
    #   name_en: AutoDispatcher
    #   intro: 轻量 PipelineDispatcher — 执行 TaskCard 并触发整条基础设施管道。
    #   desc: 轻量 PipelineDispatcher — 执行 TaskCard 并触发整条基础设施管道。；公共方法（定义序）: dispatch；源码 L71-L143
    #   inputs: task_repo
    #   outputs: 返回值
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（1 定义）
    #   name_en: public defs
    #   intro: AutoDispatcher
    #   downstream: none
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1
