# ALGO_FLOW 外部真源——trading_order_aggregate（_domain_trading 域，真源 src/zephyr/trading/trading_order_aggregate.py）
# 2026-09-15 P2-1 契约头减负批量出仓：docstring 内联块逐字节副本，extractor 经
# external 锚加载走同一 parse_algo_flow 管线；算法公共面变化时同步更新本块。
doc_type: architecture_view
ttl: permanent
module: src.zephyr.trading.trading_order_aggregate
source_of_truth: src/zephyr/trading/trading_order_aggregate.py
algo_flow: |
    # [ALGO_FLOW]
    # 层: 输入
    # - id: I1
    #   name: event_sink 参数
    #   fields: 参数 event_sink（无注解）
    #   code: trading_order_aggregate.py 顶层公共函数形参（AST 提取）
    # 层: 算法
    # - id: A1
    #   name_zh: ① TradingOrderBook
    #   name_en: TradingOrderBook
    #   intro: TradingOrder 聚合注册表（簿）——幂等注册 + 状态机迁移 + 事件发布。
    #   desc: TradingOrder 聚合注册表（簿）——幂等注册 + 状态机迁移 + 事件发布。 event_sink：注入式领域事件出口（装配批接事件总线）；缺失仅落聚合内 事件日志，s…；公共方法（定义序）: registe…
    #   inputs: event_sink
    #   outputs: 返回值
    #   （注：A1 之后另有 7 个公共定义未列入（含 7 个数据契约/异常/枚举声明类），见源码）
    # 层: 输出
    # - id: O1
    #   name_zh: 模块公共 API 面（8 定义）
    #   name_en: public defs
    #   intro: TradingOrderBook
    #   downstream: 交易运营编排层（运行时装配批）; MOD-TRADING-010 SettlementRecord 聚合（FILLED→SETTLING 联动）
    # [/ALGO_FLOW]
    #
    # 边:
    # I1 --> A1
    # A1 --> O1

# 以下 algo_flow_prose = 契约头双真源清偿归并（P2-1 死块批 2026-09-16）：
# 源码头块手写算法速记逐字副本，algo_flow 机器块未覆盖其口径故不删信息；
algo_flow_prose: |
    # [ALGO_FLOW]
    # I1: register(order_id/idempotency_key/symbol/side/quantity)——运营订单注册请求; idempotency_key 幂等键
    # I2: transition(order_id/to_status/occurred_at/note)——状态迁移请求(执行段结果/结算事件由编排层翻译后驱动)
    # F1: TradingOrderBook.register()——幂等注册: 同 idempotency_key 返回既有聚合; order_id 冲突异键 Fail-Closed
    # F2: TradingOrderBook.transition()——状态机校验(VALID_TRANSITIONS)→迁移→产出 OrderDomainEvent→event_sink 发布
    # A1: 非法转换/终态再迁移→InvalidOrderTransitionError(Fail-Closed); sink 异常仅日志不阻断
    # O1: TradingOrder 聚合根(只读快照) + OrderDomainEvent 领域事件流(append-only, 可 replay 重建)
    # [/ALGO_FLOW]
