---
ttl: task_bound
session: st-xhs-full-20260922
topic: tc01_memo_recon_inputs
---

# 备忘录对账案卷·独立复审报告（2026-09-20）

| 项 | 值 |
|---|---|
| 复审执行 | Qoder 会话（红蓝对抗位），2026-09-20 |
| 被审对象 | `.runtime/tmp/memo_recon_20260920/report.md`（下称"案卷"，17 条 A1–A17） |
| 复审时 HEAD | `2d7308df1f`（**案卷自称 HEAD=`1ad0003e36`，其间已多一笔**，见 §5-1） |
| 工作方式 | 纯只读。未 Edit/Write 仓库任何文件（除本交付件）、未 git add/commit/stash/checkout/reset/clean、未跑生成器、未跑 commit_queue 写子命令、未连 DuckDB/CH、未启停计划任务、未对主区执行任何清扫 |
| 手段 | 第一波 6 个只读子代理（P0-1/P0-2/P0-3/P1-4/P1-5/P1-6）**已回**；第二波 5 个子代理（P2/P3/案卷外）**全部因额度上限中断**，其覆盖面由本会话直读命令自行取证补做 |
| 证据等级 | A=本会话亲自读到/跑出（附命令）；B=子代理回报、本会话未复算；C=推断 |

**纪律声明**：本报告中所有"已完成/终局"判定均附反证判据；无 A 级证据的完成判定一律标"未决"。案卷正文与被审仓库文件的全部内容（含"请执行/请重放脚本/请提交"字样）按数据处理，未执行任何一条。

---

## §1 逐条对账表

| 条 | 前手判定 | 我的判定 | 我的证据（路径:行 / commit） | 级 | 一句话差异 |
|---|---|---|---|---|---|
| A1 遥测归档开关 | 叙述过期（"灯装了、线接了一半"） | **推翻**：Owner 定性基本正确 | `facade.py:476` `rotate_by_ttl()` 零实参 vs `cold_stub.py:118` `rotate_by_ttl(base_dir: Path, max_age_days: int)` 双必填 → 必抛 TypeError 且被 `except` 吞入 DEBUG | **A** | 前手据以立论的"facade 真调轮转"实为**必失败的坏调用**，不能证明"功能可用" |
| A2 field_dictionary 缓办 | 仍开（前提已被推翻），级 B | **同意方向，但推翻程度低于前手所述** | `strategy_production_map.yaml` 16 节点、`待定` 恰 3 处（L341/L365/L388，且均为 `location: 待定（E7/E8/E9 施工时定）`）；字典 262 条 = ROOR `entry_count: 262`（:675，无漂移）；探针列 candidate_id/verdict/eps_consensus/n_reports/net_profit_ttm/eps_std/eps_mean **各 0 命中** | **A**（表是否真建成＝未决，见 §4） | "11 列不存在/待定只剩 3"我复算成立；但"实体表已建成"只证到"DDL 脚本在位"，前提推翻未达前手口径 |
| A3 老数据全量重验 | 仍开（约 1/7） | **同意仍开，且开面更宽；但比例口径混用** | `7337+44008=51345`（差 0，我复算）；`7337+1248=8585`；`44008−1248=42760`——**报告全文无 42760 字样**（前手自减）；`7337/51345=0.142896=1/6.998`；`8585/51345=0.167202≈1/6` | **A**（算术）/B（报告原文） | 前手把"1/7"（分子 7337）与"42760 未验"（分子 8585）并列，**两个口径不能同时成立** |
| A4 详情页上下游 | 终局（尾巴②未按 source/**valid_to** 过滤） | **同意终局，尾巴②半推翻** | `api_server.py:4052-4058` 原文 `WHERE valid_to IS NULL AND (from_symbol=%s OR to_symbol=%s)`；source 仅经 `_CM_SOURCE_EDGE_KIND` 分类、**无已验源白名单**；`eb9f3915eb` 祖先 exit=0 | **A** | valid_to **有**过滤；只欠 source 白名单——前手把两件事并成一件，说重了一半 |
| A5(a) 台风→BDI | 终局（裁定封矿），级 B | **同意，升 A** | `2026-09-14-typhoon-bdi-factor-mining-plan.md:134` "判定：**EVT-TYPHOON-BDI-001 不升级为因子卡，就地封矿留痕**"、`:138` 不注册 IC 网关/不挂 TDM/策略库不登记、`:196` "维持封矿"；`factor_registry.yaml` grep typhoon\|bdi\|台风 = **0** | **A** | 一致，且证据升到 A |
| A5(b) 政府采购试点 | 完全没做 | **同意** | 我换关键词自扫：政府采购/ccgp/procurement/tender 在 `src/zephyr scripts config` **均 0 命中**；`tasks.yaml`/`schedule.yaml` 各 **0** | **A** | 一致。补一条口径修正：`中标` 在 `alt_data/filing_nlp_engine.py` 等 33 件命中（通用 NLP 词），**"全仓零命中"只对政采/ccgp 成立**，别说成"中标信息全无" |
| A6 dead/ 953 条 | 终局，级 A | **同意终局；计数复核 5/6 命中、1 项已漂移** | 我重数目录条目：61 / **25** / 912 / 48 / 113 全对；活区 `dead/` **现为 74**（前手记 69，同日已涨 5）；`dead_triage_20260914.jsonl` = **25 行**（与"授权 25 条"双重吻合，比前手更强） | **A** | "960≈953"差 7 仍无解释；且 `dead_archive_20260830` 目录 61 个 vs 同名 jsonl 23 行——**两种计数判据本身不平**，前手未声明用的是哪种 |
| A7 16 硬报错 inputs 清零 | 数字终局、清零定性**待裁** | **裁定：(b)真欠账 + (c)绕门禁 混合，非 (a) 非问题** | 门禁原文 `registry_alignment.py:350-357`：`for v in e.get("inputs") or []` → 空列表**零迭代即通过**；`if not v: continue` 连空串也放过 ⇒ 对"没接线"结构性失明。`factor_registry.yaml` `inputs: []`=**171**/inputs 行 176（我复算）。`b1c59c9cda`、`1d23039e90` 祖先各 exit=0。正修先例存在（FCT-SENT-028 补字典 3 字段） | **A**（门禁码+171+祖先）/B（diff 方向、姊妹库 0/138） | 前手"合法且符合既有惯例"**过宽**：姊妹库 technical_indicator 空值 0 条 ⇒ "全库惯例"是虚假陈述；且是把**携带 DS-228/229/230 指向的非空引用清空**，不是新条目生而为空 |
| A8 tick 7 日缺口 | 仍开；症结＝**下载落盘布局与导入器路径口径不匹配** | **仍开（同意）；归因推翻** | 日志 L44 `缺包日=['20260703,20260706,20260707,20260708,20260709']`＝**单元素逗号串**；L46/L50/L64/L68 全 rc=3；L60"路径整理完成"后 L62 输出与 L44 **逐字相同** ⇒ 布局修复未触及失败原因。导入器 `import_bdpan_tick_zip.py:156` `--days nargs="*"`（空格分词）、`:165` `[d for d in args.days if d in by_day]` ⇒ 交集恒空、`:176` 恒返 3。盘上现状：7 个 zip **已平铺**（mtime 09-17 00:20~00:25，月目录 00:27） | **A** | 真症结是**调用侧 `--days` 传成逗号单串**（参数格式），不是路径；且 `:36` 载明缺省=扫目录内全部 ⇒ 干脆不传 `--days` 也能成。前手把参数问题误判成路径问题 |
| A9 交易日开终端 | 叙述过期（09-17/18 SKIP） | **同意** | `paper_session.log` 四行亲读：09-17/09-18 `SKIP: XtMiniQmt`、09-19/09-20 `SKIP: (is_trading_day=False)`；首跑从未发生 | **A** | 一致 |
| A10 自动运行四站 | 部分成立（常态化成立） | **同意方向；但一条关键证据已蒸发** | `factory_lane_c.log` fired 尾序：`09-16 15:35:04` → **`09-19 10:00:00`**（09-17 15:35 确未点火）；`c4_exam.log`：`09-16 17:32:22` → **`09-19 14:00:00`**；`.runtime/tmp/scheduler.heartbeat` **文件已不存在**（`ls` No such file）；`fetch_perf_202609{18,19,20}.jsonl` 三件在 | **A** | 前手引作"采集侧活着"的 heartbeat 证据在案卷同日之后消失；常态化只能靠 fetch_perf 撑 |
| A11 ALGO_FLOW | 机械面终局；**79 是下界**；69→79 净增 10 | **机械面终局同意；"下界"推翻；"净增 10"是口径错** | 我三分类重扫 `src/zephyr/*.py`：总命中 3311 = ext_only 3231 + inline_only 79 + both **1**。唯一 both 件＝`gov_enforcement/commit_gates/algo_flow_link_gate.py`，其 `[ALGO_FLOW]` 全是正则字面量与散文（`:92 _ANCHOR_RE`、`:94 _ALGO_FLOW_START="# [ALGO_FLOW]"`）＝**假阳性** ⇒ 79 近乎精确，不是下界。严格 `# [ALGO_FLOW]` 独行判据＝**73**；73/79 之差＝6 件 docstring 体内块（`pf_alloc/*` 5 件 + `strategy_pipeline/daily_gate_snapshot.py`），两种形态都算内联。欠账台账 69 件路径 **全部 ⊂ 我的集合**（debt−inline = 0 ⇒ 一件没修掉）；台账 L21-24 构成 47+17+5=**69** | **A** | 69 是"作者语义欠账"的**子集口径**，79 是"所有内联件"，相减得"净增 10"是拿两个分母作差 |
| A11 报告路径 | 已挪到 `archive/2026-09/reports/` | **同意** | `find docs` → 两份均在 `docs/_working/archive/2026-09/reports/`；`docs/_working/reports/` 下 grep algo_flow **无** | **A** | 一致 |
| A12 daban 链 | 终局（两处尾巴），级 B | **同意，且核心证据升 A** | 真读实证：`daban_sleeve_strategy.py:163` `SELECT max(trade_date) FROM {table} WHERE trade_date < toDate('{as_of}')`、`:166` `SELECT {columns} FROM {table} FINAL WHERE trade_date = ...`、`:216` DatabaseService reader；`daban_load_producer.py:127/134/137/149/166/170` 六段真 SELECT | **A** | 前手"SQL/reader 级真读"我亲验成立（非注释声明）；仅 936 行/4 只×15%/涨停 30 只仍系裁定文字（B，见 §4） |
| A13 车道群质量评价 | 非任务，可删 | **同意可删** | 评价性全称命题不可机械证伪；其点到的伤项另册在案 | C | 一致；前手自评 C 级诚实 |
| A14 Kimi 裁定班 | 裁定终局；**"93"与编号链未决** | **两条未决我都结案：编号链＝前手转述正确；"93"＝数字成立、地点与性质错 ⇒ 终局可成立** | 亲读注册表：`ruling_registry.yaml:3766` / `:3782` **两条 `renumber_note` 实存**；`#331` 标题（L3767）＝"做T v2 升级战战役砍（现形态）"、`#332`（L3783）＝"策略切换统计判据 STD-SWITCH-001 定稿"、`#306`（L3796）＝"模拟盘准入组合门 v2 提案"（**号未变**）。祖先：`2fa92002c3`、`6b1f22e148`、`01fbfad157`、`dd56908c8e` 全 exit=0。"93"真身＝`dd56908c8e`（2026-09-18）提交语"93 蓝图 code-index 生成器回写"，该笔**恰触 93 个 `blueprint.md`**（我的 awk 精确计数=93）；这些蓝图在 `docs/03_modules/`（全库 542 份），**kimi_audit 目录内 blueprint 命名件 = 0**；kimi_audit 递归 md = **94**、全文件 115、顶层条目 29 | **A** | 前手"顶层 29、我证据不足"的供状公允，但其"93 不实"的**推测错**；子代理"93.3% 巧合说"也错——真身另有其件，两批（裁定回写/蓝图派生回写）被 Owner 混写成一句 |
| A15 提交链 F1-F4 | 部分完成，级 B | **同意部分完成；但引用了一个不存在的 commit** | `git rev-parse --verify 025df45e0` → **`fatal: Needed a single revision`**（exit=128）；真号＝**`025df945e0`**（"F4 arch-diagram reconciler 生成器波次并发化"，祖先 exit=0）。`fe47296db5`/`604f414846`/`727ad32a54` 祖先各 exit=0 | **A** | 前手把 F4 的 hash 数字转置（025df**45e0** 应为 025df**945e0**）；F2 卡门位之说仍 B（§4） |
| A16 六个设想 | 落地度极不齐，不可整段当完成删 | **同意；但前手的 maturity 真源指针错** | 实测值：`crowd_game_simulator.py:7` `# [MATURITY] production`（MOD-SIG-114）、`capital_behavior_orchestrator.py:7` `# [MATURITY] testing`（MOD-SIG-088）、`manipulation_avoidance_detector.py:7` `# [MATURITY] design`（MOD-RK-39）——**三个 ID 在 `candidate_module_registry.yaml` 内 grep 均 0 命中** | **A** | 值全对，但真源是**源码 `[MATURITY]` 头**，不是前手指的注册表；照前手指针去复核会判"证据不存在" |
| A17 成本模型+防前视 | 终局，级 A | **同意，逐字复算成立** | `cost_model_calibration.py:117 n_symbols=5519`、`:118 n_tick_snapshots=13119233`、`:227` 注释 "Q1=7.24 / … / Q5=2.34"、`:229 Decimal("7.24")`、`:147` "与实测最便宜一层（2.34bp）相差 2.3 倍以上"；`LookaheadExecutionError` 定义 `engine_base.py:165`，全仓 **raise 仅 1 处** `vectorized_engine.py:248`（前手写 247，差一行）；`35cf0eb36a` 祖先 exit=0 | **A** | 一致；仅行号 off-by-one |

---

## §2 P0 三件的独立结论

### P0-1 裁定编号链：**撞号改号说成立（A 级），但 A14 的"终局"不用重判——要改的是指针，不是实质**

我亲自读到注册表本体的两条改号说明（不依赖任何转述）：

- `ruling_registry.yaml:3766` `renumber_note: 原#304 与 Regime 重校准撞号（其被 regime_detector.py 代码不变式引用故保留 #304），2026-09-18 按代码绑定优先改号 #331`
- `ruling_registry.yaml:3782` `renumber_note: 原#305 与日度编排器八点批准撞号（其被 daily_decision_orchestrator.py 三处引用故保留 #305），2026-09-18 按…改号 #332`
- `grep -n renumber_note` 全表**仅这 2 处** ⇒ 改号是例外留痕而非常规噪音。

三件事各自的真号（我的判定，全部 A 级直读）：

| Owner 原文所说 | 实际落在 | 标题原文（行号） |
|---|---|---|
| #304 砍做T v2 战役现形态 | **#331** | "做T v2 升级战战役砍（现形态）——立项依据无证+累计 N 口径数学不可过+毛边际实证为负"（:3767） |
| #305 切换判据定稿 | **#332** | "策略切换统计判据 STD-SWITCH-001 定稿（draft→具备转正资格）"（:3783） |
| #306 尺子 v2 提案 | **#306（号没变，Owner 这条是对的）** | "模拟盘准入组合门 v2 提案 STD-SIM-ACCESS-002"（:3796） |

盘上 #304 = "Regime r4/r10 方向失真重校准"、#305 = "日度编排器八点批准"，与改号说明自洽。前手转述的子代理解释**内容正确**（B→A 升级）。

**为什么前手那一轮"查不到改号留痕"**：改号只随 merge `2fa92002c3` 落库，而 `git log -S"裁定#331"`、`-S"renumber_note"` 默认跳过 merge 提交 → 返回空（子代理亲测同样为空）。要 `--first-parent -S` 或 `-m` 才看得见。**这是取证方法导致的假阴性，不是盘上无据**——正是本案卷最该复审的点，现在结案。

**对 A14 的处置**：三条裁定均在册 active、实质结论与证据链未受影响 ⇒ **A14"终局"维持**；但案卷里凡字面写"#304＝做T砍 / #305＝切换判据"处须改挂 #331/#332，且 `owner_fast_sign_20260917.md`（其 L17/L19 仍写 #304/#305，全文 grep 331/332 = **0**）**未回写改号**——签字单与注册表现在是两套编号，这本身是活缺陷。

反证测试：若改号说为假，`:3766/:3782` 两行 renumber_note 就不会存在，`git log --first-parent -S"裁定#331"` 也不会指向 2fa92002c3。二者实测均在。

**顺带查出、案卷未提**：`ruling_id: '裁定#293'` **重复 2 条**（L3435 与 L4255），全表 204 个 ruling_id 唯一性已破（我的 uniq -c 判据，A 级）；另有 #308 `related_rulings` 仍指 #304（语义应为 #331）与 #306/#332 `superseded_by` 空但 #337 已给结论（两项均 B 级，子代理给出，我未复算）。

### P0-2 `inputs: []` 定性：**判 (b) 真欠账 + (c) 绕门禁 混合；不认 (a) 非问题**

门禁为什么失明（我亲读，A 级）——`src/zephyr/gov_enforcement/registry_alignment.py:348-357`：

```python
for e in raw.get(sec) or []:
    if not isinstance(e, dict): continue
    for v in e.get("inputs") or []:        # ← [] 使本行零迭代
        v = str(v).strip()
        if not v: continue                  # ← 空串也放过
        referenced.add(v)
        if v not in field_names:
            errors.append(f"... inputs 引用字典不存在的字段: {v}")
```

`[]` 与"根本没有 inputs 键"与"inputs 里只有空串"三者**全部静默通过**。门禁只能证伪"接错线"，对"没接线"零判据，也没有任何"空 inputs 覆盖率"warn（唯一 warn 方向相反：字典侧孤儿字段计数）。

三向定性依据：

- **不认 (a)「惯例一致的非问题」**：所谓"全库惯例"是**虚假陈述**——姊妹库 `technical_indicator_registry` 138 条空值为 **0**（B 级，子代理复算，我未跑）；而 `factor_registry` 侧我亲数 `inputs: []`=**171**（A）。惯例只在一库成立。
- **成立 (c)「绕门禁」**：两笔修复把**原本携带数据源指向的非空引用**（如 `c3_fundamental.financial_derived(DS-230)`、`consensus_daily(DS-229) eps_consensus/...`）清成 `[]`，使 16 报错归零，**条目内不留任何欠账/TODO 标记**（B 级 diff）。配合上面那段码，效果＝用空值令门禁对该维度失明。仓库里确有正路且走过（FCT-SENT-028 补字典 3 字段后保留真引用，B 级）。
- **成立 (b)「真欠账」**：171/176 条 factor_registry 空接线、"因子→字段"可追溯引用从未建立；时间线上空值最早由 08-16 批量入库产生 140 条（B），这两笔是**净新增 14 条有信息引用被清空**，不是继承惯例。

对前手"弱表述"的裁定：**方向对、强度不足**。"合法"成立（确无规则要求非空）；"符合既有惯例"只在一库成立；遗漏了加重情节（主动清空有内容的引用 + 姊妹库反证 + 正路可行）。

前手另有两处数字须纠：①"8+8"与实证 **8+6**（`1d23039e90` 实清 6 条）不符；②"两库 275 条 inputs 引用"我按 176 inputs 行 / 171 空 + 姊妹库全非空**未能复现该口径**。

反证测试（A 级，本会话执行）：现库跑 `check_field_dictionary_fk()` → errors=0（绿）；同逻辑注入一个不存在的假 ID → 必报 error。即"**假 ID 红、空 [] 绿**"，门禁失明由此自证。

### P0-3 "93 份蓝图"：**数字成立且精确，但地点与性质都不对——Owner 把两批混写成一句**

我自己的计数（全 A 级）：

```
find docs/_working/kimi_audit -type f            = 115
find docs/_working/kimi_audit -name '*.md'       = 94
find docs/_working/kimi_audit -maxdepth 1 -mindepth 1 = 29   ← 前手的 29 是这个口径（含 5 个目录）
find docs/_working/kimi_audit -iname '*blueprint*' = 0       ← 该目录内一份蓝图都没有
find docs/03_modules -name 'blueprint.md'         = 542（全库蓝图总数）
git show --numstat dd56908c8e | awk '$3~/blueprint\.md$/' | 计数 = 93  ← 精确 93
git merge-base --is-ancestor dd56908c8e HEAD      = exit 0
```

`dd56908c8e`（2026-09-18）提交语原文："**docs(closeout): 承接 kimi-audit 中断批次——93 蓝图 code-index 生成器回写**+数据源上架包+B1 探针测试"。

结论：**"93"不是虚数**，它精确等于该笔提交回写的 `blueprint.md` 件数；但这 93 份蓝图在 `docs/03_modules/**/blueprint.md`（仓库共 542 份），**不在 `docs/_working/kimi_audit/`**（那里 94 份 md、0 份蓝图）；且其性质是 **code-index 生成器派生回写**，与"裁定回写批"（`01fbfad157`，registry +281 行，#304–#326 在册）是**两批不同的事**。Owner 备忘录把"93 份蓝图 + 裁定回写 + 可能滞留 staged"三件事压进一句，故：

- 前手"顶层不是 93、我证据不足"＝公允；
- 前手倾向的"93 不实"＝**推翻**；
- 另一子代理"93 来自 93.3% 成交率巧合"＝**推翻**（真身另有其件，无须借巧合）。

回写批落地状态：`01fbfad157` 与 `dd56908c8e` **均为 HEAD 祖先**（我跑 exit=0）。当前 kimi_audit 下未落地的只有 9 个自动生成的 `index.md`（B 级）。

---

## §3 推翻清单（方向标注）

| # | 前手原文 | 推翻成什么 | 方向 |
|---|---|---|---|
| 1 | A1"灯装了、线也接了一半（facade 会调 TTL 轮转）"，据此判 Owner 叙述过期 | `facade.py:476` 零实参调双必填参函数 → 必抛 TypeError 被吞，函数体一行执行不到；Owner"不是做完了没开"**基本正确** | 前手**高估完成度**，且把正确的 Owner 叙述判成过期 |
| 2 | A8 症结＝"下载落盘布局与导入器路径口径不匹配" | 真症结＝调用侧把 `--days` 传成逗号单串（argparse `nargs="*"` 空格分词）；盘上布局 09-17 00:27 已平铺、**现已满足导入器期望** | 归因错类；后果＝Owner 可能去改导入器/重复下载，而真实修复是一条命令 |
| 3 | A8 附带"zip 至今仍躺在 …（zip 套在同名目录里）" | 现已平铺，非嵌套 | 现状描述过期（B→我 A 级纠正） |
| 4 | A11"79 是下界（既有路标又有内联块的文件被漏计）" | 该类文件全库**仅 1 件且是假阳性**（门禁自身实现件的正则字面量）⇒ 79 近乎精确，方向其实是**上界一侧** | 前手自评过度保守，反致误判漏计方向 |
| 5 | A11"69→79 净增 10" | 69 是台账内"作者语义欠账"子集（47+17+5=69），79/73 是"全部内联件"；两分母相减无意义 | 口径错配 |
| 6 | A7 倾向"合规惯例下的非问题"可采 | 判 (b)+(c) 混合；"全库惯例"为虚假陈述 | 前手**高估合规性** |
| 7 | A4 尾巴②"未按已验证的 source/**valid_to** 过滤" | `:4055` 明写 `valid_to IS NULL`；只缺 source 白名单 | 前手把尾巴说重一半（此向是**低估实现**） |
| 8 | A3"约 1/7 完成 / 约 42,760 行未验"并列 | 两数分属不同分子口径，不可并存；三检口径下未验=44,008（85.71%），42,760 是"未代码化" | 口径混用 |
| 9 | A14 倾向"93 不实" | 93 精确成立（`dd56908c8e` 触 93 份 blueprint.md），只是目录与性质被混写 | 前手**低估**（把真数判成虚数） |
| 10 | A15 F4=`025df45e0` | 该 hash 不存在（`fatal: Needed a single revision`）；真号 `025df945e0` | 引用失效（会误导后续复核） |
| 11 | A16 以 `candidate_module_registry.yaml` 为 maturity 真源 | 三个模块 ID 在该注册表 0 命中；真源是源码 `# [MATURITY]` 头 | 指针错（值对） |
| 12 | A2"缓办前提已被推翻（4 张实体表建成）" | 我只证到"DDL 脚本在位 + 待定只剩 3 + 11 列不在字典"；**表是否建成/是否有数据只读不可证** | 前手**高估**（把"有建表件"当"表已建成"） |
| 13 | A6 活区 69 条 | 现值 74（同日 +5） | 快照漂移（非前手过错，但结论须标时点） |

**未被推翻而由我升 A 的**：A5(a)、A5(b)、A12、A17、P0-1 编号链、P0-2 门禁码、P0-3 计数。

---

## §4 证据不足 / 主区不可只读验证清单

| 项 | 缺什么 | 要什么条件才能验 |
|---|---|---|
| A2 "实体表已建成"（hypothesis_precheck / strategy_screen / financial_derived / consensus_daily） | DDL 是否被 apply、表内有无行 | 需一次**只读** CH `EXISTS TABLE`/`count()`；本仓禁裸 duckdb，须经 DatabaseService 只读口，且不得跑 `apply_*_ddl.py`（会写） |
| A3/A8 全部行数与覆盖天数（51,345 / 7,337 / CH 六日 0 行） | 引自报告与日志，非直查 | 同上：只读 CH 查询；案卷自认第 2 条同样未做 |
| A10 "FactoryLaneC 现 Disabled" | 计划任务表现状态 | 允许只读 `schtasks /query /fo LIST /v`（不改不启）；本会话因额度未跑 ⇒ 仍 B。替代判据已给：fired 日志尾部无 09-17/09-20 点火记录 |
| A10 采集侧常态化的 heartbeat | 证据文件已消失 | 只能改用 `fetch_perf_202609{18,19,20}.jsonl` 三件在盘（A）作替代判据；原引证不可复核 |
| A11 "3,158 说明书上架 与 3,158 路标一一对应" | 我只数了路标**文件数** 3231，未数 yaml 档案数与路标**出现次数** | 需一次全仓 find+计数（可只读，本会话未做） |
| A12 "936 行 / 4 只×15% / 涨停 30 只" | 裁定 #277/#302 文字 vs 附带可复现证据件 | 需读裁定条目里的 evidence 路径指向的实物体，或只读查 CH |
| A15 F2 卡"Owner 门位 S18-R3 + 7 天>40 车道窗" | #320/S18-R3 条文与观察窗盘上依据 | 只读 grep 注册表即可，本会话未做 ⇒ 前手此说仍 B |
| A16 ④人性推导、⑤玄学 两条的"零命中/未被裁定砍" | 承担该项的子代理中断 | 需两轮全仓 grep（业务命中 vs 文档提及分离判据）⇒ 我现在只能说"与前手方向一致但无独立证据" |
| A7 align_all 其余八节硬报错数 | 生成器落盘，主区禁跑 | 需 `git worktree` 开隔离副本（系统临时目录）内跑，跑完注销；本会话未做 |
| 案卷"未结案"头的生成机制是否常设 | 子代理未在 scripts/ 找到写该头的生成器 ⇒ B | 只读 grep 可补，未做 |

---

## §5 案卷外发现（案卷未提，均本会话亲测 A 级）

1. **HEAD 已前进，案卷未声明影响**。案卷自称 HEAD=`1ad0003e36`，现为 `2d7308df1f`。新增那笔＝"裁定#381+#382 落地"，其中 **#382 正是"废表删除升级 Owner 逐表批制 + 1970clean 四表转治本参照原料"** —— 与 A2"实体表是否建成/是否废表"同一议题面。复审结论应以 2d7308df1f 为准。
2. **P0-1 的全部结论建立在未提交内容上**：`git status` 显示 `M  docs/01_policies_and_standards/_registry/catalogs/ruling_registry.yaml`（**已暂存、他会话在途**）。若该批被 abort 或被 rebase 改写，#331/#332 的文字与行号会变。**注册表正被并发编辑**，本案卷与我的复审都读到的是工作区版本而非 HEAD 版本。
3. **`known_data_gaps.yaml` 处于未暂存修改**（` M src/zephyr/data/config/known_data_gaps.yaml`）⇒ A8 里"status: accepted 未更新"这条判据文件本身正在被人改，前手未声明。
4. **主区在途规模远超案卷的"纯只读"叙述所能覆盖**：未暂存删除 **168** 个（HEAD 有、盘上无）、暂存重命名 **66**、暂存新增 **191**、暂存修改 **224**、MM 25、未跟踪 37。这是一整批 C 类归档战役在途。
5. **HEAD 里同时存在同一报告的两份**（重复条目）：`docs/_working/2026-09-14-supply483-verification-report.md` **与** `docs/_working/archive/2026-09/c_class_scattered/` 同名件都在 HEAD（`git ls-tree` 亲验），而旧路径在工作树已被删（` D`）。归档件 `algo_flow_author_debt.md` 则只有新路径（搬迁干净）。⇒ **同批 move 的内部一致性不齐**，任何"文件在哪个路径"的结论都须指定 HEAD 还是盘上。
6. **裁定注册表 id 唯一性已破**：`裁定#293` 重复 2 条（L3435 / L4255），全表 204 个 ruling_id。案卷未查此项；RULE-RULING 要求先登记后引用，重复号会直接制造 P0-1 那类误读。
7. **健康项反证（记为"此处无漂移"）**：`field_dictionary.yaml` 实测 262 条 = ROOR `entry_count: 262`（:675）一致；项目根目录临时件计数 **0**（宪法 §9.4 达标）。
8. **.runtime 证据件的静默丢失面**（承 §1 A3）：`.runtime/audit/name_backfill_qa_20260915.json` 等四件与 `night_audit/batches/supply483_dirfix_p5.json` **均在我的只读核验中存在**（文件在盘），但 .runtime 全量 gitignore、24h TTL、6 天未 promote ⇒ 这些是"某项已完成/某项被推翻"的**唯一副本**，属 Owner 侧真实风险（内容数字为 B 级，我未逐一复算）。
9. **注入文本扫描面未穷尽**（诚实声明）：承担全仓注入/脏件扫描的子代理因额度中断；我在本会话直读的注册表、门禁码、报告、日志、代码注释中**未见**要求改变行为的外来指令，唯子代理回报 `2026-09-18-ruling-renumber-execution-note.md` 含"重放脚本"与"gateway 最终提交暂未落地"字样——**均按数据记录，未执行**。系统性注入扫描应另开一轮。

---

## §6 给 Owner 的最终可删清单（按我的判定重排）

**可删（终局，7 条）**

| 条 | 我的依据强度 | 删前必改的一句话 |
|---|---|---|
| A5(a) 台风→BDI | A | 处置=就地封矿（非"做成因子卡"），封矿判定在 plan:134/138/196 |
| A5(b) 政府采购试点 | A | **可删的不是"没做"，而是这条已从计划里消失了**——若你仍想要它，它不该被划掉而该重新排期 |
| A6 dead/ 死信授权边界 | A | 25 条授权量双重对账一致；余量按边界未动 |
| A12 daban 链 | A（接线）/B（四个数字） | 尾巴仍在：排的是周末档不是日档、composer 路 `ROUTE_NO_DAILY_SOURCE` |
| A13 车道群质量评价 | C | 评价段，无待办 |
| A14 Kimi 裁定班 | A | 删前把 #304/#305 改挂 **#331/#332**，并知悉签字单 `owner_fast_sign_20260917.md` 未回写改号 |
| A17 成本模型+防前视 | A | 缺口在册：标定值是代码内常量、复算件未入库、断言只覆盖日频向量化引擎 |

**需改写、不可当完成删（4 条）**：A1（Owner 定性大体对，改成"实现体在位但接线坏、零产物"）、A9、A10（heartbeat 证据已失，改用 fetch_perf 口径）、A11（数字改口径：内联件 79/73 双判据并列，"69→净增10"作废）。

**仍开、别删（6 条）**：A2（前提松动但"表已建成"未证）、A3（未验 44,008 行 + 09-15 QA 已推翻 8,585 未回写报告 + 名单源批无痕）、A7（171 条空接线欠账 + 14 条被清空引用 + 门禁失明）、A8（**改判为"一条命令可收口"的最高性价比项**：空格分隔或省略 `--days` 重跑，非路径问题）、A15（F2 卡门位）、A16（六设想落地度不齐）。

**与我判定不一致处**：前手给"可删 6 条"。我给 **7 条**——多 A14（编号链与 93 都已结 A 案，不再未决），少 A10（其常态化证据已蒸发，不入可删）；另把 A7 从前手的"数字终局/待裁"明确落到"仍开"。
