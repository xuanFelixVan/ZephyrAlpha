---
ttl: task_bound
completes_when: 全流通战役收官且最终交付报告落盘
---

# 全流通战役 · 全项目断点普查（元挖矿车道 st-ff-mine0-20260918）

> "全流通"的反面清单。环节骨架见 `00_stage_skeleton.md`；派工计划见 `02_mining_dispatch_plan.md`。
> 每条断点均带**实测命令 + 实测输出**，非推测。统计口径已排除 `.aidrafts/` `.worktrees/` `data/c4_pdf_cache/`。
> 复现脚本落 `.runtime/tmp/ff-mine0/`：`dg_probe.py` `dg_struct.py` `dg_agg.py` `dg_bm.py` `dg_census.py` `orphan_scan.py`。

## 0. 总览

| 维度 | 实测值 |
|---|---|
| **断点总数** | **85** |
| 严重度 高 | **38** |
| 严重度 中 | **42** |
| 严重度 低 | **5** |
| Owner 门位（禁任何车道自行执行，只登记） | **5**（BRK-028 / 032 / 036 / 055 / + FF-11 实盘流转） |
| 按环节分布 | FF-01 **26** ｜ FF-14 **17** ｜ FF-16 **6** ｜ FF-10 **4** ｜ FF-15 **4** ｜ FF-03 **3** ｜ FF-06 **3** ｜ FF-12 **3** ｜ FF-13 **3** ｜ FF-04 **2** ｜ FF-09 **2** ｜ FF-02 **1** ｜ FF-07 **1** ｜ FF-11 **1** ｜ FF-05 **0** ｜ FF-08 **0** ｜ 全局横断 **9** |
| 按类型分布 | A 孤儿模块/零入度 **19** ｜ B 零调用生产者/未建生产者 **7** ｜ C 空表/无数据/数据腐败 **20** ｜ D 假通道/fail-open **9** ｜ E 死管线/退役未清 **12** ｜ F 无锚点环节 BM-INV-001 **8** ｜ G 横切机制未落地 **3** ｜ H 域注册表一致性 **5** ｜ I 治理/注册表 **2** |

> **注（宪法 §10 / 裁定#325 表述禁令）**：本文件不说"全绿"。以下 85 条为本轮**检出**的断点；检出面受工具口径限制（见 §J 已知盲区），非"全项目断点完备集"。

## A. 孤儿模块 / 零入度（19 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-001 | FF-14 | 孤儿模块（权威登记·总量） | `config/governance_operations_map.yaml` §counts | `python -c "import yaml;print(yaml.safe_load(open('config/governance_operations_map.yaml',encoding='utf-8'))['counts'])"` → `{'total_modules': 416, 'wired': 244, 'wired_dynamic': 6, 'wired_by_header': 71, 'suspect_orphans': 95}` | 治理运行底板 **95/416 = 22.8%** 模块无消费方 | 中 | 否 |
| BRK-002 | FF-14 | 孤儿模块（分族明细） | `config/governance_operations_map.yaml` §families | 同上脚本遍历 families → `wiring dist: {'wired':244,'wired_by_header':71,'suspect_orphan':95,'wired_dynamic':6}`；95 件按族分布 = L1_monitor 21 / L2_resource 25 / L0_lifecycle 21 / L5_selfheal 10 / L4_reap 10 / L3_fuse 3 / L6_audit 0 | 七族中六族有孤儿，L6_audit 仅 4 模块（审计族本身过薄） | 中 | 否 |
| BRK-003 | FF-15 | 孤儿模块（整族未接线） | `src/zephyr/orchestrator/lifecycle/*.py`（9 件） | GOMAP `families.L0_lifecycle` wiring=suspect_orphan：`startup_sequencer.py` / `teardown_manager.py` / `rolling_upgrade.py` / `state_synchronizer.py` / `state_propagation.py` / `system_transfer.py` / `session_conflict.py` / `incident_postmortem.py` / `housekeeping.py` | **AutoRuntime L0 引导/收尾链整族零消费** —— 启动排序、优雅停机、滚动升级、状态同步全部纸面 | 高 | 否 |
| BRK-004 | FF-10 | 孤儿模块（熔断编排层） | `src/zephyr/autonomy_core/kill_switch_orchestrator.py` | GOMAP `pipeline.layers[GOM-L3].disconnected` → `{'mod': 'zephyr.autonomy_core.kill_switch_orchestrator', 'note_zh': '五域编排层无人 import(A3 降级执行：先接域目标再挂 boot)'}` | **KillSwitch 五域编排层无人调用** —— 熔断只能单域生效，跨域级联保命断链 | 高 | 否 |
| BRK-005 | FF-14 | 孤儿模块（最后手段看门狗） | `src/zephyr/governance/resilience_governance/last_resort_watchdog.py` | GOMAP `pipeline.layers[GOM-L3].disconnected` → `'蓝图声明 CONSUMERS=escalation 但无 import(A4 待接线)'` | 蓝图声明的消费方（escalation）实际不 import → 蓝图↔代码漂移 + 保命看门狗死码 | 高 | 否 |
| BRK-006 | FF-16 | 孤儿模块（热平面预算） | `src/zephyr/infrastructure/hot_plane_budget.py` | GOMAP `pipeline.layers[GOM-L2].disconnected` → `'未反查到消费方(A5)'` | 热平面算力预算无消费方 → 资源水位闸不生效 | 中 | 否 |
| BRK-007 | 全局 | 孤儿模块（静态扫描上界） | `src/zephyr/**`（1585 件） | `python .runtime/tmp/ff-mine0/orphan_scan.py`（修正版内联脚本）→ `scanned py: 4462 src modules: 3477 / ZERO-INDEGREE: 1593 / leaf(non-entry): 1585`；Top 包 = `security.access_control` 73、`gov_code_quality.code_dedup` 52、`autonomy_core.skills` 46、`feedback_loop.diagnosers` 46、`feedback_loop.detectors` 41、`feedback_loop.gates` 41、`infrastructure.rollback` 39、`shared.contracts` 35、`infrastructure.a2a_protocol` 33 | **3477 个 src 模块中 1585（45.6%）静态零入度** —— 这是上界（含 CLI/动态注册/注册表发现），但即便按 GOMAP 严口径也有 95 件确认孤儿 | 高 | 否 |
| BRK-008 | 全局 | 孤儿节点（depgraph 快照过期） | `docs/02_enterprise_architecture/04_architecture_principles_decisions/project_handbook/01_overview.md:135` | 该文件 AUTO 区块记 `孤儿节点数 / Orphan nodes = 384`、`依赖边总数 = 22765`、`最后同步：2026-08-17`；实测 `python scripts/governance/extract_depgraph.py --summary` → `total_modules 7853 / total_production_nodes 11827`，depgraph `edges` 实测 **22805**（漂移 +40 边） | 孤儿权威数字已过期 1 个月，无法作为施工基线 | 中 | 否 |
| BRK-009 | FF-15 | 孤儿族（动态加载盲区） | `src/zephyr/autonomy_core/skills/**`（46 件） | 同 BRK-007 扫描 → `zephyr.autonomy_core.skills: 46`；`autonomy_core/all_skill_modules.py` 亦在零入度清单 | 技能层由注册表/`importlib` 动态发现，静态扫描假阳性；**需以 Agent 技能注册表（ROOR tier0）口径复核**，当前无机械证据表明 46 件技能真被路由消费 | 中 | 否 |
| BRK-010 | FF-14 | 孤儿族（访问控制） | `src/zephyr/security/access_control/**`（73 件） | 同 BRK-007 → `zephyr.security.access_control: 73`（**零入度第一大族**） | 访问控制/RBAC/会话并发层大面积未被 import；与 `kill_switch`（活链 KS=2）并存 → 需辨真伪 | 高 | 否 |
| BRK-011 | FF-12 | 孤儿族（反馈循环引擎） | `src/zephyr/feedback_loop/**`（200 件） | 同 BRK-007 → `diagnosers 46 + detectors 41 + gates 41 + verifiers 20 + collectors 18 + evolution 18 + forensic 16` = 200；`feedback_loop` 全包 340 .py（全项目最大源码包），depgraph `D_FEEDBACK_LOOP` 240 节点 + FBL 三域 213 节点 | **反馈循环引擎（Vibe Coding 2.0 核心服务，index.yaml 记 status=under_construction）过半模块零入度** → FF-12 闭环回流无实体 | 高 | 否 |
| BRK-012 | FF-14 | 孤儿族（代码去重治理） | `src/zephyr/gov_code_quality/code_dedup/**`（52 件） | 同 BRK-007 → `zephyr.gov_code_quality.code_dedup: 52` | CloneGuard 之外的去重治理族未接（注意：`clone_guard` 13 .py 是活链，二者不同物） | 中 | 否 |
| BRK-013 | FF-14 | 孤儿族（回滚恢复） | `src/zephyr/infrastructure/rollback/**`（39 件） | 同 BRK-007 → `zephyr.infrastructure.rollback: 39`；depgraph `D_INFRA_RECOVERY` 100 节点 | 回滚恢复域（L0_infrastructure）大面积未接 → 破坏性 DB 操作"可逆性"保障纸面 | 中 | 否 |
| BRK-014 | FF-15 | 孤儿族（A2A 通信） | `src/zephyr/infrastructure/a2a_protocol/**`（33 件） | 同 BRK-007 → `zephyr.infrastructure.a2a_protocol: 33`；depgraph `D_INFRA_A2A` 135 节点 | Agent 间通信协议层未接 → 多 Agent 协作（CC_14 投票优先）无传输底座 | 中 | 否 |
| BRK-015 | FF-14 | 孤儿族（容量保障） | `src/zephyr/infrastructure/capacity_assurance/**`（8 件 suspect_orphan） | GOMAP `families.L2_resource` → `budget_forecaster.py` / `cross_module_integration.py` / `host_resource_governor.py` / `risk_mitigation.py` / `schema.py` / `sli_instrumentation.py` / `tech_stack.py` / `contracts/__init__.py` 全 wiring=suspect_orphan | 容量保障整族未接 → 与 BRK-066"系统级内存水位无人看护"同根因 | 中 | 否 |
| BRK-016 | FF-09 | 孤儿模块（持仓对账器） | `src/zephyr/position/position_reconciler.py` | GOMAP `families.L5_selfheal` → `{'module': 'zephyr.position.position_reconciler', 'path': 'src/zephyr/position/position_reconciler.py', 'wiring': 'suspect_orphan', 'maturity': 'production'}` | **持仓对账器 maturity=production 却零消费方** → 系统持仓视图与券商实际不一致无自动纠偏（battle_map §14"盘中持仓对账机制"纸面） | 高 | 否 |
| BRK-017 | FF-12 | 孤儿模块（对账循环） | `src/zephyr/orchestrator/execution/reconciliation_loop.py` | GOMAP `families.L5_selfheal` → wiring=suspect_orphan, maturity=production | 对账循环未接 → FF-11→FF-12 链路断（与 BRK-020 `build_execution_report` 零调用同链） | 高 | 否 |
| BRK-018 | FF-12 | 孤儿模块（蓝图-代码对账器） | `src/zephyr/feedback_loop/gates/blueprint_code_reconciler.py` | GOMAP `families.L5_selfheal` → wiring=suspect_orphan, maturity=production | 蓝图↔代码漂移无自动对账（BRK-005 正是此类漂移的实例） | 中 | 否 |
| BRK-019 | 全局 | 反向孤儿（文档侧·干净） | `scripts/governance/d8_doc_sync/algo_flow_reverse_orphan_reconciler.py` | `python scripts/governance/d8_doc_sync/algo_flow_reverse_orphan_reconciler.py --json`（argparse 实测：`--json/--apply/--mirror/--owner-ruling/--session/--no-commit/--dry-run`）→ `{"scanned": 3231, "orphans": [], "broken_source_references": [], "unreadable": 2}` | **文档侧反向孤儿 = 0（本项为阴性证据，非断点）**；残留 `unreadable: 2` 待查（文件不可读，未纳入普查面） | 低 | 否 |

## B. 零调用函数 / 建了没接的生产者（7 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-020 | FF-11 | 零调用生产者 | `src/zephyr/ex_core/execution_report.py:65`（`def build_execution_report(`）、`:128`（`__all__` 导出） | `grep -rn "build_execution_report" --include=*.py src scripts` → 仅 3 命中：定义处 `execution_report.py:65`、导出处 `:128`、以及 `src/zephyr/shared/contracts/execution_report_contract.py:24` 的 **docstring 文字引用**。零 import、零调用 | 执行报告契约有定义、有契约文档，但**无人生产** → FF-12 对账/归因无输入 | 低 | 否 |
| BRK-021 | FF-10 | 零消费技能 | `src/zephyr/risk/hedge_execution_skill.py`（MOD-RK-042，头部 `# [BLUEPRINT]` L1 / `# [MODULE]` L2 / `# [TESTS] tests/risk/test_hedge_execution_skill.py` L14 / `# [ALGO_FLOW]` L31） | `grep -rn "hedge_execution_skill" --include=*.py src scripts` → 5 命中**全部为该文件自身头部注释**；无任何外部 import | 对冲执行技能已建（含测试+algo_flow）但**零消费** → 对冲链未接进 FF-10 风控/FF-09 仓位 | 低 | 否 |
| BRK-022 | FF-01 | 零调用写入器（登记表已陈旧） | `src/zephyr/intelligence/nightly_sentiment_window.py:195`（`def compute_nightly_sentiment(`）、`:446`（自调用）、`:469`（`__all__`）；`scripts/data/run_nightly_sentiment.py:41-42` | `known_data_gaps.yaml` id=`news_sentiment_window_no_scheduler_wiring` status=**monitoring** 记"写入器 MOD-INT-NEWS-NIGHT compute_nightly_sentiment **全仓零生产调用方**…该夜间批未接线"；但实测 `grep -rn compute_nightly_sentiment` 命中 `scripts/data/run_nightly_sentiment.py:41-42` **已接**，且 `schedule.yaml` 已有 `nightly_sentiment: {cron: "20 8 * * *", description: "compute_nightly_sentiment 日频接线"}` | **缺口台账陈旧未闭合** —— 已修好但仍挂 monitoring，会误导后续派工 | 低 | 否 |
| BRK-023 | FF-06 | 生产者全设计态 | depgraph `decision_nodes`（213 行） | `python .runtime/tmp/ff-mine0/dg_census.py` → `decision_build: [["planned","213"]]`（production/stable/generated = **0**） | 第五全景图 decisiongraph 的**节点层 100% planned** → 213 个决策点无一落地 | 高 | 否 |
| BRK-024 | FF-06 | 决策层占位 | depgraph `decision_layers`（1754 行） | `dg_agg.py` → `decision_layer_track_counts: [['placeholder','1744'], ['model_driven','10']]`；真实层仅 L0/L1/L2A/L2B/L2C/L2D/L3/L4/L5/L6 十行，其中 L2A/L2B/L2C/L2D/L5/L6 六层 `design_maturity=design, build_status=planned` | **1744/1754 = 99.4% 是 ARCH-056 占位记录**；真实决策链 10 层中 6 层未建（信号层/主力行为层/大盘预测层/知识图谱因果层/学习层/自评估层） | 高 | 否 |
| BRK-025 | FF-03 | 生产者未建（策略工厂断链） | `config/strategy_production_map.yaml` §nodes `FAC-E7` | `python -c "import yaml;[print(n) for n in yaml.safe_load(open('config/strategy_production_map.yaml',encoding='utf-8'))['nodes'] if n['node_id']=='FAC-E7']"` → `{'node_id':'FAC-E7','name_zh':'模拟盘前哨','stage':'E7','node_type':'stage','build_status':'pending','module_ref':None}` | **E7 模拟盘前哨 build_status=pending 且 module_ref=None** → E6 入库监控 与 E8 组装资金分配 之间断链，策略无"模拟盘前哨"关卡 | 高 | 否 |
| BRK-026 | FF-03 | 生产者大面积 partial | `config/strategy_production_map.yaml` §nodes（16 节点） | 同脚本遍历 → build_status 分布：`built` **4**（FAC-E1A 车道A社区货源 / FAC-E4 考试咽喉 / FAC-E5 协同去重 / FAC-E6 入库监控）、`partial` **11**（E0/E1/E1B/E1C/E1D/E1E/E1G/E2/E3/E8/E9）、`pending` **1**（E7） | **策略工厂 16 节点中仅 4 建成（25%）**；六车道进货中 5 车道 partial，E8 组装与资金分配 partial → FF-03 是全项目完工率最低的环节之一 | 高 | 否 |

## C. 空表 / 无数据 / 数据腐败（20 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-027 | FF-01 | 空表（源退役） | `c1_market.edb_data` | `known_data_gaps.yaml` id=`edb_data_ifind_quota_exhausted` gap_type=**empty_table** status=accepted → "iFind EDB 月度配额耗尽(-4318)，edb_data 表至今 **0 行**；iFind 已于 2026-08-14 退役" | EDB 宏观经济数据库表零行，下游宏观因子（FF-06 L2C 大盘预测）无输入 | 中 | 否（换源需选型，非资金/production 门位） |
| BRK-028 | FF-01 | 空表（永久断源） | `c1_market.hk_connect_flow` | id=`hk_connect_flow_hkex_disclosure_retirement` gap_type=empty_table status=accepted → "港交所 2024-08-19 起停止公布北向日频资金流数据（数据源停供）；表数据 2014-11-17~2024-08-16 完整（4052 行）"；另 id=`hk_connect_flow_source_discontinued` status=accepted 同表 | 北向资金日频流**永久断源**，需 Owner 注册替代外部源 | 高 | **是**（需 Owner 注册外部账号/数据源，同 MSCI DS-CAND-016 类） |
| BRK-029 | FF-01 | 空表（假通道·从未上线） | `c1_market.sector_fund_flow` | id=`sector_fund_flow_empty` gap_type=empty_table status=**mitigated** → "THS 板块资金流采集链路**从未灌入或已清空**；与 mootdx 死亡同族但表内无任何历史行，**疑采集任务从未上线**" | **典型假通道**：表在注册表在册、看起来有链路，实际从未有一行数据 | 高 | 否 |
| BRK-030 | FF-01 | 空表（死管线空挂） | `c3_fundamental.dividend` | id=`dividend_plan_miniqmt_retired` gap_type=empty_table status=**no_source** → "dividend_incremental 任务 **schedule=disabled** + source=miniqmt（9/18 miniqmt 清退同款病，与 A4 股东户数断供同根因——源退役后任务空挂）" | 分红配股数据链死；与 BRK-050 的 `dividend_incremental` disabled 同源 | 中 | 否 |
| BRK-031 | FF-01 | 无源空挂（2 表） | `c3_fundamental.audit_opinion`、`c3_fundamental.rights_issue` | id=`audit_opinion_no_interface` status=accepted → "AKShare 暂无专用批量审计意见接口…任务已 disabled"；id=`rights_issue_interface_removed` status=accepted → "akshare 1.18+ 已移除 stock_rights_issue_detail_sina 批量接口" | 审计意见 / 配股两张表无源，任务 disabled 空挂 | 中 | 否 |
| BRK-032 | FF-01 | 权限缺口（降级运行中） | `c1_market.l2_tick` | id=`l2_tick_permission_required` gap_type=**permission_required** status=accepted → "Level-2 逐笔行情需付费 L2 行情权限（miniQMT get_l2_quote），**用户尚未开通**。任务已 disabled，fallback_sources 降级到 get_full_tick 五档快照写入" | L2 逐笔链降级为五档快照 → 主力行为层（TDM L2B）精度受损 | 中 | **是**（需 Owner 开通付费权限） |
| BRK-033 | FF-01 | 上游接口失效（3 条） | `c1_market.northbound_hold_snapshot` | id=`akshare_hsgt_hold_stock_em_broken` / `akshare_hsgt_board_rank_em_broken` / `akshare_hsgt_stock_statistics_em_broken`，三条均 gap_type=**interface_broken** status=accepted → "akshare 1.18.75 … 实测返回 NoneType（东财网页改版致接口失效）" | 北向持仓快照三个接口同时失效 → 该表无有效来源 | 中 | 否 |
| BRK-034 | FF-01 | 数据腐败（同键双行） | `c1_market.index_valuation_daily` | id=`index_valuation_daily_duplicate_rows` gap_type=**upstream_data_corruption** status=**monitoring** → "同一 (trade_date, symbol) 存在双行：原始 akshare_csindex 行（仅 pe_ttm，cape 全空）与 CAPE 回填批行并存（000300 cape_5y_pct NULL 5138 行）" | 指数估值双行 → 下游估值因子（FF-06）读到不确定行；违反 RULE-DATA-OPS 判重纪律 | 高 | 否 |
| BRK-035 | FF-01 | 数据腐败（撞码） | `c1_market.northbound_hold_snapshot` | id=`tushare_hk_hold_2026q2_code_collision` gap_type=upstream_data_corruption status=**monitoring** → "tushare hk_hold(trade_date=20260630) 响应中 **243 组 ts_code 撞码**（SH 139 + SZ 104）：多只证券被赋予雷同假 code 并映射到合法 ts_code" | 假 code 被映射成合法 ts_code → 静默污染，最难发现的一类腐败 | 高 | 否 |
| BRK-036 | FF-01 | 时区劈叉（4.12 亿行） | `c1_market.kline_etf_{1min,5min,15min,30min,60min}` | id=`etf_minute_tz_split_pre_202607` gap_type=**field_missing** status=**monitoring** → "2026-06-30 及以前写入侧按 UTC 墙钟落 trade_time（日内小时∈[1,7]），列类型却为 Asia/Shanghai；2026-07-01 起写入侧改北京墙钟（∈[9,15]）。全族五表 **~4.12 亿行**" | ETF 分钟线全族时间轴劈叉 → 分钟级回测（FF-04）与盘中信号（FF-06）时间对齐错；修复脚本 `scripts/ch/repair_etf_minute_tz_split.py`（instL 车道独占） | 高 | **是**（资金破坏性 `--execute` 需低峰窗 + Owner 批，COORDINATION_LEDGER §7 明列） |
| BRK-037 | FF-01 | 死源（通道整体断供） | `c1_market.kline_sector_intraday` | id=`kline_sector_intraday_tdx_dead` gap_type=**source_discontinued** status=accepted → "mootdx/tdx 板块盘中分钟通道 **2026-09-11 起整体断供**（data_source 记为 'tdx'）。最后一波 09-10 本身已是残缺半日：727→563 个 code、295983→37857 行" | 板块盘中分钟线死 → `intraday_sector` 档期（5 任务，cron `*/5 9-15 * * 0-4`，mootdx TCP 直连）空转 | 高 | 否 |
| BRK-038 | FF-01 | 死源（议息日历） | `c1_market.rate_decision_calendar` | id=`rate_decision_calendar_jin10_retired` status=**no_source** → "akshare macro_bank_*（金十口径）11 央行议息接口源端 2025-10 起退役（E12 波1 决定性发现，退役波及面登记 10_data_source_candidates DS-CAND-021）" | 议息日历断源；但 `data_supply_sentinel.yaml` 仍对其设 `max_lag_days: 2, past_only: false` → **哨兵必然天天告警**（噪音） | 中 | 否 |
| BRK-039 | FF-01 | 死源（仓单归档 404） | `c1_market.futures_warehouse_receipt` | id=`futures_warehouse_receipt_shfe_archive_cap` status=**no_source** → "SHFE 官网仓单归档接口自 2025-12 起 404（ak.futures_shfe_warehouse_receipt 实测 2026-09-18 复现），provider 归档窗口硬界" | 期货仓单历史归档不可回补；2 件 backfill 任务 disabled（BRK-050） | 中 | 否 |
| BRK-040 | FF-01 | 半死管线（任务活源死） | `c1_market.alt_sz_reservoir_level` | id=`reservoir_level_source_stale` status=**monitoring** → "采集任务活（max(ingest_ts)=2026-09-17 仍逐日跑）但**源端数据停于 2026-07-31（lag=49d）**，7579 万行存量无损——疑似省水文平台停更/接口换版。属『任务活着但拉不到新数据』型半死管线" | 最隐蔽的一类断点：调度成功、日志无错、数据不更新 | 中 | 否 |
| BRK-041 | FF-01 | 结构性缺口（竞价窗口） | `c1_market.auction_snapshot` | id=`auction_window_pre_202606_gap` status=**no_source** → "竞价窗口(9:15-9:25)数据仅 live 采集产生：miniqmt 时代只有'采集链 09:15 前活着'的日期有货（2026-06 起共 40 密集日；**盘后回补通道不含盘前时段=结构性缺口**）" | 集合竞价历史无法回补 → `auction_highfreq` 档期（cron `*/10 15-25 9 * * 0-4`）产出的数据只有 40 天，FF-06 竞价类信号（`auction_microstructure_analyzer`）样本不足 | 中 | 否 |
| BRK-042 | FF-01 | 开放缺口（票房全族死） | `c1_market.alt_movie_boxoffice` | id=`alt_movie_boxoffice_source_dead` gap_type=permission_required status=**open** → "akshare 票房全家桶（movie_boxoffice_daily/cinema_daily/monthly/weekly/yearly/realtime）均走艺恩 ys.endata.cn 接口，2026-09-…" | 票房另类数据 6 个接口全族死；**status=open 未处置** | 中 | 否 |
| BRK-043 | FF-01 | 重开缺口（估值日表缺日） | `c1_market.daily_valuation` | id=`daily_valuation_2026_09_10_missing` status=**reopened** → "①09-09：daily_valuation_full_refresh（akshare）耗时 39564s(**11h**) 仅写 44687 行（约 8 日量），刷到 09-09 时只落 4875/5558 即结束=**部分写入**" | 已 closed 的缺口**复发**；11h 全量刷新部分写入 → 无幂等/无断点续跑 | 高 | 否 |
| BRK-044 | FF-01 | 缺口台账总量 | `src/zephyr/data/config/known_data_gaps.yaml`（44 条 gaps，`last_updated: 2026-09-18`） | `python -c "import yaml,collections;g=yaml.safe_load(open('src/zephyr/data/config/known_data_gaps.yaml',encoding='utf-8'))['gaps'];print(len(g),collections.Counter(x['status'] for x in g),collections.Counter(x['gap_type'] for x in g))"` → `44` / `{'completed':5,'accepted':15,'resolved':6,'monitoring':7,'open':2,'reopened':1,'mitigated':3,'no_source':5}` / `{'date_range':16,'empty_table':4,'source_stale':2,'source_discontinued':2,'no_data_source':2,'permission_required':2,'interface_broken':3,'empty_column':1,'upstream_data_corruption':6,'pipeline_idempotency':1,'source_degraded':1,'universe_missing':1,'universe_shrinking':1,'field_missing':2}` | **非闭合 33 条**（accepted 15 + monitoring 7 + no_source 5 + mitigated 3 + open 2 + reopened 1）；14 种缺口类型；`empty_table` 4 张 + `source_discontinued` 2 + `interface_broken` 3 = 9 处彻底断源 | 高 | 否 |
| BRK-045 | FF-04 | 字段缺失（回测归因链断） | `data/backtest_artifacts/bt-*.json`（c1_backtest trade_log，60 件） | id=`bt_trade_log_attribution_fields_missing` gap_type=**field_missing** status=**no_source** → "回测引擎 TradeRecord 只落 order_type='market' 常量且无 algo_id 归因字段——**60 件 bt-*.json 全量核查（2026-09-18）：order_type 无一非 market**" | 回测产物无算法归因维度 → FF-12 绩效归因引擎（TDM `TDM-F-C3-01` 多维归因）拿不到 algo_id，回测↔实盘同构性无法验证 | 高 | 否 |
| BRK-046 | FF-01 | 哨兵 fail-open 白名单 | `src/zephyr/data/config/data_supply_sentinel.yaml` §tables | `python -c "import yaml;t=yaml.safe_load(open('src/zephyr/data/config/data_supply_sentinel.yaml',encoding='utf-8'))['tables'];print(len(t),[x['table'] for x in t if x.get('allow_empty')])"` → 表清单中 `c1_market.hl_perp_snapshot_daily` / `hl_oi_snapshot_daily` / `hl_funding_history` / `hl_liquidation_raw` / `market_china_bond_yield` / `market_fund_flow_daily` 等带 **`allow_empty: true`** | 断供哨兵对白名单表**空表放行** → 这些表可以永远 0 行而不告警（与 BRK-029 `sector_fund_flow` 从未灌数同型风险） | 中 | 否 |

## D. 假通道 / fail-open 放行点（9 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-047 | 全局 | fail-open 面（总量） | `src/zephyr/**/*.py`（215 文件 / 1405 处） | Grep `fail_open\|fail-open\|FAIL_OPEN` glob=`*.py` path=`src/zephyr` output_mode=count → `Found 1405 total occurrences across 215 files`；Top 命中 = `ex_core/daban_load_producer.py` 18、`plan_engine/scenario_plan_recorder.py` 17、`plan_engine/daily_warroom_pipeline.py` 15、`plan_engine/scenario_probability_model.py` 14、`data_governance/core/runtime_lineage_collector.py` 12、`intelligence/nightly_sentiment_window.py` 11、`governance/audit/translation_coverage_reconciler.py` 11、`signal_ashare/futures_basis_monitor.py` 11 | fail-open 是全仓**普遍设计模式**（1405 处）；多数应为有意降级，但无统一登记册可机械区分"设计意图 fail-open"与"偷懒 fail-open" | 高 | 否 |
| BRK-048 | 全局 | 硬编码 fail-open 开关 | `src/zephyr/**/*.py`（5 处精确） | `grep -rn "fail_open\s*=\s*True\|fail_open: bool = True\|FAIL_OPEN" --include=*.py src scripts \| wc -l` → **5** | 5 处默认放行为 True 的开关，需逐处判是否应为 fail-closed（宪法 §9.3 永久系统四要素 / 门禁只许加严 裁定#321） | 中 | 否 |
| BRK-049 | 全局 | 吞异常（静默失败面） | `src/zephyr/**/*.py`（裸 62 处 / pass 144 处） | `grep -rn "except Exception:\s*$" --include=*.py src/zephyr \| wc -l` → **62**；`grep -rnA1 "except Exception" --include=*.py src/zephyr \| grep -c "pass$"` → **144** | **144 处"捕获后 pass"** = 管线"看起来在跑"但错误被吞 → 这是"全部管线灌水"判据最大的假象来源（与 BRK-040 半死管线同族） | 高 | 否 |
| BRK-050 | FF-01 | 任务 DAG 缺失 | `src/zephyr/data/config/tasks.yaml`（262 任务） | `python -c "import yaml;t=yaml.safe_load(open('src/zephyr/data/config/tasks.yaml',encoding='utf-8'))['tasks'];print(len(t),sum(1 for x in t if not x.get('dependencies')))"` → `262 235` | **235/262 = 89.7% 任务未声明 dependencies** → 任务间先后顺序无机械保障，`catchup_guard` 补跑无法按依赖拓扑重放 | 高 | 否 |
| BRK-051 | FF-01 | 死任务空挂（4 件） | `src/zephyr/data/config/tasks.yaml` §tasks `schedule: disabled` | `python -c "import yaml;t=yaml.safe_load(open('src/zephyr/data/config/tasks.yaml',encoding='utf-8'))['tasks'];print([x['task_id'] for x in t if str(x.get('schedule')).lower()=='disabled'])"` → `['dividend_incremental', 'futures_warehouse_receipt_backfill_czce', 'futures_warehouse_receipt_backfill_shfe', 'road_freight_index_full_refresh']` | 4 件任务在册但 schedule=disabled → 注册表有、调度无（对应 BRK-030 / BRK-039 空表） | 中 | 否 |
| BRK-052 | FF-16 | 告警不自动升级 | `config/flags.yaml` §flags.alerts | `python -c "import yaml,json;print(json.dumps(yaml.safe_load(open('config/flags.yaml',encoding='utf-8'))['flags']['alerts'],ensure_ascii=False))"` → `{"enabled": true, "description": "告警规则评估 (基础条件解析可用，Multi-Window 未实现)", "auto_escalation": false}` | **`auto_escalation=false` + Multi-Window 未实现** → 告警只评估不升级；配合 BRK-055（webhook 凭据缺）= 告警链两端都断 | 高 | 否 |
| BRK-053 | FF-16 | 遥测归档未实现（保留 flag） | `config/flags.yaml` §flags.archive | 同上 → `{"enabled": false, "description": "遥测数据归档 (未实现，保留flag)", "retention_days": 30, "compression": "gzip", "implementation_status": "not_started"}` | **flag 在册、实现 not_started** = 教科书式假通道；遥测数据无归档 → FF-12 长周期归因无历史 | 中 | 否 |
| BRK-054 | FF-16 | Schema 校验宽松 + 无死信队列 | `config/flags.yaml` §flags.schema_validation | 同上 → `{"enabled": true, "description": "Schema校验 (YAML 加载可用，runtime drift 检测未实现)", "strict_mode": false, "dlq_enabled": false}` | **`strict_mode=false` + `dlq_enabled=false` + runtime drift 未实现** → 坏数据无死信队列拦截，直接放行入库（BRK-034/035 数据腐败能落库的结构性原因） | 高 | 否 |
| BRK-055 | FF-16 | 告警推送凭据缺失 | `docs/_working/fullflow_campaign/COORDINATION_LEDGER.md` §7 | 该文件 §7 Owner 门位清单明列："需 Owner 提供凭据（**E7 告警推送 webhook 四类凭据**）"；同 §7 亦记"注：门位项登记 = 已闭环，不算未完成" | 告警推送通道无凭据 → 无法外发（微信/邮件/webhook 四类全缺）；FF-16→人 的最后一米断 | 高 | **是** |

## E. 死管线 / 退役未清（12 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-056 | 全局 | 弃用节点未清 | depgraph `nodes` build_status | `python .runtime/tmp/ff-mine0/dg_agg.py` → `nodes_status: [['generated',8711],['production',2800],['stable',316],['deprecated',93],['planned',68],['testing',7]]` | **93 个 deprecated 节点仍在 nodes 表**（退役未清）；另 `generated` 8711 = 72.6% 节点是生成态（占位/派生），`production` 仅 2800 = 23.3% | 中 | 否（注册表净删 = Owner 门位，此处仅登记不执行） |
| BRK-057 | 全局 | 设计态悬挂 | depgraph `nodes` build_status | 同 BRK-056 → `planned 68 + testing 7 = 75` | 75 个节点长期停在 planned/testing（含 `nodes_archive_module_lifecycle` 归档表存在 → 有归档机制但未用尽） | 中 | 否 |
| BRK-058 | FF-01 | 数据流图零生产作业 | depgraph `dataflow_jobs`（196 行） | `dg_census.py` → `dataflow_job_status: [['module_placeholder','stable',10],['job','generated',24],['module_placeholder','production',9],['job','planned',51],['module_placeholder','generated',14],['module_placeholder','testing',7],['module_placeholder','deprecated',24],['module_placeholder','planned',57]]` | **196 行中 121 行（61.7%）是 `entity_type='module_placeholder'`（其中 deprecated 24）**；真实 `job` 仅 75 行，且 `planned 51 + generated 24`，**production/stable = 0** → 第三全景图数据流**零生产作业** | 高 | 否 |
| BRK-059 | FF-01 | 数据流图三张空表 | depgraph `dataflow_runs` / `dataflow_datasets_metadata` / `dataflow_jobs_metadata` | `docs/02_enterprise_architecture/00_overview_entry/panorama_registry.md` §数据库真源健康度表 → `dataflow_datasets_metadata` **0 行**（"0=未填，AI 查 dataflow 会幻觉物理类型"）、`dataflow_jobs_metadata` **0 行**（"0=未填，AI 查 job 找不到源码"）、`dataflow_runs` **0 行**（"0=无运行时观测，依赖观测系统回填"） | **三张表全空**：①无运行时观测 → "全部管线灌水"无法机械证明；②AI 查 job/dataset 必幻觉（源码引用与物理类型均缺失） | 高 | 否 |
| BRK-060 | 全局 | 接口契约几乎未登记 | depgraph `interface_contracts`（5 行） | 同 panorama_registry §数据库真源健康度 → `interface_contracts` **5 行**，备注"仅 5=大部分模块接口未登记，**AI 会幻觉函数名**" | 全项目 3477 个模块只有 5 条接口契约 → 模块间 API 面无真源，跨环节接线只能靠 grep 猜 | 高 | 否 |
| BRK-061 | 全局 | 数据集零生产态 | depgraph `dataflow_datasets`（76 行） | `dg_agg.py` → `dataflow_datasets_status: [['planned',51],['generated',25]]` | **76 个数据集中 production = 0**（planned 51 + generated 25）→ 数据"货物"无一处于生产态，与 BRK-058 互为因果 | 高 | 否 |
| BRK-062 | 全局 | 全景图产物缺件（3 件） | `docs/02_enterprise_architecture/00_overview_entry/panorama_registry.md` §已建全景图清单 | 该表实测：`已建产物存在 19/22`；3 件 ❌缺失 = **PAN-BUILT-17**（依赖与路径全景图能力定位书，生成器"(已删除，不再维护)"）、**PAN-BUILT-21**（交易决策架构视图，"(已退役删除) generate_trading_flow_diagram.py"）、**PAN-BUILT-16**（架构债务注册表，输出路径 `_archive/`，"(手工维护)"） | 索引指向空气：注册表声称"已建"但产物不存在 → 违反宪法 §9.5"静态清单禁手工维护" | 中 | 否 |
| BRK-063 | 全局 | 全景图待建（16 项） | 同 panorama_registry §待建全景图清单 | 实测 `已建 22 / 待建 16 / 总数 38 / 已建覆盖率 57.9%`；待建 = 08_asset_panorama 4 项（PAN-ASSET-01..04，优先级**高**）+ 11_risk_panorama 1 项（PAN-RISK-01，**高**）+ 09_runtime_panorama 4 项（PAN-RUN-01..04，中）+ 10_security_panorama 2 项（PAN-SEC-01/02，中）+ 13_visualization 1 项 + 12_quant 2 项（可选） | **5 项高优先级全景图未建**（资产CMDB / API契约目录 / 数据目录 / 数据血缘 / 风险敞口）→ 全流通验收缺"资产与血缘"视角的机械证据 | 中 | 否 |
| BRK-064 | FF-10 | 熔断族重复未收敛 | `config/governance_operations_map.yaml` §pipeline GOM-L3.disconnected | GOM-L3 `disconnected` 4 项中 → `"KillSwitch×5/CircuitBreaker×9 重复族 | R4 收敛战役——活链 KS=2(access_control/rollback)、CB=4(shared.resilience/resilience_governance/rule_enforcement/adversarial_validation)；死员已删(reliability CB/fa…)"`；同层另 2 项已确认删除：`zephyr.orchestrator.fault_tolerance.degrade_cascade`（"已删（2026-09-15 治理减法批 A5 收官 9deb35e709 确认死码删除）"）、GOM-L1 `zephyr.infrastructure.health_monitor.health_aggregator`（"已删 CONFIRMED-DEAD"）、GOM-L4 `zephyr.governance.security_governance.ghost_scan`（"已删 R4 与 reaper scan_ghost_windows 重复"） | KillSwitch 5 个实现只有 2 个是活链、CircuitBreaker 9 个只有 4 个是活链 → **熔断入口不唯一**，紧急停机可能走错分支 | 中 | 否 |
| BRK-065 | FF-15 | 双轨孵化入口 | `config/governance_operations_map.yaml` §pipeline GOM-L0.disconnected | GOM-L0 → `"双轨并存 | process_pool 与 process_lifecycle_gateway 两套孵化入口(M1 合并候选,见挖矿地图 §5)"`；GOM-L0 mounts 8 件含 `zephyr.shared.infra.process_pool` + `zephyr.shared.infra.process_lifecycle_gateway` + `zephyr.infrastructure.process_supervisor` + `zephyr.trading.windows_service` + `zephyr.shared.lifecycle.daemon_registry` + `zephyr.trading.boot_hooks` + `zephyr.infrastructure.runtime.startup_shutdown` + `zephyr.trading.auto_runtime_core._OllamaProcessManager` | **进程出生入口 8 个 mount、2 套并行体系** → 违反宪法 §9.3"永久系统四要素·自动触发"的唯一入口原则；reaper 看护面可能漏 | 中 | 否 |
| BRK-066 | FF-14 | 系统级看护空白 | `config/governance_operations_map.yaml` §pipeline GOM-L2.disconnected | GOM-L2 → `"系统级看护空白 | 无人看护全系统提交内存水位(M2 门禁候选;resource_guard 只护 gov_drift,L5 只护 LLM 成本)"`；GOM-L2 mounts 仅 5 件（全环节最少之一） | 全系统内存水位无看护 → CONSTRUCTION_DISCIPLINE §6 记"压测 worker 50 曾三度压死宿主"的根因未闭合 | 中 | 否 |
| BRK-067 | FF-01 | 双任务并发互踩 | `src/zephyr/data/config/tasks.yaml` `kline_sector_880_incremental` + `kline_sector_incremental` | `known_data_gaps.yaml` id=`kline_sector_concept_shrinking` gap_type=**universe_shrinking** status=mitigated → "两任务 **daily_kline 调度同时触发，并发竞争 tqcenter SDK 单例（通达信插件，非线程安全）**"；佐证：`schedule.yaml` `daily_kline` cron `30 16 * * 0-4` executor=**heavy**，`intraday_sector` 描述明记"mootdx TCP直连，独立于miniqmt慢任务" | 同一档期两任务争非线程安全 SDK 单例 → 标的池收缩（universe_shrinking）；BRK-050 的 DAG 缺失（235/262 无 dependencies）是同类根因 | 中 | 否 |

## F. 无锚点环节 BM-INV-001（8 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-068 | FF-02 | 无锚点环节（15 件·全项目最大簇） | depgraph `battle_map_steps` LEFT JOIN `battle_map_anchors`，flow_stage='research_incubation' | `python .runtime/tmp/ff-mine0/dg_bm.py` → NO-ANCHOR 15 件：`BM-RES-03` 假设管理与研究发现沉淀、`BM-RES-03-A` 假设生命周期管理、`BM-RES-03-B` 研究发现知识库、`BM-RES-03-C` 研究目录与搜索引擎、`BM-RES-04` 研究工作流编排、`BM-RES-04-A` DAG编排与任务调度、`BM-RES-05-A` Notebook集成与一键转生产、`BM-RES-05-B` 研究协作中心、`BM-RES-05-C` 研究信息隔离墙、`BM-RES-06-A` LLM研究助手、`BM-RES-06-B` 论文追踪、`BM-RES-07-A` 策略进化与因子挖掘、`BM-RES-08-A` 知识清洗流水线、`BM-RES-09-A` 知识类型分类体系、`BM-RES-10-A` 模块工厂架构、`BM-RES-11-A` 采集源分类与调度 | **研究孵化 34 环节中 15 件（44%）无任何承载模块** → 策略/因子"诞生地"整段纸面；直接导致 FF-03 策略工厂 E1 六车道 5 条 partial（BRK-026） | 高 | 否 |
| BRK-069 | FF-04 | 无锚点环节（4 件·偏差归因子链） | 同上，flow_stage='backtest_validation' | `dg_bm.py` → `BM-BT-05-H-A` 滑点偏差归因、`BM-BT-05-H-B` 数据滞后偏差归因、`BM-BT-05-H-C` 前瞻偏差残留归因、`BM-BT-05-H-D` 延迟差异偏差归因 | 回测↔实盘**偏差归因四件套全无承载模块** → 无法解释 sim↔live divergence（CC_06 四模式开关的 shadow 模式核心度量） | 中 | 否 |
| BRK-070 | FF-07 | 无锚点环节（1 真缺 + 2 退役占位） | 同上，flow_stage='buy_flow' | `dg_bm.py` → `BM-BUY-06` **外部指令盯盘**（真实缺失）；`BM-BUY-05` / `BM-BUY-14` = "编号退役占位（历史断档，禁止复用）" | 外部指令盯盘无承载（人工指令轨 human_override 的盘中落点缺）；2 个编号永久断档 | 中 | 否 |
| BRK-071 | FF-03 | 无锚点环节（2 件） | 同上，flow_stage='model_training' | `dg_bm.py` → `BM-MT-03` **AutoML与超参优化**、`BM-MT-04` **因子发现与因果发现** | 训练域两大能力无承载模块；与 strategy_production_map `FAC-E1C` 公式挖掘机 / `FAC-E1E` 模型基线（均 partial）同链 | 中 | 否 |
| BRK-072 | FF-10 | 无锚点环节（1 件·黑天鹅） | 同上，flow_stage='risk_control' | `dg_bm.py` → `BM-RC-12` **极端事件与黑天鹅** | 风控 50 环节中唯一无锚点者恰是**压力测试/黑天鹅**——最需要的一环没建 | 高 | 否 |
| BRK-073 | FF-09 | 无锚点 + 阶段归属错位 | 同上，step_id=`BM-SEL-20`，flow_stage='position_management' | `dg_bm.py` → `NO-ANCHOR ['BM-SEL-20', '多策略交叉投票', 'position_management']` | **step_id 前缀 `BM-SEL`（选股）却挂在 `position_management`（仓位）阶段** → 违反 `battle_map_domain_policy` BM-INV-004 域漂移不变量，且无锚点。双重违例 | 中 | 否 |
| BRK-074 | 全局 | 生成器产物漂移（无锚点计数） | `docs/02_enterprise_architecture/07_trading_decision_architecture/battle_map/battle_map_panorama.md:24` | 该文档记 `| 无锚点环节（BM-INV-001） | 5 | No-Anchor Steps | 5 |`；DB 实测 `dg_bm.py` → **27 件**（`steps: 341 no_anchor: 27`） | **文档 5 vs 真源 27，漂移 22 件（5.4 倍）** → 生成器 `generate_battle_map_diagram.py` 未重跑，全项目最重要的健康指标被低估 | 高 | 否 |
| BRK-075 | 全局 | 生成器产物漂移（五态口径） | 同 `battle_map_panorama.md:21-27` | 文档记 `环节总数 341 / 流转边 114 / 锚点 588 / 运营态 151 / 设计态 157 / 弃用态 20 / 候选态 8 / 缺失态 5`；DB 实测 `dg_census.py` → `bm_state: production 200 / planned 164 / stable 147 / generated 39 / deferred 16 / candidate 11 / NULL 10 / design 1`（合计 588 ✓）、`bm_anchor_target_graph: depgraph-primary 383 / depgraph-supplement 177 / candidate-supplement 17 / candidate-primary 11` | 环节/边/锚点三个总数**吻合**（341/114/588），但**状态口径两套**：文档按 step 五态（151/157/20/8/5），DB 按 anchor 的 `status_snapshot` 八值（200/164/147/39/16/11/10/1）。两套口径无映射表 → 无法机械判"运营态"到底几个 | 中 | 否 |

## G. 横切机制未落地（3 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-076 | FF-13 | 横切机制 100% 设计态 | `docs/02_enterprise_architecture/07_trading_decision_architecture/battle_map/battle_map_12_cross_cutting.md:19-70` | 该文档 §文档基本信息 → `横切类别数 = 17`；§横切视图总览 mermaid 尾行实测 `class CC_01,CC_02,…,CC_17 design`（**17 个全部 design，production = 0**）；17 项 = CC_01 §13筛选漏斗 / CC_02 §14盘中实时事件 / CC_03 §15计算节奏时序 / CC_04 §16能力冲突矩阵仲裁 / CC_05 §1.7分布感知增强 / CC_06 四模式开关 / CC_07 应急保命降级路径 / CC_08 四轨并行架构 / CC_09 共享信号注入层 / CC_10 信号生命周期治理 / CC_11 因子治理引擎 / CC_12 硬边界约束体系 / CC_13 因子直通层 / CC_14 投票优先多Agent协作 / CC_15 事件溯源与配置中心 / CC_16 外部对标清单 / CC_17 模型量化 | **系统级横切机制 17/17 零落地** → 这是"全流通"最深的断层面：不是某个模块没接，而是**跨环节的机制层整体不存在** | 高 | 否 |
| BRK-077 | FF-13 | 四模式开关未建 | 同 `battle_map_12_cross_cutting.md` §四模式开关（CC_06） | 该节实测文字："同一套决策代码路径，通过模式开关切换运行形态，保证 sim↔实盘同构：backtest / paper / shadow / live…**模式开关在执行层（execution flow）生效**…实盘同构铁律：回测引擎必须串入 D_POSITION + D_RISK，不跳过仓位和风控"；状态 = `class CC_06 design` | **模拟盘转正门（前任工段⑧）的机械保障不存在** —— shadow 模式（测 sim↔live divergence）未建，配合 BRK-069 偏差归因四件套无锚点 → 转正判据无量化依据 | 高 | 否 |
| BRK-078 | FF-13 | 应急保命轨纸面 | 同 `battle_map_12_cross_cutting.md` §应急保命降级路径（CC_07）+ depgraph `decision_tracks` | CC_07 实测文字："当模型/策略/信号失效时，系统逐级降级保命——每一层失效都有硬编码兜底…降级触发由 Kill Switch 熔断 + 数据健康监控驱动"，状态 `class CC_07 design`；`decision_tracks` 实测 `{'track_id':'emergency','track_name':'应急保命轨','priority':'4','activation_condition':'所有模型/策略/信号失效时'}` 已登记，但 `decision_nodes` **213 行全 planned**（BRK-023）→ 该轨无任何落地节点 | **保命轨有登记、有优先级、零节点** → 最坏情况下无兜底；叠加 BRK-004（KillSwitch 五域编排无人 import）+ BRK-064（KS 5 实现仅 2 活链）= 应急链路三重断 | 高 | 否 |

## H. 域注册表一致性（5 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-079 | 全局 | 幻影域 FK 违例 | `docs/01_policies_and_standards/_registry/catalogs/battle_map_domain_policy.yaml` §flow_stage_allowed_domains.stock_selection.allowed | 交叉验证脚本实测 → `in bm policy but NOT in depgraph (1): ['D_SIGNAL']`（policy 的 `stock_selection.allowed` 列表含 `D_SIGNAL`，而 `architecture_model/index.yaml` §domains 75 域中无 `D_SIGNAL`；`D_SIGNAL` 仅存在于 FDR 且 subdomain=`signal_legacy`） | 作战地图域白名单引用不存在的域 → BM-INV-004 域漂移检查对 `D_SIGNAL` 永远空转（GATE-DOMAIN-FK 同类风险） | 中 | 否 |
| BRK-080 | 全局 | 域覆盖缺口（34/75 域） | 同 `battle_map_domain_policy.yaml` §flow_stage_allowed_domains（11 阶段 allowed 并集） | 交叉验证脚本实测 → `domains allowed in some flow_stage: 42`；`depgraph domains never allowed in ANY flow_stage (34): D_ARCHIVE_SCRIPTS D_ARCH_GUARD D_ARCH_SCRIPTS D_AUDITTEST D_AUTONOMY_CORE D_AUTONOMY_PERM D_CODE_SCRIPTS D_COMPLIANCE_SCRIPTS D_CONTRACTS D_DATA_SCRIPTS D_FRONTEND D_GOVERNANCE D_GOV_AUDIT D_GOV_CODE_QUALITY D_GOV_DOCS D_GOV_DRIFT D_GOV_ENFORCEMENT D_GOV_OPS_RESILIENCE D_GOV_REPAIR D_GOV_RULE D_GOV_SCRIPTS D_INFRASTRUCTURE D_INFRA_A2A D_INFRA_OPS D_INFRA_RECOVERY D_INFRA_TELEMETRY D_INTEGRATION_GATEWAY D_META_SCRIPTS D_REGIME D_SECURITY_LLM D_SEC_SCRIPTS D_SIGLEGACY D_STRUCT_SCRIPTS D_TEST` | **作战地图 11 阶段结构性只覆盖 42/75 域（56%）** → 34 域（含 D_AUTONOMY_CORE 512 节点、D_FRONTEND 79、D_INFRA_RUNTIME 除外的全部治理/脚本/AI 域）在 battle_map 体系中**无环节归属**，是本次骨架必须新增 FF-13..FF-16 的机械依据 | 高 | 否 |
| BRK-081 | 全局 | 域分类缺口（19 域） | 同 `battle_map_domain_policy.yaml` §domain_classification | 实测 `business_domains` 38 + `tool_domains` 19 = 57；`in depgraph but NOT in battle_map policy (19): D_ARCHIVE_SCRIPTS D_ARCH_GUARD D_ARCH_SCRIPTS D_AUDITTEST D_AUTONOMY_PERM D_CODE_SCRIPTS D_COMPLIANCE_SCRIPTS D_CONTRACTS D_DATA_SCRIPTS D_INFRA_OPS D_INFRA_TELEMETRY D_INTEGRATION_GATEWAY D_META_SCRIPTS D_PLAN D_SECURITY_LLM D_SEC_SCRIPTS D_SIGLEGACY D_STRUCT_SCRIPTS D_TEST` | 19 域未被 business/tool 二分裁定 → BM-INV-004 的 `target_role`（primary 严查 / supplement 豁免）对这些域无法判定。注：`D_PLAN`(41节点) 虽未分类但出现在 `position_management.allowed`，属**分类册与白名单册自相矛盾** | 中 | 否 |
| BRK-082 | 全局 | FDR ↔ depgraph 双向漂移 | `docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml`（83 entries）vs `architecture_model/index.yaml` §domains（75） | 实测 → `FDR distinct domains: 68`；`in FDR but NOT in depgraph (4): ['D_EXECUTION','D_ORDER','D_PORTFOLIO','D_SIGNAL']`（4 者 subdomain 均为 `*_legacy`：execution_legacy / order_legacy / portfolio_legacy / signal_legacy）；`in depgraph but NOT in FDR (11): D_ARCHIVE_SCRIPTS D_ARCH_GUARD D_ARCH_SCRIPTS D_CODE_SCRIPTS D_COMPLIANCE_SCRIPTS D_CONTRACTS D_DATA_SCRIPTS D_META_SCRIPTS D_SEC_SCRIPTS D_STRUCT_SCRIPTS D_TEST` | 两本域册不同步：**4 个遗留域在 FDR 在册但 depgraph 已无**（退役未清）、**11 个脚本/测试域在 depgraph 在册但 FDR 未登记**（违反 RULE-REGISTRY：ROOR 是发现唯一真源，两册应一致） | 中 | 否 |
| BRK-083 | FF-06 | 空域（0 节点） | depgraph `D_SIGLEGACY` / `D_TEST`；`docs/03_modules/_domain_red_blue_validator/` | `project_handbook/05_trading_domains.md` §3 AUTO 域清单实测 → `D_SIGLEGACY 信号遗留设计态 L2_domain **0**`、`D_TEST Test Domain — **0**`；另 `docs/03_modules/` 目录 .md 计数实测 `_domain_red_blue_validator: 0`（**空目录**） | 2 个注册域零节点 + 1 个模块蓝图目录零文档 → 注册表/目录树中的空壳（`D_SIGLEGACY` 与 BRK-079/082 的 `D_SIGNAL` legacy 族同源） | 低 | 否 |

## I. 治理 / 注册表（2 条）

| 断点ID | 环节ID | 类型 | 精确路径:行号 | 证据（命令 + 实测输出） | 影响 | 修复难度 | Owner门位 |
|---|---|---|---|---|---|---|---|
| BRK-084 | FF-14 | ROOR 扫描未完成 + 1 册已坏 | `docs/registry_of_registries.yaml` §summary | `python -c "import yaml;print(yaml.safe_load(open('docs/registry_of_registries.yaml',encoding='utf-8'))['summary'])"` → `{'total_tiers': 3, 'total_registries': 73, 'fully_scanned': 19, 'pending_scan': 5, 'broken': 1, 'by_tier': {'tier_0_core': 11, 'tier_1_governance': 28, 'tier_2_runtime': 34}}` | **73 册中仅 19 册完成扫描（26%）、5 册待扫、1 册 broken**；另 `by_tier` 合计 11+28+34=**73** 但实测各 tier 的 `registries` 列表长度 = 12/28/34 = **74**（tier_0 元数据 11 vs 实际 12，**计数漂移 1**，违反宪法 §4.3"计数用字段不写死在散文"） | 中 | 否 |
| BRK-085 | FF-14 | 蓝图承认孤儿（待接线族） | `docs/01_policies_and_standards/_registry/catalogs/battle_map_domain_policy.yaml` §acknowledged_orphans.modules | 实测该键为 list，已读出的条目 ≥9 件（每条含 `blueprint_id`/`domain`/`reason`/`review_frequency: monthly`/`promotion_criteria`）：`MOD-PLAN-025` 盘中明日预告器（testing 已落码，消费者=作战指挥页明日预案卡，**W0 集成未做**）、`MOD-REGIME-001` 体制识别器（planned 待实现）、`MOD-REGIME-002` 体制识别子系统（stable 已实现但等 001 promote）、`MOD-REGIME-005`、`MOD-PA-007` 体制元分配器（planned）、`MOD-POS-020` 策略账本（planned）、`MOD-POS-021` 硬风险聚合器（planned）、`MOD-POS-022` 预算变更处理器（planned）、`MOD-INF-011` 向量记忆蓝图（planned） | 已登记的"待接线/待实现"蓝图族，全部 `review_frequency: monthly` 但无自动触发器（违反宪法 §9.3"reconciler 必须事件触发"）；**注：该 list 未全量读完（输出截断），FF-14 子挖矿须先取全量条数** | 中 | 否 |

## J. 已知盲区（本轮未覆盖的检出面，须由分环节子挖矿补）

1. **恒真门禁未系统扫**：本轮只扫了 `fail_open`/`except…pass`，未逐 gate 验"能否红"（裁定#325 要求附变异/红蓝证据）。depgraph `gates` 表实测 **288 行**（列：`gate_id/name/entry/description/files_trigger/always_run/category/status/source/event_driven/auto_start`），其 `status` / `event_driven` / `auto_start` 三字段分布未取 → **FF-14 子挖矿第一优先**。
2. **DEBUG 放行未扫**：`grep DEBUG` 类放行点本轮未做（1405 处 fail_open 已占满检出面预算）。
3. **空表未直连 ClickHouse 实测**：BRK-027..031 的空表结论来自 `known_data_gaps.yaml` 登记（`last_updated: 2026-09-18`），未跑 `SELECT count(*)`。ClickHouse 直查须走 `DatabaseService`（宪法 §9.1）+ `c1_market` 全系带 `FINAL` → **FF-01 子挖矿第一优先**。
4. **`data/audit_trail/` 未逐件核**：实测该目录 14 项（`chain_state.json` / `events.jsonl` + 3 个 `.bak_*` / `gate_chain.jsonl` / `merkle_batches` / `merkle_hourly` / `merkle_state.json` / `cold` / `hot` / `warm` / 2 个 `.lock`），未验证 merkle 链是否连续、cold/warm/hot 三层是否有内容 → FF-14 子挖矿。
5. **动态加载孤儿未用 gate 口径复核**：BRK-007 的 1585 件是静态上界，须用 `scripts/governance/check_wiring_orphan.py` + `scripts/governance/d1_structure/detect_orphan_py.py`（二者本身也在 suspect_orphan 清单里，BRK-002）跑权威口径 → 注意 ORPHAN-MODULE gate 明文"scripts/ 里的 import 不算引用"。
6. **depgraph 快照未刷新**：BRK-008 已记 edges 漂移 +40。施工前须 `python scripts/governance/generate_project_depgraph.py --force`（耗时 2-3 分钟）后重算各环节"模块数"列。
7. **测试面未普查**：`tests/` 下 129 个子目录、12 处跨目录同名 test 文件（CONSTRUCTION_DISCIPLINE §6），本轮未统计各环节测试覆盖缺口 → 各分环节子挖矿按作业簿第 7 节补。
8. **前端 10 Tab 逐个未核**：FF-16 只取了 `config/flags.yaml` + panorama PAN-VIS-01 待建状态，未逐 Tab 验"页面能否打开、数据是否来自活链" → FF-16 子挖矿。
