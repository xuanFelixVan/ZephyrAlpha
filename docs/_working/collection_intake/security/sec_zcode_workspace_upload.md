---
ttl: task_bound
session: st-collintake-20260920
topic: collection_intake_20260920
---

# ⚠️ ZCode 静默上传代码库事件——P0 行动项 2026-09-20

> 事件核实 `[外部]`：**属实**，约 2026-09-18 前后曝光。开发者 ferstar 发现 `~/.zcode` 目录 700+MB，追查发现智谱 ZCode 桌面应用在登录态把**整个工作区快照（含完整 .git 历史、LFS 缓存）加密上传阿里云 OSS**，解密密钥仅服务器持有，超出其隐私政策（"仅对话中提交的内容"）口径。OSCHINA/凤凰网/知乎/HN（"Inside ZCode: Silently uploading your Git history to the cloud"）多方报道。
- 官方回应（与剪报一致，交叉印证）：归因="**代码库索引（Repo Wiki）**"功能上线初期**默认开启**；Bug 已修复，云端数据"用完即删"；补救=①开源客户端 ②第三方安全评估 ③全体用户一次周额度重置。
- **本会话正运行于 ZCode——本项目（含 .git 全历史与 SECRETS 体系）是直接暴露面。**

## P0 行动项（Owner 亲办或授权后本会话代办，今天完成）

1. **查本地残留**：检查 `C:\Users\fanzi\.zcode` 目录体积与内容（发现者的线索就是异常体积）；查找工作区快照/打包缓存/上传队列残留文件。
2. **关功能**：确认 ZCode 设置中"代码库索引 / RepoWiki"状态并**显式关闭**（默认开启过的历史版本即使升级也可能留配置）。
3. **密钥暴露面盘点**：对照 `SECRETS.md` 与 RULE-SECRETS 三道 gate——若本仓曾被索引上传：secrets.py 管理的全部密钥（tushare token/交易所 key/LLM key 等）**按已泄露处理轮换**；.git 历史中的敏感串一并视为暴露。
4. **跟进官方补救**：客户端开源与第三方审计报告发布后复查（当前"云端即焚"是官方单方口径，无第三方验证）。

## 项目侧既有防线（盘点结论）

- 密钥规范：secrets.py 统一管理、禁裸 getenv（三道 gate）——若轮换，只动 secrets 库一处 `[记忆]`。
- 上传面：本项目数据资产主要在本地 CH/duckdb，不在 git LFS；LFS 缓存是否启用待第 1 步实查确认。

> 执行记录：待办未动——本会话未擅自检查/修改 ZCode 安装与配置（涉及 Owner 个人工具环境），三项待 Owner 一句话授权即可代办。
