fix(git-guard): #ARCH-GIT-CLEAN-GUARD-FIX clean自伤检测+19号重建+AI审查指令

根因：git alias 无法覆盖内置 clean 命令（git 2.48.1 Windows 实测），
git clean -fd 直接执行内置 clean，完全绕过 git_guard.py。
且 _EXTRACTORS 无 clean 条目导致 extractor=None 直接 passthrough，零拦截。
2026-08-11 灾难：AI 执行 git clean -fd 误删 19_northbound_hold_snapshot.md
等多个 untracked 文件（物理删除不进回收站）。

修复：
1. git_guard.py 新增 _check_self_harm_clean 和 _get_untracked_files
2. check_and_execute 新增 clean 分支：有 untracked + 未授权 = 阻断
3. 19 号文件从对话历史重建
4. AI_review_instructions.md 24路指令42篇全覆盖纳入 git 跟踪
5. 所有 design_memos 修改一并保护性提交

注意：alias 覆盖内置命令的系统性问题仍存在，后续需用 git hooks 加固
