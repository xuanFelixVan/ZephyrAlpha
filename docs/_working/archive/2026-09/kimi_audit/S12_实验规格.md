---
ttl: task_bound
rule_form: data
verifiability: manual
title: S12 自证实验规格书（不变式集+注入样本+极端日反证场景）——转 Flash/夜间执行包
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
---

# S12 自证实验规格（基线 edd503ef 后）

> 全局规则 2：每条移交必带机读判据（阈值+反例+什么算红）。判据写不出=没想清楚——本页每条都已写死。
> 执行归属：E1-E3 小件可夜间直接做；E4-E6 需数十行施工，转 Flash；E7 为方法学裁定前置。

## E1 metamorphic 增补三条不变式（S1-T2/S1-A7 覆盖缺口收口）

- **INV-5 成本单调性**：同一回测 run，佣金/滑点参数 ×2 → 净值逐日不增且收益差 ≈ 成本差（±5% 容差）。红=成本上升净值不降。落点 `tests/metamorphic/test_s11_metamorphic_invariants.py` 同族。
- **INV-6 账本闭合**：引擎真跑后 `reconcile_cash_ledger` 残差 ≤0.01 且 `within_tolerance=True`（H3/H4 已有此锁——本规格把它从链测试**提升为不变式层**，并接线到 CLI 路径）。
- **INV-7 市值恒等**：逐日 `nav == cash + Σ qty×price`，缺价结转最后已知价时也成立。红=任一日断裂。
- 判据：三条各带反例（成本倒挂/双计佣金/缺价 NaN 注入）。

## E2 CLI 路径现金对账接线（S1-T1）

- 改 `scripts/run_backtest.py` run_one：产物 metrics 增 `cash_ledger_reconciliation=reconcile_cash_ledger(...)`；缺 cash_history → 该字段 fail-closed False。
- 验收：`python scripts/run_backtest.py --strategy topn-momentum --symbols 600519.SH --start 2024-01-01 --end 2024-06-30` 后 artifact JSON 含该字段且 within_tolerance=True；反例=删掉一笔成交后重跑必须 False。
- 判据：新钉测试进 `tests/backtest/`（GT-15 结构闸同族）。

## E3 哨兵存活探针（S8-V3 收口）

- 扫 `tasks.yaml`/注册表里所有 `feed_task`/哨兵引用，断言指向的任务名真实存在且近 7 日有运行记录。
- 红=哨兵指向不存在任务（当前已知 `kline_index_breadth_refresh` 一件）。
- 落点 `tests/data/` 或治理门禁任选，判据同上。

## E4 C4 翻译件 SCD-2 宇宙 retrofit + 全量重考（转 Flash）

- 改动：`_c4_engine.py` 宇宙选取改 SCD-2 时点口径（复用 `fw_backtest.py:172-226` 范式：快照日≤信号日 ∩ 在窗）；`scripts/backtest/translated/c4_*.py` 受影响 15 件+gftd/6 件 fact 已修后重跑。
- 判据：①修复后 85 件全量重跑，新旧 strategy_screen 行逐件对比——flip 清单（成绩方向翻转件）必须全列；②通用 PIT 闸入库：两份仅 X 日后分歧的行情输入 → 断言 weights[:X] 逐位不变（B1 探针泛化版，一闸罩全族）。
- 验收命令：`python scripts/backtest/c4_batch_screen.py --auto-oos-pending`（重跑通道）+ 新闸 `pytest tests/backtest/test_c4_pit_universal_gate.py`。
- 回滚：revert 单笔；旧行保留 suspect 标记不删。

## E5 regime 迟滞四臂对照（S13 F-3 / T1A-6 升级）

- 实验：`framework_composer` 三套方案的 regime_overrides 在 迟滞{开3日确认/关} × 切换成本{计/不计} 四臂下重跑 2024-01..2025-08。
- 判据：报告四臂净收益差与换手差；迟滞收益 = (开-关) 扣成本后 >0 且 bootstrap CI 不含零 → STD-SWITCH-001 的 3 日窗获实证；≤0 → 回 S13 修尺。
- 反例：迟滞开在 2024-09-24 暴动窗的表现必须单列（防迟滞吃掉极端行情）。

## E6 STD-SIM-ACCESS-002 全量重考（S5 draft→frozen 前置）

- 对在架组合+17 存活者+4 核心（剔除 S6 死刑两员后）用 v1/v2 双尺重打分，出排序对照表。
- 判据：翻转清单逐条落"可解释/不可解释"；不可解释件 >10% → 新尺不得 frozen。
- 依赖：E4 完成（污染件先修）。

## E7 历史极端日反证场景库（S12-exp 扩展为正式场景集）

- 场景（覆盖窗内）：W1 2024-02 微盘崩盘 / W2 2024-09-24 暴动反转 / W3 2025-04 关税恐慌 + 新增**涨跌停不可成交约束检查**（极端日信号能否成交=fillability 断言）。
- 判据（沿用 S12-exp 已跑通的机读框架）：危机窗 ρ̄>0.50 红；窗 maxDD>1.5×全样本红；组合跑输指数 10pp 红；新增红=信号在涨停日发出买入且引擎成交（不可成交日成交=引擎洞）。
- 基线已通过（S12-exp PASS，但 W2/W3 ρ̄=NaN 由 CAND-e3da6fa7 全零收益致——S6 挂起件自证后重跑本场景）。

## E8 门禁失效探针（S4-F7 收口）

- 探针：人为构造 git diff 故障（mock run_git rc≠0）→ 内容扫描型门禁必须 warn 留痕且统计面上报；注入 YAML 破损 → REGISTRY-YAML-PARSE 只拦该表门禁不连坐（S18 R-09 判据）。
- 红=故障注入后无任何 warn 记录=静默失效。
