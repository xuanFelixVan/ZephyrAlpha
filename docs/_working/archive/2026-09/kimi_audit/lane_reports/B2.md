---
ttl: task_bound
rule_form: data
verifiability: manual
title: lane B2 报告（kimi-audit 班次 v4 分包）
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
---

# B2 lane 报告 — 测试有效性审计（S15 战场）

> lane 长：kimi-audit-b2 ｜ 日期：2026-09-17 ｜ 方法：逐条问「我改坏被测代码，这个测试会红吗」——
> 能构造变异的一律当场变异（改一行源码 → 跑测试 → 应红 → 还原 → git diff 零残留核实）。
> 本 lane 共执行 **17 次源码变异（M1–M17）+ 3 个模式级实证 + 1 次红蓝报告取证**；
> 全部变异已还原，12 个被变异源码文件 `git status --porcelain` 全空（零残留）。

## 〇、执行摘要

抽样面内 **真绿是主流**：昨夜新落库三件套（redline/h3h4/combo 门）与钱闸四文件
（dsr/fdr/metamorphic×2）共 13 次变异**全部被杀**，断言含已知答案锚、oracle 独立复算、
fail-closed 钉扎，质量高。**假绿集中在老治理测试**：5 处已当场补断言直改（复跑全绿+
变异闭环验证）；另发现 **1 个基线即红**（S11 全零行用例）与 **1 套系统性假绿**
（红蓝对抗套件：检出率 0.6667、两个检测脚本不存在，套件照样全绿）。

---

## 一、假绿清单（file:line + 缺什么断言 + 证据）

### A. assert True / 恒真式集群（变异实证 = 变异存活）

| # | 点位 | 病灶 | 变异/实证证据 | 处置 |
|---|------|------|--------------|------|
| A1 | tests/trading/test_work_dag.py:52-57,117-122,169-174 | `try: X(); assert False; except Exception: pass` **自我中和**——assert False 抛的 AssertionError 被自己的 except 吞掉，永不红。缺：`pytest.raises(ValidationError)` | 模式实证 `.runtime/tmp/exp/b2/test_b2_pattern_proofs.py::test_pattern_self_neutralizing_with_lax_constructor` 通过=构造器不校验时该模式仍绿；对照实证今日真 WorkNode 确实 raise | **已直改**×3，复跑 16 passed |
| A2 | tests/agent_rbac/test_sequence_guard_agent_rbac.py:91 | 恒真式 `assert result is not None or result is None`。缺：跨会话共享 target 必须报风险 | **M15 存活**：`check_cross_session` 恒返 None（检测整体失效），10 passed 照旧 | **已直改**（断言 not None + "shared_file" in result），复跑 10 passed |
| A3 | 同文件 TestSafeSequence:80 / TestReset:107,113 | 三处裸 `assert True`。缺：安全序列不误拦 / reset 后历史真清空 | **M14 存活**：`reset_session` 改空操作，10 passed 照旧 | **已直改**×3（半截序列不得触发禁止链）；**闭环**：重放 M14 → `test_reset_session_clears` 红（`'FORBIDDEN sequence detected: data_exfiltration' is None`）→ 还原 |
| A4 | tests/gov_code_dedup/test_micro_clone.py:23,27 | 23 行只数块数不看内容（注释「7行→3个5行窗口」还是错的：实际 6 行、窗口=3、非重叠步进→2 块）；27 行纯 stub `assert True` | **M16 存活**：每块截短一行，2 passed 照旧 | **已直改**（钉死两块确切内容 + 短文件/尾余行边界断言），复跑 2 passed |
| A5 | tests/f_lifecycle/test_f21_auto_run.py:91,107；test_f21_auto_startup.py:59,136,149；test_f21_event_driven.py:65,72 | `assert True  # 不抛异常即可`——smoke 级，只守「不炸」不守「干了」。缺：注册/订阅结果断言 | **M17 存活**：`register_shared_monitoring_probes` 整体短路（注册体为空），test_f21_auto_run 10 passed 照旧 | 未直改（面广，smoke 有其价值）；建议后续补 `len(hm.probe_fns) > 0` 类结果断言 |
| A6 | tests/rule/test_rule_red_blue.py:143,169,213,292,362 + 392-412 | 五处 `assert True`；终闸 `test_generate_report` 只断言 `total >= 9`，**从不门检出率**。红蓝对抗记录 RED/YELLOW 到 JSON 但套件永绿 | 取证：跑套件 **9 passed 1 skipped**，同时报告 `detection_rate=0.6667, yellow=3`——`audit_registration.py`/`diagnose_depgraph.py` **两个检测脚本不存在**（ls 核实），检出能力缺 1/3 照样全绿 | **未直改**：补检出率门位会立刻翻红（检测脚本缺失是独立病灶），须 Owner/治理域决策。另注：该测试写生产路径 `data/databases/governance_metadata/red_blue_report.json`（§9.6 测试隔离违规，已备份+还原该文件） |

### B. mock 掉被测对象排查（结论：抽样三处均为合法边界桩，真绿）

| 点位 | 判读 | 证据 |
|------|------|------|
| tests/pf_core/test_framework_composer_shrinkage.py:291-297,326-332 | monkeypatch 的是 `_build_member_panels`/`_member_signal_contracts`/`_select_vectorizable_members`——**取数/路由边界桩**（合成面板注入），被测件（引擎选择、披露组装、双跑差值）全走真码，且有 spy_init 抓真实引擎类 | M13 变异（dual off 侧 phase 串改）被杀：`test_dual_off_side_failure_not_silent` 红（off→on diff） |
| 同文件:421 | stub `_run_framework_backtest_core` 是为了**强制构造 off 侧失败路径**来测 dual 编排的错误传播——被测单元是 `run_framework_backtest_shrinkage_dual` 本身，stub 的是其依赖 | 同上，M13 直接命中该用例的断言 |
| tests/pf_core/test_framework_composer.py:382 | stub `StrategyRunner.build_weight_panel`（CH 取数边界），e2e 断言落在真产物：run_id/落盘文件/metrics 键 | 旁证：M11（compose α 被吞）被 S11 oracle 锚杀 |
| tests/pf_core/test_eventdriven_sleeve_strategy.py:100-131 | 三处 patch 的是 `sleeve_mod.detect_anomaly`/`compute_event_score`（intelligence 域依赖），被测的是策略对依赖输出的**反应**（剔除/告警/不整批抛） | **M12 被杀**：负向异动剔除条件改 `if False:` → `test_negative_anomaly_excluded` 红（`{'600001': 0.1}` 非空）——证明 mock 缝真实接入决策链，非空转 |

### C. 异常吞没抽样判读（实测口径：`except Exception: pass`=67 处、`...: return`=26 处）

| # | 点位 | 判定 | 依据 |
|---|------|------|------|
| C1 | tests/trading/test_work_dag.py ×3 | **假绿** | 见 A1（自我中和） |
| C2 | tests/agent_rbac/test_cross_model_consistency.py:266-270 | **假绿** | `assert result.decision is not None` 在 try 内，AssertionError 被 `except Exception: pass` 吞——畸形 op 返 None 也绿。缺：断言移出 try |
| C3 | tests/governance/commit_gates/test_import_integrity_gate.py:555-580 | **假绿** | 整个测试体（含 `assert hits == []  # fail-open`）裹在 try/except-pass 里——fail-open 失效（异常外溢）也绿。模式实证 `test_pattern_self_neutralizing_failopen_broken` 通过 | 
| C4 | tests/architecture/test_contract_consistency.py:145-170 | **半假绿** | 单契约 import/检查异常被吞=该契约静默豁免 required 校验（对比同文件 129-130 兄弟测试会把加载失败记违规）；violations 汇总闸本身是真的 |
| C5 | 同文件:250-275 | **半假绿** | P1 版同病 |
| C6 | tests/infrastructure/adversarial/test_f18_governance_adversarial.py:199-200,244-245,290-291,323-324,386-387 | **半假绿** | 每文件 read_text 失败=该文件豁免安全扫描（无留痕）；且 250 行发现真风险走 `pytest.skip` 而非 fail——`test_scripts_no_arbitrary_code_execution` 永不红 |
| C7 | tests/governance/test_apply_decisiongraph_smoke.py:255-265 | 弱但诚实 | 目标窄（只捕 NameError 缺 import），docstring 声明；AttributeError 类回归会漏 |
| C8 | tests/git/test_git_commit_extreme.py:224-227 | **真绿** | 吞的是场景内预期异常，不变量 `assert not lock_file.exists()` 在 try 外（230 行） |
| C9 | tests/audit/audit_core/test_audit_integration_fracture.py:286-289 | **真绿** | 同上模式：5 次写入失败是布景，真断言在 291（readonly）+293（raises） |
| C10 | tests/task/test_task_repo_auto_commit.py:218-226 | **假绿** | 吞异常后唯一断言是 `hasattr(repo, ...)` 恒真——「transition→auto_commit 被调」完全未验（注释自认） |
| C11 | tests/infrastructure/mcp/test_mcp_signal_shutdown.py 等 teardown 簇（kill/unlink/shutdown 处） | 良性 | 清理路径吞异常，不变量均在 try 外；建议改 log 但不构成假绿 |
| C12 | tests/f_lifecycle/test_f5_auto_shutdown.py:62-65 等 | 良性 | 同 C11 |

### D. 基线即红（「全绿」说法的反例）

- **tests/metamorphic/test_s11_metamorphic_invariants.py::test_signal_all_zero_rows_preserved_as_cash_days** 基线即红（单跑复现）：
  测试期望全零成员面板保留为现金日（`compose_weight_panels` 返零面板），
  当前源码 framework_composer.py:1069 直接 `raise FrameworkValidationError("无任何参与成员的面板…")`。
  测试语义与现源码 fail-closed 行为冲突——疑似源码行为演进后该钉扎未跟进。**禁动 src 纪律：只记录**。
  处置建议：要么更新该用例钉扎 fail-closed 新语义（pytest.raises），要么 Owner 裁定这是生产回归。

---

## 二、真绿确认清单（变异验证证据：变异体被杀）

| 变异 | 被测源码 | 变异内容 | 结果（杀变异的测试） |
|------|---------|---------|---------------------|
| M1 | src/zephyr/ex_sor/risk_redline.py:106 | `daily_red` 阈值 ×2 | **3 红**（yellow_then_red/blackswan_grace/grace_only_once），8 场景覆盖有效 |
| M2 | src/zephyr/backtest/core/portfolio.py:464 | `within = True` 恒放行 | **4 红**（双计佣金/漏记成交/脏行/无样本四反证全爆）——h3h4 账本反证链名实相符 |
| M3 | scripts/backtest/promotion_combo_gate.py:128 | 回撤公式去 `max` 累积 | **1 红**（test_max_drawdown_math：0.0833≠0.25 已知答案锚） |
| M4 | src/zephyr/backtest/core/metrics.py:317 | 砍掉样本量<60 退化门 | **1 红**（test_short_series_degenerate_dsr：dsr 0.7535≠0.0） |
| M5 | 同文件:333 | 年化无风险利率当日频喂入（量纲错配回归=A4 坏路径复活） | **1 红**（test_dsr_matches_official_calculator：0.0≠0.9853，官方件逐位锚） |
| M6 | src/zephyr/factor/analysis/bhy_fdr.py:111 | 调和因子 c(m) 恒置 1 | **1 红**（阈值锚 0.0600≠0.02628） |
| M7 | 同文件:124-125 | 拒绝掩码不映射回输入顺序 | **1 红**（test_unsorted_input_maps_back：index0 True≠False） |
| M8 | pattern_evidence_certifier.py:91 | hits>n 短路返 0.0→1.0 | **2 红**（degenerate_known_values[110-100] + inf_hits） |
| M9 | 同文件:130 | 收缩公式丢先验 k·baseline | **2 红**（shrunk_rate_degenerate + shrunk_absolute_anchor 手算锚） |
| M10 | vectorized_engine.py:981 | 归一化除法被吞（信号强度不再不变） | **2 红双杀**（S11 signal_strength_invariant + h3h4 normalize 读数）——双文件独立覆盖同一函数 |
| M11 | framework_composer.py:1104 | compose 成员权重 α 被吞 | **1 红**（compose 列置换 oracle 独立复算锚） |
| M12 | event_driven_sleeve_strategy.py:210 | 负向异动剔除被吞 | **1 红**（见 B 表） |
| M13 | framework_composer.py:2807 | dual off 侧失败相位串改 | **1 红**（见 B 表） |

**判绿评级**：redline / h3h4 / combo 门 / dsr / fdr / fourgate / s11（除 D 项）均为**真绿**——
已知答案锚 + 反证链 + fail-closed 钉扎 + oracle 复算，变异无一存活。
次级观察（非假绿，记录在案）：test_metrics_dsr.py:96-99 `test_is_overfitting_floor_semantics`
为半自指断言（用同一常量重算谓词），对 `<`→`<=` 边界变异钝感；test_metrics_dsr.py:89-94
`test_more_trials_lower_dsr` 用非严格不等号，对「n_trials 恒常数化」变异钝感
（但 M5 证明官方件逐位锚可兜住量纲类回归）；metrics.py:352-355 的 fallback_default
fail-closed 分支（S1-A4 裁定）在本测试文件内无直接覆盖。

---

## 三、当场补断言直改（留工作区；改动前逐文件 `lock_files.py acquire kimi-audit-b2`，改毕复跑后 release）

| 文件 | 改动 | 复跑输出 |
|------|------|---------|
| tests/trading/test_work_dag.py | 3 处自中和块 → `pytest.raises(ValidationError)`（补 import pytest/pydantic） | 16 passed |
| tests/agent_rbac/test_sequence_guard_agent_rbac.py | 恒真式+3 处 assert True → 跨会话共享检出断言、安全序列 `result is None`、reset 后半截序列不触发 | 10 passed；**闭环验证**：重放 M14 变异 → test_reset_session_clears 红 → 还原源码 |
| tests/gov_code_dedup/test_micro_clone.py | 数量断言 → 两块逐字节内容锚；stub → 短文件空块+尾余行丢弃边界断言 | 2 passed |
| tests/governance/commit_gates/test_import_integrity_gate.py | 拆除吞没 fail-open 断言的外层 try/except（fail-open 失效必须红） | 67 passed（真码确实 fail-open，修复后断言为真） |
| tests/task/test_task_repo_auto_commit.py | 恒真 hasattr → 「迁移成功则 mock 必被调」条件断言；场景搭不起来时显式 `pytest.skip` 而非装绿 | 8 passed 1 skipped（skip 本身即证据：旧版在该环境恒空转） |

---

## 四、未直改记录（须 Owner/对应域决策）

1. **tests/rule/test_rule_red_blue.py**：检出率门位缺失 + 两个检测脚本不存在 + 测试写生产路径
   （data/databases/governance_metadata/red_blue_report.json）。补门=套件立刻翻红，属治理域决策。
2. **tests/metamorphic/test_s11_metamorphic_invariants.py::test_signal_all_zero_rows_preserved_as_cash_days**
   基线即红（见 D）——测试语义 vs 源码 fail-closed 演进，需裁定方向。
3. tests/architecture/test_contract_consistency.py 两处契约豁免吞没（C4/C5）：
   建议对齐 129-130 兄弟测试，异常记 violation 而非静默豁免。
4. tests/f18 安全扫描五处吞没 + skip 兜底（C6）：建议 unreadable 文件计数出声、real_risks 改 fail。
5. tests/f_lifecycle/test_f21_*.py smoke 簇（A5）：建议补结果断言（如 probe 注册数>0）。
6. tests/agent_rbac/test_cross_model_consistency.py:266-270（C2）：断言移出 try 即修复，未在本批直改范围。

---

## 五、结论（≤10 行）

1. 抽样面真绿占绝对主流：17 次变异 13 杀 4 存活，被杀的全是昨夜新测试+钱闸——这批是真在证明东西。
2. 假绿高度集中在老治理测试的三种定型病灶：assert True 空转、try/except 自我中和、恒真式。
3. 最严重单点：红蓝对抗套件（test_rule_red_blue）检出率 0.6667、两个检测脚本不存在，照样 9 passed——治理自检的「全绿」本身是假绿。
4. 次严重：S11 metamorphic 有 1 条基线即红用例，「全仓全绿」的对外说法不成立。
5. 已当场直改 5 个测试文件 12 处断言，复跑 103 passed + 1 honest-skip，其中 rbac 修复经变异闭环验证（补完后原存活变异体被杀）。
6. 钱闸四文件（dsr/fdr/metamorphic×2）断言质量为全仓标杆：已知答案锚+oracle 复算+边界钉扎，建议作为其他域的写法范本。
7. mock 自替排查三处全部判合法（边界桩≠替被测逻辑），M12/M13 变异证明 mock 缝真实连通决策链。
8. 源码侧零改动残留（12 个被变异文件 git 全空）；生产码问题仅记录未动（S11 语义冲突、metrics fallback 覆盖缺口）。
9. 遗留决策项 4 条（第四节），均不阻塞本 lane 结论。
10. 本 lane 工作目录：报告 `docs/_working/kimi_audit/lane_reports/B2.md`，实证件 `.runtime/tmp/exp/b2/`。
