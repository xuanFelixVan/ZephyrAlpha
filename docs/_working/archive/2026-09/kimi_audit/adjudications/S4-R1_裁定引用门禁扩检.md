---
ttl: task_bound
rule_form: data
verifiability: manual
title: 裁定书草案——S4-R1：RULING-REFERENCE 门禁检测面扩到裸 #NNN 引用
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
status: 待 Owner 签
---

# 裁定书草案 S4-R1：RULING-REFERENCE 检测面补齐

## 现状（含证据锚点）

门禁 `ruling_reference_gate.py:83` 只认字面量 `裁定#NNN`（`_RULING_REF_RE`）。实证不对称：同一悬空号 304，`docs/_working/factory/t_v2/2026-09-17-t-v2-campaign-design.md:28` 写 `#304` 顺利入库（commit `1897f03095`），而 `s3_pending_rulings_inventory.md` 写 `裁定 304 号` 被硬拦（死信 q-20260917-kimi-audit-0002）。**同一违规两命运=检测面残缺**（S4-F1）。

## 选项

| 选项 | 内容 | 最坏情形 |
|---|---|---|
| A. 维持现状 | 只拦 `裁定#NNN` | 裸 `#NNN` 引用永久零保护；缺陷模式 #15 无机检面 |
| B. **扩检裸 #NNN（限定文档区）** | 正则扩为 `(?:裁定)?#(\d{3,})`，仅作用于 `docs/` 下 .md；排除 `#ARCH-`/`#MOD-`/`#BT-` 等字母前缀形态；三位以下数字不碰 | 误伤 GitHub issue 式引用——本仓 docs 区无此习俗（issue 引用带字母前缀或 URL），误判面接近零 |
| C. 门禁不动+周审计扫描兜底 | 注册表侧周期全仓扫 | 发现滞后一周；违规件已入库 |

## 依据

缺陷模式 #15（裁定引用不核真源）已入册（v1.1.0，commit 9f8e11a7 批次）；没有机检面的缺陷模式=靠记忆防。本门禁属 S18-R4 不可放松白名单——**本提案是加严不是放松**，语义方向与既有裁定一致。

## 风险

regex 变更=门禁判据变更，须 Owner 签；签发后先跑全仓存量扫描出豁免基线（存量裸引用合法件一次登记），防历史件连环误拦。

## 建议+置信度

**建议 B，置信度高。** 附带执行纪律：变更同日跑存量扫描+豁免基线登记。

## 若不同意应看什么数据

`docs/` 区裸 `#NNN` 引用全仓普查清单——若合法用途 >50 处，降级为方案 C。
