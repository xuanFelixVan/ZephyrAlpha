---
ttl: task_bound
---

> ## 结案报告（2026-09-15 由 st-fullchain-20260914 核验）
> **总结论：设计/计划类且无落地证据，保守保留。处置=**保留**。**
>
> **✅ 已完成**：无显式完成信号
>
> **⚠️ 未完成**：无待办信号
>
> **核验方式**：全文扫描完成/待办信号 + 引用文件存在性核验（引用 2 个，其中判废弃 0、路径漂移 0）+ commit 提及 0 处。
>
> **处置建议**：保留。（本报告由清理批自动生成，判定依据=文档自身信号 + 代码侧核验）






# 投入产出表结构锚施工方案（Owner 2026-09-13 口头放行，薄方案即施工）

> 背景与渠道调研见对话记录（Owner 问"免费产业链/供应链数据"，AI 检索后裁定优先级：IO 表 > 年报前五大供应商 > 链长制图谱 > 开源 KG 仅对照）。本方案=第一梯队施工件。

## 0. 目标（一句话）

给产业链图谱引入**官方结构真源**：2020 年全国投入产出表直接消耗系数——部门级上下游强度首次获得可机械判定的量化依据（tier 退役裁定遗留的判定依据缺口），用于：①现有 ig_edge supply/structure 边的强度校准 ②S21 断链补边的 evidence 依据 ③链间传导权重（galaxy 消费候选）。

## 1. 口径裁定（AI 自裁，Owner 可否决）

- **D1 主锚**：2020 年全国投入产出表 **153 部门**（最细官方调查表）。**降级锚**：42 部门表（公开度最高，先跑通管道，管道与粒度解耦，153 到手无缝替换）。2023 延长表=时效升级，不换管道。数据获取顺序：data.stats.gov.cn API → CEADS → 官方出版物转载源。
- **D2 映射规则**：IO 部门 →申万 2021 行业→图谱链。映射表是**规则数据=YAML**（登记进注册表体系，禁硬编码在代码里）；残差（无法自动映射的部门）逐条人工裁定留痕。
- **D3 落地形态**：部门级传导是独立于环节边（ig_edge）的另一层——**新表**承载（io_coefficient 矩阵，带 year/PIT 三时间戳，DDL 走 apply_industry_graph_ddl.py 直写 DB=架构数据）。不改动 ig_edge 表结构，不污染环节边语义。
- **D4 消费路径**：本批只落"数据资产+校准报告"，**不自动改边**。补边动作（S21 断链用 IO evidence）逐链走 websearch_ingest 唯一通道，source_doc 带 IO 锚。防一次性大改图。

## 2. 施工件

- `scripts/industry_graph/io_ingest.py`：fetch（数据获取）→ parse（系数矩阵解析）→ map（YAML 映射）→ load（入库）四段式，幂等，走 DatabaseService/正规 DDL 通道。新建 scripts/ 文件三件套登记。
- 映射表：`docs/01_policies_and_standards/_registry/catalogs/io_sector_sws_map.yaml`（规则数据）。

## 3. 验收线

1. 42 部门（或 153 部门）直接消耗系数矩阵全量入库，行数=部门²量级，PIT 字段齐。
2. 映射 YAML 覆盖率报告（可自动映射比例+残差清单）。
3. 校准报告落 `docs/_working/`：现有活跃链 supply/structure 边 vs IO 系数的对照样本 ≥10 条。
4. 引擎 [GREEN] 0 违规保持；两套测试全绿；Gateway 提交。

## 4. 红线照旧

写库前 backup；总控锁；禁 DELETE；临时文件走 `.runtime/tmp/`；不碰交易面。

## 5. 本批不做（防蔓延）

年报前五大供应商解析（第二施工件，iFinD 供应链模块盘点先行）、链长制图谱灌库、开源 KG 对照、153 部门→环节级细粒度映射。
