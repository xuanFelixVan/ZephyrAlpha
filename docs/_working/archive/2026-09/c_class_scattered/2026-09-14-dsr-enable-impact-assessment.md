---
ttl: task_bound
---

> ## 结案报告（2026-09-15 由 st-fullchain-20260914 核验）
> **总结论：未结案（仍有待办）。处置=**保留**。**
>
> **✅ 已完成（1 条，摘录）**
> - L75: `decision_gate.py:683`：「DSR 判定器已启用但未注入 dsr，按不通过处理（fail-closed）」。
>
> **⚠️ 未完成（1 条，逐条摘录）**
> - L109: ## 七、待 Owner 拍板
>
> **核验方式**：全文扫描完成/待办信号 + 引用文件存在性核验（引用 2 个，其中判废弃 0、路径漂移 0）+ commit 提及 5 处。
>
> **处置建议**：保留。（本报告由清理批自动生成，判定依据=文档自身信号 + 代码侧核验）






# DSR 默认开启——冲击面评估（2026-09-14 实测）

> Owner 2026-09-14 指令："DSR 默认关闭要不要改成默认开启？先评估冲击面，优先级最高。"
> **结论先行：不建议现在直接拨开关。** 有三个口径问题必须先修，否则开了一个错误的开关，比不开更危险。
> 全部数字来自 CH `c1_backtest.strategy_screen` 实查（1102 行）与 `metrics.calculate_dsr` 源码实读。

> **勘误（2026-09-15，核销记录见全链路方案附录 A.5）**：
> ① §五 的 E[max(Z_N)]=4.90 应为 **4.5262**——4.90 漏减了 (lnπ+lnlnN)/(2√(2lnN)) 项；相对 N=35 的抬升相应为 **2.3121σ** 而非 2.69σ（按 `metrics.py:368-380` 实算）；
> ② 引用路径更正：`c4_deflated_sharpe_runner.py` 实际位于 `src/zephyr/backtest/regime_validation/`（:117 为 num_trials 字段注释，非计算点）；`_c4_engine.py` 位于 `scripts/backtest/translated/`；
> ③ **新发现（比原结论更严重）**：仓库有两套 DSR 实现、量纲约定不一致——`metrics.py` 路径把**年化** Sharpe（`:198` ×√252）配**日频** T（`calculate_full_metrics:437` 传 len(returns)，`:364` 按日频除），σ_SR 系统性偏小、z 虚高、DSR 偏向 1，**该路径不可用**；台账链路（`c4_batch_screen.py:134` → 官方件）单位一致；
> ④ 两套实现均缺 Bailey-LdP 正典所需的"跨试验夏普截面方差 V[{SR_n}]"输入（正典用它定门槛 SR*）；
> ⑤ **综合结论：本表 133 条台账 DSR 在按 run 档案重算核对完成前，一律不得作为决策输入。**

> ## 第 0 步重算已完成（2026-09-15 接班会话实放定案，冻结维持、理由改写）
> **重算对象**：台账 max 那条 DSR=0.9809（`SCR-C4-20260913-232609` / `CAND-e3da6fa71af1`，`c4_e3da6fa71af1_panic_rebound.py`，IS 2020-2023）。
> **方法**：单策略重放（build→run_backtest→daily_net_returns 全走当日代码与当日库）→ 官方件 `DeflatedSharpeCalculator` 三口径重算；脚本 `.runtime/tmp/dsr_recalc_e3da6fa71af1.py`。
>
> | 口径 | 实测 | 判定 |
> |---|---|---|
> | 官方件，N=1（**当时真实批内数**） | **0.9880** | ≈台账 0.9809（差值=数据漂移，见下）✅ 来源定案 |
> | 官方件，N=35（上轮反算的假设） | 0.5170 | 上轮"理论 ≈0.30"即此口径的粗算——**N=35 假设错误**，该行真实 N=1 |
> | `metrics.py` 坏口径，N=35 | **1.0000** | "系统性偏向 1"实锤；且台账链路不走它，与台账值无关 |
>
> **三个新事实（全部改写认知）**：
> 1. **0.9809 的身世**：`C4-2016-2019-sample3` 批 5 行里，4 行出自 23:21 主 run（批内 N=4，`eaddc3f9db4e` DSR=0.6694 与 N=4 口径自洽验算通过）；0.9809 这条是 5 分钟后 **23:26 solo 重跑——批内 N=1，E[max(Z_N)]=0，零多重测试折减**。它一枝独秀不是因为策略最强，是因为**它根本没被折减**。
> 2. **数据层漂移实锤**：同模块同窗口当日重放 Sharpe 0.983→1.15（09-14 行情修复批改写下层序列）；run 档案只存统计量不存收益序列 → **历史 DSR 不可逐位复现**。
> 3. **第 4 坑（N 未落账）**：`num_trials`=批内行数且台账无 N 字段 → solo run 零折减、批间 DSR（N=1~48 混杂）不可横比、事后无法审计任一行的 N。
>
> **冻结维持，理由改写**：145 条（原 133+09-15 两批 VAL 各 6）DSR 冻结不再是因为"来源不明"（来源已定案=官方件一致口径），而是因为 **① N 未落账且批间不可比 ② 数据层漂移不可复现 ③ solo 条目零折减虚高**。解冻条件=N 账本（累计口径）重建 + 存量按可考证 N 重算回填（§六-1 不变，另加"N 字段落台账 schema"）。

> ## 冻结解除（2026-09-15，st-f06dsr-shift2-20260915）
> 解冻条件已全部满足：
> 1. **N 账本建成**：MOD-BT-200 `TrialLedger` + `trial_ledger_registry.yaml`（commit f4d1ea4f），count=269（台账 is_sharpe 非空行）+ 4212（F-06 批次 A 网格 5 批 manifest 可复核）= **4481**；
> 2. **num_trials 落台账 schema**：S03-N1 加列 + 本班 23 个 run 回填可考证值；
> 3. **存量重算回填完成**：261 行（refold 199 + normal_approx 62）+ 8 行诚实跳过（failed_obsolete/sim_deviation 无窗口证据），报告=`dsr-recalc/2026-09-15-dsr-recalc-report.md`，逐行明细=`dsr-recalc/2026-09-15-dsr-recalc-rows.yaml`。锚点行 0.9809 **翻案为 0.0517**（N=1 solo 零折减原点→累计 N=4481），新分布 max=0.1448、≥0.5 行数=0——与本评估累计口径预测"0/133 存活"方向一致。
> 4. **三步走第①步（先修口径）就此收口**；第②步（阈值三线 SSOT 统一）与第③步（开关只对新批次、未注入记 not_tested）由后续班次执行。
> 注意：重算产出=当日口径新值，与历史值不可逐位比（N 口径变更+09-14 数据漂移双重原因），逐行声明见报告 §7。

## 一、先纠正一个根本性误解：DSR 是概率，不是夏普

`src/zephyr/backtest/core/metrics.py:385`：`dsr = _norm_cdf(z)` —— **DSR ∈ [0, 1]，是"这个 Sharpe 超出运气成分的概率"**，不是调整后的夏普值。

源码注释（L389-390）给的两条线才是权威口径：

| 阈值 | 语义 | 出处 |
|---|---|---|
| **0.5** | `DSR_OVERFITTING_FLOOR`——**运气中值否决线**，低于此 = 无超出运气的证据 | metrics.py:391 `is_overfitting = dsr < 0.5` |
| **0.95** | **显著性放行线** | MOD-SIM-024 `is_significant`；`sharpe_calculator_fixer.py:168` `dsr_threshold: float = 0.95` |

**由此得出第一个坏消息**：`DecisionGate` 的测试里用的是 `DecisionGateConfig(dsr_threshold=0.5)`（`tests/backtest/test_decision_gate.py:264` 等 8 处）。若照抄这个数进生产，**等于"只要不比抛硬币差就放行"——这不是防过拟合，是发安慰剂。**

同时暴露同名不同义的隐患：`sharpe_calculator_fixer.dsr_threshold=0.95`（正确）与 `DecisionGateConfig.dsr_threshold`（tests 用 0.5）**两个同名配置、两套量纲理解**，必须统一。

## 二、实证：DSR 其实一直在算，只是没进判定

| 指标 | 数值 |
|---|---|
| 台账 `c1_backtest.strategy_screen` 总行 | **1102** |
| 有 `is_sharpe` | 184 |
| 有 `deflated_sharpe` | **133** |
| 有 IS 但无 DSR | **51** |
| verdict 分布 | screened_in 381 / deferred_c4 321 / rejected 216 / oos_tested 126 / translated_c4 53 / failed_obsolete 4 / sim_deviation 1 |

现有 `deflated_sharpe` 分位（133 条）：
p25=0.0002 → **中位数 0.0114** → p75=0.1007 → p95=0.4366 → max=0.9809

也就是说：**一半以上的策略，其"超出运气"的置信度只有 1% 左右。** DSR 一直在忠实地告诉我们这个事实，只是从来没人拿它卡人。

## 三、冲击面实测

按累计试验数重折减（方法：反解 z = Φ⁻¹(DSR)，扣减 ΔE[max(Z_N)]，再 Φ 回概率）：

| 口径 | DSR ≥ 0.5 存活 | DSR ≥ 0.95 存活 |
|---|---|---|
| 现状（批内 N，35~374） | 6 / 133 | **1 / 133** |
| 累计 N = 1102（台账总量） | 3 / 133 | **0 / 133** |
| 累计 N = 10000（未来工厂量级） | 1 / 133 | **0 / 133** |

Top 几条在累计口径下的变化：

| 策略 | 批次 | IS Sharpe | DSR(批内) | DSR(累计) |
|---|---|---|---|---|
| CAND-e3da6fa71af1 | C4-2016-2019-sample3 | 0.983 | **0.9809** | 0.5339 |
| CAND-dc5b80aa3614 | C4-translated-20260912 | 1.066 | 0.8879 | 0.8167 |
| CAND-eaddc3f9db4e | C4-2016-2019-sample3 | 0.825 | 0.6694 | 0.0607 |
| CAND-e3da6fa71af1 | C4-translated-20260912 | 1.15 | 0.5309 | 0.4071 |

**注意一个正面信号**：唯一 DSR 冲到 0.98 的 CAND-e3da6fa71af1，正是讨论稿里唯一双窗口及格的那条"恐慌反弹"。DSR 独立地把同一条选了出来——**指标本身是有效的**，不是摆设。

## 四、三个必须同时修的坑

### 坑 1：`num_trials` 口径错（最严重）
`c4_deflated_sharpe_runner.py:117` 注释：「`num_trials: int  # 多重比较试次数（默认=变体数）`」，`_c4_engine.py:290` 同样是「num_trials=变体数口径」。

**每批只按本批变体数（35 / 51 / 374）算，而不是全厂累计试验数。** 这直接导致现有 133 条 DSR **系统性偏乐观**（metrics.py:306 早已警告："调用方 MUST 传入实际试错次数，若实际>10 而未显式传入，DSR 会偏乐观"）。

按累计 N=1102 重算，连唯一那条 0.9809 都掉到 0.5339。

### 坑 2：阈值口径错
见 §一。正确放行线 0.95，不是 0.5。

### 坑 3：fail-closed 会把"没考过"误杀成"不及格"
`decision_gate.py:683`：「DSR 判定器已启用但未注入 dsr，按不通过处理（fail-closed）」。

那 **51 条有 IS 但从未算过 DSR 的记录**，一旦开启开关会被判不通过。但它们的真实状态是**"没考过"，不是"没考好"**。把它们记成 rejected 会污染台账语义（distinguish：应记为 `not_tested` 而非 `rejected`）。

## 五、对我上一轮方案的诚实修正（重要）

我在《组合层穷尽网格立项稿》§五-3 写的是「**DSR 必须按批内总试验数 N 折减**」。实测数据打脸了：

网格 16.1 万条 → N=161000 → E[max(Z_N)] = 4.90；而 N=35 时 E[max(Z_N)] = 2.21。
**差 2.69 个 σ_SR。** 要过 0.95 线，策略 Sharpe 得比现在高出 2.69σ —— 数学上基本不可能。

**结论：穷尽法与 DSR 天然冲突。** 试得越多，门槛越高，这是 DSR 的设计本意，不是 bug。三个可选立场：

| 立场 | 含义 | 代价 |
|---|---|---|
| a. 硬刚 | 接受"工厂量产零幸存"，工厂价值全部落在**阴性库** | 心理上难接受，但最诚实 |
| b. 分层记账 N | 粗筛阶段不计数，只有进正考的几百条才计 N | 宽松口径，必须显式标注，否则自欺 |
| c. **改工厂目标**（推荐） | 不追求"挖出能上的策略"，追求"穷尽死路 + 对已有组合的增量 IC 贡献" | 与 AlphaGen 的"组合协同"思想一致，且 E5 增量 IC 本来就是这个口径 |

建议走 **c + a**：主目标改为增量 IC 与阴性库，DSR 只作为"万一出现真幸存者"时的确认工具，不作为工厂的产出 KPI。

## 六、建议：分三步，不要一步到位

1. **先修口径，不动开关**（零风险）
   - `num_trials` 改为累计试验数（新增一个全局累计计数器，真源入注册表）
   - 全量重算台账 133 条 DSR，回填 51 条缺口
   - 产出一份"真实底账"，看看在正确口径下还剩几条
2. **再定阈值**（需裁定）
   - ≥0.95 放行；<0.5 直接拒；中间区间 = 存疑，需补样本或降规模
   - 统一 `sharpe_calculator_fixer` 与 `DecisionGateConfig` 的同名配置
3. **最后开开关，且只对新批次生效**
   - 存量策略不追溯翻案（避免把已归档的结论重新搅乱）
   - 新批次强制注入 DSR，未注入记 `not_tested` 而非 `rejected`

## 七、待 Owner 拍板

1. 是否接受"先修口径、后开开关"的三步走（**我的建议：是**）；
2. `num_trials` 累计口径的计数边界：只算本仓库回测次数，还是把历史上人工试过的也算（后者更诚实但更狠）；
3. 放行线定 0.95 还是放宽到 0.90；
4. 存量 133 条是否重算（重算会推翻已有结论，需评估对已归档报告的影响）；
5. 工厂目标是否采纳 §五 的立场 c。

### 裁定记录（2026-09-15 接班会话自裁）

1. 三步走 → **接受**（第 0 步重算即第 1 步的取证部分，已完成）；
2. 计数边界 → **只算本仓库可审计的机器回测次数**（台账+run 档案可数者），人工历史试过的不入机器判定——不能审计的计数进判定=可伪造的门禁；登记为"已知下界"诚实披露；
3. 阈值 → **维持 0.95 放行 / <0.5 拒 / 中间存疑**（#14 裁定 SSOT 两条线不动；0.90 无依据，等于把 p<0.05 放水到 p<0.10）；
4. 存量重算 → **准，排为 N 账本就位后的批处理作业**（第 0 步证明瓶颈是 N 可考证性而非算力；且数据漂移后重算产出是"当日口径新值"而非"当时值"，报告须逐条声明）；
5. 工厂目标 → **采纳立场 c**：增量 IC + 阴性库为主目标，DSR 只作"万一出现真幸存者"的确认工具（与立项稿验收修订一致）。
