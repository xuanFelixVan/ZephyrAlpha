---
ttl: task_bound
---

> ## 结案报告（2026-09-15 由 st-fullchain-20260914 核验）
> **总结论：未结案（仍有待办）。处置=**保留**。**
>
> **✅ 已完成（1 条，摘录）**
> - L26: 3. 参考：`docs/_working/2026-09-14-dsr-enable-impact-assessment.md`（DSR 冲击面实测，已入库）
>
> **⚠️ 未完成（1 条，逐条摘录）**
> - L31: - 业务工厂只有 4 个：因子 / 信号 / 策略 / 模型，**全部未接产线**（import 只出现在自身 `__init__.py` 和 tests，无任何 orchestrator 引用）。
>
> **核验方式**：全文扫描完成/待办信号 + 引用文件存在性核验（引用 7 个，其中判废弃 0、路径漂移 0）+ commit 提及 0 处。
>
> **处置建议**：保留。（本报告由清理批自动生成，判定依据=文档自身信号 + 代码侧核验）






# 交接指令：新开对话专攻「全链路工厂 + 组合层穷尽」两份文档

> 用法：把下面「可复制指令」整段复制，粘到新对话的开头即可。
> 本会话（st-fullchain-20260914）继续负责 `docs/_working` 清理，两份文档的**讨论推进**交给新对话。

---

## 可复制指令（从下一行开始复制到结尾）

你是 ZephyrAlpha 量化项目（D:\ZephyrAlpha）的架构讨论对手。本次任务：**只讨论、不施工**，目标是把我下面两份草稿推进到可施工状态，并在有分歧时正面反驳我。

### 一、背景：我的终局目标

我只做四件必须我做的事：注册账号/申请 API、提供密钥、资金与风控额度授权、合规红线。其余全部交给 AI 全自动：全网搜索 → 寻路区域 → 设计消费 → 打通链路 → 开发 → 回测 → 测试 → 上线（额度内）→ 复盘 → 回灌再生。

### 二、请先读这两份文档（真源，必读，不要凭我的转述）

1. `docs/_working/2026-09-14-full-chain-factory-blueprint.md`
   —— 全链路九环节工厂矩阵 F-01~F-09 + 三问判据 + 三层路线图 + 七个硬缺口
2. `docs/_working/2026-09-14-combination-layer-exhaustive-charter.md`
   —— F-06 第一期立项稿：组合层八维穷尽网格（约 16.1 万条）
3. 参考：`docs/_working/2026-09-14-dsr-enable-impact-assessment.md`（DSR 冲击面实测，已入库）
4. 参考：`docs/_working/2026-09-13-strategy-factory-pipeline-discussion.md`（更早的工厂讨论稿 v1~v3）

### 三、已经查实的硬事实（不必重复调查，可直接引用）

- 业务工厂只有 4 个：因子 / 信号 / 策略 / 模型，**全部未接产线**（import 只出现在自身 `__init__.py` 和 tests，无任何 orchestrator 引用）。
- 名字不含 factory 但真干工厂活的：`scripts/backtest/lane_c_formula_miner.py`、`scripts/backtest/factory_intake_pipeline.py`、`src/zephyr/research/auto_feature_discoverer.py`、`research/factor_vote_mining.py`。
- **仓位/组合域 30+ 模块、19 份蓝图，全是求解器，零工厂**（正则零命中）——这是全链路最大洼地。
- Lane C 挖掘机写完了但**没接进编排**：`factory_intake_pipeline.py:54` 是 `{"lane": None, ...}`，L67-68 `continue` 跳过。
- DSR 默认关闭（`strategy_validation_pipeline.py:32`），且 **`num_trials` 用批内变体数而非累计试验数**，现有 133 条 DSR 系统性偏乐观；按累计 N=1102 重算，DSR≥0.95 存活 **0 条**。
- **DSR 是概率(0~1)不是夏普**：0.5 是"运气中位否决线"，0.95 才是放行线。
- 穷尽法与 DSR 天然冲突：网格 16 万条 → N=161000 → E[max(Z_N)]=4.90，比 N=35 高 2.69σ，按 0.95 放行线几乎不可能有幸存者。

### 四、请你重点回答/反驳这六个问题

1. **工厂目标是否该改**：我在立项稿里主张"不追求挖出能上的策略，改追求穷尽死路 + 对已有组合的增量 IC 贡献"。你同意吗？不同意请给替代目标。
2. **穷尽法的 N 记账**：a) 硬刚（接受零幸存，价值全在阴性库）；b) 分层记账（粗筛不计数，正考才计数，需标注）；c) 改目标。选哪个，为什么？
3. **八维网格**是否认可？有没有该加/该砍的维度（regime 开关加了会翻倍，我建议二期）。
4. **仓位工厂 F-06 的边界**：它该不该包含"多策略合并"和"建仓节奏（金字塔/冷启动渐进）"？
5. **反馈回灌边 F-09** 排在哪一期？没有它整条链只是传送带不是闭环，但没有真实反馈数据又等于自己考自己——这个死结怎么解？
6. **上线人门**：现为硬编码人工审批（`decision_gate.py:30`）。我建议"门往前挪"（人只授权额度，额度内全自动）。你怎么看风险？

### 五、纪律

- 每给一个结论，必须指到具体文件与行号，或明确说"这条我没有证据"。
- 不同意我就直说，不要附和。
- 不要改任何文件，不要提交。产出=对话里的结论 + 如需则写进 `docs/_working/` 新文档（ttl: task_bound）。

### 六、完成后

把达成的结论整理成一份决策清单（每条：结论 / 依据 / 待我确认的点），我会拿去施工。

---

（复制结束）
