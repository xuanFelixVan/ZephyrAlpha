---
ttl: task_bound
---

> **只读报告**：DS 视觉合规扫描（调色板外颜色检出）。生成器：.runtime/tmp/ds_compliance_scan.py。用途=全仪表盘翻新派单依据（四件套夜战 P6）。不改任何页面。

# DS 视觉合规扫描报告 · 2026-09-01 01:45 中国标准时间

许可色集：main.css :root 变量族 + DS-5 图表专项登记色（共 45 个）

## 违规总览（11 个文件，79 种越板色）

| 文件 | 越板色（出现次数） |
|---|---|
| pages\backtest.html | `#555`×2 |
| pages\cryptobt.html | `#000`×1 |
| pages\design.html | `#000`×6 `#14202e`×3 `#1a2330`×3 `#fafafa`×2 `#e8f1ff`×2 `#0a7cff`×1 `#1e3a36`×1 `#777`×1 `#404040`×1 `#453f1c`×1 `#8a8a8a`×1 `#1c2836`×1 `#f0f1f3`×1 `#1b2f45`×1 `#969696`×1 `#332f16`×1 `#2a2540`×1 `#432627`×1 `#381f21`×1 `#8a8d91`×0 `#dba20e`×0 `#e0e0e0`×0 `#ff9800`×0 `#42a5f5`×0 `#1a1d21`×0 |
| pages\experiment.html | `#210`×1 `#196`×1 |
| pages\modlib.html | `#000`×3 `#8a93a0`×0 |
| pages\overview.html | `#fff`×1 |
| pages\position.html | `#207`×2 `#205`×2 |
| pages\sentiment.html | `#23282f`×0 |
| pages\sysstatus.html | `#197`×1 `#198`×1 `#196`×1 |
| pages\task.html | `#207`×1 `#205`×1 |
| styles\main.css | `#000`×13 `#fff`×8 `#fafafa`×6 `#111`×6 `#f0f1f3`×5 `#999`×3 `#1a2330`×3 `#0a7cff`×2 `#262728`×2 `#ddd`×2 `#222324`×2 `#969696`×2 `#b3b3b3`×2 `#262626`×2 `#5a5a5a`×2 `#1a1a1a`×1 `#5a2026`×1 `#1e3a36`×1 `#453f1c`×1 `#e0e0e0`×1 `#444`×1 `#e8f1ff`×1 `#14202e`×1 `#7a2c33`×1 `#ffb3ab`×1 `#2a0e0d`×1 `#888`×1 `#3a2f00`×1 `#432627`×1 `#0d2a26`×1 `#3a3a3a`×1 `#141518`×1 `#8a8d91`×0 `#181b1f`×0 `#dba20e`×0 `#23282f`×0 `#f76816`×0 `#1a1b1d`×0 `#1a1d21`×0 |
