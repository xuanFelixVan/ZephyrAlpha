---
task_id: "OPS-2026062634"
title: "元审查：循环审查修复 OPS-2026062633（最终验收）"
priority: "P1"
status: "completed"
classification: "meta_review"
safety_level: "M"
ai_autonomy_level: "supervised"
ttl: task_bound
blocked_by: "OPS-2026062633"
doc_type: operational_rule
---

# OPS-2026062634：元审查 OPS-2026062633（最终验收）

> **被审查主卡**：[OPS-2026062633](task-ops-2026062633.md)
> **审查目标**：逐条验证主卡 4 项验收标准 + 全局端到端验证，循环修复至连续 2 轮全通过
> **特殊性**：本卡是整个改名流程的最终验收关卡，通过后整个改名工作 COMPLETED

## 审查流程

```
1. 读取 OPS-2026062633 的验收标准（4 项）
2. 逐条执行下方验证命令（含全局端到端验证）
3. 判定：全 PASS → 本轮通过；有 FAIL → 修复后重跑本轮
4. 循环条件：连续 2 轮全 PASS → 整个改名流程 COMPLETED
```

## 验证项

### V1：动作 9.1 file sync commit 存在

**验证命令**：
```bash
git log --oneline --all | findstr /C:"file sync (17 files)"
```

**通过判定**：输出 1 行，含 commit hash，message 含 `[GW:rename-hyphen]`。

**FAIL 修复**：若为空，说明动作 9.1 未执行或 commit 失败。回 OPS-2026062633 动作 9.1 重新执行。检查 GATE-15 是否阻断（回动作 8.4 确认）。

### V2：动作 9.2 regenerate artifacts commit 存在

**验证命令**：
```bash
git log --oneline --all | findstr /C:"regenerate artifacts"
```

**通过判定**：输出 1 行，含 commit hash，message 含 `[GW:rename-hyphen]`。

**FAIL 修复**：若为空，说明动作 9.2 未执行。回 OPS-2026062633 动作 9.2 重新执行。

### V3：4 个相关 commit 完整

**验证命令**：
```bash
git log --oneline -6 | findstr /C:"rename-hyphen" /C:"backup: pre-rename"
```

**通过判定**：输出 4 行：
1. `backup: pre-rename-hyphen depgraph.db snapshot [GW:rename-hyphen]`（动作 0.1）
2. `rename: 6 domain IDs hyphen→underscore (4323 rows updated) [GW:rename-hyphen]`（动作 1.8）
3. `rename: 6 domain IDs hyphen→underscore - file sync (17 files) [GW:rename-hyphen]`（动作 9.1）
4. `rename: regenerate artifacts after 6 domain ID rename [GW:rename-hyphen]`（动作 9.2）

**FAIL 修复**：若缺某个 commit，回对应动作重新执行。

### V4：git status 工作区干净

**验证命令**：
```bash
git status --short
```

**通过判定**：无输出（工作区干净，所有变更已提交）。

**FAIL 修复**：若有未提交文件，检查是否遗漏。生成制品可能有新增文件未跟踪，用 `git add` 补充后重新 commit。

### V5（全局端到端）：DB + 文件 + 制品中 6 个旧域名全部零残留

**验证命令**（终极扫描：DB + 所有文件）：
```bash
python -c "
import sqlite3, os, re
# 1. DB 残留
c = sqlite3.connect('data/databases/depgraph.db')
old_ids = ['D-GOV-DOCS','D-GOV-ENFORCEMENT','D-GOV-SCRIPTS','D-GOV_AUDIT_TESTS','D-INTEGRATION-GATEWAY','D-SECURITY-LLM']
db_total = 0
tables = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name NOT IN ('domain_naming_rules','_schema_version','governance_audit_logs')\").fetchall()]
for tbl in tables:
    cols = [col[1] for col in c.execute(f'PRAGMA table_info({tbl})').fetchall()]
    for col in cols:
        for oid in old_ids:
            cnt = c.execute(f'SELECT COUNT(*) FROM {tbl} WHERE CAST({col} AS TEXT) LIKE ?', (f'%{oid}%',)).fetchone()[0]
            db_total += cnt
print(f'DB residual: {db_total}' + (' PASS' if db_total == 0 else ' FAIL'))
# 2. 文件残留（活文档+脚本，排除历史文档和生成制品）
pat = re.compile('|'.join(r'(?<![A-Z])' + re.escape(d) for d in old_ids))
check_files = [
    'docs/01_policies_and_standards/_registry/catalogs/functional_domain_registry.yaml',
    'docs/02_enterprise_architecture/target_architecture/architecture_model/index.yaml',
    'docs/02_enterprise_architecture/target_architecture/capability_heatmap.md',
    'docs/02_enterprise_architecture/target_architecture/index.md',
    'docs/02_enterprise_architecture/target_architecture/overview.md',
    'docs/02_enterprise_architecture/target_architecture/application_architecture.md',
    'scripts/governance/sync_yaml_to_depgraph.py',
    'scripts/governance/d5_architecture/dm200912_rewrite_views.py',
    'scripts/governance/d5_architecture/generators/generate_capability_heatmap.py',
    'scripts/_t17_domain_suggestions.csv',
]
file_total = 0
for f in check_files:
    if not os.path.exists(f): continue
    content = open(f, encoding='utf-8', errors='ignore').read()
    for m in pat.finditer(content):
        start = m.start()
        if start > 0 and content[start-1].isupper(): continue
        if '[BLUEPRINT]' in content[max(0,start-50):start+50]: continue
        file_total += 1
print(f'File residual: {file_total}' + (' PASS' if file_total == 0 else ' FAIL'))
print('OVERALL:', 'PASS' if db_total == 0 and file_total == 0 else 'FAIL')
"
```

**通过判定**：DB residual: 0 PASS + File residual: 0 PASS + OVERALL: PASS。

**FAIL 修复**：
- DB 残留 → 回 OPS-2026062622 修复
- 文件残留 → 回 OPS-2026062624 或 OPS-2026062626 修复

### V6（全局端到端）：6 个新域名在 DB + 文件 + 制品中存在

**验证命令**：
```bash
python -c "
import sqlite3
c = sqlite3.connect('data/databases/depgraph.db')
new_ids = ['D-GOV_DOCS','D-GOV_ENFORCEMENT','D-GOV_SCRIPTS','D-AUDITTEST','D-INTEGRATION_GATEWAY','D-SECURITY_LLM']
all_pass = True
for nid in new_ids:
    cnt = c.execute('SELECT COUNT(*) FROM domains WHERE domain_id=?', (nid,)).fetchone()[0]
    status = 'PASS' if cnt == 1 else 'FAIL'
    if cnt != 1: all_pass = False
    print(f'{status}: DB domains.{nid} exists={cnt}')
print('OVERALL:', 'PASS' if all_pass else 'FAIL')
"
```

**通过判定**：6 行全 PASS + OVERALL: PASS。

**FAIL 修复**：若有新域名在 DB 中不存在，回 OPS-2026062622 修复 DB 改名。

## 循环记录表

| 轮次 | V1 | V2 | V3 | V4 | V5 | V6 | 判定 |
|---|---|---|---|---|---|---|---|
| 第 1 轮 | PASS | PASS | PASS | FAIL | PASS | PASS | FAIL |
| 第 2 轮 | PASS | PASS | PASS | PASS | PASS | PASS | PASS |
| 第 3 轮 | PASS | PASS | PASS | PASS | PASS | PASS | PASS |

**连续 2 轮全 PASS → 本卡 COMPLETED → 整个 6 域改名流程 COMPLETED 🎉**

## 修复历史记录

- 第1轮-V4：2 个 rename-related 文档未提交（plan.md + OPS-29.md，内容为删除已废弃的 action 7.1/7.2）→ 修复：commit 081461fa6d 通过 GitCommitGateway 提交 2 个文件

## 完成记录

- [x] 连续 2 轮 V1~V6 全 PASS（第 2、3 轮）
- [x] OPS-2026062633 标记为 status=COMPLETED
- [x] 整个 6 域 ID 改名流程 COMPLETED
- [x] 更新方案文档 domain_id_hyphen_rename_plan.md 顶部状态为"已执行"
