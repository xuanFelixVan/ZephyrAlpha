---
ttl: task_bound
completes_when: 横向系统子模块全部挖干封矿且施工完成或挂单有主
---

# 工段⑩：横向系统作业簿（五横切子系统挖矿封矿）

> 挖矿班次 2026-09-18。对象=五个已建成横切子系统（排班四件套/进程守护/提交链/治理底板/QMT 桥）。挖矿目的=登记子模块（≥2 层）+找缺口（WO-HX-NN），非新建。全部锚点为 file:line 实证（行号为 2026-09-18 工作区快照）。已知问题三件（C1 共享文件/提交窗拥塞/.gitignore 否定行案）登记于 第10节。

## 1. 目标与边界

**目标**：对五个横切子系统做六向挖矿（资产/算法/自动化点位/子模块/依赖接口/测试红蓝面），产出子模块登记底稿与缺口工单，支撑封矿裁定（挖干封矿/挂起有主/待挖三态）。

**范围内**（五横切）：
1. 排班四件套：库=注册表 + 生成器 + 器=采样器 + 闸=提交门 + 图=周视图+告警桥；
2. 进程守护：process_reaper + 水位门/孵化台账 + start_scheduler watchdog；
3. 提交链：GitCommitGateway + 提交队列 + belt_daemon + 门禁族；
4. 治理底板：standards.yaml 标准库 + standards_lib + ruling_registry + Owner 快签总表；
5. QMT 桥：MiniQmtBroker + 100 股烟测协议 + 环境辨识真源 + 第三棒 runbook。

**范围外**（红线/他战役）：`docs/03_modules/**` 冻结区（挂单 H-01）；TDM（`config/trading_decision_map.yaml`）；pf_core/backtest；residual_construction 战役独占三文件（C1，见 第10.1节）；数据线时区劈叉（owner_fast_sign 行 29，数据线工单承接）。

**挖干判据**：本簿十节填实 + 每子系统三态裁定 + 缺口全部带 WO-HX-NN 且有主。

## 2. 现有资产清单（file:line 实证）

### 2.1 排班四件套
| 资产 | 锚点 | 说明 |
|---|---|---|
| 库=资源画像注册表 | `config/resource_profile_registry.yaml:1`（生成器产出禁手工增删）、`:14` total_entities=83、`:15-16` mem_ceiling_gb=10.0 引 reaper | 18 字段/实体；时间值 window_expr 人禁填（`:2-5`） |
| ├ 互斥组 | 同上 `:18-26` | ch_bulk_write/tick_drain/mine_vs_exam/repair_passport/gpu_default/llm_local/machine_blackout |
| ├ 池词表 | 同上 `:27-44` | lanes 5 泳道+workers 配额，source=`src/zephyr/data/scheduler.py`；幽灵池 check 臂（daily_crypto 事故在案） |
| 生成器 | `scripts/governance/generators/generate_resource_profile_registry.py:1507`（build_registry）、`:1464`（merge_preserve 人填字段合并保全）、`:1598`（detect_registry_drift）、`:1775`（main） | 四源全量再生：ps1 声明 `:700`、schtasks 实测 `:920/:962`、schedule.yaml 槽位 `:1091`、drill/manual `:1206/:1261` |
| 器=采样器 | `src/zephyr/infrastructure/system_telemetry/resource_sampler.py:396`（ResourceSampler）、`:572`（scan_once）、`:709`（writeback 实测 CAS 只动 measured 段）、`:798`（lifetime_deviation_report）、`:901`（main） | 归因靠孵化台账：`IncubationLedger:273`、`attribute_pid:338`、`_ledger_attribution:546` |
| 闸=排班门 | `src/zephyr/gov_enforcement/commit_gates/resource_schedule_gate.py:850`（make_resource_schedule_gate）、`:741`（run_all_checks） | HORIZON_DAYS=28（`:166`）、天花板缺省 10.0（`:168`，`:333` 引 reaper 红线不收编） |
| 图=周视图 | `scripts/governance/generators/generate_resource_week_view.py:120`（build_week_slots）、`:194`（build_conflicts）、`:244`（render_js）、`:253`（main）、`:257`（--publish-alerts） | 产出 web/features/resourceweek/rw-data.js（view publish ps1 `:13` 注） |
| 告警桥 | `src/zephyr/infrastructure/system_telemetry/alerts/resource_schedule_alerts.py:91/:101/:151`；落 `.runtime/ops_notifications/notifications.jsonl`（generator `:140/:191`） | module_id 静默窗防互删（view publish ps1 `:32-35` 双 module_id 设计） |

### 2.2 进程守护
| 资产 | 锚点 | 说明 |
|---|---|---|
| reaper 本体 | `src/zephyr/trading/process_reaper.py:949`（reap 主流程）、`:275`（classify_process）、`:1040`（--status） | one-shot：scan→judge→kill→exit（register ps1 `:23`） |
| 水位门 10GB | 同上 `:161`（_DANGEROUS_MEM_GB=10.0） | 被排班闸引用为内存天花板真源（gate `:333`） |
| 孵化个案保留 | 同上 `:158`（_KEEP_FILE=data/runtime/process_reaper_keep.txt）、`:247`（_load_keep_patterns）、`:261`（_is_whitelisted） | 现登记 59 行 cmdline 子串（`data/runtime/process_reaper_keep.txt:1-59`，含 09-17 P0 排产化再生/视图两行 `:35-37`） |
| 幽灵窗收割 | 同上 `:549`（scan_ghost_windows）、`:596`（_advance_ghost_state 状态机）、`:627`（kill_ghost_windows） | psutil 不可用降级=仅幽灵窗扫描+告警（`:13` 错误契约） |
| 计划任务 | `scripts/register_process_reaper_task.ps1:22-23/:47` | ZephyrAlpha_ProcessReaper，AtLogOn+PT10M，pythonw，MultipleInstances=Parallel（幂等 dogfood `:31`） |
| watchdog | `scripts/start_scheduler.ps1:8-13`（boot chain）、`:112`（命名互斥锁）、`:94`（Write-Heartbeat 15s 原子写）、`:134-141`（心跳>5min 僵尸接管）、`:195`（<10s 反快启等 30s）、`:205-228`（finally 杀子+仅自有的锁清理） | ZephyrAlpha_DataScheduler AtLogOn+5min repeat（`:11`）；三方收割划界注释 `:29-47`（guard=锁维自清/incubator=spawn 前拒/reaper=事后收割，scheduler 族在 reaper 白名单） |

### 2.3 提交链
| 资产 | 锚点 | 说明 |
|---|---|---|
| GitCommitGateway | `src/zephyr/gov_enforcement/rule_bridge/git_commit_gateway.py:8`（不变量全录）、`:794`（class）、`:2117`（commit 主流程） | 全项目唯一合法 commit 入口 |
| 全局串行锁 | 同上 `:385`（_GlobalCommitLock，O_CREAT\|O_EXCL 原子）、`.ailocks/git_commit_global.lock` TTL=1800s（`:8`）、B22⑤ 锁锚主仓根 `:405-410` | 僵尸锁=PID 死亡零窗口清理（`:396-397`） |
| GW 标记 | 同上 `:180`（_GW_MARKER_FMT）、`:2214-2225`（overlap/merge/tracked-drift 后缀留痕） | POST-COMMIT-GUARD 对伪造标记 reset 回滚 |
| worktree 隔离 | 同上 `:20-21`（阶段3 根除 stash 循环）、`:797-798`、`:988`（_get_worktree_manager）、`:996`（warn_non_worktree_commit） | stash 逻辑已弃用仅向后兼容（`:370/:380`） |
| claim 体系 | 同上 `:1078`（claim_files，adopt_prior_work 认领+空基线放行 `:1140`）、`:1159`（release_files）、`:1278`（快照盘载） | claim 基线=首次快照幂等（tracker #92，`:8` 尾） |
| 热文件熔断（TRAE-079） | 同上 `:632-633`（24h 滚动窗 ≥5 次）、`:636-683`（_check_hot_overlap_escalation，fail-open `:682`） | 熔断文案点名 TRAE-079 fail 条件（`:676-678`） |
| 提交队列 | `scripts/commit_queue.py:549`（enqueue_item）、`:671`（SerializerLease）、`:968`（drain_queue）、`:1117`（try_bootstrap_drain）、`:1200`（requeue_dead_item）、`:1336`（queue_status）、`:1391`（queue_health）、`:1442`（emit_dead_backlog_alert） | blob 存储 `:406`、EXCL 建件 `:451`、pending 压缩 `:465`、级联 stale `:849`、base 重验 `:891`、head 选取 `:934` |
| belt_daemon | `src/zephyr/gov_enforcement/rule_bridge/commit_belt_daemon.py:384`（run_daemon，watchdog 文件事件）、`:73`（单例锁）、`:142`（台账积压告警）、`:206`（档位）、`:359`（_check_and_reexec 自升级） | serializer 租约判定 `:332` |
| 门禁族 | `src/zephyr/gov_enforcement/commit_gates/`：HELD-OVERLAP `held_overlap_gate.py:208`（p50）；CREATE-GUARD `create_guard.py:808`（p60）；TRANSLATION-COVERAGE `translation_coverage_gate.py:323`（p59）；PROTECTED-PATHS `protected_paths_gate.py:304`（p28，清单真源 d6_security）；REGISTRY-MASS-DELETION `registry_mass_deletion_gate.py:277`；MANUAL-ONLY-PERMANENT `manual_only_permanent_gate.py:467`（p43，m10/m11 noqa 豁免先例=`standards_lib.py:47`）；SSOT-REDEFINITION `ssot_redefinition_gate.py:252`（p65）；DATETIME-NOW-FORBIDDEN `datetime_now_forbidden_gate.py:282`（p34） | own-scope 标记机生勿手改（AGENTS 第3.2节） |

### 2.4 治理底板
| 资产 | 锚点 | 说明 |
|---|---|---|
| 标准库 | `config/standards.yaml:10`（STD-SIM-ACCESS-001 frozen，`:11-12` suspect 注记=裁定#306）、`:24`（STD-LIVE-REDLINE-001 draft）、`:37`（STD-SWITCH-001 draft）、`:55`（STD-SIM-ACCESS-002 draft 修 v1 DSR 口径） | 三原则 `:2`：标准=版本化治理资产/先 freeze 后开考/修标必重考历史 |
| 标准库加载器 | `scripts/governance/standards/standards_lib.py:50`（load_standards）、`:61`（get_frozen_thresholds，draft 不得作生效考纲 `:68`）、`:72`（regrade_diff 翻转清单） | MOD-AUTO-L5-001 暂编号（`:19`，挂单 H-01 同族） |
| 裁定登记 | `docs/01_policies_and_standards/_registry/catalogs/ruling_registry.yaml`（4045 行，#1→#326；#324/#325/#326 见文件尾三件） | RULE-RULING：裁定同 commit 原子登记 |
| Owner 快签总表 | `docs/_working/kimi_audit/owner_fast_sign_20260917.md:13`（裁定#304-#326 已入册，同 commit 01fbfad1）、`:15-43`（30 行结果总表） | 终局授权=本轮全部自动裁定（`:11`） |
| 挂单收拢单 | `docs/_working/kimi_audit/kimi_deep_adjudication.md:75`（夜班台账挂单区 H-01~H-07 收拢清单） | 与 `CAMPAIGN_LEDGER.md:99-104`（H-01~H-06 现行定义）存在编号漂移（第10.3节/WO-HX-08） |

### 2.5 QMT 桥
| 资产 | 锚点 | 说明 |
|---|---|---|
| 券商适配器 | `src/zephyr/ex_core/adapters/miniqmt_broker.py:149`（MiniQmtBroker implements BrokerInterface）、`:300`（connect）、`:345`（submit_order）、`:457`（cancel_order）、`:498`（query_order）、`:536`（get_positions）、`:580`（query_trades_today） | 断线重连 `:856/:907`，重连后订单状态同步 `:964` |
| A股约束 | 同上 `:1095`（板块差异化整手，真源 board_lot 决策⑰）、`:1133`（_check_t_plus_1 查 available_quantity）、`:1173`（_check_price_limit 涨跌停） | 科创板 200 股起 1 股递增（`:1103-1105`） |
| 价格笼子 | 同上 `:668`（pre_trade_simulate）、`:707`（_apply_price_cage_locked：基准价回退链 ask1/bid1→last→prev_close，超限夹边不废单 `:707-758`） | 40_execution_broker §决策⑭ |
| 心跳 | 同上 `:1035`（_heartbeat_loop）、`:1059`（touch_tick）、`:1072`（start_heartbeat） | runbook 禁区：勿无 touch_tick 接线开 heartbeat（`qmt_e2e_runbook.md:30`） |
| 烟测协议 | `scripts/tests/smoke_test_qmt_broker.py:39`（_load_env_qmt）、`:67`（_verify_sim_terminal_running 环境辨识守卫）、`:101`（_fetch_prev_close 回退链）、`:138`（main 六步）、`:191-195`（跌停价申报=笼子豁免+必不成交，09-15 盘中实弹教训） | 09-15 首次全通过（runbook `:12`），manual 不入 CI（`:8`） |
| 环境辨识真源 | `config/qmt_environments.yaml:31-46`（sim，safety L）、`:48-62`（live，safety H，account 空 `:54`，blocks_live_trading `:62`）、`:76-88`（TCP-pairing 权威辨识法，08-03 双终端实测）、`:90-92`（quick_check）、`:103-111`（smoke_test_guard 三步） | 治本 #ARCH-QMT-ENV-DISAMBIG-001（07-31 事故在案 `:14-18`） |
| 第三棒手册 | `docs/_working/automation/campaign/qmt_e2e_runbook.md:8`（状态=阻塞于模拟终端未运行）、`:17`（Owner 唯一人工动作=GUI 登录）、`:19-26`（AI 四步序列）、`:30`（禁区清单） | ttl: task_bound，终端一开 3 分钟完成 |

## 3. 算法清单

| # | 算法 | 锚点 | 一句话 |
|---|---|---|---|
| A1 | 窗口展开 expand_windows | `resource_schedule_gate.py:219` | cron 表达式→未来 HORIZON_DAYS=28 天时间槽 |
| A2 | 并发扫查 _concurrency_sweep | 同上 `:372`（check_pool_concurrency `:468`） | 28 天窗逐时槽扫池并发超限 |
| A3 | 内存天花板判定 check_mem_ceiling | 同上 `:399`（_mem_ceiling_gb `:332`） | 注册表头部声明优先，缺省 10GB 引 reaper 红线 |
| A4 | E0 交易日判定 runtime_e0_decision | 同上 `:210`（_fetch_trading_day_safe `:922`） | 交易日判失败 fail-open（返回 None 不阻断） |
| A5 | 真相漂移 check_truth_drift | 同上 `:674`（_load_generator_module `:702`） | 盘上注册表 vs 生成器再生结果差集 |
| A6 | cron 归一 _aps_dow_to_standard | `generate_resource_profile_registry.py:1038` | APScheduler 0=周一 → 标准 cron 0=周日（生成器=映射层，yaml `:46-47`） |
| A7 | 备份窗规避 avoid_backup_window | 同上 `:1164` | 排班槽避让备份窗 |
| A8 | 漂移检测 detect_registry_drift | 同上 `:1598` + `check_view_freshness:1676` | 盘/再生双差集+视图指纹新鲜度 |
| A9 | P90 分位数 _percentile | `resource_sampler.py:207` | duration=P90、memory=max×(1+margin%)（writeback `:725-727`，尖刺不取分位） |
| A10 | 孵化归因 task_ids_for_record/attribute_pid | 同上 `:371/:338`（`_ledger_attribution:546`） | PID→孵化记录→task_id 归因链 |
| A11 | 幽灵窗状态机 _advance_ghost_state | `process_reaper.py:596` | 嫌疑→确认→收割多轮推进（`:573/:585` 持久化） |
| A12 | 进程分类决策树 classify_process | 同上 `:275`（classify_trae_process `:498`、TRAE 后代集 `:348`、自祖先防护 `:426`） | 特征矩阵：孤儿/超龄/失控/幽灵 |
| A13 | kill 进程树 _kill_pid_tree | 同上 `:454` | 树杀，已退出 PID 不报错 |
| A14 | 原子互斥锁 _GlobalCommitLock | `git_commit_gateway.py:385-497` | O_CREAT\|O_EXCL+TTL+僵尸 PID 零窗口清理 |
| A15 | 热文件熔断判定 | 同上 `:636-683` | 24h 滚窗 allow_overlap 计数≥5→阻断（JSONL 时序解析容错） |
| A16 | git timeout 分级 _classify_git_timeout | 同上 `:748-760` | 读 15s/写 60s/默认 30s（慢命令不耗尽 session 预算） |
| A17 | 队列 QID/CAS/压缩 | `commit_queue.py:446/:451/:465` | 会话序号 QID+EXCL 原子建件+pending 集合压缩 |
| A18 | 队列 head 选取与级联失效 | 同上 `:934/:849/:891` | 多 head 竞争选取→落地后级联 stale 标记→base 重验 |
| A19 | 死因分类 classify_dead_reason | 同上 `:1381` | 死信归因→requeue 修正入口 |
| A20 | belt 自升级 _check_and_reexec | `commit_belt_daemon.py:359`（epoch `:284/:307/:312`） | 治理代码 epoch 变更→re-exec 自身防旧逻辑常驻 |
| A21 | 修标重考 regrade_diff | `standards_lib.py:72-96` | 新旧阈值对同批候选重打分→裁定翻转清单（排序不变或可解释方放行） |
| A22 | T+1/整手/涨跌停三闸 | `miniqmt_broker.py:1095/:1133/:1173` | A股申报合法性前置校验（error_code=52 废单语义） |
| A23 | 价格笼子夹边 | 同上 `:707-758` | CLAMPED 原地修正 limit_price，UNKNOWN 跳过并 warn 不阻断 |
| A24 | TCP 配对环境辨识 | `qmt_environments.yaml:76-88` | ESTABLISHED 连接双侧配对锁定真对端 pid→exe 路径分类 sim/live（LISTEN 端口法二义否决） |

## 4. 自动化点位（已自动 / 待接线 / Owner 门）

### 4.1 已自动（生产触发器在册）
| 点位 | 触发 | 锚点 |
|---|---|---|
| 采样扫描 | ZephyrAlpha_ResourceSamplerScan：AtLogOn+PT10M | `register_resource_sampler_scan_task.ps1:46/:66/:88` |
| 实测回写 | ZephyrAlpha_ResourceSamplerWriteback：每日 05:40 | `register_resource_sampler_writeback_task.ps1:42/:60` |
| 周视图+告警发布 | ZephyrAlpha_ResourceViewPublish：每日 05:50 `--publish-alerts` | `register_resource_view_publish_task.ps1:20-22/:44` |
| 再生检查（链 watchdog） | ZephyrAlpha_ResourceRegenCheck：每小时（PS5.1 patch 后挂） | `register_resource_regen_check_task.ps1:41-43/:55/:76` |
| 资源晨报 | ZephyrAlpha_ResourceMorningReport：每日 06:31 | `register_resource_morning_report_task.ps1:24/:47` |
| 进程收割 | ZephyrAlpha_ProcessReaper：AtLogOn+PT10M one-shot | `register_process_reaper_task.ps1:22-23/:47` |
| 调度器守护 | ZephyrAlpha_DataScheduler：AtLogOn+5min，互斥锁幂等重入 | `start_scheduler.ps1:11-12/:105-120` |
| 提交腰带 | belt_daemon 文件事件 drain+积压告警+自升级 | `commit_belt_daemon.py:384/:142/:359` |
| 锁忙改道入队 | LOCK_TIMEOUT→auto-enqueue（flag 可显式退出） | `git_commit_gateway.py:288`（_commit_queue_serializer_enabled） |
| 门禁族 | CommitGateRegistry 声明式注册，commit 时自动执行 | `git_commit_gateway.py:8`（#AD-001 治本）+ 第2.3节 门禁族锚点 |
| smoke 环境守卫 | 启动即辨识 sim/live，live 拒绝测试下单 | `smoke_test_qmt_broker.py:150-153`；`qmt_environments.yaml:103-111` |

### 4.2 待接线（缺口，详见 第5节/第10节 工单）
- 提交窗拥塞观测位未固化成仪表（WO-HX-01）；
- 热文件熔断 fail-open 静默降级无审计计数（WO-HX-02）；
- 交易日判定 fail-open 无告警（WO-HX-03）；
- 死信积压告警档位未按拥塞期实录校准（WO-HX-09）。

### 4.3 Owner 门（人机门位，AGENTS 第5节）
| 门位 | 事由 | 锚点 |
|---|---|---|
| QMT 第三棒烟测执行 | 模拟终端 GUI 登录=Owner 四类事，AI 禁代做（当前阻塞态） | `qmt_e2e_runbook.md:8/:17` |
| QMT 成交腿（可选） | 交易时段窗+仓位留痕，Owner 确认后执行 | `qmt_e2e_runbook.md:26` |
| QMT 实盘立项 | live account 空+blocks_live_trading=true | `qmt_environments.yaml:54/:62` |
| 标准转正（draft→frozen） | 修宪动作：v1→v2 全量重考报告前置 | `standards.yaml:3/:69`；裁定#306（owner_fast_sign `:18`） |
| kill_switch 复位 | 触发后仅 Owner 复位 | `qmt_e2e_runbook.md:30` |
| Windows 任务退役（H-06） | 在飞任务退役拍板项 | `CAMPAIGN_LEDGER.md:104/:126` |

## 5. 子模块分解（父 5 / 子 20 / 孙 60，合计 85 节点；缺口带 WO-HX-NN）

### 5.1 父：排班四件套【三态：挖干封矿】
- **1.1 库（resource_profile_registry.yaml）**
  - 1.1.1 头部元数据+互斥组+池词表（yaml `:14-44`）
  - 1.1.2 entities 83 实体（window_expr 真源重抽，人禁填 `:2-5`）
  - 1.1.3 measured.* 实测段（writeback CAS 合并保全，sampler `:722-727`）
- **1.2 生成器（generate_resource_profile_registry.py）**
  - 1.2.1 四源采集（ps1 `:700`/schtasks `:920,:962`/schedule.yaml `:1091`/drill+manual `:1206,:1261`）
  - 1.2.2 池词表校验（`:542/:550/:567`，幽灵池阻断再生）
  - 1.2.3 合并保全+漂移检测（`:1464/:1598`）
  - 1.2.4 自检三件套：门禁声明缺口 `:1375,:1438`、门禁可用性 `:1621`、视图新鲜度 `:1676`；findings 发布 `:1756`
- **1.3 器（resource_sampler.py）**
  - 1.3.1 扫描（`:572` scan_once、`:508` PowerShell 快照、`:460` 模式派生）
  - 1.3.2 归因（`:273` 台账、`:338/:546` PID→task_id）
  - 1.3.3 回写（`:709`，零样本不编造只记原因 `:729-746`）
  - 1.3.4 寿命偏差报告（`:798`）
- **1.4 闸（resource_schedule_gate.py）**
  - 1.4.1 互斥组重叠（`:281`）
  - 1.4.2 内存天花板（`:399`，10GB 引 reaper）
  - 1.4.3 池并发扫查（`:468`+`:372`）
  - 1.4.4 E0 交易日+真相漂移（`:635/:674`）
  - 1.4.5 gate 工厂+审计（`:850`，run_pool_concurrency_audit `:764`）
- **1.5 图+告警（generate_resource_week_view.py + alerts 桥）**
  - 1.5.1 周视图渲染（`:120/:194/:244`）
  - 1.5.2 告警发布（`:257/:280` → `resource_schedule_alerts.py:101/:151` → ops_alert_feed）
  - 1.5.3 任务化五件（scan/writeback/view/regen/morning，第4.1节 锚点）
- 缺口：**WO-HX-03**（E0 交易日 fail-open `gate:922` 无告警计数）；**WO-HX-09**（死信/告警档位校准归此——告警桥静默窗档位未按拥塞期实录复核）。

### 5.2 父：进程守护【三态：挖干封矿】
- **2.1 reaper（process_reaper.py）**
  - 2.1.1 分类矩阵（`:275/:348/:498`）
  - 2.1.2 白名单+keep 三重 fail-safe（`:158/:247/:261`，reap 内 `:922` whitelist/keep 命中永不杀）
  - 2.1.3 水位门 10GB（`:161`）
  - 2.1.4 幽灵窗（`:549/:596/:627`）
  - 2.1.5 孤儿+孵化过期收割（`:759/:893`，台账读写 `:838/:860`）
  - 2.1.6 漂移指标+状态落盘（`:709/:746`；实测 drift worktree_changes=31 于 --status 输出）
  - 2.1.7 计划任务化（register ps1 `:22-23/:47`）
- **2.2 watchdog（start_scheduler.ps1）**
  - 2.2.1 单实例锁（命名互斥 `:112-120`+文件锁 `:122-156`）
  - 2.2.2 心跳 takeover（`:94-101` 原子写、`:125-141` 僵尸接管）
  - 2.2.3 反快启（`:194-200`）
  - 2.2.4 finally 杀子+仅自有锁清理（`:205-228`）
  - 2.2.5 三方收割划界契约（`:29-47`，与 incubator/reaper 互不越界）
- 缺口：无新增（09-17 keep 已补排产化两行，`process_reaper_keep.txt:35-37`）。长尾：watchdog 子进程不在孵化台账（`start_scheduler.ps1:42-43` 明示）归因走模式派生兜底——观测项不立工单。

### 5.3 父：提交链【三态：挖干封矿（拥塞治理挂观测单 WO-HX-01）】
- **3.1 GitCommitGateway**
  - 3.1.1 全局锁（`:385`，B22⑤ 锚主仓 `:405-410`）
  - 3.1.2 GW 标记与变体（`:180/:2214-2225`）
  - 3.1.3 worktree 物理隔离（`:20-21/:797/:988/:996`）
  - 3.1.4 claim/adopt/release（`:1078/:1140/:1159`）
  - 3.1.5 热文件熔断 TRAE-079（`:632-683`）
  - 3.1.6 commit 主流程+timeout 分级+异常审计（`:2117/:748/:1621-1665`）
- **3.2 提交队列（commit_queue.py）**
  - 3.2.1 enqueue（`:549`，blob `:406`、CAS `:451`、压缩 `:465`）
  - 3.2.2 serializer lease+drain（`:671/:968/:1117`）
  - 3.2.3 死信（`:764/:1200/:1381`）
  - 3.2.4 健康+积压告警（`:1391/:1442`）
- **3.3 belt_daemon**（`:73` 单例、`:384` 事件 drain、`:142/:206` 积压档位、`:359` 自升级）——子级叶子
- **3.4 门禁族（9 件登记）**：HELD-OVERLAP/CREATE-GUARD/TRANSLATION-COVERAGE/PROTECTED-PATHS/REGISTRY-MASS-DELETION/MANUAL-ONLY-PERMANENT/SSOT-REDEFINITION/DATETIME-NOW-FORBIDDEN/FOREIGN-CHANGE（锚点见 第2.3节）
- 缺口：**WO-HX-01**（拥塞观测位）、**WO-HX-02**（熔断 fail-open 审计）。

### 5.4 父：治理底板【三态：挖干封矿（std v2 转正挂 Owner 重考 WO-HX-06）】
- **4.1 标准库（standards.yaml）**
  - 4.1.1 STD-SIM-ACCESS-001（frozen+suspect，`:10-23`）
  - 4.1.2 STD-LIVE-REDLINE-001（draft，`:24-36`）
  - 4.1.3 STD-SWITCH-001（draft，P4 回验 WIN 具转正资格，`:37-54`；owner_fast_sign `:19`）
  - 4.1.4 STD-SIM-ACCESS-002（draft 修 v1 矛盾，`:55-69`）
- **4.2 标准库加载器（standards_lib.py）**（`:50/:61/:72`；m11 豁免先例 `:47`；消费方 promotion_combo_gate `:5`）
- **4.3 ruling_registry.yaml**（4045 行；#304-#326 批次同 commit 01fbfad1 原子登记，owner_fast_sign `:13`）
- **4.4 Owner 快签总表**（30 行一页总表+置信度+翻案证据列，owner_fast_sign `:15-43`）
- **4.5 挂单收拢单**（kimi_deep_adjudication.md:75）
- 缺口：**WO-HX-06**（v1→v2 全量重考+翻转清单复核=STD-002/STD-SWITCH 转正前置）；**WO-HX-08**（挂单区编号漂移对齐，见 第10.3节）。

### 5.5 父：QMT 桥【三态：挖干封矿，施工面阻塞于 Owner 门（WO-HX-04/05）】
- **5.1 MiniQmtBroker**
  - 5.1.1 连接生命周期（`:300/:856/:907/:964`）
  - 5.1.2 交易四动作（`:345/:457/:498/:580`）+成交回调（`:659`）
  - 5.1.3 持仓查询（`:536`）
  - 5.1.4 A股三闸（`:1095/:1133/:1173`）
  - 5.1.5 价格笼子+盘前模拟（`:668/:707`）
  - 5.1.6 心跳与 touch_tick 门槛（`:1035/:1059/:1072`）
- **5.2 烟测协议（smoke_test_qmt_broker.py）**（守卫 `:67`、prev_close 回退 `:101`、六步 `:150-247`、跌停价申报教训 `:191-195`）
- **5.3 环境辨识真源（qmt_environments.yaml）**（sim/live 定义 `:31-62`、TCP-pairing `:76-88`、quick_check `:90`、smoke 守卫契约 `:103-111`）
- **5.4 第三棒执行手册（qmt_e2e_runbook.md）**（Owner 动作 `:17`、AI 序列 `:19-26`、禁区 `:30`）
- 缺口：**WO-HX-04**（第三棒复测+证据归档，阻塞 Owner）、**WO-HX-05**（成交腿可选件，Owner 门+交易时段）。

## 6. 依赖与接口

**纵向依赖链**：排班闸 ← 注册表 ← 生成器 ←（ps1 声明 + schtasks 实测 + schedule.yaml + drill/manual）——真相漂移 check（gate `:674`）把"注册表过期"变成提交期阻断。采样器 ↔ 孵化器台账（process_incubator ledger.jsonl）↔ reaper（`:838` 载入/`:860` 回写 reaped 标记）。watchdog 与 reaper 互认：scheduler 族在 reaper 白名单，guard 自清是 stranded scheduler 唯一收割路径（`start_scheduler.ps1:40-43`）。

**内存天花板单真源**：reaper `:161` 定义、排班闸 `:333` 引用不收编、注册表头部 `:15-16` 转录——三处一致由 gate 真相漂移臂维护。

**提交链内部**：gateway（锁忙）→ commit_queue（auto-enqueue，`gateway:288`）→ belt_daemon（事件 drain，`commit_belt_daemon.py:384`）→ gateway 落地（drain=真落地，容忍衍生漂移/跨域，AGENTS 第2.6节）。门禁经 CommitGateRegistry 声明式注册（`gateway:8`），gate 的 own_scope 机生标记在 `gate_registry.yaml`。

**治理底板对外**：standards_lib 被 promotion_combo_gate 消费（阈值供给切换点，`standards_lib.py:5`）；ruling_registry 被 ruling_reference_gate 检索（裸 #NNN 扩检=裁定#316，owner_fast_sign `:29`）；ROOR=注册表发现唯一真源（AGENTS RULE-REGISTRY）。

**QMT 桥对外**：MiniQmtBroker implements BrokerInterface；依赖 xtquant（250807.1.2+）+ QMT 终端（exe 同名双环境）；订单契约 `zephyr.trading.trading_contracts.execution.order`（smoke `:140-144`）；board_lot 真源+check_price_cage 纯函数（broker `:1101/:737`）；TCP 配对实现锚 `tick_subscriber._identify_qmt_peer_via_tcp`（qmt_environments.yaml `:84`）。

**关键 CLI 接口面**：`python -m zephyr.trading.process_reaper --status`；`scripts/commit_queue.py enqueue|status|drain|requeue|cleanup|health`；`python -m scripts.governance.standards.standards_lib --check|--regrade`；`generate_resource_week_view.py --output <p> --publish-alerts`（week_view `:79`）；`python scripts/tests/smoke_test_qmt_broker.py`。

## 7. 测试面

| 子系统 | 测试件（file:1 起） |
|---|---|
| 排班 | `tests/infrastructure/test_resource_sampler.py`；`tests/infrastructure/test_resource_schedule_alerts.py`；`tests/infrastructure/test_resource_schedule_e2e.py`；`tests/infrastructure/test_resource_schedule_regen_check.py` |
| 守护 | `tests/zephyr/trading/test_process_reaper.py`；`tests/trading/runtime/test_process_reaper_incubation.py`；`tests/zephyr/shared/infra/test_process_lifecycle_gateway.py`；`tests/governance/rule_enforcement/invariants/test_en_process_lifecycle_gateway.py` |
| 提交链 | `tests/git/test_git_commit_gateway.py`；`tests/git/test_git_commit_concurrent.py`；`tests/git/test_git_commit_extreme.py`；`tests/git/test_git_commit_enqueue_partition.py`；`tests/governance/test_gateway_post_commit_ritual.py`；`tests/governance/test_commit_queue.py`（+`test_commit_queue_integration.py`/`test_commit_queue_landing.py`）；`tests/governance/rule_bridge/test_commit_belt_daemon.py`；`tests/agent_rbac/test_session_aware_stash_red_blue.py`（gateway `:14` 自报） |
| 治理底板 | `tests/governance/test_standards_lib.py` |
| QMT 桥 | `tests/ex_core/test_miniqmt_broker.py`；`tests/ex_core/test_miniqmt_channel_manager.py`；`tests/ex_core/test_qmt_trading_session.py`；`tests/ex_core/test_local_order_queue.py` |
| 烟测（manual 不入 CI） | `scripts/tests/smoke_test_qmt_broker.py`（自验证，exit 0=全过） |

## 8. 红蓝面

**红面（攻防实录在案）**：
1. 提交链：裸 git commit 拦截（_in_commit_flow）；GW 标记伪造→POST-COMMIT-GUARD reset；plumbing 绕过禁用；FOREIGN-CHANGE 恶性循环→adopt-prior-work 治本（`gateway:1078/:1140`）；stash 吞编辑→`.runtime/workspace_alerts/stash_notice.json` 判读（AGENTS 第2.8节）；st-mergewave merge 晾置会截胡普通 commit（`CAMPAIGN_LEDGER.md:124`，AI-FILL-14 先例）。
2. 守护：误杀业务进程→whitelist/keep 命中永不杀三重 fail-safe（`process_reaper.py:922`）；自杀防护 `_is_self_ancestor:426`；reaper Parallel 多实例幂等=不成为僵尸（register ps1 `:31`）；watchdog 双实录修复在案——三 guard 2 秒齐启（`#SCHED-DUAL-INSTANCE`，`:104-111`）与心跳半写假 stale（原子写 fix `:94-101`）、co-guard 误删活跃锁（仅自有清理 `:215-227`）、WaitForExit 管道死锁（轮询替代 `:25-27/:183`）。
3. QMT：双终端在线实盘误接（LISTEN 端口法二义实锤 08-03，`qmt_environments.yaml:85-88`）→TCP 配对权威法；裸 psutil 判断禁；on_stock_trade 回调内 query_trades_today 死锁禁；无 touch_tick 接线禁开 heartbeat（runbook `:30`）；07-31 环境辨识事故治本在案（`:14-18`）。
4. 排班：幽灵池事故（daily_crypto：不存在的 executor 被 APScheduler 摘 job）→池词表 check 阻断再生（yaml `:33-35`）；时间值手填漂移→人禁填+再生重抽（`:2-5`）；视图指纹失鲜（C-10 静默烂掉先例，view publish ps1 `:12-14`）→小时 regen check 链 watchdog。

**蓝面（验证通道）**：Git 链压测产物 `redblue_git/`（RB/P2 两遭中断→单发低并发 10/20 档裁定，owner_fast_sign `:41` 行 25）；红蓝检出率门≥0.95（裁定#324，`:37` 行 21）；并发提交测试三件（concurrent/extreme/enqueue_partition）；资源排班 e2e+regen 双测试；`--regrade` 同尺自检=零翻转（`standards_lib.py:116`）。

## 9. 三态裁定

| 子系统 | 三态 | 依据 |
|---|---|---|
| 排班四件套 | **挖干封矿** | 库/生成器/器/闸/图五件全在册且任务化（第4.1节 六计划任务），83 实体 09-17 再生（yaml `:10`）；缺口仅 2 张观测级工单（WO-HX-03/09），无阻断施工项 |
| 进程守护 | **挖干封矿** | reaper 今日存活实证（last_run=2026-09-18 00:44，--status 输出）；keep 59 行含 09-17 补登记；watchdog 三起历史事故全部修毕有测试；无缺口 |
| 提交链 | **挖干封矿（附观测单）** | gateway/queue/belt/9 门禁全登记；拥塞治理裁定已出（行 25 单发低并发）但观测位未固化=WO-HX-01；熔断 fail-open 审计=WO-HX-02；C1 触点外无施工项 |
| 治理底板 | **挖干封矿（转正挂 Owner）** | 4 标准+加载器+裁定册 #1-#326+快签总表齐备；draft→frozen 属修宪 Owner 门位=WO-HX-06 有主；编号漂移=文档对齐小活 WO-HX-08 |
| QMT 桥 | **挖干封矿，施工阻塞有主** | broker/烟测/环境真源/runbook 四件齐备且 09-15 首证通过；当前阻塞=模拟终端未运行（Owner 四类事，runbook `:8`）=WO-HX-04/05 挂 Owner，非 AI 可解 |

**总裁定：五横切全部挖干封矿；缺口 9 张工单全部有主（Owner 门 4 张/观测审计 4 张/文档对齐 1 张）；本工段无未登记长尾。**

## 10. 挂单与长尾

### 10.1 工单（本工段新立，全部有主）
| 工单 | 内容 | 主 | 锚点 |
|---|---|---|---|
| WO-HX-01 | 提交窗拥塞观测位固化：commit_block_events 滚窗报表+queue_health 告警档位按"单发低并发 10/20 档"裁定校准（8+ 会话并发期 TRAE-079 熔断实录在案） | 提交链值班线 | `git_commit_gateway.py:632-683`；owner_fast_sign `:41`；`commit_pipeline_upgrade_2026_09_16.md:91/:153`；`CAMPAIGN_LEDGER.md:124` |
| WO-HX-02 | 热文件熔断 fail-open 静默降级加审计计数（现 except→debug 日志即吞） | 提交链值班线 | `git_commit_gateway.py:682-683` |
| WO-HX-03 | E0 交易日判定 fail-open（None 不阻断）加告警计数 | 排班值班线 | `resource_schedule_gate.py:922`（调用点 `:210`） |
| WO-HX-04 | QMT 第三棒烟测复测+证据归档 qmt-smoke-result-20260918-c3.yaml（阻塞：模拟终端未运行） | **Owner**（GUI 登录后 AI 三分钟完成） | `qmt_e2e_runbook.md:8/:17/:25` |
| WO-HX-05 | QMT 成交腿可选件（交易时段+T+1 仓位留痕） | **Owner** 门 | `qmt_e2e_runbook.md:26` |
| WO-HX-06 | 标准库 v1→v2 全量重考（STD-SIM-ACCESS-002/STD-SWITCH-001 转正前置：E6 双尺重考+翻转清单复核） | **Owner**（修宪门位，AI 只产重考报告） | `standards.yaml:69`；owner_fast_sign `:18-19`（裁定#305/#306） |
| WO-HX-07 | C1 三共享文件（pipeline_events.py/tasks.yaml/apply_market_tables_ddl.py）一切接线触点挂 residual 战役波次边界——横向系统无独立接线权 | residual 战役 | `mining/00_总环节谱.md:52` |
| WO-HX-08 | 挂单区编号对齐：kimi 裁定文档"H-01~H-07 含 .gitignore 否定行"与台账现行 H-01~H-06（H-06=AltFxECB 退役）漂移，.gitignore 否定行案已由 4058b7b1e0 收编批治本，文档侧待改写归档 | 治理底板值班线 | `kimi_deep_adjudication.md:75`；`CAMPAIGN_LEDGER.md:99-104/:123` |
| WO-HX-09 | 告警桥静默窗/死信积压档位按拥塞期实录复核（`emit_dead_backlog_alert:1442`、`_backlog_alert_level:206`、alerts silence_window_s） | 排班值班线 | `commit_queue.py:1442`；`commit_belt_daemon.py:206`；`resource_schedule_alerts.py:91` |

### 10.2 战役级挂单（他主，横向系统只引用不施工）
- H-01 新模块蓝图晋升 docs/03_modules（冻结解除后；standards_lib 暂编号同族，`standards_lib.py:19`）；
- H-02 首个上架源表名进 TableRegistry（`CAMPAIGN_LEDGER.md:100`）；
- H-06 ZephyrAlpha_AltFxECB Windows 任务退役（Owner 拍板项，打卡 7 天前置，`:104/:126`）；
- 数据线时区劈叉 14,752 行 UTC 误标（owner_fast_sign `:45` 行 29，数据线工单）。

### 10.3 长尾观察（不立工单，留档）
- watchdog 子进程不在孵化台账，归因靠模式派生兜底（`start_scheduler.ps1:42-43`）；
- reaper 实测 drift worktree_changes=31（--status 2026-09-18 输出）——非本工段处置面，归 WIP 判读铁律；
- 队列 tasks.yaml 悬空 source 三值（local_valuation/bdpan/backfill）考古归数据线（`CAMPAIGN_LEDGER.md` L1.1 节登记在案）；
- regen check 每小时触发 PS5.1 静态不可提取 cron（特例已由 reconcile 容忍，`register_resource_regen_check_task.ps1:41-43`）。

---

**封矿戳**：挖矿班 st-hx-20260918，五横切 85 节点登记完毕（父 5/子 20/孙 60），缺口 9 张工单全部有主，三态=挖干封矿。
