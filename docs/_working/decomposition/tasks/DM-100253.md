---
module_id: DM-100253
title: "DM-100253: 删除 depgraph.db domains 表 9 个小写命名域脏数据记录"
doc_type: audit_report
status: active
version: 1.0.0
date: 2026-06-27
owner: ZephyrAlpha-Owner
ttl: task_bound
completes_when: "DM-100253 9 个小写命名域脏数据清理完成且任务状态为 DONE"
---

# DM-100253: 删除 depgraph.db domains 表 9 个小写命名域脏数据记录

| 字段 | 值 |
|------|-----|
| task_id | DM-100253 |
| namespace | DM |
| title | 删除 depgraph.db domains 表 9 个小写命名域脏数据记录 |
| status | BLOCKED |
| priority | P1 |
| phase | 3 |
| execution_model | glm |
| safety_level | M |
| directive | PANORAMA-REBUILD |
| classification | internal |
| ai_autonomy_level | supervised |
| fallback_model | deepseek |
| source_blueprint | PANORAMA-REBUILD-DISCUSSION |
| source_section | §二 / §三.1 |
| blocked_by | DM-100252 |

## description

根因：D:\ZephyrAlpha\data\databases\depgraph.db 的 domains 表存在 9 个小写命名域脏数据（data/governance/infrastructure/intelligence/observability/orchestration/resilience/security/testing），由 sync 脚本 bug 产生，nodes 表中无对应 domain_id 的节点，污染全景图域定义使域数虚高为 61。治根：从 domains 表物理删除这 9 条脏数据记录，恢复 domains 表为纯 D-XXX 标准域（52 域）。施工步骤：先备份 D:\ZephyrAlpha\data\databases\depgraph.db 到 D:\ZephyrAlpha\data\databases\depgraph.db.backup.pre_clean_domains，然后执行 DELETE FROM domains WHERE domain_id NOT LIKE 'D-%' 删除 9 个小写域（已确认 nodes 表无小写 domain_id 节点，删除安全）。验收标准：SELECT COUNT(*) FROM domains WHERE domain_id NOT LIKE 'D-%' 返回 0，且 domains 总行数为 52。

## files_in_scope

- D:\ZephyrAlpha\data\databases\depgraph.db

## deliverables

- D:\ZephyrAlpha\data\databases\depgraph.db（9 小写域已清理）

## applicable_rules

- {'module_id': 'MOD-TASK_SYSTEM', 'section': '§3.2.1', 'reason': 'TaskCard Schema——任务格式合规'}
- {'module_id': 'TRAE-003', 'section': 'RULE-SIX', 'reason': '任务粒度门 R1-R6'}

## allowed_touch

- D:\ZephyrAlpha\data\databases\depgraph.db

## acceptance

- SELECT COUNT(*) FROM domains WHERE domain_id NOT LIKE 'D-%' = 0 且总行数 = 52

## 其他字段

| 字段 | 值 |
|------|-----|
| rollback_instructions | 用 depgraph.db.backup.pre_clean_domains 覆盖 depgraph.db |
| post_sync_standard | [] |
| estimated_complexity |  |
