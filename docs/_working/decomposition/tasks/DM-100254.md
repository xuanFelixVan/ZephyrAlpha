---
module_id: DM-100254
title: "DM-100254: 对齐 depgraph.db 架构三表域数（domains/archdomainlayers/archdomaincapacity）"
doc_type: log
status: active
version: 1.0.0
date: 2026-06-27
owner: ZephyrAlpha-Owner
ttl: task_bound
completes_when: "DM-100254 架构三表域数对齐完成且任务状态为 DONE"
---

> **裁定 #ARCH-REN-001（2026-06-26）**：6 个域 ID 连字符→下划线改名：
> D-GOV-DOCS→D-GOV_DOCS, D-GOV-ENFORCEMENT→D-GOV_ENFORCEMENT, D-GOV-SCRIPTS→D-GOV_SCRIPTS,
> D-GOV_AUDIT_TESTS→D-AUDITTEST, D-INTEGRATION-GATEWAY→D-INTEGRATION_GATEWAY, D-SECURITY-LLM→D-SECURITY_LLM。
> 本文档中出现的旧域名均为历史记录，已由上述裁定更新。

# DM-100254: 对齐 depgraph.db 架构三表域数（domains/arch_domain_layers/arch_domain_capacity）

| 字段 | 值 |
|------|-----|
| task_id | DM-100254 |
| namespace | DM |
| title | 对齐 depgraph.db 架构三表域数（domains/arch_domain_layers/arch_domain_capacity） |
| status | BLOCKED |
| priority | P1 |
| phase | 3 |
| execution_model | glm |
| safety_level | M |
| directive | PANORAMA-REBUILD |
| classification | internal |
| ai_autonomy_level | supervised |
| fallback_model | deepseek |
| source_blueprint | PANORAMA-REBUILD-DISCUSSION |
| source_section | §三.4 |
| blocked_by | DM-100253 |

## description

根因：D:\ZephyrAlpha\data\databases\depgraph.db 架构三表域数不一致——domains 表 52 域（DM-100253 清理后）、arch_domain_layers 表 50 域（缺 D-GOV-DOCS/D-GOV-SCRIPTS/D-TEST，多 D-TRAE 幽灵域）、arch_domain_capacity 表 52 域，导致域容量/分层检查漏域。治根：补齐 arch_domain_layers 缺失的 3 个 D-XXX 域分层记录（参照同组域 layer 分配），删除 D-TRAE 幽灵域记录（domains 表无此域），使三表域数一致为 52。施工步骤：先备份 D:\ZephyrAlpha\data\databases\depgraph.db 到 D:\ZephyrAlpha\data\databases\depgraph.db.backup.pre_align_layers，然后查询 arch_domain_layers 表现有 D-GOV-* 域的 layer 分配作为参照，为 D-GOV-DOCS/D-GOV-SCRIPTS/D-TEST 插入对应分层记录，DELETE FROM arch_domain_layers WHERE domain_id='D-TRAE'。验收标准：三表 DISTINCT domain_id 数量一致且等于 52，无幽灵域。

## files_in_scope

- D:\ZephyrAlpha\data\databases\depgraph.db

## deliverables

- D:\ZephyrAlpha\data\databases\depgraph.db（三表对齐后）

## applicable_rules

- {'module_id': 'MOD-TASK_SYSTEM', 'section': '§3.2.1', 'reason': 'TaskCard Schema——任务格式合规'}
- {'module_id': 'TRAE-003', 'section': 'RULE-SIX', 'reason': '任务粒度门 R1-R6'}

## allowed_touch

- D:\ZephyrAlpha\data\databases\depgraph.db

## acceptance

- SELECT COUNT(DISTINCT domain_id) FROM domains = SELECT COUNT(DISTINCT domain_id) FROM arch_domain_layers = SELECT COUNT(DISTINCT domain_id) FROM arch_domain_capacity = 52

## 其他字段

| 字段 | 值 |
|------|-----|
| rollback_instructions | 用 D:\ZephyrAlpha\data\databases\depgraph.db.backup.pre_align_layers 恢复 |
| post_sync_standard | [] |
| estimated_complexity |  |
