---
module_id: DM-100255
title: "DM-100255: 53 D_ 域归并映射 + 4 空壳域处置方案"
doc_type: audit_report
status: active
version: 2.0.0
date: 2026-07-03
owner: ZephyrAlpha-Owner
ttl: task_bound
completes_when: "DM-100255 53 域归并映射输出完成且 4 空壳域处置方案确定，任务状态为 DONE"
ssot_warning: "本文件是 TaskRepository 的 human companion，真源为 SQLite DB，禁止以此文件内容为准做决策，查询请用 TaskRepository.get(task_id)"
---

> ⚠ **SSoT 警告**：本文件是 TaskRepository 的 human companion（同步产物），真源为 SQLite DB。禁止以本文件内容为准做决策（status/blocked_by 等可能过期）。查询任务真源请用 `TaskRepository.get(task_id)` 或 MCP `task_manager.get_task`。

# DM-100255: 53 D_ 域归并映射 + 4 空壳域处置方案

| 字段 | 值 |
|------|-----|
| task_id | DM-100255 |
| namespace | DM |
| title | 53 D_ 域归并映射 + 4 空壳域处置方案 |
| status | DONE |
| priority | P1 |
| phase | 3 |
| execution_model | glm |
| safety_level | M |
| directive | PANORAMA-REBUILD |
| classification | internal |
| ai_autonomy_level | supervised |
| fallback_model | deepseek |
| source_blueprint | PANORAMA-REBUILD-DISCUSSION |
| source_section | §〇.3 / §五.1 |
| blocked_by | 无（方案已输出） |

## description

**原任务（已过时）**：根因：架构升级总纲 `architecture_upgrade_discussion.md` §2.1 裁定 39 域方案，但数据库 domains 表实际有 52 个 D-XXX 域，两者存在 13 域差异。治根：逐域比对 52 D-XXX 域与 39 域方案，输出差异归并映射表。

**过时原因（2026-07-03 审核发现）**：
1. `architecture_upgrade_discussion.md` 文件不存在（files_in_scope 引用失效）
2. `depgraph.db` 已废弃（P2 PG 迁移，现为 PostgreSQL）
3. 域数已从 52 变为 53（裁定#204 域名 D-XXX→D_XXX 格式归一化）
4. deliverable `域归并映射报告.md` 从未创建（任务标 COMPLETED 但 deliverable 不存在=数据完整性问题）

**重新定义（2026-07-03）**：在 PG 环境下，对 53 个 D_ 域做归并映射分析，重点处置 4 个空壳域（0 nodes 引用）。归并映射分析直接在本任务卡中输出（向内收原则——不创建独立报告文件）。

## 53 域归并映射分析

### 概览

| 类别 | 数量 | 处置 |
|------|------|------|
| 活跃域（有 nodes 引用） | 49 | 保留（KEEP） |
| 空壳域（0 nodes 引用） | 4 | 删除（DELETE） |
| **合计** | **53** | **49 保留 + 4 删除** |

### 49 活跃域清单（全部保留）

以下 49 域在 `nodes` 表中有模块引用（production_nodes > 0），全部保留，不做归并：

```
D_ALT_DATA(7)         D_ASHARE_SIGNAL(7)   D_AUDITTEST(1699)     D_AUTONOMY_CORE(114)
D_AUTONOMY_PERM(14)   D_BACKTEST(10)       D_COMPLIANCE(25)      D_CROSS_ASSET(8)
D_DATA_ENG(7)         D_DATA_GOV(7)        D_DATA_SEC(7)         D_DIGITAL_TWIN(8)
D_EXEC_SIM(7)         D_EX_CORE(13)        D_EX_SOR(7)           D_FACTOR(14)
D_FRONTEND(16)        D_FUNDAMENTAL_SIGNAL(25) D_GOVERNANCE(874) D_GOV_AUDIT(2)
D_GOV_DOCS(2)         D_GOV_DRIFT(1)       D_GOV_ENFORCEMENT(176) D_GOV_SCRIPTS(430)
D_INFRA_A2A(93)       D_INFRA_OPS(10)      D_INFRA_RECOVERY(74)  D_INFRA_RUNTIME(149)
D_INFRA_TELEMETRY(25) D_INTEGRATION(73)    D_INTEGRATION_GATEWAY(20) D_INTELLIGENCE(45)
D_KNOWLEDGE(9)        D_MKT_DATA(8)        D_ML_SERVE(7)         D_ML_TRAIN(12)
D_OPS(3)              D_PF_ALLOC(8)        D_PF_CORE(14)         D_POSITION(8)
D_REPORTING(10)       D_RISK(22)           D_SECURITY(147)       D_SECURITY_LLM(44)
D_SELL_DECISION(7)    D_SHARED(229)        D_SIGQC(7)            D_SIMULATION(11)
D_TRADING(488)
```

> 注：括号内为 `nodes` 表中该域的模块数。所有活跃域平级，无父子关系（硬约束）。

### 4 空壳域处置方案

以下 4 域在 `nodes` 表中无模块引用（production_nodes = 0），为空壳域，需删除。

#### 1. D_BEHAVIORAL_AUDIT（行为审计）✅ 已删除（2026-07-03）

| 属性 | 值 |
|------|-----|
| domain_name | 行为审计 |
| build_status | stable |
| description | 行为审计域(从D-SECURITY拆出,behavioral_auditor) |
| nodes 引用 | 0 |
| 外部引用 | **0**（仅 domains 表自身 1 条） |
| 处置 | **已执行**：通过 apply_depgraph.py --delete-domain D_BEHAVIORAL_AUDIT 删除（2026-07-03） |
| 归并去向 | 语义归并到 D_GOV_AUDIT（审计域，2 nodes）——"行为审计"是"审计"的子集 |
| 矛盾说明 | build_status=stable 但 0 nodes，状态与实际不符 |
| 删除验证 | affected=1 row（domains 表），POST-RENAME-CHECK 0 residual，M11=0 验证通过 |

#### 2. D_GOV_REPAIR（rollback）✅ 已删除（2026-07-03）

| 属性 | 值 |
|------|-----|
| domain_name | rollback |
| build_status | deprecated |
| description | 双轨Checkpoint(git commit + SQLite JSONL dump) |
| nodes 引用 | 0 |
| 外部引用 | **7**（arch_path_mappings.domain_id 7 条） |
| 处置 | **已执行**：通过 apply_depgraph.py --delete-domain D_GOV_REPAIR 删除（2026-07-03） |
| 归并去向 | 功能已被 D_GOV_ENFORCEMENT（176 nodes）覆盖——修复=执行的一部分 |
| 删除验证 | M11=0（真实 PG 查询），域引用残留=0，3行 nodes 已迁移至 D_GOV_SCRIPTS（belongs_to=MOD-GOV-REPAIR 是模块引用非域引用，无需清理） |

#### 3. D_GOV_RULE（rule_governance）✅ 已删除（2026-07-03）

| 属性 | 值 |
|------|-----|
| domain_name | rule_governance |
| build_status | planned |
| description | 规则配置管理 |
| nodes 引用 | 0 |
| 外部引用 | **1**（domain_events.source_domain 1 条；初版分析误报 arch_path_mappings 3 条，dry-run 实测为 0） |
| 处置 | **已执行**：通过 apply_depgraph.py --delete-domain D_GOV_RULE 删除（2026-07-03） |
| 归并去向 | 功能已被 D_GOVERNANCE（874 nodes）覆盖——规则配置管理=治理核心 |
| 删除验证 | affected=2 rows（domain_events.source_domain 1 + domains 1），M11=0 验证通过。POST-RENAME-CHECK 发现 constraint_id 残留 `CONSTRAINT_D-GOV-RULE_ENFORCEMENT`（旧格式连字符命名，from_domain=D_GOVERNANCE），通过 `apply_depgraph.py --delete-constraint` 删除过时约束（apply_depgraph.py 自动 PG 备份 depgraph_20260703_120655.json） |

#### 4. D_SIGLEGACY（siglegacy）✅ 已删除（2026-07-03）

| 属性 | 值 |
|------|-----|
| domain_name | siglegacy |
| build_status | planned |
| description | 信号遗留设计态节点（原 D-SIGNAL 拆分后遗留的设计态占位域） |
| nodes 引用 | 0 |
| 外部引用 | **66**（cmd_delete_domain --dry-run 实测：domain_dependencies.from_domain 6 + domain_dependencies.to_domain 8 + domain_events.source_domain 4 + domain_events.target_domains 2 + contracts.provider_domain 28 + contracts.consumer_domain 16 + arch_constraints.from_domain 1 + arch_path_mappings.domain_id 1） |
| 处置 | **已执行**：通过 apply_depgraph.py --delete-domain D_SIGLEGACY 删除（2026-07-03） |
| 归并去向 | 无（legacy 占位域，直接删除） |
| 删除验证 | affected=65 rows（dry-run 66，contracts.consumer_domain 实际 15 vs dry-run 16，dry-run 后数据微变），7 表域引用全部清理，M11=0 验证通过 |
| 内容列残留 | POST-RENAME-CHECK 发现 7 行内容列残留（旧格式 `D-SIGLEGACY` 连字符，非域引用列，不影响 M11）：contracts.schema_definition 1（description 文本提及）+ domain_events.name 1（脏数据：事件名=域名）+ domain_events.payload_schema 3（脏数据：payload_schema 填域列表非 JSON）+ nodes.owner 1（脏数据：owner=域名）+ nodes_metadata.owner 1。这些是历史脏数据，apply_depgraph.py 无内容列清理功能，记录为后续清理项 |
| 勘误说明 | 初版分析（20 行/5 表）遗漏了 contracts.provider_domain(28) + contracts.consumer_domain(16) + domain_events.target_domains(2)。cmd_delete_domain --dry-run 自动发现全部 66 行引用，工具就绪后手动分析不再需要 |

### 执行约束

1. **depgraph 修改必须用 apply_depgraph.py**（硬约束：全景图是真源）——4 空壳域的实际删除需通过 `apply_depgraph.py --delete-domain <DOMAIN_ID>` 执行，禁止直接 SQL DELETE。本任务卡仅输出处置方案，实际删除为后续执行任务。
2. **工具就绪（2026-07-03）**：`apply_depgraph.py --delete-domain` 已实现（cmd_delete_domain），17 步 DELETE/REPLACE 覆盖 11 表，安全门检查 nodes 引用（>0 需 --force），dry-run 已验证 4 域引用计数。
3. **备份先行**：改 depgraph 前必须 `git commit` 备份（硬约束）。
4. **引用清理顺序**：cmd_delete_domain 自动按正确顺序执行（先清外部引用 steps 2-17，最后删 domains 行 step1），避免 FK 违规。rule_bindings.domain_id SET NULL（规则是全局资产）。
5. **M11 指标验证**：删除后运行 `py -3.12 scripts/governance/architecture_health_dashboard.py --metric M11` 验证无新增违规。

### 后续执行命令（✅ 全部已执行 2026-07-03，4/4 空壳域删除完成）

```bash
# 备份先行：apply_depgraph.py 每次写操作自动 PG 备份（depgraph_*.json）
# 备份点：HEAD 6121042de4（稳定回滚点）

# 逐域删除（dry-run 确认 + 实际执行）
py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_BEHAVIORAL_AUDIT --dry-run  # ✅ total=1
py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_BEHAVIORAL_AUDIT  # ✅ affected=1，0 residual

py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_GOV_REPAIR --dry-run  # ✅ total=8
py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_GOV_REPAIR  # ✅ 已执行，M11=0

py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_GOV_RULE --dry-run  # ✅ total=2
py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_GOV_RULE  # ✅ affected=2，constraint_id 残留已清理
py -3.12 scripts/governance/apply_depgraph.py --delete-constraint CONSTRAINT_D-GOV-RULE_ENFORCEMENT  # ✅ 过时约束清理

py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_SIGLEGACY --dry-run  # ✅ total=66
py -3.12 scripts/governance/apply_depgraph.py --delete-domain D_SIGLEGACY  # ✅ affected=65，7 行内容列残留记录为后续项

# M11 验证
py -3.12 scripts/governance/architecture_health_dashboard.py --metric M11  # ✅ M11=0（OK），4/4 空壳域删除完成
```

## files_in_scope

- D:\ZephyrAlpha\data\databases\depgraph（PostgreSQL，通过 apply_depgraph.py 访问）
- D:\ZephyrAlpha\scripts\governance\apply_depgraph.py（cmd_delete_domain 工具就绪）

## deliverables

- 本任务卡（DM-100255.md）——53 域归并映射 + 4 空壳域处置方案（向内收原则：不创建独立报告文件）
- apply_depgraph.py cmd_delete_domain 功能就绪（17 步 DELETE + 安全门 + dry-run 验证）

## applicable_rules

- {'module_id': 'MOD-TASK_SYSTEM', 'section': '§3.2.1', 'reason': 'TaskCard Schema——任务格式合规'}
- {'module_id': 'TRAE-003', 'section': 'RULE-SIX', 'reason': '任务粒度门 R1-R6'}

## allowed_touch

- D:\ZephyrAlpha\docs\_working\decomposition\tasks\DM-100255.md

## acceptance

- 归并映射覆盖全部 53 D_ 域（49 保留 + 4 删除）
- 4 空壳域每个有明确处置方案（删除 + 引用清理顺序 + 归并去向）
- 4 空壳域外部引用情况已查清（dry-run 实测：D_BEHAVIORAL_AUDIT=0, D_GOV_REPAIR=7, D_GOV_RULE=1, D_SIGLEGACY=66）
- cmd_delete_domain 工具就绪，dry-run 4 域全部通过，安全门对活跃域（D_GOVERNANCE 874 nodes）正确阻断
- ✅ 4/4 空壳域已实际删除（2026-07-03）：D_BEHAVIORAL_AUDIT(1) + D_GOV_REPAIR(8) + D_GOV_RULE(2) + D_SIGLEGACY(65) = 76 rows affected
- ✅ M11=0 验证通过（域引用一致性违规数=0，真实 PG 查询）
- ⚠ 后续清理项：D_SIGLEGACY 内容列残留 7 行（旧格式 D-SIGLEGACY 历史脏数据，非域引用列，不影响 M11），apply_depgraph.py 无内容列清理功能，需扩展工具或手动 SQL

## 其他字段

| 字段 | 值 |
|------|-----|
| rollback_instructions | apply_depgraph.py 每次写操作自动 PG 备份（tmp/pg_backups/depgraph_*.json），4 域删除可通过重新 INSERT domains 行回滚 |
| post_sync_standard | [] |
| estimated_complexity |  |
