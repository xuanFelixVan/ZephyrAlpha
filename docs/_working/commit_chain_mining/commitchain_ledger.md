---
ttl: task_bound
---

# 提交链挖矿-施工战役总台账（st-commitchain-20260922）

> Owner 通宵总包令：对提交落地链先挖矿挖干全环节（骨架+每环节子文档）→ 封矿 → 施工 → 两轮零 + 红蓝两轮零 → 网关端到端交付。不停不问，自裁留痕。
> 会话：st-commitchain-20260922（心跳守护 pid 常驻）。写域：队列/网关/门禁层 + 本目录文档。

## 批次志

| 批 | 内容 | 状态 |
|---|------|------|
| B0 | 冷启动+护栏（reaper 活/锁清理/会话注册 pid=20180 心跳守护） | ✅ 03:3x |
| B1 | 真源研读：mining_sop 1.4.0 / skeleton_mining_policy / construction SOP / 66号文考古锚 / CREATE-GUARD+token 机制 | ✅ |
| B2 | 10 路并行挖矿子代理（环节1-9 内部向 + 环节10 业界对标全网向） | 🔄 在飞 |
| B3 | 主会话考古数据批：09-21/22 全量死信 301 + done 97 统计（死因门禁杀手榜/文件数分布/requeue 链） | ✅ 数据已入 00 骨架 |
| B4 | 环节文档落盘（每环节一份，六向台账+自审闸三态） | ⬜ 等子代理 |
| B5 | 封矿审计（三扫收敛+封顶声明）+ 设计 v1.0 施工图 | ⬜ |
| B6 | 并发施工（分域子代理）+ 主会话接线 | ⬜ |
| B7 | 测试两轮零 + 红蓝两轮零 | ⬜ |
| B8 | 网关落地 + 归属核验 + 临时清理 | ⬜ |
| B9 | QMT 模拟桥 100 股端到端验证（机械判 sim 真源，歧义即登记跳过） | ⬜ |

## 自裁裁定（SC-N，全部按"客观架构师/第一性原理/终局全貌"框架留痕）

| # | 裁定 | 理由（一句话） |
|---|------|--------------|
| SC-1 | 降级直改主区施工（登记原因） | 队列输入面=主区暂存区，worktree 合并反而引入 serializer 快照复杂度；当前写域（scripts/commit_queue.py 等）无他会话活跃 claim，队列本身即单写者保障（并行协调政策 §10 降级登记在案，GW 计数接受周审计） |
| SC-2 | 他会话孤儿 diff 两件代偿落地：.pre-commit-config.yaml（裁定#374 主题十 10-2a 已知限制注记一行，纯注释）+ alert_threshold_registry.yaml（已 staged 的真源路径 archive 化修正，对应已落地的 workclean 归档迁移） | 两件均机械可证为纯文本/路径零行为变更且引用已落地事实；不代偿则我的写域永远带外来脏 diff（连坐）。落地时单独成批归因原会话 |
| SC-3 | 拆批阈值=40 文件（interactive 车道），带显式逃生旗 | 数据驱动：done 项文件数 p50=3 / p90=110（workclean 巨批拉偏），>40 即离开常态分布；machine 车道（reconciler 派生）豁免；逃生旗防误伤真正原子的归档大迁移 |
| SC-4 | 预检层只挂 T0 级门禁（纯文本+注册表可判），克隆/pytest/DB 类不进预检 | 预检必须在秒级完成且零仓库变更，否则把租约成本换个地方收；杀手榜 T0 家族（CREATE-GUARD 32/RULING-REF 17/NO-BARE-SQL 15/ALGO-FLOW 14/EXEMPT-FM 11/TRANSLATION 10）占 301 死的 33% |
| SC-5 | 不做活体租约抢占 | 「活体不可抢」是防 worktree 互踩的血泪不变量（2026-09-17 抢锁事故）；治本靠预检+拆批把在途时长压下来，而非引入抢占新风险面 |
| SC-6 | QMT 模拟单测试置于 B9 且机械判 sim 三证（账户归属/桥配置/下单回执落点），任一证不带→登记跳过留 Owner | 资金安全红线优先于交付完整度；宪法 §5 高门位精神 |

## 风险与在飞会话

- 活跃会话（03:4x 实测）：st-residual-20260922 / st-integrated-bt-20260922 / st-sim-launch-20260922 / st-ulib2-20260921 / st-unified-plan-20260920 + worker —— 落地全部走队列排队，禁插队禁杀守护（裁定#281③）。
- 队列正在被改善对象使用中：所有改动必须向后兼容（预检默认开但白名单制、拆批有逃生旗、status 纯增量输出、daemon 靠 epoch re-exec 换血）。

## 交付判据（Owner 令原文映射）

- 挖矿：环节零遗漏（骨架路由表全覆盖+每环节文档六向台账）→ 封矿五判据核查落盘。
- 施工：Owner §四 六项建议全部落地或有 SC 级替代裁定。
- 测试：连续两轮 0 问题 + 红蓝对抗两轮 0 问题。
- 交付：GitCommitGateway 落地 + git log 归属核验 + 临时文件清理 + 终局汇报（全绿/零遗留/零待裁定，SC-6 除外——按令原文"遇到无法裁定→登记留 Owner"执行）。

## 红蓝对抗记录（两轮零判据）

- **红队 R1**（子代理，探针留档 .runtime/tmp/redteam_0922/）：P1-1 快段子集开关门未套住调用（已修：门内移）｜P1-2 difflib autojunk 二次方可被打穿（10 万行实测 63s；已修：默认启发式+2 万行上限跳过）｜P2-1 status 非 dict JSON 崩溃面（已修 isinstance 双处）｜P2-2 大小写假红（normcase 已修）｜P2-3 `..` 归一化（已修）｜P2-4 越仓读（空串跳过已修）｜P2-5 .yml 口径对齐锁内（已修）｜P2-6 emit 覆盖写踩 burst 日期键（合并写已修）｜P2-7 阈值 0 地雷（invalid_threshold 已修）｜**P2-8 machine 车道自报绕硬顶=既有缺口非本次引入 → 登记 known-limitation**（lane 身份派生待 Owner 窗口）。
- **蓝方修复后 R2**：探针复跑全部确认修复生效（a2 假红消除/a3-a5 修固/a10 0.03s）+ 182 测试两轮全绿 + ruff/format 8 文件全净。
- **环境注记**：test_commit_queue_integration 50-commit 真落地测试在基线（无本战役改动）同样挂死——生产队列当晚持续有会话落大项，共享锁争用所致，非本战役回归；留静默窗复测。

## 终局状态

- 施工：R1 预检扩容（3 直接准入+2 内联适配）/R2 批硬顶 40+逃生旗/R3 两段式通道/R4 租约事件唤醒+epoch 三子树/R5 status 三块/R6 爆发告警+env 补盲+health 两键 + D7 快赢捆绑全量落地。
- 挂起（解锁条件在案）：k=4 通道池（Owner 签 S18-R3）/字典序 FIFO→created_at（动 66号不变量须 Owner）/blobs GC（Owner）/TRANSLATION-COVERAGE 预检化（待死信率复测）/收敛批量化与 reconciler 禁 auto-commit（契约面）/gate_cache_preflight 启用（Owner 手柄）/own_scope 五台补登记（机生通道）/P2-8 lane 身份派生。
- 封矿：活体租约抢占（F9 红线）/全局 fail_fast（归因语义）/主区真 checkout 化/新检测器（净零）/enqueue 核心嵌门禁（零依赖不变量）。
