---
ttl: task_bound
rule_form: data
verifiability: manual
title: S1 钱闸数学 A1-A8 三态裁定（Kimi 深度裁定班次）
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
---

# S1 钱闸数学裁定（基线 commit: 368a01cbb7 起，班次 2026-09-17）

> 方法：六轴法（deep_review_policy §3）+ 14 缺陷模式前置对照；一档 P0 双角色加试（每个定级结论附"复算反驳者"最强三条反驳）。
> 证据主力=三个并行取证代理的逐字源码包（file:line 全部抽查复核过关键行）。

## 1. 三态裁定总表

| # | 对象 | 裁定 | 一句话依据 |
|---|---|---|---|
| A1 | 防噪音四闸 | **对**（设计层） | 四闸各针对真实失效模式（含 399106 成例）；弱项=无机读强制面，属流程纪律闸 → 转 S4/S10 关注 |
| A2 | Wilson 区间 | **对** | 实现=标准 Wilson 下界，z=1.96 双侧 95% 作下界（≈单侧 97.5%）=偏保守方向安全；n≤0→0 ✓ |
| A3 | FDR | **对** | bhy_fdr 步进+BY 谐波修正实现正确（教科书向量钉扎在案）；m=调用方批次族，BH 族定义合法 |
| A4 | DSR+台账 | **基本对，1×P1+1×P2 已直改** | 内核三处统一委托、Pearson 峰度转换与 E[max] 闭式均有解析锚钉扎；fallback N=10 放水已治本；台账补登 81 |
| A5 | ANOVA | **对**（前提检验不适用） | factory_grid_anova=fANOVA 方差分解，无 F/p 推断，正态/方差齐性前提不适用；ddof 混用已声明近似 |
| A6 | binomial_ge_pvalue | **克隆件错（P1 已直改）** | canonical 件退化分支完备；克隆件缺 p0∈{0,1} 分支+零校验+零直测（缺陷模式 #4 实证） |
| A7 | metamorphic 63 | **不变式对，覆盖不够** | 4 族不变式数学全真；缺成本单调性/现金守恒/市值恒等式不变式 → 补写规格转 S12 |
| A8 | 引擎真值断言 | **部分对** | 生产引擎会计=Portfolio 拒单制+reconcile 对账器（整装路径接线）✓；CLI 路径无现金闭合判读（P2→S12）；幽灵 [TESTS] 锚已直改 |

## 2. 发现清单（file:line + 验证法 + P 级）

### 已直改（本批 commit 内）

| # | P | 发现 | 锚点 | 修法 | 验证法 |
|---|---|---|---|---|---|
| S1-F1 | **P1** | `metrics._resolve_n_trials` 账本不可读时回退 N=10 且**照常出 DSR**——欠折减=钱闸静默放水（DSR 偏乐观方向） | `src/zephyr/backtest/core/metrics.py:251-259`（原版） | fallback 路径落 `dsr=0.0 + dsr_degenerate=True + is_overfitting=True`（与 SDC-4 同 posture："判不了"不作钱闸证据） | `pytest tests/backtest/test_metrics_dsr.py` 全绿；构造账本不可读环境应见 dsr_degenerate=True |
| S1-F2 | **P1** | `binomial_ge_pvalue_small` 克隆件缺 p0∈{0,1} 退化分支——复活闸 baseline=0/1 时 `math.log(0)` 直接 domain error 崩闸；零入参校验、零直测 | `src/zephyr/signal_ashare/strategy_signal/pattern_lifecycle.py:74-91`（原版） | 删除克隆公式，委托 canonical `pattern_evidence_certifier.binomial_ge_pvalue`（公共导出件）；保留 hits≤0/n≤0→1.0 复活闸语义 | `pytest tests/signal_ashare/strategy_signal/test_pattern_lifecycle.py + tests/metamorphic/test_fourgate_metamorphic_invariants.py` 全绿 |
| S1-F3 | **P2** | N 台账漏登 2026-09-17 sharpe2 重考 81 次试验（重考消费累计 N=4481 却不回写）→ 后续批次 DSR 系统性偏松 | `docs/01_policies_and_standards/_registry/catalogs/trial_ledger_registry.yaml`（登记前 4481） | `TrialLedger.record_run` 幂等补登 batch_id=sharpe2_prep_e4_reexam_20260917 n=81 → 累计 **4562** | `TrialLedger().cumulative_trials()==4562`；重复调用返回 status=exists |
| S1-F4 | P3 | `strategy_screen.num_trials` schema 注释残留"批内实际参考策略数"，实际写入累计口径（5548b45ca4 起）=注释-行为漂移 | `schemas/categories/backtest/backtest_strategy_screen.py:66`（+头部 §7） | 注释改累计口径+留历史行 NULL 说明（CH 侧 COMMENT 刷新走 DDL apply，挂单不直改） | 肉眼复核注释 vs c4_batch_screen.py:135-152 写入口径 |
| S1-F5 | P3 | `/api/pattern-winrate` docstring 称"按 Wilson 下界降序"，SQL 实按 `hit_rate DESC` | `src/zephyr/frontend/dashboard/api_server.py:4224` vs `:4241` | docstring 改述实际行为+指认 Wilson 真口径所在 | 肉眼复核 |
| S1-F6 | P3 | `bh_filter` 报告展示列 `bh_threshold`（纯 BH）与判定口径（BHY c(m) 修正）不一致 | `src/zephyr/strategy_pipeline/bh_fdr.py:77` 附近 | 报告补 `bhy_family_threshold` 字段（判定真阈值），不动既有字段 | `pytest tests/strategy_pipeline/test_bh_fdr.py` 全绿 |
| S1-F7 | P3 | `_c4_engine.py` 头 `[TESTS]/[MODIFY-GUARD]` 锚 `tests/backtest/test_c4_engine.py`——该文件**从未存在于 git 历史**（幽灵引用，缺陷模式 #9）；docstring 残留批内 N 口径 | `scripts/backtest/translated/_c4_engine.py:11,16,27` | 改锚三件真实守护件（test_c4_batch_smoke/test_c4_deflated_sharpe_runner/test_c4_auto_oos）；docstring N 口径更正 | `git log --all -- tests/backtest/test_c4_engine.py` 为空（已核） |

### 记档不改（含误报边缘）

| # | P | 发现 | 处置 |
|---|---|---|---|
| S1-N1 | P3 | 62 行 is-only 回填行 DSR 用旧 (1−SR²/4) 系数（c88d5e33db 修复前执行），未二次回填 | **数值 immaterial**：日频 SR≈0.09 时 SR² 项 <1e-3，V[SR] 偏差 <0.4%，z 偏移 ~0.3%，DSR 偏移 ~0.002 量级，不触及 0.5/0.95 门。不重回填，记档 |
| S1-N2 | P3 | `statistical_hygiene_auditor.get_false_discovery_rate_estimate` 是启发式冒名件（0.05×n_metrics/n_anomalies），非 BH | 名字误导但无钱闸消费方；记档，改名建议转 S10 |
| S1-N3 | P3 | `eval_f2_narrow_backtest.py` 硬编码 `_N_TRIALS=8`（批内口径，披露在案） | 与累计口径裁定不一致但属评估脚本自披露；挂 S5/S13 注意清单 |
| S1-N4 | P3 | Wilson 两口径并存（raw n_events vs n_eff 折扣）经裁定为有意；`rate>1` 无守卫（上游域保证 [0,1]） | 接受现状；守卫补强归 S12 不变式规格 |
| S1-N5 | P3 | fANOVA `var(ddof=1)` vs `_between_var(/n)` 混用 | 模块自述"近似正交"且用途=剪枝排序非推断；接受 |

### 转办（S12/Flash）

| # | P | 事项 | 判据形态 |
|---|---|---|---|
| S1-T1 | P2 | CLI 路径（run_backtest.py）不接 `reconcile_cash_ledger`——CLI 产物只有 cash_curve 落盘、无账本闭合判读（整装路径有，framework_composer.py:2185-2212） | 进 S12 规格：CLI run_one 调 reconcile_cash_ledger 落 metrics.cash_ledger_reconciliation，缺 cash_history fail-closed |
| S1-T2 | P2 | metamorphic 覆盖缺口：成本单调性（费用↑净值↓）全仓无断言；现金守恒/持仓×价=市值未入不变式层 | 进 S12 规格：新增 INV-5 成本单调 + INV-6 账本闭合 + INV-7 市值恒等三条不变式 |

## 3. 复算反驳者记录（一档双角色加试）

**对 A4"基本对"**：①累计 N=4481 含 2×1999 网格高相关格点，累计口径或过度折减？——方向保守=对钱闸安全；N_eff 估计器（compute_effective_rank）在案但仅披露位，升级挂 S5。②fallback 改 fail-closed 误伤离线？——账本文件在库内，不可读=异常态，77 测试全绿实证。③refold_dsr 只平移 E[max] 不动 σ_SR？——存量重算标准近似，主线批次 A3 已用精确口径重算。
**对 A6"克隆件错"**：①baseline 真会到 0/1？——death.baseline 缺省 0.5 之外无护栏，池化基线可贴 0（全败族）；低频≠可崩，崩溃方向=闸失效。②循环导入？——懒导入+certifier 不反向依赖，测试实证。③hits>n 语义变化？——两版同为 0.0，一致。
**对 A7"覆盖不够"**：①四闸 49 条已厚？——厚在统计件；引擎会计层靠教科书单测兜底而非不变式，且 CLI 路径无对账（A8-F1 联动），缺口成立。
**对 A2/A3"对"**：①Wilson 下界系统性偏袒大样本 pattern？——设计意图即保守口径。②BH m=批内而非累计？——跨批多重性由 DSR 累计 N 管，两闸分工成立（A4 佐证）。③BY 过保守漏真信号？——过保守方向对钱闸安全，漏信号代价 < 放行假信号代价。

## 4. 提交

代码+注册表一批：`fix(money-gates): S1 钱闸裁定直改——DSR fallback fail-closed/binomial 克隆收敛/N台账补登81/四处口径与幽灵锚漂移 [GW:kimi-audit:non-worktree]`
本文件随批提交。
