---
ttl: task_bound
rule_form: data
verifiability: manual
title: S18 提交链路根因表（治本版，含并发吞吐模型结论）
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
---

# S18 提交链路根因表（基线 9ec091e8 后，班次 2026-09-17）

> 证据底料：lane_reports/A1.md（提交税）、A2.md（堵点榜）、A3.md（吞吐模型）——三 lane 全实测数据，本表只做裁定性归并。
> 五栏+两标注： symptomatic?=止痛件标记（超时/重试/日志类只记账）；连锁半径=该层失效时同时失去意义的"已通过"结论面。

## 0. 吞吐模型结论（A3 lane，实测锚定）

**当前架构系统上限 = 24 笔/小时**：serializer 单通道服务时间 S=149s（近窗实测 P50），理论 3600/149=24.2/h 与实测小时落地峰值 24 精确吻合。夜间 N=50 车道需求 ~100 笔/h（缺口 5 倍）；N=100 时 claim 碰撞率→~100%，有效吞吐崩落至 5-15/h（实测佐证：P90 落地延迟 7h、门禁空转 1.46 次/笔、队列死信率 44.6-57.5%）。
提速次序（有依赖关系，乱序无效）：**①claim 绑定前置+TTL 收窄+热点注册表分片（1.6-4.8×，是②③的前置）→ ③头部 O(仓库) 门禁 diff 化（1.4×/通道）→ ②serializer k=4 分区通道（3.4×→82/h）**。组合①+③+②(k=4)≈112/h 有效吞吐，N=50 达标；N=100 需 k=8（163-196/h）。

## 1. 根因表（一行一病灶）

| 编号 | 现象（计数+锚点） | 机理 | 治本改法 | 为何不再复发（机读判据+反例+什么算红） | symptomatic? | 连锁半径 |
|---|---|---|---|---|---|---|
| R-01 | integrity post-flush 重登记 31 笔/24h，间隔 P50 28.4min；队列死信 13.1% 死于 rules_integrity 漂移（A1） | `capability_canonical_file_registry.yaml` 在 golden-hash 保护清单（`validate_rules_integrity.py:123-127`）且 42 笔/24h 触碰它→`git_commit_gateway.py:1831/1894` 每笔 flush 无条件重登记+**独立尾 commit** | ①重登记并入触发它的主 commit（同 commit 原子化）；②内容哈希未变不出 commit（两道防空转闸已在案但挡不住"变了 hash 行"的合法触发——真问题是**触发频率本身**=该注册表被当便利贴用） | 判据：无受保护文件变更的 commit 后 24h 内 integrity 独立 commit 数=0；反例：受保护文件变更而 DB 未更新→`--check` 报 TAMPERED（真报）；红=独立尾 commit 复现 | 否 | 全仓提交税 11% 可消除量+队列死信 13.1% |
| R-02 | serializer 单通道 24 笔/h 硬顶；lease 300s 无续租，落地锁等待恰=300s 的潜伏双写窗（A3 §潜伏风险） | 单通道串行化是设计（防连坐），但通道数=1 是定值不是定理 | k=4 按域分区通道（先建 lease 续租机制）；**通道数=Owner 裁定项，本表只给模型** | 判据：k 通道下小时落地峰值 ≥4×24×0.8；反例：同域两通道并发落地同文件→冲突率上升；红=有效吞吐/通道数 <0.6（分区不均） | 否 | 夜间全部 50-100 车道 |
| R-03 | 门禁链 P50 46.4s/P95 153.3s，113 门禁/笔；CREATE-GUARD P99 94s、CAPABILITY-OVERLAP P90 19.9s（A2/A3） | 头部内容扫描型门禁 O(仓库) 逐笔重扫 | diff 化+基线复用既有口径（604f414846 的 git index+HEAD 差分+全绿短路模式推广到头部 6 件） | 判据：头部 6 门禁 P50 合计 <10s；反例：diff 基线漏扫外来 staged→own-scope 审计告警；红=任一门禁扫描面退化为全仓且无基线缓存 | 否 | 每笔提交墙钟 |
| R-04 | CLAIM-REQUIRED ×50（P50 33s）：`git_commit.py:1044-1059` finally 失败即释放 claim+文件保持 staged→重试不 reclaim→撞门；派生写入混入下一提交者 staged 集（A2） | claim 生命周期与提交事务错配：释放先于落盘确认；派生写入无归属 | claim 绑定移到提交成功后释放；派生写入（reconciler/integrity）强制归入触发会话名下；TTL 300s 取代 30min | 判据：CLAIM-REQUIRED 触发率 →0（24h <5 次）；反例：提交失败后 claim 不释放=死会话锁尸——TTL 300s 兜底；红=锁尸率上升（lock_files.py status 超 10 件/session） | 否 | 全体直改会话 |
| R-05 | worktree_drift_watchdog.jsonl **156,617 行**，37% 同哈希零变化重复写；自身派生提交峰值日 120 笔≈吃单通道上限 21%（A3） | `_save_state` 被 `alert_enabled` 门控（`worktree_drift_watchdog.py:1249-1267`）——observe-only 模式不写 state→每次扫描都当新漂移落盘 | 状态不变不落盘（state 持久化与 alert 门控解耦）；派生提交并入触发笔（同 R-01②） | 判据：watchdog 日写入量 <1000 行且零变化重复率 <5%；反例：真实漂移被合并吃掉不告警——状态哈希变更必须落盘；红=零变化重复率回升 >20% | 否 | 审计存储+单通道吞吐 |
| R-06 | 堵点本告警自激：1095 行同一事实（积压≥阈值）落盘间隔 P50 2.0s（A2） | `_check_ledger_backlog`（`commit_belt_daemon.py:138-185`）无状态变化检测，每 tick 重写 | 与 R-05 同型：状态变化才落盘+告警带冷却窗（同状态 30min 内不重复） | 判据：同事实告警 30min 内 ≤1 条；红=冷却期内状态翻转漏报 | 否 | 堵点本可读性 |
| R-07 | hook_tracked_drift 1994 行：366 文件跨日复发（最长 33 天无人处置），allowlist 对 TOP30 覆盖仅 18%（A2） | 提醒无处置闭环=噪声化；底层 TOCTOU 为真 | 与 R-01② 同源治本（派生写入并入触发笔后，漂移提醒自然消失）；残余真漂移件进周审计清单 | 判据：复发文件数周环比下降；红=复发清单 33 天无人看（当前状态）——挂周审计强制消费 | 半（并入是治本；周审计是止痛兜底） | 派生件一致性 |
| R-08 | GATE-REGENERATE reconciler timed out ×N（180s 硬顶，15 生成器串联，`reconciliation_registry.py:8097`）；DEPGRAPH-FRESHNESS WARN 1048min（A2） | 串联×单件硬顶=总时长随生成器数线性超阈；freshness 只 warn 无人行动 | 生成器并发化+按需触发（mtime 先行短路）；DEPGRAPH WARN 超阈值 24h 自动升级为待办（否则删掉该 WARN） | 判据：reconcile 总墙钟 <180s；红=超时复发；WARN 件=24h 无行动自动出声 | 半（并发化治本；升级是止痛但带判据） | 提交链尾段 |
| R-09 | REGISTRY-YAML-PARSE 一处破损连锁硬拦 CREATE-GUARD/SSOT-REDEFINITION/ARCH-REFERENCE（A2；本班 S1 曾亲历 st-autolnk 误插断裂） | 共享注册表解析失败=多门同瘫（fail-closed 方向正确但半径过大） | 解析失败隔离到该注册表所属门禁，不连坐其他门禁；写侧已有 safe_write 校验，补"写后 1min 内解析复查" | 判据：单注册表破损时其他门禁正常执行；红=连坐复发 | 否 | 三门禁同时失能面 |
| R-10 | MERGE_HEAD 晾置无人收尾（本班实证 e19bc24c 晾置，发起会话已死； abort 被死会话 WIP 阻挡两轮） | 会话死亡无遗物回收：merge 状态/staged/claim 三件套无托管 | 会话死亡判定时 gateway 自动 `merge --abort`（内容在分支上零损失）+staged 归档 stash+claim 释放——三件套一并入死会话回收流程 | 判据：死会话后 `git status` 无 MERGE_HEAD；反例：活会话的 merge 被误收——死亡判定须双证（心跳+锁过期）；红=误收活会话 | 否 | 独占班次/夜班全部车道 |
| R-11 | 门禁空转 1.46 次/笔（preflight 与正式门禁重复执行同判据）（A3） | 预检与正式两道独立跑同一套扫描 | 预检结果缓存进正式链（同 staged 哈希短路）；P0-A 锁外预检（376 次 P50 2s 快败）已是有效样板 | 判据：空转率 →1.0 次/笔；红=缓存键漂移导致预检绿正式红却不复扫 | 半（缓存复用治本；但实现层要防键漂移） | 每笔提交墙钟 |
| R-12 | DANGLING-REFERENCE ×13 中 10 次=旧宪法 6.1 节（换版尾债，09-12 v1→L0）（A2） | 换版未原子化改写存量引用 | 换版时全仓引用一次性改写进同一 commit（本班已实证门禁会拦新增，存量须主动清） | 判据：全仓 grep 旧章节号=0；红=新版换版后旧号引用 >0 | 否 | 宪法可信度 |

## 2. 止痛记账区（只记账，不当治本结论）

| 现象 | 止痛件 | 为什么不是治本 |
|---|---|---|
| GATE-REGENERATE 180s | 调 600s | 串联总长随生成器数增长，下次还撞 |
| index.lock 互踩 ×48 | 加重试退避 | 互踩源=派生提交并发（R-01/R-05 治本后自然消失） |
| DEPGRAPH-FRESHNESS | 调阈值 | WARN 无人看=阈值无意义 |

## 3. 裁定书索引（Owner 签即生效）

- `adjudications/S18-R1_噪声降级清单.md`（R-05/R-06/R-07 的条件触发改制）
- `adjudications/S18-R2_衍生提交并入.md`（R-01 原子化+并入策略）
- `adjudications/S18-R3_并发通道与锁粒度.md`（R-02/R-04；通道数=Owner 定值）
- `adjudications/S18-R4_不可放松门禁白名单.md`（钱闸/own-scope/内容寻址/POST-COMMIT-GUARD/RULING-REFERENCE）
