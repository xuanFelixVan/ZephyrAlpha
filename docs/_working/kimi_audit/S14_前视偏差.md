---
ttl: task_bound
rule_form: data
verifiability: manual
title: S14 前视偏差专项审计——实锤清单与处置
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
---

# S14 前视偏差专项（基线 f1144362 后；证据=S14 探针实验+B1 lane 全量取证+L0 复核）

> 核心问句：**下单那一刻，这个数看得到吗？**

## 1. 实锤清单（全部有产物级证据）

| # | P | 破口 | 证据 | 处置 |
|---|---|---|---|---|
| S14-0 | **P1** | `pit_shift` 负值两处静默吞成同 bar 信号（门卫放行前视零报错） | S14 探针：pit=-1 与 pit=0 产物逐字节相同（`.runtime/tmp/exp/s14_result.json`） | **已直改**（commit 146238da：两处 fail-closed raise+钉测试） |
| S14-1 | **P1** | `_c4_engine.py:79-93,234-250` 用**当前指数名单**回灌 2020-2023 IS 窗（幸存者偏差），波及 15/85 翻译件（11 直接+4 经 `_valuation_engine`） | B1 lane V1；仓内已有正确范式 `fw_backtest.py:172-226`（SCD-2∩窗） | 修复=SCD-2 宇宙 retrofit（>50 行手术）→ **转 Flash 规格**（S12-E4） |
| S14-2 | **P1** | `c4_b37f550ba2af_gftd.py:62-96` 信号用当日 bar 判定盖当日戳 | B1 探针 RED 实锤（`tests/backtest/test_b1_gftd_samebar_probe.py`，xfail-strict 在库） | 直改信号 shift（小）+重跑该件 → 本班可做则做，否则随 S14-4 批量 |
| S14-3 | **P1** | `factor_strategy_template.py:181-189` assemble_weights 无 ≤T-1 平移+`:200-209` universe=全窗成交额 top40（全窗统计量=前视）——模板级缺陷，6 件 c4_fact_* 全中 | B1 探针 RED 实锤（`test_b1_fact_assemble_samebar_probe.py`，xfail-strict 在库） | 模板直改（shift 一行+universe 改滚动窗）→ 进本批直改 |
| S14-4 | P1 | 污染链：V1-V3 经 strategy_screen（translated_c4）→ `screen_source.fetch_bothwin` 进 C6 及格集底座 | B1 §污染链 | **台账处置裁定书**：受影响行标 suspect+修复后重跑（S14-R3） |
| — | GREEN | CGO `shift(-1)` 生存乘积：数学上精确抵消（公共乘子与 i 无关，归一化约掉）+12 标的探针实证 | B1 反证 GREEN（`test_b1_cgo_pit_probe.py` 在库作回归守卫） | 误报档案记档 |
| — | GREEN | 主引擎层 PIT 防线：vectorized T+1 硬断言、stk_limit/ST ≤T 快照、财务 announce_date 锚、regime 链 shift(1)、竞价回补 09:15-09:25 严格窗 | B1 §三 | 查无违规（有效结论） |

## 2. 判读

破口全部集中在 **C4 翻译件生态（scripts 层）**——生产引擎层 PIT 纪律扎实，快筛层为速度牺牲了时点纪律。这正是"弱模型干错了自己发现不了"的层：翻译件是生成物，生成器模板有病=整族带病。
**量级估计**：85 件翻译件中 15 件成分回灌+6 件模板同 bar+1 件 gftd 同 bar=**最多 22/85（26%）带前视或幸存者污染**；C6 及格集底座含这些件 → sharpe2_prep 的池子质量结论（S5 翻转清单 F-2/F-3 已联动冻结）须待修复重考。

## 3. 本班直改与转办

- 已直改：pit_shift 双点 fail-closed（146238da）。
- 本批直改：`factor_strategy_template.py` ≤T-1 平移+universe 滚动窗（含模板级测试钉）。
- 转 Flash（S12 规格）：①SCD-2 宇宙 retrofit 进 `_c4_engine`；②修复后全量 85 件重跑+strategy_screen 受影响行标 suspect；③"翻译件通用 PIT 闸"（两份行情仅 X 日后分歧→断言 weights[:X] 逐位不变，一闸罩全族）。

## 4. 复算反驳者

1. "C4 是快筛粗口径，污染无所谓"——错：screen 成绩进 bothwin 及格集底座+组队评审，粗筛的钱闸属性在案（C4 强制 DSR 即证）。
2. "模板 shift 会改变全部 6 件历史成绩"——正是目的：含前视的成绩本来就不该作数；重跑后新成绩才是真成绩。
3. "当前成分回灌在指数族影响小"——15/85 的面+沪深300 成分 2020-2023 变迁率（年换血~10%）= 幸存者方向系统性偏多，不可豁免。
