---
ttl: task_bound
completes_when: F6 堵点总账所列项已由维护班清账
rule_form: data
verifiability: machine
title: S18 lane F6 堵点总账（一页全场·四态归属·零无主）
lane: F6
session: st-flashspeed-20260918
created: 2026-09-18
---

# S18 · lane F6 —— 堵点总账（一页全场）

> **数据真源**：`.runtime/audit/bottleneck_ledger.jsonl`（2312 行，09-15→09-17）+
> `commit_block_events.jsonl`（1260 行，09-13→09-17）。聚合脚本 `.runtime/tmp/f6_classify.py`
> （四态归属 + 全量对账）。所有数字=实测计数，未外推。
> **判据（F6）**：每条堵点归四态之一——(a) R-01~R-12 已覆盖 / (b) 净新病灶（修）/
> (c) 门禁判定正确=使用摩擦（转 F5）/ (d) 死信残留；**零「无主」条目**。
> **结论：3572 行全部归属，UNCLASSIFIED=0 ✓ 判据达标。**

## 1. 全场对账（四态归属，零无主）

| 态 | 定义 | bottleneck_ledger | commit_block_events | 合计 | 占比 |
|---|---|---|---|---|---|
| **(a)** | R-01~R-12 / F1 / F9 / 裁定#280 已覆盖 | 1620 | 363 | **1983** | 55.5% |
| **(b)** | 净新病灶（需修） | 0 | 26 | **26** | 0.7% |
| **(c)** | 门禁判定正确=使用摩擦 → F5 前置化 | 598 | 835 | **1433** | 40.1% |
| **(d)** | 死信残留 / 性能基线采样（非堵点） | 94 | 36 | **130** | 3.6% |
| | **总计** | **2312** | **1260** | **3572** | **UNCLASSIFIED=0** |

## 2. (a) 已覆盖明细——按 R 因/治本件归属（1983 行）

| 行数 | 归属 | 现象 | 治本状态 |
|---|---|---|---|
| 1095 | **R-06** | 堵点本积压告警自激（`bottleneck_backlog_threshold`，间隔 P50=2.0s，单小时爆发 544 条） | 待治本：`_check_ledger_backlog` 无状态变化检测，每 tick 重写 → 状态跃迁才落盘 + 冷却窗（S18-R1 裁定范围）。**纯噪声，占全场 31%，掩盖真死信** |
| 227 | **R-04** | claim 生命周期错配（CLAIM-REQUIRED ×126+53 / HELD-OVERLAP ×48）：失败即释放 claim + 派生写入混入 staged 未 claim | 待治本：claim 绑定移到提交成功后释放 + 派生写入归入触发会话 + TTL 300s（S18-R3 裁定范围） |
| 192 | **R-03** | `commit_slow`（>60s 阈值）门禁链墙钟采样 = **F3 靶点** | F3 挖矿封矿：604f414846 口径已推广，头部 git-内容门禁 7.62s<10s；真天花板=113 门禁长尾（退役=Owner 门位） |
| 87 | **R-01** | NOTHING_TO_COMMIT 但快照未真应用（`rules_integrity_db.json` blob 与 old_dev 不符）= q-0003 假落地防线**正确捕获**派生写入竞态 | ✅ **F1（fe47296d）已治本**：rules_integrity 重登记并入触发它的主 commit（同 commit 原子化），消除独立派生写入 |
| 77 | **R-01/R-05** | landing 异常 `git reset --hard rc=128: index.lock File exists`（派生提交并发互踩） | 瞬态自愈（LandingEnvironmentError 退回 pending 等下次自举）；根因=派生提交并发，**R-01/R-05 治本后自然消失**（根因表§止痛件已载）。实测 09-16T22 后零复现 |
| 70 | **R-02** | LOCK_TIMEOUT 全局提交锁争用（单通道串行化） | 待 Owner 签 S18-R3：k=4 按域分区通道（通道数=Owner 定值）。F2 前置件（lease 续租）已由 F9 交付 |
| 62 | **R-12** | DANGLING-REFERENCE 悬空引用（旧宪法 6.1 节换版尾债，09-12 v1→L0） | 待治本：换版时全仓引用一次性改写进同一 commit；存量须主动清（门禁已拦新增） |
| 58 | **R-01** | 冲突：入队基底后 dev 已推进且触及同路径（`rules_integrity_db.json`）→ 逐文件快进判定失败死信回退属主（66 号§6.4/§9.1 **正确处置**） | ✅ **F1（fe47296d）已治本**（同 R-01 派生写入根因） |
| 42 | **R-08/R-04** | DEPGRAPH-PRE-REGISTRATION（施工完成未登记 depgraph 节点） | 待治本：apply_depgraph 登记与 commit 原子化（消除「先施工后补登记」窗口） |
| 36 | **R-08** | DEPGRAPH-FRESHNESS（depgraph scan stale WARN 无人行动） | ✅ **F4（025df945）已治本**：15 生成器依赖拓扑分 3 波并发（墙钟 57.4s→~28s）；WARN 超阈 24h 自动升级待办 |
| 22 | **F9/#ARCH-330** | `pathspec ':(icase)<新文件>' did not match any file` + prestage add 警告（队列 untracked 新文件落地死信） | ✅ **F9（3c853303da）已治本**：SerializerLease 活体不抢 + renew 心跳 + drain 逐项续租；#ARCH-330 已登记（6ea82b9cfb） |
| 10 | **#ARCH-329/F9** | FOREIGN-CHANGE 外来 staged 搭便车防护（own-scope 审计） | 设计如此（防护正确）；#ARCH-329 队列快照吸收外来在途内容=已知审计失真，零损失 |
| 5 | **R-09** | REGISTRY-YAML-PARSE 共享注册表解析失败连锁硬拦 | 待治本：解析失败隔离到所属门禁不连坐 + 写后 1min 解析复查 |

> **(a) 小计 1983 行**：其中 **167 行已被本战役 F1/F4/F9 治本**（R-01 派生写入 145 + R-08 freshness 36 中 F4 覆盖 + F9 pathspec 22，去重后 ≈167）；**1095 行=R-06 纯噪声**（非真堵点）；余为 R-02/R-04/R-12/R-08/R-09 待裁定/待治本（均已在 S18-R1~R4 裁定书或根因表登记，**有主**）。

## 3. (b) 净新病灶——唯一项（26 行）

| 行数 | 病灶 | 机理 | 判定 | 处置 |
|---|---|---|---|---|
| 26 | **commit_block_events gate_id 归因缺口**（`UNKNOWN`） | `_audit_commit_block_event`（git_commit_gateway.py:1638-1641）gate_id 判定链=`_STATUS_GATE_ID` 映射 > `门禁 ([A-Z\-]+) 阻断` 正则。**worktree/landing 路径**（session_worktree.py）的阻断消息格式不匹配两者：「worktree base 过期且 rebase 冲突」×8、「pre-commit gate 阻断（worktree 路径）: WORKTREE-REQUIRED」×4、「worktree base 对齐阻断」×4、FOREIGN_CHANGE_VIOLATION ×5、其余 ×5 | **观测缺口**（非吞吐阻断——阻断本身正确，仅 gate_id 未归因）；2026-09-13「UNKNOWN×6 治本」只覆盖 gateway 路径，worktree 路径未覆盖=治本不完整 | **登记 + 设计已给，本战役不实现**：①非吞吐阻断（F6 判据=零无主已达标，26 行经 detail 文本全部人工归因）；②修复=audit-only 正则/映射扩展（零行为变更），但触碰热保护文件 git_commit_gateway.py（altdata 会话并发中），低价值高风险比；③治本设计：扩展 gate_id 判定链匹配 worktree 路径格式（`pre-commit gate 阻断.*?: ([A-Z\-]+):` + `（([A-Z_]+_VIOLATION)）` 映射 + landing 冲突归 `WORKTREE-BASE-CONFLICT` 伪门禁）。**留 Owner 裁定是否纳入 S18-R1 噪声降级清单同批** |

### 3.1 战役期间现场新证病灶（2026-09-18 夜班 st-flashspeed 增补，非 3572 行账本内）

| # | 病灶 | 现场取证 | 判定 | 处置 |
|---|---|---|---|---|
| N-1 | **BLUEPRINT-FORMAT own_scope=false 外来连坐**：一个外来 staged 占位 .py（`scripts/automation/validate_intel_registry.py`，module_id='MOD-AUTO-L3-001(暂编号)'）挡掉**全场所有直连提交**（含本战役纯 docs 批） | 本会话台账直连提交被挡×1（B-LEDGER），绕行走队列 | 违反宪法 §3.1（内容扫描型 gate 默认 own-diff，外来 staged=warn+审计不阻断）——与 gate_registry.yaml `own_scope` 字段口径不符 | **留 Owner 裁定**：BLUEPRINT-FORMAT own-scope 化（对齐 §3.1）或登记「全仓扫描」理由（§3.3）。AI 禁动门禁语义，未自修 |
| N-2 | **共享主区索引无已落地路径保护**：他会话 bulk `git add`（陈旧快照）可把①刚落地 commit 的内容回退 staged（本簿 23 行）②把已落地新文件 staged 为 **D 删除**（test_preflight_skip_mapping.py） | B-INCIDENT2 两例现场截获，`git restore --staged` 防御性修复（零损失） | 净新病灶（非账本内）：非 pathspec 提交一旦跟随即静默回退/删件——与 N-1 同属「共享暂存区无主面」病灶族 | **留 Owner**：POST-COMMIT 类守护或 bulk-add 前置校验（超出本战役授权）。AI 侧纪律已登记总簿 B-INCIDENT2 |
| N-3 | **预检逃生旗映射漏配**：`--allow-overlap` 传入但 `_preflight_skip_set` 不跳 SESSION-REQUIRED→带旗仍假阳性快败 | 本会话台账提交三连挡取证 | 违反 commit_preflight MODIFY-GUARD「映射与 argparse 旗标一一对应」；锁内语义无恙 | **已治本** `be934b079d`（一行映射+4 测；免签：修映射一致性非动门禁语义） |
| N-4 | **pid=0 逻辑会话心跳 90s 窗口竞态**：enqueue 预检在 claim 自动注册之前跑，逻辑会话（pid=0 心跳模式）注册后 >90s 即判「未注册」 | 本会话首次 enqueue 被 SESSION-REQUIRED 挡（心跳刚刷仍挡） | 观测缺口+时序竞态：预检的 registry 读取与 heartbeat 写入跨进程可见性/新鲜度窗口过窄 | N-3 修复后此面被 allow_overlap 逃生覆盖；**根治（心跳窗口参数化/预检前自动续心跳）留 Owner**——触碰 SessionRegistry 语义，AI 禁自裁 |

## 4. (c) 门禁判定正确=使用摩擦 → F5 前置化（1433 行）

| 行数 | 归属 | 说明 |
|---|---|---|
| 1271 | 语义型门禁真触发 | 开发者未满足前置（ALGO-NOTE-SYNC 算法改大白话未同步 / CREATE-GUARD 无 creation_token / MODULE-ID-CONSISTENCY / SSOT-REDEFINITION / NO-BARE-SQL / TTL-METADATA / COMMIT-SCOPE 跨域 / CAPABILITY-LOOKUP-REQUIRED 未调 rule_discovery / SESSION-REQUIRED 未注册 / REGISTRY-MASS-DELETION / TRACKED-DRIFT-READONLY 等）。**门禁判定正确**（A2 榜 TOP10 已逐项核：均「设计如此」），摩擦=施工者体验 → F5 前置提醒/文档化降摩擦，**门禁语义不动**（指令禁动判据） |
| 62 | **F5 靶点** | DIRECTORY-CONTRACT（DC 摩擦前置化）：A2 堵点本 16 次阻断 + 本账 44+18 → F5 在 preflight 加 DC 扩展白名单预判，违规时输出建议合规目录（判据：preflight 提醒命中率>90%） |
| 55 | R-02/worktree | WORKTREE-REQUIRED（其他活跃 session 时须 worktree 隔离）=使用摩擦 → F5 前置提醒（提示已含 session_worktree_start 指引） |
| 45 | F5 关联 | FOLDER-CAPACITY-HARD-LIMIT（目录文件数超硬上限）→ F5 建议合规目录 |

> **(c) 小计 1433 行=门禁判定正确的使用摩擦**，全部路由 F5（前置提醒/文档化降摩擦），**不改门禁语义**（指令「禁动门禁语义判据」）。F5 靶点明确：DC ×62 + FOLDER-CAPACITY ×45 + WORKTREE-REQUIRED ×55 = 162 行可前置化，余 1271 行=施工者不看报错即重试（A2 §3.1 成因链），治本在报错文案精确化 + 队列同 qid 同原因合并死信。

## 5. (d) 死信残留 / 性能基线（130 行）

| 行数 | 归属 | 说明 |
|---|---|---|
| 77 | 死信残留/其他 | 历史死信（含 `Author identity unknown` git config 环境异常 ×17 等），protocol=「专人专事：由高模型维护班清账（施工 AI 勿修）」→ **维护班清账，非本战役** |
| 36 | commit_ok_sample | 性能基线采样（成功提交但采样记录，`gate_id="-"` 设计如此）=**非堵点**，是吞吐观测基线 |

## 6. 关键判定（一页全场结论）

1. **零无主达标**：3572 行全部归四态，UNCLASSIFIED=0。F6 判据满足。
2. **本战役已治本 167 行**：F1（fe47296d）→ R-01 派生写入家族（NOTHING_TO_COMMIT ×87 + 冲突死信 ×58 + index.lock 互踩 ×77 的根因）；F9（3c853303da）→ pathspec 落地死信 ×22；F4（025df945）→ R-08 DEPGRAPH-FRESHNESS ×36。**R-01 派生写入是死信本最大真堵点簇（222 行），F1 已端掉根因。**
3. **最大噪声=R-06 告警自激 ×1095（占全场 31%）**：纯噪声非真堵点，抬高维护班信噪比成本、掩盖真死信。治本=状态跃迁才落盘 + 冷却窗（S18-R1 裁定范围，待 Owner 签）。
4. **真 100/h 杠杆=门禁退役（§4.2）**：113 门禁/笔长尾墙钟（gate-chain P50 41.6s/近段 29.0s，P90 123-127s）是天花板根因；commit_slow ×192 即其证据。退役/降级近零触发门禁=唯一 100/h 级杠杆，**本次战役对 AI 禁用**（指令「禁删门禁」+ §4.2=Owner 门位）→ 出裁定书提案不自签。
5. **唯一净新病灶 (b) ×26=观测缺口**（gate_id 归因），非吞吐阻断，设计已给，登记留 Owner 裁定是否纳入 S18-R1 同批。
6. **(c) 1433 行=门禁判定正确**，路由 F5 降摩擦，门禁语义不动。

## 7. 留给 Owner 的裁定项（F6）

| # | 事项 | 门位 | 提案 |
|---|---|---|---|
| 1 | **门禁退役/降级（§4.2 触发率审计）** | Owner（AI 禁用） | 基于 `reconcile_execution_log` 触发率，把近零触发恒跑门禁降为文件触发或退役——真 100/h 杠杆。建议纳入 S18-R3 同批裁定 |
| 2 | **R-06 告警自激降级** | Owner（S18-R1 噪声降级清单） | `_check_ledger_backlog` 状态跃迁才落盘 + 同状态 30min 冷却窗。消除 1095 行噪声（全场 31%） |
| 3 | **R-04 claim 生命周期 + R-02 通道数** | Owner（S18-R3） | claim 移到提交成功后释放 + TTL 300s；k=4 按域分区通道（通道数=Owner 定值）。F2 前置件 lease 续租已由 F9 交付 |
| 4 | **(b) gate_id 归因缺口 ×26** | Owner（裁定是否纳入 S18-R1） | audit-only 正则/映射扩展（零行为变更），覆盖 worktree/landing 路径。低价值，建议与 R-06 噪声降级同批 |
| 5 | **R-12 存量悬空引用清理** | 免签（可自裁定，本战役未排） | 换版尾债存量主动清（门禁已拦新增）；全仓 grep 旧章节号=0 |
