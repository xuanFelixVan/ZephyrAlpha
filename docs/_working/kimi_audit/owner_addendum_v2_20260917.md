---
ttl: task_bound
rule_form: data
verifiability: manual
title: Owner 追加令 v2（2026-09-17 死线更正 + 七小时冲刺 + 新增 S18）
owner: ZephyrAlpha-Owner
language: zh
created: 2026-09-17
---

# Owner 追加令 v2（2026-09-17 17:10 下达）

> 凌驾于 [kimi_deep_adjudication.md](kimi_deep_adjudication.md) 的**时间安排**，不改它的判据与三问边界。

## 0. 先做三件事，再做任何战场

1. **死线更正**：主案文首"截止 2026-09-18 18:00 / 墙钟约 27 小时"是**过期事实**。真实死线=**今晚 2026-09-17 24:00，一过 0 点 token 归零，没有明天**。现在就 claim `docs/_working/kimi_audit/kimi_deep_adjudication.md`，把那一行改成 09-17 24:00——这个文件别留给别人改，会撞车。
2. **不许打断你正在跑的战场和后台实验**：按"当前战场收尾 → 落盘提交 → 切入 §2 时间表"执行。战场中途不切换是唯一硬规则。
3. **30 分钟内交断线保底件（§1）**，它先于一切。理由：0 点一到你就消失，没落盘的等于没做。

## 1. 断线保底件 = Owner 速裁表（最高优先，17:40 前落盘+提交）

`docs/_working/kimi_audit/owner_fast_sign_20260917.md`，一页一张表，把你**现在已有证据**的全部待签项列出来，Owner 今晚就能签、不依赖你后续任何结论。

每行 8 列：编号｜事项｜一句话现状｜证据锚点(file:line)｜建议｜置信度｜最坏情形｜签不签的后果。

必须含：S2 做T v2 判砍的执行确认、EXP 复评三项、排班 v2 残余门位（一次性任务删除 / 蓝图×2 / L-6 / C-12）、通道遗留（`security_event_bus` / `telemetry`）、夜班台账 H-01~H-07、S5 尺子若尚未定稿则列"现行阈值维持 / 改动"两案。

**这份表是你今晚的最低交付——后面什么都不做，它也要在。**

## 2. 今晚时间盒（硬切；到点换档；不追求做完，追求落盘）

| 时刻 | 档 | 收口物 |
|---|---|---|
| 17:10–17:40 | §1 速裁表 | owner_fast_sign 落盘+提交 |
| 17:40–18:40 | S13 策略切换统计判据收尾 | 切换规则定稿 + 翻转清单 |
| 18:40–18:55 | 扫实验台账一次（等待免费，禁轮询） | 判读入档 |
| 18:55–20:10 | S14 前视偏差专项 | 时点违规清单 + 能变红的前视探针测试（直改） |
| 20:10–21:10 | **S18 提交与门禁链路提速审计（新增，§3）** | 瓶颈榜 + 放行裁定书 + Flash 施工规格 |
| 21:10–21:55 | S5 定尺子 | standards.yaml 定稿提案 + 翻转清单 |
| 21:55–22:25 | S3 裁定书**按族批签**（12–14 页；放弃 96 页逐条，明细留在速裁表） | adjudications/ 族级批文 |
| 22:25–23:00 | 转 Flash 夜间交接包 + 被推翻结论清单 + P0 待复核清单 | flash 指令增补 + executive_summary 骨架 |
| 23:00–24:00 | **强制收官**：两轮循环自检、临时件清零、release 全部锁 | 连续两轮零问题才算完 |

**23:00 起禁开任何新战场**——那一个小时只属于落盘与自检。宁欠一个战场，不可欠一份提交。

## 3. S18 提交与门禁链路提速审计（Owner 点名追加）

为什么重要：本项目夜间 50–100 个施工队并发，**提交与门禁的吞吐 = 全项目开发速度**。

已实测弹药（别重测，直接在上面归因）：

- 24h 314 笔提交中衍生/治理税 **78 笔 = 24.8%**（`chore(integrity)` post-flush 重登记 29、其他 chore 23、`chore(reconciler)` batched 19、merge 4、`chore(derived)` watchdog 派生收敛 3）→ 每 1 笔真实提交附 0.33 笔衍生提交。
- 24h 提交堵点提醒 **215 次**：TOP ×86（P50 **81s**）、CLAIM-REQUIRED ×40（P50 33s）、DANGLING-REFERENCE ×16（P50 35s）。
- `GATE-REGENERATE: reconciler timed out (timeout=180s)`；`DEPGRAPH-FRESHNESS WARN 1048min`；`SECRET-REGISTRY-DRIFT required_key_missing[...]` 噪声。

**日志真源（Owner 17:25 实测，昨夜至今夜的混乱全在里面，别去翻报告）**：

| 文件 | 行数 | 覆盖 |
|---|---|---|
| `.runtime/audit/commit_block_events.jsonl` | 1191 | 09-13→09-17 |
| `.runtime/audit/bottleneck_ledger.jsonl` | 2312 | 09-15→09-17 |
| `.runtime/audit/preflight_events.jsonl` | 1124 | 09-15→09-17 |
| `.runtime/audit/gate_execution_stats.jsonl` | 735 | 09-15→09-17 |
| `.runtime/audit/hook_tracked_drift.jsonl` | 1994 | 08-15→09-17 |
| `.runtime/audit/safe_write.jsonl` | 43712 | 08-23→09-17 |
| `.runtime/audit/worktree_drift_watchdog.jsonl` | **156617** | 08-15→（**它自己可能就是病灶：一个看门狗一年写 15 万行**） |
| `.runtime/commit_queue/*.seq` | 322 个（44 个近 24h 有动作） | 含死信与 blob |
| `.runtime/abuse_monitor/abuse_baseline.json`、`.runtime/ai_error_patterns/aggregated_patterns.json` | — | 滥用基线与错误模式聚合 |

必答五问：

1. 78 笔衍生税逐笔归类：哪些是**必然代价**（内容寻址 / 审计链 / 不可逆登记），哪些是**自激循环**（A 提交→触发 B→B 自提→再触发 A）。给出"可消除比例"。post-flush 时序竞态已有治本 commit（`b78a6eafeb` / `24c6046b80`），查它为什么还在 29 次/24h 复发。
2. 215 次堵点逐个归因：**设计如此**（锁语义、claim 前提）还是**实现低效**（全仓扫描、串行、超时无短路）。
3. 50–100 车道并发的真实瓶颈在哪一层：锁粒度 / commit-queue serializer 单通道 / 内容扫描型 gate 的 O(仓库) 成本 / claim TTL 30min 与长任务不匹配。产出**瓶颈榜 + 各层提速倍数估算**（这才是你该做的数学与架构判断）。
4. 哪些**不能放松**（钱闸、own-scope 差分口径、内容寻址、POST-COMMIT-GUARD、RULING-REFERENCE），哪些**严格性已超过收益**（噪声型漂移提醒、纯衍生件重复扫描）。
5. 若只能改三处：改哪三处、预期省多少分钟/夜、副作用半径多大。

**治本口径（Owner 硬要求，不接受止痛）**：S18 的产出形状是**根因表**，一行四栏——`现象（含日志计数）｜机理（为什么会自激/为什么慢）｜治本改法（改哪一层结构）｜为何不再复发（可验判据+反例样本）`。
以下三类**不算治本，写进表里要标 `symptomatic`**：①加大超时值（180s→600s）；②加重试/退避；③加日志或把日志降级。真正的治本长这样：消除自激触发环、把 O(仓库) 扫描改成 O(diff) 且差分基线可复用、把串行单通道改成结构化并发、让看门狗只在状态变化时落盘。
每行还要标**严重级**（判据同 S4：这一层静默失效或长期高摩擦时，全仓哪些"已通过"结论/多少个车道的工时同时失去意义——连锁半径即严重级）。

**边界（防你顺手重构，重要）**：

- 允许直改：**结构性小改**——全绿短路、差分基线复用 `604f414846` 的既有口径、锁粒度收窄、自激触发改条件触发；改完跑相关单测、按战场粒度提交。超时值与日志降级这类**止痛件只准记账，不准当作治本结论提交**。
- 不许动：任何门禁的**语义与判据**、删门禁、`risk_tier_registry.yaml` 门位、serializer 通道数——一律写成一页式裁定书等 Owner 签（三问第三问）。
- 施工量大但判据明确的性能改造：写成**带机读判据的交接包**转 Flash 夜间，别自己写几百行。

## 4. 今晚的配额用法（目标：0 点前用尽，不浪费）

- 余量 42.4pp ÷ 到 0 点 ≈ 燃烧率需 **≥6.1pp/h** 才用得完；你目前长窗均摊 ≈4.7pp/h → 自然跑完只到约 33pp，**余约 9pp = 白扔两小时**。所以：把等待压到最少（后台实验照跑，等待免费），省下的时间全砸进 S18/S14 的读代码与写探针测试。
- 判读密度不够时**不要碎问、不要为耗而耗**：宁可把 S15（假绿清单）与 S12（极端日反证场景规格）提前做实。
- 禁：把同一份大文件读第二遍（读前先查自己的战场笔记）；轮询实验；前台 `sleep`/`tail` 盯盘。

## 5. "一个管业务、一个管 Git"怎么分（Owner 已判：今晚不另开）

- **今晚不开第二个 Kimi 会话**：同一配额池（两个会话各发一遍大上下文=燃烧率翻倍、双双见底且都没保底）；同仓抢锁（`gate_registry.yaml` / `git_commit.py` / `commit_queue.py` / `lock_files.py` 这条链被两个会话同时改必互相吞档，事故在案）；结论互相引用会触发 DANGLING-REFERENCE / RULING-REFERENCE 连环红。
- **正确的切法是按"判断 vs 施工"切，不是按"业务 vs Git"切**：你当设计者（判断密集、token 便宜、只有你能做的根因判定与放行裁定），Flash 当施工队（体积大、判据明确、免费）。
- **Git 专役对话应当在今晚 0 点后开，开 Flash 档**，输入就是你 S18 产出的施工包（含机读判据与反例样本），明早 Owner 只看红灯/绿灯。
- 若 Owner 硬要今晚就有一条 Git 车道：**21:10 你落盘瓶颈榜之前，任何旁路会话禁改治理链文件**，只许做只读取证。

## 6. 收官报告额外必答（今晚新增三条）

1. S18 三句话结论：治理税能砍掉多少、瓶颈第一名是什么、Owner 该签哪几张放行单。
2. 被推翻的结论清单（照旧，这是本案子的成绩单）。
3. 今晚未做完的战场**逐条转成 Flash 可执行交接包或速裁表条目**——不许留"我还没来得及做"。
