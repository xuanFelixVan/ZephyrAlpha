---
module_id: ""
title: ""
doc_type: blueprint
status: draft
version: "0.1.0"
layer: cross_layer
owner: ZephyrAlpha-Owner
classification: confidential
language: zh
created_by: human_plus_agent
date: ""
ttl: permanent
summary: ""
tags: []
rule_form: structural
scope: global
stability: evolving
verifiability: manual
depends_on: []
---

# {模块名称} 蓝图 + 施工指引

> module_id: {XXX-NNN} | version: {X.Y.Z} | status: {draft/active/deprecated} | layer: {l{xx}_{name}/cross_layer}

---

## ⚠️ Vibe Coding 蓝图编写铁律

> 以下铁律来自实战经验。违反任何一条都可能导致后续 AI 施工时出现幻觉、路径漂移、执行失败。
> AI 编写蓝图时**必须**逐条确认已遵守。
>
> **本模板已合并蓝图（设计）和施工指引（实施）——对 100% AI 开发来说，蓝图和施工图本就是一体的。**
> AI 只需输出这一份文档：前 10 节定义架构，第 11 节给出实施步骤。

| # | 铁律 | 为什么 | 违反后果 |
|---|------|--------|---------|
| 1 | **所有路径必须是绝对路径**（含盘符 `D:\`） | AI 零记忆，不知道相对路径的基准在哪 | 文件创建到错误位置 |
| 2 | **必备链接不可省略**——即使与前序文档重复也必须完整列出 | AI 每次新 session 是零记忆，不记得前序文档写了什么 | AI 跳过不读，施工时缺少关键信息 |
| 3 | **蓝图必须是最终设计结果**——不记录决策过程、不保存未选方案 | 决策过程是草稿的事——蓝图是施工依据，不是讨论记录 | 蓝图过厚，关键信息被噪音淹没 |
| 4 | **产出物路径必须与 GOV-DOC-002 一致** | AI 不知道项目目录规范，会自行创建路径 | 路径幻觉——文件放错位置 |
| 5 | **涉及文件范围必须明确列出** | AI 不知道边界在哪，会越界修改 | 范围漂移——改了不该改的文件 |
| 6 | **容量估算必须写** | AI 不知道系统能容纳多少，可能设计出无法扩展的方案 | 容量瓶颈——上线后发现不够用 |
| 7 | **迁移/废弃方案必须写** | AI 不知道旧东西怎么处理，可能直接删除或保留 | 断链——旧引用找不到文件；或垃圾积累 |
| 8 | **"待定"/"建议"/"按需"等模糊词禁止使用** | AI 无法处理模糊指令，需要明确的二元判断 | 执行漂移——AI 自行决定，可能选错 |
| 9 | **蓝图必须自包含**——关键信息不能只写"详见XX" | AI 可能不读引用的文件 | 信息缺失——AI 缺少关键上下文 |
| 10 | **删除文件必须遵守安全删除协议**——禁止直接删除任何文件 | 没有git备份，删除不可逆；AI可能误判文件"没用了" | 永久丢失——无法恢复 |

---

## ⚠️ 安全删除协议

> **核心原则**：删除文件是不可逆操作。项目当前没有 git 备份，一旦删除就永远丢失。
> 蓝图中如果涉及文件废弃/迁移/删除，**必须**在此写出安全删除方案。

### 蓝图中的删除决策清单

> ⚠️ 如果本蓝图会导致文件被废弃/迁移/删除，**必须**在此列出。

| # | 待删除/废弃文件 | 完整绝对路径 | 删除类型 | 接收文件 | 安全删除方案 |
|---|---------------|------------|---------|---------|------------|
| 1 | {文件名} | `D:\ZephyrAlpha\...` | {迁移型/覆盖型/废弃型} | `D:\ZephyrAlpha\...` | {迁移→交叉验证→标记deprecated→Phase4物理删除} |

### 删除铁律

| # | 铁律 | 原因 |
|---|------|------|
| 1 | 禁止蓝图阶段物理删除任何文件 | 蓝图只做决策，不做执行 |
| 2 | 迁移型删除必须逐条迁移、逐条验证 | 批量迁移容易遗漏 |
| 3 | 物理删除只能在 stable 搬入阶段执行 | 给足缓冲期，deprecated 至少保持1个Phase |
| 4 | 物理删除必须人类确认 | AI 不得自行决定删除文件 |
| 5 | "宁可慢，不可漏" | 没有git备份，删了就没了 |

---

## 必备链接

> ⚠️ AI 蓝图编写者**必须**先读取以下所有文件，确认内容已理解后再开始编写。
> **禁止省略**——即使某些文件在多个蓝图中重复出现，也必须在此完整列出。
> **禁止使用相对路径**——所有路径必须从盘符开始写完整绝对路径。

| # | 文件 | module_id | 版本 | 完整绝对路径 | 编写时用途 |
|---|------|-----------|------|------------|----------|
| 1 | 元数据注册表 | PS-STD-001 | {当前版本} | `D:\ZephyrAlpha\docs\01_policies_and_standards\meta\metadata-registry.md` | 编号规则、doc_type词表、frontmatter模板 |
| 2 | 目录结构标准 | GOV-DOC-002 | {当前版本} | `D:\ZephyrAlpha\docs\01_policies_and_standards\governance\document\directory-structure-standard.md` | 路径映射、边界判据 |
| 3 | 治理方法论 | PS-STD-011 | {当前版本} | `D:\ZephyrAlpha\docs\01_policies_and_standards\meta\governance-methodology-standard.md` | MTH-012 涌现式设计 + MTH-013 路径合规创建 |
| 4 | 文件命名规范 | GOV-DOC-003 | {当前版本} | `D:\ZephyrAlpha\docs\01_policies_and_standards\governance\document\file-naming-standard.md` | 命名规则 |
| 5 | 模块 ID 注册表 | — | — | `D:\ZephyrAlpha\docs\02_enterprise_architecture\target-architecture\architecture-model\module-id-registry.yaml` | 编号注册 |
| 6 | 架构总览 | — | — | `D:\ZephyrAlpha\docs\02_enterprise_architecture\target-architecture\00-overview.md` | 架构上下文 |
| 7 | 治理规则主注册表 | — | — | `D:\ZephyrAlpha\docs\01_policies_and_standards\_registry\catalogs\document-metadata-index.yaml` | 现有规则索引 |
| 8 | AI 自治权限注册表 | GOV-AI-001 | — | `D:\ZephyrAlpha\docs\01_policies_and_standards\_registry\catalogs\ai-autonomy-authority-registry.md` | AI 操作权限 |

> 💡 **为什么必须列完整路径？** 因为 AI 每次新 session 是零记忆。如果只写 `metadata-registry.md`，AI 不知道它在哪个盘、哪个目录下，会搜索整个项目——搜索可能找到错误文件或超时。

---

## 项目中已有类似功能

> ⚠️ 防重复检查：编写蓝图前**必须**确认项目中是否已有类似功能。如果已有，必须说明为什么不能复用/扩展。

| # | 已有模块/文件 | 完整绝对路径 | 功能重叠点 | 为什么不能复用 |
|---|-------------|------------|----------|-------------|
| 1 | {模块名} | `D:\ZephyrAlpha\...` | {重叠什么} | {为什么不能直接用} |

> 如果无类似功能，填写"无"。

---

## 涉及的文件范围

> ⚠️ 防范围漂移：本蓝图涉及的文件范围。AI 编写和施工时**不得**超出此范围。
> **必须列出每个文件的完整绝对路径**，AI 不知道相对路径的基准。

| # | 文件/目录 | 完整绝对路径 | 关系 | 变更类型 |
|---|---------|------------|------|---------|
| 1 | {文件/目录名} | `D:\ZephyrAlpha\...` | {读取/修改/新建/迁移/废弃} | {具体变更内容} |

> 💡 **为什么必须列完整路径？** AI 不知道项目结构，如果只写 `governance/security/`，AI 不知道它在 `01_policies_and_standards/` 下还是 `02_enterprise_architecture/` 下。

---

## 1. 设计背景与目标

### 1.1 背景

{描述当前痛点、业务需求、技术背景}

### 1.2 目标

| # | 目标 | 可衡量标准 |
|---|------|-----------|
| 1 | {目标 1} | {验收标准} |

### 1.3 不包含的目标

| # | 明确排除 | 原因 |
|---|---------|------|
| 1 | {不做什么} | {为什么不做} |

---

## 2. 模块边界

### 2.1 职责范围

| # | 职责 | 说明 |
|---|------|------|
| 1 | {职责 1} | {具体做什么} |

### 2.2 不包含的职责

| # | 排除项 | 由谁负责 |
|---|--------|---------|
| 1 | {不做什么} | {哪个模块做} |

---

## 3. 接口契约

> ⚠️ 完整升级方案——6 个子节。强制 **Pydantic V2 BaseModel**（ADR-0040），禁止 `@dataclass`。
> 接口契约是模块对外的合同——下游模块和 AI 施工者都依赖此节了解"输入什么、输出什么、能调用什么"。

### 3.1 公共 API

> 列出所有公开类和方法。每个方法必须包含：函数签名 + 输入/输出说明 + 核心逻辑描述。

```python
from pydantic import BaseModel

class {ModuleName}:
    """模块主类——一句话说明职责"""

    def method_name(self, param: str) -> "{ResultType}":
        """
        方法说明

        输入：param 的含义和约束
        输出：ResultType 的含义和结构
        核心逻辑：简要描述做什么
        """
        ...
```

### 3.2 数据模型

> ⚠️ 强制 **Pydantic V2 BaseModel**——禁止 `@dataclass`。
> 每个字段必须有类型注解 + Field(description) + 必要时的 validator。
> 枚举类型必须用 `str, Enum`，列出所有合法值。

```python
from pydantic import BaseModel, Field, field_validator
from enum import Enum
from typing import Optional

class {EnumName}(str, Enum):
    """枚举说明——列出所有合法值"""
    VALUE_A = "value_a"
    VALUE_B = "value_b"

class {DataModel}(BaseModel):
    """模型说明"""
    field_name: str = Field(..., description="字段说明")
    optional_field: Optional[str] = Field(default=None, description="可选字段说明")

    @field_validator("field_name")
    @classmethod
    def validate_field(cls, v: str) -> str:
        """校验逻辑"""
        ...
        return v
```

### 3.3 输入契约

> 每个接口的输入约束——字段、必填、格式要求。

| 接口 | 输入字段 | 必填 | 约束 |
|------|---------|:---:|------|
| `method_name()` | `param_name` | ✅/❌ | 约束条件（格式、范围、路径要求） |

### 3.4 输出契约

> 每个接口的成功/失败输出——包括返回值和错误码。

| 接口 | 成功输出 | 失败输出 |
|------|---------|---------|
| `method_name()` | `ResultType`：字段说明 | `ERROR_CODE_1` / `ERROR_CODE_2` |

### 3.5 MCP 接口

> 如果本模块需要暴露为 MCP Tool，在此定义。若无，填写"本模块不暴露 MCP 接口"。

**Tools**：

| Tool | API | 输入 | 输出 |
|------|-----|------|------|
| `tool_name` | `method_name()` | `{param: type}` | `{field: type}` |

**错误码**：`ERROR_CODE(HTTP_STATUS)` — 说明

### 3.6 契约版本

> 声明接口兼容性承诺——哪些变更需要审批，哪些 AI 可自主。

| 契约部分 | 兼容性 | 说明 |
|---------|:---:|------|
| 新增字段/方法 | ✅ 向后兼容 | 不影响已有消费者 |
| 删除/重命名字段/方法 | ❌ 破坏性 | 需 Owner 审批 + 迁移方案 |
| 新增枚举值 | ✅ 向后兼容 | 不破坏已有逻辑 |
| MCP Tool 新增 | ✅ 向后兼容 | 不影响已有消费者 |
| MCP 输入 Schema 修改 | ⚠️ 需通知 | 消费者需更新参数 |

**变更通知**：破坏性变更→Owner审批+蓝图minor+1。兼容性变更→AI自主+patch+1。

---

## 4. 约束条件

### 4.1 技术约束

| # | 约束 | 原因 |
|---|------|------|
| 1 | {技术约束} | {为什么} |

### 4.2 容量估算

> ⚠️ **必须填写**。AI 不知道系统能容纳多少，可能设计出无法扩展的方案。

| 维度 | 当前规模 | 峰值需求 | 系统极限 | 是否够用 | 扩展方案 |
|------|:------:|:------:|:------:|:------:|---------|
| {文件数/模块数/数据量} | {N} | {N} | {N} | ✅/❌ | {不够时怎么扩展} |

> 💡 **为什么必须写容量？** 实战教训：编号规则设计时没算容量，后来发现编号空间不够，被迫重新设计编号体系，导致全量迁移。

### 4.3 迁移/废弃方案

> ⚠️ 如果本蓝图会导致现有文件被废弃或迁移，**必须**在此写出具体方案。

| # | 废弃/迁移对象 | 当前位置 | 目标位置 | 处理方式 | 引用更新方案 |
|---|-------------|---------|---------|---------|------------|
| 1 | {文件名} | `D:\ZephyrAlpha\...` | `D:\ZephyrAlpha\...` 或 "删除" | {迁移+重定向/标记deprecated/物理删除} | {搜索全项目引用并更新} |

---

## 5. 依赖关系

| 依赖模块 | 依赖类型 | 依赖内容 | 版本要求 |
|---------|---------|---------|---------|
| {模块 ID} | {必须/可选} | {依赖什么} | {版本} |

---

## 6. 产出物存放目录

> ⚠️ 防路径漂移：蓝图阶段**必须**规划好所有产出物的物理存放路径。
> AI 施工时**必须**严格按此路径存放，**不得**自行创建新路径。
> **所有路径必须是绝对路径**（含盘符）。
> 路径必须与 GOV-DOC-002（directory-structure-standard.md）§5.1.2 路径映射表一致。
> 新目录/新文件创建必须遵守 **MTH-013 路径架构合规创建原则**——先在索引中查询再创建，不得自行决定目录层级。

| 产出物类型 | 存放完整绝对路径 | 说明 |
|----------|---------------|------|
| 蓝图文件 | `D:\ZephyrAlpha\docs\03_modules\{layer}\{module-name}\blueprint.md` | 本文件（含设计和施工指引） |
| 业务代码 | `D:\ZephyrAlpha\src\zephyr\{layer_id}\` | Python 源码 |
| 测试代码 | `D:\ZephyrAlpha\tests\{layer_id}\` | 测试用例 |

> 💡 **一个文件，全流程覆盖**：对于 100% AI 开发的项目，蓝图和施工指引合并在一份 `blueprint.md` 中。AI 读这一份文件，就同时获得架构定义（第 1-10 节）和实施步骤（第 11 节）。

---

## 7. 集成目标

> ⚠️ 防孤岛：本模块**必须**集成到哪个现有系统？

| 集成目标系统 | 集成方式 | 集成点 | 验证方法 |
|------------|---------|--------|---------|
| {系统名} | {新增接口/修改现有接口/事件订阅/配置注入} | {具体哪个类/函数/配置} | {怎么验证集成成功} |

---

## 8. 需要更新的相关内容

> ⚠️ 防遗漏：创建本模块时，以下现有文件**必须**同步更新。

| # | 需更新的文件 | 完整绝对路径 | 更新内容 | 更新原因 |
|---|------------|------------|---------|---------|
| 1 | 模块 ID 注册表 | `D:\ZephyrAlpha\docs\02_enterprise_architecture\target-architecture\architecture-model\module-id-registry.yaml` | {新增/修改什么} | {为什么} |
| 2 | 蓝图注册表 | `D:\ZephyrAlpha\docs\03_modules\blueprint-registry.yaml` | {新增/修改什么} | {为什么} |
| 3 | 治理资产清单 | `D:\ZephyrAlpha\docs\01_policies_and_standards\_registry\catalogs\document-metadata-index.yaml` | {新增/修改什么} | {为什么} |

---

## 9. 已知风险与缓解

| # | 风险 | 概率 | 影响 | 缓解策略 |
|---|------|------|------|---------|
| 1 | {风险描述} | {高/中/低} | {高/中/低} | {怎么应对} |

---

## 10. 后果（Consequences）

> 采纳这些设计决策后，会带来什么后果？**必须**写正面和负面。

{描述正面和负面后果}

---

## 11. 施工指引

> ⚠️ 本节是原独立"施工图"的合并内容。对 100% AI 开发来说，蓝图+施工指引合并为一份文档。
> AI 施工者**直接读本蓝图**即可开始施工——**无需另一份"施工图"文件**。

### ⚠️ AI 施工前检查清单

> ⚠️ AI 施工者**必须**在开始施工前逐项确认。任何一项为 ❌ 都**不得**开始施工。

| # | 检查项 | 确认方式 | 状态 |
|---|--------|---------|:----:|
| 1 | 已读取本蓝图全部内容（§1-§10 架构 + §11 施工指引） | 逐节确认 | ☐ |
| 2 | 已读取必备链接中所有真源文件 | 逐个打开确认 | ☐ |
| 3 | PS-STD-001 编号规则（§5）已理解 | 能回答"GOV-SEC-001是什么" | ☐ |
| 4 | GOV-DOC-002 防幻觉路径映射（§5.1.2）已理解 | 能回答"某类文件该放哪" | ☐ |
| 5 | MTH-013 路径合规创建原则已理解 | 能回答"新文件创建前三步验证流程" | ☐ |
| 6 | 每个施工步骤都对应明确的蓝图接口契约（§3） | 逐步骤追溯 | ☐ |

### 11.1 施工策略

| 项目 | 内容 |
|------|------|
| 施工阶段数 | {N} 个 Phase（或单 Phase 一次性完成） |
| 施工模式 | {新建 / 扩展 / 重构迁移} |
| 核心风险 | {施工过程中的最大风险} |

### 11.2 前置条件

| # | 依赖项 | 依赖类型 | 当前状态 | 是否满足 |
|---|--------|---------|:---:|:---:|
| 1 | {依赖模块/文件已就绪} | {hard/soft} | ☐ | ☐ |
| 2 | {前置蓝图已 approved} | hard | ☐ | ☐ |

### 11.3 实施步骤

> ⚠️ 每个步骤严格按"读→做→产→检"四步执行。
> 每个步骤**必须**包含：产出物完整绝对路径、验收标准、G7 完整度门禁检查项。

#### 步骤 1：{步骤名称}

| 项目 | 内容 |
|------|------|
| 对应蓝图契约 | §3.{N} |
| 产出位置 | `D:\ZephyrAlpha\...` |
| 验收标准 | {客观、可检查的标准} |
| G7 检查项 | {上游文件是否全部列出？下游产出物路径是否全部精确？回滚方案是否可执行？} |

**创建文件清单**：

| module_id | 文件名 | doc_type | 完整绝对路径 |
|-----------|--------|----------|------------|
| {MOD-XXX-NNN} | {file-name.py} | {code} | `D:\ZephyrAlpha\...\file-name.py` |

**内容编写指引**：

| 文件 | 核心内容 | 必须包含 |
|------|---------|---------|
| {file-name.py} | {这个文件在讲什么} | {至少3条可验证的实现要点} |

#### 步骤 2：{步骤名称}

| 项目 | 内容 |
|------|------|
| 对应蓝图契约 | §3.{N} |
| 产出位置 | `D:\ZephyrAlpha\...` |
| 验收标准 | {客观、可检查的标准} |
| G7 检查项 | {上游文件是否全部列出？下游产出物路径是否全部精确？回滚方案是否可执行？} |

{如需更多步骤，复制上表}

### 11.4 回滚方案

> ⚠️ 每个步骤如果出问题，**必须**有明确的回滚操作。

| 步骤 | 如果出问题 | 回滚操作 |
|------|----------|---------|
| 1 | {什么问题} | {具体回滚步骤} |

> 💡 **为什么必须有回滚方案？** AI 执行出错后不知道怎么回退，要么继续执行导致错误传播，要么停下来打断流程。

### 11.5 施工完成标准

| # | 产出物 | 存放完整绝对路径 | 是否存在 | 内容非空 |
|---|--------|---------------|:---:|:---:|
| 1 | {产出物名} | `D:\ZephyrAlpha\...\` | ☐ | ☐ |

### 11.6 施工状态

> 施工者在施工结束后填写此表。

| 字段 | 值 | 填写者 |
|------|-----|-------|
| construction_status | {not_started / in_progress / completed} | 施工者 |
| verification_status | {unverified / passed / failed} | 审计者 |

---

## 治理信息

### SSoT 声明

| 内容 | 真源 | 非真源 |
|------|------|--------|
| {本蓝图的核心架构设计} | **本文档 §1-§10** | {已被取代的旧蓝图/旧文档} |
| {本模块的施工步骤} | **本文档 §11** | {已废弃的旧施工图} |
| {本模块的接口契约} | **本文档 §3** | — |

**任何与本蓝图冲突的定义，以本蓝图为准。**

### 消费者注册表

| Tier | 消费者 | 依赖内容 |
|:----:|--------|---------|
| Tier 1 | {下游模块蓝图路径} | §3 接口契约、§5 依赖关系 |
| Tier 2 | {集成目标系统文件路径} | §7 集成点 |
| Tier 3 | {代码文件路径} | §3 数据模型、§6 产出物路径 |

### 变更同步规则

| 变更类型 | Tier 1（下游蓝图） | Tier 2（集成系统） |
|---------|------------------|------------------|
| 新增/修改接口契约 | 下游检查接口兼容性 | 检查集成点兼容性 |
| 修改施工步骤 | 下游更新产出物引用 | 更新配置文件 |
| 修改模块边界 | 下游更新依赖声明 | 更新集成路由 |

### 修改条件

| 变更类型 | 审批要求 |
|---------|---------|
| 接口契约新增/修改（§3） | 需 Owner 审批 + 通知所有消费者 |
| 模块边界修改（§2） | 需 Owner 审批 |
| 施工步骤微调（命令、路径修正） | AI 可自主修改 |
| 非关键补充（风险缓解、后果描述） | AI 可自主修改 |

---

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| {YYYY-MM-DD} | 0.1.0 | 初始版本 |
