---
actual_disk_path: ''
belongs_to: ''
classification: confidential
construction_progress: not_started
created_by: human_plus_agent
date: ''
depends_on: []
doc_type: template
functional_domain: ''
generation: 1
language: zh
last_updated: ''
last_verified: ''
layer: cross_layer
module_id: GOV-028
owner: ZephyrAlpha-Owner
parent_module: ''
priority: P2
references: []
rule_form: structural
scope: global
ssot_claims: []
stability: evolving
status: Draft
summary: ''
tags: []
template_for: blueprint
title: Blueprint Construction Template
ttl: permanent
verifiability: manual
version: 2.0.0
# === 蓝图模板 v2.0.0 — 四图对齐（ARCH-056）===
# depgraph.nodes 是架构数据真源（SSoT），蓝图是派生视图。
# 蓝图只存全景图(depgraph)没有的独有信息：设计意图/职责边界/接口契约/施工指引/验收标准/决策记录/约束条件/错误处理
# §0.1 代码文件清单 = 自动生成（派生自 depgraph.nodes），禁止手写
# §0.6 四图对齐视图 = 自动生成（派生自 depgraph + dataflow + decision）
# 查询文件清单：python scripts/governance/extract_depgraph.py --modules <module_id>
# 查询依赖关系：python scripts/governance/extract_depgraph.py --paths
---

<!--
COMPLIANCE_CHECKLIST — 机器可解析合规清单
蓝图 MUST 包含以下所有标题（精确匹配关键词）。缺一 = 不合规。
脚本：python scripts/governance/check_blueprint_compliance.py <蓝图路径>
-->
<!--
REQUIRED_SECTIONS:
  overview: "概述"
  §0: "代码对齐验证"
  §0.1: "代码文件清单"  # v2.0.0: 自动生成（派生自 depgraph.nodes），禁止手写
  §0.2: "对齐验证矩阵"
  §0.3: "版本-代码映射"
  §0.4: "SSoT与责任唯一性"
  §0.5: "代码目录唯一性"
  §0.6: "四图对齐视图"  # v2.0.0 新增：自动生成（depgraph + dataflow + decision + blueprint 四图对齐）
  §1: "设计背景与目标"
  §1.1: "背景"
  §1.2: "目标范围"
  §1.4: "运行场景约束"
  §1.5: "利益相关者"
  §1.6: "差距"
  §1.7: "典型场景"
  §2: "模块边界"
  §2.1: "职责边界"
  §3: "架构设计"
  §3.1: "组件架构"
  §3.2: "数据流"
  §3.3: "状态生命周期"
  §4: "接口契约"
  §4.1: "公共 API"
  §4.2: "数据模型"
  §4.3: "输入契约"
  §4.4: "输出契约"
  §4.5: "MCP 接口"
  §4.6: "契约版本"
  §4.7: "OCP 扩展点"
  §5: "约束条件"
  §5.1: "技术约束"
  §5.2: "容量估算"
  §5.3: "迁移"
  §5.4: "非功能需求与服务水平"
  §5.5: "自动化触发机制"
  §5.7: "禁止模式与导入约束"
  §6: "错误处理"
  §6.1: "可观测性"
  §6.2: "退化矩阵"
  §8: "安全考量"
  §9: "测试策略"
  §10: "依赖关系"
  §10.5: "概念重叠声明"
  §10.6: "依赖链风险评级"
  §11: "产出物"
  §12: "集成目标"
  §13: "需要更新"
  §14: "风险"
  §16: "施工指引"
  §16.7: "参考实现规格"
  §16.8: "施工参考卡"
  §16.10: "故障与操作"
  §16.12: "并发操作"
  §17: "容量升级"
  §18: "决策记录"
  glossary: "术语表"
  blindspots: "已知问题"
  checklist: "自检与闭合清单"
  maturity: "成熟度"
  roadmap: "版本演进路线图"
  pre_1: "Vibe Coding"
  pre_2: "安全删除"
  pre_3: "必备链接"
  pre_4: "已有类似功能"
  pre_5: "涉及的文件范围"
  ssot_claims: "frontmatter必须包含ssot_claims字段"
  consumer_min: "§11产出物表格必须包含consumer_min列"
  overlap_check: "§2.1必须包含职责唯一性声明表格"
  temporal_type: "每个章节MUST标注temporal_type: permanent|construction_temporary|operational_temporary。压缩时只删construction_temporary"
END_REQUIRED_SECTIONS
-->

<!-- H1 格式: # {English Module Name} 蓝图+施工图 — {中文一句话描述}。中文描述MUST与frontmatter.title一致 -->
# {English Name} 蓝图+施工图 — {中文一句话描述}

> module_id: {XXX-NNN} | version: {X.Y.Z} | status: {draft/active/deprecated} | layer: {{domain_name}/cross_layer}
> actual_disk_path: {src/zephyr/{pkg}/} | generation: {N} | construction_progress: {not_started~completed}

## 概述

> 3~5 句话建立心理模型。

{本蓝图描述 {模块名}——它解决了 {核心问题}。核心职责包括：{职责1}、{职责2}、{职责3}。当前规模 {当前数据}，目标容量 {目标数据}。上游依赖 {上游模块}，下游被 {下游模块} 消费。}

---

> **标准锚点（防幻觉）**——本蓝图必须严格遵循以下标准：
> - 蓝图+施工图模板：[blueprint-construction-template.md](file:///d:/ZephyrAlpha/docs/01_policies_and_standards/templates/blueprint-construction-template.md)
> - AI 压缩工作流标准：[trae_030_doc_numbering_metadata.yaml](file:///d:/ZephyrAlpha/docs/01_policies_and_standards/rules/trae_030_doc_numbering_metadata.yaml)
> - 代码头部标准：[code-construction-standards.md §7](file:///d:/ZephyrAlpha/docs/01_policies_and_standards/governance/engineering/code-construction-standards.md)
> - 依赖图：[dependency_path_panorama.md](file:///d:/ZephyrAlpha/docs/02_enterprise_architecture/04_architecture_principles_decisions/dependency_path_panorama.md)（V6.0 依赖路径全景图）；机器真源：PostgreSQL depgraph（`python scripts/governance/extract_depgraph.py --modules {module_id}`）
> - 优化规则：先 Layer 1（蓝图模板合规）→ 后 Layer 2（规格化砍削）

---

## §0 代码对齐验证

> 版本变更后 MUST 重新填写。

### §0.1 代码文件清单

> **v2.0.0 变更**：本节为自动生成（派生自 depgraph.nodes），禁止手写。
> 真源：PostgreSQL depgraph.nodes 表（`blueprint_id = {module_id}` 的所有 file 粒度节点）
> 生成命令：`python scripts/governance/extract_depgraph.py --modules {module_id}`
> 手写漂移率 20.7%（ARCH-055），v2.0.0 起由 depgraph 单向派生消除漂移。

> 存在性：未实现/已实现/已阻塞（MUST注明原因）/已废弃（MUST在§5.3说明）
> 归属判定：本模块 / ⚠️建议迁移至{模块} / ⚠️与{文件}重叠。编写时 MUST 逐文件判断：此文件的职责是否属于本蓝图 §2.1 声明的职责边界？不属于→标注建议迁移。与同包内其他文件功能重叠→标注重叠。

| # | 文件名 | 对应蓝图章节 | 职责 | 存在性 | 归属判定 | 阻塞原因（仅已阻塞） |
|---|--------|------------|------|:-----:|---------|-------------------|
| 1 | {file_name}.py | §3.{N} | {一句话职责} | {未实现/已实现/已阻塞/已废弃} | {本模块/⚠️建议迁移至{模块}/⚠️与{文件}重叠} | {阻塞时填写} |

### §0.2 对齐验证矩阵

> 每次蓝图版本升级后填写。验证 construction_progress 是否与代码实际状态一致。

| 验证项 | 验证方法 | 结果 |
|--------|---------|:---:|
| construction_progress = completed → 代码文件清单100%存在 | `ls src/zephyr/{pkg}/` 逐文件核对 | ☐ |
| construction_progress = partially_implemented → 已实现章节的代码存在 | 按章节核对 | ☐ |
| construction_progress = scaffold → __init__.py 存在且非空 | `cat __init__.py` | ☐ |
| construction_progress = design_only → 代码目录不存在或为空 | `ls src/zephyr/{pkg}/` | ☐ |
| 蓝图描述的类/函数名 = 代码中的类/函数名 | `grep "class\|def" *.py` | ☐ |
| 代码 [BLUEPRINT] 头部指向 = 本蓝图 module_id | `grep "\[BLUEPRINT\]" *.py` 核对 module_id | ☐ |
| §4.2 每个数据模型的 SSoT 文件中确实存在该模型 | `grep "class {ModelName}" {ssot_file}.py` 逐模型核对 | ☐ |
| §0.1 每个文件的职责与其他文件无重叠 | 交叉比对职责列 | ☐ |
| §0.1 归属判定列无 ⚠️ 标记 或 ⚠️ 标记已有处置计划 | 逐文件核对 | ☐ |
| §5.5 自动化触发机制状态列与代码实现一致 | `python scripts/governance/d5_architecture/checkers/check_blueprint_automation_sync.py --blueprint <蓝图路径>` | ☐ |

### §0.3 版本-代码映射

> 记录蓝图版本与代码实现的对应关系。容量升级蓝图（generation≥2）必须填写。

| 蓝图版本 | 代码覆盖范围 | 缺失组件 | 缺失原因 |
|---------|------------|---------|---------|
| v1.0.0 (基线) | {已实现的组件列表} | — | — |
| v2.0.0 (容量升级) | {已实现的升级组件} | {未实现的升级组件} | {待施工/优先级} |

### §0.4 SSoT 与责任唯一性声明

> 防幻觉核心：AI 默认假设蓝图=真源，实际可能不是。本节显式声明真源边界。
> 不填写 = AI 假设蓝图覆盖的一切都是真源 → 与其他模块冲突时静默覆盖。

| # | 声明维度 | 本蓝图是真源 | 真源在别处（委托） | 委托目标 |
|---|---------|:----------:|:---------------:|---------|
| 1 | {职责维度，如"预算计算逻辑"} | ✅ / ❌ | ✅ / ❌ | {MOD-XXX §N} |
| 2 | {数据维度，如"预算配置Schema"} | ✅ / ❌ | ✅ / ❌ | {MOD-XXX §N} |

> 填写规则：每行只选一个 ✅。两个 ✅ = 责任重叠 = 必须在 §10.5 声明并给出委托关系。

### §0.5 代码目录唯一性声明

> 防副本冲突：同一功能可能存在于多个目录（历史迁移残留、重构遗留）。AI 不知道哪个是主目录。
> 不填写 = AI 遇到两个同名模块时随机选择 → 可能改了副本而非真源。

| # | 声明项 | 值 |
|---|--------|-----|
| 1 | 主代码目录 | `src/zephyr/{pkg}/`（MUST与 frontmatter.actual_disk_path 一致） |
| 2 | 已知副本目录 | {无 / `src/zephyr/{old_pkg}/` — 原因：{迁移残留，计划删除日期}} |
| 3 | 副本处置状态 | {无副本 / 已迁移消费者数：N/总消费者数：M / 全部迁移完成可删除} |

> 副本存在时 MUST 在 §16.10 故障与操作 中记录迁移进度。全部消费者迁移完成前禁止删除副本。

---

### §0.6 四图对齐视图

> **v2.0.0 新增**：自动生成（派生自 depgraph + dataflow + decision + blueprint 四图对齐，ARCH-056）。
> 本节展示模块在四图中的位置和核心字段，满足"模块清单含四图对齐信息"需求。
> 生成命令：`python scripts/governance/d5_architecture/generators/align_panoramas.py --module {module_id}`
> 禁止手写——depgraph 是架构数据真源，蓝图是派生视图。

#### 四图位置

| 图 | 位置 | 状态 | 链接 |
|----|------|------|------|
| 依赖图 (depgraph) | `blueprint_id={module_id}` 的 file 节点 | {production/design} | `extract_depgraph.py --modules {module_id}` |
| 数据流图 (dataflow) | {JOB-NNN 生产的 Dataset / 消费的 Dataset} | {active/planned} | `apply_dataflowgraph.py --list-datasets` |
| 决策架构图 (decision) | {decision_nodes 中的节点} | {production/design} | `generate_decision_diagram.py` |
| 蓝图 (blueprint) | 本文件 | {Draft/Active} | — |

#### 四核心字段

| 字段 | depgraph 值（真源） | 蓝图 frontmatter 值（声明） | 是否一致 |
|------|-------------------|--------------------------|:-------:|
| module_id | {module_id} | {module_id} | ✅/❌ |
| domain_id | {D_XXX} | {responsibility_domain} | ✅/❌ |
| build_status | {production/design} | {construction_progress} | ✅/❌ |
| file_count | {N 文件} | {§0.1 行数} | ✅/❌ |

> 冲突时以 depgraph 为准（ARCH-056 + ARCH-MM-001 声明 vs 验证框架）。

---

## §1 设计背景与目标

### 1.1 背景

{描述当前痛点、业务需求、技术背景}

### 1.2 目标范围

| # | 类型 | 内容 | 标准/原因 |
|---|:----:|------|----------|
| 1 | ✅ 包含 | {目标} | {可衡量标准} |
| 2 | ❌ 排除 | {不做什么} | {为什么不做} |

### 1.4 运行场景约束

| 约束 | 影响 |
|------|------|
| 施工轨道：{T轨可施工 / C轨blocked（解除条件：{xxx}）} | {对设计决策和AI施工权限的影响} |
| {运行环境约束} | {对设计决策的影响} |

### 1.5 利益相关者映射

| 角色 | 关注点 | 参与阶段 | 约束 |
|------|--------|---------|------|
| {Owner} | {架构决策} | {设计+施工} | {审批权限} |

### 1.6 当前态/目标态差距

| 维度 | 当前态 | 目标态 | 差距 | 优先级 |
|------|--------|--------|------|:------:|
| {功能} | {现状} | {目标} | {差距描述} | P0/P1/P2 |

### 1.7 典型场景

| 场景 | 触发 | 处理流程 | 输出 |
|------|------|---------|------|
| {场景名} | {什么触发} | {步骤1→步骤2→步骤3} | {产出} |

---

## §2 模块边界

### 2.1 职责边界

> **核心职责声明**（MUST 填写）：本蓝图的核心职责是 `{一句话}`。职责数量：{N}。
> N>3 时 MUST 执行拆分判定（见"蓝图拆分判定标准"）。横切关注点（安全/审计/可观测）不计入 N，但 MUST 在下方表格中标注类型为"横切"。

| # | 类型 | 职责 | 详情 | 负责方 |
|---|:----:|------|------|--------|
| 1 | ✅ 包含 | {职责} | {具体做什么} | 本模块 |
| 2 | ❌ 排除 | {不做什么} | {为什么不做} | {哪个模块做} |

#### 职责唯一性声明

> 与 frontmatter.ssot_claims 互补：ssot_claims 声明真源范围，本表声明职责不重叠。
> 机器验证：`python scripts/governance/check_ssot_uniqueness.py --blueprint <module_id>`

| 声明项 | 无重叠模块 | 验证方式 |
|--------|-----------|---------|
| <核心职责1> | [MOD-XXX, MOD-YYY] | `python scripts/governance/check_ssot_uniqueness.py --blueprint <module_id>` |
| <核心职责2> | [MOD-ZZZ] | 同上 |

> 概念级重叠（如 drift 检测、budget 检查等跨模块概念）在 §10.5 声明。

---

## §3 架构设计

> 本节回答"系统怎么组成、组件怎么交互"。≤500 字 + 1 张组件交互表。

### 3.1 组件架构

| # | 组件 | 职责 | 依赖 | 交互方式 |
|---|------|------|------|---------|
| 1 | {ComponentName} | {一句话职责} | {依赖的其他组件} | {同步调用/事件/队列/共享存储} |

### 3.2 数据流

| # | 上游 | 处理逻辑 | 下游 | 数据格式 | 转换规则 |
|---|--------|---------|---------|---------|---------|
| 1 | {上游模块/外部系统} | ①{步骤1}→②{步骤2}→③{步骤3} | {下游模块/存储} | {Pydantic Model / dict / file} | {输入→输出的映射规则} |

### 3.3 状态生命周期

> 如果模块有明确的状态机，必须在此描述。无状态模块填写"本模块无状态机"。

| 当前状态 | 触发事件 | 目标状态 | 守卫条件 |
|---------|---------|---------|---------|
| {StateA} | {EventX} | {StateB} | {Condition} |

---

## §4 接口契约

> 强制 **Pydantic V2 BaseModel**（KBG-0040），禁止 `@dataclass`。

### 4.1 公共 API

> 列出所有公开类和方法。每个方法必须包含：函数签名 + 输入/输出说明 + **执行流程**。
> ⚠️ 执行流程是防幻觉关键——没有它AI会编造方法内部逻辑。简单getter/setter可填"直接返回字段"。

```python
class {ModuleName}:
    """模块主类——一句话说明职责"""
    def method_name(self, param: str) -> "{ResultType}":
        """方法说明"""
        ...
```

| 方法 | 执行流程 | 关键决策点 |
|------|---------|-----------|
| `method_name()` | ①{步骤1} → ②{步骤2} → ③{步骤3} | {哪步需要判断/分支/回滚} |

### 4.2 数据模型

> ⚠️ 强制 **Pydantic V2 BaseModel**——禁止 `@dataclass`。枚举用 `str, Enum`。

```python
class {EnumName}(str, Enum):
    VALUE_A = "value_a"

class {DataModel}(BaseModel):
    field_name: str = Field(..., description="字段说明")
    optional_field: Optional[str] = Field(default=None, description="可选字段说明")
```

> ⚠️ **数据模型 SSoT 声明**——每个模型 MUST 声明唯一真源文件。多文件定义同名模型 = 漂移源。
> AI import 时 MUST 从 SSoT 文件导入，禁止从非 SSoT 文件导入同名模型。

| 模型名 | SSoT文件 | 其他定义位置 | 状态 |
|--------|---------|------------|------|
| {DataModel} | {ssot_file}.py | — | ✅ 唯一源 |
| {OverlappingModel} | {ssot_file}.py | {other_file}.py(兼容层,待统一) | ⚠️ 多源冲突 |

### 4.3 输入契约

> 每个接口的输入约束——字段、必填、格式要求。

| 接口 | 输入字段 | 必填 | 约束 |
|------|---------|:---:|------|
| `method_name()` | `param_name` | ✅/❌ | 约束条件（格式、范围、路径要求） |

### 4.4 输出契约

> 每个接口的成功/失败输出——包括返回值和错误码。

| 接口 | 成功输出 | 失败输出 |
|------|---------|---------|
| `method_name()` | `ResultType`：字段说明 | `ERROR_CODE_1` / `ERROR_CODE_2` |

### 4.5 MCP 接口（条件可选）

> 条件可选：仅暴露MCP Tool的模块填写。无MCP填写"本模块不暴露 MCP 接口"。

**Tools**：

| Tool | API | 输入 | 输出 |
|------|-----|------|------|
| `tool_name` | `method_name()` | `{param: type}` | `{field: type}` |

**错误码**：`ERROR_CODE(HTTP_STATUS)` — 说明

### 4.6 契约版本

> 声明接口兼容性承诺——哪些变更需要审批，哪些 AI 可自主。

| 契约部分 | 兼容性 | 说明 |
|---------|:---:|------|
| 新增字段/方法 | ✅ 向后兼容 | 不影响已有消费者 |
| 删除/重命名字段/方法 | ❌ 破坏性 | 需 Owner 审批 + 迁移方案 |
| 新增枚举值 | ✅ 向后兼容 | 不破坏已有逻辑 |
| MCP Tool 新增 | ✅ 向后兼容 | 不影响已有消费者 |
| MCP 输入 Schema 修改 | ⚠️ 需通知 | 消费者需更新参数 |

**变更通知**：破坏性变更→Owner审批+蓝图minor+1。兼容性变更→AI自主+patch+1。

### 4.7 OCP 扩展点（条件可选）

> ⚠️ **条件可选**：仅当本模块有可扩展基类/策略模式/插件机制时填写。
> 判定条件：模块使用 Strategy/Plugin/Template Method 等可扩展模式 → MUST 填写此节。
> 无扩展点的模块填写"本模块无 OCP 扩展点"。

| 扩展点 | 基类/接口 | 默认实现 | 扩展契约 | 注册方式 |
|--------|----------|---------|---------|---------|
| {扩展点名称} | `{BaseClass}` | `{DefaultImpl}` | {扩展时必须遵守的约束} | {工厂注册/配置注入/自动发现} |

---

## §5 约束条件

### 5.1 技术约束

> 蓝图只保留约束本身。"为什么这样约束"属于决策过程，记录在 §18 决策记录中。

| # | 约束 | 值 |
|---|------|-----|
| 1 | {技术约束} | {约束值/阈值} |

### 5.2 容量估算

> **必须填写**。缺失后果：容量瓶颈——上线后发现不够用。

| 维度 | 当前规模 | 峰值需求 | 系统极限 | 是否够用 | 扩展方案 |
|------|:------:|:------:|:------:|:------:|---------|
| {文件数/模块数/数据量} | {N} | {N} | {N} | ✅/❌ | {不够时怎么扩展} |

### 5.3 迁移/废弃方案（条件可选）

> [时态:临时] 执行完毕后删除（铁律#12）。无迁移填写"无迁移/废弃"。缺失后果：断链——旧引用找不到文件；或垃圾积累。

| # | 废弃/迁移对象 | 当前位置 | 目标位置 | 处理方式 | 引用更新方案 |
|---|-------------|---------|---------|---------|------------|
| 1 | {文件名} | `docs/...` 或 `src/zephyr/...` | `docs/...` 或 "删除" | {迁移+重定向/标记deprecated/物理删除} | {搜索全项目引用并更新} |

### 5.4 非功能需求与服务水平

> SLI/SLO/Error Budget/告警阈值 列为 MUST 填写项。缺失后果：容错策略凭空猜测，降级阈值无依据。

| 维度 | NFR指标 | NFR目标 | 测量方式 | SLI | SLO | Error Budget | 告警阈值 |
|------|--------|--------|---------|-----|-----|-------------|---------|
| 可用性 | {SLA} | {99.9%} | {监控} | {可用性SLI} | {99.9%} | {每月允许失败次数} | {告警阈值} |
| 可维护性 | {MTTR} | {<30min} | {故障记录} | — | — | — | — |
| 可观测性 | {指标覆盖率} | {100%} | {指标审计} | — | — | — | — |

### §5.5 自动化触发机制

> ⚠️ **每个操作 MUST 声明触发方式**。未声明 = 手动 = AI 不知道何时自动执行。
> 触发方式枚举：`auto_boot`(系统启动时) / `auto_scheduled`(定时调度) / `auto_event`(事件驱动) / `on_demand`(手动调用)

| 操作 | 触发方式 | 调度策略 | 当前状态 |
|------|---------|---------|---------|
| {服务启动} | auto_boot | {boot序列第N步} | ✅已实现 / ⚠️待实现 |
| {定时维护} | auto_scheduled | {cron表达式或间隔} | ⚠️待实现 |
| {事件响应} | auto_event | {触发事件描述} | ⚠️待实现 |
| {手动操作} | on_demand | — | ✅已实现 |

### §5.7 禁止模式与导入约束

| # | 类型 | 禁止项 | 替代/允许项 | 原因 |
|---|:----:|--------|-----------|------|
| 1 | 编码模式 | {禁止的模式} | {正确的做法} | {为什么禁止} |
| 2 | 导入源 | {zephyr.{其他层}.*} | {zephyr.{pkg}.*} | {分层约束} |

---

## §6 错误处理

> 本节回答"出了问题怎么办"。≤300 字 + 1 张异常处理表。
> §6 = 单个操作/调用的异常处理（try/except 级别）。§6.2 = 整个组件/子系统不可用时的降级策略（熔断/回退级别）。
> ⚠️ MUST 包含 §10 依赖不可用时的降级/熔断场景。MUST 声明是否存在依赖循环（§10.3 执行顺序依赖中 A→B 且 B→A = 循环）。

| # | 异常场景 | 检测方式 | 恢复策略 | 影响范围 |
|---|---------|---------|---------|---------|
| 1 | {异常描述} | {超时/异常/监控} | {重试/降级/熔断/回滚} | {影响哪些下游} |

### 6.1 可观测性规格

> 指标名 + 类型 + 采集方式 + 告警阈值 + 告警级别。缺失后果：故障无法发现，性能退化无法感知。

| 指标名 | 类型 | 采集方式 | 告警阈值 | 告警级别 |
|--------|------|---------|---------|---------|
| {metric_name} | {Counter/Gauge/Histogram} | {自动埋点/手动上报} | {阈值} | {P1/P2/P3} |

### 6.2 退化矩阵

> 与§6不同：§6=单个操作/调用的异常，本节=组件/子系统不可用时的降级。缺失后果：部分失败时系统行为不可预测。

| 组件 | 失败后可用功能 | 不可用功能 | 降级策略 | 恢复条件 |
|------|-------------|-----------|---------|---------|
| {ComponentA} | {只读/缓存数据} | {写入/实时数据} | {熔断+缓存回退} | {组件恢复健康} |

---

## §8 安全考量

> 本节回答"有哪些安全威胁、怎么缓解"。≤200 字 + 1 张威胁缓解表。

| # | 威胁 | 影响 | 缓解措施 | 验证方式 |
|---|------|------|---------|---------|
| 1 | {威胁描述} | {高/中/低} | {缓解方案} | {怎么验证缓解有效} |

---

## §9 测试策略

> 本节回答"怎么验证模块正确工作"。≤200 字 + 1 张测试覆盖表。

| # | 测试类型 | 覆盖范围 | 关键测试用例 | 通过标准 |
|---|---------|---------|------------|---------|
| 1 | 单元测试 | {哪些函数/类} | {核心用例} | {覆盖率/通过率} |
| 2 | 集成测试 | {哪些模块交互} | {核心用例} | {端到端通过} |

---

## §10 依赖关系

> **v2.0.0 变更**：文件级依赖委托 depgraph.edges 表（真源），蓝图不再存储。
> §10.1 仅声明模块级依赖（模块间关系），文件级依赖通过 depgraph 查询。
> 查询命令：`python scripts/governance/extract_depgraph.py --modules {module_id}`

### 10.1 依赖声明

> 模块级依赖声明。文件级依赖由 depgraph.edges 自动维护，禁止在此重复声明。

| 依赖模块 | 依赖类型 | 依赖内容 | 版本要求 | 蓝图路径 |
|---------|---------|---------|---------|---------|
| {模块 ID} | {必须/可选} | {依赖什么} | {版本} | `docs/03_modules/{layer}/{module}/blueprint.md` |

### 10.2 依赖图对齐声明

> 蓝图 §10.1 声明的依赖 MUST 与全局依赖图一致。不一致 = 漂移。
> 全局依赖图 SSoT：PostgreSQL depgraph（nodes/edges 表）——通过 `python scripts/governance/extract_depgraph.py --modules {module_id}` 查询
> 人工可读视图：[dependency_path_panorama.md](file:///d:/ZephyrAlpha/docs/02_enterprise_architecture/04_architecture_principles_decisions/dependency_path_panorama.md)

| # | 对齐项 | 对齐方式 | 对齐状态 | 验证命令 |
|---|--------|---------|:-------:|---------|
| 1 | §10.1 依赖声明 ↔ PostgreSQL depgraph.nodes | 蓝图声明的每个依赖在 depgraph.nodes 中有对应条目 | {已对齐/未对齐} | `python scripts/governance/extract_depgraph.py --modules {module_id}` |
| 2 | §11 产出物路径 ↔ 依赖图 §19 path_mappings | 路径一致 | {已对齐/未对齐} | 同上 |
| 3 | §0 代码文件清单 ↔ 依赖图节点 code_path | 节点存在 | {已对齐/未对齐} | `python scripts/governance/d5_architecture/validators/validate_dependency_graph_template.py` |

### 10.3 内部依赖图（条件可选）

> 全局依赖图只覆盖模块级，不覆盖脚本级。本节补充蓝图内部脚本/模块间的执行顺序和数据流依赖。
> **为什么需要**：项目有 388 个治理脚本，脚本间有复杂的数据流和执行顺序依赖。
> 没有脚本级依赖图 → AI 不知道改一个文件会影响哪些脚本 → 已导致 D-GAP-21 / CT-IMPACT-001 / GAP-002 三个缺口。
> **格式**：表格（非 Mermaid 图）——AI 解析表格比解析图更准确。
> 无内部依赖填写"无内部依赖"。

#### 执行顺序依赖

> 脚本 A 必须在脚本 B 之前执行。

| 上游脚本 | 下游脚本 | 依赖内容 | 验证方式 |
|---------|---------|---------|---------|
| {script_a.py} | {script_b.py} | {A 的产出物是 B 的前置条件} | {检查 A 的产出物是否存在} |

#### 数据流依赖

> 模块/脚本 A 的输出是模块/脚本 B 的输入。

| 生产者 | 消费者 | 数据类型 | 传输方式 |
|--------|--------|---------|---------|
| {module_a.py} | {module_b.py} | {预测结果/配置/状态} | {函数调用/事件总线/共享数据库/YAML文件} |

### 10.4 自动化规格（条件可选）

> 每个蓝图 MUST 声明自动化的三要素。纯策略/标准文档可填"不适用"。

| # | 自动化项 | 是否需要 | 理由 | 实现方式 | 现有工具 | 缺口 | 触发方式 | 触发条件 |
|---|---------|:-------:|------|---------|---------|------|---------|---------|
| 1 | 依赖图自动生成 | {是/否/不适用} | {理由} | {AST解析+manifest} | {tool} | {缺口} | {CI/pre-commit} | {文件变更时} |
| 2 | 依赖对齐自动验证 | {是/否/不适用} | {理由} | {CI门禁} | {tool} | {缺口} | {CI门禁} | {PR提交时} |
| 3 | 临时时态内容自动清理 | {是/否/不适用} | {理由} | {压缩工作流脚本} | {tool} | {缺口} | {手动} | {压缩工作流执行时} |
| 4 | 施工步骤完成度自动检测 | {是/否/不适用} | {理由} | {pytest+mypy+ruff} | {tool} | {缺口} | {CI pipeline} | {代码提交时} |

### 10.5 概念重叠声明

> 职责级唯一性在 §2.1 声明，本节声明概念级重叠。防重复实现：不同模块可能实现相同概念（如 drift 检测、budget 检查）。AI 不知道重叠 → 重复造轮子。
> 本节声明本模块与其他模块的概念重叠及委托关系。
> 无重叠填写"无概念重叠"。

| # | 重叠概念 | 重叠维度 | 对方模块 | 委托关系 | 处置状态 |
|---|---------|---------|---------|---------|---------|
| 1 | {概念名，如"drift检测"} | {检测逻辑/数据模型/配置} | {MOD-XXX} | {本模块委托对方 / 对方委托本模块 / 共存（需协调）} | {已处置/待协调} |

> 委托关系=共存时，MUST 在 §12.1 域契约锚点中声明协调机制。

### 10.6 依赖链风险评级

> 防级联故障：依赖链深度≥3 时，上游故障会级联传播。AI 不知道链深度 → 无法评估修改风险。
> 无外部依赖填写"无外部依赖风险"。

| # | 依赖链 | 链深度 | 风险等级 | 熔断机制 | 处置状态 |
|---|--------|:-----:|---------|---------|---------|
| 1 | {本模块→MOD-A→MOD-B→MOD-C} | {N} | {L1(深度≤2)/L2(深度3)/L3(深度≥4)} | {有/无} | {已有熔断/需添加/不适用} |

> 链深度≥3（L2+）时 MUST 在 §6.2 退化矩阵中声明退化策略。链深度≥4（L3）时 MUST 有熔断机制。

---

## §11 产出物存放目录

> 代码文件 MUST 标注十五字段头部（[BLUEPRINT]/[MODULE]/[DOMAIN]/[DEPENDENCIES]/[CONSUMERS]/[STARTUP]/[MATURITY]/[INVARIANTS]/[MODIFY-GUARD]/[STABILITY]/[SAFETY]/[AI_AUTONOMY]/[ERROR_CONTRACT]/[TESTS]/[TTL]；真源→trae_047 field_specs.a_full.required）。
> §11 产出物路径 MUST 与依赖图 §19 path_mappings 一致。验证：`python scripts/governance/d5_architecture/validators/validate_path_alignment.py --blueprint {module_id}`

| 产出物类型 | 存放完整路径（相对优先） | 职责 | consumer_min | 注册位置 |
|----------|---------------|------|:-----------:|---------|
| 蓝图文件 | `docs/03_modules/{layer}/{module-name}/blueprint.md` | 本文件（含设计和施工指引） | ≥0 | blueprint_registry.yaml |
| 业务代码 | `src/zephyr/{pkg}/` | Python 源码 | ≥1 | `__init__.py` __all__ |
| 测试代码 | `tests/{功能域}/` | 测试用例（按功能域归类，非测试类型维度；真源见 AGENTS.md §7） | ≥0 | pytest 自动发现 |

---

## §12 集成目标

> ⚠️ 防孤岛：本模块**必须**集成到哪个现有系统？

| 集成目标系统 | 集成方式 | 集成点 | 验证方法 |
|------------|---------|--------|---------|
| {系统名} | {新增接口/修改现有接口/事件订阅/配置注入} | {具体哪个类/函数/配置} | {怎么验证集成成功} |

### 12.1 域契约锚点（条件可选）

> ⚠️ **条件可选**：仅当本模块有域治理集成契约时填写。
> 判定条件：模块 belongs_to 或 references 中包含 DOM-GOV-XXX → MUST 填写此节。

| 域契约ID | 域 | 契约内容 | 对方模块 | 同步更新规则 |
|---------|-----|---------|---------|------------|
| {DOM-GOV-XXX} | {治理域} | {契约内容} | {MOD-XXX} | {修改此契约必须同步更新对方蓝图} |

---

## §13 需要更新的相关内容

> ⚠️ 防遗漏：创建本模块时，以下现有文件**必须**同步更新。

| # | 需更新的文件 | 完整路径（相对优先） | 更新内容 | 更新原因 |
|---|------------|------------|---------|---------|
| 1 | 模块 ID 注册表 | `architecture_model/module_id_registry.yaml` | {新增/修改什么} | {为什么} |
| 2 | 蓝图注册表 | `docs/03_modules/blueprint_registry.yaml` | {新增/修改什么} | {为什么} |
| 3 | 治理资产清单 | `docs/01_policies_and_standards/_registry/catalogs/document-metadata-index-registry.yaml` | {新增/修改什么} | {为什么} |
| 4 | 依赖图 | PostgreSQL depgraph（`extract_depgraph.py --modules {module_id}`）+ [dependency_path_panorama.md](file:///d:/ZephyrAlpha/docs/02_enterprise_architecture/04_architecture_principles_decisions/dependency_path_panorama.md) | {新增/修改什么} | {为什么} |

---

## §14 已知风险与缓解

> 本节回答"有什么风险、怎么应对"。同时承接原 §15 后果中的**负面后果**——设计决策带来的已知代价。
> 正面后果与 §1 目标重复，不在此记录。

| # | 风险/负面后果 | 概率 | 影响 | 缓解策略 | 类型 |
|---|-------------|------|------|---------|------|
| 1 | {风险描述或负面后果} | {高/中/低} | {高/中/低} | {怎么应对} | {风险/负面后果} |

---

## §16 施工指引

> 蓝图 §16 只写设计级施工指引（做什么、为什么）。具体施工细节（改哪个文件哪行改成什么）写在任务卡 description 里，详见 GOV-TASK-001 §6。任务卡永久保留——COMPLETED/CANCELLED 的卡是审计链唯一真源，禁止删除。

### ⚠️ AI 施工前检查清单

> ⚠️ AI 施工者**必须**在开始施工前逐项确认。任何一项为 ❌ 都**不得**开始施工。

| # | 检查项 | 确认方式 | 状态 |
|---|--------|---------|:----:|
| 1 | 已读取本蓝图全部内容（概述 + §1-§10 架构 + §0 对齐 + §16 施工指引） | 逐节确认 | ☐ |
| 2 | 已读取必备链接中所有真源文件 | 逐个打开确认 | ☐ |
| 3 | PS-STD-001 编号规则（§5）已理解 | 能回答"GOV-SEC-001是什么" | ☐ |
| 4 | GOV-DOC-002 防幻觉路径映射（§5.1.2）已理解 | 能回答"某类文件该放哪" | ☐ |
| 5 | MTH-013 路径合规创建原则已理解 | 能回答"新文件创建前三步验证流程" | ☐ |
| 6 | 每个施工步骤都对应明确的蓝图接口契约（§4） | 逐步骤追溯 | ☐ |
| 7 | §0 代码对齐验证已填写且与实际代码一致 | 逐项核对 | ☐ |

### 16.1 施工策略

| 项目 | 内容 |
|------|------|
| 施工阶段数 | {N} 个 Phase（或单 Phase 一次性完成） |
| 施工模式 | {新建 / 扩展 / 重构迁移} |
| 核心风险 | {施工过程中的最大风险} |
| 目标 generation | {N} — 本次施工将蓝图从 generation {N-1} 升级到 generation {N} |

### 16.2 前置条件

| # | 依赖项 | 依赖类型 | 当前状态 | 是否满足 |
|---|--------|---------|:---:|:---:|
| 1 | {依赖模块/文件已就绪} | {hard/soft} | ☐ | ☐ |
| 2 | {前置蓝图已 approved} | hard | ☐ | ☐ |

### 16.3 实施步骤

> ⚠️ 每个步骤严格按"读→做→产→检"四步执行。
> 每个步骤**必须**包含：产出物完整路径（相对优先）、验收标准、验证命令、G7 完整度门禁检查项。
>
> [时态:临时] 通过验证后可删除（pytest+mypy+ruff exit 0），只保留"步骤N:已完成"。

#### 步骤 1：{步骤名称}

| 项目 | 内容 |
|------|------|
| 对应蓝图契约 | §4.{N} |
| 产出位置 | `src/zephyr/...` 或 `docs/...` |
| 验收标准 | {客观、可检查的标准} |
| 验证命令 | `{可执行的验证命令，如 python -m pytest tests/xxx.py -k test_xxx}` |
| G7 检查项 | {上游文件是否全部列出？下游产出物路径是否全部精确？回滚方案是否可执行？} |
| AI 自治范围 | {immutable_core/human_gated/ai_modifiable}——本步骤中哪些操作 AI 可自主执行，哪些需人类审批 |
| 检查点 | {本步骤完成后的中间验证点——确认产出物存在且非空后才进入下一步} |

**创建文件清单**：

| module_id | 文件名 | doc_type | 完整路径（相对优先） |
|-----------|--------|----------|------------|
| {MOD-XXX-NNN} | {file-name.py} | {code} | `src/zephyr/.../file-name.py` |

**内容编写指引**：

| 文件 | 核心内容 | 必须包含 |
|------|---------|---------|
| {file-name.py} | {这个文件在讲什么} | {至少3条可验证的实现要点} |

{更多步骤复制步骤1表格格式}

### 16.4 回滚方案

> ⚠️ 每个步骤如果出问题，**必须**有明确的回滚操作。

| 步骤 | 如果出问题 | 回滚操作 |
|------|----------|---------|
| 1 | {什么问题} | {具体回滚步骤} |

### 16.5 施工完成与生产就绪标准

> 代码存在 ≠ 生产就绪。"完成"=代码存在，"就绪"=可安全上线。

| # | 检查项 | 通过标准 | 类型 | 状态 |
|---|--------|---------|:----:|:----:|
| 1 | {产出物名} 存在 | `ls docs/...` 或 `ls src/zephyr/...` exit 0 | 完成 | ☐ |
| 2 | {产出物名} 非空 | `cat` 有内容 | 完成 | ☐ |
| 3 | SLO 已定义且可测量 | §5.4 每项 SLI 有测量方式 | 就绪 | ☐ |
| 4 | 监控指标已埋点 | §6.1 每项指标有采集实现 | 就绪 | ☐ |
| 5 | 告警已配置 | §6.1 每项阈值有告警规则 | 就绪 | ☐ |
| 6 | 退化策略已实现 | §6.2 每个组件有降级逻辑 | 就绪 | ☐ |
| 7 | 回滚方案已验证 | §16.4 回滚操作可执行 | 就绪 | ☐ |
| 8 | 文档已更新 | §13 需要更新的文件全部更新 | 就绪 | ☐ |
| 9 | 集成测试已通过 | §12 每个集成点有测试 | 就绪 | ☐ |

### 16.6 施工状态

> 施工者在施工结束后填写此表。

| 字段 | 值 | 填写者 |
|------|-----|-------|
| construction_status | {not_started / in_progress / completed} | 施工者 |
| verification_status | {unverified / passed / failed} | 审计者 |
| code_alignment_verified | {yes / no} | 审计者 |

### 16.7 参考实现规格

> 判定：删掉后AI会编造→保留；可从代码读取→删除（铁律#15）。[时态:施工参考]
> ⚠️ §4.1执行流程是方法级流程，本节是算法/协议/SQL级规格——两者互补，不重复。
> 简单模块（CRUD/配置）可填"本模块无复杂实现规格"。

| # | 规格名称 | 类型 | 规格内容 | 对应代码 |
|---|---------|------|---------|---------|
| 1 | {规格名} | {SQL/算法/协议} | {精确规格——代码块或伪代码} | {src/zephyr/{pkg}/{file}.py} |

### 16.8 施工参考卡

| # | 类型 | 名称 | 用途/说明 | 参数/字段 | 输出/约束 |
|---|:----:|------|----------|----------|----------|
| 1 | 命令 | `python -m zephyr.{pkg}.{cmd}` | {用途} | `{--arg}: {说明}` | {输出格式} |
| 2 | 配置 | `{config}.yaml` → `{field}` | {说明} | {类型/必填/默认值} | {约束条件} |

### 16.10 故障与操作手册

> 与§6不同：§6=运行时异常场景，本节=施工时故障+运行时操作。[时态:施工参考]

| # | 阶段 | 场景 | 触发条件 | 诊断/操作 | 恢复/产出 | 验证/回退 |
|---|:----:|------|---------|----------|----------|----------|
| 1 | 施工 | {施工故障} | {什么情况} | {怎么确认} | {恢复步骤} | {验证方式} |
| 2 | 运行 | {运行故障} | {什么情况} | {怎么确认} | {恢复步骤} | {验证方式} |

### 16.12 并发操作模型

> 多 AI Session 同时操作时的冲突策略。

| 冲突场景 | 检测方式 | 解决策略 | 合并规则 |
|---------|---------|---------|---------|
| {同文件写入} | {SHA256 校验} | {后写者重试} | {字段级合并} |
| {同资源竞争} | {锁检测} | {排队等待} | {FIFO} |

> 如无并发场景，填写"本模块无并发操作"。

---

## §17 容量升级附录

> ⚠️ generation≥2 的蓝图**必须**填写此附录。
> 容量升级方案是蓝图正文的一部分，不是独立文档。
> 格式要求：增量式——只写新增/变更部分，不重复基线内容。

### §17.1 容量基线

> ⚠️ 所有 generation≥1 的蓝图 MUST 填写容量基线。

| 资源 | 当前基线 | 测量方式 |
|------|---------|---------|
| {资源名} | {当前值} | {怎么测量} |

### §17.2 缺口清单

| 缺口ID | 当前瓶颈 | 升级方案 | 优先级 | 触发阈值 | 目标版本 | 状态 |
|--------|---------|---------|:------:|---------|---------|:----:|
| GAP-001 | {描述} | {方案} | P0/P1/P2 | {阈值} | vX.Y.Z | {待施工/施工中/已完成} |

### §17.3 升级版本矩阵

| 版本 | generation | 升级类型 | 核心变更 | 代码覆盖 |
|------|:---:|---------|---------|:---:|
| v1.0.0 | 1 | 基线 | {初始设计} | {✅/⚠️/❌} |
| v2.0.0 | 2 | 容量升级 | {升级内容} | {✅/⚠️/❌} |

### 升级组件清单

> 列出升级版本新增的组件。与 §0 代码文件清单对应。

| 组件名 | 对应缺口 | 代码文件 | 施工Phase | 状态 |
|--------|---------|---------|----------|:---:|
| {ComponentName} | GAP-001 | {file}.py | Phase N | {待施工/已存在} |

---

## §18 决策记录

> 记录蓝图中的关键设计决策。
> 与变更记录不同——变更记录记"改了什么"，决策记录记"为什么这样设计"。
> **本节同时覆盖原 §7 备选方案**——§18 的"选项"列已包含备选方案信息，无需独立章节。
> **本节同时覆盖原 §15 后果**——负面后果合并到 §14 风险，正面后果与 §1 目标重复无需独立记录。
> **时态属性**：决策记录属于**永久时态**——AI 修改设计时必读。

| # | 决策ID | 决策 | 选项 | 选中 | 依据 | 日期 |
|---|--------|------|------|------|------|------|
| 1 | D-{MOD}-01 | {决策描述} | {A/B/C} | {A} | {为什么选A——可引用外部标准/事故/实验数据} | {YYYY-MM-DD} |

> ⚠️ 决策ID + 依据列必填。无决策ID = 无法交叉引用。无依据 = AI 不知道决策的权威来源。

---

## 术语表

| 术语 | 精确定义 | 易混淆术语 | 区别 |
|------|---------|-----------|------|
| {术语} | {一句话精确定义} | {容易混淆的术语} | {与易混淆术语的关键区别} |

---

## 已知问题与盲点登记

> 与§14不同：§14=设计风险，本节=已知具体问题。[时态:防漂移防线]

| # | 问题 | 严重性 | 根因 | 解决方案 | 约束编号 | 状态 |
|---|------|:------:|------|---------|---------|:----:|
| 1 | {问题描述} | {极高/高/中/低} | {为什么存在} | {具体解决步骤} | {§5.1 #N} | {待解决/已解决} |

---

## 自检与闭合清单

> "设计"阶段项=闭合验证：确保蓝图各章节交叉引用完整，无孤岛。

| # | 阶段 | 检查项 | 确认方式 | 状态 |
|---|:----:|--------|---------|:----:|
| 1 | 设计 | §3 每个组件在 §4 有对应接口 | 逐组件核对 | ☐ |
| 2 | 设计 | §4 每个接口在 §16 有对应施工步骤 | 逐接口核对 | ☐ |
| 3 | 设计 | §5 每个约束在 §9 有对应测试 | 逐约束核对 | ☐ |
| 4 | 设计 | §0.1 每个代码文件在 §11 有对应产出物路径 | 逐文件核对 | ☐ |
| 5 | 设计 | §10 每个依赖在 PostgreSQL depgraph.nodes 有对应条目 | `python scripts/governance/extract_depgraph.py --modules {module_id}` | ☐ |
| 5.1 | 设计 | §0.1 每个代码文件已同步到 path-ownership-map.yaml 且无路径冲突 | `python scripts/governance/generators/generate_path_ownership_map.py --write && --conflicts` | ☐ |
| 6 | 前 | 已读取蓝图全文（概述→§0→§1-§18→术语表→自检清单） | 逐节确认 | ☐ |
| 7 | 前 | 术语表中每个术语含义已理解 | 能回答"X和Y的区别是什么" | ☐ |
| 8 | 前 | 成熟度声明中 volatile/evolving 的部分已标记 | 知道哪些设计可改哪些不可改 | ☐ |
| 9 | 前 | 已知问题登记中未解决的问题已知晓 | 知道哪些坑不能踩 | ☐ |
| 10 | 中 | 每步施工后执行验证命令 | exit 0 才进下一步 | ☐ |
| 11 | 中 | 新代码文件头部十五字段完整 | 逐文件核对 | ☐ |
| 12 | 中 | 修改接口契约后检查 §18 决策记录 | 决策ID+依据已更新 | ☐ |
| 13 | 后 | §0 代码对齐验证已更新 | construction_progress 与实际一致 | ☐ |
| 14 | 后 | 临时时态内容已清理 | 迁移方案已执行→删除 | ☐ |

---

## 成熟度声明

> frozen不可改，volatile谨慎改。[时态:防漂移防线]

| 设计维度 | 成熟度 | 信心 | 升级标准 | 说明 |
|---------|:------:|:---:|---------|------|
| 核心架构 | {frozen/stable/evolving/volatile} | {高/中/低} | {从当前等级升级到更高等级的条件} | {为什么是这个等级} |
| 接口契约 | {frozen/stable/evolving/volatile} | {高/中/低} | {从当前等级升级到更高等级的条件} | {为什么是这个等级} |
| 数据模型 | {frozen/stable/evolving/volatile} | {高/中/低} | {从当前等级升级到更高等级的条件} | {为什么是这个等级} |
| 施工步骤 | {frozen/stable/evolving/volatile} | {高/中/低} | {从当前等级升级到更高等级的条件} | {为什么是这个等级} |

---

## 版本演进路线图

> 与§17不同：§17=容量升级，本节=功能演进。[时态:防漂移防线]

| 版本 | 核心变更 | 前置版本 | 施工状态 |
|------|---------|---------|:-------:|
| v0.1.0 | {初始设计} | — | {已完成/施工中/待施工} |
| v0.2.0 | {功能扩展} | v0.1.0 | {待施工} |

---

## ⚠️ Vibe Coding 蓝图编写铁律

> **写作原则**（优先级从高到低）：
> 1. **防幻觉/防漂移 > 一切**——删掉会导致幻觉或漂移的内容，即使它是"理由"也必须保留
> 2. **AI只需规则不需理由**——不会导致幻觉的理由可删
> 3. **区分信号保留**——"与§X不同"是规则边界，不是理由

> 以下铁律 AI 编写蓝图时**必须**逐条确认已遵守。
> **时态属性**：施工声明——永久保留，不可改为链接引用。
> **章节完整性**：REQUIRED_SECTIONS 中所有章节不可省略，合规脚本 `check_blueprint_compliance.py` 强制验证。各章节头部标注了缺失后果。

| # | 铁律 | 为什么 | 违反后果 |
|---|------|--------|---------|
| 1 | **所有路径必须完整可定位**——优先使用项目根相对路径+正斜杠（如 `docs/03_modules/...`）；绝对路径仅限 Python 代码外场景且必须用正斜杠（如 `D:/ZephyrAlpha/...`） | Python 字符串中 `\Z`/`\0` 等是非法转义序列（3.14+ SyntaxError）；正斜杠跨平台兼容 | 文件创建到错误位置 / Python SyntaxError |
| 2 | **必备链接不可省略**——即使与前序文档重复也必须完整列出 | AI 每次新 session 是零记忆，不记得前序文档写了什么 | AI 跳过不读，施工时缺少关键信息 |
| 3 | **蓝图必须是最终设计结果**——不记录决策过程、不保存未选方案 | — | 蓝图过厚，关键信息被噪音淹没 |
| 4 | **产出物路径必须与 GOV-DOC-002 一致** | AI 不知道项目目录规范，会自行创建路径 | 路径幻觉——文件放错位置 |
| 5 | **涉及文件范围必须明确列出** | AI 不知道边界在哪，会越界修改 | 范围漂移——改了不该改的文件 |
| 6 | **"待定"/"建议"/"按需"等模糊词禁止使用** | AI 无法处理模糊指令，需要明确的二元判断 | 执行漂移——AI 自行决定，可能选错 |
| 7 | **蓝图必须自包含**——关键信息不能只写"详见XX" | AI 可能不读引用的文件 | 信息缺失——AI 缺少关键上下文 |
| 8 | **删除文件必须遵守安全删除协议**——禁止直接删除任何文件 | 没有git备份，删除不可逆；AI可能误判文件"没用了" | 永久丢失——无法恢复 |
| 9 | **construction_progress 必须与代码实际状态一致** | — | 重复造轮子或跳过施工 |
| 10 | **actual_disk_path 必须与 §11 产出物路径一致** | — | 搜索失败、导入错误 |
| 11 | **已实现代码不在蓝图中重复**——§0.1 标记`已实现`的模块，蓝图只保留接口签名（§4），不复制实现代码 | 代码文件是 SSoT，蓝图复制代码=双源漂移 | AI 改蓝图忘改代码，或改代码忘改蓝图 |
| 12 | **临时时态内容执行完毕后从蓝图删除**——迁移方案、升级执行计划等临时时态内容，一旦执行完毕即成为历史，从蓝图删除。蓝图只保留永久时态内容（架构/接口/约束/当前状态） | 蓝图是当前设计文档，不是历史记录 | 蓝图膨胀，关键信息被历史噪音淹没 |
| 13 | **蓝图内容拆分判定**——职责不同→拆分独立蓝图；职责相同→原地升级。判定标准见"蓝图拆分判定标准" | — | AI 不知道该读哪个蓝图，跨模块影响无法追踪 |
| 14 | **术语表不可省略**——每个蓝图 MUST 包含术语表，定义本蓝图的关键术语和易混淆术语的区别 | AI 对术语产生理解漂移 | 设计和代码与蓝图意图不一致 |
| 15 | **参考实现规格 vs 已实现代码重复**——接口契约无法表达的逻辑规格（SQL并发控制/算法步骤/协议序列）MUST 保留在 §16.7；Pydantic模型字段定义等接口代码不重复（铁律#11） | 删掉规格→AI编造实现逻辑；保留接口代码→双源漂移 | 关键逻辑实现错误 / 蓝图与代码不一致 |
| 16 | **对标验证表格 vs 对标散文**——结构化的对标表格（如行业对标矩阵）是验证基准，MUST 保留；长篇对标散文段落（如"对标K8s ResourceQuota的原理是..."）MUST 删除 | 表格=验证基准，散文=L2背景知识 | 丢表格→无法验证设计完整性；留散文→噪音淹没关键信息 |

---

## 蓝图拆分判定标准

> 铁律 #15 的操作定义——当蓝图内容超过 ~800 行或包含多个独立职责域时，MUST 执行拆分判定。

### 判定流程

| 判定条件 | 结果 | 操作 |
|---------|------|------|
| 服务对象相同 + 变更频率同步 + 依赖关系重叠 | 原地升级 | 在 §17 容量升级附录中增量记录 |
| 有独立 module_id 前缀 | 拆分 | 创建子蓝图，belongs_to=本蓝图 |
| 有独立 Phase 路线图和交付节奏 | 拆分 | 同上 |
| 有独立依赖关系图（与主体 depends_on 交集<50%） | 拆分 | 同上 |
| 内容超100行且与主体无直接数据流 | 拆分 | 同上 |
| 拆分后 | MUST验证 | 子蓝图有独立frontmatter+概述+§0~§18；本蓝图§10新增引用；blueprint_registry.yaml同步更新 |

---

## ⚠️ 安全删除协议

> **核心原则**：删除文件是不可逆操作。项目当前没有 git 备份，一旦删除就永远丢失。
> 蓝图中如果涉及文件废弃/迁移/删除，**必须**在此写出安全删除方案。
>
> **时态属性**：本节属于**施工声明**——AI 施工涉及删除时必读。永久保留在蓝图中。

### 蓝图中的删除决策清单

> ⚠️ 如果本蓝图会导致文件被废弃/迁移/删除，**必须**在此列出。

| # | 待删除/废弃文件 | 完整路径（相对优先） | 删除类型 | 接收文件 | 安全删除方案 |
|---|---------------|------------|---------|---------|------------|
| 1 | {文件名} | `docs/...` 或 `src/zephyr/...` | {迁移型/覆盖型/废弃型} | `docs/...` 或 `src/zephyr/...` | {迁移→交叉验证→标记deprecated→Phase4物理删除} |

### 删除铁律

| # | 铁律 | 原因 |
|---|------|------|
| 1 | 禁止蓝图阶段物理删除任何文件 | 蓝图只做决策，不做执行 |
| 2 | 迁移型删除必须逐条迁移、逐条验证 | 批量迁移容易遗漏 |
| 3 | 物理删除只能在 stable 搬入阶段执行 | 给足缓冲期，deprecated 至少保持1个Phase |
| 4 | 物理删除必须人类确认 | AI 不得自行决定删除文件 |
| 5 | "宁可慢，不可漏" | 没有git备份，删了就没了 |

---

## 必备链接

> ⚠️ AI 蓝图编写者**必须**先读取以下所有文件，确认内容已理解后再开始编写。
> **禁止省略**——即使某些文件在多个蓝图中重复出现，也必须在此完整列出。
> **路径格式**——优先使用项目根相对路径+正斜杠（如 `docs/01_policies_and_standards/...`）；绝对路径仅限 Python 代码外场景且必须用正斜杠（如 `D:/ZephyrAlpha/...`）。禁止 Windows 反斜杠路径（`\Z` 等 Python 非法转义序列）。
>
> **时态属性**：本节属于**施工声明**——AI 进入蓝图时必读。不可改为链接引用——
> AI 不会主动跳转链接读取，删掉 = 失去上下文防线。永久保留在蓝图中。

| # | 文件 | module_id | 完整路径（相对优先） | 编写时用途 |
|---|------|-----------|------------|----------|
| 1 | 元数据注册表 | PS-STD-001 | `docs/01_policies_and_standards/rules/trae_043_meta_rule_metadata.yaml` | 编号规则、doc_type词表 |
| 2 | 目录结构标准 | GOV-DOC-002 | `docs/01_policies_and_standards/rules/trae_028_doc_structure_naming.yaml doc_002` | 路径映射 |
| 3 | 模块ID注册表 | — | `architecture_model/module_id_registry.yaml` | 编号注册 |
| 4 | AI自治权限注册表 | GOV-AI-001 | `docs/01_policies_and_standards/_registry/catalogs/ai_autonomy_authority_registry.yaml` | AI操作权限 |
| 5 | {按项目实际补充} | {—} | `docs/...` | {用途} |

---

## 项目中已有类似功能

> ⚠️ 防重复检查：编写蓝图前**必须**确认项目中是否已有类似功能。如果已有，必须说明为什么不能复用/扩展。

| # | 已有模块/文件 | 完整路径（相对优先） | 功能重叠点 | 为什么不能复用 |
|---|-------------|------------|----------|-------------|
| 1 | {模块名} | `docs/...` 或 `src/zephyr/...` | {重叠什么} | {为什么不能直接用} |

> 如果无类似功能，填写"无"。

---

## 涉及的文件范围

> ⚠️ 防范围漂移：本蓝图涉及的文件范围。AI 编写和施工时**不得**超出此范围。
> **必须列出每个文件的完整路径**（项目根相对路径+正斜杠），确保零歧义定位。

| # | 文件/目录 | 完整路径（相对优先） | 关系 | 变更类型 |
|---|---------|------------|------|---------|
| 1 | {文件/目录名} | `src/zephyr/...` 或 `docs/...` | {读取/修改/新建/迁移/废弃} | {具体变更内容} |

---

## 治理信息

### SSoT 声明

> 编写时 MUST Grep 搜索项目中是否有其他文件定义了相同内容。发现重复→填入"漂移风险"列。

| 内容 | 真源 | 非真源 | 漂移风险 |
|------|------|--------|---------|
| {本蓝图的核心架构设计} | **本文档 §1-§10** | {已被取代的旧蓝图/旧文档} | {无/⚠️{文件}重复定义——以本蓝图为准} |
| {本模块的施工步骤} | **本文档 §16** | {已废弃的旧施工图} | {无} |
| {本模块的接口契约} | **本文档 §4** | — | {无} |
| {代码文件清单与对齐状态} | **本文档 §0** | blueprint_registry.yaml（派生） | {无} |
| {容量升级方案} | **本文档 §17** | 独立升级文档（已废弃） | {无} |
| {数据模型定义} | **{ssot_file}.py** | 其他文件中的同名模型=兼容层(待统一) | {无/⚠️{文件}定义同名模型} |

**任何与本蓝图冲突的定义，以本蓝图为准。**

### 消费者注册表

| Tier | 消费者 | 依赖内容 |
|:----:|--------|---------|
| Tier 1 | {下游模块蓝图路径} | §4 接口契约、§10 依赖关系 |
| Tier 2 | {集成目标系统文件路径} | §12 集成点 |
| Tier 3 | {代码文件路径} | §4 数据模型、§11 产出物路径 |

### 变更审批与同步规则

| 变更类型 | 审批要求 | Tier 1 同步（下游蓝图） | Tier 2 同步（集成系统） |
|---------|---------|---------------------|---------------------|
| 接口契约新增/修改（§4） | 需 Owner 审批 + 通知所有消费者 | 下游检查接口兼容性 | 检查集成点兼容性 |
| 模块边界修改（§2） | 需 Owner 审批 | 下游更新依赖声明 | 更新集成路由 |
| construction_progress 变更 | 需 §0 对齐验证通过 | 下游更新依赖状态 | 更新集成测试 |
| 施工步骤微调（命令、路径修正） | AI 可自主修改 | 下游更新产出物引用 | 更新配置文件 |
| 非关键补充（风险缓解、后果描述） | AI 可自主修改 | — | — |
| 容量升级方案新增（§17） | 需 Owner 审批 | 下游评估影响 | 更新容量预算 |

---

## 变更记录

> 变更历史通过 Git log 追踪。

---

## 蓝图特有章节

> **硬规则**：模板章节=合规下限，可超出。超出下限的内容 **MUST** 写在本章节内。
> 写在本章节外 = 压缩工作流视为冗余 → **可删**。写在本章节内 + 标注三要素 = **不可删**（理由过时除外）。

> **内容质量要求**：本章节内容 MUST 已压缩至最简——表格>列表>段落，零废话，零对标，零解释。
> 未压缩的定制内容 = 压缩工作流下次会来砍。

### 蓝图特有：{章节名称}

| 要素 | 内容 |
|------|------|
| 来源 | {规格化/回填/Owner要求} |
| 仅本蓝图 | {为什么其他蓝图不需要} |
| 不可砍 | {砍掉后果：幻觉/漂移/功能丢失} |

{已压缩的特有内容——表格优先，零废话}

---
