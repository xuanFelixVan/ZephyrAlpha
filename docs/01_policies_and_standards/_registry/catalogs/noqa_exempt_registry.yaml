# [A_config] module_id=CFG-noqa-exempt-registry | layer=config | stability=stable | safety=L | ai_autonomy=human_gated
# --- 治理锚定 ---
# blueprint: MOD-GATE_ENGINE
# module_id: CFG-NOQA-REGISTRY-001
# stability: stable
# safety_level: L
# ai_autonomy: human_gated
# ttl: permanent
# --- 治理锚定结束 ---
# noqa_exempt_registry.yaml — 自定义 noqa 豁免标记登记表（SSoT）
#
# 真源：本 YAML 文件（规则数据，TRAE-062）
# 派生：architecture_health_dashboard.py 检测器动态加载合法标记清单；
#       noqa_validation_gate.py（priority=71）校验 staged 文件的 noqa 标记合规性
# 禁止：检测器内硬编码标记字符串（MUST 从本 registry 动态加载）
#
# 用途：登记项目自定义的 `# noqa: <marker>` 标记，作为豁免机制的唯一真源。
#       新标记 MUST 先在本 registry 登记，否则被 noqa_validation_gate 硬阻断。
#       标记使用 MUST 附理由（格式：`# noqa: <marker>  <METRIC>豁免: <理由>`，双空格分隔，理由>=10字符）。
#
# 维护者：ZephyrAlpha-Owner
# 版本：1.0.0
# 创建日期：2026-07-17
# 关联裁定：#ARCH-NOQA-GOV-001
#
# 标记命名规范：`<metric_id_lower>-<语义>`，如 `m02-manual` / `m10-time-trigger`

markers:
  - marker: "m02-manual"
    metric_id: "M02"
    description: "manual-only 永久脚本豁免——常驻服务特征检测"
    applicable_conditions:
      - "CLI 启动的常驻服务（如交易主入口 python -m zephyr.trading）"
      - "启动后自动运行 reconcile 循环，非 reconciler 无需事件触发"
      - "Windows SCM 服务（windows_service.py）"
      - "CI 兜底监控脚本（vms_cron_monitor.py）"
    reason_required: true
    reason_format: "M02豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  - marker: "m03-duplicate"
    metric_id: "M03"
    description: "重复簇函数豁免——AI 趋同演化非复制粘贴"
    applicable_conditions:
      - "不同模块为相似问题生成相似代码（趋同演化），非复制粘贴"
      - "M05（文件复制对=0）已覆盖文件级复制检测"
      - "接口实现/协议方法等合法重复（per-file 豁免）"
    reason_required: true
    reason_format: "M03豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  - marker: "m07-orphan"
    metric_id: "M07"
    description: "死代码/orphan 模块豁免——动态引用无法被静态扫描发现"
    applicable_conditions:
      - "动态引用（importlib/reflection）无法被静态扫描发现"
      - "entry point 模块（被外部调用而非内部 import）"
    reason_required: true
    reason_format: "M07豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  - marker: "m10-time-trigger"
    metric_id: "M10"
    description: "时间触发残留豁免——注释/枚举值/锁等待等非真实时间触发"
    applicable_conditions:
      - "schema 中 'cron' 枚举值/注释提及 'cron'（非实际 cron 代码）"
      - "锁等待 while True+sleep（非周期触发，是等待锁释放）"
      - "perm_trigger_gate 自身（检测器含模式字符串）"
      - "threading.Timer 超时（非周期触发，是单次超时）"
      - "CI 定期 job 兜底调用（非进程内 daemon，如 rollback_scheduler.schedule_drill）"
      - "time.sleep 长睡眠（如 reconcile 心跳轮询，手动启动后自动运行）"
    reason_required: true
    reason_format: "M10豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  - marker: "gate-vocab"
    metric_id: null  # 非 dashboard 指标，是 GATE-VOCAB 门禁豁免
    description: "GATE-VOCAB 词表硬编码豁免——DDL-as-Code 枚举/schema 合法值"
    applicable_conditions:
      - "DDL-as-Code 的 CHECK 枚举（DDL 本身就是词表定义）"
      - "schema 定义中的合法值集合（YAML schema 的 enum 字段）"
      - "词表加载器自身（vocabulary_loader.py 等需要引用词表名）"
    reason_required: true
    reason_format: "gate-vocab豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  - marker: "MSG-EXPOSURE"
    metric_id: null  # 非 dashboard 指标，是 msg_exposure_gate 行级豁免
    description: "MSG-EXPOSURE gate 行级豁免——raise 语句异常消息含敏感变量名的合规特殊情况"
    applicable_conditions:
      - "raise 语句的异常消息含变量名，但该变量非敏感数据（如状态机状态名/schema 版本号/注入目标标签）"
      - "SQL 迁移调试上下文（DDL/DML 语句非用户数据）"
      - "错误立即被 except 捕获不外泄（如注入引擎测试）"
    reason_required: true
    reason_format: "MSG-EXPOSURE豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"  # 行级豁免（不同于 M 系列的 per-file）
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  - marker: "MSG-STYLE"
    metric_id: null  # 非 dashboard 指标，是 msg_style_gate 行级豁免
    description: "MSG-STYLE gate 行级豁免——raise 语句异常消息含 Unicode 箭头/中文句号的合规特殊情况"
    applicable_conditions:
      - "raise 语句的异常消息含 Unicode 箭头 ->（U+2192）但语义正确（如数学描述）"
      - "raise 语句的异常消息以中文句号。结尾但符合该处语境"
    reason_required: true
    reason_format: "MSG-STYLE豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"  # 行级豁免（不同于 M 系列的 per-file）
    created: "2026-07-17"
    created_by: "ARCH-NOQA-GOV-001"

  # ── Phase 3.6 第1期 AST 门禁检测器自豁免（2026-07-19）──────────────────
  # 检测器源码含检测模式字符串（用于 AST 匹配），非实际违规
  - marker: "m20-snapshot-drift"
    metric_id: "M20"
    description: "SNAPSHOT-DRIFT 检测器自豁免——源码引用 snapshot 路径用于校验，非实际 drift"
    applicable_conditions:
      - "snapshot_drift_gate.py 检测器源码含 _SNAPSHOT_REL_PATH 常量（用于校验目标文件）"
      - "源码字符串非实际快照漂移，是检测逻辑的输入参数"
    reason_required: true
    reason_format: "M20豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-19"
    created_by: "ARCH-GOV-CONVERGENCE-META-Phase-3.6"

  - marker: "m01-vocab-hardcode"
    metric_id: "M01"
    description: "VOCAB-CHAIN 检测器自豁免——源码含 SSoT 路径模式字符串用于 AST 匹配，非实际硬编码"
    applicable_conditions:
      - "vocab_chain_gate.py 检测器源码含 _SSOT_PATH_PATTERNS 正则字符串（用于匹配违规）"
      - "源码字符串非实际硬编码，是检测逻辑的模式定义"
    reason_required: true
    reason_format: "M01豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-19"
    created_by: "ARCH-GOV-CONVERGENCE-META-Phase-3.6"

  - marker: "m02-manual-trigger"
    metric_id: "M02"
    description: "MANUAL-ONLY-PERMANENT 检测器自豁免——源码含检测模式字符串(argparse/input/sys.argv)用于 AST 匹配，非实际 manual 触发"
    applicable_conditions:
      - "manual_only_permanent_gate.py 检测器源码含 _MANUAL_TRIGGER_IDENTIFIERS 集合（用于 AST 匹配）"
      - "源码字符串非实际 manual 触发，是检测逻辑的模式定义"
    reason_required: true
    reason_format: "M02豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-19"
    created_by: "ARCH-GOV-CONVERGENCE-META-Phase-3.6"

  # ── P3-1.2 治本（#ARCH-P3-FOLLOWUP-TODOS-001 裁定 B / C，2026-07-20）──────
  # MANUAL-ONLY-PERMANENT gate 的合法豁免路径——AI/CI 按需调用的 permanent runner
  - marker: "m11-perm-manual-legitimate"
    metric_id: "M02"  # 复用 M02 manual-only 语义维度（同维度不同豁免类型）
    description: "permanent manual 触发脚本合法豁免——AI/CI 按需调用（非 cron/非 daemon/非常驻服务）的回归 runner / 一次性治理脚本"
    applicable_conditions:
      - "permanent 脚本被 AI/CI 按需触发（人工或 CI workflow），非进程内常驻服务"
      - "无事件订阅但属于合法 manual 触发场景（如回归 runner / 一次性数据修复脚本 / 紧急修复脚本）"
      - "不同于 m02-manual-trigger（检测器自豁免）：m11 是业务脚本的合法 manual 触发豁免"
    reason_required: true
    reason_format: "M11豁免: <具体理由>（必须说明为何非常驻服务）"
    reason_min_length: 10
    scope: "per-file"
    created: "2026-07-20"
    created_by: "ARCH-P3-FOLLOWUP-TODOS-001"

  # ── 5.46 时间与时区处理防复发（DATETIME-NOW-FORBIDDEN gate 扩展，2026-07-20）──
  # src/zephyr/ 全量禁止 time.time() 和 datetime.now() 无参数；生成器全量禁 datetime.now()
  - marker: "m46-time"
    metric_id: "M46"  # 5.46 时区处理防复发维度
    description: "时间戳误用豁免——benchmark 基准测试 / 性能计时等合法 time.time() 场景"
    applicable_conditions:
      - "benchmark 基准测试代码（需 wall-clock 测量而非 monotonic）"
      - "性能计时场景（time.time() 是合法选择）"
      - "外部库 API 强制要求 time.time() 返回值（如 cache_key=time.time()）"
    reason_required: true
    reason_format: "M46豁免: <具体理由>（必须说明为何 time.time() 不可替代）"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-20"
    created_by: "ARCH-ANTI-RECURRENCE-P0"

  - marker: "bare-subprocess"
    metric_id: null  # 非 dashboard 指标，是 BARE-SUBPROCESS gate 行级豁免
    description: "BARE-SUBPROCESS gate 行级豁免——裸 subprocess 调用检测的合法特殊情况"
    applicable_conditions:
      - "BARE-SUBPROCESS 检测器自身（源码含检测模式字符串 subprocess.run/Popen 用于 AST 匹配，非实际调用）"
      - "测试文件中模拟/断言裸 subprocess 调用的测试用例（assert call.args 等场景）"
      - "文档字符串/注释中引用 subprocess.run 字面量（非可执行代码）"
      - "例外文件中已有 process_pool.run_subprocess_hidden 包装的 subprocess 调用（兼容性保留）"
    reason_required: true
    reason_format: "bare-subprocess豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"  # 行级豁免（per-line）
    created: "2026-07-20"
    created_by: "ARCH-RUNCOMMAND-WINDOW-FLASH-001"

  - marker: "no-verify-ruling"
    metric_id: null  # 非 dashboard 指标，是 RULING-COMMIT-VERIFIED gate commit-msg 级豁免
    description: "RULING-COMMIT-VERIFIED gate commit-msg 级豁免——文档'已完成'声明 commit hash 真实性硬验证的逃生通道"
    applicable_conditions:
      - "草稿/占位 ruling 文档（'已完成'声明为模板，commit hash 尚未生成）"
      - "迁移历史 ruling 文档时引用了其他仓库的 commit hash（跨仓库迁移场景）"
      - "测试环境无法访问真实 git history（CI 干跑场景）"
    reason_required: true
    reason_format: "[no-verify-ruling:<具体理由>]"
    reason_min_length: 10
    scope: "commit-message"  # commit-msg 级豁免（per-commit）
    created: "2026-07-20"
    created_by: "ARCH-WORKSPACE-DRIFT-SYSTEMIC-001"

  - marker: "blueprint-amodule-cross-check"
    metric_id: null  # 非 dashboard 指标，是 BLUEPRINT-AMODULE-CROSS-CHECK gate 行级豁免
    description: "BLUEPRINT-AMODULE-CROSS-CHECK gate 行级豁免——同模块双拼写的合法迁移场景"
    applicable_conditions:
      - "历史遗留双拼写文件在批量统一迁移期间暂豁免（迁移完成后移除）"
      - "测试文件中模拟双拼写场景的测试用例"
    reason_required: true
    reason_format: "blueprint-amodule-cross-check豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-22"
    created_by: "ARCH-MODULE-ID-DUAL-SPELLING-001"

  - marker: "consumers-accuracy"
    metric_id: null  # 非 dashboard 指标，是 CONSUMERS-ACCURACY gate 文件级豁免
    description: "CONSUMERS-ACCURACY gate 文件级豁免——[CONSUMERS] 字段准确性检测的逃生通道"
    applicable_conditions:
      - "[CONSUMERS] 字段含抽象代号（MOD-XXX/SH-XXX）无法静态验证（gate 已豁免，noqa 用于边界场景）"
      - "[CONSUMERS] 字段含动态生成消费者（如 __init__.py 聚合 re-export，消费者为运行时拼接）"
      - "测试文件中模拟 [CONSUMERS] 字段的测试用例（非真实消费者声明）"
      - "迁移历史文件时 [CONSUMERS] 暂保留旧值（planned 治理，一次性豁免）"
    reason_required: true
    reason_format: "consumers-accuracy豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-file"  # 文件级豁免（per-file）
    created: "2026-07-21"
    created_by: "ARCH-CONSUMERS-ACCURACY-001"

  - marker: "import-integrity"
    metric_id: null  # 非 dashboard 指标，是 GATE-IMPORT-INTEGRITY gate 行级豁免
    description: "GATE-IMPORT-INTEGRITY gate 行级豁免——悬空 import 检测的合法特殊情况"
    applicable_conditions:
      - "模块通过 sys.modules 注册别名（如 governance/__init__.py 注册 zephyr.governance.event_hook 指向子目录模块）"
      - "模块在 try/except 中 import，运行时可能不存在但代码已处理 ImportError"
      - "模块路径在运行时动态拼接，静态分析无法解析"
      - "re-export shim 通过 __init__.py 聚合，目标模块在不同包路径"
    reason_required: true
    reason_format: "import-integrity豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"  # 行级豁免（per-line）
    created: "2026-07-21"
    created_by: "ARCH-CROSS-COMMIT-ATOMICITY-001"

  - marker: "any-abuse"
    metric_id: "M145"  # 5.145 类型注解完整性防复发维度
    description: "GATE-ANY-ABUSE gate 行级豁免——函数签名裸 Any 检测的合法特殊情况"
    applicable_conditions:
      - "动态 Plugin 接口边界（运行时加载的第三方插件，类型不可静态推断）"
      - "Protocol 边界（runtime_checkable Protocol 的异构实现集，单一类型无法覆盖）"
      - "第三方 API 边界（外部库返回 Any，本层无法收敛）"
      - "桥接 API（同方法接受多种异构类型，用 isinstance 分派）"
      - "测试 fixture / mock 注入点（测试框架 API 要求 Any）"
    reason_required: true
    reason_format: "any-abuse豁免: <具体理由>（必须说明为何不可替换为具体类型/Protocol）"
    reason_min_length: 10
    scope: "per-line"  # 行级豁免（per-line）——标记出现在裸 Any 参数/返回值所在行
    created: "2026-07-22"
    created_by: "ARCH-ANY-GOVERNANCE-001"

  # ── warn-only→hard gate 批量升级（ARCH-WARN-TO-HARD-GATE-BATCH，2026-07-23）──
  - marker: "a100-asyncio"
    metric_id: "M23"  # 5.100 异步资源生命周期维度
    description: "ASYNCIO-RUN-IN-CONTEXT gate 行级豁免——asyncio.run/get_event_loop/new_event_loop 调用的合法特殊情况"
    applicable_conditions:
      - "async_utils canonical 实现自身（run_coroutine_sync 内部需调用 asyncio 原语）"
      - "测试文件中模拟 asyncio 调用的测试用例"
      - "文档字符串/注释中引用 asyncio.run 字面量（非可执行代码）"
    reason_required: true
    reason_format: "a100-asyncio豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-23"
    created_by: "ARCH-WARN-TO-HARD-GATE-BATCH"

  - marker: "n114-final"
    metric_id: "M25"  # 5.114 Final/@final 强制维度
    description: "MUTABLE-CONST-WITHOUT-FINAL gate 行级豁免——模块级可变常量缺 Final 标注的合法特殊情况"
    applicable_conditions:
      - "动态生成常量（运行时拼接 key 的 dict，Final 无法静态标注）"
      - "测试文件中模拟可变常量的测试用例"
      - "迁移历史文件暂保留旧式常量（planned 治理，一次性豁免）"
    reason_required: true
    reason_format: "n114-final豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-23"
    created_by: "ARCH-WARN-TO-HARD-GATE-BATCH"

  - marker: "r144-open"
    metric_id: "M27"  # 5.144 资源清理顺序维度
    description: "OPEN-WITHOUT-WITH gate 行级豁免——open() 未在 with 内的合法特殊情况"
    applicable_conditions:
      - "open() 返回值立即传入上下文管理器包装（如 contextlib.closing(open(...))）"
      - "open() 在 try/finally 中显式 close（已有异常隔离模式）"
      - "测试文件中模拟 open 调用的测试用例"
    reason_required: true
    reason_format: "r144-open豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-23"
    created_by: "ARCH-WARN-TO-HARD-GATE-BATCH"

  - marker: "e34-env"
    metric_id: "M30"  # 5.34 环境隔离维度
    description: "ZEPHYR-ENV-DIRECT-ACCESS gate 行级豁免——os.environ[\"ZEPHYR_ENV\"] 直访的合法特殊情况"
    applicable_conditions:
      - "config 层 canonical 读取自身（src/zephyr/shared/foundation/config 目录已目录级豁免）"
      - "测试文件中模拟环境变量直访的测试用例"
      - "迁移历史文件暂保留旧式直访（planned 治理，一次性豁免）"
    reason_required: true
    reason_format: "e34-env豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-23"
    created_by: "ARCH-WARN-TO-HARD-GATE-BATCH"

  - marker: "table-name-registry"
    metric_id: null  # 非 dashboard 指标，是 TABLE-NAME-REGISTRY gate 行级豁免
    description: "TABLE-NAME-REGISTRY gate 行级豁免——动态表名 fallback 的合法特殊情况"
    applicable_conditions:
      - "payload.table 缺失时的 fallback 表名（由 tasks.yaml 设置时此 fallback 不执行）"
      - "xtquant 报表名转小写作为表名（c3_fundamental 域动态报表）"
    reason_required: true
    reason_format: "table-name-registry豁免: <具体理由>"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-07-29"
    created_by: "ARCH-TABLE-NAME-REGISTRY-001"

  # ── 广义 except 合法豁免（钩子/回调非阻断场景，2026-08-01）──────────────────
  - marker: "m12-broad-except-legitimate"
    metric_id: null  # 非 dashboard 指标，是广义 except 合法场景豁免
    description: "广义 except Exception 合法豁免——钩子/回调/post-commit 非阻断场景必须吞异常"
    applicable_conditions:
      - "post-commit 钩子必须非阻断（异常不能中断主流程）"
      - "回调函数吞异常以保证主流程继续（如统计刷新失败不阻断 commit）"
      - "非业务核心路径的 best-effort 操作（失败可降级/跳过）"
    reason_required: true
    reason_format: "M12豁免: <具体理由>（必须说明为何必须吞异常）"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-08-01"
    created_by: "ARCH-SSOT-GLOSSARY-MERGE-001"

  # ── 生成器幂等性时间源豁免（#ARCH-REGEN-NONIDEMPOTENT-001，2026-08-05）────────
  # gate-generator-no-realtime-time pre-commit hook 的合法豁免标记
  - marker: "arch-regen-nonidempotent"
    metric_id: null  # 非 dashboard 指标，是 gate-generator-no-realtime-time gate 行级豁免
    description: "生成器实时时间源豁免——日粒度时间源或已人工评估无 reconciler 非收敛风险"
    applicable_conditions:
      - "日粒度时间源 datetime.now(UTC).strftime('%Y-%m-%d')，24 小时内幂等（需配合 m46-time 标注）"
      - "已人工评估并确认无 reconciler 非收敛风险的生成器时间源使用"
    reason_required: true
    reason_format: "arch-regen-nonidempotent豁免: <具体理由>（必须说明为何不影响生成器幂等性）"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-08-05"
    created_by: "ARCH-REGEN-NONIDEMPOTENT-001"

  # ── 长参数列表存量签名契约豁免（AI-NIGHT-001 波3，2026-08-20）────────
  # NO-LONG-PARAM-LIST gate 行级豁免（对齐 bare-subprocess/import-integrity 同族通道）
  - marker: "long-param-list"
    metric_id: null  # 非 dashboard 指标，是 NO-LONG-PARAM-LIST gate 行级豁免
    description: "NO-LONG-PARAM-LIST gate 行级豁免——存量长参数函数签名契约绑定的合法场景"
    applicable_conditions:
      - "公共包装器/适配器与下游实现签名契约绑定（参数对象重构属破坏性变更，须专项裁定排期）"
      - "协议/接口实现强制签名（override 不可改形）"
    reason_required: true
    reason_format: "long-param-list豁免: <具体理由>（必须说明签名契约绑定方 + 重构登记路径）"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-08-20"
    created_by: "AI-NIGHT-001 波3（Owner 通用裁定条款 §四-2 授权）"

  # ── 裸SQL存量伪新增豁免（AI-NIGHT-001 波3，2026-08-20）────────────────
  # NO-BARE-SQL gate 行级豁免（对齐 bare-subprocess/import-integrity 同族通道）
  - marker: "bare-sql"
    metric_id: null  # 非 dashboard 指标，是 NO-BARE-SQL gate 行级豁免
    description: "NO-BARE-SQL gate 行级豁免——存量 SQL 行被 format 重排伪'新增'（gate 只检 added 行）、且动态标识符/f-string 插值使 SQL_* 常量集中化不可机械施行的治理DBA脚本场景"
    applicable_conditions:
      - "存量 SQL 行被 ruff format 重排在 diff 中伪'新增'，非真实新增裸 SQL"
      - "治理 DBA 脚本（depgraph/battle_map/decisiongraph/schema 管理）参数化查询，SQL 即业务语言"
      - "动态标识符（列名/表名 f-string 插值）场景，SQL_* 常量集中化不可机械施行"
    reason_required: true
    reason_format: "bare-sql豁免: <具体理由>（必须说明存量/伪新增性质 + §5.160.2 集中化专项登记路径）"
    reason_min_length: 10
    scope: "per-line"
    created: "2026-08-20"
    created_by: "AI-NIGHT-001 波3（Owner 通用裁定条款 §四-2 授权）"

# 禁止标记清单（常见误用，noqa_validation_gate 硬阻断）
forbidden_markers:
  - "noqa"        # 裸 noqa 无 marker code，禁止（必须指定 marker）
  - ""            # 空 marker code，禁止