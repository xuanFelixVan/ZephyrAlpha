---
module_id: KE-873----------------package-000
status: active
title: 4. 同层模块（相对导入，仅限同 package 内）
category: governance
---

# 4. 同层模块（相对导入，仅限同 package 内）

4. 同层模块（相对导入，仅限同 package 内）
from .models import FactorSignal
from .utils import normalize_tick
```
