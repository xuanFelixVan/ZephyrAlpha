---
module_id: KE-2380-----should-000
title: 6.3 `references` 字段（SHOULD）
category: module_blueprint
---

# 6.3 `references` 字段（SHOULD）

6.3 `references` 字段（SHOULD）

Level 2 模块蓝图**可以**在 frontmatter 中声明 `references` 字段——列出其**直接依赖的其他模块蓝图**（同级 Level 2）：

```yaml
