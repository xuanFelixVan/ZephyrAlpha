---
module_id: KE-278---------------p0-000
status: active
title: 3.3 攻击树 — 最高优先级场景（P0）
category: documentation
---

# 3.3 攻击树 — 最高优先级场景（P0）

3.3 攻击树 — 最高优先级场景（P0）

```
攻击目标：导出生产 API Key / 资金账户凭证
│
├── 路径 A：Prompt Injection 触发 AI 输出密钥
│   ├── A1：恶意 Markdown 注入 Cursor 聊天
│   │   └── 缓解：LSG L1 classify_input() 检测注入模式 [P0]
│   └── A2：污染向量库让 AI 读取包含密钥的上下文
│       └── 缓解：VMS 写入时 Secret Scanner (§6.3) [P0]
│
├── 路径 B：Agent 越权读取 .env
│   ├── B1：AI 生成 `cat .env` 命令
│   │   └── 缓解：Agent Sandbox 命令白名单 [P0]
│   └── B2：AI 通过 Python os.environ 读取
│       └── 缓解：Sandbox 环境变量过滤（移除 SECRET_* 前缀）[P0]
│
└── 路径 C：误提交 .env 到 git
    ├── C1：开发者手动提交
    │   └── 缓解：git-secrets pre-commit hook [P0]
    └── C2：AI 代码生成中包含密钥常量
        └── 缓解：LSG L3 输出 Secret Pattern + trufflehog CI [P0]
```

**防御深度**：每条路径有 **2+ 缓解点**，任何单点失效不应导致密钥泄露（fail-closed）。

---
