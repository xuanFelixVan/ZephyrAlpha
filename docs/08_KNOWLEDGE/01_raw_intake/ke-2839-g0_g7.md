---
module_id: KE-2741
status: active
title: 五、核心流程 — G0-G7 任务门禁结构化规则
category: module_blueprint
---

# 五、核心流程 — G0-G7 任务门禁结构化规则

五、核心流程 — G0-G7 任务门禁结构化规则

> 以下将蓝图 §2 中的自然语言门禁描述升级为**确定性 YAML 规则**——AI agent 可直接消费。
> 每一条 `check` 是布尔表达式（禁止问句），每一条 `on_failure` 有明确的 `fix_hint`。
