---
module_id: KE-governance-latest-000
title: 三、状态快照管理（LATEST 覆盖写入模式）
category: governance
---

# 三、状态快照管理（LATEST 覆盖写入模式）

三、状态快照管理（LATEST 覆盖写入模式）

> **旧体系教训**：旧体系中每次扫描都新建一个带日期的文件（如 `scan-20260413.json`、`scan-20260414.json`），导致目录中堆积大量历史快照，AI 不知道哪个是最新的。
