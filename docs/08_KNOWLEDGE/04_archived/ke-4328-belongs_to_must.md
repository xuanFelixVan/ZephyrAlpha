---
module_id: KE-4168-------belongs-to--must-002
title: 6.2 域蓝图的 `belongs_to`（MUST）
category: module_blueprint
ttl: permanent
doc_type: knowledge_entry
---

# 6.2 域蓝图的 `belongs_to`（MUST）

6.2 域蓝图的 `belongs_to`（MUST）

Level 1 域蓝图 MUST 声明 `belongs_to` 指向 Level 0 总蓝图：

```yaml
