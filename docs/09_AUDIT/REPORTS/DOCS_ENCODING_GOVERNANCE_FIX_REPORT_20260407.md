---
module_id: 09_AUDIT_REPORTS_001
version: 1.0.0
status: Active
created_date: 2026-04-07
last_updated: 2026-04-07
owner: 文档管理团队
responsibility:
  - 提供文档支持
---

# docs 编码治理修复报告（2026-04-07）

## 目标

对 `docs/**/*.md` 进行“专业蓝图文档治理式修复”，优先解决**可确定修复**的编码/结构问题（编码可读、结构可解析、代码块/mermaid 不吞文），并将**不可逆的内容断裂**（如 `汉字?`）转为可追溯的“待补全清单”。

## 范围与验收口径

- **全量范围**：`docs/**/*.md`
- **治理验收范围（推荐口径）**：排除
  - `docs/06_ARCHIVE/**`
  - `docs/**/audit_state/**`

原因：归档/审计产物通常用于留存“当时的异常证据”，不建议做内容级重写；但仍可继续清理其结构性问题（如围栏不闭合）以便阅读。

## 执行的修复动作（按治理流程）

- **严格解码口径**：统一按 `utf-8-sig` 严格解码扫描（避免 `chardet` 误判）。
- **批量可确定修复**：
  - mojibake 反转（保守逐行/逐段接受策略）
  - 高字节伪影清理（仅在“无中文且高字节显著”的行做替换/删除）
  - C1 控制字符（U+0080–U+009F）剥离
- **结构性定向修补**：
  - fenced code block 未闭合（``` 数量为奇数）→ 追加闭合 fence
  - 仅在 `mermaid` 围栏内部清理历史残片（避免误伤正常金额符号 `¥`）
- **治理追溯文件**：
  - 基线：`docs/09_AUDIT/STATE/encoding_artifact_baseline.json`
  - 批修后：`docs/09_AUDIT/STATE/encoding_artifact_after_bulk.json`
  - 最终复检：`docs/09_AUDIT/STATE/encoding_artifact_final.json`

## 统计对比

### 修复前（基线）

来自 `docs/09_AUDIT/STATE/encoding_artifact_baseline.json`：

- **文件数**：2453
- **严格解码失败**：0
- **U+FFFD**：0
- **C1 控制字符命中文档数**：3
- **高字节伪影命中文档数**：84
- **mojibake 命中文档数**：11
- **汉字? 断裂命中文档数**：602
- **围栏不闭合（odd fences）文档数**：82

### 修复后（最终复检）

来自 `docs/09_AUDIT/STATE/encoding_artifact_final.json`：

#### 全量范围

- **文件数**：2471
- **严格解码失败**：0
- **U+FFFD**：0
- **C1 控制字符命中文档数**：0
- **高字节伪影命中文档数**：23
- **mojibake 命中文档数**：1（集中在审计产物）
- **汉字? 断裂命中文档数**：602（内容断裂，非编码可确定修复项）
- **围栏不闭合（odd fences）文档数**：9（集中在归档/审计产物）

#### 治理验收范围（排除 archive/audit_state）

- **文件数**：1613
- **严格解码失败**：0
- **U+FFFD**：0
- **C1 控制字符命中文档数**：0
- **mojibake 命中文档数**：0
- **围栏不闭合（odd fences）文档数**：0
- **汉字? 断裂命中文档数**：386（需要模板回填/人工补全）

## 剩余问题与下一步建议

### 1) `汉字?` 截断（内容断裂，需“治理补全”）

- 已生成待补全清单（治理范围）：`docs/09_AUDIT/STATE/cjk_question_mark_todo_scoped.json`
  - **文件数**：385
  - **命中总数**：24143（已执行一轮“高置信度规则化回填”后）
  - 每个文件包含：命中数量、前 10 条样例（行号 + 片段）

#### 已执行：高置信度规则化回填（第二阶段）

- 脚本：`scripts/fix_cjk_question_mark_governed.py`
- 策略：跳过 fenced code block 与链接行，仅对常见断裂词尾做回填（如“设计/功能/模块/报告/监控/约束/集成”等），避免盲猜语义。
- 执行结果：
  - 修复文件数：370 → 187（第二轮扩展分隔符后新增修复）
  - 回填次数：1160 → 1017（第二轮扩展分隔符后新增回填）
- 统计输出：
  - `docs/09_AUDIT/STATE/cjk_question_mark_stats_after_rules.json`

建议处理方式：
- 先对 **蓝图类文档**（`docs/**/BLUEPRINTS/**`）按优先级（P0/P1）做模板回填与关键段落补全。
- 对“目录/章节/表格骨架”先修成可读，再补语义，不要盲猜具体数据或结论。

### 2) 少量高字节字符残留

当前剩余的高字节字符，主要可能来自：
- 合法符号（如 `¥`、NBSP 等）
- 或少量仍不满足“高字节密度阈值”的残片

如果你要求“绝对纯净”，下一轮可增加规则：仅替换 **U+00A0（NBSP）→ 空格**，并对特定目录做更严格清理。

## 附录：`01_BLUEPRINTS` 蓝图目录收尾（2026-04-07）

### 范围

- 目录：`docs/05_IMPLEMENTATION/06_CONSTRUCTION_DOCS/01_BLUEPRINTS/`
- 口径：以 `汉字?`（中文字符后紧跟 `?`）为“内容断裂待补全”信号；并同步清理同一处的孤立 `?` 残留（不影响代码变量名/链接/mermaid）。

### 执行方式（治理式：批量 + 定向）

- 批量规则化回填：`scripts/fix_cjk_question_mark_blueprints.py`
  - 跳过 fenced code block、跳过链接行，仅在“中文 + ? + 分隔符/行尾”场景替换，避免误伤。
- 定向高置信度补全：对 Top 命中文档逐个建立脚本（例如 `scripts/fix_*_gaps.py`），以“可确定补全”为准则：
  - 标题/表格字段/清单项：补全断裂词尾（如“目标/设计/依赖/价值/透明度/复杂/组合”等）
  - 代码块：仅补全中文注释/文档字符串的断裂，不重写代码逻辑与变量名
  - 结构：把被挤压到同一行的条目拆行，恢复可读性与可解析性

### 结果（收尾验收）

- 产物：`docs/09_AUDIT/STATE/cjk_question_mark_todo_blueprints.json`
- 最终状态：`total_files = 0`、`total_hits = 0`
- 说明：这代表 `01_BLUEPRINTS` 目录内的 `汉字?` 断裂已全部被“可追溯、可审计、可读”的方式清理到 0。

### 残余风险与建议

- `汉字?` 本质是上游内容截断的信号；本轮仅对“高置信度可确定补全”进行回填，**不会**对不确定语义做猜测性补写。
- 若后续出现“语义缺失但无 `?` 信号”的情况，建议按 P0/P1/P2 分级人工补全：
  - P0：目录/标题/围栏/表格骨架（保证可解析）
  - P1：关键定义与验收口径（保证可用）
  - P2：示例/数据/策略细节（保证完备）

