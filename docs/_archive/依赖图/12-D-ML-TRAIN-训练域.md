# 12 — D-ML-TRAIN 训练域

> **状态**: DRAFT | **核心层**: L11 模型层 | **成熟度**: L1 🔵 骨架
> **运行平面**: Cold(H=2) | **一句话**: 训练模型、发现因子、闭环进化

## §0 域定义

| 维度 | 内容 |
|------|------|
| 域ID | D-ML-TRAIN |
| 域名 | 机器学习训练域 |
| 职责 | 模型训练、因子发现、实验管理、元学习（Cold平面） |
| 核心层 | L11(模型层) |
| 成熟度 | L1 🔵骨架 |
| 优先级 | P1 |
| 架构定位 | 可选增强域——因子域和信号域可以不用ML独立运行 |
| 核心Aggregate | AGG-008 Model + ENT-006 ModelVersion |
| 核心事件 | E-ML-01 ModelTrained / E-RS-03 ModelValidated |
| 运行平面 | Cold(H=2)，离线批处理，不影响实时推理 |
| 不变量执行 | INV-011(Cold→Hot禁止直接通信)：TRAIN产出模型必须经SERVE影子验证后方可进Warm |

## §1 子模块清单（5个子模块，中等厚度）

> **骨架来源**：学习系统7阶段流水线(A8) S0~S4+S6 → TRAIN覆盖"知识→模型"的创建侧

| ID | 名称 | 职责 | 优先级 | 开发状态 | 对标能力 | 对标流水线 |
|----|------|------|:------:|:--------:|---------|-----------|
| MT-01 | TrainingPipeline | 模型训练+验证+可复现性：PyTorch训练→ONNX导出、seed管理、config快照、进化式代码生成(S4 DSL+AST沙箱)、分析师Agent反馈循环 | P0 | ✅ 已有ABC | C-029●模型工厂 | S4模块创建 |
| MT-02 | ExperimentTracker | A/B实验+Champion-Challenger+统计验证+自动晋升：wandb集成、实验血缘追踪、DSR/CPCV v2/White's Reality/Probabilistic BT | P1 | ✅ 已有 | C-007◐闭环优化、C-003◐回测 | S5试运行编排 |
| MT-03 | AutoMLEngine | 自动模型选择+超参优化+因子挖掘：Optuna贝叶斯优化、早停、Qlib因子挖掘、辩论式因子精炼(FactorMAD)、质量-多样性优化(QuantEvolve) | P1 | ❌ | C-029●模型工厂 | S3映射+S4创建 |
| MT-04 | FeatureDiscovery | 因子发现+因果发现+特征工程：PC/GES/LiNGAM因果发现、AutoML因子挖掘、特征交叉、辩论式因子精炼、三重语义一致性 | P1 | ❌ | C-034○主力自迭代、C-035○庄家自迭代、C-036◐群体博弈 | S2分类 |
| MT-05 | DriftAdapter | 概念漂移检测+自适应重训练+元学习：DDM/EDDM/ADWIN检测、MAML快速适应、在线EWC防遗忘、技能库(Voyager)、元反思、AutoSkill | P1 | ❌ | C-025◐质量保障、C-033◐过拟合防护 | S6元学习 |

### 旧子模块归并映射

| 旧ID | 旧名称 | 归入 |
|------|--------|------|
| D-ML-01 | Model Training Pipeline | MT-01 |
| D-ML-04 | Experiment Pipeline | MT-02 |
| D-ML-07 | AutoML Engine | MT-03 |
| D-ML-11 | Hyperparameter Optimizer | MT-03 |
| D-ML-12 | Training Data Manager | MT-01(内嵌) |
| D-ML-13 | Model Lineage Tracker | MT-02(内嵌) |
| D-ML-15 | Causal Discovery Engine | MT-04 |
| D-ML-18 | Concept Drift Adapter | MT-05 |
| D-ML-19 | Synthetic Data Generator | MT-01(内嵌) |
| D-ML-84 | Qlib AI Factor Mining | MT-03 |
| D-ML-85 | FinRL Deep RL | MT-03(内嵌) |
| D-ML-117 | AI Factor Mining Engine | MT-04 |
| D-ML-118 | Feature Engineering Automation | MT-04 |
| D-ML-46 | M3 Code Generation Model Adapter | MT-01(内嵌) |
| D-ML-62~65 | wandb/实验追踪系列 | MT-02 |
| D-ML-66/70/105 | 市场状态识别系列 | →D-SIGNAL |
| D-ML-71/76~78 | NLP情感分析系列 | →D-ALT-DATA |
| D-ML-79~81 | CNN/GNN/Transformer系列 | MT-01(训练框架内) |
| D-ML-35~57 | LLM路由/方案/适配器系列 | →MS-06 LLMGateway |
| D-ML-20 | Distribution Fitting Engine | →D-RISK |
| D-ML-17 | Federated Model Trainer | P3远期，暂不归入 |

### 能力覆盖检查

| 能力 | 优先级 | 角色 | 覆盖子模块 | 状态 |
|------|:------:|:----:|-----------|:----:|
| C-029 模型工厂 | P1 | ●核心 | MT-01+MT-03 | ✅ |
| C-003 回测 | P0 | ◐辅助 | MT-02(编排) | ✅(核心在D-SIMULATION) |
| C-006 策略工厂 | P0 | ◐辅助 | MT-01(代码生成) | ✅(核心在D-AUTONOMY) |
| C-007 闭环优化 | P0 | ◐辅助 | MT-02+MT-05 | ✅(核心在D-AUTONOMY) |
| C-025 质量保障 | P1 | ◐辅助 | MT-05 | ✅(核心在D-SIMULATION) |
| C-033 过拟合防护 | P1 | ◐辅助 | MT-02+MT-05 | ✅(核心在D-SIMULATION) |
| C-034 主力行为自迭代 | P1 | ○间接 | MT-04 | ✅ |
| C-035 庄家行为自迭代 | P1 | ○间接 | MT-04 | ✅ |
| C-036 群体博弈模拟 | P1 | ◐辅助 | MT-04 | ✅(核心在D-SIGNAL) |
| C-014 大盘预测 | P1 | ○间接 | MT-01(训练) | ✅(核心在D-SIGNAL) |
| C-021 市场状态 | P1 | ○间接 | MT-04(因果) | ✅(核心在D-SIGNAL) |

## §2 域内依赖图

```
                    ┌─────────────────────┐
                    │   D-DATA (CTR-001)  │
                    │   D-FACTOR (E-RS-01)│
                    │   D-KNOWLEDGE       │
                    └─────────┬───────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
     ┌────────────┐  ┌────────────┐  ┌────────────┐
     │  MT-04     │  │  MT-01     │  │  MT-03     │
     │  Feature   │──│  Training  │◀─│  AutoML    │
     │  Discovery │  │  Pipeline  │  │  Engine    │
     └────────────┘  └─────┬──────┘  └────────────┘
          │产出新因子        │产出模型       │产出最优配置
          ▼                 ▼              │
     ┌──────────┐  ┌────────────┐         │
     │ D-FACTOR │  │  MT-02     │◀────────┘
     │ (E-RS-01)│  │  Experiment│
     └──────────┘  │  Tracker   │
                   └─────┬──────┘
                         │E-RS-03
                         ▼
                   ┌────────────┐     ┌────────────┐
                   │  MT-05     │◀────│  MS-03     │
                   │  Drift     │E-OP-02 Drift     │
                   │  Adapter   │     │  Monitor   │
                   └─────┬──────┘     └────────────┘
                         │重训练请求
                         ▼
                   ┌────────────┐
                   │  MT-01     │
                   │  Training  │
                   │  Pipeline  │
                   └────────────┘
```

### 域内依赖关系表

| 源 | 目标 | 依赖类型 | 说明 |
|----|------|---------|------|
| MT-04 | MT-01 | 数据供给 | 发现的新因子/特征→训练Pipeline输入 |
| MT-03 | MT-01 | 配置供给 | AutoML产出最优模型架构+超参→训练Pipeline |
| MT-01 | MT-02 | 产物流转 | 训练完成→进入实验追踪+验证 |
| MT-05 | MT-01 | 重训练触发 | 漂移检测→触发自适应重训练 |
| MT-04 | D-FACTOR | 域间产出 | 新因子→因子域注册 |

## §3 域间接口

### 消费依赖（TRAIN 依赖其他域）

| 契约/事件 | 供给域 | 强度 | 内容 | 消费子模块 |
|----------|--------|:----:|------|-----------|
| CTR-001 | D-DATA | H | NormalizedMarketData | MT-01, MT-04 |
| E-RS-01 | D-FACTOR | E | FactorComputed | MT-01, MT-04 |
| — | D-AUTONOMY-CORE | H | GPU资源+LLM服务 | MT-01, MT-03 |
| — | D-INFRA-RUNTIME | S | 离线计算调度 | MT-01 |
| E-OP-02 | D-ML-SERVE | E | ModelDriftDetected | MT-05 |
| — | D-KNOWLEDGE | E | 知识包(S2输出) | MT-04 |
| — | D-RESEARCH | E | 研究资产 | MT-02 |
| — | D-REPORTING | E | 绩效反馈 | MT-05 |
| — | D-SIGNAL | S | 信号反馈 | MT-05 |

### 产出依赖（其他域依赖 TRAIN）

| 产出 | 消费域 | 强度 | 说明 | 产出子模块 |
|------|--------|:----:|------|-----------|
| E-ML-01 ModelTrained | D-ML-SERVE | E | 训练完成→推理域加载 | MT-01 |
| E-RS-03 ModelValidated | D-ML-SERVE | H | 验证通过→推理域可部署 | MT-02 |
| 新因子/特征 | D-FACTOR | E | 因子发现→因子域注册 | MT-04 |
| 因果图 | D-SIGNAL | E | 因果发现→信号域参考 | MT-04 |

### TRAIN↔SERVE 通信约束（INV-011核心不变量）

```
TRAIN (Cold, H=2) ──✗── 直接通信 ──✗── SERVE (Warm, H=3)
       │                                        │
       │  E-ML-01 ModelTrained (event)          │
       ├───────────────────────────────────────▶│  MS-01 ModelRegistry接收
       │                                        │
       │  E-RS-03 ModelValidated (event)        │
       ├───────────────────────────────────────▶│  MS-04 ModelValidator影子验证
       │                                        │
       │  E-OP-02 ModelDriftDetected (event)    │
       │◀───────────────────────────────────────┤  MS-03 DriftMonitor发出
       │                                        │
       │  ⚠ INV-011: Cold输出必须先经影子验证    │
       │  ⚠ INV-019: Warm→Cold必须通过异步通信   │
       │  ⚠ INV-010: PIT契约三平面共享schema     │
```

## §4 域事件流

### 产出事件

| 事件ID | 事件名 | 触发条件 | 载荷 | 消费者 |
|--------|--------|---------|------|--------|
| E-ML-01 | ModelTrained | MT-01训练完成 | model_id, version, training_config, metrics, artifact_path, trained_at | MS-01 ModelRegistry |
| E-RS-03 | ModelValidated | MT-02验证通过 | model_id, version, validation_metrics, experiment_id, validated_at | MS-01 ModelRegistry |
| E-ML-02 | NewFactorDiscovered | MT-04发现新因子 | factor_id, formula, ic, ir, discovery_method | D-FACTOR |
| E-ML-03 | RetrainTriggered | MT-05触发重训练 | model_id, drift_type, drift_score, trigger_reason | MT-01 |

### 消费事件

| 事件ID | 事件名 | 供给域 | TRAIN处理 |
|--------|--------|--------|----------|
| E-OP-02 | ModelDriftDetected | D-ML-SERVE | MT-05消费→判断漂移类型→触发重训练或参数调整 |
| E-RS-01 | FactorComputed | D-FACTOR | MT-01消费因子作为训练特征；MT-04消费作为因果发现输入 |
| — | KnowledgePackageReady | D-KNOWLEDGE | MT-04消费知识包→因子发现+因果发现 |

### 事件流时序（训练→验证→部署闭环）

```
D-DATA ──CTR-001──▶ MT-01 TrainingPipeline ──E-ML-01──▶ MS-01 ModelRegistry
D-FACTOR ──E-RS-01──▶ MT-01                     │
                        MT-02 ExperimentTracker   │
                           │E-RS-03               ▼
                           └──────────────────▶ MS-04 ModelValidator
                                                  │影子验证(INV-011)
                                                  ▼
                                              MS-01 标记ACTIVE
                                                  │
                                                  ▼
                                              MS-02 InferenceEngine
                                                  │
MS-03 DriftMonitor ──E-OP-02──▶ MT-05 DriftAdapter ──E-ML-03──▶ MT-01 重训练
```

## §5 激活前提

| 前提 | 域 | 必要性 | 说明 |
|------|-----|:------:|------|
| 数据管道就绪 | D-DATA | 必须 | CTR-001 NormalizedMarketData可访问 |
| 因子计算部分就绪 | D-FACTOR | 部分 | 至少一组因子可作为ML输入特征 |
| GPU资源可用 | D-AUTONOMY | 必须 | RTX 3090 VRAM分配≥2GB(训练) |
| ONNX Runtime已安装 | 基础设施 | 必须 | CUDA支持+CPU fallback |
| 知识库可访问 | D-KNOWLEDGE | 可选 | MT-04因子发现需要知识包输入 |

### 激活阶段

| 阶段 | 前提 | 可激活子模块 |
|------|------|-------------|
| Phase 1 | D-DATA就绪 | MT-01(基础训练) |
| Phase 2 | Phase 1 + D-FACTOR部分就绪 | MT-02(实验追踪)、MT-03(AutoML) |
| Phase 3 | Phase 2 + D-KNOWLEDGE就绪 | MT-04(因子发现)、MT-05(漂移适应) |

## §6 设计决策记录

| # | 决策 | 理由 | 影响 |
|---|------|------|------|
| 1 | 105子模块→5子模块精简 | 旧草稿子模块粒度过细(D-ML-35~57全是LLM路由变体)，按7阶段流水线骨架归并 | 旧ID映射见§1归并表 |
| 2 | Feature Store归D-DATA-03，ML只是消费者 | 数据域是特征的唯一真相源，避免双写；PIT正确性由D-DATA保证 | MT-01通过CTR-001消费 |
| 3 | 模型格式: ONNX(生产)+PyTorch(训练) | PyTorch训练生态成熟，ONNX推理可移植 | MT-01产出PyTorch→SERVE转换为ONNX |
| 4 | INV-011由SERVE侧执行 | Cold→Hot禁止直接通信，影子验证门禁在SERVE的MS-04执行 | TRAIN只负责训练+发布事件，不直接推送模型到推理 |
| 5 | LLM路由/方案/适配器系列归MS-06 | LLM推理服务是Warm平面关注点，TRAIN只消费LLM服务 | D-ML-35~57全部归入MS-06 |
| 6 | NLP情感分析归D-ALT-DATA | NLP情感分析本质是另类数据处理，不是模型训练 | D-ML-71/76~78归入D-ALT-DATA |
| 7 | 市场状态识别归D-SIGNAL | MarketRegime是信号域的核心能力，ML只提供训练支撑 | D-ML-66/70/105归入D-SIGNAL |
| 8 | GPU资源由D-AUTONOMY统一分配 | RTX 3090 24GB需在ML训练/MC-VaR/LLM间分配 | 所有GPU需求通过D-AUTONOMY调度 |
| 9 | 7阶段流水线S0~S2不单独设子模块 | S0采集+S1清洗+S2分类的前半段由D-DATA/D-KNOWLEDGE承担，TRAIN只消费其产出 | MT-04覆盖S2的因子发现+因果发现部分 |
| 10 | E-0085(SERVE→TRAIN hard)解读为模型Schema依赖 | SERVE需要理解TRAIN的模型包格式(CR-MODEL)才能加载推理，不是业务逻辑依赖 | 模型包格式由MT-01定义，SERVE消费 |

## §7 风险架构(A4)交叉内容

> 来源：风险架构 §1.2 模型风险(漂移检测五分类+CUSUM) + §7.3 事后重训触发(V1-V6门禁)

### §7.1 漂移检测五分类

> 对齐 2025-2026 学术前沿。训练域关注漂移的分类体系，以指导MT-05 DriftAdapter的检测逻辑与重训策略选择。

| 漂移类型 | 定义 | 检测方法 | 阈值 |
|---------|------|---------|------|
| 协变量漂移(Covariate Drift) | P_train(X) ≠ P_test(X) | PSI / KS / Wasserstein距离 | PSI>0.25 / KS p<0.01 |
| 概念漂移(Concept Drift) | P_train(Y\|X) ≠ P_test(Y\|X) | 性能衰减+ADWIN+DDM+CUSUM | AUC下降>5% |
| 标签漂移(Label Drift) | P_train(Y) ≠ P_test(Y) | 基准率监控+CUSUM | 基准率偏移>20% |
| 公平性漂移(Fairness Drift) | 子群体性能差异扩大 | 子群体AUC/Recall差异 | 子群体差异>10% |
| 上游数据漂移 | 数据管道Schema/质量变化 | Schema校验+空值率+格式检查 | 空值率>5% / Schema变更 |

### §7.2 CUSUM控制图

> 对齐 2025-2026 学术前沿，补充PSI/KS的持续性偏移检测。训练域关注CUSUM参数设定，以指导MT-05漂移检测的灵敏度配置。

| 检测对象 | CUSUM参数 | 预警阈值 | 行动阈值 | 优势 |
|---------|----------|---------|---------|------|
| 模型预测偏差 | 参考值k=0.5σ, 决策区间h=5σ | 单侧累积和>3σ | 累积和>5σ | 检测持续性小幅偏移(PSI/KS可能漏检) |
| 因子IC趋势 | k=0.5×IC标准差 | IC CUSUM>2σ | IC CUSUM>4σ | 比滚动IC更早发现衰减趋势 |
| 交易成本趋势 | k=0.5×成本标准差 | 成本CUSUM>2σ | 成本CUSUM>4σ | 检测隐性成本上升(市场微观结构变化) |

### §7.3 事后重训触发与V1-V6门禁

> 训练域是重训流程的执行者(MT-01 TrainingPipeline)，V1-V6门禁是重训后模型上线的必要验证关卡。

**重训触发条件与流程**：

```
漂移检测超限(PSI>0.25 / 性能衰减>5% / CUSUM>5σ)
    │
    ├──→ [自动触发] 模型降级为"仅建议"模式
    │
    ├──→ [自动触发] 通知Trader+Risk Manager
    │
    ├──→ [自动触发] 收集漂移期间数据+标注
    │
    └──→ [人工审批] 触发重训流程
              │
              ├── 重训数据准备(PIT合规)
              ├── 重训+验证(V1-V6门禁)
              ├── 影子模式运行(≥5个交易日)
              ├── A/B对比(新模型vs旧模型)
              └── 人工审批上线/否决
```

**重训门禁**（对齐能力定位书§2-d约束七）：

| 门禁 | 验证内容 | 通过条件 |
|------|---------|---------|
| V1 因子验证 | 新因子IC/ICIR | \|IC\|>阈值+Purged K-Fold |
| V2 信号验证 | 信号方向准确率 | Walk-Forward通过 |
| V3 策略验证 | 策略PnL/Sharpe/回撤 | Walk-Forward+Permutation Test |
| V4 管线验证 | 全链路端到端 | Walk-Forward+模拟盘 |
| V5 上线验证 | 影子模式运行≥5日+与实盘一致性 | 影子模式PnL偏差<5%+人工审批 |
| V6 风控验证 | 风控触发/熔断/保护性减仓 | 极端场景重放 |

## §8 安全架构约束（源自A5安全架构）

> 来源：A5安全架构 §1.2 数据域

### §8.1 域边界定义

> 来源：A5安全架构 §1.2

覆盖 D-DATA（数据核心）、D-FACTOR（因子）、D-SIGNAL（信号）、D-DATA-ENG（数据工程）、D-ALT-DATA（另类数据）、D-ML-TRAIN（ML训练子域）。数据域包含ML训练子域，用于模型训练数据集的存储与管理，安全等级与数据域一致。数据域是系统的信息基础，所有策略和决策的数据来源。

**为什么数据域需要独立安全域**：因子公式和信号逻辑是量化系统的核心知识产权。PIT（Point-in-Time）数据的完整性直接决定回测可信度，数据污染会导致策略失效和错误决策。另类数据可能涉及合规风险，需要独立管控。

### §8.2 资产分类与信任等级

> 来源：A5安全架构 §1.2

| 资产类型 | 信任等级 | 分类 | 示例 |
|---------|---------|------|------|
| 因子公式 | 绝密（L3） | 核心资产 | Alpha因子表达式、因子构造逻辑 |
| 信号逻辑 | 绝密（L3） | 核心资产 | 信号生成算法、信号组合权重 |
| PIT数据 | 机密（L2） | 敏感资产 | 历史时点正确数据、复权因子 |
| 另类数据 | 机密（L2） | 敏感资产 | 舆情数据、供应链数据 |
| 原始行情 | 内部（L1） | 业务资产 | Tick数据、K线数据 |
| 数据质量报告 | 内部（L1） | 业务资产 | 缺失率、异常值统计 |

### §8.3 数据流入规则

> 来源：A5安全架构 §1.2

| 来源域 | 允许流入的数据 | 安全检查点 |
|--------|--------------|-----------|
| 外部（iFind） | 行情数据、财务数据 | 数据源认证+格式校验+PIT一致性检查 |
| 外部（另类） | 另类数据 | 数据源审批+合规审查+格式校验 |
| 交易域 | 交易结果数据 | 数据降级处理确认 |

### §8.4 数据流出规则

> 来源：A5安全架构 §1.2

| 目标域 | 允许流出的数据 | 安全检查点 |
|--------|--------------|-----------|
| 交易域 | 行情、因子值、信号 | 数据签名+完整性校验 |
| 治理域 | 数据质量报告 | 脱敏处理 |
| 运维域 | 审计日志 | 日志签名 |
| 数据域→ML训练子域（域内子域流） | 训练数据集 | PIT隔离+数据版本标记 |

### §8.5 安全控制要求

> 来源：A5安全架构 §1.2

- 因子公式和信号逻辑存储时使用AES-256加密，运行时解密到受保护内存区域
- PIT数据必须有时点标记和完整性校验，防止未来信息泄露（look-ahead bias）
- iFind API凭证仅在数据域进程内可见，禁止跨域传递
- 另类数据接入必须经过人工审批（HB-SEC-06），审批记录写入审计链
- 数据域到LLM的调用必须100%脱敏（HB-SEC-02，全域硬边界，非仅数据域），因子公式和信号逻辑禁止以未脱敏形式发送到外部LLM（经100%脱敏降级为L1后可通过白名单LLM代理通道发送，详见A5安全架构§2.4）

## §9 S4 模块创建与接入层（源自学习系统架构）

> 来源：学习系统架构 §7 S4 模块创建与接入层 (lines 1196-1406)
> 对应子模块：MT-01 TrainingPipeline（进化式代码生成+分析师Agent反馈循环）

### §9.1 模块创建流程

```
ModuleMappingResult → 模块创建:

1. UPDATE_PARAM (参数更新)
   ├─ 已有模块完全满足需求, 但参数需要调整
   ├─ 例: "光纤修复五天线" → 更新板块轮动追踪器的光纤板块参数
   ├─ 流程: 生成参数更新方案 → 人工审核 → 应用更新 → 进入S5试运行
   └─ 约束: 参数变更数≤3 (与B-008对齐)

2. EXTEND_MODULE (模块扩展)
   ├─ 已有模块部分满足需求, 需要扩展功能
   ├─ 例: "回踩质量A/B/C三级" → 扩展§6.1.3板块轮动追踪器的回踩质量评估
   ├─ 流程: 生成扩展方案 → LLM辅助代码生成 → 人工审核 → 注册新版本 → 进入S5
   └─ 约束: 扩展不可破坏已有功能(向后兼容)

3. CREATE_NEW (新建模块)
   ├─ 无匹配模块, 需要从零创建
   ├─ 例: "马拉松行情生命周期" → 新建§6.7行情生命周期阶段模块
   ├─ 流程:
   │   ├─ a. ModuleRequirementSpec → LLM生成模块设计文档
   │   ├─ b. 设计文档 → 人工审核(架构师角色)
   │   ├─ c. 审核通过 → LLM辅助代码生成
   │   ├─ d. 代码 → 人工审核(开发者角色)
   │   ├─ e. 审核通过 → 注册到Module Registry
   │   ├─ f. 接入交易流水线对应层级
   │   └─ g. 进入S5试运行
   └─ 约束: 新模块必须归属于现有架构层级(不可产生层级外模块)
```

### §9.2 LLM辅助代码生成（v4.0升级：DSL约束+AST沙箱+三重语义一致性+进化式代码生成+分析师Agent反馈）

```
LLM辅助代码生成流程:

输入: ModuleRequirementSpec + 交易流水线架构文档(§1~§29相关章节)
  ├─ LLM需要理解:
  │   ├─ 目标层级的输入/输出契约
  │   ├─ 同层级已有模块的代码风格和接口规范
  │   ├─ 全局约束(B-001~B-020, §20方法论约束)
  │   └─ C-033过拟合防护要求
  ├─ 生成内容:
  │   ├─ 模块设计文档(接口定义+数据流+依赖关系)
  │   ├─ 因子表达式（DSL空间内，仅使用预定义算子组合）
  │   ├─ 模块代码骨架(类定义+方法签名+输入输出类型)
  │   ├─ 单元测试骨架(正常/边界/异常场景)
  │   └─ 接入配置(注册到对应工厂的配置文件)
  └─ 人工审核重点:
      ├─ 逻辑正确性(LLM可能生成看似合理但逻辑错误的代码)
      ├─ 与现有模块的接口一致性
      ├─ 是否引入未来信息(Point-in-Time违规)
      └─ 是否违反硬边界约束

因子DSL约束（v4.0新增，裁定3✅）:
  ├─ 6类预定义算子（对标QuantaAlpha）:
  │   ├─ 数学算子: abs, log, sign, power, sqrt
  │   ├─ 时序算子: ts_mean, ts_std, ts_rank, ts_delta, ts_sum, ts_max, ts_min
  │   ├─ 截面算子: cs_rank, cs_zscore, cs_demean, cs_quantile
  │   ├─ 逻辑算子: if_then, and, or, not
  │   ├─ 比较算子: gt, lt, eq, ge, le
  │   └─ 聚合算子: group_mean, group_sum, group_rank
  ├─ LLM只能在DSL空间内组合算子，不可自由生成任意Python代码
  └─ 后续按东方证券建议扩展至70维特征体系

AST沙箱三层安全（v4.0新增，裁定3✅）:
  ├─ 第1层 白名单验证: 只允许预定义算子，禁止import/exec/eval/open等危险操作
  ├─ 第2层 复杂度约束: 最大嵌套深度5 / 最大参数数量10 / 最大节点数50
  ├─ 第3层 语义验证: 输出范围检查 / NaN检查 / 无穷大检查 / 类型检查
  └─ 依据: Hubble(Celestial Quant Lab/UBC, arXiv 2026)DSL+AST沙箱100%计算稳定性

三重语义一致性约束（v4.0新增，QuantaAlpha arXiv 2026）:
  ├─ 假设(Hypothesis) → 因子表达式(Expression) → 可执行代码(Code)
  ├─ 三者必须语义一致: 假设描述的经济逻辑 = 表达式计算的数学逻辑 = 代码执行的程序逻辑
  ├─ 验证方法: LLM交叉验证三者的一致性
  └─ 不一致 → 拒绝生成，要求重新对齐

进化式代码生成（v4.0新增，ProFiT Nof1 2025）:
  ├─ 单次生成 → 多轮进化迭代
  ├─ 流程: LLM生成代码v1 → 回测 → LLM分析回测结果+识别弱点 → 重写代码v2 → 回测 → ... → 收敛
  ├─ 收敛条件: 连续2轮Sharpe无提升 或 达到最大进化轮数(5轮)
  ├─ 保留: 每轮进化的代码+回测结果存入技能库（S6），供未来新任务检索复用
  └─ 约束: 进化过程中不可违反AST沙箱约束

分析师Agent反馈循环（v4.0新增，裁定4✅）:
  ├─ Generator(GLM-5.1): 生成模块代码
  ├─ Critic(DeepSeek V4 Pro): 审查代码，识别逻辑漏洞/过拟合风险/代码缺陷
  ├─ 反馈循环: Generator根据Critic反馈修正代码 → Critic再审查 → 收敛
  ├─ 收敛条件: Critic无新批评 或 达到最大轮数(3轮)
  └─ 最终: Judge(Claude)综合评估 → 通过AST沙箱 → 人工审核
  依据: Man Group AlphaGPT反馈循环使IC从0.58%→2.23%

轨迹级进化（裁定✅R-29）:
  ├─ 每次知识→模块映射视为一条"研究轨迹"（假设→构建→回测→优化）
  ├─ 进化时定位轨迹中的次优步骤进行定向修正
  ├─ 交叉复用互补高奖励片段
  └─ 依据: QuantaAlpha (arXiv 2602.07085v3) 轨迹级进化

适配器机制（裁定✅R-30）:
  ├─ 基础模型冻结 + 适配器(LoRA等)轻量微调
  ├─ 适配器学习分布漂移的表示→映射为参数调整
  ├─ 无需全量重训，RTX 3090可运行
  └─ 依据: 在线学习增强选股模型 (2026) 适配器机制

可解释设计约束（v5.0新增，Explainable By Design JFML 2025）:
  ├─ 代码生成阶段即嵌入可解释性约束，而非事后添加SHAP/LIME
  ├─ 约束1: 每个生成的模块必须包含self.explain()方法，返回自然语言解释
  ├─ 约束2: 因子表达式必须附带经济学假设文本（与三重语义一致性的"假设"对齐）
  ├─ 约束3: 模块输出必须包含特征贡献度（至少top-3特征+贡献百分比）
  └─ 与§10.2.9可解释性门控联动：可解释设计→门控验证→部署

AutoML Engine（v7.0新增，裁定✅R-83）:
  ├─ Optuna自动超参搜索+模型选择（随机森林/XGBoost/LightGBM等）
  ├─ 与S4模块创建联动：AutoML自动选择最优模型架构+超参
  ├─ 纯Python，Optuna+scikit-learn+lightgbm
  └─ 依据: Optuna (NeurIPS 2025) 自动ML / AutoML在量化中的应用

Factor Mining Agent（v7.0新增，裁定✅R-84）:
   ├─ LLM并发因子假设生成：一次生成10+因子假设
   ├─ 去重：与已有因子IC相关性<0.7+语义去重
   ├─ 验证：IC测试+Walk-Forward+过拟合检测
   ├─ 与辩论式因子精炼(§5.2 Step 5)联动：Mining Agent生成→辩论精炼→验证
   ├─ ⚠️与§6.1 LLM遗传编程变异算子的关系：Factor Mining Agent从零生成新因子假设（创造），LLM遗传编程变异对已有策略池进行语义引导的变异进化（改良）。两者互补：Mining Agent扩展因子空间边界，遗传编程变异在已有策略邻域内搜索更优解。流水线顺序：Mining Agent生成→辩论精炼→验证→注册→遗传编程变异在策略池中继续进化
   └─ 依据: QuantaAlpha (2026) 进化式因子挖掘 / FactorMAD (ICAIF 2025)

Hypothesis Manager（v7.0新增，裁定✅R-85）:
  ├─ 假设CRUD：创建/读取/更新/删除研究假设
  ├─ 证据关联：假设→证据链(支持/反驳/中性证据)
  ├─ 状态机：提出→验证中→已验证→已推翻→已归档
  ├─ 与S2知识分类联动：每条分类知识包可关联一个或多个假设
  └─ 依据: 科学假设管理 (2025-2026) / QuantaAlpha假设驱动
```

### §9.3 输出契约

```
S4输出: NewModule
  ├─ schema_version: string（输出契约Schema版本，v5.0新增，Event Schema Versioning）
  ├─ module_id: 唯一标识
  ├─ module_type: factor|signal|strategy|risk|model|utility
  ├─ target_layer: 目标层级
  ├─ target_factory: 目标工厂
  ├─ creation_type: UPDATE_PARAM|EXTEND_MODULE|CREATE_NEW
  ├─ design_doc: 模块设计文档
  ├─ code: 模块代码
  ├─ test_code: 单元测试代码
  ├─ config: 接入配置
  ├─ registry_entry: Module Registry注册条目
  ├─ evolution_history: [EvolutionRound] | null（进化式代码生成历史，含每轮代码+回测结果，v4.0新增）
  ├─ agent_feedback_log: [AgentFeedbackRound] | null（分析师Agent反馈记录，v4.0新增）
  ├─ semantic_consistency: SemanticConsistencyResult | null（三重语义一致性验证结果，v4.0新增）
  ├─ trajectory: ResearchTrajectory | null（研究轨迹记录，v4.0新增R-29）
  ├─ adapter_config: AdapterConfig | null（适配器配置，含LoRA权重等，v4.0新增R-30）
  ├─ explainability_design: {self_explain_method: bool, economic_hypothesis: string | null, feature_contribution: [{feature: string, contribution_pct: float}] | null} | null（可解释设计约束输出，v5.0新增）
  ├─ automl_result: {model_type: string, hyperparams: dict, cv_score: float} | null（AutoML Engine自动模型选择+超参搜索结果，v7.0新增R-83）
  ├─ factor_mining_result: {hypotheses: [FactorHypothesis], dedup_result: DedupResult, validation_result: ValidationResult} | null（Factor Mining Agent因子挖掘结果，v7.0新增R-84）
  ├─ hypothesis: {hypothesis_id: string, evidence_chain: [Evidence], status: proposed|verifying|verified|refuted|archived} | null（Hypothesis Manager假设管理输出，v7.0新增R-85）
  ├─ human_review_status: PENDING|APPROVED|REJECTED
  ├─ reviewer: 审核人
  └─ review_notes: 审核意见
```

### 四十六、决策树与强化学习交易决策架构（Decision Tree & RL Trading Decision Architecture）

**架构现状**: 完全缺失。架构有各层模块，但缺乏**从信号到操作的端到端决策流程**——即23个决策节点的标准化流程。

**核心逻辑**: 23节点决策流程是人为设定的固定决策树，不适应市场状态变化。专业机构用**Decision Tree Learning**从数据中自动学习决策规则，或用**Reinforcement Learning**端到端优化交易策略。固定决策树是起点，但最终应被数据驱动的决策架构替代。

**缺失功能**:

#### 46.1 决策树学习

| 功能点 | 量化方法 | 说明 |
|--------|---------|------|
| 特征工程 | 将各模块输出作为决策树输入特征 | 情绪指数/体制概率/资金流/量能/技术指标 |
| 决策树训练 | CART/Random Forest/Gradient Boosting | 从历史数据中自动学习决策规则 |
| 特征重要性 | 各模块输出的特征重要性排名 | 量化各模块的决策贡献度 |
| 剪枝 | CCP(Cost-Complexity Pruning) | 防止过拟合 |

#### 46.2 强化学习优化

| 功能点 | 量化方法 | 说明 |
|--------|---------|------|
| 状态空间 | 各模块输出的连续/离散特征 | 市场状态向量 |
| 动作空间 | 买入/卖出/持有+仓位比例 | 交易动作 |
| 奖励函数 | 风险调整收益（Sharpe/Sortino） | RL优化目标 |
| 策略学习 | PPO/SAC等策略梯度方法 | 端到端优化交易策略 |

#### 46.3 可解释性保障

| 功能点 | 量化方法 | 说明 |
|--------|---------|------|
| SHAP值 | 每个决策的SHAP解释 | Man Group AlphaGPT：每个决策必须可解释 |
| 决策路径可视化 | 决策树的路径可视化 | 理解模型为什么做出某个决策 |
| 人工干预接口 | 关键节点保留人工覆盖 | RL建议+人工确认的混合决策 |

#### 46.4 学术与业界对标

**对标1: Man Group AlphaGPT实践**

每个交易决策必须可解释。用SHAP值解释模型决策。人工干预接口保留在关键节点。

**对标2: Kinlay "RL for Optimal Execution" (2026)**

用PPO在LOBSTER数据上训练最优执行策略，beat TWAP基准。RL端到端优化是交易决策的前沿方向。

**建议归属层**: 决策编排器（决策树/RL策略选择+执行）+ L4 风控层（人工干预接口）

## §10 S6 元学习与自我进化层（源自学习系统架构）

> 来源：学习系统架构 §9 S6 元学习与自我进化层 (lines 1617-1837)
> 对应子模块：MT-05 DriftAdapter（元学习+漂移适应+技能库）

### §10.1 元学习维度（v4.0升级：RSI架构4维度+技能库+在线EWC+轻量Agent化）

```
元学习架构（v4.0）:

1. 学习如何学习 (Learn to Learn) — Prompt自优化循环（STOP模式）
   核心思想: 调整知识提取的LLM prompt和分类策略, 提高提取质量
   ├─ 反馈源: S5试运行结果 → 哪些知识提取后产出了有效模块, 哪些没有
   ├─ 优化对象: LLM提取prompt / 分类规则 / 置信度阈值
   ├─ 方法: A/B测试不同prompt版本 → 保留提取质量更高的版本
   ├─ Prompt自优化（v4.0新增，STOP模式）: LLM分析prompt效果 → 自动生成改进prompt → 人工审核 → 部署
   └─ 约束: prompt变更需人工审核(防止LLM自我优化到不可控)

2. 学习架构优化 (Learn Architecture) — 代码自纠正循环（RISE模式）
   核心思想: 发现更优的模块组合方式, 优化交易流水线架构
   ├─ 反馈源: C-007闭环结果 → 哪些模块组合效果最好
   ├─ 优化对象: 模块间数据流 / 层级间注入规则 / 模块依赖关系
   ├─ 方法: 发现高频模块组合 → 提炼为"复合模块" → 减少数据流跳数
   ├─ 代码自纠正（v4.0新增，RISE模式）: 模块代码运行异常 → LLM自动定位+修正 → 人工审核 → 部署
   └─ 约束: 架构变更需人工审批(防止破坏已有稳定架构)

3. 学习代码生成 (Learn Code Generation) — 技能库积累（Voyager模式）
   核心思想: 提升LLM辅助代码生成的质量, 减少人工审核工作量
   ├─ 反馈源: S4人工审核记录 → 哪些代码被批准, 哪些被拒绝, 拒绝原因
   ├─ 优化对象: 代码生成prompt / 代码模板 / 审核检查清单
   ├─ 方法: 积累"批准代码"和"拒绝代码"的对比样本 → 微调代码生成模型
   ├─ 技能库（v4.0新增，Voyager模式）:
   │   ├─ 成功模块的代码片段/策略模板/因子公式 → 结构化存储
   │   ├─ 新任务优先从技能库检索复用 → 加速收敛
   │   ├─ 经验记忆结构化索引（v4.0新增，FactorMiner arXiv 2026）:
   │   │   ├─ 知识图谱索引: 因子间的语义关系
   │   │   ├─ 因子家族索引: 同族因子的演化历史
   │   │   └─ 市场制度索引: 因子在不同制度下的表现
   │   ├─ 技能结构化三元组格式（v5.0新增，Skill Library ICLR 2025）
   │   │   ├─ 每个技能存储为(条件, 动作, 效果)三元组
   │   │   │   ├─ 条件: 技能适用的市场状态/因子类型/数据条件
   │   │   │   ├─ 动作: 技能的具体执行步骤（代码/公式/参数）
   │   │   │   └─ 效果: 技能执行后的预期效果（IC提升/Sharpe变化）
   │   │   └─ 三元组格式使技能检索更精确（按条件匹配→按效果排序）
   │   │   └─ v6.0增强: 技能依赖解析（dependencies字段→自动编排执行顺序→AutoSkill发现的技能可自动组合为复杂工作流）
   │   ├─ 自动技能发现（v5.0新增，AutoSkill OpenAI 2025）
   │   │   ├─ LLM分析成功/失败的研究轨迹→自动抽象为新技能
   │   │   ├─ 新技能经回测验证后注册到技能库
   │   │   └─ 无需人工定义技能分类，系统自动发现可复用模式
   │   └─ 检索引导新因子生成方向
   └─ 约束: 代码生成模型不可自动部署(需人工验证)

4. 学习方法论 (Learn Methodology) — 元优化器优化自身超参（Meta-Harness）
   核心思想: 从论文/社区中学习新的建模方法, 扩展系统方法论库
   ├─ 反馈源: 学术论文/量化社区/开源项目
   ├─ 优化对象: §29架构增强的方法论库
   ├─ 方法: 定期扫描arXiv/GitHub/量化论坛 → 提取新方法 → 评估适用性
   ├─ Meta-Harness（v4.0新增，Strategy Arena 2026）:
   │   ├─ 优化学习系统自身的超参数: 变异率/匹配阈值/审核策略
   │   ├─ 方法: 学习系统的超参 → A/B测试 → 保留更优配置 → 递归优化
   │   └─ "改进改进能力"：不是优化策略，而是优化优化策略的能力
   └─ 约束: 新方法论需通过§29.20优先级评估后才可进入实现路线

5. MAML快速适应（v5.0新增，Meta-Quant AAAI 2025）
   ├─ 问题: 新市场/新品种上线时，系统需要大量数据才能适应
   ├─ 方法: MAML(Model-Agnostic Meta-Learning)变体
   │   ├─ 在历史市场数据上元训练初始化参数
   │   ├─ 新市场只需5-10 episode即可适应
   │   └─ 适用于S4代码生成模型的快速迁移
   ├─ 与维度7在线EWC的互补关系: MAML提供好的初始化参数（前向迁移），EWC约束参数更新不偏离历史重要知识（后向保持）。两者协同：MAML让新任务快速起步，EWC确保适应新任务时不遗忘旧任务
   └─ v6.0增强: ICL作为元学习替代方案（精心设计prompt含历史成功/失败案例→LLM上下文中适应新市场→无需显式元训练→与维度5 MAML互补：小样本用ICL，大样本用MAML）
   └─ 约束: MAML元训练在RTX 3090上可行（小模型<1M参数）

6. 元反思（v5.0新增，Self-Evolving Agent arXiv 2025）
   ├─ 问题: 当前反思仅改进策略/代码，不改进反思过程本身
   ├─ 方法: 经验回放+反思提炼+技能注册+元反思四步闭环
   │   ├─ 经验回放: 回放历史成功/失败案例
   │   ├─ 反思提炼: 从案例中提炼可复用的反思模式
   │   ├─ 技能注册: 将反思模式注册为可复用技能
   │   └─ 元反思: 反思"反思过程本身"的质量→改进反思策略
   └─ 约束: 元反思频率≤1次/周（防止过度反思消耗资源）

7. 在线EWC防遗忘（v4.0新增，ProAdapt Electronics 2026）
   ├─ 问题: 元学习更新参数时可能遗忘历史知识（灾难性遗忘）
   ├─ 方法: Fisher信息矩阵正则化关键参数
   │   ├─ 计算每个参数对历史任务的重要性（Fisher信息）
   │   ├─ 更新时对重要参数施加正则化约束
   │   └─ 效果: 保留历史知识 + 适应新知识
   └─ 约束: EWC正则化强度由Meta-Harness动态调整

8. 轻量Agent化（v4.0新增，裁定6替代方案）
   ├─ 将S6的4个维度各自封装为独立Agent（逻辑Agent化，非物理分布式）
   │   ├─ PromptOptimizer Agent: 负责维度1
   │   ├─ ArchitectureOptimizer Agent: 负责维度2
   │   ├─ CodeGenerator Agent: 负责维度3
   │   └─ MethodologyLearner Agent: 负责维度4
   ├─ 共享同一进程和GPU内存，通过消息队列协调
   ├─ 优势: Agent分工+模块化，但不突破单机约束
   └─ 约束: 不做MARL训练，不做分布式协调（裁定6❌硬边界门禁）

9. Experiment Tracker（v7.0新增，裁定✅R-92）
   ├─ MLflow/wandb实验追踪：超参记录+指标对比+实验版本管理
   ├─ 与S5试运行联动：每次试运行自动记录为一次实验
   ├─ 与S6元学习联动：实验结果驱动元学习策略调整
   └─ 依据: MLflow (2025) / wandb (2025) 实验追踪最佳实践

10. Walk-Forward Analyzer完整版（v7.0新增，裁定✅R-93）
    ├─ 滚动Walk-Forward：固定窗口滚动（现有S5基础版）
    ├─ 锚定Walk-Forward：训练起点固定，窗口逐步扩展
    ├─ 扩展Walk-Forward：训练窗口逐步扩展+测试窗口固定
    ├─ 与R-59市场状态感知Walk-Forward联动：三种WFA均可叠加市场状态感知
    └─ 依据: Walk-Forward Analysis (Pardo 2025) / AlgoXpert (2026)

11. 过拟合检测扩展（v7.0新增，裁定✅R-94）
    ├─ Bonferroni校正：最保守的多重检验校正
    ├─ FDR(Benjamini-Hochberg)：控制错误发现率，比Bonferroni更宽松
    ├─ BHY(Benjamini-Hochberg-Yekutieli)：考虑因子间相关性，最稳健
    ├─ 与DSR联动：多重检验校正+DSR联合判定过拟合
    └─ 依据: 多重检验校正 (JFE 2025) / Harvey et al. (2016) 因子动物园

12. Look-Ahead Bias Detector（v7.0新增，裁定✅R-95）
    ├─ 时序数据前视偏差自动检测
    ├─ 检测维度：未来数据泄漏/幸存者偏差/重述数据使用
    ├─ 与R-15 Point-in-Time门控联动：Detector检测+PIT门控预防
    └─ 依据: 前视偏差检测 (2025-2026) / 量化回测常见陷阱

13. Signal Confidence Scorer（v7.0新增，裁定✅R-96）
    ├─ Platt Scaling：逻辑回归校准信号置信度
    ├─ Isotonic Regression：保序回归校准信号置信度
    ├─ MC Dropout不确定性：蒙特卡洛Dropout估计预测不确定性
    ├─ 与§10.2.9可解释性门控联动：置信度评分作为可解释性的一部分
    └─ 依据: 概率校准 (Platt 1999 / Niculescu-Mizil 2005) / MC Dropout (Gal 2016)

14. 三层参数优化（v7.0新增，裁定✅R-97）
    ├─ 第1层 实时微调：在线学习微调参数（响应速度最快，幅度最小）
    ├─ 第2层 周期优化：每周/每月周期性参数优化（响应速度中等，幅度中等）
    ├─ 第3层 结构进化：季度级模型结构进化（响应速度最慢，幅度最大）
    ├─ 与S6元学习联动：三层优化由Meta-Harness统一调度
    └─ 依据: 多时间尺度优化 (2025-2026) / ProAdapt分层适应

15. Monte Carlo Engine（v7.0新增，裁定❌R-103）
    ├─ GPU加速蒙特卡洛模拟：策略风险分布估计+压力测试
    └─ 门禁: GPU集群+CUDA并行计算框架就绪

16. VaR Calculator（v7.0新增，裁定❌R-104）
    ├─ 蒙特卡洛VaR计算：基于GPU加速蒙特卡洛的VaR/CVaR估计
    └─ 门禁: GPU集群+CUDA并行计算框架就绪

17. Market Digital Twin（v7.0新增，裁定❌R-105）
    ├─ 代理人引擎+订单簿仿真：模拟市场微观结构
    └─ 门禁: 多机集群+高频数据源接入就绪

18. 数字孪生系列（v7.0新增，裁定❌R-106）
    ├─ 依赖图/实时同步/混沌实验：系统级数字孪生
    └─ 门禁: 多机集群+实时数据同步框架就绪
```

### §10.2 学习效果反馈闭环

```
学习效果反馈闭环:

交易流水线 → 学习系统（4条外部反馈路径的接口定义与频率见§11.2效果反馈接口）:
  ├─ C-010复盘数据 → "注入的知识是否有效"
  ├─ C-007闭环结果 → "学习策略是否需要调整"
  ├─ C-022数据质量报告 → "数据源优先级是否需要调整"
  └─ C-033过拟合报告 → "是否标记为过拟合知识"

学习系统内部闭环:
  ├─ S5试运行结果 → S2知识提取策略调整
  ├─ S5试运行结果 → S3模块匹配策略调整
  ├─ S4审核记录 → S4代码生成策略调整
  ├─ S5长期效果 → S0采集策略调整(增减数据源)
  ├─ 新市场/新品种上线 → S6 MAML快速适应(v5.0新增): 元训练初始化参数→5-10 episode适应
  ├─ 历史成功/失败案例 → S6元反思闭环(v5.0新增): 经验回放→反思提炼→技能注册→元反思
  ├─ 研究轨迹分析 → S6 AutoSkill自动技能发现(v5.0新增): 轨迹→抽象技能→回测验证→注册技能库
  └─ 技能库检索 → S6技能三元组匹配(v5.0新增): 按条件匹配→按效果排序→引导生成方向
  ├─ 新市场/新品种上线 → S6 ICL元学习(v6.0新增): 精心设计prompt含历史案例→LLM上下文中适应→与MAML互补
  └─ 技能依赖图分析 → S6技能依赖解析(v6.0新增): 依赖解析→自动编排执行顺序→AutoSkill技能可组合为复杂工作流
  ├─ 实验结果分析 → S6 Experiment Tracker(v7.0新增): 实验追踪→超参记录→指标对比→驱动元学习策略调整
  ├─ Walk-Forward分析结果 → S6 Walk-Forward Analyzer完整版(v7.0新增): 滚动/锚定/扩展三种WFA结果→策略验证反馈→参数优化方向
  ├─ 多重检验结果 → S6过拟合检测扩展(v7.0新增): Bonferroni/FDR/BHY校正→与DSR联合判定→过拟合知识标记
  ├─ 前视偏差检测 → S6 Look-Ahead Bias Detector(v7.0新增): 前视偏差检测→数据质量反馈→与PIT门控联动预防
  ├─ 信号置信度评估 → S6 Signal Confidence Scorer(v7.0新增): Platt/Isotonic校准+MC Dropout不确定性→信号质量反馈→可解释性门控输入
  └─ 三层优化结果 → S6三层参数优化(v7.0新增): 实时微调/周期优化/结构进化三层→Meta-Harness统一调度→参数优化闭环
```

### 四十二、交易绩效归因与策略退化检测模型（Performance Attribution & Strategy Degradation Detection Model）

**架构现状**: 完全缺失。"交易绩效监控"无量化框架。

**核心逻辑**: 交易绩效监控不只是"看盈亏"，而是**Performance Attribution**（归因分析）+ **Strategy Degradation Detection**（策略退化检测）。因子IC衰减=策略退化，需要自动检测并降权。

**缺失功能**:

#### 42.1 绩效归因

| 功能点 | 量化方法 | 说明 |
|--------|---------|------|
| 收益归因 | Brinson模型：配置效应+选择效应+交互效应 | 分解收益来源 |
| 因子归因 | 各因子对组合收益的贡献 | 识别哪个因子在赚钱/亏钱 |
| 风险归因 | 各因子对组合风险的贡献 | 识别风险来源 |

#### 42.2 策略退化检测

| 功能点 | 量化方法 | 说明 |
|--------|---------|------|
| IC衰减检测 | 因子IC的60日移动平均趋势 | IC衰减>50%=策略退化 |
| 拥挤度检测 | 使用同一策略的参与者数量估计 | 拥挤度上升=超额收益将消失 |
| 自动降权 | 策略退化时自动将权重降为0 | Man Group AlphaGPT实践 |

#### 42.3 学术与业界对标

**对标1: Brinson "Determinants of Portfolio Performance" (1986, FAJ)**

Brinson模型是绩效归因的学术标准。分解为配置效应+选择效应。

**对标2: Man Group AlphaGPT实践**

因子失效时自动降权至0。每个信号的权重必须可解释。

**建议归属层**: L4 风控层（绩效归因+退化检测）+ 模块48（动态信号权重联动）

---

## §8 运维架构(A9)规格

> **搬入来源**: 运维架构(A9) §1.3 GPU调度策略(训练视角) + §2.4 Cold平面(ML训练) + §5灾备架构(模型备份)
> **搬入原则**: 将A9中D-ML-TRAIN域承载的运维规格搬入本域，保持A9原文颗粒度。

### §8.1 GPU调度——训练视角（A9§1.3）

| 时段 | GPU模式 | 显存占用 | 训练任务 | 切换触发 |
|------|---------|:--------:|---------|---------|
| 盘前(08:30-09:00) | 推理模型加载 | 8-10GB | ❌禁止训练 | 定时加载推理模型 |
| 盘中(09:15-15:00) | 推理优先 | 8-10GB | ❌禁止训练 | HC-01交易时段保护 |
| 午休(11:30-13:00) | LLM最小集+轻量训练 | 4GB | ⚠️轻量训练(小模型) | 定时切换 |
| 盘后(15:00-15:30) | 推理→训练切换中 | 4GB→16-18GB | 训练任务加载中 | 定时切换 |
| 夜间(15:30-08:30) | 训练优先 | 16-18GB | ✅全量训练 | 定时切换 |

> **训练硬约束**：交易时段(09:15-15:00)GPU显存0GB分配给训练进程(P5)。训练模型权重保存在CPU RAM(热备)，盘中推理模型卸载后可~5s恢复。

### §8.2 Cold平面ML训练资源限制（A9§2.4）

| 资源 | 限制 | 执行方式 |
|------|------|---------|
| CPU | 最多使用核16-19 | P5进程CPU亲和性设置 |
| GPU | 盘中0GB/盘后16-18GB | GPU调度器显存配额 |
| 内存 | 最多20GB | 进程内存硬限制 |
| 磁盘IO | 低优先级 | Windows进程优先级BelowNormal |

### §8.3 模型备份与版本管理（A9§5）

| 备份对象 | 同步方式 | 同步频率 | 保留策略 |
|---------|---------|:--------:|---------|
| 模型权重文件 | robocopy增量 | 每小时 | 保留最近10个版本 |
| 训练参数+超参 | Git版本管理 | 每次训练 | 保留全部历史 |
| 训练数据集引用 | 配置文件同步 | 每次变更 | 保留最近30个版本 |

## 来自Agent架构(A7)的内容

### 来自Agent架构(A7) §6.6 策略自我修正闭环 — 训练反馈闭环

> 源自Agent架构(A7) §6.6 策略自我修正闭环。训练域是策略自我修正闭环中"修正应用"和"修正效果验证"的执行者——MT-01 TrainingPipeline执行参数微调(ai_modifiable)和模型重训，MT-02 ExperimentTracker执行修正效果验证。

```
┌──────────────────────────────────────────────────────────────┐
│                    策略自我修正闭环                             │
│                                                              │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 执行    │───→│ 评估    │───→│ 反思    │───→│ 修正    │  │
│  │ (Actor) │    │(Evaluator)│  │(Reflector)│  │(Fixer)  │  │
│  └─────────┘    └─────────┘    └─────────┘    └────┬────┘  │
│       ↑                                             │       │
│       │              ┌──────────────┐               │       │
│       └──────────────│ 修正应用     │←──────────────┘       │
│                      │              │                       │
│                      │ · ai_modifiable: 自动应用            │
│                      │ · human_gated: 提交审批              │
│                      │ · immutable: 仅记录建议              │
│                      └──────────────┘                       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              修正效果验证                             │   │
│  │  · 修正后执行N次同类任务（N=5）                       │   │
│  │  · 对比修正前后效果（Sharpe/IC/准确率）               │   │
│  │  · 改善>阈值→确认修正；改善<阈值→回滚                │   │
│  │  · 确认修正→写入情景记忆→更新Agent Card              │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

| 修正类型 | 应用方式 | 训练域执行 | 验证要求 | 回滚条件 |
|---------|---------|-----------|---------|---------|
| 参数微调（ai_modifiable） | 自动应用 | MT-01 TrainingPipeline执行参数更新 | 5次执行后验证 | 效果下降>5%→回滚 |
| 规则调整（human_gated） | 提交审批后应用 | MT-01执行重训+MT-02实验追踪 | 回测V3+V4门禁 | 审批拒绝→不应用 |
| 策略优化建议（immutable） | 仅记录，不自动应用 | MT-02 ExperimentTracker记录 | 人工评估 | — |

### 来自Agent架构(A7) §6.7 与学习系统架构(A8)的接口 — 完整搬入

> 源自Agent架构(A7) §6.7 与学习系统架构(A8)的接口。训练域是A7→A8接口的核心执行者——反思轨迹上报、优化建议下发、情景记忆同步、能力演进请求均涉及训练域子模块。

| 接口 | 方向 | 数据格式 | 频率 | 训练域执行 |
|------|------|---------|------|----------|
| 反思轨迹上报 | A7→A8 | JSON（轨迹+反思+修正） | 每次L1反思后 | MT-02 ExperimentTracker记录反思轨迹→MT-01 TrainingPipeline消费作为重训输入 |
| 优化建议下发 | A8→A7 | JSON（优化建议+验证结果） | A8学习周期完成后 | MT-02验证优化建议→MT-01执行模型/参数更新 |
| 情景记忆同步 | A7↔A8 | 向量嵌入（FAISS索引） | 日度 | MT-05 DriftAdapter消费情景记忆→判断漂移类型→触发重训练 |
| 能力演进请求 | A7→A8 | SKILL.md草案 | 新能力提案时 | MT-01 TrainingPipeline执行新能力训练→MT-02验证→注册 |

**A7→A8接口与训练域子模块的映射**：

| A7→A8接口 | 训练域子模块 | 执行方式 |
|----------|------------|---------|
| 反思轨迹上报 | MT-01 + MT-02 | 反思轨迹→MT-02记录→MT-01消费作为重训数据 |
| 优化建议下发 | MT-01 + MT-02 | A8优化建议→MT-02验证→MT-01执行模型/参数更新 |
| 情景记忆同步 | MT-05 | 情景记忆→MT-05消费→判断漂移类型→触发重训练或参数调整 |
| 能力演进请求 | MT-01 + MT-03 | 新能力需求→MT-03 AutoML选择模型架构→MT-01训练→MT-02验证 |

### 来自Agent架构(A7) §9.2.2 Agent→业务功能域消费映射 — 与训练域相关的Agent映射

> 源自Agent架构(A7) §9.2.2。训练域(D-ML-TRAIN)是多个Agent架构组件的辅助域——自反Agent和Agent冷启动均涉及训练域。

| 架构组件 | 对应功能域（主域+辅助域） | 与D-ML-TRAIN的关系 |
|----------|------------------------|------------------|
| 自反Agent | D-AUTONOMY-CORE（主域）+ **D-ML-TRAIN** + D-KNOWLEDGE | 辅助域：反思轨迹→A8学习系统→MT-01重训；优化建议→MT-01执行模型更新 |
| Agent冷启动 | D-AUTONOMY-CORE（主域）+ **D-ML-TRAIN** | 辅助域：SKILL.md→MT-01能力验证→MT-02注册→上线 |

**Agent→业务功能域消费映射（训练域视角）**：

| Agent | 消费域 | 产出域 | 与D-ML-TRAIN的关系 |
|-------|--------|--------|------------------|
| 研究Agent | D-DATA、D-ALT-DATA🔴、D-KNOWLEDGE | D-FACTOR、D-KNOWLEDGE | 间接：研究Agent产出新因子提案→MT-04 FeatureDiscovery消费 |
| 归因Agent | D-DATA、D-REPORTING | D-REPORTING、D-PF-CORE | 间接：归因Agent产出优化建议→经A7→A8接口→MT-01重训 |

### 来自Agent架构(A7) §17 遗留问题裁定 — 与训练域相关的条目

> 源自Agent架构(A7) §17 遗留问题裁定。以下LP条目与训练域直接相关——涉及GPU显存、模型训练、影子测试等训练域核心约束。

| 编号 | 遗留问题 | 裁定 | 硬边界门禁条件 | 与D-ML-TRAIN的关系 |
|:----:|---------|:----:|--------------|------------------|
| LP-002 | Agent记忆向量检索(RAG) | 🔴 暂缓(不能建) | ①GPU显存≥48GB；②需要非结构化数据语义检索；③AUM≥500万 | GPU显存约束直接影响MT-01训练资源分配；嵌入模型需~2GB显存挤占训练配额 |
| LP-004 | 影子模式测试 | 🔴 暂缓(不能建) | ①GPU显存≥48GB；②多GPU架构；③或战略Agent影子测试走API推理 | 影子模式是MT-02→MS-04模型验证的关键环节，GPU限制导致无法本地并行运行新旧模型对比 |
| LP-006 | 混沌工程环境 | 🟢 能建 | —（在仿真环境D-SIMULATION执行） | 混沌实验可验证MT-01训练Pipeline的容错性，在仿真环境中用历史行情回放模拟 |
| LP-014 | MCP×A2A集成框架 | 🔴 暂缓(不能建) | ①外部工具≥10个需MCP统一管理；②多Agent框架互操作需求；③有第二位开发人员加入 | MCP集成可能影响MT-01/MT-03的外部工具调用方式，MVP阶段用Python SDK直接调用 |

**LP-002 Agent记忆向量检索(RAG)详情**：

| 维度 | 说明 |
|------|------|
| MVP替代方案 | SQLite全文检索(FTS5)+Redis缓存；语义记忆用结构化表（因子定义/策略规则/市场状态映射） |
| 不能建的硬边界理由 | 约束二(硬件)：GPU显存盘中8-10GB，嵌入模型需~2GB，挤占本地LLM空间；量化系统记忆核心是结构化数据不是自然语言 |
| 未来开通门禁 | ①GPU显存≥48GB；②需要非结构化数据语义检索时；③AUM≥500万需更深度知识管理时 |

**LP-004 影子模式测试详情**：

| 维度 | 说明 |
|------|------|
| MVP替代方案 | 金丝雀部署：新版本Agent先处理5%请求，监控决策质量，逐步扩大到100%。战略Agent升级用API推理做影子对比（不占本地GPU） |
| 不能建的硬边界理由 | 约束二(硬件)：GPU显存盘中8-10GB，影子模式需额外8-10GB，总计16-20GB接近24GB上限 |
| 未来开通门禁 | ①GPU显存≥48GB；②多GPU架构；③或战略Agent影子测试全部走API推理 |

**LP-006 混沌工程环境详情**：

| 维度 | 说明 |
|------|------|
| MVP实现方式 | 在仿真环境(D-SIMULATION)执行混沌实验，用历史行情回放模拟真实条件；非交易时段可在生产环境做轻量级测试（如Agent进程崩溃恢复） |
| 能建的硬边界依据 | 约束五(RTO<5分钟)：混沌实验不影响生产；约束三(miniQMT模拟盘可用) |
| 生产镜像环境 | 不能建——约束二(单机部署)限制，需第二台PC或云服务器 |

**LP-014 MCP×A2A集成框架详情**：

| 维度 | 说明 |
|------|------|
| MVP替代方案 | A2A协议处理Agent间通信，外部工具调用直接通过Python SDK（无MCP中间层） |
| 不能建的硬边界理由 | 约束一(单人开发)：MCP+A2A双协议集成增加开发复杂度；约束二(单机部署)：MCP主要解决多框架互操作，单框架无需 |
| 未来开通门禁 | ①外部工具≥10个需MCP统一管理；②多Agent框架互操作需求；③有第二位开发人员加入 |


---

## §11 合规约束（源自A6合规架构）

> **搬入来源**: 合规架构(A6) §4.3模型注册与治理+§4.2可解释性要求+§8.3合规测试框架+§7法规映射
> **搬入原则**: 筛选与ML训练直接相关的合规约束，从D-ML-TRAIN视角重写。

### §11.1 模型注册与治理——训练视角

> 对标合规架构§4.3模型注册与治理，训练域是模型注册表的核心生产者。

| 合规要求 | 约束 | 实现方式 | 当前状态 |
|---------|------|---------|---------|
| 训练数据指纹 | 每次训练须生成训练数据集SHA-256指纹，写入模型注册表training_data_hash字段 | 数据集加载时自动计算SHA-256+写入注册表（不可变） | ✅能建 |
| 模型版本管理 | 模型代码+参数+训练数据指纹绑定，版本不可变 | 语义化版本号+code_hash+param_hash+training_data_hash四元组 | ✅能建 |
| 训练审计日志 | 每次训练的完整参数+数据集版本+性能指标+审批记录 | 训练日志写入模型日志（≥5年保留，哈希链保护） | ✅能建 |
| 参数变更监控 | 模型参数变更须独立审批+实时监控未授权变更 | 参数存储区读写分离+变更告警（Two Sigma教训） | ✅能建 |
| 模型生命周期门禁 | 开发→注册→验证→审批→上线→监控→退役全流程合规 | 影子模式运行≥5交易日+人工审批必须+漂移检测 | ✅能建 |

### §11.2 可解释性要求——训练视角

> 对标合规架构§4.2可解释性要求，训练域须产出可解释性证据。

| 合规要求 | 约束 | 实现方式 | 当前状态 |
|---------|------|---------|---------|
| 特征重要性排序 | 训练完成的模型须提供全局特征重要性排序 | SHAP摘要图+特征重要性排名 | ✅能建 |
| 训练数据偏差检测 | 训练数据须评估代表性偏差（行业偏差/市值偏差/时间偏差） | 偏差评估报告+代表性统计+与基准分布对比 | ✅能建 |
| Tier分层可解释性 | 按模型风险层级提供不同深度的可解释性 | Tier1:SHAP+LIME双归因; Tier2:Top-5特征; Tier3:全局排序 | ✅能建 |
| SHAP+LIME双归因 | 交易决策模型须同时提供SHAP全局+LIME局部解释 | LIME实时(<12ms)+SHAP盘后批量 | ✅能建 |

### §11.3 合规测试框架——模型验证视角

> 对标合规架构§8.3合规测试框架，训练域须满足模型验证门禁。

| 测试类型 | 内容 | 频率 | 自动化 |
|---------|------|------|--------|
| 回溯测试合规 | 训练模型须通过历史数据回测验证，PIT一致性检查 | 每次模型上线前 | ✅ |
| 模型验证门禁 | V3+V4验证通过后方可审批上线 | 每次模型上线前 | ✅ |
| 训练数据审计 | 确保训练数据不含内幕信息+偏差在可接受范围 | 每次训练 | ✅ |
| 漂移检测 | 模型性能退化告警+数据漂移检测 | 持续 | ✅ |
| 合规压力测试 | 极端场景下模型表现验证 | 每季度 | ✅ |

### §11.4 法规映射——AI训练相关

| 编号 | 法规 | 关键条款 | 对D-ML-TRAIN的影响 | 合规义务 |
|:----:|------|---------|-------------------|---------|
| INT-001 | EU AI Act | Art.10数据治理:训练数据的质量/相关性/代表性→偏差评估 | GATE-006激活后训练数据须满足Art.10数据治理要求 | 训练数据偏差评估报告+数据质量文档 |
| INT-009 | SR 26-2 | 模型风险管理三支柱:开发/验证/治理 | 参考性（适用门槛总资产>$30B），三支柱框架原则可参考 | 模型注册表+生命周期门禁+独立验证 |
| CN-008 | 《生成式AI服务管理暂行办法》 | AI生成内容需标识+提供者备案 | 个人使用不触发备案，训练流程须可审计 | 训练日志+模型版本可追溯 |

## 数据架构域模块补充

> **📦搬入来源**: 数据架构 v6.0 §17.5

### §17.5 D-ML-TRAIN/D-ML-SERVE 训练/推理域缺失模块 — D-ML-TRAIN部分

| 模块ID | 模块名称 | 功能简述 | 二元结论 | 蓝图备注 |
|--------|---------|---------|---------|---------|
| D-ML-TRAIN-01 | Training Dataset Manager | 训练数据集管理(多源训练数据集+数据版本+数据血缘+数据质量+数据分割) | ✅能建。在Feature Registry中增加training_dataset表，记录每个训练数据集的因子版本+时间范围+分割策略。与§11.2.5版本历史对齐 | 📐项目内有蓝图编号MOD-L11-001已建设(部分) |
