# --- 治理锚定 ---
# blueprint: MOD-INF-035 | docs/03_modules/_cross_layer/auto-runtime-core/blueprint.md | §
# module_id: MOD-INF-035
# stability: evolving
# safety_level: M
# ai_autonomy: ai_modifiable
# ttl: permanent
# --- 治理锚定结束 ---
module_id: MOD-INF-035
capability_id: a2a-protocol
category: coordination
description: Agent-to-Agent coordination protocol (MOD-INF-025) — discovery, communication,
  conflict resolution, arbitration, and consensus
examples:
- description: a2a agent discovery and registration
  action: discover
- description: cross-agent message routing and handoff
  action: send_message
- description: conflict detection and arbitration
  action: detect_conflict
- description: negotiation, voting, and debate
  action: negotiate
input_schema:
  properties:
    action:
      enum:
      - discover
      - register
      - send_message
      - detect_conflict
      - arbitrate
      - negotiate
      - vote
      - debate
      type: string
    agent_id:
      type: string
    payload:
      type: object
  type: object
name: A2A Protocol
output_schema:
  properties:
    result:
      type: object
    status:
      type: string
  type: object
priority: P1
registered_at: '2026-05-08T22:00:00.000000'
requires_human: false
runtime_plane: warm
status: ACTIVE
tags:
- a2a
- agent-to-agent
- coordination
- conflict-resolution
- arbitration
- consensus
- discovery
