# demo_e2e_pipeline（L00 AkshareProvider）可选依赖——与 pyproject [project.optional-dependencies] demo 对齐
# clickhouse-driver 已升级为核心依赖（见 requirements.txt），此处仅保留真可选数据源
-r requirements.txt
akshare>=1.12.0
baostock>=0.8.8,<0.9.0          # 交易日历/沪深300成分股（baostock_provider）
