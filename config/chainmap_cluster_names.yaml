# ── chainmap L1 族名 override（二期 Commit C 规则版，Owner 2026-09-09）──
# 语义：key=簇 id（C01~C48，/api/chainmap-galaxy 输出）；value=归一后族名（<=6 字）。
# 优先级：本文件条目 > 自动族名（簇内枢纽链名去括号+"族"）；未列出的簇继续用自动族名。
# 生效：mtime 缓存——改本文件即失效重算（/api/tdm 同款机制），无需重启 api_server。
# 演进：Commit C LLM 版（OllamaChat 离线批量）另行派单；产业链数据库层改名 61 条标题腔链后
#       自动族名基线会漂移，届时再批量生成（本次只登记机制，禁止提前生成——Owner 收口指令）。
#
# 示例（取消注释即生效）：
# C01: AIoT
# C02: 半导体
