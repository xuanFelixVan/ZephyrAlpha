# [BLUEPRINT] MOD-INF-016 | docs/03_modules/_cross_layer/shared_core/blueprint.md | §
# [MODULE] zephyr.shared.api.api_index
# [DOMAIN] D_SHARED
# [DEPENDENCIES]
# [CONSUMERS]
# [STARTUP] imported
# [MATURITY] prototype
# [INVARIANTS] none
# [MODIFY-GUARD] none
# [STABILITY] evolving
# [SAFETY] L
# [AI_AUTONOMY] ai_modifiable
# [ERROR_CONTRACT]
# [TESTS]
# [A_module] module_id=MOD-SHR_api_index | layer=module | stability=evolving | safety=L | ai_autonomy=ai_modifiable
# [TTL] permanent

"""
shared/ API 索引 — AI session 冷启动时的"员工通讯录"

本文件列出 src/zephyr/shared/ 下所有公开 API。
AI 创建新代码前 MUST 先对照此索引，确认功能是否已有 SSoT 实现。

对标：AGENTS.md §8.2.1 _shared/ API 速查目录（scripts 侧）
      Google Kythe 全局代码索引（让开发者/AI 发现已有代码）
      Anthropic CLAUDE.md 搜索优先原则

使用方式：
  1. 需要路径常量？-> 查 paths.py
  2. 需要时间函数？-> 查 time_utils.py
  3. 需要解析 frontmatter？-> 查 frontmatter_utils.py
  4. 需要估算 token？-> 查 token_utils.py
  5. 需要计算内容指纹？-> 查 content_fingerprint.py
  6. 需要金融契约？-> 查 contracts/money.py, contracts/instrument.py
  7. 需要其他？-> 先 grep 此目录再决定是否新建

禁止行为（D-D-07 / AGENTS.md §8.2.1）：
  ❌ 在业务代码中本地重定义以下任何符号
  ❌ 用 Path(__file__).parents[N] 计算 REPO_ROOT
  ❌ 用 datetime.now(timezone.utc).isoformat() 代替 now_iso()
  ❌ 用 content.find("---", 3) + yaml.safe_load 代替 parse_frontmatter()
  ❌ 用 len(text) // 4 代替 estimate_tokens()
"""

# ═══════════════════════════════════════════════════════════════════════════
# paths.py — 项目路径常量 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.io.paths import ...
#
# ┌──────────────────────┬──────────────────────────────────────────────────┐
# │ 符号                  │ 用途                                            │
# ├──────────────────────┼──────────────────────────────────────────────────┤
# │ find_repo_root()     │ 从任意位置查找项目根目录（不依赖文件深度）        │
# │ REPO_ROOT: Path      │ 项目根目录绝对路径                               │
# │ DB_DIR: Path         │ 数据库目录 (data)                                 │
# │ DB_PATH: Path        │ SQLite 数据库文件路径                            │
# │ GATES_DIR: Path      │ 门禁 YAML 目录                                  │
# │ SNAPSHOTS_DIR: Path  │ 运行时快照目录                                   │
# │ RATIONALE_LOG_PATH   │ 架构决策日志路径                                 │
# │ VECTOR_INDEX_DIR     │ 向量索引缓存目录                                 │
# │ MODELS_CACHE_DIR     │ 模型缓存目录                                    │
# └──────────────────────┴──────────────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# time_utils.py — 时间工具函数 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.utils.time_utils import ...
#
# ┌──────────────────────┬──────────────────────────────────────────────────┐
# │ 符号                  │ 用途                                            │
# ├──────────────────────┼──────────────────────────────────────────────────┤
# │ now_utc() -> datetime │ 当前 UTC 时间（可 mock）                        │
# │ now_iso() -> str      │ 当前 UTC ISO 8601 字符串                        │
# │ default_now() -> dt   │ Pydantic Field(default_factory=...) 工厂        │
# └──────────────────────┴──────────────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# frontmatter_utils.py — Markdown/YAML frontmatter 解析 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.io.frontmatter_utils import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ parse_frontmatter(content)   │ 解析 Markdown YAML frontmatter -> dict   │
# │ parse_frontmatter_from_file  │ 从文件路径解析 frontmatter -> dict       │
# │ parse_yaml_header(content)   │ 解析 YAML 文件顶层字段 -> dict           │
# │ extract_body(content)        │ 提取 frontmatter 之后的正文 -> str       │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# token_utils.py — Token 估算 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.infrastructure.capacity_assurance.token_budget import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ estimate_tokens(text) -> int │ 估算文本 token 数（1 token ≈ 4 字符）   │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# content_fingerprint.py — 内容指纹 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.io.content_fingerprint import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ compute_hash(filepath) -> str│ 计算文件 SHA-256 指纹                    │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# schemas.py — 核心数据模型 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.schema.schemas import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ BASE_CONFIG                  │ Pydantic model_config 基础配置           │
# │ Task                         │ 任务 Pydantic 模型                       │
# │ AuditReport                  │ 审计报告模型                             │
# │ KnowledgeEntry               │ 知识条目模型                             │
# │ FailurePattern               │ 失败模式模型                             │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# capability.py — 能力管控 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.security.capability import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ Capability                   │ 能力枚举                                 │
# │ CapabilityDenied             │ 能力拒绝异常                             │
# │ CapabilityRegistry           │ 能力注册表（单例）                       │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# ssot_guard.py — SSoT 守卫 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.security.ssot_guard import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ SsotGuard                    │ SSoT 一致性守卫                          │
# │ CheckResult                  │ 检查结果                                 │
# │ GuardReport                  │ 守卫报告                                 │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# dos_launcher.py — 指令加载器 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.api.dos_launcher import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ DOSLauncher                  │ 指令加载器                               │
# │ DOSResult                    │ 指令执行结果                             │
# │ DirectiveInfo                │ 指令元信息                               │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# observer.py — 观察者模式 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.infra.observer import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ EventBus                     │ 事件总线                                 │
# │ Observer                     │ 观察者接口                               │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# contracts/ — 金融契约 SSoT
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.contracts.portfolio.money import Money, MoneyCurrencyMismatchError, MoneyPrecisionError
# from zephyr.shared.contracts.market.instrument import Instrument, Stock, ETF, Future, Option, Bond, FX, Crypto
# from zephyr.shared.contracts.core.runtime_plane_tag import RuntimePlaneTag
# from zephyr.shared.contracts.core.timestamp import utc_now_iso, parse_iso_timestamp

# ═══════════════════════════════════════════════════════════════════════════
# errors.py — 统一错误层次（Phase 1 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.foundation.errors import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ ZephyrBaseError              │ 所有业务异常的根                         │
# │ ConfigError                  │ 配置系统错误                             │
# │ ContractError                │ 数据契约错误                             │
# │ SecurityError                │ 安全相关错误                             │
# │ ValidationError              │ 输入校验错误                             │
# │ TaskError                    │ 任务系统错误                             │
# │ PipelineError                │ 管线错误                                 │
# │ GateError                    │ 门禁错误                                 │
# │ ContextError                 │ 上下文引擎错误                           │
# │ FeedbackError                │ 反馈循环错误                             │
# │ DataError                    │ 数据层错误                               │
# │ IOError                      │ I/O 错误                                │
# │ UnimplementedError           │ 施工占位                                 │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# constants.py — 共享枚举集中 re-export（Phase 1 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.foundation.constants import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ AssetClass                   │ 资产大类（stock/bond/fx/crypto/...）      │
# │ OrderSide / OrderType / ...  │ 订单相关枚举                             │
# │ EventType                    │ Observer 事件类型枚举                    │
# │ TaskStatus / KeCategory / .. │ 核心模型枚举                             │
# │ RuntimePlane / HOT_PATH_...  │ 运行时平面 + 延迟预算常量                │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# events/event_schemas.py — 事件体 Pydantic Schema（Phase 1 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.events import ...
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ FileEventPayload             │ FILE_EVENT 事件体 Schema                 │
# │ TimeEventPayload             │ TIME_EVENT 事件体 Schema                 │
# │ TaskEventPayload             │ TASK_EVENT 事件体 Schema                 │
# │ ManualEventPayload           │ MANUAL_EVENT 事件体 Schema               │
# │ MetricEventPayload           │ METRIC_EVENT 事件体 Schema               │
# │ EVENT_PAYLOAD_MAP            │ EventType -> Schema 映射字典              │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# resilience/retry.py — 重试策略（Phase 2 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.resilience import async_retry, RetryConfig
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ async_retry                  │ 异步重试装饰器（指数退避+jitter）         │
# │ RetryConfig                  │ 重试配置（max_attempts/delay/异常白名单） │
# │ RetryExhaustedError          │ 重试耗尽异常                             │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# resilience/circuit_breaker.py — 熔断器（Phase 2 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.resilience import CircuitBreaker, CircuitOpenError
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ CircuitBreaker               │ 三态熔断器（CLOSED/OPEN/HALF_OPEN）       │
# │ CircuitOpenError             │ 熔断器 OPEN 时拒绝调用抛出               │
# │ CircuitState                 │ 熔断状态枚举                             │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# resilience/fallback.py — 降级策略（Phase 2 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.resilience import FallbackChain, fallback
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ FallbackChain                │ 降级策略链（按序尝试）                    │
# │ FallbackExhaustedError       │ 降级链全部耗尽异常                       │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# lifecycle/hooks.py — 模块生命周期（Phase 2 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.lifecycle import LifecycleAware, LifecycleManager
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ LifecycleAware               │ 模块生命周期 Protocol                    │
# │ LifecycleManager             │ 生命周期编排器（注册/启动/关闭/健康检查） │
# │ LifecycleState               │ 生命周期状态枚举                         │
# │ ModuleHealth                 │ 模块健康检查结果                         │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# flags.py — FeatureFlag 开关（Phase 2 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.foundation.flags import global_flag_registry, FeatureFlag
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ FeatureFlag                  │ 功能开关定义                             │
# │ FlagRegistry                 │ 全局开关注册表（单例）                    │
# │ FlagState                    │ ALWAYS_ON / CONDITIONAL / ALWAYS_OFF      │
# │ global_flag_registry         │ 全局 FlagRegistry 实例                   │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# types.py — 语义化类型别名（Phase 3 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.foundation.types import TaskId, ModuleId, FilePath, SessionId
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ TaskId / ModuleId / SessionId│ 语义化 NewType（mypy 区分 str 子类型）   │
# │ FilePath / AbsPath            │ 相对/绝对路径类型别名                   │
# │ AgentId / ContractId          │ Agent / 契约标识符                      │
# │ FingerprintHash / TokenCount  │ Hash / Token 计数类型                   │
# │ BlueprintVersion / DocumentId │ 蓝图版本 / 文档标识                     │
# │ MetricName / SSoT_Key         │ 指标名 / SSoT 注册键                    │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# diff_utils.py — diff/patch 工具（Phase 3 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.utils.diff_utils import compute_diff, apply_patch
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ compute_diff                  │ 计算两个字符串的 unified diff            │
# │ compute_file_diff             │ 计算两个文件的 unified diff              │
# │ apply_patch                   │ 应用 patch（strict/fuzzy）              │
# │ try_apply_patch               │ 安全尝试应用 patch                       │
# │ similarity_ratio              │ 字符串相似度 (0.0~1.0)                  │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# file_utils.py — 安全文件操作（Phase 3 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.io.file_utils import atomic_write, backup_and_rollback
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ atomic_write                  │ POSIX 原子写入（.tmp->rename）            │
# │ safe_read                     │ 安全读取 + 可选 SHA-256 校验             │
# │ backup_file                   │ 创建文件备份（轮转 .bak.N）              │
# │ restore_backup                │ 从备份恢复文件                           │
# │ backup_and_rollback           │ 上下文管理器——异常自动回滚               │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# logging.py — 结构化日志系统（Phase 4 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.utils.logging import get_logger, TraceContext, configure_root_logger
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ ZephyrLogger                 │ 结构化 JSON 日志器                        │
# │ get_logger(name, **ctx)      │ 工厂函数——获取/创建 + 注入 module_id      │
# │ TraceContext                 │ trace_id 传播上下文管理器                 │
# │ configure_root_logger        │ 应用入口一次性配置（控制台+文件双模式）     │
# │ trace_id_var / session_id_var│ contextvars——跨调用链自动传播             │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# shared-quickref.yaml — AI 零歧义快速参考（Phase 4 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# 文件路径: src/zephyr/shared/shared-quickref.yaml
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 段落                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ foundation                   │ 数据契约 + 错误层次 + 类型别名 + 常量    │
# │ utilities                    │ 路径/时间/Token/Frontmatter/diff/文件/配置│
# │ resilience                   │ 重试/熔断/降级 + 生命周期 + FeatureFlag   │
# │ events                       │ EventBus + 5 事件体 Schema               │
# │ logging                      │ 结构化日志使用方法 + 模式示例             │
# │ core                         │ models + blueprint_decomposer            │
# │ consumers                    │ 6 消费者兼容性矩阵                        │
# │ contract_tests               │ 契约测试入口命令                         │
# │ common_traps                 │ 5 大常见 AI 施工陷阱 + 纠正方案           │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# testing.py — 测试夹具/工厂（Phase 5 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.utils.testing import make_valid_task, make_p0_task, make_completed_task
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ make_valid_task              │ 构造 valid-by-construction Task 实例     │
# │ make_p0_task                 │ 构造 P0 + H 安全等级紧急任务             │
# │ make_completed_task          │ 构造 COMPLETED 状态任务                  │
# │ make_valid_audit_report      │ 构造 AuditReport + 默认 AuditFinding     │
# │ make_valid_knowledge_entry   │ 构造 KnowledgeEntry（KE-NNN）            │
# │ make_valid_failure_pattern   │ 构造 FailurePattern（F-NNN）             │
# │ make_valid_handoff_package   │ 构造 HandoffPackage 交接包               │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# migration.py — Schema 版本化迁移（Phase 5 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.utils.migration import migrate_task, downgrade_task, latest_schema_version
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ migrate_task(data, version)  │ BFS 最短路径版本化迁移                  │
# │ downgrade_task(data, ver)    │ 回滚到旧版本                            │
# │ latest_schema_version()      │ 返回当前最新版本号                       │
# │ MIGRATIONS                   │ 迁移注册表 dict                          │
# │ MigrationError               │ 迁移失败异常                            │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# deprecation.py — API 废弃策略（Phase 5 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.foundation.deprecation import deprecated, set_deprecation_mode, DeprecationMode
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ @deprecated(since=, ...)     │ 标记废弃函数/类/方法                    │
# │ set_deprecation_mode(mode)   │ warn/strict/silent 全局切换              │
# │ get_deprecation_mode()       │ 查询当前模式                            │
# │ DeprecationMode              │ 模式常量类：WARN/STRICT/SILENT          │
# │ DeprecatedAPIError           │ strict 模式下抛出的异常                 │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# events/dlq.py — 死信队列（Phase 6 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.events.dlq import DeadLetterQueue, attach_dlq_to_observer
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ DeadLetterQueue               │ 死信队列——SQLite持久化+定时重试         │
# │ DeadLetter                    │ 死信条目 dataclass                       │
# │ attach_dlq_to_observer        │ 挂载DLQ到Observer（零侵入monkey-patch）  │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# __version__.py — 版本协商（Phase 6 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.__version__ import __version__, check_shared_version
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ __version__                   │ PEP 440 版本字符串 "0.7.0"              │
# │ __version_info__              │ 版本元组 (0, 7, 0)                      │
# │ MIN_COMPATIBLE_SHARED_VERSION │ 最低兼容版本常量                        │
# │ check_shared_version(ver, ..) │ 运行时版本校验（strict抛异常/warn日志）  │
# │ VersionMismatchError          │ strict模式下版本不匹配异常              │
# └──────────────────────────────┴──────────────────────────────────────────┘

# ═══════════════════════════════════════════════════════════════════════════
# health.py — 聚合健康检查（Phase 6 新增）v0.1.0
# ═══════════════════════════════════════════════════════════════════════════
#
# from zephyr.shared.lifecycle.health import AggregateHealth, collect_health
#
# ┌──────────────────────────────┬──────────────────────────────────────────┐
# │ 符号                          │ 用途                                    │
# ├──────────────────────────────┼──────────────────────────────────────────┤
# │ AggregateHealth               │ 聚合健康检查器                          │
# │ HealthSummary                 │ 健康摘要 dataclass + to_dict()          │
# │ HealthStatus                  │ ALL_HEALTHY/DEGRADED/UNHEALTHY/UNKNOWN  │
# │ collect_health()              │ 一次性收集所有模块健康状态              │
# └──────────────────────────────┴──────────────────────────────────────────┘
