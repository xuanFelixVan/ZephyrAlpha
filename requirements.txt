# Dependencies — SSoT: pyproject.toml [project.dependencies]
# This file is a mirror of pyproject.toml; update pyproject.toml first, then sync here.
# 5.17.11 修复：统一 SSoT + 添加主版本上界（防止 breaking change）
#
# 2026-07-21 裁定 #ARCH-TORCH-CPU-ONLY:
# 项目代码 0 个 `import torch`,但 sentence-transformers 间接依赖。
# embedding_router.py 明确 device="cpu" —— GPU CUDA 完全浪费。
# 治本:torch 已替换为 CPU 版 (2.5.1+cu121 4.27GB → 2.13.0+cpu 0.48GB)。
# 重装命令: pip install torch --index-url https://download.pytorch.org/whl/cpu --no-deps
# 阿里云镜像备用: pip install torch -i https://mirrors.aliyun.com/pypi/simple/ --no-cache-dir
pydantic>=2.0.0,<3.0.0
pyyaml>=6.0,<7.0
pandas>=2.0.0,<3.0.0
psutil>=5.9.0,<7.0
chromadb>=0.4.24,<1.0.0
mcp>=1.0.0,<2.0.0
openai>=1.0.0,<2.0.0
sentence-transformers>=3.0.0,<4.0.0
structlog>=24.1.0,<25.0.0
pyarrow>=15.0.0,<26.0.0  # 上界与 SSoT pyproject.toml 对齐（<20→<26：datasets 4.x 要求 pyarrow>=21）
psycopg2-binary>=2.9.0,<3.0.0
# ClickHouse native protocol 驱动：核心基础设施 database_service(MOD-INF-002) lazy-import (#ARCH-REQ-CLASSIFY-001)
clickhouse-driver>=0.2.6,<1.0.0
# H1 Redis 业务热缓存驱动 (MOD-H1_REDIS_HOT, INFRA-DB-007)——SSoT pyproject.toml 核心依赖，本镜像曾遗漏
redis>=5.0.0,<6.0.0
plotly>=6.0.0,<7.0.0
streamlit>=1.50.0,<2.0.0
# v3.0.0 可视化技术栈切换 (#ARCH-047): Panel+HoloViz+plotly_resampler
panel>=1.5.0,<2.0.0
holoviews>=1.19.0,<2.0.0
datashader>=0.16.0,<1.0.0
hvplot>=0.10.0,<1.0.0
plotly_resampler>=0.9.0,<1.0.0
# 5.30.4 修复：python-dotenv 被 __init__.py 引用但未声明（幽灵依赖）
python-dotenv>=1.0.0,<2.0.0
# MOD-L00-004 数据源集成器：APScheduler 调度器 + SQLAlchemy JobStore
apscheduler>=3.10.0,<4.0.0
sqlalchemy>=2.0.0,<3.0.0
# MOD-L00-004 交易日历守卫：exchange_calendars XSHG 日历（节假日/调休精确判断）
exchange_calendars>=4.13,<5.0
