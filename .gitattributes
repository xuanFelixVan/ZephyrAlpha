# ZephyrAlpha .gitattributes
# Enforce UTF-8 encoding and LF line endings at the git level
# This prevents encoding corruption caused by core.autocrlf=true on Windows

# Default: auto-detect text files, normalize to LF in repo
* text=auto eol=lf

# Python source
*.py text eol=lf
*.pyi text eol=lf

# Documentation and configuration
*.md text eol=lf
*.rst text eol=lf
*.txt text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
*.json text eol=lf
*.toml text eol=lf
*.cfg text eol=lf
*.ini text eol=lf
*.csv text eol=lf

# Web and shell
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.ts text eol=lf
*.sh text eol=lf
*.bash text eol=lf

# Git and editor config
.gitattributes text eol=lf
.gitignore text eol=lf
.editorconfig text eol=lf

# Binary files - prevent any conversion
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.gz binary
*.tar binary
*.whl binary
*.egg binary
*.pyc binary
*.pyd binary
*.so binary
*.dll binary
*.exe binary

# Git LFS - Large model files (DM-100197) — REMOVED 2026-08-03 (ARCH-MODEL-LIFECYCLE-001 Phase 2)
# 治本：原 LFS 规则覆盖 data/models/local_model/** 下12种模型格式，
# 但 .gitignore 的 `data/models/` 规则已排除这些路径，LFS 规则永远无法触发（死代码）。
# 模型文件管理策略改为：永不入库（.gitignore 排除）+ download_models.py 脚本获取（Phase 3）。
# 历史 LFS 对象已通过 git filter-repo 从所有分支清除（Phase 1）。
