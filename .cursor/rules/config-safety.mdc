---
description: 风控配置锁定、编码安全、环境变量规范（编辑 config/** 时自动触发）
globs: config/**/*.yaml,config/**/*.yml,.env*
alwaysApply: false
---

# ZephyrAlpha 配置安全规则（Layer 1 — config 触发）

---

## 风控配置锁定（绝对规则）

`config/risk/` 目录下的任何文件（仓位限制、止损线、VaR 参数）是**最高风险配置**：

- 修改 `config/risk/` 时，commit message **必须包含** `risk-config-change:` 前缀
- 修改后必须在 session log 中记录**变更理由**
- 禁止 AI 在没有用户明确指令的情况下修改 `config/risk/` 下任何值
- 禁止通过脚本自动修改风控参数（必须手动确认）

示例 commit：
```
risk-config-change: 调整最大单仓位从 5% 到 3% | 原因: 降低波动期集中风险
```

---

## 单一配置源原则

- 同一功能的配置只能存在**一份**
- 发现重复配置时：评估后保留更完善的版本，在**同一 commit** 中删除另一个
- 禁止创建 `config-v2.yaml`、`config-backup.yaml` 等版本化配置文件

---

## 编码安全（config 文件专属）

- 所有 `.yaml`/`.yml` 文件必须使用 **UTF-8 编码（不加 BOM）**
- 禁止使用 Windows 默认编码（GBK/UTF-16）保存配置文件
- 使用 Python 读写配置时：必须显式指定 `encoding='utf-8'`
- 使用 PowerShell 读写配置时：必须指定 `-Encoding UTF8`

---

## 环境变量规范

- 所有密钥和凭证必须通过 `.env` 文件或环境变量注入，**禁止硬编码**
- `.env` 文件不得提交到版本控制（确认在 `.gitignore` 中）
- 配置文件中的占位符格式：`${ENV_VAR_NAME}`
