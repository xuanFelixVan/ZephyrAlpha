# ZephyrAlpha 代码施工规范

## 施工前必读（每次开始代码施工前执行）

1. 读 `docs/04_CONSTRUCTION/PLANS/MASTER_DEVELOPMENT_PLAN.md` — 确认当前 Phase 和任务
2. 读对应的施工图文档（`docs/04_CONSTRUCTION/PLANS/CONSTRUCTION_PLAN_*.md`）
3. 确认目标模块的蓝图（`docs/01_FRAMEWORK/` 中的对应蓝图文件，按 module_id 或文件名查找）

## 施工后必做（每次完成一个模块后执行）

按 Change Propagation Map 同步更新：

```
新增 src/ 代码模块 → docs/02_ARCHITECTURE/MODULE_INVENTORY.md, 对应层 INDEX, MASTER_PLAN 打勾
修改 pyproject.toml → docs/02_ARCHITECTURE/DEV_ENV_SETUP.md
```

详见 `docs/01_GOVERNANCE/PLAYBOOKS/INDEX.md` 中的 construction-change-impact-playbook

## AI 生成代码规范

### 标记要求
AI 生成的代码块在第一行注释中标记（便于 Owner 复核）：
```python
# AI-generated: [简短描述生成目的]
```

### 生成质量要求
- 所有公共函数**必须有 type hints**（mypy 可通过）
- 所有公共函数**必须有 docstring**（Google 风格）
- 使用 `structlog` 记录关键步骤，包含 `layer`/`module`/`operation` 上下文
- 禁止硬编码路径、密钥、主机名

### 错误处理
- 使用 `ZephyrBaseError` 异常层次（见 `docs/02_ARCHITECTURE/TECH_DECISION_RECORDS.md` ADR-004）
- 禁止空的 `except:` 块
- 外部 API 调用必须有 retry 逻辑（使用 `tenacity`）

## Commit Message 格式

```
<type>(<scope>): <description>

[可选 body]
[可选 footer]
```

**type 限定**：
| type | 用途 |
|---|---|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（不影响功能） |
| `docs` | 文档变更 |
| `test` | 测试相关 |
| `chore` | 构建/工具/依赖 |
| `governance` | 治理规则/文档治理 |

**scope 示例**：`data`, `features`, `signals`, `risk`, `execution`, `docs`

**示例**：
```
feat(data): add AKShare A-share daily OHLCV adapter

- Implements retry logic with tenacity (3 attempts, exponential backoff)
- Validates output against OHLCVBar Pydantic model
- Adds unit tests with mocked HTTP responses
```

## Session Artifact 清理规则

每次会话结束前：
- [ ] 删除本次会话创建的临时脚本（一次性任务已完成）
- [ ] 删除 `docs/09_AUDIT/STATE/` 中本次产生的非永久报告（如仅用于调试）
- [ ] 确认 `scripts/` 根目录无散落脚本

## 测试要求

- 新增业务代码必须有对应测试（`src/tests/unit/{layer}/test_{module}.py`）
- 测试覆盖率目标：≥ 80%（`pytest --cov=src/zephyr --cov-fail-under=80`）
- 跨层集成测试放在 `src/tests/integration/`

## 禁止行为

- 禁止在会话中使用 `git add .` 或 `git add -A`（必须精确指定路径）
- 禁止跳过 pre-commit hooks（`--no-verify`）
- 禁止在 `src/` 外创建业务 `.py` 文件
- 禁止硬编码任何密钥或凭证
- 禁止创建 `scripts/archive/` 目录（已废止，禁止向此目录写入新脚本）
