---
description: 双编辑器编码损坏的根因分析、检测和修复流程（Cursor+Trae 交替使用时按需读取）
alwaysApply: false
---

# ZephyrAlpha 编码安全规则（Layer 2 — 按需触发）

> 触发场景：发现乱码 / 切换 Cursor 和 Trae 之前 / 编码损坏修复时

---

## 根因分析

**Cursor + Trae 双编辑器交替使用**曾导致多个治理核心文件出现阿拉伯文乱码：

- Windows 下 PowerShell `Out-File` 默认输出 UTF-16 LE
- Trae 若 `files.autoGuessEncoding: true`，会将 UTF-8 文件误识别为 GBK 并以 GBK 保存
- 某些 PowerShell 脚本 `echo` 重定向默认 GBK
- 混合编码的文件在另一个编辑器中打开时表现为乱码

---

## 切换编辑器前置检查

**从 Cursor 切换到 Trae（或反向）之前必须完成**：

1. 确认当前编辑器所有文件已保存并关闭
2. 运行编码检查：
   ```bash
   python scripts/hooks/doc_guard_pre_commit.py --scan-encoding
   ```
3. 确认 Trae 设置：`files.autoGuessEncoding` = `false`
4. 确认 Trae 设置：`files.encoding` = `utf8`

---

## 编码损坏检测

```python
# 快速检测文件编码
with open('suspect_file.md', 'rb') as f:
    raw = f.read(1000)
    print(raw[:100])  # 看是否有 0x00 字节（UTF-16）或 GBK 字节序列
```

或使用治理钩子：
```bash
python scripts/hooks/doc_guard_pre_commit.py --scan-encoding docs/
```

---

## 修复流程

1. **隔离**：git stash 或标记损坏文件为 BLOCKER，不要继续编辑
2. **识别**：使用 `chardet` 或 `file` 命令确认实际编码
3. **转换**：
   ```python
   with open('damaged.md', 'r', encoding='gbk') as f:
       content = f.read()
   with open('damaged.md', 'w', encoding='utf-8') as f:
       f.write(content)
   ```
4. **验证**：重新运行编码扫描确认修复
5. **提交**：使用 `chore: fix encoding corruption in xxx.md` 提交

---

## 禁止行为

- **禁止**使用 `echo` 重定向创建文件（`echo "text" > file.md` → GBK）
- **禁止**使用 PowerShell `Out-File` 默认参数（默认 UTF-16 LE 或 GBK）
- **禁止**在两个编辑器中同时打开同一文件进行编辑
- **禁止**在发现乱码后继续编辑该文件（先修复，再编辑）

---

## 安全写入模板

**PowerShell 安全写入**：
```powershell
# 正确：显式指定 UTF-8
$content | Out-File -FilePath "file.md" -Encoding UTF8
# 或：
[System.IO.File]::WriteAllText("file.md", $content, [System.Text.Encoding]::UTF8)
```

**Python 安全写入**：
```python
with open('file.md', 'w', encoding='utf-8', newline='\n') as f:
    f.write(content)
```
