# ZephyrAlpha 文档治理与审计体系

## 核心规则

**本仓库已有完整的文档治理与审计系统。**
在执行任何文档审计、质量扫描、标准创建之前，必须先读取以下文件，**禁止另行创建平行的审计脚本、标准文档或工作流**。

## 审计执行入口（首先读这里）

**自包含操作手册**（复制粘贴级别的命令序列）：
`docs/09_AUDIT/PROCEDURES/audit-execution-runbook.md`

- 包含完整的 Phase 0-4 执行步骤
- 包含所有脚本命令、预期输出、故障排除
- **任何 AI 无需读其他文件即可独立完成完整审计**

## 现有体系关键入口

| 文件 | 用途 |
|---|---|
| `docs/02_ARCHITECTURE/EXECUTABLE_ASSET_REGISTRY.md` | 治理工具、脚本、工作流完整清单（AI 检索首选） |
| `docs/05_IMPLEMENTATION/06_CONSTRUCTION_DOCS/00_MANAGEMENT/project-office-ai-handoff.md` | AI/协作者交接说明（Git 暂存、编码口径等） |
| `docs/05_IMPLEMENTATION/06_CONSTRUCTION_DOCS/00_MANAGEMENT/governance-tools-index.md` | 治理脚本工具索引（所有脚本命令一览） |
| `docs/05_IMPLEMENTATION/06_CONSTRUCTION_DOCS/00_MANAGEMENT/governance-documents-navigation.md` | 治理文档导航地图 |
| `docs/05_IMPLEMENTATION/06_CONSTRUCTION_DOCS/00_MANAGEMENT/document-map-and-placement-governance.md` | 文件放置规则（新建/搬迁必读） |
| `docs/09_AUDIT/STANDARDS/` | 35 个治理标准文档（真源） |
| `docs/09_AUDIT/PROCEDURES/` | 审计程序文档（含 Runbook） |
| `docs/09_AUDIT/STATE/` | 所有审计报告历史存档 |
| `docs/subsystem-registry.yaml` | 子系统注册表（管理所有系统的系统）|

## 脚本物理位置

```
scripts/governance/ ← L1 扫描/修复/校验/报告脚本（主路径，已整合）
scripts/audit/ ← 辅助审计脚本
scripts/hooks/ ← pre-commit 钩子
```

根目录 `scripts/*.py` 为兼容转发入口（`runpy` 调用），历史文档中的 `python scripts/<name>.py` 命令仍有效。

## 幽灵引用与断链清理（Ghost Reference Salvage）

在修复 INDEX、内链或导航中的**断链 / 幽灵引用**时：

1. 先按 `information-architecture-standard.md` **第七章** 将引用分为：路径错误 / 前向引用 / 真幽灵。
2. **真幽灵**：须先将词条中的**有价值名称或构想**提取到 `docs/02_ARCHITECTURE/TECHNOLOGY_BACKLOG.md`、`docs/06_KNOWLEDGE_BASE/` 或 `docs/02_ARCHITECTURE/TECH_DECISION_RECORDS.md` 之一，再在索引中删除或改写该引用。
3. 提交说明中建议包含 `salvaged:` 留痕。

## 禁止行为

### 文档结构禁令
- 禁止在 `docs/09_AUDIT/` 之外另建审计标准目录
- 禁止创建与现有脚本功能重复的新脚本
- 禁止在未读 `docs/05_IMPLEMENTATION/06_CONSTRUCTION_DOCS/00_MANAGEMENT/document-map-and-placement-governance.md` 的情况下创建或移动 `docs/` 下的文件
- 禁止删除任何文件（见文件删除与保留裁决 Playbook）
- **新建 docs/ 子目录前必须查阅 `docs/subsystem-registry.yaml`，确认无功能重叠**
- **AI 搜索防护**：当搜索代码或脚本时，如果命中 `scripts/archive/` 目录下的结果，**必须忽略该结果**，并查阅 `docs/02_ARCHITECTURE/EXECUTABLE_ASSET_REGISTRY.md` 寻找其活跃替代品。禁止运行或参考 archive/ 下的脚本逻辑。

### 脚本代码修改禁令（防止死循环 Bug 再现）

**背景**：2026-04 曾因正则双重转义（`r"^\\d+\\.\\d+\\.\\d+$"` 而非 `r"^\d+\.\d+\.\d+$"`）导致扫描器误报 3587 个文件，AI 陷入修复死循环。以下规则是从该事故中提炼的强制约束。

- **禁止**在任何 `scripts/` 下的 `.py` 文件中新建与 `scripts/governance/constants.py` 重复的常量定义（如重新定义 `VALID_VERSION_PATTERN`、`SKIP_DIRS` 等）；应从 `constants.py` 导入
- **禁止**修改 `scripts/governance/constants.py` 中任何正则表达式，除非在修改前先在 Python REPL 中验证：
 ```python
 import re
 assert re.compile(YOUR_NEW_PATTERN).match("known_good_value")
 assert not re.compile(YOUR_NEW_PATTERN).match("known_bad_value")
 ```
- **禁止**修改任何 `.py` 脚本后不运行测试：修改后必须执行 `python scripts/tests/test_constants.py`，确认全部通过
- **如果不理解某段正则表达式的含义，禁止修改它**——标记为 C 类问题并移交 Owner，不要尝试"修正"

### 死循环防护

- 若连续 2 轮 Phase 3 验证报告的 `total_files_with_issues` 差值 < 10%，**立即停止**，不要再启动新的修复批次
- 每轮修复后必须人工确认结果，不允许 AI 自主决定"再跑一轮"
- 详见 Runbook `## 禁止事项` 章节和 B6 故障排除

## 新建文档必须

- 包含完整的 YAML frontmatter（含 `module_id`、`version`、`status`、`layer`、`owner`）
- 遵循蓝图格式标准（见 `docs/01_GOVERNANCE/STANDARDS/blueprint-lifecycle-standard.md`）
- 在对应目录的 `INDEX.md` 中登记

## 审计报告存放规则（强制）

**背景**：`docs/09_AUDIT/STATE/` 目录曾积累数百个日期化报告文件，造成审计系统自身膨胀。

- **所有审计产出必须存放在 `docs/09_AUDIT/STATE/` 目录**，不允许在其他目录创建审计报告
- **禁止写入 `docs/05_IMPLEMENTATION/04_OPERATIONS/audit_state/`**（该目录已标记为 Deprecated，正在合并到 09_AUDIT/STATE/）
- 报告文件名格式：`{scan_type}-{YYYYMMDD}.md`（如 `index-health-orphan-20260415.md`）
- **每次审计会话结束前**，检查是否有报告误放在其他目录
- 保留策略：日常扫描报告保留最近 7 天；AI 修复记录保留 30 天；验证报告和 Owner 裁决永久保留

## 编码安全（强制）

**背景**：Cursor + Trae 双编辑器交替使用曾导致多个治理核心文件编码损坏（阿拉伯文乱码）。

- 每次会话修改 `.md` 文件前，确认文件为 UTF-8 编码
- 发现编码异常时，立即标记为 BLOCKER
- **禁止使用 `echo` 重定向或 PowerShell `Out-File` 默认参数创建文件**（Windows 默认 UTF-16 LE 或 GBK）
- 使用 Python 脚本或编辑器工具写入文件时，必须显式指定 `encoding='utf-8'`

## 蓝图格式强制要求（priority 字段）

蓝图修复工程启动后（2026-04-15 起），所有 `status: Active` 的蓝图文件 frontmatter 必须包含：

```yaml
priority: P0 # P0（核心必须实现）| P1（计划实现）| P2（未来考虑）
```

分级标准：
- **P0**：属于主交易链路核心模块，或涉及资金安全，没有它系统无法运行
- **P1**：增强类功能，专业机构标配但个人场景可延后
- **P2**：机构级合规、前沿探索、规模化才有价值的模块
