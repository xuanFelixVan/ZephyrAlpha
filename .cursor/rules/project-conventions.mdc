---
description: Cursor 专用规则（方案输出规范、交接协议、双编辑器防护）— 补充 core-governance.mdc
alwaysApply: true
---

# ZephyrAlpha 项目约定（Layer 0 补充）

> 注意：核心治理规则已移至 `core-governance.mdc`。本文件仅保留 Cursor 特有的规范。

---

> 会话启动协议见 `core-governance.mdc` 第六节。
> 路径锁定表、File Movement Protocol、Change Propagation Map 见 `doc-governance.mdc`（编辑 docs/*.md 时自动触发）。

## 方案输出规范（强制）

**每次输出多步执行方案时，必须为每个步骤标注以下信息**：

| 字段 | 说明 | 示例 |
|---|---|---|
| 建议模型 | 具体型号 | Claude Sonnet 4 / Gemini 2.5 Flash / Kimi / Composer 2 |
| 平台 | 在哪执行 | Cursor / Trae |
| 成本等级 | T0–T3（见下表） | T1 |
| Thinking | 是否开启推理 | ON / OFF |
| MAX Mode | 是否需要扩展上下文 | ON / OFF |
| 合并会话 | 是否与前一步合并到同一会话 | 是 / 否 |

**执行反馈阶段不需要重复标注**（只在规划/方案阶段标注）。

### 固定模型搭子（默认全在 Cursor）

| 档位 | 模型 | 平台 | 成本 | 典型任务 |
|:---:|---|---|:---:|---|
| T0 | Composer 2 | Cursor | 最低 | 改几行、git mv、跑脚本、简单 INDEX 补行 |
| T1 | Gemini 2.5 Flash | Cursor | 低 | 批量结构化修复、路径替换、frontmatter 批修、照图写测试/Schema |
| T2 | Kimi | Cursor（或 Trae 免费） | 低 | 中文大文档阅读、100+ 文件价值扫描、跨文件对比 |
| T3 | Claude Sonnet 4 | Cursor | 高 | ISO 级标准文档、施工图设计、核心模块首次实现、架构裁决、Gap Analysis |

### 成本优化四步（规划任意多步任务时用）

1. **任务分级**：每步需要 T0/T1/T2/T3 哪一档？
2. **模型匹配**：用能完成该步的**最便宜**档位；禁止用 T3 做 T0。
3. **平台归集**：同平台任务打成一个批次；**依赖关系优先于固定顺序**。
4. **会话合并**：同档位、同平台、无依赖冲突的相邻步骤合并为一轮会话。

## 双编辑器防护规则（快速参考）

- **切换编辑器前**：确保所有文件已保存并关闭
- **切换编辑器后第一步**：运行 `python scripts/hooks/doc_guard_pre_commit.py --scan-encoding`
- **详细流程**：见 `encoding-safety.mdc`（按需触发）

## 任务交接协议（Handoff Protocol）

当需要切换模型或切换编辑器执行下一步时，必须输出标准化交接指令，格式如下：

```
【ZephyrAlpha 任务交接指令】
执行模型：[模型名称] | Thinking: [ON/OFF] | MAX Mode: [ON/OFF]

## 当前状态
- 项目阶段：Phase X（[阶段名称]）
- 刚完成：[上一步完成的任务简述]
- 待执行：[下一步任务标题]

## 必读文件（按顺序）
1. [文件路径] — [读取目的]
2. [文件路径] — [读取目的]

## 执行任务
[具体任务描述，包括：操作文件路径、预期输出、验收标准]

## 完成后输出
[完成后需要报告的内容格式]

## 禁止事项
- [具体禁止行为]
```

---

> Write Gate 详细规则见 `doc-governance.mdc`（编辑 docs/*.md 时自动触发）。

---

## Git 操作规范

- 不使用 `git add -A` 或 `git add .`，始终精确指定路径
- commit 前先 `git status` 确认暂存文件
- 不 push 到远程，除非用户明确要求
- 不使用 `--force`，不跳过 hooks（`--no-verify`）

## 文件操作规范

- 编辑前必须先 Read 文件
- YAML frontmatter 使用单引号包裹含特殊字符的字符串
- Markdown 文件使用 UTF-8 编码（不加 BOM）
- 不创建 README.md 或文档文件，除非用户明确要求

> 仓库目录约定和废弃路径列表见 `core-governance.mdc` 第八节（常驻加载）或 `doc-governance.mdc` 最后一节。
